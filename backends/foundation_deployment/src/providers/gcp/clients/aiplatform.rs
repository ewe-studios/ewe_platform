//! Auto-generated API clients for gcp/aiplatform.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_batch_prediction_jobs_create_execute()` to send, or `aiplatform_batch_prediction_jobs_create` for simplest API.

pub fn aiplatform_batch_prediction_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: Option<&str>,
    body: &GoogleCloudAiplatformV1BatchPredictionJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/batchPredictionJobs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_batch_prediction_jobs_create_execute()` or `aiplatform_batch_prediction_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_batch_prediction_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_batch_prediction_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchPredictionJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_batch_prediction_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_batch_prediction_jobs_create_task()`.
/// For the simplest API, use `aiplatform_batch_prediction_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_batch_prediction_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_batch_prediction_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_batch_prediction_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_batch_prediction_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformBatchPredictionJobsCreateArgs {
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchPredictionJob,
}

/// GET v1/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_batch_prediction_jobs_create_builder()` + `aiplatform_batch_prediction_jobs_create_execute()`.
/// For task-level control, use `aiplatform_batch_prediction_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_batch_prediction_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformBatchPredictionJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_batch_prediction_jobs_create_builder(
        client,
        args.parent.as_deref(),
        &args.body,
    )?;
    aiplatform_batch_prediction_jobs_create_execute(builder)
}

/// GET v1/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_batch_prediction_jobs_get_execute()` to send, or `aiplatform_batch_prediction_jobs_get` for simplest API.

pub fn aiplatform_batch_prediction_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/batchPredictionJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_batch_prediction_jobs_get_execute()` or `aiplatform_batch_prediction_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_batch_prediction_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_batch_prediction_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchPredictionJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_batch_prediction_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_batch_prediction_jobs_get_task()`.
/// For the simplest API, use `aiplatform_batch_prediction_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_batch_prediction_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_batch_prediction_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_batch_prediction_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_batch_prediction_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformBatchPredictionJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_batch_prediction_jobs_get_builder()` + `aiplatform_batch_prediction_jobs_get_execute()`.
/// For task-level control, use `aiplatform_batch_prediction_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_batch_prediction_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformBatchPredictionJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_batch_prediction_jobs_get_builder(client, &args.name)?;
    aiplatform_batch_prediction_jobs_get_execute(builder)
}

/// GET v1/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_batch_prediction_jobs_list_execute()` to send, or `aiplatform_batch_prediction_jobs_list` for simplest API.

pub fn aiplatform_batch_prediction_jobs_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/batchPredictionJobs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_batch_prediction_jobs_list_execute()` or `aiplatform_batch_prediction_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_batch_prediction_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_batch_prediction_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListBatchPredictionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListBatchPredictionJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_batch_prediction_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_batch_prediction_jobs_list_task()`.
/// For the simplest API, use `aiplatform_batch_prediction_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_batch_prediction_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_batch_prediction_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListBatchPredictionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_batch_prediction_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_batch_prediction_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformBatchPredictionJobsListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_batch_prediction_jobs_list_builder()` + `aiplatform_batch_prediction_jobs_list_execute()`.
/// For task-level control, use `aiplatform_batch_prediction_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_batch_prediction_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformBatchPredictionJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListBatchPredictionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_batch_prediction_jobs_list_builder(
        client,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_batch_prediction_jobs_list_execute(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_custom_jobs_operations_cancel_execute()` to send, or `aiplatform_custom_jobs_operations_cancel` for simplest API.

pub fn aiplatform_custom_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/customJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_custom_jobs_operations_cancel_execute()` or `aiplatform_custom_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_custom_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_custom_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_custom_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_custom_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_custom_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_custom_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformCustomJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_custom_jobs_operations_cancel_builder()` + `aiplatform_custom_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_custom_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformCustomJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_custom_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_custom_jobs_operations_cancel_execute(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_custom_jobs_operations_delete_execute()` to send, or `aiplatform_custom_jobs_operations_delete` for simplest API.

pub fn aiplatform_custom_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/customJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_custom_jobs_operations_delete_execute()` or `aiplatform_custom_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_custom_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_custom_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_custom_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_custom_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_custom_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_custom_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformCustomJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_custom_jobs_operations_delete_builder()` + `aiplatform_custom_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_custom_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformCustomJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_custom_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_custom_jobs_operations_delete_execute(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_custom_jobs_operations_get_execute()` to send, or `aiplatform_custom_jobs_operations_get` for simplest API.

pub fn aiplatform_custom_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/customJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_custom_jobs_operations_get_execute()` or `aiplatform_custom_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_custom_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_custom_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_custom_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_custom_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_custom_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_custom_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformCustomJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_custom_jobs_operations_get_builder()` + `aiplatform_custom_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_custom_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformCustomJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_custom_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_custom_jobs_operations_get_execute(builder)
}

/// GET v1/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_custom_jobs_operations_list_execute()` to send, or `aiplatform_custom_jobs_operations_list` for simplest API.

pub fn aiplatform_custom_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/customJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_custom_jobs_operations_list_execute()` or `aiplatform_custom_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_custom_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_custom_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_custom_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_custom_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_custom_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_custom_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformCustomJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_custom_jobs_operations_list_builder()` + `aiplatform_custom_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_custom_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformCustomJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_custom_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_custom_jobs_operations_list_execute(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_custom_jobs_operations_wait_execute()` to send, or `aiplatform_custom_jobs_operations_wait` for simplest API.

pub fn aiplatform_custom_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/customJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_custom_jobs_operations_wait_execute()` or `aiplatform_custom_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_custom_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_custom_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_custom_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_custom_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_custom_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_custom_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_custom_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformCustomJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_custom_jobs_operations_wait_builder()` + `aiplatform_custom_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_custom_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_custom_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformCustomJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_custom_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_custom_jobs_operations_wait_execute(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_data_labeling_jobs_operations_cancel_execute()` to send, or `aiplatform_data_labeling_jobs_operations_cancel` for simplest API.

pub fn aiplatform_data_labeling_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/dataLabelingJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_data_labeling_jobs_operations_cancel_execute()` or `aiplatform_data_labeling_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_data_labeling_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_data_labeling_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_data_labeling_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_data_labeling_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_data_labeling_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDataLabelingJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_data_labeling_jobs_operations_cancel_builder()` + `aiplatform_data_labeling_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDataLabelingJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_data_labeling_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_data_labeling_jobs_operations_cancel_execute(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_data_labeling_jobs_operations_delete_execute()` to send, or `aiplatform_data_labeling_jobs_operations_delete` for simplest API.

pub fn aiplatform_data_labeling_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/dataLabelingJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_data_labeling_jobs_operations_delete_execute()` or `aiplatform_data_labeling_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_data_labeling_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_data_labeling_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_data_labeling_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_data_labeling_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_data_labeling_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDataLabelingJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_data_labeling_jobs_operations_delete_builder()` + `aiplatform_data_labeling_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDataLabelingJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_data_labeling_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_data_labeling_jobs_operations_delete_execute(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_data_labeling_jobs_operations_get_execute()` to send, or `aiplatform_data_labeling_jobs_operations_get` for simplest API.

pub fn aiplatform_data_labeling_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/dataLabelingJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_data_labeling_jobs_operations_get_execute()` or `aiplatform_data_labeling_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_data_labeling_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_data_labeling_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_data_labeling_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_data_labeling_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_data_labeling_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDataLabelingJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_data_labeling_jobs_operations_get_builder()` + `aiplatform_data_labeling_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDataLabelingJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_data_labeling_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_data_labeling_jobs_operations_get_execute(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_data_labeling_jobs_operations_list_execute()` to send, or `aiplatform_data_labeling_jobs_operations_list` for simplest API.

pub fn aiplatform_data_labeling_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/dataLabelingJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_data_labeling_jobs_operations_list_execute()` or `aiplatform_data_labeling_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_data_labeling_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_data_labeling_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_data_labeling_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_data_labeling_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_data_labeling_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDataLabelingJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_data_labeling_jobs_operations_list_builder()` + `aiplatform_data_labeling_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDataLabelingJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_data_labeling_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_data_labeling_jobs_operations_list_execute(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_data_labeling_jobs_operations_wait_execute()` to send, or `aiplatform_data_labeling_jobs_operations_wait` for simplest API.

pub fn aiplatform_data_labeling_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/dataLabelingJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_data_labeling_jobs_operations_wait_execute()` or `aiplatform_data_labeling_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_data_labeling_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_data_labeling_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_data_labeling_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_data_labeling_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_data_labeling_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_data_labeling_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDataLabelingJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_data_labeling_jobs_operations_wait_builder()` + `aiplatform_data_labeling_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_data_labeling_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_data_labeling_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDataLabelingJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_data_labeling_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_data_labeling_jobs_operations_wait_execute(builder)
}

/// GET v1/datasets
/// Creates a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_create_execute()` to send, or `aiplatform_datasets_create` for simplest API.

pub fn aiplatform_datasets_create_builder(
    client: &SimpleHttpClient,
    parent: Option<&str>,
    body: &GoogleCloudAiplatformV1Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/datasets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/datasets
/// Creates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_create_execute()` or `aiplatform_datasets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets
/// Creates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_create_task()`.
/// For the simplest API, use `aiplatform_datasets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsCreateArgs {
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Dataset,
}

/// GET v1/datasets
/// Creates a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_create_builder()` + `aiplatform_datasets_create_execute()`.
/// For task-level control, use `aiplatform_datasets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_create(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_create_builder(client, args.parent.as_deref(), &args.body)?;
    aiplatform_datasets_create_execute(builder)
}

/// GET v1/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_delete_execute()` to send, or `aiplatform_datasets_delete` for simplest API.

pub fn aiplatform_datasets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/datasets/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_delete_execute()` or `aiplatform_datasets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_delete_builder()` + `aiplatform_datasets_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_delete_builder(client, &args.name)?;
    aiplatform_datasets_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_get_execute()` to send, or `aiplatform_datasets_get` for simplest API.

pub fn aiplatform_datasets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/datasets/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_get_execute()` or `aiplatform_datasets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_get_task()`.
/// For the simplest API, use `aiplatform_datasets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_get_builder()` + `aiplatform_datasets_get_execute()`.
/// For task-level control, use `aiplatform_datasets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_get_builder(client, &args.name, args.readMask.as_deref())?;
    aiplatform_datasets_get_execute(builder)
}

/// GET v1/datasets
/// Lists Datasets in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_list_execute()` to send, or `aiplatform_datasets_list` for simplest API.

pub fn aiplatform_datasets_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/datasets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets
/// Lists Datasets in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_list_execute()` or `aiplatform_datasets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDatasetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets
/// Lists Datasets in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_list_task()`.
/// For the simplest API, use `aiplatform_datasets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/datasets
/// Lists Datasets in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_list_builder()` + `aiplatform_datasets_list_execute()`.
/// For task-level control, use `aiplatform_datasets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_list_builder(
        client,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_datasets_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_patch_execute()` to send, or `aiplatform_datasets_patch` for simplest API.

pub fn aiplatform_datasets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/datasets/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_patch_execute()` or `aiplatform_datasets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_patch_task()`.
/// For the simplest API, use `aiplatform_datasets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Dataset,
}

/// GET v1/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_patch_builder()` + `aiplatform_datasets_patch_execute()`.
/// For task-level control, use `aiplatform_datasets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_patch(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_datasets_patch_execute(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_annotation_specs_operations_cancel_execute()` to send, or `aiplatform_datasets_annotation_specs_operations_cancel` for simplest API.

pub fn aiplatform_datasets_annotation_specs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/annotationSpecs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_annotation_specs_operations_cancel_execute()` or `aiplatform_datasets_annotation_specs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_annotation_specs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_datasets_annotation_specs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_annotation_specs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_annotation_specs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_annotation_specs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsAnnotationSpecsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_annotation_specs_operations_cancel_builder()` + `aiplatform_datasets_annotation_specs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsAnnotationSpecsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_annotation_specs_operations_cancel_builder(client, &args.name)?;
    aiplatform_datasets_annotation_specs_operations_cancel_execute(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_annotation_specs_operations_delete_execute()` to send, or `aiplatform_datasets_annotation_specs_operations_delete` for simplest API.

pub fn aiplatform_datasets_annotation_specs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/annotationSpecs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_annotation_specs_operations_delete_execute()` or `aiplatform_datasets_annotation_specs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_annotation_specs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_annotation_specs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_annotation_specs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_annotation_specs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_annotation_specs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsAnnotationSpecsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_annotation_specs_operations_delete_builder()` + `aiplatform_datasets_annotation_specs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsAnnotationSpecsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_annotation_specs_operations_delete_builder(client, &args.name)?;
    aiplatform_datasets_annotation_specs_operations_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_annotation_specs_operations_get_execute()` to send, or `aiplatform_datasets_annotation_specs_operations_get` for simplest API.

pub fn aiplatform_datasets_annotation_specs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/annotationSpecs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_annotation_specs_operations_get_execute()` or `aiplatform_datasets_annotation_specs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_annotation_specs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_get_task()`.
/// For the simplest API, use `aiplatform_datasets_annotation_specs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_annotation_specs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_annotation_specs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_annotation_specs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsAnnotationSpecsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_annotation_specs_operations_get_builder()` + `aiplatform_datasets_annotation_specs_operations_get_execute()`.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsAnnotationSpecsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_annotation_specs_operations_get_builder(client, &args.name)?;
    aiplatform_datasets_annotation_specs_operations_get_execute(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_annotation_specs_operations_list_execute()` to send, or `aiplatform_datasets_annotation_specs_operations_list` for simplest API.

pub fn aiplatform_datasets_annotation_specs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/annotationSpecs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_annotation_specs_operations_list_execute()` or `aiplatform_datasets_annotation_specs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_annotation_specs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_list_task()`.
/// For the simplest API, use `aiplatform_datasets_annotation_specs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_annotation_specs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_annotation_specs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_annotation_specs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsAnnotationSpecsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_annotation_specs_operations_list_builder()` + `aiplatform_datasets_annotation_specs_operations_list_execute()`.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsAnnotationSpecsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_annotation_specs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_datasets_annotation_specs_operations_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_annotation_specs_operations_wait_execute()` to send, or `aiplatform_datasets_annotation_specs_operations_wait` for simplest API.

pub fn aiplatform_datasets_annotation_specs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/annotationSpecs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_annotation_specs_operations_wait_execute()` or `aiplatform_datasets_annotation_specs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_annotation_specs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_datasets_annotation_specs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_annotation_specs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_annotation_specs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_annotation_specs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_annotation_specs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsAnnotationSpecsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_annotation_specs_operations_wait_builder()` + `aiplatform_datasets_annotation_specs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_datasets_annotation_specs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_annotation_specs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsAnnotationSpecsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_annotation_specs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_datasets_annotation_specs_operations_wait_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_annotations_operations_cancel_execute()` to send, or `aiplatform_datasets_data_items_annotations_operations_cancel` for simplest API.

pub fn aiplatform_datasets_data_items_annotations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/annotations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_annotations_operations_cancel_execute()` or `aiplatform_datasets_data_items_annotations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_annotations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_annotations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_annotations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_annotations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_annotations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsAnnotationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_annotations_operations_cancel_builder()` + `aiplatform_datasets_data_items_annotations_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsAnnotationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_data_items_annotations_operations_cancel_builder(client, &args.name)?;
    aiplatform_datasets_data_items_annotations_operations_cancel_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_annotations_operations_delete_execute()` to send, or `aiplatform_datasets_data_items_annotations_operations_delete` for simplest API.

pub fn aiplatform_datasets_data_items_annotations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/annotations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_annotations_operations_delete_execute()` or `aiplatform_datasets_data_items_annotations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_annotations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_annotations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_annotations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_annotations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_annotations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsAnnotationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_annotations_operations_delete_builder()` + `aiplatform_datasets_data_items_annotations_operations_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsAnnotationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_data_items_annotations_operations_delete_builder(client, &args.name)?;
    aiplatform_datasets_data_items_annotations_operations_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_annotations_operations_get_execute()` to send, or `aiplatform_datasets_data_items_annotations_operations_get` for simplest API.

pub fn aiplatform_datasets_data_items_annotations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/annotations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_annotations_operations_get_execute()` or `aiplatform_datasets_data_items_annotations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_annotations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_get_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_annotations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_annotations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_annotations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_annotations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsAnnotationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_annotations_operations_get_builder()` + `aiplatform_datasets_data_items_annotations_operations_get_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsAnnotationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_data_items_annotations_operations_get_builder(client, &args.name)?;
    aiplatform_datasets_data_items_annotations_operations_get_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_annotations_operations_list_execute()` to send, or `aiplatform_datasets_data_items_annotations_operations_list` for simplest API.

pub fn aiplatform_datasets_data_items_annotations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/annotations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_annotations_operations_list_execute()` or `aiplatform_datasets_data_items_annotations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_annotations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_list_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_annotations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_annotations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_annotations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_annotations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsAnnotationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_annotations_operations_list_builder()` + `aiplatform_datasets_data_items_annotations_operations_list_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsAnnotationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_annotations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_datasets_data_items_annotations_operations_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_annotations_operations_wait_execute()` to send, or `aiplatform_datasets_data_items_annotations_operations_wait` for simplest API.

pub fn aiplatform_datasets_data_items_annotations_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/annotations/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_annotations_operations_wait_execute()` or `aiplatform_datasets_data_items_annotations_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_annotations_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_wait_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_annotations_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_annotations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_annotations_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_annotations_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_annotations_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsAnnotationsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_annotations_operations_wait_builder()` + `aiplatform_datasets_data_items_annotations_operations_wait_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_annotations_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_annotations_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsAnnotationsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_annotations_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_datasets_data_items_annotations_operations_wait_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_operations_cancel_execute()` to send, or `aiplatform_datasets_data_items_operations_cancel` for simplest API.

pub fn aiplatform_datasets_data_items_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_operations_cancel_execute()` or `aiplatform_datasets_data_items_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_operations_cancel_builder()` + `aiplatform_datasets_data_items_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_operations_cancel_builder(client, &args.name)?;
    aiplatform_datasets_data_items_operations_cancel_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_operations_delete_execute()` to send, or `aiplatform_datasets_data_items_operations_delete` for simplest API.

pub fn aiplatform_datasets_data_items_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_operations_delete_execute()` or `aiplatform_datasets_data_items_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_operations_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_operations_delete_builder()` + `aiplatform_datasets_data_items_operations_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_operations_delete_builder(client, &args.name)?;
    aiplatform_datasets_data_items_operations_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_operations_get_execute()` to send, or `aiplatform_datasets_data_items_operations_get` for simplest API.

pub fn aiplatform_datasets_data_items_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_operations_get_execute()` or `aiplatform_datasets_data_items_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_operations_get_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_operations_get_builder()` + `aiplatform_datasets_data_items_operations_get_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_operations_get_builder(client, &args.name)?;
    aiplatform_datasets_data_items_operations_get_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_operations_list_execute()` to send, or `aiplatform_datasets_data_items_operations_list` for simplest API.

pub fn aiplatform_datasets_data_items_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_operations_list_execute()` or `aiplatform_datasets_data_items_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_operations_list_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_operations_list_builder()` + `aiplatform_datasets_data_items_operations_list_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_datasets_data_items_operations_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_data_items_operations_wait_execute()` to send, or `aiplatform_datasets_data_items_operations_wait` for simplest API.

pub fn aiplatform_datasets_data_items_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/dataItems/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_data_items_operations_wait_execute()` or `aiplatform_datasets_data_items_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_data_items_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_data_items_operations_wait_task()`.
/// For the simplest API, use `aiplatform_datasets_data_items_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_data_items_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_data_items_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_data_items_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_data_items_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDataItemsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_data_items_operations_wait_builder()` + `aiplatform_datasets_data_items_operations_wait_execute()`.
/// For task-level control, use `aiplatform_datasets_data_items_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_data_items_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDataItemsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_data_items_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_datasets_data_items_operations_wait_execute(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_dataset_versions_create_execute()` to send, or `aiplatform_datasets_dataset_versions_create` for simplest API.

pub fn aiplatform_datasets_dataset_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1DatasetVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/datasetVersions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_dataset_versions_create_execute()` or `aiplatform_datasets_dataset_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_dataset_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_dataset_versions_create_task()`.
/// For the simplest API, use `aiplatform_datasets_dataset_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_dataset_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_dataset_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_dataset_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDatasetVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DatasetVersion,
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_dataset_versions_create_builder()` + `aiplatform_datasets_dataset_versions_create_execute()`.
/// For task-level control, use `aiplatform_datasets_dataset_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_create(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDatasetVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_dataset_versions_create_builder(client, &args.parent, &args.body)?;
    aiplatform_datasets_dataset_versions_create_execute(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_dataset_versions_delete_execute()` to send, or `aiplatform_datasets_dataset_versions_delete` for simplest API.

pub fn aiplatform_datasets_dataset_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/datasetVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_dataset_versions_delete_execute()` or `aiplatform_datasets_dataset_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_dataset_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_dataset_versions_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_dataset_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_dataset_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_dataset_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_dataset_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDatasetVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_dataset_versions_delete_builder()` + `aiplatform_datasets_dataset_versions_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_dataset_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDatasetVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_dataset_versions_delete_builder(client, &args.name)?;
    aiplatform_datasets_dataset_versions_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_dataset_versions_get_execute()` to send, or `aiplatform_datasets_dataset_versions_get` for simplest API.

pub fn aiplatform_datasets_dataset_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/datasetVersions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_dataset_versions_get_execute()` or `aiplatform_datasets_dataset_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DatasetVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_dataset_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_dataset_versions_get_task()`.
/// For the simplest API, use `aiplatform_datasets_dataset_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_dataset_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_dataset_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_dataset_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDatasetVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_dataset_versions_get_builder()` + `aiplatform_datasets_dataset_versions_get_execute()`.
/// For task-level control, use `aiplatform_datasets_dataset_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDatasetVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_dataset_versions_get_builder(
        client,
        &args.name,
        args.readMask.as_deref(),
    )?;
    aiplatform_datasets_dataset_versions_get_execute(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_dataset_versions_list_execute()` to send, or `aiplatform_datasets_dataset_versions_list` for simplest API.

pub fn aiplatform_datasets_dataset_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/datasetVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_dataset_versions_list_execute()` or `aiplatform_datasets_dataset_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDatasetVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_dataset_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_dataset_versions_list_task()`.
/// For the simplest API, use `aiplatform_datasets_dataset_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_dataset_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_dataset_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_dataset_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDatasetVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_dataset_versions_list_builder()` + `aiplatform_datasets_dataset_versions_list_execute()`.
/// For task-level control, use `aiplatform_datasets_dataset_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDatasetVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_dataset_versions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_datasets_dataset_versions_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_dataset_versions_patch_execute()` to send, or `aiplatform_datasets_dataset_versions_patch` for simplest API.

pub fn aiplatform_datasets_dataset_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1DatasetVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/datasetVersions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_dataset_versions_patch_execute()` or `aiplatform_datasets_dataset_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DatasetVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_dataset_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_dataset_versions_patch_task()`.
/// For the simplest API, use `aiplatform_datasets_dataset_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_dataset_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_dataset_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_dataset_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDatasetVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DatasetVersion,
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_dataset_versions_patch_builder()` + `aiplatform_datasets_dataset_versions_patch_execute()`.
/// For task-level control, use `aiplatform_datasets_dataset_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_patch(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDatasetVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_dataset_versions_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_datasets_dataset_versions_patch_execute(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_dataset_versions_restore_execute()` to send, or `aiplatform_datasets_dataset_versions_restore` for simplest API.

pub fn aiplatform_datasets_dataset_versions_restore_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/datasetVersions/{}:restore",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_dataset_versions_restore_execute()` or `aiplatform_datasets_dataset_versions_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_dataset_versions_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_dataset_versions_restore_task()`.
/// For the simplest API, use `aiplatform_datasets_dataset_versions_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_dataset_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_dataset_versions_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_dataset_versions_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_dataset_versions_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsDatasetVersionsRestoreArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_dataset_versions_restore_builder()` + `aiplatform_datasets_dataset_versions_restore_execute()`.
/// For task-level control, use `aiplatform_datasets_dataset_versions_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_dataset_versions_restore(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsDatasetVersionsRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_dataset_versions_restore_builder(client, &args.name)?;
    aiplatform_datasets_dataset_versions_restore_execute(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_operations_cancel_execute()` to send, or `aiplatform_datasets_operations_cancel` for simplest API.

pub fn aiplatform_datasets_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_operations_cancel_execute()` or `aiplatform_datasets_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_datasets_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_operations_cancel_builder()` + `aiplatform_datasets_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_datasets_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_operations_cancel_builder(client, &args.name)?;
    aiplatform_datasets_operations_cancel_execute(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_operations_delete_execute()` to send, or `aiplatform_datasets_operations_delete` for simplest API.

pub fn aiplatform_datasets_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_operations_delete_execute()` or `aiplatform_datasets_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_operations_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_operations_delete_builder()` + `aiplatform_datasets_operations_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_operations_delete_builder(client, &args.name)?;
    aiplatform_datasets_operations_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_operations_get_execute()` to send, or `aiplatform_datasets_operations_get` for simplest API.

pub fn aiplatform_datasets_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_operations_get_execute()` or `aiplatform_datasets_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_operations_get_task()`.
/// For the simplest API, use `aiplatform_datasets_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_operations_get_builder()` + `aiplatform_datasets_operations_get_execute()`.
/// For task-level control, use `aiplatform_datasets_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_operations_get_builder(client, &args.name)?;
    aiplatform_datasets_operations_get_execute(builder)
}

/// GET v1/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_operations_list_execute()` to send, or `aiplatform_datasets_operations_list` for simplest API.

pub fn aiplatform_datasets_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_operations_list_execute()` or `aiplatform_datasets_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_operations_list_task()`.
/// For the simplest API, use `aiplatform_datasets_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_operations_list_builder()` + `aiplatform_datasets_operations_list_execute()`.
/// For task-level control, use `aiplatform_datasets_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_datasets_operations_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_operations_wait_execute()` to send, or `aiplatform_datasets_operations_wait` for simplest API.

pub fn aiplatform_datasets_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_operations_wait_execute()` or `aiplatform_datasets_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_operations_wait_task()`.
/// For the simplest API, use `aiplatform_datasets_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_operations_wait_builder()` + `aiplatform_datasets_operations_wait_execute()`.
/// For task-level control, use `aiplatform_datasets_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_datasets_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_datasets_operations_wait_execute(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_saved_queries_operations_cancel_execute()` to send, or `aiplatform_datasets_saved_queries_operations_cancel` for simplest API.

pub fn aiplatform_datasets_saved_queries_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/savedQueries/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_saved_queries_operations_cancel_execute()` or `aiplatform_datasets_saved_queries_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_saved_queries_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_datasets_saved_queries_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_saved_queries_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_saved_queries_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_saved_queries_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsSavedQueriesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_saved_queries_operations_cancel_builder()` + `aiplatform_datasets_saved_queries_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsSavedQueriesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_saved_queries_operations_cancel_builder(client, &args.name)?;
    aiplatform_datasets_saved_queries_operations_cancel_execute(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_saved_queries_operations_delete_execute()` to send, or `aiplatform_datasets_saved_queries_operations_delete` for simplest API.

pub fn aiplatform_datasets_saved_queries_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/savedQueries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_saved_queries_operations_delete_execute()` or `aiplatform_datasets_saved_queries_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_saved_queries_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_delete_task()`.
/// For the simplest API, use `aiplatform_datasets_saved_queries_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_saved_queries_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_saved_queries_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_saved_queries_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsSavedQueriesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_saved_queries_operations_delete_builder()` + `aiplatform_datasets_saved_queries_operations_delete_execute()`.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsSavedQueriesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_saved_queries_operations_delete_builder(client, &args.name)?;
    aiplatform_datasets_saved_queries_operations_delete_execute(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_saved_queries_operations_get_execute()` to send, or `aiplatform_datasets_saved_queries_operations_get` for simplest API.

pub fn aiplatform_datasets_saved_queries_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/savedQueries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_saved_queries_operations_get_execute()` or `aiplatform_datasets_saved_queries_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_saved_queries_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_get_task()`.
/// For the simplest API, use `aiplatform_datasets_saved_queries_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_saved_queries_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_saved_queries_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_saved_queries_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsSavedQueriesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_saved_queries_operations_get_builder()` + `aiplatform_datasets_saved_queries_operations_get_execute()`.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsSavedQueriesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_saved_queries_operations_get_builder(client, &args.name)?;
    aiplatform_datasets_saved_queries_operations_get_execute(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_saved_queries_operations_list_execute()` to send, or `aiplatform_datasets_saved_queries_operations_list` for simplest API.

pub fn aiplatform_datasets_saved_queries_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/savedQueries/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_saved_queries_operations_list_execute()` or `aiplatform_datasets_saved_queries_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_saved_queries_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_list_task()`.
/// For the simplest API, use `aiplatform_datasets_saved_queries_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_saved_queries_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_saved_queries_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_saved_queries_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsSavedQueriesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_saved_queries_operations_list_builder()` + `aiplatform_datasets_saved_queries_operations_list_execute()`.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsSavedQueriesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_saved_queries_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_datasets_saved_queries_operations_list_execute(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_datasets_saved_queries_operations_wait_execute()` to send, or `aiplatform_datasets_saved_queries_operations_wait` for simplest API.

pub fn aiplatform_datasets_saved_queries_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/datasets/{}/savedQueries/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_datasets_saved_queries_operations_wait_execute()` or `aiplatform_datasets_saved_queries_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_datasets_saved_queries_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_wait_task()`.
/// For the simplest API, use `aiplatform_datasets_saved_queries_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_datasets_saved_queries_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_datasets_saved_queries_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_datasets_saved_queries_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_datasets_saved_queries_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDatasetsSavedQueriesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_datasets_saved_queries_operations_wait_builder()` + `aiplatform_datasets_saved_queries_operations_wait_execute()`.
/// For task-level control, use `aiplatform_datasets_saved_queries_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_datasets_saved_queries_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDatasetsSavedQueriesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_datasets_saved_queries_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_datasets_saved_queries_operations_wait_execute(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_deployment_resource_pools_operations_cancel_execute()` to send, or `aiplatform_deployment_resource_pools_operations_cancel` for simplest API.

pub fn aiplatform_deployment_resource_pools_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/deploymentResourcePools/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_deployment_resource_pools_operations_cancel_execute()` or `aiplatform_deployment_resource_pools_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_deployment_resource_pools_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_deployment_resource_pools_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_deployment_resource_pools_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_deployment_resource_pools_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_deployment_resource_pools_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDeploymentResourcePoolsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_deployment_resource_pools_operations_cancel_builder()` + `aiplatform_deployment_resource_pools_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformDeploymentResourcePoolsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_deployment_resource_pools_operations_cancel_builder(client, &args.name)?;
    aiplatform_deployment_resource_pools_operations_cancel_execute(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_deployment_resource_pools_operations_delete_execute()` to send, or `aiplatform_deployment_resource_pools_operations_delete` for simplest API.

pub fn aiplatform_deployment_resource_pools_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/deploymentResourcePools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_deployment_resource_pools_operations_delete_execute()` or `aiplatform_deployment_resource_pools_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_deployment_resource_pools_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_delete_task()`.
/// For the simplest API, use `aiplatform_deployment_resource_pools_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_deployment_resource_pools_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_deployment_resource_pools_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_deployment_resource_pools_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDeploymentResourcePoolsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_deployment_resource_pools_operations_delete_builder()` + `aiplatform_deployment_resource_pools_operations_delete_execute()`.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformDeploymentResourcePoolsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_deployment_resource_pools_operations_delete_builder(client, &args.name)?;
    aiplatform_deployment_resource_pools_operations_delete_execute(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_deployment_resource_pools_operations_get_execute()` to send, or `aiplatform_deployment_resource_pools_operations_get` for simplest API.

pub fn aiplatform_deployment_resource_pools_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/deploymentResourcePools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_deployment_resource_pools_operations_get_execute()` or `aiplatform_deployment_resource_pools_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_deployment_resource_pools_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_get_task()`.
/// For the simplest API, use `aiplatform_deployment_resource_pools_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_deployment_resource_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_deployment_resource_pools_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_deployment_resource_pools_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDeploymentResourcePoolsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_deployment_resource_pools_operations_get_builder()` + `aiplatform_deployment_resource_pools_operations_get_execute()`.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformDeploymentResourcePoolsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_deployment_resource_pools_operations_get_builder(client, &args.name)?;
    aiplatform_deployment_resource_pools_operations_get_execute(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_deployment_resource_pools_operations_list_execute()` to send, or `aiplatform_deployment_resource_pools_operations_list` for simplest API.

pub fn aiplatform_deployment_resource_pools_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/deploymentResourcePools/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_deployment_resource_pools_operations_list_execute()` or `aiplatform_deployment_resource_pools_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_deployment_resource_pools_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_list_task()`.
/// For the simplest API, use `aiplatform_deployment_resource_pools_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_deployment_resource_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_deployment_resource_pools_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_deployment_resource_pools_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDeploymentResourcePoolsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_deployment_resource_pools_operations_list_builder()` + `aiplatform_deployment_resource_pools_operations_list_execute()`.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformDeploymentResourcePoolsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_deployment_resource_pools_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_deployment_resource_pools_operations_list_execute(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_deployment_resource_pools_operations_wait_execute()` to send, or `aiplatform_deployment_resource_pools_operations_wait` for simplest API.

pub fn aiplatform_deployment_resource_pools_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/deploymentResourcePools/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_deployment_resource_pools_operations_wait_execute()` or `aiplatform_deployment_resource_pools_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_deployment_resource_pools_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_wait_task()`.
/// For the simplest API, use `aiplatform_deployment_resource_pools_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_deployment_resource_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_deployment_resource_pools_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_deployment_resource_pools_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_deployment_resource_pools_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformDeploymentResourcePoolsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_deployment_resource_pools_operations_wait_builder()` + `aiplatform_deployment_resource_pools_operations_wait_execute()`.
/// For task-level control, use `aiplatform_deployment_resource_pools_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_deployment_resource_pools_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformDeploymentResourcePoolsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_deployment_resource_pools_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_deployment_resource_pools_operations_wait_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_compute_tokens_execute()` to send, or `aiplatform_endpoints_compute_tokens` for simplest API.

pub fn aiplatform_endpoints_compute_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1ComputeTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:computeTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_compute_tokens_execute()` or `aiplatform_endpoints_compute_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_compute_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ComputeTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_compute_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_compute_tokens_task()`.
/// For the simplest API, use `aiplatform_endpoints_compute_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_compute_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_compute_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_compute_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsComputeTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ComputeTokensRequest,
}

/// GET v1/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_compute_tokens_builder()` + `aiplatform_endpoints_compute_tokens_execute()`.
/// For task-level control, use `aiplatform_endpoints_compute_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_compute_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsComputeTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_compute_tokens_builder(client, &args.endpoint, &args.body)?;
    aiplatform_endpoints_compute_tokens_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_count_tokens_execute()` to send, or `aiplatform_endpoints_count_tokens` for simplest API.

pub fn aiplatform_endpoints_count_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1CountTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:countTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_count_tokens_execute()` or `aiplatform_endpoints_count_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_count_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CountTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_count_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_count_tokens_task()`.
/// For the simplest API, use `aiplatform_endpoints_count_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_count_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_count_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_count_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsCountTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CountTokensRequest,
}

/// GET v1/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_count_tokens_builder()` + `aiplatform_endpoints_count_tokens_execute()`.
/// For task-level control, use `aiplatform_endpoints_count_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_count_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsCountTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_count_tokens_builder(client, &args.endpoint, &args.body)?;
    aiplatform_endpoints_count_tokens_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_fetch_predict_operation_execute()` to send, or `aiplatform_endpoints_fetch_predict_operation` for simplest API.

pub fn aiplatform_endpoints_fetch_predict_operation_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1FetchPredictOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:fetchPredictOperation",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_fetch_predict_operation_execute()` or `aiplatform_endpoints_fetch_predict_operation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_fetch_predict_operation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_fetch_predict_operation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_fetch_predict_operation_task()`.
/// For the simplest API, use `aiplatform_endpoints_fetch_predict_operation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_fetch_predict_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_fetch_predict_operation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_fetch_predict_operation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsFetchPredictOperationArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FetchPredictOperationRequest,
}

/// GET v1/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_fetch_predict_operation_builder()` + `aiplatform_endpoints_fetch_predict_operation_execute()`.
/// For task-level control, use `aiplatform_endpoints_fetch_predict_operation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_fetch_predict_operation(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsFetchPredictOperationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_endpoints_fetch_predict_operation_builder(client, &args.endpoint, &args.body)?;
    aiplatform_endpoints_fetch_predict_operation_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_generate_content_execute()` to send, or `aiplatform_endpoints_generate_content` for simplest API.

pub fn aiplatform_endpoints_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:generateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_generate_content_execute()` or `aiplatform_endpoints_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_generate_content_task()`.
/// For the simplest API, use `aiplatform_endpoints_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_generate_content_builder()` + `aiplatform_endpoints_generate_content_execute()`.
/// For task-level control, use `aiplatform_endpoints_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_generate_content_builder(client, &args.model, &args.body)?;
    aiplatform_endpoints_generate_content_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_predict_execute()` to send, or `aiplatform_endpoints_predict` for simplest API.

pub fn aiplatform_endpoints_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:predict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_predict_execute()` or `aiplatform_endpoints_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PredictResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_predict_task()`.
/// For the simplest API, use `aiplatform_endpoints_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictRequest,
}

/// GET v1/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_predict_builder()` + `aiplatform_endpoints_predict_execute()`.
/// For task-level control, use `aiplatform_endpoints_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_predict(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_predict_builder(client, &args.endpoint, &args.body)?;
    aiplatform_endpoints_predict_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:predictLongRunning
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_predict_long_running_execute()` to send, or `aiplatform_endpoints_predict_long_running` for simplest API.

pub fn aiplatform_endpoints_predict_long_running_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictLongRunningRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:predictLongRunning",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_predict_long_running_execute()` or `aiplatform_endpoints_predict_long_running`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_predict_long_running_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_predict_long_running_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_predict_long_running_task()`.
/// For the simplest API, use `aiplatform_endpoints_predict_long_running()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_predict_long_running_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_predict_long_running_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_predict_long_running`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsPredictLongRunningArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictLongRunningRequest,
}

/// GET v1/endpoints/{endpointsId}:predictLongRunning
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_predict_long_running_builder()` + `aiplatform_endpoints_predict_long_running_execute()`.
/// For task-level control, use `aiplatform_endpoints_predict_long_running_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_predict_long_running(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsPredictLongRunningArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_endpoints_predict_long_running_builder(client, &args.endpoint, &args.body)?;
    aiplatform_endpoints_predict_long_running_execute(builder)
}

/// GET v1/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_stream_generate_content_execute()` to send, or `aiplatform_endpoints_stream_generate_content` for simplest API.

pub fn aiplatform_endpoints_stream_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}:streamGenerateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_stream_generate_content_execute()` or `aiplatform_endpoints_stream_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_stream_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_stream_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_stream_generate_content_task()`.
/// For the simplest API, use `aiplatform_endpoints_stream_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_stream_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_stream_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_stream_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsStreamGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_stream_generate_content_builder()` + `aiplatform_endpoints_stream_generate_content_execute()`.
/// For task-level control, use `aiplatform_endpoints_stream_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_stream_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsStreamGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_endpoints_stream_generate_content_builder(client, &args.model, &args.body)?;
    aiplatform_endpoints_stream_generate_content_execute(builder)
}

/// GET v1/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_chat_completions_execute()` to send, or `aiplatform_endpoints_chat_completions` for simplest API.

pub fn aiplatform_endpoints_chat_completions_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}/chat/completions",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_chat_completions_execute()` or `aiplatform_endpoints_chat_completions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_chat_completions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_chat_completions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_chat_completions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_chat_completions_task()`.
/// For the simplest API, use `aiplatform_endpoints_chat_completions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_chat_completions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_chat_completions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_chat_completions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_chat_completions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsChatCompletionsArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_chat_completions_builder()` + `aiplatform_endpoints_chat_completions_execute()`.
/// For task-level control, use `aiplatform_endpoints_chat_completions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_chat_completions(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsChatCompletionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_endpoints_chat_completions_builder(client, &args.endpoint, &args.body)?;
    aiplatform_endpoints_chat_completions_execute(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_operations_cancel_execute()` to send, or `aiplatform_endpoints_operations_cancel` for simplest API.

pub fn aiplatform_endpoints_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_operations_cancel_execute()` or `aiplatform_endpoints_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_endpoints_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_operations_cancel_builder()` + `aiplatform_endpoints_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_endpoints_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_operations_cancel_builder(client, &args.name)?;
    aiplatform_endpoints_operations_cancel_execute(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_operations_delete_execute()` to send, or `aiplatform_endpoints_operations_delete` for simplest API.

pub fn aiplatform_endpoints_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_operations_delete_execute()` or `aiplatform_endpoints_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_operations_delete_task()`.
/// For the simplest API, use `aiplatform_endpoints_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_operations_delete_builder()` + `aiplatform_endpoints_operations_delete_execute()`.
/// For task-level control, use `aiplatform_endpoints_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_operations_delete_builder(client, &args.name)?;
    aiplatform_endpoints_operations_delete_execute(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_operations_get_execute()` to send, or `aiplatform_endpoints_operations_get` for simplest API.

pub fn aiplatform_endpoints_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_operations_get_execute()` or `aiplatform_endpoints_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_operations_get_task()`.
/// For the simplest API, use `aiplatform_endpoints_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_operations_get_builder()` + `aiplatform_endpoints_operations_get_execute()`.
/// For task-level control, use `aiplatform_endpoints_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_operations_get_builder(client, &args.name)?;
    aiplatform_endpoints_operations_get_execute(builder)
}

/// GET v1/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_operations_list_execute()` to send, or `aiplatform_endpoints_operations_list` for simplest API.

pub fn aiplatform_endpoints_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_operations_list_execute()` or `aiplatform_endpoints_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_operations_list_task()`.
/// For the simplest API, use `aiplatform_endpoints_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_operations_list_builder()` + `aiplatform_endpoints_operations_list_execute()`.
/// For task-level control, use `aiplatform_endpoints_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_endpoints_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_endpoints_operations_list_execute(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_endpoints_operations_wait_execute()` to send, or `aiplatform_endpoints_operations_wait` for simplest API.

pub fn aiplatform_endpoints_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/endpoints/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_endpoints_operations_wait_execute()` or `aiplatform_endpoints_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_endpoints_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_endpoints_operations_wait_task()`.
/// For the simplest API, use `aiplatform_endpoints_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_endpoints_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_endpoints_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_endpoints_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEndpointsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_endpoints_operations_wait_builder()` + `aiplatform_endpoints_operations_wait_execute()`.
/// For task-level control, use `aiplatform_endpoints_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_endpoints_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformEndpointsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_endpoints_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_endpoints_operations_wait_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_features_operations_delete_execute()` to send, or `aiplatform_feature_groups_features_operations_delete` for simplest API.

pub fn aiplatform_feature_groups_features_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_features_operations_delete_execute()` or `aiplatform_feature_groups_features_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_features_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_features_operations_delete_task()`.
/// For the simplest API, use `aiplatform_feature_groups_features_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_features_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_features_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_features_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsFeaturesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_features_operations_delete_builder()` + `aiplatform_feature_groups_features_operations_delete_execute()`.
/// For task-level control, use `aiplatform_feature_groups_features_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsFeaturesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_features_operations_delete_builder(client, &args.name)?;
    aiplatform_feature_groups_features_operations_delete_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_features_operations_get_execute()` to send, or `aiplatform_feature_groups_features_operations_get` for simplest API.

pub fn aiplatform_feature_groups_features_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_features_operations_get_execute()` or `aiplatform_feature_groups_features_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_features_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_features_operations_get_task()`.
/// For the simplest API, use `aiplatform_feature_groups_features_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_features_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_features_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_features_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsFeaturesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_features_operations_get_builder()` + `aiplatform_feature_groups_features_operations_get_execute()`.
/// For task-level control, use `aiplatform_feature_groups_features_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsFeaturesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_features_operations_get_builder(client, &args.name)?;
    aiplatform_feature_groups_features_operations_get_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_features_operations_list_wait_execute()` to send, or `aiplatform_feature_groups_features_operations_list_wait` for simplest API.

pub fn aiplatform_feature_groups_features_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/features/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_features_operations_list_wait_execute()` or `aiplatform_feature_groups_features_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_features_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_features_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_feature_groups_features_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_features_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_features_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_features_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsFeaturesOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_features_operations_list_wait_builder()` + `aiplatform_feature_groups_features_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_feature_groups_features_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsFeaturesOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_features_operations_list_wait_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_feature_groups_features_operations_list_wait_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_features_operations_wait_execute()` to send, or `aiplatform_feature_groups_features_operations_wait` for simplest API.

pub fn aiplatform_feature_groups_features_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/features/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_features_operations_wait_execute()` or `aiplatform_feature_groups_features_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_features_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_features_operations_wait_task()`.
/// For the simplest API, use `aiplatform_feature_groups_features_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_features_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_features_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_features_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsFeaturesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_features_operations_wait_builder()` + `aiplatform_feature_groups_features_operations_wait_execute()`.
/// For task-level control, use `aiplatform_feature_groups_features_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_features_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsFeaturesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_features_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_feature_groups_features_operations_wait_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_operations_delete_execute()` to send, or `aiplatform_feature_groups_operations_delete` for simplest API.

pub fn aiplatform_feature_groups_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_operations_delete_execute()` or `aiplatform_feature_groups_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_operations_delete_task()`.
/// For the simplest API, use `aiplatform_feature_groups_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_operations_delete_builder()` + `aiplatform_feature_groups_operations_delete_execute()`.
/// For task-level control, use `aiplatform_feature_groups_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_operations_delete_builder(client, &args.name)?;
    aiplatform_feature_groups_operations_delete_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_operations_get_execute()` to send, or `aiplatform_feature_groups_operations_get` for simplest API.

pub fn aiplatform_feature_groups_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_operations_get_execute()` or `aiplatform_feature_groups_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_operations_get_task()`.
/// For the simplest API, use `aiplatform_feature_groups_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_operations_get_builder()` + `aiplatform_feature_groups_operations_get_execute()`.
/// For task-level control, use `aiplatform_feature_groups_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_operations_get_builder(client, &args.name)?;
    aiplatform_feature_groups_operations_get_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_operations_list_wait_execute()` to send, or `aiplatform_feature_groups_operations_list_wait` for simplest API.

pub fn aiplatform_feature_groups_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_operations_list_wait_execute()` or `aiplatform_feature_groups_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_feature_groups_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_operations_list_wait_builder()` + `aiplatform_feature_groups_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_feature_groups_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_operations_list_wait_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_feature_groups_operations_list_wait_execute(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_groups_operations_wait_execute()` to send, or `aiplatform_feature_groups_operations_wait` for simplest API.

pub fn aiplatform_feature_groups_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureGroups/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_groups_operations_wait_execute()` or `aiplatform_feature_groups_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_groups_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_groups_operations_wait_task()`.
/// For the simplest API, use `aiplatform_feature_groups_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_groups_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_groups_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_groups_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_groups_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureGroupsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_groups_operations_wait_builder()` + `aiplatform_feature_groups_operations_wait_execute()`.
/// For task-level control, use `aiplatform_feature_groups_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_groups_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureGroupsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_groups_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_feature_groups_operations_wait_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_feature_views_operations_delete_execute()` to send, or `aiplatform_feature_online_stores_feature_views_operations_delete` for simplest API.

pub fn aiplatform_feature_online_stores_feature_views_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/featureViews/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_feature_views_operations_delete_execute()` or `aiplatform_feature_online_stores_feature_views_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_feature_views_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_delete_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_feature_views_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_feature_views_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_feature_views_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_feature_views_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresFeatureViewsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_feature_views_operations_delete_builder()` + `aiplatform_feature_online_stores_feature_views_operations_delete_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresFeatureViewsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_feature_views_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_feature_online_stores_feature_views_operations_delete_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_feature_views_operations_get_execute()` to send, or `aiplatform_feature_online_stores_feature_views_operations_get` for simplest API.

pub fn aiplatform_feature_online_stores_feature_views_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/featureViews/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_feature_views_operations_get_execute()` or `aiplatform_feature_online_stores_feature_views_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_feature_views_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_get_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_feature_views_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_feature_views_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_feature_views_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_feature_views_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresFeatureViewsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_feature_views_operations_get_builder()` + `aiplatform_feature_online_stores_feature_views_operations_get_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresFeatureViewsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_feature_online_stores_feature_views_operations_get_builder(client, &args.name)?;
    aiplatform_feature_online_stores_feature_views_operations_get_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_feature_views_operations_list_wait_execute()` to send, or `aiplatform_feature_online_stores_feature_views_operations_list_wait` for simplest API.

pub fn aiplatform_feature_online_stores_feature_views_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/featureViews/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_feature_views_operations_list_wait_execute()` or `aiplatform_feature_online_stores_feature_views_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_feature_views_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_feature_views_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_feature_views_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_feature_views_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_feature_views_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresFeatureViewsOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_feature_views_operations_list_wait_builder()` + `aiplatform_feature_online_stores_feature_views_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresFeatureViewsOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_feature_views_operations_list_wait_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_feature_online_stores_feature_views_operations_list_wait_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_feature_views_operations_wait_execute()` to send, or `aiplatform_feature_online_stores_feature_views_operations_wait` for simplest API.

pub fn aiplatform_feature_online_stores_feature_views_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/featureViews/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_feature_views_operations_wait_execute()` or `aiplatform_feature_online_stores_feature_views_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_feature_views_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_wait_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_feature_views_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_feature_views_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_feature_views_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_feature_views_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_feature_views_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresFeatureViewsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_feature_views_operations_wait_builder()` + `aiplatform_feature_online_stores_feature_views_operations_wait_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_feature_views_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_feature_views_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresFeatureViewsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_feature_views_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_feature_online_stores_feature_views_operations_wait_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_operations_delete_execute()` to send, or `aiplatform_feature_online_stores_operations_delete` for simplest API.

pub fn aiplatform_feature_online_stores_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_operations_delete_execute()` or `aiplatform_feature_online_stores_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_operations_delete_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_operations_delete_builder()` + `aiplatform_feature_online_stores_operations_delete_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_operations_delete_builder(client, &args.name)?;
    aiplatform_feature_online_stores_operations_delete_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_operations_get_execute()` to send, or `aiplatform_feature_online_stores_operations_get` for simplest API.

pub fn aiplatform_feature_online_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_operations_get_execute()` or `aiplatform_feature_online_stores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_operations_get_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_operations_get_builder()` + `aiplatform_feature_online_stores_operations_get_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_operations_get_builder(client, &args.name)?;
    aiplatform_feature_online_stores_operations_get_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_operations_list_wait_execute()` to send, or `aiplatform_feature_online_stores_operations_list_wait` for simplest API.

pub fn aiplatform_feature_online_stores_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_operations_list_wait_execute()` or `aiplatform_feature_online_stores_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_operations_list_wait_builder()` + `aiplatform_feature_online_stores_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_operations_list_wait_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_feature_online_stores_operations_list_wait_execute(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_feature_online_stores_operations_wait_execute()` to send, or `aiplatform_feature_online_stores_operations_wait` for simplest API.

pub fn aiplatform_feature_online_stores_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featureOnlineStores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_feature_online_stores_operations_wait_execute()` or `aiplatform_feature_online_stores_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_feature_online_stores_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_feature_online_stores_operations_wait_task()`.
/// For the simplest API, use `aiplatform_feature_online_stores_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_feature_online_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_feature_online_stores_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_feature_online_stores_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_feature_online_stores_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeatureOnlineStoresOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_feature_online_stores_operations_wait_builder()` + `aiplatform_feature_online_stores_operations_wait_execute()`.
/// For task-level control, use `aiplatform_feature_online_stores_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_feature_online_stores_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeatureOnlineStoresOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_feature_online_stores_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_feature_online_stores_operations_wait_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_features_operations_cancel_execute()` to send, or `aiplatform_featurestores_entity_types_features_operations_cancel` for simplest API.

pub fn aiplatform_featurestores_entity_types_features_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/features/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_features_operations_cancel_execute()` or `aiplatform_featurestores_entity_types_features_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_features_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_features_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_features_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_features_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_features_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesFeaturesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_features_operations_cancel_builder()` + `aiplatform_featurestores_entity_types_features_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_features_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_featurestores_entity_types_features_operations_cancel_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_features_operations_delete_execute()` to send, or `aiplatform_featurestores_entity_types_features_operations_delete` for simplest API.

pub fn aiplatform_featurestores_entity_types_features_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_features_operations_delete_execute()` or `aiplatform_featurestores_entity_types_features_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_features_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_delete_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_features_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_features_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_features_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_features_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesFeaturesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_features_operations_delete_builder()` + `aiplatform_featurestores_entity_types_features_operations_delete_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_features_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_featurestores_entity_types_features_operations_delete_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_features_operations_get_execute()` to send, or `aiplatform_featurestores_entity_types_features_operations_get` for simplest API.

pub fn aiplatform_featurestores_entity_types_features_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_features_operations_get_execute()` or `aiplatform_featurestores_entity_types_features_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_features_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_get_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_features_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_features_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_features_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_features_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesFeaturesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_features_operations_get_builder()` + `aiplatform_featurestores_entity_types_features_operations_get_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_featurestores_entity_types_features_operations_get_builder(client, &args.name)?;
    aiplatform_featurestores_entity_types_features_operations_get_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_features_operations_list_execute()` to send, or `aiplatform_featurestores_entity_types_features_operations_list` for simplest API.

pub fn aiplatform_featurestores_entity_types_features_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/features/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_features_operations_list_execute()` or `aiplatform_featurestores_entity_types_features_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_features_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_list_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_features_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_features_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_features_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_features_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesFeaturesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_features_operations_list_builder()` + `aiplatform_featurestores_entity_types_features_operations_list_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_features_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_featurestores_entity_types_features_operations_list_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_features_operations_wait_execute()` to send, or `aiplatform_featurestores_entity_types_features_operations_wait` for simplest API.

pub fn aiplatform_featurestores_entity_types_features_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/features/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_features_operations_wait_execute()` or `aiplatform_featurestores_entity_types_features_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_features_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_wait_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_features_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_features_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_features_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_features_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesFeaturesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_features_operations_wait_builder()` + `aiplatform_featurestores_entity_types_features_operations_wait_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_features_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_features_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_features_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_featurestores_entity_types_features_operations_wait_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_operations_cancel_execute()` to send, or `aiplatform_featurestores_entity_types_operations_cancel` for simplest API.

pub fn aiplatform_featurestores_entity_types_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_operations_cancel_execute()` or `aiplatform_featurestores_entity_types_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_operations_cancel_builder()` + `aiplatform_featurestores_entity_types_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_featurestores_entity_types_operations_cancel_builder(client, &args.name)?;
    aiplatform_featurestores_entity_types_operations_cancel_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_operations_delete_execute()` to send, or `aiplatform_featurestores_entity_types_operations_delete` for simplest API.

pub fn aiplatform_featurestores_entity_types_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_operations_delete_execute()` or `aiplatform_featurestores_entity_types_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_delete_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_operations_delete_builder()` + `aiplatform_featurestores_entity_types_operations_delete_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_featurestores_entity_types_operations_delete_builder(client, &args.name)?;
    aiplatform_featurestores_entity_types_operations_delete_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_operations_get_execute()` to send, or `aiplatform_featurestores_entity_types_operations_get` for simplest API.

pub fn aiplatform_featurestores_entity_types_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_operations_get_execute()` or `aiplatform_featurestores_entity_types_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_get_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_operations_get_builder()` + `aiplatform_featurestores_entity_types_operations_get_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_operations_get_builder(client, &args.name)?;
    aiplatform_featurestores_entity_types_operations_get_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_operations_list_execute()` to send, or `aiplatform_featurestores_entity_types_operations_list` for simplest API.

pub fn aiplatform_featurestores_entity_types_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_operations_list_execute()` or `aiplatform_featurestores_entity_types_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_list_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_operations_list_builder()` + `aiplatform_featurestores_entity_types_operations_list_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_featurestores_entity_types_operations_list_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_entity_types_operations_wait_execute()` to send, or `aiplatform_featurestores_entity_types_operations_wait` for simplest API.

pub fn aiplatform_featurestores_entity_types_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/entityTypes/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_entity_types_operations_wait_execute()` or `aiplatform_featurestores_entity_types_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_entity_types_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_wait_task()`.
/// For the simplest API, use `aiplatform_featurestores_entity_types_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_entity_types_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_entity_types_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_entity_types_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_entity_types_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresEntityTypesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_entity_types_operations_wait_builder()` + `aiplatform_featurestores_entity_types_operations_wait_execute()`.
/// For task-level control, use `aiplatform_featurestores_entity_types_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_entity_types_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresEntityTypesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_entity_types_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_featurestores_entity_types_operations_wait_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_operations_cancel_execute()` to send, or `aiplatform_featurestores_operations_cancel` for simplest API.

pub fn aiplatform_featurestores_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_operations_cancel_execute()` or `aiplatform_featurestores_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_featurestores_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_operations_cancel_builder()` + `aiplatform_featurestores_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_featurestores_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_operations_cancel_builder(client, &args.name)?;
    aiplatform_featurestores_operations_cancel_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_operations_delete_execute()` to send, or `aiplatform_featurestores_operations_delete` for simplest API.

pub fn aiplatform_featurestores_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_operations_delete_execute()` or `aiplatform_featurestores_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_operations_delete_task()`.
/// For the simplest API, use `aiplatform_featurestores_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_operations_delete_builder()` + `aiplatform_featurestores_operations_delete_execute()`.
/// For task-level control, use `aiplatform_featurestores_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_operations_delete_builder(client, &args.name)?;
    aiplatform_featurestores_operations_delete_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_operations_get_execute()` to send, or `aiplatform_featurestores_operations_get` for simplest API.

pub fn aiplatform_featurestores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_operations_get_execute()` or `aiplatform_featurestores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_operations_get_task()`.
/// For the simplest API, use `aiplatform_featurestores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_operations_get_builder()` + `aiplatform_featurestores_operations_get_execute()`.
/// For task-level control, use `aiplatform_featurestores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_operations_get_builder(client, &args.name)?;
    aiplatform_featurestores_operations_get_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_operations_list_execute()` to send, or `aiplatform_featurestores_operations_list` for simplest API.

pub fn aiplatform_featurestores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_operations_list_execute()` or `aiplatform_featurestores_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_operations_list_task()`.
/// For the simplest API, use `aiplatform_featurestores_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_operations_list_builder()` + `aiplatform_featurestores_operations_list_execute()`.
/// For task-level control, use `aiplatform_featurestores_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_featurestores_operations_list_execute(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_featurestores_operations_wait_execute()` to send, or `aiplatform_featurestores_operations_wait` for simplest API.

pub fn aiplatform_featurestores_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/featurestores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_featurestores_operations_wait_execute()` or `aiplatform_featurestores_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_featurestores_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_featurestores_operations_wait_task()`.
/// For the simplest API, use `aiplatform_featurestores_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_featurestores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_featurestores_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_featurestores_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_featurestores_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformFeaturestoresOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_featurestores_operations_wait_builder()` + `aiplatform_featurestores_operations_wait_execute()`.
/// For task-level control, use `aiplatform_featurestores_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_featurestores_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformFeaturestoresOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_featurestores_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_featurestores_operations_wait_execute(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_hyperparameter_tuning_jobs_operations_cancel_execute()` to send, or `aiplatform_hyperparameter_tuning_jobs_operations_cancel` for simplest API.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/hyperparameterTuningJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_hyperparameter_tuning_jobs_operations_cancel_execute()` or `aiplatform_hyperparameter_tuning_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_hyperparameter_tuning_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_hyperparameter_tuning_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_hyperparameter_tuning_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformHyperparameterTuningJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder()` + `aiplatform_hyperparameter_tuning_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformHyperparameterTuningJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_hyperparameter_tuning_jobs_operations_cancel_execute(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_hyperparameter_tuning_jobs_operations_delete_execute()` to send, or `aiplatform_hyperparameter_tuning_jobs_operations_delete` for simplest API.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/hyperparameterTuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_hyperparameter_tuning_jobs_operations_delete_execute()` or `aiplatform_hyperparameter_tuning_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_hyperparameter_tuning_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_hyperparameter_tuning_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_hyperparameter_tuning_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_hyperparameter_tuning_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformHyperparameterTuningJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_hyperparameter_tuning_jobs_operations_delete_builder()` + `aiplatform_hyperparameter_tuning_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformHyperparameterTuningJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_hyperparameter_tuning_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_hyperparameter_tuning_jobs_operations_delete_execute(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_hyperparameter_tuning_jobs_operations_get_execute()` to send, or `aiplatform_hyperparameter_tuning_jobs_operations_get` for simplest API.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/hyperparameterTuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_hyperparameter_tuning_jobs_operations_get_execute()` or `aiplatform_hyperparameter_tuning_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_hyperparameter_tuning_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_hyperparameter_tuning_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_hyperparameter_tuning_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_hyperparameter_tuning_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformHyperparameterTuningJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_hyperparameter_tuning_jobs_operations_get_builder()` + `aiplatform_hyperparameter_tuning_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformHyperparameterTuningJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_hyperparameter_tuning_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_hyperparameter_tuning_jobs_operations_get_execute(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_hyperparameter_tuning_jobs_operations_list_execute()` to send, or `aiplatform_hyperparameter_tuning_jobs_operations_list` for simplest API.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/hyperparameterTuningJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_hyperparameter_tuning_jobs_operations_list_execute()` or `aiplatform_hyperparameter_tuning_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_hyperparameter_tuning_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_hyperparameter_tuning_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_hyperparameter_tuning_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_hyperparameter_tuning_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformHyperparameterTuningJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_hyperparameter_tuning_jobs_operations_list_builder()` + `aiplatform_hyperparameter_tuning_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformHyperparameterTuningJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_hyperparameter_tuning_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_hyperparameter_tuning_jobs_operations_list_execute(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_hyperparameter_tuning_jobs_operations_wait_execute()` to send, or `aiplatform_hyperparameter_tuning_jobs_operations_wait` for simplest API.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/hyperparameterTuningJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_hyperparameter_tuning_jobs_operations_wait_execute()` or `aiplatform_hyperparameter_tuning_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_hyperparameter_tuning_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_hyperparameter_tuning_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_hyperparameter_tuning_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_hyperparameter_tuning_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_hyperparameter_tuning_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformHyperparameterTuningJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_hyperparameter_tuning_jobs_operations_wait_builder()` + `aiplatform_hyperparameter_tuning_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_hyperparameter_tuning_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_hyperparameter_tuning_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformHyperparameterTuningJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_hyperparameter_tuning_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_hyperparameter_tuning_jobs_operations_wait_execute(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_index_endpoints_operations_cancel_execute()` to send, or `aiplatform_index_endpoints_operations_cancel` for simplest API.

pub fn aiplatform_index_endpoints_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexEndpoints/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_index_endpoints_operations_cancel_execute()` or `aiplatform_index_endpoints_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_index_endpoints_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_index_endpoints_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_index_endpoints_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_index_endpoints_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_index_endpoints_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_index_endpoints_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexEndpointsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_index_endpoints_operations_cancel_builder()` + `aiplatform_index_endpoints_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_index_endpoints_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformIndexEndpointsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_index_endpoints_operations_cancel_builder(client, &args.name)?;
    aiplatform_index_endpoints_operations_cancel_execute(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_index_endpoints_operations_delete_execute()` to send, or `aiplatform_index_endpoints_operations_delete` for simplest API.

pub fn aiplatform_index_endpoints_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexEndpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_index_endpoints_operations_delete_execute()` or `aiplatform_index_endpoints_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_index_endpoints_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_index_endpoints_operations_delete_task()`.
/// For the simplest API, use `aiplatform_index_endpoints_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_index_endpoints_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_index_endpoints_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_index_endpoints_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexEndpointsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_index_endpoints_operations_delete_builder()` + `aiplatform_index_endpoints_operations_delete_execute()`.
/// For task-level control, use `aiplatform_index_endpoints_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformIndexEndpointsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_index_endpoints_operations_delete_builder(client, &args.name)?;
    aiplatform_index_endpoints_operations_delete_execute(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_index_endpoints_operations_get_execute()` to send, or `aiplatform_index_endpoints_operations_get` for simplest API.

pub fn aiplatform_index_endpoints_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexEndpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_index_endpoints_operations_get_execute()` or `aiplatform_index_endpoints_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_index_endpoints_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_index_endpoints_operations_get_task()`.
/// For the simplest API, use `aiplatform_index_endpoints_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_index_endpoints_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_index_endpoints_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_index_endpoints_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexEndpointsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_index_endpoints_operations_get_builder()` + `aiplatform_index_endpoints_operations_get_execute()`.
/// For task-level control, use `aiplatform_index_endpoints_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformIndexEndpointsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_index_endpoints_operations_get_builder(client, &args.name)?;
    aiplatform_index_endpoints_operations_get_execute(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_index_endpoints_operations_list_execute()` to send, or `aiplatform_index_endpoints_operations_list` for simplest API.

pub fn aiplatform_index_endpoints_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexEndpoints/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_index_endpoints_operations_list_execute()` or `aiplatform_index_endpoints_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_index_endpoints_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_index_endpoints_operations_list_task()`.
/// For the simplest API, use `aiplatform_index_endpoints_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_index_endpoints_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_index_endpoints_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_index_endpoints_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexEndpointsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_index_endpoints_operations_list_builder()` + `aiplatform_index_endpoints_operations_list_execute()`.
/// For task-level control, use `aiplatform_index_endpoints_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformIndexEndpointsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_index_endpoints_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_index_endpoints_operations_list_execute(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_index_endpoints_operations_wait_execute()` to send, or `aiplatform_index_endpoints_operations_wait` for simplest API.

pub fn aiplatform_index_endpoints_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexEndpoints/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_index_endpoints_operations_wait_execute()` or `aiplatform_index_endpoints_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_index_endpoints_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_index_endpoints_operations_wait_task()`.
/// For the simplest API, use `aiplatform_index_endpoints_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_index_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_index_endpoints_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_index_endpoints_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_index_endpoints_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexEndpointsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_index_endpoints_operations_wait_builder()` + `aiplatform_index_endpoints_operations_wait_execute()`.
/// For task-level control, use `aiplatform_index_endpoints_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_index_endpoints_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformIndexEndpointsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_index_endpoints_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_index_endpoints_operations_wait_execute(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_indexes_operations_cancel_execute()` to send, or `aiplatform_indexes_operations_cancel` for simplest API.

pub fn aiplatform_indexes_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexes/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_indexes_operations_cancel_execute()` or `aiplatform_indexes_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_indexes_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_indexes_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_indexes_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_indexes_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_indexes_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_indexes_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_indexes_operations_cancel_builder()` + `aiplatform_indexes_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_indexes_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformIndexesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_indexes_operations_cancel_builder(client, &args.name)?;
    aiplatform_indexes_operations_cancel_execute(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_indexes_operations_delete_execute()` to send, or `aiplatform_indexes_operations_delete` for simplest API.

pub fn aiplatform_indexes_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_indexes_operations_delete_execute()` or `aiplatform_indexes_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_indexes_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_indexes_operations_delete_task()`.
/// For the simplest API, use `aiplatform_indexes_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_indexes_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_indexes_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_indexes_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_indexes_operations_delete_builder()` + `aiplatform_indexes_operations_delete_execute()`.
/// For task-level control, use `aiplatform_indexes_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformIndexesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_indexes_operations_delete_builder(client, &args.name)?;
    aiplatform_indexes_operations_delete_execute(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_indexes_operations_get_execute()` to send, or `aiplatform_indexes_operations_get` for simplest API.

pub fn aiplatform_indexes_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_indexes_operations_get_execute()` or `aiplatform_indexes_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_indexes_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_indexes_operations_get_task()`.
/// For the simplest API, use `aiplatform_indexes_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_indexes_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_indexes_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_indexes_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_indexes_operations_get_builder()` + `aiplatform_indexes_operations_get_execute()`.
/// For task-level control, use `aiplatform_indexes_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformIndexesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_indexes_operations_get_builder(client, &args.name)?;
    aiplatform_indexes_operations_get_execute(builder)
}

/// GET v1/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_indexes_operations_list_execute()` to send, or `aiplatform_indexes_operations_list` for simplest API.

pub fn aiplatform_indexes_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexes/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_indexes_operations_list_execute()` or `aiplatform_indexes_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_indexes_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_indexes_operations_list_task()`.
/// For the simplest API, use `aiplatform_indexes_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_indexes_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_indexes_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_indexes_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_indexes_operations_list_builder()` + `aiplatform_indexes_operations_list_execute()`.
/// For task-level control, use `aiplatform_indexes_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformIndexesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_indexes_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_indexes_operations_list_execute(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_indexes_operations_wait_execute()` to send, or `aiplatform_indexes_operations_wait` for simplest API.

pub fn aiplatform_indexes_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/indexes/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_indexes_operations_wait_execute()` or `aiplatform_indexes_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_indexes_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_indexes_operations_wait_task()`.
/// For the simplest API, use `aiplatform_indexes_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_indexes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_indexes_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_indexes_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_indexes_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformIndexesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_indexes_operations_wait_builder()` + `aiplatform_indexes_operations_wait_execute()`.
/// For task-level control, use `aiplatform_indexes_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_indexes_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformIndexesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_indexes_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_indexes_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:upload
/// Upload a file into a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_media_upload_execute()` to send, or `aiplatform_media_upload` for simplest API.

pub fn aiplatform_media_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1UploadRagFileRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles:upload",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:upload
/// Upload a file into a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_media_upload_execute()` or `aiplatform_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1UploadRagFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1UploadRagFileResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:upload
/// Upload a file into a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_media_upload_task()`.
/// For the simplest API, use `aiplatform_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1UploadRagFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMediaUploadArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UploadRagFileRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:upload
/// Upload a file into a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_media_upload_builder()` + `aiplatform_media_upload_execute()`.
/// For task-level control, use `aiplatform_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_media_upload(
    client: &SimpleHttpClient,
    args: &AiplatformMediaUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1UploadRagFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_media_upload_builder(client, &args.parent, &args.body)?;
    aiplatform_media_upload_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_artifacts_operations_cancel_execute()` to send, or `aiplatform_metadata_stores_artifacts_operations_cancel` for simplest API.

pub fn aiplatform_metadata_stores_artifacts_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/artifacts/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_artifacts_operations_cancel_execute()` or `aiplatform_metadata_stores_artifacts_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_artifacts_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_artifacts_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_artifacts_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_artifacts_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_artifacts_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresArtifactsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_artifacts_operations_cancel_builder()` + `aiplatform_metadata_stores_artifacts_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresArtifactsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_metadata_stores_artifacts_operations_cancel_builder(client, &args.name)?;
    aiplatform_metadata_stores_artifacts_operations_cancel_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_artifacts_operations_delete_execute()` to send, or `aiplatform_metadata_stores_artifacts_operations_delete` for simplest API.

pub fn aiplatform_metadata_stores_artifacts_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/artifacts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_artifacts_operations_delete_execute()` or `aiplatform_metadata_stores_artifacts_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_artifacts_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_delete_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_artifacts_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_artifacts_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_artifacts_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_artifacts_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresArtifactsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_artifacts_operations_delete_builder()` + `aiplatform_metadata_stores_artifacts_operations_delete_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresArtifactsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_metadata_stores_artifacts_operations_delete_builder(client, &args.name)?;
    aiplatform_metadata_stores_artifacts_operations_delete_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_artifacts_operations_get_execute()` to send, or `aiplatform_metadata_stores_artifacts_operations_get` for simplest API.

pub fn aiplatform_metadata_stores_artifacts_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/artifacts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_artifacts_operations_get_execute()` or `aiplatform_metadata_stores_artifacts_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_artifacts_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_get_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_artifacts_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_artifacts_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_artifacts_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_artifacts_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresArtifactsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_artifacts_operations_get_builder()` + `aiplatform_metadata_stores_artifacts_operations_get_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresArtifactsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_artifacts_operations_get_builder(client, &args.name)?;
    aiplatform_metadata_stores_artifacts_operations_get_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_artifacts_operations_list_execute()` to send, or `aiplatform_metadata_stores_artifacts_operations_list` for simplest API.

pub fn aiplatform_metadata_stores_artifacts_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/artifacts/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_artifacts_operations_list_execute()` or `aiplatform_metadata_stores_artifacts_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_artifacts_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_list_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_artifacts_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_artifacts_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_artifacts_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_artifacts_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresArtifactsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_artifacts_operations_list_builder()` + `aiplatform_metadata_stores_artifacts_operations_list_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresArtifactsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_artifacts_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_metadata_stores_artifacts_operations_list_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_artifacts_operations_wait_execute()` to send, or `aiplatform_metadata_stores_artifacts_operations_wait` for simplest API.

pub fn aiplatform_metadata_stores_artifacts_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/artifacts/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_artifacts_operations_wait_execute()` or `aiplatform_metadata_stores_artifacts_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_artifacts_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_wait_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_artifacts_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_artifacts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_artifacts_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_artifacts_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_artifacts_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresArtifactsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_artifacts_operations_wait_builder()` + `aiplatform_metadata_stores_artifacts_operations_wait_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_artifacts_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_artifacts_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresArtifactsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_artifacts_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_metadata_stores_artifacts_operations_wait_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_contexts_operations_cancel_execute()` to send, or `aiplatform_metadata_stores_contexts_operations_cancel` for simplest API.

pub fn aiplatform_metadata_stores_contexts_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/contexts/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_contexts_operations_cancel_execute()` or `aiplatform_metadata_stores_contexts_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_contexts_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_contexts_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_contexts_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_contexts_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_contexts_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresContextsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_contexts_operations_cancel_builder()` + `aiplatform_metadata_stores_contexts_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresContextsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_metadata_stores_contexts_operations_cancel_builder(client, &args.name)?;
    aiplatform_metadata_stores_contexts_operations_cancel_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_contexts_operations_delete_execute()` to send, or `aiplatform_metadata_stores_contexts_operations_delete` for simplest API.

pub fn aiplatform_metadata_stores_contexts_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/contexts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_contexts_operations_delete_execute()` or `aiplatform_metadata_stores_contexts_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_contexts_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_delete_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_contexts_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_contexts_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_contexts_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_contexts_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresContextsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_contexts_operations_delete_builder()` + `aiplatform_metadata_stores_contexts_operations_delete_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresContextsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_metadata_stores_contexts_operations_delete_builder(client, &args.name)?;
    aiplatform_metadata_stores_contexts_operations_delete_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_contexts_operations_get_execute()` to send, or `aiplatform_metadata_stores_contexts_operations_get` for simplest API.

pub fn aiplatform_metadata_stores_contexts_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/contexts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_contexts_operations_get_execute()` or `aiplatform_metadata_stores_contexts_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_contexts_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_get_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_contexts_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_contexts_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_contexts_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_contexts_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresContextsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_contexts_operations_get_builder()` + `aiplatform_metadata_stores_contexts_operations_get_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresContextsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_contexts_operations_get_builder(client, &args.name)?;
    aiplatform_metadata_stores_contexts_operations_get_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_contexts_operations_list_execute()` to send, or `aiplatform_metadata_stores_contexts_operations_list` for simplest API.

pub fn aiplatform_metadata_stores_contexts_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/contexts/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_contexts_operations_list_execute()` or `aiplatform_metadata_stores_contexts_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_contexts_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_list_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_contexts_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_contexts_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_contexts_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_contexts_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresContextsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_contexts_operations_list_builder()` + `aiplatform_metadata_stores_contexts_operations_list_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresContextsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_contexts_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_metadata_stores_contexts_operations_list_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_contexts_operations_wait_execute()` to send, or `aiplatform_metadata_stores_contexts_operations_wait` for simplest API.

pub fn aiplatform_metadata_stores_contexts_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/contexts/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_contexts_operations_wait_execute()` or `aiplatform_metadata_stores_contexts_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_contexts_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_wait_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_contexts_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_contexts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_contexts_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_contexts_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_contexts_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresContextsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_contexts_operations_wait_builder()` + `aiplatform_metadata_stores_contexts_operations_wait_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_contexts_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_contexts_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresContextsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_contexts_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_metadata_stores_contexts_operations_wait_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_executions_operations_cancel_execute()` to send, or `aiplatform_metadata_stores_executions_operations_cancel` for simplest API.

pub fn aiplatform_metadata_stores_executions_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/executions/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_executions_operations_cancel_execute()` or `aiplatform_metadata_stores_executions_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_executions_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_executions_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_executions_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_executions_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_executions_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresExecutionsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_executions_operations_cancel_builder()` + `aiplatform_metadata_stores_executions_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresExecutionsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_metadata_stores_executions_operations_cancel_builder(client, &args.name)?;
    aiplatform_metadata_stores_executions_operations_cancel_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_executions_operations_delete_execute()` to send, or `aiplatform_metadata_stores_executions_operations_delete` for simplest API.

pub fn aiplatform_metadata_stores_executions_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/executions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_executions_operations_delete_execute()` or `aiplatform_metadata_stores_executions_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_executions_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_delete_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_executions_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_executions_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_executions_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_executions_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresExecutionsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_executions_operations_delete_builder()` + `aiplatform_metadata_stores_executions_operations_delete_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresExecutionsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_metadata_stores_executions_operations_delete_builder(client, &args.name)?;
    aiplatform_metadata_stores_executions_operations_delete_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_executions_operations_get_execute()` to send, or `aiplatform_metadata_stores_executions_operations_get` for simplest API.

pub fn aiplatform_metadata_stores_executions_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/executions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_executions_operations_get_execute()` or `aiplatform_metadata_stores_executions_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_executions_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_get_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_executions_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_executions_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_executions_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_executions_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresExecutionsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_executions_operations_get_builder()` + `aiplatform_metadata_stores_executions_operations_get_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresExecutionsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_executions_operations_get_builder(client, &args.name)?;
    aiplatform_metadata_stores_executions_operations_get_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_executions_operations_list_execute()` to send, or `aiplatform_metadata_stores_executions_operations_list` for simplest API.

pub fn aiplatform_metadata_stores_executions_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/executions/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_executions_operations_list_execute()` or `aiplatform_metadata_stores_executions_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_executions_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_list_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_executions_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_executions_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_executions_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_executions_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresExecutionsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_executions_operations_list_builder()` + `aiplatform_metadata_stores_executions_operations_list_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresExecutionsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_executions_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_metadata_stores_executions_operations_list_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_executions_operations_wait_execute()` to send, or `aiplatform_metadata_stores_executions_operations_wait` for simplest API.

pub fn aiplatform_metadata_stores_executions_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/executions/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_executions_operations_wait_execute()` or `aiplatform_metadata_stores_executions_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_executions_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_wait_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_executions_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_executions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_executions_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_executions_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_executions_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresExecutionsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_executions_operations_wait_builder()` + `aiplatform_metadata_stores_executions_operations_wait_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_executions_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_executions_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresExecutionsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_executions_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_metadata_stores_executions_operations_wait_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_operations_cancel_execute()` to send, or `aiplatform_metadata_stores_operations_cancel` for simplest API.

pub fn aiplatform_metadata_stores_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_operations_cancel_execute()` or `aiplatform_metadata_stores_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_operations_cancel_builder()` + `aiplatform_metadata_stores_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_operations_cancel_builder(client, &args.name)?;
    aiplatform_metadata_stores_operations_cancel_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_operations_delete_execute()` to send, or `aiplatform_metadata_stores_operations_delete` for simplest API.

pub fn aiplatform_metadata_stores_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_operations_delete_execute()` or `aiplatform_metadata_stores_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_operations_delete_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_operations_delete_builder()` + `aiplatform_metadata_stores_operations_delete_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_operations_delete_builder(client, &args.name)?;
    aiplatform_metadata_stores_operations_delete_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_operations_get_execute()` to send, or `aiplatform_metadata_stores_operations_get` for simplest API.

pub fn aiplatform_metadata_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_operations_get_execute()` or `aiplatform_metadata_stores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_operations_get_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_operations_get_builder()` + `aiplatform_metadata_stores_operations_get_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_operations_get_builder(client, &args.name)?;
    aiplatform_metadata_stores_operations_get_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_operations_list_execute()` to send, or `aiplatform_metadata_stores_operations_list` for simplest API.

pub fn aiplatform_metadata_stores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_operations_list_execute()` or `aiplatform_metadata_stores_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_operations_list_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_operations_list_builder()` + `aiplatform_metadata_stores_operations_list_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_metadata_stores_operations_list_execute(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_metadata_stores_operations_wait_execute()` to send, or `aiplatform_metadata_stores_operations_wait` for simplest API.

pub fn aiplatform_metadata_stores_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/metadataStores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_metadata_stores_operations_wait_execute()` or `aiplatform_metadata_stores_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_metadata_stores_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_metadata_stores_operations_wait_task()`.
/// For the simplest API, use `aiplatform_metadata_stores_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_metadata_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_metadata_stores_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_metadata_stores_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_metadata_stores_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMetadataStoresOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_metadata_stores_operations_wait_builder()` + `aiplatform_metadata_stores_operations_wait_execute()`.
/// For task-level control, use `aiplatform_metadata_stores_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_metadata_stores_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformMetadataStoresOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_metadata_stores_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_metadata_stores_operations_wait_execute(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_migratable_resources_operations_cancel_execute()` to send, or `aiplatform_migratable_resources_operations_cancel` for simplest API.

pub fn aiplatform_migratable_resources_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/migratableResources/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_migratable_resources_operations_cancel_execute()` or `aiplatform_migratable_resources_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_migratable_resources_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_migratable_resources_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_migratable_resources_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_migratable_resources_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_migratable_resources_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_migratable_resources_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMigratableResourcesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_migratable_resources_operations_cancel_builder()` + `aiplatform_migratable_resources_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_migratable_resources_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformMigratableResourcesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_migratable_resources_operations_cancel_builder(client, &args.name)?;
    aiplatform_migratable_resources_operations_cancel_execute(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_migratable_resources_operations_delete_execute()` to send, or `aiplatform_migratable_resources_operations_delete` for simplest API.

pub fn aiplatform_migratable_resources_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/migratableResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_migratable_resources_operations_delete_execute()` or `aiplatform_migratable_resources_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_migratable_resources_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_migratable_resources_operations_delete_task()`.
/// For the simplest API, use `aiplatform_migratable_resources_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_migratable_resources_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_migratable_resources_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_migratable_resources_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMigratableResourcesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_migratable_resources_operations_delete_builder()` + `aiplatform_migratable_resources_operations_delete_execute()`.
/// For task-level control, use `aiplatform_migratable_resources_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformMigratableResourcesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_migratable_resources_operations_delete_builder(client, &args.name)?;
    aiplatform_migratable_resources_operations_delete_execute(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_migratable_resources_operations_get_execute()` to send, or `aiplatform_migratable_resources_operations_get` for simplest API.

pub fn aiplatform_migratable_resources_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/migratableResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_migratable_resources_operations_get_execute()` or `aiplatform_migratable_resources_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_migratable_resources_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_migratable_resources_operations_get_task()`.
/// For the simplest API, use `aiplatform_migratable_resources_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_migratable_resources_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_migratable_resources_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_migratable_resources_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMigratableResourcesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_migratable_resources_operations_get_builder()` + `aiplatform_migratable_resources_operations_get_execute()`.
/// For task-level control, use `aiplatform_migratable_resources_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformMigratableResourcesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_migratable_resources_operations_get_builder(client, &args.name)?;
    aiplatform_migratable_resources_operations_get_execute(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_migratable_resources_operations_list_execute()` to send, or `aiplatform_migratable_resources_operations_list` for simplest API.

pub fn aiplatform_migratable_resources_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/migratableResources/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_migratable_resources_operations_list_execute()` or `aiplatform_migratable_resources_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_migratable_resources_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_migratable_resources_operations_list_task()`.
/// For the simplest API, use `aiplatform_migratable_resources_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_migratable_resources_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_migratable_resources_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_migratable_resources_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMigratableResourcesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_migratable_resources_operations_list_builder()` + `aiplatform_migratable_resources_operations_list_execute()`.
/// For task-level control, use `aiplatform_migratable_resources_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformMigratableResourcesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_migratable_resources_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_migratable_resources_operations_list_execute(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_migratable_resources_operations_wait_execute()` to send, or `aiplatform_migratable_resources_operations_wait` for simplest API.

pub fn aiplatform_migratable_resources_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/migratableResources/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_migratable_resources_operations_wait_execute()` or `aiplatform_migratable_resources_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_migratable_resources_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_migratable_resources_operations_wait_task()`.
/// For the simplest API, use `aiplatform_migratable_resources_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_migratable_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_migratable_resources_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_migratable_resources_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_migratable_resources_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformMigratableResourcesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_migratable_resources_operations_wait_builder()` + `aiplatform_migratable_resources_operations_wait_execute()`.
/// For task-level control, use `aiplatform_migratable_resources_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_migratable_resources_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformMigratableResourcesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_migratable_resources_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_migratable_resources_operations_wait_execute(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_model_deployment_monitoring_jobs_operations_cancel_execute()` to send, or `aiplatform_model_deployment_monitoring_jobs_operations_cancel` for simplest API.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/modelDeploymentMonitoringJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_model_deployment_monitoring_jobs_operations_cancel_execute()` or `aiplatform_model_deployment_monitoring_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_model_deployment_monitoring_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_model_deployment_monitoring_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_model_deployment_monitoring_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelDeploymentMonitoringJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder()` + `aiplatform_model_deployment_monitoring_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformModelDeploymentMonitoringJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_model_deployment_monitoring_jobs_operations_cancel_execute(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_model_deployment_monitoring_jobs_operations_delete_execute()` to send, or `aiplatform_model_deployment_monitoring_jobs_operations_delete` for simplest API.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/modelDeploymentMonitoringJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_model_deployment_monitoring_jobs_operations_delete_execute()` or `aiplatform_model_deployment_monitoring_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_model_deployment_monitoring_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_model_deployment_monitoring_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_model_deployment_monitoring_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_model_deployment_monitoring_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelDeploymentMonitoringJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_model_deployment_monitoring_jobs_operations_delete_builder()` + `aiplatform_model_deployment_monitoring_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformModelDeploymentMonitoringJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_model_deployment_monitoring_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_model_deployment_monitoring_jobs_operations_delete_execute(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_model_deployment_monitoring_jobs_operations_get_execute()` to send, or `aiplatform_model_deployment_monitoring_jobs_operations_get` for simplest API.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/modelDeploymentMonitoringJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_model_deployment_monitoring_jobs_operations_get_execute()` or `aiplatform_model_deployment_monitoring_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_model_deployment_monitoring_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_model_deployment_monitoring_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_model_deployment_monitoring_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_model_deployment_monitoring_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelDeploymentMonitoringJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_model_deployment_monitoring_jobs_operations_get_builder()` + `aiplatform_model_deployment_monitoring_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformModelDeploymentMonitoringJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_model_deployment_monitoring_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_model_deployment_monitoring_jobs_operations_get_execute(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_model_deployment_monitoring_jobs_operations_list_execute()` to send, or `aiplatform_model_deployment_monitoring_jobs_operations_list` for simplest API.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/modelDeploymentMonitoringJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_model_deployment_monitoring_jobs_operations_list_execute()` or `aiplatform_model_deployment_monitoring_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_model_deployment_monitoring_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_model_deployment_monitoring_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_model_deployment_monitoring_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_model_deployment_monitoring_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelDeploymentMonitoringJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_model_deployment_monitoring_jobs_operations_list_builder()` + `aiplatform_model_deployment_monitoring_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformModelDeploymentMonitoringJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_model_deployment_monitoring_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_model_deployment_monitoring_jobs_operations_list_execute(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_model_deployment_monitoring_jobs_operations_wait_execute()` to send, or `aiplatform_model_deployment_monitoring_jobs_operations_wait` for simplest API.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/modelDeploymentMonitoringJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_model_deployment_monitoring_jobs_operations_wait_execute()` or `aiplatform_model_deployment_monitoring_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_model_deployment_monitoring_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_model_deployment_monitoring_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_model_deployment_monitoring_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_model_deployment_monitoring_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_model_deployment_monitoring_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelDeploymentMonitoringJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_model_deployment_monitoring_jobs_operations_wait_builder()` + `aiplatform_model_deployment_monitoring_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_model_deployment_monitoring_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_model_deployment_monitoring_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformModelDeploymentMonitoringJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_model_deployment_monitoring_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_model_deployment_monitoring_jobs_operations_wait_execute(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_evaluations_operations_cancel_execute()` to send, or `aiplatform_models_evaluations_operations_cancel` for simplest API.

pub fn aiplatform_models_evaluations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/evaluations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_evaluations_operations_cancel_execute()` or `aiplatform_models_evaluations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_evaluations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_evaluations_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_models_evaluations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_evaluations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_evaluations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_evaluations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsEvaluationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_evaluations_operations_cancel_builder()` + `aiplatform_models_evaluations_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_models_evaluations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformModelsEvaluationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_evaluations_operations_cancel_builder(client, &args.name)?;
    aiplatform_models_evaluations_operations_cancel_execute(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_evaluations_operations_delete_execute()` to send, or `aiplatform_models_evaluations_operations_delete` for simplest API.

pub fn aiplatform_models_evaluations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/evaluations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_evaluations_operations_delete_execute()` or `aiplatform_models_evaluations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_evaluations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_evaluations_operations_delete_task()`.
/// For the simplest API, use `aiplatform_models_evaluations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_evaluations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_evaluations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_evaluations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsEvaluationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_evaluations_operations_delete_builder()` + `aiplatform_models_evaluations_operations_delete_execute()`.
/// For task-level control, use `aiplatform_models_evaluations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformModelsEvaluationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_evaluations_operations_delete_builder(client, &args.name)?;
    aiplatform_models_evaluations_operations_delete_execute(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_evaluations_operations_get_execute()` to send, or `aiplatform_models_evaluations_operations_get` for simplest API.

pub fn aiplatform_models_evaluations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/evaluations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_evaluations_operations_get_execute()` or `aiplatform_models_evaluations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_evaluations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_evaluations_operations_get_task()`.
/// For the simplest API, use `aiplatform_models_evaluations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_evaluations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_evaluations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_evaluations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsEvaluationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_evaluations_operations_get_builder()` + `aiplatform_models_evaluations_operations_get_execute()`.
/// For task-level control, use `aiplatform_models_evaluations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformModelsEvaluationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_evaluations_operations_get_builder(client, &args.name)?;
    aiplatform_models_evaluations_operations_get_execute(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_evaluations_operations_list_execute()` to send, or `aiplatform_models_evaluations_operations_list` for simplest API.

pub fn aiplatform_models_evaluations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/evaluations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_evaluations_operations_list_execute()` or `aiplatform_models_evaluations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_evaluations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_evaluations_operations_list_task()`.
/// For the simplest API, use `aiplatform_models_evaluations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_evaluations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_evaluations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_evaluations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsEvaluationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_evaluations_operations_list_builder()` + `aiplatform_models_evaluations_operations_list_execute()`.
/// For task-level control, use `aiplatform_models_evaluations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformModelsEvaluationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_evaluations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_models_evaluations_operations_list_execute(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_evaluations_operations_wait_execute()` to send, or `aiplatform_models_evaluations_operations_wait` for simplest API.

pub fn aiplatform_models_evaluations_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/evaluations/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_evaluations_operations_wait_execute()` or `aiplatform_models_evaluations_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_evaluations_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_evaluations_operations_wait_task()`.
/// For the simplest API, use `aiplatform_models_evaluations_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_evaluations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_evaluations_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_evaluations_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_evaluations_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsEvaluationsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_evaluations_operations_wait_builder()` + `aiplatform_models_evaluations_operations_wait_execute()`.
/// For task-level control, use `aiplatform_models_evaluations_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_evaluations_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformModelsEvaluationsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_evaluations_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_models_evaluations_operations_wait_execute(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_operations_cancel_execute()` to send, or `aiplatform_models_operations_cancel` for simplest API.

pub fn aiplatform_models_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_operations_cancel_execute()` or `aiplatform_models_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_models_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_operations_cancel_builder()` + `aiplatform_models_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_models_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformModelsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_operations_cancel_builder(client, &args.name)?;
    aiplatform_models_operations_cancel_execute(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_operations_delete_execute()` to send, or `aiplatform_models_operations_delete` for simplest API.

pub fn aiplatform_models_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_operations_delete_execute()` or `aiplatform_models_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_operations_delete_task()`.
/// For the simplest API, use `aiplatform_models_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_operations_delete_builder()` + `aiplatform_models_operations_delete_execute()`.
/// For task-level control, use `aiplatform_models_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformModelsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_operations_delete_builder(client, &args.name)?;
    aiplatform_models_operations_delete_execute(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_operations_get_execute()` to send, or `aiplatform_models_operations_get` for simplest API.

pub fn aiplatform_models_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_operations_get_execute()` or `aiplatform_models_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_operations_get_task()`.
/// For the simplest API, use `aiplatform_models_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_operations_get_builder()` + `aiplatform_models_operations_get_execute()`.
/// For task-level control, use `aiplatform_models_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformModelsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_operations_get_builder(client, &args.name)?;
    aiplatform_models_operations_get_execute(builder)
}

/// GET v1/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_operations_list_execute()` to send, or `aiplatform_models_operations_list` for simplest API.

pub fn aiplatform_models_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_operations_list_execute()` or `aiplatform_models_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_operations_list_task()`.
/// For the simplest API, use `aiplatform_models_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_operations_list_builder()` + `aiplatform_models_operations_list_execute()`.
/// For task-level control, use `aiplatform_models_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformModelsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_models_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_models_operations_list_execute(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_models_operations_wait_execute()` to send, or `aiplatform_models_operations_wait` for simplest API.

pub fn aiplatform_models_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/models/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_models_operations_wait_execute()` or `aiplatform_models_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_models_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_models_operations_wait_task()`.
/// For the simplest API, use `aiplatform_models_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_models_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_models_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_models_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_models_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformModelsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_models_operations_wait_builder()` + `aiplatform_models_operations_wait_execute()`.
/// For task-level control, use `aiplatform_models_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_models_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformModelsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_models_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_models_operations_wait_execute(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_execution_jobs_operations_cancel_execute()` to send, or `aiplatform_notebook_execution_jobs_operations_cancel` for simplest API.

pub fn aiplatform_notebook_execution_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookExecutionJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_execution_jobs_operations_cancel_execute()` or `aiplatform_notebook_execution_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_execution_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_notebook_execution_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_execution_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_execution_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_execution_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookExecutionJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_execution_jobs_operations_cancel_builder()` + `aiplatform_notebook_execution_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookExecutionJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_execution_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_notebook_execution_jobs_operations_cancel_execute(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_execution_jobs_operations_delete_execute()` to send, or `aiplatform_notebook_execution_jobs_operations_delete` for simplest API.

pub fn aiplatform_notebook_execution_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookExecutionJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_execution_jobs_operations_delete_execute()` or `aiplatform_notebook_execution_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_execution_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_notebook_execution_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_execution_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_execution_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_execution_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookExecutionJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_execution_jobs_operations_delete_builder()` + `aiplatform_notebook_execution_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookExecutionJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_execution_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_notebook_execution_jobs_operations_delete_execute(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_execution_jobs_operations_get_execute()` to send, or `aiplatform_notebook_execution_jobs_operations_get` for simplest API.

pub fn aiplatform_notebook_execution_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookExecutionJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_execution_jobs_operations_get_execute()` or `aiplatform_notebook_execution_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_execution_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_notebook_execution_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_execution_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_execution_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_execution_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookExecutionJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_execution_jobs_operations_get_builder()` + `aiplatform_notebook_execution_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookExecutionJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_execution_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_notebook_execution_jobs_operations_get_execute(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_execution_jobs_operations_list_execute()` to send, or `aiplatform_notebook_execution_jobs_operations_list` for simplest API.

pub fn aiplatform_notebook_execution_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookExecutionJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_execution_jobs_operations_list_execute()` or `aiplatform_notebook_execution_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_execution_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_notebook_execution_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_execution_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_execution_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_execution_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookExecutionJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_execution_jobs_operations_list_builder()` + `aiplatform_notebook_execution_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookExecutionJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_execution_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_notebook_execution_jobs_operations_list_execute(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_execution_jobs_operations_wait_execute()` to send, or `aiplatform_notebook_execution_jobs_operations_wait` for simplest API.

pub fn aiplatform_notebook_execution_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookExecutionJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_execution_jobs_operations_wait_execute()` or `aiplatform_notebook_execution_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_execution_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_notebook_execution_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_execution_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_execution_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_execution_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_execution_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookExecutionJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_execution_jobs_operations_wait_builder()` + `aiplatform_notebook_execution_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_notebook_execution_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_execution_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookExecutionJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_execution_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_notebook_execution_jobs_operations_wait_execute(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtime_templates_operations_cancel_execute()` to send, or `aiplatform_notebook_runtime_templates_operations_cancel` for simplest API.

pub fn aiplatform_notebook_runtime_templates_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimeTemplates/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtime_templates_operations_cancel_execute()` or `aiplatform_notebook_runtime_templates_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtime_templates_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_notebook_runtime_templates_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtime_templates_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtime_templates_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtime_templates_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimeTemplatesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtime_templates_operations_cancel_builder()` + `aiplatform_notebook_runtime_templates_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimeTemplatesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_notebook_runtime_templates_operations_cancel_builder(client, &args.name)?;
    aiplatform_notebook_runtime_templates_operations_cancel_execute(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtime_templates_operations_delete_execute()` to send, or `aiplatform_notebook_runtime_templates_operations_delete` for simplest API.

pub fn aiplatform_notebook_runtime_templates_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimeTemplates/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtime_templates_operations_delete_execute()` or `aiplatform_notebook_runtime_templates_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtime_templates_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_delete_task()`.
/// For the simplest API, use `aiplatform_notebook_runtime_templates_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtime_templates_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtime_templates_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtime_templates_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimeTemplatesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtime_templates_operations_delete_builder()` + `aiplatform_notebook_runtime_templates_operations_delete_execute()`.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimeTemplatesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_notebook_runtime_templates_operations_delete_builder(client, &args.name)?;
    aiplatform_notebook_runtime_templates_operations_delete_execute(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtime_templates_operations_get_execute()` to send, or `aiplatform_notebook_runtime_templates_operations_get` for simplest API.

pub fn aiplatform_notebook_runtime_templates_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimeTemplates/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtime_templates_operations_get_execute()` or `aiplatform_notebook_runtime_templates_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtime_templates_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_get_task()`.
/// For the simplest API, use `aiplatform_notebook_runtime_templates_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtime_templates_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtime_templates_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtime_templates_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimeTemplatesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtime_templates_operations_get_builder()` + `aiplatform_notebook_runtime_templates_operations_get_execute()`.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimeTemplatesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtime_templates_operations_get_builder(client, &args.name)?;
    aiplatform_notebook_runtime_templates_operations_get_execute(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtime_templates_operations_list_execute()` to send, or `aiplatform_notebook_runtime_templates_operations_list` for simplest API.

pub fn aiplatform_notebook_runtime_templates_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimeTemplates/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtime_templates_operations_list_execute()` or `aiplatform_notebook_runtime_templates_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtime_templates_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_list_task()`.
/// For the simplest API, use `aiplatform_notebook_runtime_templates_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtime_templates_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtime_templates_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtime_templates_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimeTemplatesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtime_templates_operations_list_builder()` + `aiplatform_notebook_runtime_templates_operations_list_execute()`.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimeTemplatesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtime_templates_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_notebook_runtime_templates_operations_list_execute(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtime_templates_operations_wait_execute()` to send, or `aiplatform_notebook_runtime_templates_operations_wait` for simplest API.

pub fn aiplatform_notebook_runtime_templates_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimeTemplates/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtime_templates_operations_wait_execute()` or `aiplatform_notebook_runtime_templates_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtime_templates_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_wait_task()`.
/// For the simplest API, use `aiplatform_notebook_runtime_templates_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtime_templates_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtime_templates_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtime_templates_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtime_templates_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimeTemplatesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtime_templates_operations_wait_builder()` + `aiplatform_notebook_runtime_templates_operations_wait_execute()`.
/// For task-level control, use `aiplatform_notebook_runtime_templates_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtime_templates_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimeTemplatesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtime_templates_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_notebook_runtime_templates_operations_wait_execute(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtimes_operations_cancel_execute()` to send, or `aiplatform_notebook_runtimes_operations_cancel` for simplest API.

pub fn aiplatform_notebook_runtimes_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimes/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtimes_operations_cancel_execute()` or `aiplatform_notebook_runtimes_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtimes_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_notebook_runtimes_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtimes_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtimes_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtimes_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtimes_operations_cancel_builder()` + `aiplatform_notebook_runtimes_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtimes_operations_cancel_builder(client, &args.name)?;
    aiplatform_notebook_runtimes_operations_cancel_execute(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtimes_operations_delete_execute()` to send, or `aiplatform_notebook_runtimes_operations_delete` for simplest API.

pub fn aiplatform_notebook_runtimes_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtimes_operations_delete_execute()` or `aiplatform_notebook_runtimes_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtimes_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_delete_task()`.
/// For the simplest API, use `aiplatform_notebook_runtimes_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtimes_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtimes_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtimes_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtimes_operations_delete_builder()` + `aiplatform_notebook_runtimes_operations_delete_execute()`.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtimes_operations_delete_builder(client, &args.name)?;
    aiplatform_notebook_runtimes_operations_delete_execute(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtimes_operations_get_execute()` to send, or `aiplatform_notebook_runtimes_operations_get` for simplest API.

pub fn aiplatform_notebook_runtimes_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtimes_operations_get_execute()` or `aiplatform_notebook_runtimes_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtimes_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_get_task()`.
/// For the simplest API, use `aiplatform_notebook_runtimes_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtimes_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtimes_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtimes_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtimes_operations_get_builder()` + `aiplatform_notebook_runtimes_operations_get_execute()`.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtimes_operations_get_builder(client, &args.name)?;
    aiplatform_notebook_runtimes_operations_get_execute(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtimes_operations_list_execute()` to send, or `aiplatform_notebook_runtimes_operations_list` for simplest API.

pub fn aiplatform_notebook_runtimes_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimes/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtimes_operations_list_execute()` or `aiplatform_notebook_runtimes_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtimes_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_list_task()`.
/// For the simplest API, use `aiplatform_notebook_runtimes_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtimes_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtimes_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtimes_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtimes_operations_list_builder()` + `aiplatform_notebook_runtimes_operations_list_execute()`.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtimes_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_notebook_runtimes_operations_list_execute(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_notebook_runtimes_operations_wait_execute()` to send, or `aiplatform_notebook_runtimes_operations_wait` for simplest API.

pub fn aiplatform_notebook_runtimes_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/notebookRuntimes/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_notebook_runtimes_operations_wait_execute()` or `aiplatform_notebook_runtimes_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_notebook_runtimes_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_wait_task()`.
/// For the simplest API, use `aiplatform_notebook_runtimes_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_notebook_runtimes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_notebook_runtimes_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_notebook_runtimes_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_notebook_runtimes_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformNotebookRuntimesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_notebook_runtimes_operations_wait_builder()` + `aiplatform_notebook_runtimes_operations_wait_execute()`.
/// For task-level control, use `aiplatform_notebook_runtimes_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_notebook_runtimes_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformNotebookRuntimesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_notebook_runtimes_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_notebook_runtimes_operations_wait_execute(builder)
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_operations_cancel_execute()` to send, or `aiplatform_operations_cancel` for simplest API.

pub fn aiplatform_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_operations_cancel_execute()` or `aiplatform_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_operations_cancel_builder()` + `aiplatform_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_operations_cancel_builder(client, &args.name)?;
    aiplatform_operations_cancel_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_operations_delete_execute()` to send, or `aiplatform_operations_delete` for simplest API.

pub fn aiplatform_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_operations_delete_execute()` or `aiplatform_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_operations_delete_task()`.
/// For the simplest API, use `aiplatform_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_operations_delete_builder()` + `aiplatform_operations_delete_execute()`.
/// For task-level control, use `aiplatform_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_operations_delete_builder(client, &args.name)?;
    aiplatform_operations_delete_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_operations_get_execute()` to send, or `aiplatform_operations_get` for simplest API.

pub fn aiplatform_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_operations_get_execute()` or `aiplatform_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_operations_get_task()`.
/// For the simplest API, use `aiplatform_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_operations_get_builder()` + `aiplatform_operations_get_execute()`.
/// For task-level control, use `aiplatform_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_operations_get_builder(client, &args.name)?;
    aiplatform_operations_get_execute(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_operations_list_execute()` to send, or `aiplatform_operations_list` for simplest API.

pub fn aiplatform_operations_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_operations_list_execute()` or `aiplatform_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_operations_list_task()`.
/// For the simplest API, use `aiplatform_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformOperationsListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_operations_list_builder()` + `aiplatform_operations_list_execute()`.
/// For task-level control, use `aiplatform_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_operations_list_builder(
        client,
        args.filter.as_deref(),
        args.name.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_operations_list_execute(builder)
}

/// GET v1/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_operations_wait_execute()` to send, or `aiplatform_operations_wait` for simplest API.

pub fn aiplatform_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_operations_wait_execute()` or `aiplatform_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_operations_wait_task()`.
/// For the simplest API, use `aiplatform_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_operations_wait_builder()` + `aiplatform_operations_wait_execute()`.
/// For task-level control, use `aiplatform_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_operations_wait_execute(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_persistent_resources_operations_cancel_execute()` to send, or `aiplatform_persistent_resources_operations_cancel` for simplest API.

pub fn aiplatform_persistent_resources_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/persistentResources/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_persistent_resources_operations_cancel_execute()` or `aiplatform_persistent_resources_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_persistent_resources_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_persistent_resources_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_persistent_resources_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_persistent_resources_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_persistent_resources_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_persistent_resources_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPersistentResourcesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_persistent_resources_operations_cancel_builder()` + `aiplatform_persistent_resources_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_persistent_resources_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformPersistentResourcesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_persistent_resources_operations_cancel_builder(client, &args.name)?;
    aiplatform_persistent_resources_operations_cancel_execute(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_persistent_resources_operations_delete_execute()` to send, or `aiplatform_persistent_resources_operations_delete` for simplest API.

pub fn aiplatform_persistent_resources_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/persistentResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_persistent_resources_operations_delete_execute()` or `aiplatform_persistent_resources_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_persistent_resources_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_persistent_resources_operations_delete_task()`.
/// For the simplest API, use `aiplatform_persistent_resources_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_persistent_resources_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_persistent_resources_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_persistent_resources_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPersistentResourcesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_persistent_resources_operations_delete_builder()` + `aiplatform_persistent_resources_operations_delete_execute()`.
/// For task-level control, use `aiplatform_persistent_resources_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformPersistentResourcesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_persistent_resources_operations_delete_builder(client, &args.name)?;
    aiplatform_persistent_resources_operations_delete_execute(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_persistent_resources_operations_get_execute()` to send, or `aiplatform_persistent_resources_operations_get` for simplest API.

pub fn aiplatform_persistent_resources_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/persistentResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_persistent_resources_operations_get_execute()` or `aiplatform_persistent_resources_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_persistent_resources_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_persistent_resources_operations_get_task()`.
/// For the simplest API, use `aiplatform_persistent_resources_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_persistent_resources_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_persistent_resources_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_persistent_resources_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPersistentResourcesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_persistent_resources_operations_get_builder()` + `aiplatform_persistent_resources_operations_get_execute()`.
/// For task-level control, use `aiplatform_persistent_resources_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformPersistentResourcesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_persistent_resources_operations_get_builder(client, &args.name)?;
    aiplatform_persistent_resources_operations_get_execute(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_persistent_resources_operations_list_execute()` to send, or `aiplatform_persistent_resources_operations_list` for simplest API.

pub fn aiplatform_persistent_resources_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/persistentResources/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_persistent_resources_operations_list_execute()` or `aiplatform_persistent_resources_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_persistent_resources_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_persistent_resources_operations_list_task()`.
/// For the simplest API, use `aiplatform_persistent_resources_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_persistent_resources_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_persistent_resources_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_persistent_resources_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPersistentResourcesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_persistent_resources_operations_list_builder()` + `aiplatform_persistent_resources_operations_list_execute()`.
/// For task-level control, use `aiplatform_persistent_resources_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformPersistentResourcesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_persistent_resources_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_persistent_resources_operations_list_execute(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_persistent_resources_operations_wait_execute()` to send, or `aiplatform_persistent_resources_operations_wait` for simplest API.

pub fn aiplatform_persistent_resources_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/persistentResources/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_persistent_resources_operations_wait_execute()` or `aiplatform_persistent_resources_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_persistent_resources_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_persistent_resources_operations_wait_task()`.
/// For the simplest API, use `aiplatform_persistent_resources_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_persistent_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_persistent_resources_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_persistent_resources_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_persistent_resources_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPersistentResourcesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_persistent_resources_operations_wait_builder()` + `aiplatform_persistent_resources_operations_wait_execute()`.
/// For task-level control, use `aiplatform_persistent_resources_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_persistent_resources_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformPersistentResourcesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_persistent_resources_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_persistent_resources_operations_wait_execute(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_pipeline_jobs_operations_cancel_execute()` to send, or `aiplatform_pipeline_jobs_operations_cancel` for simplest API.

pub fn aiplatform_pipeline_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/pipelineJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_pipeline_jobs_operations_cancel_execute()` or `aiplatform_pipeline_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_pipeline_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_pipeline_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_pipeline_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_pipeline_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_pipeline_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPipelineJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_pipeline_jobs_operations_cancel_builder()` + `aiplatform_pipeline_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformPipelineJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_pipeline_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_pipeline_jobs_operations_cancel_execute(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_pipeline_jobs_operations_delete_execute()` to send, or `aiplatform_pipeline_jobs_operations_delete` for simplest API.

pub fn aiplatform_pipeline_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/pipelineJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_pipeline_jobs_operations_delete_execute()` or `aiplatform_pipeline_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_pipeline_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_pipeline_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_pipeline_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_pipeline_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_pipeline_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPipelineJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_pipeline_jobs_operations_delete_builder()` + `aiplatform_pipeline_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformPipelineJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_pipeline_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_pipeline_jobs_operations_delete_execute(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_pipeline_jobs_operations_get_execute()` to send, or `aiplatform_pipeline_jobs_operations_get` for simplest API.

pub fn aiplatform_pipeline_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/pipelineJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_pipeline_jobs_operations_get_execute()` or `aiplatform_pipeline_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_pipeline_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_pipeline_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_pipeline_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_pipeline_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_pipeline_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPipelineJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_pipeline_jobs_operations_get_builder()` + `aiplatform_pipeline_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformPipelineJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_pipeline_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_pipeline_jobs_operations_get_execute(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_pipeline_jobs_operations_list_execute()` to send, or `aiplatform_pipeline_jobs_operations_list` for simplest API.

pub fn aiplatform_pipeline_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/pipelineJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_pipeline_jobs_operations_list_execute()` or `aiplatform_pipeline_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_pipeline_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_pipeline_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_pipeline_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_pipeline_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_pipeline_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPipelineJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_pipeline_jobs_operations_list_builder()` + `aiplatform_pipeline_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformPipelineJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_pipeline_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_pipeline_jobs_operations_list_execute(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_pipeline_jobs_operations_wait_execute()` to send, or `aiplatform_pipeline_jobs_operations_wait` for simplest API.

pub fn aiplatform_pipeline_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/pipelineJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_pipeline_jobs_operations_wait_execute()` or `aiplatform_pipeline_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_pipeline_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_pipeline_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_pipeline_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_pipeline_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_pipeline_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_pipeline_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPipelineJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_pipeline_jobs_operations_wait_builder()` + `aiplatform_pipeline_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_pipeline_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_pipeline_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformPipelineJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_pipeline_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_pipeline_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Gets a GenAI cache config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_get_cache_config_execute()` to send, or `aiplatform_projects_get_cache_config` for simplest API.

pub fn aiplatform_projects_get_cache_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/cacheConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Gets a GenAI cache config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_get_cache_config_execute()` or `aiplatform_projects_get_cache_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_get_cache_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_get_cache_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CacheConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CacheConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Gets a GenAI cache config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_get_cache_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_get_cache_config_task()`.
/// For the simplest API, use `aiplatform_projects_get_cache_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_get_cache_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_get_cache_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CacheConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_get_cache_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_get_cache_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsGetCacheConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Gets a GenAI cache config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_get_cache_config_builder()` + `aiplatform_projects_get_cache_config_execute()`.
/// For task-level control, use `aiplatform_projects_get_cache_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_get_cache_config(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsGetCacheConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CacheConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_get_cache_config_builder(client, &args.name)?;
    aiplatform_projects_get_cache_config_execute(builder)
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Updates a cache config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_update_cache_config_execute()` to send, or `aiplatform_projects_update_cache_config` for simplest API.

pub fn aiplatform_projects_update_cache_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CacheConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/cacheConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Updates a cache config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_update_cache_config_execute()` or `aiplatform_projects_update_cache_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_update_cache_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_update_cache_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Updates a cache config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_update_cache_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_update_cache_config_task()`.
/// For the simplest API, use `aiplatform_projects_update_cache_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_update_cache_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_update_cache_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_update_cache_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_update_cache_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsUpdateCacheConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CacheConfig,
}

/// GET v1/projects/{projectsId}/cacheConfig
/// Updates a cache config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_update_cache_config_builder()` + `aiplatform_projects_update_cache_config_execute()`.
/// For task-level control, use `aiplatform_projects_update_cache_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_update_cache_config(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsUpdateCacheConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_update_cache_config_builder(client, &args.name, &args.body)?;
    aiplatform_projects_update_cache_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:askContexts
/// Agentic Retrieval Ask API for RAG.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_ask_contexts_execute()` to send, or `aiplatform_projects_locations_ask_contexts` for simplest API.

pub fn aiplatform_projects_locations_ask_contexts_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1AskContextsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:askContexts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:askContexts
/// Agentic Retrieval Ask API for RAG.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_ask_contexts_execute()` or `aiplatform_projects_locations_ask_contexts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_ask_contexts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_ask_contexts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AskContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AskContextsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:askContexts
/// Agentic Retrieval Ask API for RAG.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_ask_contexts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_ask_contexts_task()`.
/// For the simplest API, use `aiplatform_projects_locations_ask_contexts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_ask_contexts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_ask_contexts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AskContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_ask_contexts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_ask_contexts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsAskContextsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AskContextsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:askContexts
/// Agentic Retrieval Ask API for RAG.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_ask_contexts_builder()` + `aiplatform_projects_locations_ask_contexts_execute()`.
/// For task-level control, use `aiplatform_projects_locations_ask_contexts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_ask_contexts(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsAskContextsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AskContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_ask_contexts_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_ask_contexts_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:asyncRetrieveContexts
/// Asynchronous API to retrieves relevant contexts for a query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_async_retrieve_contexts_execute()` to send, or `aiplatform_projects_locations_async_retrieve_contexts` for simplest API.

pub fn aiplatform_projects_locations_async_retrieve_contexts_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1AsyncRetrieveContextsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:asyncRetrieveContexts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:asyncRetrieveContexts
/// Asynchronous API to retrieves relevant contexts for a query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_async_retrieve_contexts_execute()` or `aiplatform_projects_locations_async_retrieve_contexts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_async_retrieve_contexts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_async_retrieve_contexts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:asyncRetrieveContexts
/// Asynchronous API to retrieves relevant contexts for a query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_async_retrieve_contexts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_async_retrieve_contexts_task()`.
/// For the simplest API, use `aiplatform_projects_locations_async_retrieve_contexts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_async_retrieve_contexts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_async_retrieve_contexts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_async_retrieve_contexts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_async_retrieve_contexts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsAsyncRetrieveContextsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AsyncRetrieveContextsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:asyncRetrieveContexts
/// Asynchronous API to retrieves relevant contexts for a query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_async_retrieve_contexts_builder()` + `aiplatform_projects_locations_async_retrieve_contexts_execute()`.
/// For task-level control, use `aiplatform_projects_locations_async_retrieve_contexts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_async_retrieve_contexts(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsAsyncRetrieveContextsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_async_retrieve_contexts_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_async_retrieve_contexts_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:augmentPrompt
/// Given an input prompt, it returns augmented prompt from vertex rag store to guide LLM towards generating grounded responses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_augment_prompt_execute()` to send, or `aiplatform_projects_locations_augment_prompt` for simplest API.

pub fn aiplatform_projects_locations_augment_prompt_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1AugmentPromptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:augmentPrompt",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:augmentPrompt
/// Given an input prompt, it returns augmented prompt from vertex rag store to guide LLM towards generating grounded responses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_augment_prompt_execute()` or `aiplatform_projects_locations_augment_prompt`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_augment_prompt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_augment_prompt_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AugmentPromptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AugmentPromptResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:augmentPrompt
/// Given an input prompt, it returns augmented prompt from vertex rag store to guide LLM towards generating grounded responses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_augment_prompt_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_augment_prompt_task()`.
/// For the simplest API, use `aiplatform_projects_locations_augment_prompt()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_augment_prompt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_augment_prompt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AugmentPromptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_augment_prompt_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_augment_prompt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsAugmentPromptArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AugmentPromptRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:augmentPrompt
/// Given an input prompt, it returns augmented prompt from vertex rag store to guide LLM towards generating grounded responses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_augment_prompt_builder()` + `aiplatform_projects_locations_augment_prompt_execute()`.
/// For task-level control, use `aiplatform_projects_locations_augment_prompt_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_augment_prompt(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsAugmentPromptArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AugmentPromptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_augment_prompt_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_augment_prompt_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:corroborateContent
/// Given an input text, it returns a score that evaluates the factuality of the text. It also extracts and returns claims from the text and provides supporting facts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_corroborate_content_execute()` to send, or `aiplatform_projects_locations_corroborate_content` for simplest API.

pub fn aiplatform_projects_locations_corroborate_content_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1CorroborateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:corroborateContent",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:corroborateContent
/// Given an input text, it returns a score that evaluates the factuality of the text. It also extracts and returns claims from the text and provides supporting facts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_corroborate_content_execute()` or `aiplatform_projects_locations_corroborate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_corroborate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_corroborate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CorroborateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CorroborateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:corroborateContent
/// Given an input text, it returns a score that evaluates the factuality of the text. It also extracts and returns claims from the text and provides supporting facts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_corroborate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_corroborate_content_task()`.
/// For the simplest API, use `aiplatform_projects_locations_corroborate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_corroborate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_corroborate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CorroborateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_corroborate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_corroborate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCorroborateContentArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CorroborateContentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:corroborateContent
/// Given an input text, it returns a score that evaluates the factuality of the text. It also extracts and returns claims from the text and provides supporting facts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_corroborate_content_builder()` + `aiplatform_projects_locations_corroborate_content_execute()`.
/// For task-level control, use `aiplatform_projects_locations_corroborate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_corroborate_content(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCorroborateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CorroborateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_corroborate_content_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_corroborate_content_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:deploy
/// Deploys a model to a new endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deploy_execute()` to send, or `aiplatform_projects_locations_deploy` for simplest API.

pub fn aiplatform_projects_locations_deploy_builder(
    client: &SimpleHttpClient,
    destination: &str,
    body: &GoogleCloudAiplatformV1DeployRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:deploy",
        destination,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:deploy
/// Deploys a model to a new endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deploy_execute()` or `aiplatform_projects_locations_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:deploy
/// Deploys a model to a new endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deploy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeployArgs {
    /// Path parameter: destination
    pub destination: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DeployRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:deploy
/// Deploys a model to a new endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deploy_builder()` + `aiplatform_projects_locations_deploy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deploy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_deploy_builder(client, &args.destination, &args.body)?;
    aiplatform_projects_locations_deploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluate_dataset_execute()` to send, or `aiplatform_projects_locations_evaluate_dataset` for simplest API.

pub fn aiplatform_projects_locations_evaluate_dataset_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudAiplatformV1EvaluateDatasetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:evaluateDataset",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluate_dataset_execute()` or `aiplatform_projects_locations_evaluate_dataset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluate_dataset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluate_dataset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluate_dataset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluate_dataset_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluate_dataset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluate_dataset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluate_dataset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluate_dataset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluate_dataset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluateDatasetArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluateDatasetRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluate_dataset_builder()` + `aiplatform_projects_locations_evaluate_dataset_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluate_dataset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluate_dataset(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluateDatasetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_evaluate_dataset_builder(client, &args.location, &args.body)?;
    aiplatform_projects_locations_evaluate_dataset_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluate_instances_execute()` to send, or `aiplatform_projects_locations_evaluate_instances` for simplest API.

pub fn aiplatform_projects_locations_evaluate_instances_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudAiplatformV1EvaluateInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:evaluateInstances",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluate_instances_execute()` or `aiplatform_projects_locations_evaluate_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluate_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluateInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluateInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluate_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluate_instances_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluate_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluate_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluateInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluate_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluate_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluateInstancesArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluateInstancesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluate_instances_builder()` + `aiplatform_projects_locations_evaluate_instances_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluate_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluate_instances(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluateInstancesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluateInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluate_instances_builder(
        client,
        &args.location,
        &args.body,
    )?;
    aiplatform_projects_locations_evaluate_instances_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_generate_instance_rubrics_execute()` to send, or `aiplatform_projects_locations_generate_instance_rubrics` for simplest API.

pub fn aiplatform_projects_locations_generate_instance_rubrics_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudAiplatformV1GenerateInstanceRubricsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:generateInstanceRubrics",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_generate_instance_rubrics_execute()` or `aiplatform_projects_locations_generate_instance_rubrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_generate_instance_rubrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_generate_instance_rubrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateInstanceRubricsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_generate_instance_rubrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_generate_instance_rubrics_task()`.
/// For the simplest API, use `aiplatform_projects_locations_generate_instance_rubrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_generate_instance_rubrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_generate_instance_rubrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_generate_instance_rubrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_generate_instance_rubrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsGenerateInstanceRubricsArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateInstanceRubricsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_generate_instance_rubrics_builder()` + `aiplatform_projects_locations_generate_instance_rubrics_execute()`.
/// For task-level control, use `aiplatform_projects_locations_generate_instance_rubrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_generate_instance_rubrics(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsGenerateInstanceRubricsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_generate_instance_rubrics_builder(
        client,
        &args.location,
        &args.body,
    )?;
    aiplatform_projects_locations_generate_instance_rubrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateSyntheticData
/// Generates synthetic (artificial) data based on a description
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_generate_synthetic_data_execute()` to send, or `aiplatform_projects_locations_generate_synthetic_data` for simplest API.

pub fn aiplatform_projects_locations_generate_synthetic_data_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudAiplatformV1GenerateSyntheticDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:generateSyntheticData",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateSyntheticData
/// Generates synthetic (artificial) data based on a description
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_generate_synthetic_data_execute()` or `aiplatform_projects_locations_generate_synthetic_data`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_generate_synthetic_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_generate_synthetic_data_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateSyntheticDataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateSyntheticDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateSyntheticData
/// Generates synthetic (artificial) data based on a description
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_generate_synthetic_data_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_generate_synthetic_data_task()`.
/// For the simplest API, use `aiplatform_projects_locations_generate_synthetic_data()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_generate_synthetic_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_generate_synthetic_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateSyntheticDataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_generate_synthetic_data_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_generate_synthetic_data`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsGenerateSyntheticDataArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateSyntheticDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateSyntheticData
/// Generates synthetic (artificial) data based on a description
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_generate_synthetic_data_builder()` + `aiplatform_projects_locations_generate_synthetic_data_execute()`.
/// For task-level control, use `aiplatform_projects_locations_generate_synthetic_data_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_generate_synthetic_data(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsGenerateSyntheticDataArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateSyntheticDataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_generate_synthetic_data_builder(
        client,
        &args.location,
        &args.body,
    )?;
    aiplatform_projects_locations_generate_synthetic_data_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_get_execute()` to send, or `aiplatform_projects_locations_get` for simplest API.

pub fn aiplatform_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_get_execute()` or `aiplatform_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_get_builder()` + `aiplatform_projects_locations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Gets a RagEngineConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_get_rag_engine_config_execute()` to send, or `aiplatform_projects_locations_get_rag_engine_config` for simplest API.

pub fn aiplatform_projects_locations_get_rag_engine_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Gets a RagEngineConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_get_rag_engine_config_execute()` or `aiplatform_projects_locations_get_rag_engine_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_get_rag_engine_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_get_rag_engine_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagEngineConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RagEngineConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Gets a RagEngineConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_get_rag_engine_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_get_rag_engine_config_task()`.
/// For the simplest API, use `aiplatform_projects_locations_get_rag_engine_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_get_rag_engine_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_get_rag_engine_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagEngineConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_get_rag_engine_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_get_rag_engine_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsGetRagEngineConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Gets a RagEngineConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_get_rag_engine_config_builder()` + `aiplatform_projects_locations_get_rag_engine_config_execute()`.
/// For task-level control, use `aiplatform_projects_locations_get_rag_engine_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_get_rag_engine_config(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsGetRagEngineConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagEngineConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_get_rag_engine_config_builder(client, &args.name)?;
    aiplatform_projects_locations_get_rag_engine_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_list_execute()` to send, or `aiplatform_projects_locations_list` for simplest API.

pub fn aiplatform_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_list_execute()` or `aiplatform_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_list_builder()` + `aiplatform_projects_locations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:retrieveContexts
/// Retrieves relevant contexts for a query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_retrieve_contexts_execute()` to send, or `aiplatform_projects_locations_retrieve_contexts` for simplest API.

pub fn aiplatform_projects_locations_retrieve_contexts_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1RetrieveContextsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}:retrieveContexts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:retrieveContexts
/// Retrieves relevant contexts for a query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_retrieve_contexts_execute()` or `aiplatform_projects_locations_retrieve_contexts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_retrieve_contexts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_retrieve_contexts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RetrieveContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RetrieveContextsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:retrieveContexts
/// Retrieves relevant contexts for a query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_retrieve_contexts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_retrieve_contexts_task()`.
/// For the simplest API, use `aiplatform_projects_locations_retrieve_contexts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_retrieve_contexts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_retrieve_contexts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RetrieveContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_retrieve_contexts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_retrieve_contexts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRetrieveContextsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RetrieveContextsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:retrieveContexts
/// Retrieves relevant contexts for a query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_retrieve_contexts_builder()` + `aiplatform_projects_locations_retrieve_contexts_execute()`.
/// For task-level control, use `aiplatform_projects_locations_retrieve_contexts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_retrieve_contexts(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRetrieveContextsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RetrieveContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_retrieve_contexts_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_retrieve_contexts_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Updates a RagEngineConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_update_rag_engine_config_execute()` to send, or `aiplatform_projects_locations_update_rag_engine_config` for simplest API.

pub fn aiplatform_projects_locations_update_rag_engine_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1RagEngineConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Updates a RagEngineConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_update_rag_engine_config_execute()` or `aiplatform_projects_locations_update_rag_engine_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_update_rag_engine_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_update_rag_engine_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Updates a RagEngineConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_update_rag_engine_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_update_rag_engine_config_task()`.
/// For the simplest API, use `aiplatform_projects_locations_update_rag_engine_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_update_rag_engine_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_update_rag_engine_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_update_rag_engine_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_update_rag_engine_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsUpdateRagEngineConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RagEngineConfig,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig
/// Updates a RagEngineConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_update_rag_engine_config_builder()` + `aiplatform_projects_locations_update_rag_engine_config_execute()`.
/// For task-level control, use `aiplatform_projects_locations_update_rag_engine_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_update_rag_engine_config(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsUpdateRagEngineConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_update_rag_engine_config_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_update_rag_engine_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}:cancel
/// Cancels a BatchPredictionJob. Starts asynchronous cancellation on the BatchPredictionJob. The server makes the best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetBatchPredictionJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On a successful cancellation, the BatchPredictionJob is not deleted;instead its BatchPredictionJob.state is set to CANCELLED. Any files already outputted by the job are not deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_batch_prediction_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_batch_prediction_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_batch_prediction_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelBatchPredictionJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/batchPredictionJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}:cancel
/// Cancels a BatchPredictionJob. Starts asynchronous cancellation on the BatchPredictionJob. The server makes the best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetBatchPredictionJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On a successful cancellation, the BatchPredictionJob is not deleted;instead its BatchPredictionJob.state is set to CANCELLED. Any files already outputted by the job are not deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_batch_prediction_jobs_cancel_execute()` or `aiplatform_projects_locations_batch_prediction_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}:cancel
/// Cancels a BatchPredictionJob. Starts asynchronous cancellation on the BatchPredictionJob. The server makes the best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetBatchPredictionJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On a successful cancellation, the BatchPredictionJob is not deleted;instead its BatchPredictionJob.state is set to CANCELLED. Any files already outputted by the job are not deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_batch_prediction_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_batch_prediction_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_batch_prediction_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_batch_prediction_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_batch_prediction_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsBatchPredictionJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelBatchPredictionJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}:cancel
/// Cancels a BatchPredictionJob. Starts asynchronous cancellation on the BatchPredictionJob. The server makes the best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetBatchPredictionJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On a successful cancellation, the BatchPredictionJob is not deleted;instead its BatchPredictionJob.state is set to CANCELLED. Any files already outputted by the job are not deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_batch_prediction_jobs_cancel_builder()` + `aiplatform_projects_locations_batch_prediction_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsBatchPredictionJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_batch_prediction_jobs_cancel_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_batch_prediction_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_batch_prediction_jobs_create_execute()` to send, or `aiplatform_projects_locations_batch_prediction_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_batch_prediction_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchPredictionJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/batchPredictionJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_batch_prediction_jobs_create_execute()` or `aiplatform_projects_locations_batch_prediction_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchPredictionJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_batch_prediction_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_batch_prediction_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_batch_prediction_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_batch_prediction_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_batch_prediction_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsBatchPredictionJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchPredictionJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Creates a BatchPredictionJob. A BatchPredictionJob once created will right away be attempted to start.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_batch_prediction_jobs_create_builder()` + `aiplatform_projects_locations_batch_prediction_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsBatchPredictionJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_batch_prediction_jobs_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_batch_prediction_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Deletes a BatchPredictionJob. Can only be called on jobs that already finished.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_batch_prediction_jobs_delete_execute()` to send, or `aiplatform_projects_locations_batch_prediction_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_batch_prediction_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/batchPredictionJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Deletes a BatchPredictionJob. Can only be called on jobs that already finished.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_batch_prediction_jobs_delete_execute()` or `aiplatform_projects_locations_batch_prediction_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Deletes a BatchPredictionJob. Can only be called on jobs that already finished.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_batch_prediction_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_batch_prediction_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_batch_prediction_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_batch_prediction_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_batch_prediction_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsBatchPredictionJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Deletes a BatchPredictionJob. Can only be called on jobs that already finished.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_batch_prediction_jobs_delete_builder()` + `aiplatform_projects_locations_batch_prediction_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsBatchPredictionJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_batch_prediction_jobs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_batch_prediction_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_batch_prediction_jobs_get_execute()` to send, or `aiplatform_projects_locations_batch_prediction_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_batch_prediction_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/batchPredictionJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_batch_prediction_jobs_get_execute()` or `aiplatform_projects_locations_batch_prediction_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchPredictionJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_batch_prediction_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_batch_prediction_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_batch_prediction_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_batch_prediction_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_batch_prediction_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsBatchPredictionJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs/{batchPredictionJobsId}
/// Gets a BatchPredictionJob
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_batch_prediction_jobs_get_builder()` + `aiplatform_projects_locations_batch_prediction_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsBatchPredictionJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1BatchPredictionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_batch_prediction_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_batch_prediction_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_batch_prediction_jobs_list_execute()` to send, or `aiplatform_projects_locations_batch_prediction_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_batch_prediction_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/batchPredictionJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_batch_prediction_jobs_list_execute()` or `aiplatform_projects_locations_batch_prediction_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListBatchPredictionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListBatchPredictionJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_batch_prediction_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_batch_prediction_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_batch_prediction_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_batch_prediction_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListBatchPredictionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_batch_prediction_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_batch_prediction_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsBatchPredictionJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batchPredictionJobs
/// Lists BatchPredictionJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_batch_prediction_jobs_list_builder()` + `aiplatform_projects_locations_batch_prediction_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_batch_prediction_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_batch_prediction_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsBatchPredictionJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListBatchPredictionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_batch_prediction_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_batch_prediction_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Creates cached content, this call will initialize the cached content in the data storage, and users need to pay for the cache data storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_cached_contents_create_execute()` to send, or `aiplatform_projects_locations_cached_contents_create` for simplest API.

pub fn aiplatform_projects_locations_cached_contents_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1CachedContent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/cachedContents",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Creates cached content, this call will initialize the cached content in the data storage, and users need to pay for the cache data storage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_cached_contents_create_execute()` or `aiplatform_projects_locations_cached_contents_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CachedContent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Creates cached content, this call will initialize the cached content in the data storage, and users need to pay for the cache data storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_cached_contents_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_cached_contents_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_cached_contents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_cached_contents_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_cached_contents_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCachedContentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CachedContent,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Creates cached content, this call will initialize the cached content in the data storage, and users need to pay for the cache data storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_cached_contents_create_builder()` + `aiplatform_projects_locations_cached_contents_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCachedContentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_cached_contents_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_cached_contents_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Deletes cached content
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_cached_contents_delete_execute()` to send, or `aiplatform_projects_locations_cached_contents_delete` for simplest API.

pub fn aiplatform_projects_locations_cached_contents_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/cachedContents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Deletes cached content
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_cached_contents_delete_execute()` or `aiplatform_projects_locations_cached_contents_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Deletes cached content
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_cached_contents_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_cached_contents_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_cached_contents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_cached_contents_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_cached_contents_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCachedContentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Deletes cached content
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_cached_contents_delete_builder()` + `aiplatform_projects_locations_cached_contents_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCachedContentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_cached_contents_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_cached_contents_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Gets cached content configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_cached_contents_get_execute()` to send, or `aiplatform_projects_locations_cached_contents_get` for simplest API.

pub fn aiplatform_projects_locations_cached_contents_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/cachedContents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Gets cached content configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_cached_contents_get_execute()` or `aiplatform_projects_locations_cached_contents_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CachedContent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Gets cached content configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_cached_contents_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_cached_contents_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_cached_contents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_cached_contents_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_cached_contents_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCachedContentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Gets cached content configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_cached_contents_get_builder()` + `aiplatform_projects_locations_cached_contents_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCachedContentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_cached_contents_get_builder(client, &args.name)?;
    aiplatform_projects_locations_cached_contents_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Lists cached contents in a project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_cached_contents_list_execute()` to send, or `aiplatform_projects_locations_cached_contents_list` for simplest API.

pub fn aiplatform_projects_locations_cached_contents_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/cachedContents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Lists cached contents in a project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_cached_contents_list_execute()` or `aiplatform_projects_locations_cached_contents_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListCachedContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListCachedContentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Lists cached contents in a project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_cached_contents_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_cached_contents_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_cached_contents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListCachedContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_cached_contents_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_cached_contents_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCachedContentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents
/// Lists cached contents in a project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_cached_contents_list_builder()` + `aiplatform_projects_locations_cached_contents_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCachedContentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListCachedContentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_cached_contents_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_cached_contents_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Updates cached content configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_cached_contents_patch_execute()` to send, or `aiplatform_projects_locations_cached_contents_patch` for simplest API.

pub fn aiplatform_projects_locations_cached_contents_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1CachedContent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/cachedContents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Updates cached content configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_cached_contents_patch_execute()` or `aiplatform_projects_locations_cached_contents_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CachedContent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Updates cached content configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_cached_contents_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_cached_contents_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_cached_contents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_cached_contents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_cached_contents_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_cached_contents_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCachedContentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CachedContent,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cachedContents/{cachedContentsId}
/// Updates cached content configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_cached_contents_patch_builder()` + `aiplatform_projects_locations_cached_contents_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_cached_contents_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_cached_contents_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCachedContentsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CachedContent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_cached_contents_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_cached_contents_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}:cancel
/// Cancels a CustomJob. Starts asynchronous cancellation on the CustomJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetCustomJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the CustomJob is not deleted; instead it becomes a job with a CustomJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and CustomJob.state is set to CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_custom_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelCustomJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}:cancel
/// Cancels a CustomJob. Starts asynchronous cancellation on the CustomJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetCustomJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the CustomJob is not deleted; instead it becomes a job with a CustomJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and CustomJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_cancel_execute()` or `aiplatform_projects_locations_custom_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}:cancel
/// Cancels a CustomJob. Starts asynchronous cancellation on the CustomJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetCustomJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the CustomJob is not deleted; instead it becomes a job with a CustomJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and CustomJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelCustomJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}:cancel
/// Cancels a CustomJob. Starts asynchronous cancellation on the CustomJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetCustomJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the CustomJob is not deleted; instead it becomes a job with a CustomJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and CustomJob.state is set to CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_cancel_builder()` + `aiplatform_projects_locations_custom_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_custom_jobs_cancel_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_custom_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Creates a CustomJob. A created CustomJob right away will be attempted to be run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_create_execute()` to send, or `aiplatform_projects_locations_custom_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1CustomJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Creates a CustomJob. A created CustomJob right away will be attempted to be run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_create_execute()` or `aiplatform_projects_locations_custom_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CustomJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CustomJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Creates a CustomJob. A created CustomJob right away will be attempted to be run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CustomJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CustomJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Creates a CustomJob. A created CustomJob right away will be attempted to be run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_create_builder()` + `aiplatform_projects_locations_custom_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CustomJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_custom_jobs_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_custom_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Deletes a CustomJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_delete_execute()` to send, or `aiplatform_projects_locations_custom_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Deletes a CustomJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_delete_execute()` or `aiplatform_projects_locations_custom_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Deletes a CustomJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Deletes a CustomJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_delete_builder()` + `aiplatform_projects_locations_custom_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_custom_jobs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_custom_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Gets a CustomJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_get_execute()` to send, or `aiplatform_projects_locations_custom_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Gets a CustomJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_get_execute()` or `aiplatform_projects_locations_custom_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CustomJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CustomJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Gets a CustomJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CustomJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}
/// Gets a CustomJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_get_builder()` + `aiplatform_projects_locations_custom_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CustomJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_custom_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_custom_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Lists CustomJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_list_execute()` to send, or `aiplatform_projects_locations_custom_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Lists CustomJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_list_execute()` or `aiplatform_projects_locations_custom_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListCustomJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListCustomJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Lists CustomJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListCustomJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs
/// Lists CustomJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_list_builder()` + `aiplatform_projects_locations_custom_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListCustomJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_custom_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_custom_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_custom_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_custom_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_custom_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_custom_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_custom_jobs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_custom_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_operations_delete_execute()` or `aiplatform_projects_locations_custom_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_operations_delete_builder()` + `aiplatform_projects_locations_custom_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_custom_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_custom_jobs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_custom_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_operations_get_execute()` or `aiplatform_projects_locations_custom_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_operations_get_builder()` + `aiplatform_projects_locations_custom_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_custom_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_custom_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_custom_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_operations_list_execute()` or `aiplatform_projects_locations_custom_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_operations_list_builder()` + `aiplatform_projects_locations_custom_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_custom_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_custom_jobs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_custom_jobs_operations_wait_execute()` to send, or `aiplatform_projects_locations_custom_jobs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_custom_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/customJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_custom_jobs_operations_wait_execute()` or `aiplatform_projects_locations_custom_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_custom_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_custom_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_custom_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_custom_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_custom_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_custom_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsCustomJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customJobs/{customJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_custom_jobs_operations_wait_builder()` + `aiplatform_projects_locations_custom_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_custom_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_custom_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsCustomJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_custom_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_custom_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}:cancel
/// Cancels a DataLabelingJob. Success of cancellation is not guaranteed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelDataLabelingJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}:cancel
/// Cancels a DataLabelingJob. Success of cancellation is not guaranteed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_cancel_execute()` or `aiplatform_projects_locations_data_labeling_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}:cancel
/// Cancels a DataLabelingJob. Success of cancellation is not guaranteed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelDataLabelingJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}:cancel
/// Cancels a DataLabelingJob. Success of cancellation is not guaranteed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_cancel_builder()` + `aiplatform_projects_locations_data_labeling_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_cancel_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_data_labeling_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Creates a DataLabelingJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_create_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1DataLabelingJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Creates a DataLabelingJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_create_execute()` or `aiplatform_projects_locations_data_labeling_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DataLabelingJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DataLabelingJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Creates a DataLabelingJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DataLabelingJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DataLabelingJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Creates a DataLabelingJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_create_builder()` + `aiplatform_projects_locations_data_labeling_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DataLabelingJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_data_labeling_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Deletes a DataLabelingJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_delete_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Deletes a DataLabelingJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_delete_execute()` or `aiplatform_projects_locations_data_labeling_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Deletes a DataLabelingJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Deletes a DataLabelingJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_delete_builder()` + `aiplatform_projects_locations_data_labeling_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_data_labeling_jobs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_data_labeling_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Gets a DataLabelingJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_get_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Gets a DataLabelingJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_get_execute()` or `aiplatform_projects_locations_data_labeling_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DataLabelingJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DataLabelingJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Gets a DataLabelingJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DataLabelingJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}
/// Gets a DataLabelingJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_get_builder()` + `aiplatform_projects_locations_data_labeling_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DataLabelingJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_data_labeling_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Lists DataLabelingJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_list_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Lists DataLabelingJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_list_execute()` or `aiplatform_projects_locations_data_labeling_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDataLabelingJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDataLabelingJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Lists DataLabelingJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDataLabelingJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs
/// Lists DataLabelingJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_list_builder()` + `aiplatform_projects_locations_data_labeling_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDataLabelingJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_data_labeling_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_data_labeling_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_data_labeling_jobs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_operations_delete_execute()` or `aiplatform_projects_locations_data_labeling_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder()` + `aiplatform_projects_locations_data_labeling_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_data_labeling_jobs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_operations_get_execute()` or `aiplatform_projects_locations_data_labeling_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_get_builder()` + `aiplatform_projects_locations_data_labeling_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_data_labeling_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_operations_list_execute()` or `aiplatform_projects_locations_data_labeling_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_list_builder()` + `aiplatform_projects_locations_data_labeling_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_data_labeling_jobs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_data_labeling_jobs_operations_wait_execute()` to send, or `aiplatform_projects_locations_data_labeling_jobs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/dataLabelingJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_data_labeling_jobs_operations_wait_execute()` or `aiplatform_projects_locations_data_labeling_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_data_labeling_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_data_labeling_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_data_labeling_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDataLabelingJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataLabelingJobs/{dataLabelingJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder()` + `aiplatform_projects_locations_data_labeling_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_data_labeling_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_data_labeling_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDataLabelingJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_data_labeling_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_create_execute()` to send, or `aiplatform_projects_locations_datasets_create` for simplest API.

pub fn aiplatform_projects_locations_datasets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_create_execute()` or `aiplatform_projects_locations_datasets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Dataset,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_create_builder()` + `aiplatform_projects_locations_datasets_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_datasets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_delete_execute()` to send, or `aiplatform_projects_locations_datasets_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_delete_execute()` or `aiplatform_projects_locations_datasets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_delete_builder()` + `aiplatform_projects_locations_datasets_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_datasets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:export
/// Exports data from a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_export_execute()` to send, or `aiplatform_projects_locations_datasets_export` for simplest API.

pub fn aiplatform_projects_locations_datasets_export_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ExportDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:export",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:export
/// Exports data from a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_export_execute()` or `aiplatform_projects_locations_datasets_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:export
/// Exports data from a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_export_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsExportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExportDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:export
/// Exports data from a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_export_builder()` + `aiplatform_projects_locations_datasets_export_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_export(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_export_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_datasets_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_get_execute()` to send, or `aiplatform_projects_locations_datasets_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_get_execute()` or `aiplatform_projects_locations_datasets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_get_builder()` + `aiplatform_projects_locations_datasets_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_get_builder(
        client,
        &args.name,
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_datasets_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_datasets_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_get_iam_policy_execute()` or `aiplatform_projects_locations_datasets_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_get_iam_policy_builder()` + `aiplatform_projects_locations_datasets_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_datasets_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:import
/// Imports data into a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_import_execute()` to send, or `aiplatform_projects_locations_datasets_import` for simplest API.

pub fn aiplatform_projects_locations_datasets_import_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ImportDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:import",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:import
/// Imports data into a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_import_execute()` or `aiplatform_projects_locations_datasets_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:import
/// Imports data into a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_import_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsImportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ImportDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:import
/// Imports data into a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_import_builder()` + `aiplatform_projects_locations_datasets_import_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_import(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_import_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_datasets_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists Datasets in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_list_execute()` to send, or `aiplatform_projects_locations_datasets_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists Datasets in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_list_execute()` or `aiplatform_projects_locations_datasets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDatasetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists Datasets in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists Datasets in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_list_builder()` + `aiplatform_projects_locations_datasets_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_patch_execute()` to send, or `aiplatform_projects_locations_datasets_patch` for simplest API.

pub fn aiplatform_projects_locations_datasets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_patch_execute()` or `aiplatform_projects_locations_datasets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Dataset,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_patch_builder()` + `aiplatform_projects_locations_datasets_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_datasets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:searchDataItems
/// Searches DataItems in a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_search_data_items_execute()` to send, or `aiplatform_projects_locations_datasets_search_data_items` for simplest API.

pub fn aiplatform_projects_locations_datasets_search_data_items_builder(
    client: &SimpleHttpClient,
    dataset: &str,
    annotationFilters: Option<&str>,
    annotationsFilter: Option<&str>,
    annotationsLimit: Option<i32>,
    dataItemFilter: Option<&str>,
    dataLabelingJob: Option<&str>,
    fieldMask: Option<&str>,
    orderBy: Option<&str>,
    orderByAnnotation_orderBy: Option<&str>,
    orderByAnnotation_savedQuery: Option<&str>,
    orderByDataItem: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    savedQuery: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:searchDataItems",
        dataset,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = annotationFilters {
        query_parts.push(format!("annotationFilters={}", val));
    }
    if let Some(val) = annotationsFilter {
        query_parts.push(format!("annotationsFilter={}", val));
    }
    if let Some(val) = annotationsLimit {
        query_parts.push(format!("annotationsLimit={}", val));
    }
    if let Some(val) = dataItemFilter {
        query_parts.push(format!("dataItemFilter={}", val));
    }
    if let Some(val) = dataLabelingJob {
        query_parts.push(format!("dataLabelingJob={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = orderByAnnotation_orderBy {
        query_parts.push(format!("orderByAnnotation_orderBy={}", val));
    }
    if let Some(val) = orderByAnnotation_savedQuery {
        query_parts.push(format!("orderByAnnotation_savedQuery={}", val));
    }
    if let Some(val) = orderByDataItem {
        query_parts.push(format!("orderByDataItem={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = savedQuery {
        query_parts.push(format!("savedQuery={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:searchDataItems
/// Searches DataItems in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_search_data_items_execute()` or `aiplatform_projects_locations_datasets_search_data_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_search_data_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_search_data_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchDataItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SearchDataItemsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:searchDataItems
/// Searches DataItems in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_search_data_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_search_data_items_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_search_data_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_search_data_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_search_data_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchDataItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_search_data_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_search_data_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSearchDataItemsArgs {
    /// Path parameter: dataset
    pub dataset: String,
    /// Query parameter: annotationFilters
    pub annotationFilters: Option<String>,
    /// Query parameter: annotationsFilter
    pub annotationsFilter: Option<String>,
    /// Query parameter: annotationsLimit
    pub annotationsLimit: Option<i32>,
    /// Query parameter: dataItemFilter
    pub dataItemFilter: Option<String>,
    /// Query parameter: dataLabelingJob
    pub dataLabelingJob: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: orderByAnnotation_orderBy
    pub orderByAnnotation_orderBy: Option<String>,
    /// Query parameter: orderByAnnotation_savedQuery
    pub orderByAnnotation_savedQuery: Option<String>,
    /// Query parameter: orderByDataItem
    pub orderByDataItem: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: savedQuery
    pub savedQuery: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:searchDataItems
/// Searches DataItems in a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_search_data_items_builder()` + `aiplatform_projects_locations_datasets_search_data_items_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_search_data_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_search_data_items(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSearchDataItemsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchDataItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_search_data_items_builder(
        client,
        &args.dataset,
        args.annotationFilters.as_deref(),
        args.annotationsFilter.as_deref(),
        args.annotationsLimit,
        args.dataItemFilter.as_deref(),
        args.dataLabelingJob.as_deref(),
        args.fieldMask.as_deref(),
        args.orderBy.as_deref(),
        args.orderByAnnotation_orderBy.as_deref(),
        args.orderByAnnotation_savedQuery.as_deref(),
        args.orderByDataItem.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.savedQuery.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_search_data_items_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_datasets_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_datasets_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_set_iam_policy_execute()` or `aiplatform_projects_locations_datasets_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_set_iam_policy_builder()` + `aiplatform_projects_locations_datasets_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_datasets_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_datasets_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_datasets_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_test_iam_permissions_execute()` or `aiplatform_projects_locations_datasets_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_test_iam_permissions_builder()` + `aiplatform_projects_locations_datasets_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_test_iam_permissions_builder(
        client,
        &args.resource,
        args.permissions.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}
/// Gets an AnnotationSpec.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_annotation_specs_get_execute()` to send, or `aiplatform_projects_locations_datasets_annotation_specs_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_annotation_specs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/annotationSpecs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}
/// Gets an AnnotationSpec.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_annotation_specs_get_execute()` or `aiplatform_projects_locations_datasets_annotation_specs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AnnotationSpec>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AnnotationSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}
/// Gets an AnnotationSpec.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_annotation_specs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_annotation_specs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_annotation_specs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AnnotationSpec>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_annotation_specs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_annotation_specs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsAnnotationSpecsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}
/// Gets an AnnotationSpec.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_annotation_specs_get_builder()` + `aiplatform_projects_locations_datasets_annotation_specs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AnnotationSpec>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_annotation_specs_get_builder(
        client,
        &args.name,
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_annotation_specs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/annotationSpecs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_execute()` or `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_annotation_specs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder()` + `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_execute()` to send, or `aiplatform_projects_locations_datasets_annotation_specs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/annotationSpecs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_execute()` or `aiplatform_projects_locations_datasets_annotation_specs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_annotation_specs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_annotation_specs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder()` + `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_datasets_annotation_specs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_annotation_specs_operations_get_execute()` to send, or `aiplatform_projects_locations_datasets_annotation_specs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/annotationSpecs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_annotation_specs_operations_get_execute()` or `aiplatform_projects_locations_datasets_annotation_specs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_annotation_specs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_annotation_specs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_annotation_specs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_get_builder()` + `aiplatform_projects_locations_datasets_annotation_specs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_annotation_specs_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_annotation_specs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_annotation_specs_operations_list_execute()` to send, or `aiplatform_projects_locations_datasets_annotation_specs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/annotationSpecs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_annotation_specs_operations_list_execute()` or `aiplatform_projects_locations_datasets_annotation_specs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_annotation_specs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_annotation_specs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_annotation_specs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_list_builder()` + `aiplatform_projects_locations_datasets_annotation_specs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_annotation_specs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_datasets_annotation_specs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_execute()` to send, or `aiplatform_projects_locations_datasets_annotation_specs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/annotationSpecs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_execute()` or `aiplatform_projects_locations_datasets_annotation_specs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_annotation_specs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_annotation_specs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/annotationSpecs/{annotationSpecsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder()` + `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_annotation_specs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_annotation_specs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems
/// Lists DataItems in a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_list_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems
/// Lists DataItems in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_list_execute()` or `aiplatform_projects_locations_datasets_data_items_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDataItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDataItemsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems
/// Lists DataItems in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDataItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems
/// Lists DataItems in a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_list_builder()` + `aiplatform_projects_locations_datasets_data_items_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDataItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_data_items_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations
/// Lists Annotations belongs to a dataitem.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_annotations_list_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_annotations_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/annotations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations
/// Lists Annotations belongs to a dataitem.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_annotations_list_execute()` or `aiplatform_projects_locations_datasets_data_items_annotations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListAnnotationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListAnnotationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations
/// Lists Annotations belongs to a dataitem.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_annotations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_annotations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListAnnotationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_annotations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_annotations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsAnnotationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations
/// Lists Annotations belongs to a dataitem.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_annotations_list_builder()` + `aiplatform_projects_locations_datasets_data_items_annotations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListAnnotationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_annotations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_data_items_annotations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/annotations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_execute()` or `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder()` + `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/annotations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_execute()` or `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_annotations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder()` + `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_annotations_operations_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/annotations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_execute()` or `aiplatform_projects_locations_datasets_data_items_annotations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_annotations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_builder()` + `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_get_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_datasets_data_items_annotations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_annotations_operations_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/annotations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_execute()` or `aiplatform_projects_locations_datasets_data_items_annotations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_annotations_operations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_annotations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_builder()` + `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_list_builder(
            client,
            &args.name,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.returnPartialSuccess,
        )?;
    aiplatform_projects_locations_datasets_data_items_annotations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/annotations/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_execute()` or `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_annotations_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/annotations/{annotationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder()` + `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder(
            client,
            &args.name,
            args.timeout.as_deref(),
        )?;
    aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_operations_cancel_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_operations_cancel_execute()` or `aiplatform_projects_locations_datasets_data_items_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_operations_cancel_builder()` + `aiplatform_projects_locations_datasets_data_items_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_data_items_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_operations_delete_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_operations_delete_execute()` or `aiplatform_projects_locations_datasets_data_items_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_operations_delete_builder()` + `aiplatform_projects_locations_datasets_data_items_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_data_items_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_operations_get_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_operations_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_operations_get_execute()` or `aiplatform_projects_locations_datasets_data_items_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_operations_get_builder()` + `aiplatform_projects_locations_datasets_data_items_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_data_items_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_operations_list_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_operations_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_operations_list_execute()` or `aiplatform_projects_locations_datasets_data_items_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_operations_list_builder()` + `aiplatform_projects_locations_datasets_data_items_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_datasets_data_items_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_data_items_operations_wait_execute()` to send, or `aiplatform_projects_locations_datasets_data_items_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_datasets_data_items_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/dataItems/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_data_items_operations_wait_execute()` or `aiplatform_projects_locations_datasets_data_items_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_data_items_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_data_items_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_data_items_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_data_items_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_data_items_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_data_items_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDataItemsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataItems/{dataItemsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_data_items_operations_wait_builder()` + `aiplatform_projects_locations_datasets_data_items_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_data_items_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_data_items_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_data_items_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_data_items_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_dataset_versions_create_execute()` to send, or `aiplatform_projects_locations_datasets_dataset_versions_create` for simplest API.

pub fn aiplatform_projects_locations_datasets_dataset_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1DatasetVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/datasetVersions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_dataset_versions_create_execute()` or `aiplatform_projects_locations_datasets_dataset_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_dataset_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_dataset_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_dataset_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_dataset_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_dataset_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDatasetVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DatasetVersion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Create a version from a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_dataset_versions_create_builder()` + `aiplatform_projects_locations_datasets_dataset_versions_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDatasetVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_dataset_versions_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_datasets_dataset_versions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_dataset_versions_delete_execute()` to send, or `aiplatform_projects_locations_datasets_dataset_versions_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_dataset_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/datasetVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_dataset_versions_delete_execute()` or `aiplatform_projects_locations_datasets_dataset_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_dataset_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_dataset_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_dataset_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_dataset_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_dataset_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDatasetVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Deletes a Dataset version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_dataset_versions_delete_builder()` + `aiplatform_projects_locations_datasets_dataset_versions_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDatasetVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_dataset_versions_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_datasets_dataset_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_dataset_versions_get_execute()` to send, or `aiplatform_projects_locations_datasets_dataset_versions_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_dataset_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/datasetVersions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_dataset_versions_get_execute()` or `aiplatform_projects_locations_datasets_dataset_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DatasetVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_dataset_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_dataset_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_dataset_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_dataset_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_dataset_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDatasetVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Gets a Dataset version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_dataset_versions_get_builder()` + `aiplatform_projects_locations_datasets_dataset_versions_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDatasetVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_dataset_versions_get_builder(
        client,
        &args.name,
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_dataset_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_dataset_versions_list_execute()` to send, or `aiplatform_projects_locations_datasets_dataset_versions_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_dataset_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/datasetVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_dataset_versions_list_execute()` or `aiplatform_projects_locations_datasets_dataset_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDatasetVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_dataset_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_dataset_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_dataset_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_dataset_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_dataset_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDatasetVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions
/// Lists DatasetVersions in a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_dataset_versions_list_builder()` + `aiplatform_projects_locations_datasets_dataset_versions_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDatasetVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListDatasetVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_dataset_versions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_dataset_versions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_dataset_versions_patch_execute()` to send, or `aiplatform_projects_locations_datasets_dataset_versions_patch` for simplest API.

pub fn aiplatform_projects_locations_datasets_dataset_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1DatasetVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/datasetVersions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_dataset_versions_patch_execute()` or `aiplatform_projects_locations_datasets_dataset_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DatasetVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_dataset_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_dataset_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_dataset_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_dataset_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_dataset_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDatasetVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DatasetVersion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}
/// Updates a DatasetVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_dataset_versions_patch_builder()` + `aiplatform_projects_locations_datasets_dataset_versions_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDatasetVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DatasetVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_dataset_versions_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_datasets_dataset_versions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_dataset_versions_restore_execute()` to send, or `aiplatform_projects_locations_datasets_dataset_versions_restore` for simplest API.

pub fn aiplatform_projects_locations_datasets_dataset_versions_restore_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/datasetVersions/{}:restore",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_dataset_versions_restore_execute()` or `aiplatform_projects_locations_datasets_dataset_versions_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_dataset_versions_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_restore_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_dataset_versions_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_dataset_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_dataset_versions_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_dataset_versions_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_dataset_versions_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsDatasetVersionsRestoreArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/datasetVersions/{datasetVersionsId}:restore
/// Restores a dataset version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_dataset_versions_restore_builder()` + `aiplatform_projects_locations_datasets_dataset_versions_restore_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_dataset_versions_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_dataset_versions_restore(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsDatasetVersionsRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_dataset_versions_restore_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_dataset_versions_restore_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_operations_cancel_execute()` to send, or `aiplatform_projects_locations_datasets_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_datasets_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_operations_cancel_execute()` or `aiplatform_projects_locations_datasets_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_operations_cancel_builder()` + `aiplatform_projects_locations_datasets_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_datasets_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_operations_delete_execute()` to send, or `aiplatform_projects_locations_datasets_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_operations_delete_execute()` or `aiplatform_projects_locations_datasets_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_operations_delete_builder()` + `aiplatform_projects_locations_datasets_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_datasets_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_operations_get_execute()` to send, or `aiplatform_projects_locations_datasets_operations_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_operations_get_execute()` or `aiplatform_projects_locations_datasets_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_operations_get_builder()` + `aiplatform_projects_locations_datasets_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_datasets_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_operations_list_execute()` to send, or `aiplatform_projects_locations_datasets_operations_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_operations_list_execute()` or `aiplatform_projects_locations_datasets_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_operations_list_builder()` + `aiplatform_projects_locations_datasets_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_datasets_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_operations_wait_execute()` to send, or `aiplatform_projects_locations_datasets_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_datasets_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_operations_wait_execute()` or `aiplatform_projects_locations_datasets_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_operations_wait_builder()` + `aiplatform_projects_locations_datasets_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}
/// Deletes a SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_delete_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}
/// Deletes a SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_delete_execute()` or `aiplatform_projects_locations_datasets_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}
/// Deletes a SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}
/// Deletes a SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_delete_builder()` + `aiplatform_projects_locations_datasets_saved_queries_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_datasets_saved_queries_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_datasets_saved_queries_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries
/// Lists SavedQueries in a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_list_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries
/// Lists SavedQueries in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_list_execute()` or `aiplatform_projects_locations_datasets_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSavedQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListSavedQueriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries
/// Lists SavedQueries in a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSavedQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries
/// Lists SavedQueries in a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_list_builder()` + `aiplatform_projects_locations_datasets_saved_queries_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSavedQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_saved_queries_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_saved_queries_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_execute()` or `aiplatform_projects_locations_datasets_saved_queries_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_saved_queries_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder()` + `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_saved_queries_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_operations_delete_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_operations_delete_execute()` or `aiplatform_projects_locations_datasets_saved_queries_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_datasets_saved_queries_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder()` + `aiplatform_projects_locations_datasets_saved_queries_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_saved_queries_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_operations_get_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_operations_get` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_operations_get_execute()` or `aiplatform_projects_locations_datasets_saved_queries_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_saved_queries_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_get_builder()` + `aiplatform_projects_locations_datasets_saved_queries_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_saved_queries_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_datasets_saved_queries_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_operations_list_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_operations_list` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_operations_list_execute()` or `aiplatform_projects_locations_datasets_saved_queries_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_saved_queries_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_list_builder()` + `aiplatform_projects_locations_datasets_saved_queries_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_saved_queries_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_datasets_saved_queries_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_datasets_saved_queries_operations_wait_execute()` to send, or `aiplatform_projects_locations_datasets_saved_queries_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/savedQueries/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_datasets_saved_queries_operations_wait_execute()` or `aiplatform_projects_locations_datasets_saved_queries_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_datasets_saved_queries_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_datasets_saved_queries_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_datasets_saved_queries_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDatasetsSavedQueriesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/savedQueries/{savedQueriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder()` + `aiplatform_projects_locations_datasets_saved_queries_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_datasets_saved_queries_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_datasets_saved_queries_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_datasets_saved_queries_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// Create a DeploymentResourcePool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_create_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_create` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1CreateDeploymentResourcePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// Create a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_create_execute()` or `aiplatform_projects_locations_deployment_resource_pools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// Create a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deployment_resource_pools_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CreateDeploymentResourcePoolRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// Create a DeploymentResourcePool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_create_builder()` + `aiplatform_projects_locations_deployment_resource_pools_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_deployment_resource_pools_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_deployment_resource_pools_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Delete a DeploymentResourcePool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_delete_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_delete` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Delete a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_delete_execute()` or `aiplatform_projects_locations_deployment_resource_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Delete a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deployment_resource_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Delete a DeploymentResourcePool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_delete_builder()` + `aiplatform_projects_locations_deployment_resource_pools_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_deployment_resource_pools_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_deployment_resource_pools_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Get a DeploymentResourcePool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_get_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_get` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Get a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_get_execute()` or `aiplatform_projects_locations_deployment_resource_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DeploymentResourcePool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DeploymentResourcePool =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Get a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DeploymentResourcePool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deployment_resource_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Get a DeploymentResourcePool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_get_builder()` + `aiplatform_projects_locations_deployment_resource_pools_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DeploymentResourcePool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_deployment_resource_pools_get_builder(client, &args.name)?;
    aiplatform_projects_locations_deployment_resource_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// List DeploymentResourcePools in a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_list_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_list` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// List DeploymentResourcePools in a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_list_execute()` or `aiplatform_projects_locations_deployment_resource_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListDeploymentResourcePoolsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListDeploymentResourcePoolsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// List DeploymentResourcePools in a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListDeploymentResourcePoolsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deployment_resource_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools
/// List DeploymentResourcePools in a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_list_builder()` + `aiplatform_projects_locations_deployment_resource_pools_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListDeploymentResourcePoolsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_deployment_resource_pools_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_deployment_resource_pools_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Update a DeploymentResourcePool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_patch_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_patch` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1DeploymentResourcePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Update a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_patch_execute()` or `aiplatform_projects_locations_deployment_resource_pools_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Update a DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deployment_resource_pools_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DeploymentResourcePool,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}
/// Update a DeploymentResourcePool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_patch_builder()` + `aiplatform_projects_locations_deployment_resource_pools_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_deployment_resource_pools_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_deployment_resource_pools_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}:queryDeployedModels
/// List DeployedModels that have been deployed on this DeploymentResourcePool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_builder(
    client: &SimpleHttpClient,
    deploymentResourcePool: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}:queryDeployedModels",
        deploymentResourcePool,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}:queryDeployedModels
/// List DeployedModels that have been deployed on this DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_execute()` or `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryDeployedModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1QueryDeployedModelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}:queryDeployedModels
/// List DeployedModels that have been deployed on this DeploymentResourcePool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryDeployedModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_query_deployed_models`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsQueryDeployedModelsArgs {
    /// Path parameter: deploymentResourcePool
    pub deploymentResourcePool: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}:queryDeployedModels
/// List DeployedModels that have been deployed on this DeploymentResourcePool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_builder()` + `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_query_deployed_models(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsQueryDeployedModelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryDeployedModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_builder(
            client,
            &args.deploymentResourcePool,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    aiplatform_projects_locations_deployment_resource_pools_query_deployed_models_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_execute()` or `aiplatform_projects_locations_deployment_resource_pools_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_deployment_resource_pools_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder()` + `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_deployment_resource_pools_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_operations_delete_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_operations_delete_execute()` or `aiplatform_projects_locations_deployment_resource_pools_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_deployment_resource_pools_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder()` + `aiplatform_projects_locations_deployment_resource_pools_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_deployment_resource_pools_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_operations_get_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_operations_get` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_operations_get_execute()` or `aiplatform_projects_locations_deployment_resource_pools_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_deployment_resource_pools_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_get_builder()` + `aiplatform_projects_locations_deployment_resource_pools_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_deployment_resource_pools_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_deployment_resource_pools_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_operations_list_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_operations_list` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_operations_list_execute()` or `aiplatform_projects_locations_deployment_resource_pools_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_deployment_resource_pools_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_list_builder()` + `aiplatform_projects_locations_deployment_resource_pools_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_deployment_resource_pools_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_deployment_resource_pools_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_deployment_resource_pools_operations_wait_execute()` to send, or `aiplatform_projects_locations_deployment_resource_pools_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/deploymentResourcePools/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_deployment_resource_pools_operations_wait_execute()` or `aiplatform_projects_locations_deployment_resource_pools_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_deployment_resource_pools_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_deployment_resource_pools_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_deployment_resource_pools_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsDeploymentResourcePoolsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deploymentResourcePools/{deploymentResourcePoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder()` + `aiplatform_projects_locations_deployment_resource_pools_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_deployment_resource_pools_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_deployment_resource_pools_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_deployment_resource_pools_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_compute_tokens_execute()` to send, or `aiplatform_projects_locations_endpoints_compute_tokens` for simplest API.

pub fn aiplatform_projects_locations_endpoints_compute_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1ComputeTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:computeTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_compute_tokens_execute()` or `aiplatform_projects_locations_endpoints_compute_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_compute_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ComputeTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_compute_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_compute_tokens_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_compute_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_compute_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_compute_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_compute_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsComputeTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ComputeTokensRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_compute_tokens_builder()` + `aiplatform_projects_locations_endpoints_compute_tokens_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_compute_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_compute_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsComputeTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_compute_tokens_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_compute_tokens_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_count_tokens_execute()` to send, or `aiplatform_projects_locations_endpoints_count_tokens` for simplest API.

pub fn aiplatform_projects_locations_endpoints_count_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1CountTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:countTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_count_tokens_execute()` or `aiplatform_projects_locations_endpoints_count_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_count_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CountTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_count_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_count_tokens_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_count_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_count_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_count_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_count_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsCountTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CountTokensRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:countTokens
/// Perform a token counting.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_count_tokens_builder()` + `aiplatform_projects_locations_endpoints_count_tokens_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_count_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_count_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsCountTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_count_tokens_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_count_tokens_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Creates an Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_create_execute()` to send, or `aiplatform_projects_locations_endpoints_create` for simplest API.

pub fn aiplatform_projects_locations_endpoints_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    endpointId: Option<&str>,
    body: &GoogleCloudAiplatformV1Endpoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endpointId {
        query_parts.push(format!("endpointId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Creates an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_create_execute()` or `aiplatform_projects_locations_endpoints_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Creates an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: endpointId
    pub endpointId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Endpoint,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Creates an Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_create_builder()` + `aiplatform_projects_locations_endpoints_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_create_builder(
        client,
        &args.parent,
        args.endpointId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Deletes an Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_delete_execute()` to send, or `aiplatform_projects_locations_endpoints_delete` for simplest API.

pub fn aiplatform_projects_locations_endpoints_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Deletes an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_delete_execute()` or `aiplatform_projects_locations_endpoints_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Deletes an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Deletes an Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_delete_builder()` + `aiplatform_projects_locations_endpoints_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_endpoints_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:deployModel
/// Deploys a Model into this Endpoint, creating a DeployedModel within it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_deploy_model_execute()` to send, or `aiplatform_projects_locations_endpoints_deploy_model` for simplest API.

pub fn aiplatform_projects_locations_endpoints_deploy_model_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1DeployModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:deployModel",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:deployModel
/// Deploys a Model into this Endpoint, creating a DeployedModel within it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_deploy_model_execute()` or `aiplatform_projects_locations_endpoints_deploy_model`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_deploy_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_deploy_model_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:deployModel
/// Deploys a Model into this Endpoint, creating a DeployedModel within it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_deploy_model_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_deploy_model_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_deploy_model()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_deploy_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_deploy_model_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_deploy_model_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_deploy_model`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsDeployModelArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DeployModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:deployModel
/// Deploys a Model into this Endpoint, creating a DeployedModel within it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_deploy_model_builder()` + `aiplatform_projects_locations_endpoints_deploy_model_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_deploy_model_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_deploy_model(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsDeployModelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_deploy_model_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_deploy_model_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directPredict
/// Perform an unary online prediction request to a `gRPC` model server for Vertex first-party products and frameworks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_direct_predict_execute()` to send, or `aiplatform_projects_locations_endpoints_direct_predict` for simplest API.

pub fn aiplatform_projects_locations_endpoints_direct_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1DirectPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:directPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directPredict
/// Perform an unary online prediction request to a `gRPC` model server for Vertex first-party products and frameworks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_direct_predict_execute()` or `aiplatform_projects_locations_endpoints_direct_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_direct_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_direct_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DirectPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DirectPredictResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directPredict
/// Perform an unary online prediction request to a `gRPC` model server for Vertex first-party products and frameworks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_direct_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_direct_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_direct_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_direct_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_direct_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DirectPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_direct_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_direct_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsDirectPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DirectPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directPredict
/// Perform an unary online prediction request to a `gRPC` model server for Vertex first-party products and frameworks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_direct_predict_builder()` + `aiplatform_projects_locations_endpoints_direct_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_direct_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_direct_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsDirectPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DirectPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_direct_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_direct_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directRawPredict
/// Perform an unary online prediction request to a `gRPC` model server for custom containers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_direct_raw_predict_execute()` to send, or `aiplatform_projects_locations_endpoints_direct_raw_predict` for simplest API.

pub fn aiplatform_projects_locations_endpoints_direct_raw_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1DirectRawPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:directRawPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directRawPredict
/// Perform an unary online prediction request to a `gRPC` model server for custom containers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_direct_raw_predict_execute()` or `aiplatform_projects_locations_endpoints_direct_raw_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_direct_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_direct_raw_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DirectRawPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1DirectRawPredictResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directRawPredict
/// Perform an unary online prediction request to a `gRPC` model server for custom containers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_direct_raw_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_direct_raw_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_direct_raw_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_direct_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_direct_raw_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DirectRawPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_direct_raw_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_direct_raw_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsDirectRawPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DirectRawPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:directRawPredict
/// Perform an unary online prediction request to a `gRPC` model server for custom containers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_direct_raw_predict_builder()` + `aiplatform_projects_locations_endpoints_direct_raw_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_direct_raw_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_direct_raw_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsDirectRawPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1DirectRawPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_direct_raw_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_direct_raw_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:explain
/// Perform an online explanation. If deployed_model_id is specified, the corresponding DeployModel must have explanation_spec populated. If deployed_model_id is not specified, all DeployedModels must have explanation_spec populated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_explain_execute()` to send, or `aiplatform_projects_locations_endpoints_explain` for simplest API.

pub fn aiplatform_projects_locations_endpoints_explain_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1ExplainRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:explain",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:explain
/// Perform an online explanation. If deployed_model_id is specified, the corresponding DeployModel must have explanation_spec populated. If deployed_model_id is not specified, all DeployedModels must have explanation_spec populated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_explain_execute()` or `aiplatform_projects_locations_endpoints_explain`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_explain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_explain_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExplainResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ExplainResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:explain
/// Perform an online explanation. If deployed_model_id is specified, the corresponding DeployModel must have explanation_spec populated. If deployed_model_id is not specified, all DeployedModels must have explanation_spec populated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_explain_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_explain_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_explain()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_explain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_explain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExplainResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_explain_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_explain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsExplainArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExplainRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:explain
/// Perform an online explanation. If deployed_model_id is specified, the corresponding DeployModel must have explanation_spec populated. If deployed_model_id is not specified, all DeployedModels must have explanation_spec populated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_explain_builder()` + `aiplatform_projects_locations_endpoints_explain_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_explain_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_explain(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsExplainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExplainResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_explain_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_explain_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_fetch_predict_operation_execute()` to send, or `aiplatform_projects_locations_endpoints_fetch_predict_operation` for simplest API.

pub fn aiplatform_projects_locations_endpoints_fetch_predict_operation_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1FetchPredictOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:fetchPredictOperation",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_fetch_predict_operation_execute()` or `aiplatform_projects_locations_endpoints_fetch_predict_operation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_fetch_predict_operation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_fetch_predict_operation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_fetch_predict_operation_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_fetch_predict_operation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_fetch_predict_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_fetch_predict_operation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_fetch_predict_operation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsFetchPredictOperationArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FetchPredictOperationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_fetch_predict_operation_builder()` + `aiplatform_projects_locations_endpoints_fetch_predict_operation_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_fetch_predict_operation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_fetch_predict_operation(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsFetchPredictOperationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_fetch_predict_operation_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_fetch_predict_operation_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_generate_content_execute()` to send, or `aiplatform_projects_locations_endpoints_generate_content` for simplest API.

pub fn aiplatform_projects_locations_endpoints_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:generateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_generate_content_execute()` or `aiplatform_projects_locations_endpoints_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_generate_content_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_generate_content_builder()` + `aiplatform_projects_locations_endpoints_generate_content_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_generate_content_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_generate_content_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets an Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_get_execute()` to send, or `aiplatform_projects_locations_endpoints_get` for simplest API.

pub fn aiplatform_projects_locations_endpoints_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_get_execute()` or `aiplatform_projects_locations_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Endpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Endpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets an Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_get_builder()` + `aiplatform_projects_locations_endpoints_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Endpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_get_builder(client, &args.name)?;
    aiplatform_projects_locations_endpoints_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_list_execute()` to send, or `aiplatform_projects_locations_endpoints_list` for simplest API.

pub fn aiplatform_projects_locations_endpoints_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    gdcZone: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = gdcZone {
        query_parts.push(format!("gdcZone={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_list_execute()` or `aiplatform_projects_locations_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListEndpointsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: gdcZone
    pub gdcZone: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_list_builder()` + `aiplatform_projects_locations_endpoints_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.gdcZone.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_endpoints_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:mutateDeployedModel
/// Updates an existing deployed model. Updatable fields include min_replica_count, max_replica_count, required_replica_count, autoscaling_metric_specs, disable_container_logging (v1 only), and enable_container_logging (v1beta1 only).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_mutate_deployed_model_execute()` to send, or `aiplatform_projects_locations_endpoints_mutate_deployed_model` for simplest API.

pub fn aiplatform_projects_locations_endpoints_mutate_deployed_model_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1MutateDeployedModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:mutateDeployedModel",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:mutateDeployedModel
/// Updates an existing deployed model. Updatable fields include min_replica_count, max_replica_count, required_replica_count, autoscaling_metric_specs, disable_container_logging (v1 only), and enable_container_logging (v1beta1 only).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_mutate_deployed_model_execute()` or `aiplatform_projects_locations_endpoints_mutate_deployed_model`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_mutate_deployed_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_mutate_deployed_model_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:mutateDeployedModel
/// Updates an existing deployed model. Updatable fields include min_replica_count, max_replica_count, required_replica_count, autoscaling_metric_specs, disable_container_logging (v1 only), and enable_container_logging (v1beta1 only).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_mutate_deployed_model_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_mutate_deployed_model_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_mutate_deployed_model()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_mutate_deployed_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_mutate_deployed_model_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_mutate_deployed_model_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_mutate_deployed_model`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsMutateDeployedModelArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1MutateDeployedModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:mutateDeployedModel
/// Updates an existing deployed model. Updatable fields include min_replica_count, max_replica_count, required_replica_count, autoscaling_metric_specs, disable_container_logging (v1 only), and enable_container_logging (v1beta1 only).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_mutate_deployed_model_builder()` + `aiplatform_projects_locations_endpoints_mutate_deployed_model_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_mutate_deployed_model_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_mutate_deployed_model(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsMutateDeployedModelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_mutate_deployed_model_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_mutate_deployed_model_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Updates an Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_patch_execute()` to send, or `aiplatform_projects_locations_endpoints_patch` for simplest API.

pub fn aiplatform_projects_locations_endpoints_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Endpoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Updates an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_patch_execute()` or `aiplatform_projects_locations_endpoints_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Endpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Updates an Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Endpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Endpoint,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Updates an Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_patch_builder()` + `aiplatform_projects_locations_endpoints_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Endpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_predict_execute()` to send, or `aiplatform_projects_locations_endpoints_predict` for simplest API.

pub fn aiplatform_projects_locations_endpoints_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:predict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_predict_execute()` or `aiplatform_projects_locations_endpoints_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PredictResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predict
/// Perform an online prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_predict_builder()` + `aiplatform_projects_locations_endpoints_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predictLongRunning
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_predict_long_running_execute()` to send, or `aiplatform_projects_locations_endpoints_predict_long_running` for simplest API.

pub fn aiplatform_projects_locations_endpoints_predict_long_running_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictLongRunningRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:predictLongRunning",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_predict_long_running_execute()` or `aiplatform_projects_locations_endpoints_predict_long_running`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_predict_long_running_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_predict_long_running_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_predict_long_running_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_predict_long_running()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_predict_long_running_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_predict_long_running_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_predict_long_running`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsPredictLongRunningArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictLongRunningRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:predictLongRunning
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_predict_long_running_builder()` + `aiplatform_projects_locations_endpoints_predict_long_running_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_predict_long_running_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_predict_long_running(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsPredictLongRunningArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_predict_long_running_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_predict_long_running_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_raw_predict_execute()` to send, or `aiplatform_projects_locations_endpoints_raw_predict` for simplest API.

pub fn aiplatform_projects_locations_endpoints_raw_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1RawPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:rawPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_raw_predict_execute()` or `aiplatform_projects_locations_endpoints_raw_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_raw_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_raw_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_raw_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_raw_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_raw_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_raw_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_raw_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsRawPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RawPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_raw_predict_builder()` + `aiplatform_projects_locations_endpoints_raw_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_raw_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_raw_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsRawPredictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_raw_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_raw_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_server_streaming_predict_execute()` to send, or `aiplatform_projects_locations_endpoints_server_streaming_predict` for simplest API.

pub fn aiplatform_projects_locations_endpoints_server_streaming_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1StreamingPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:serverStreamingPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_server_streaming_predict_execute()` or `aiplatform_projects_locations_endpoints_server_streaming_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_server_streaming_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_server_streaming_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1StreamingPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1StreamingPredictResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_server_streaming_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_server_streaming_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_server_streaming_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_server_streaming_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_server_streaming_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1StreamingPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_server_streaming_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_server_streaming_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsServerStreamingPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamingPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_server_streaming_predict_builder()` + `aiplatform_projects_locations_endpoints_server_streaming_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_server_streaming_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_server_streaming_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsServerStreamingPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1StreamingPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_server_streaming_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_server_streaming_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_stream_generate_content_execute()` to send, or `aiplatform_projects_locations_endpoints_stream_generate_content` for simplest API.

pub fn aiplatform_projects_locations_endpoints_stream_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:streamGenerateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_stream_generate_content_execute()` or `aiplatform_projects_locations_endpoints_stream_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_stream_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_stream_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_stream_generate_content_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_stream_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_stream_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_stream_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_stream_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsStreamGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_stream_generate_content_builder()` + `aiplatform_projects_locations_endpoints_stream_generate_content_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_stream_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_stream_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsStreamGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_stream_generate_content_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_stream_generate_content_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_stream_raw_predict_execute()` to send, or `aiplatform_projects_locations_endpoints_stream_raw_predict` for simplest API.

pub fn aiplatform_projects_locations_endpoints_stream_raw_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1StreamRawPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:streamRawPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_stream_raw_predict_execute()` or `aiplatform_projects_locations_endpoints_stream_raw_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_stream_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_stream_raw_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_stream_raw_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_stream_raw_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_stream_raw_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_stream_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_stream_raw_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_stream_raw_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_stream_raw_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsStreamRawPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamRawPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_stream_raw_predict_builder()` + `aiplatform_projects_locations_endpoints_stream_raw_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_stream_raw_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_stream_raw_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsStreamRawPredictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_stream_raw_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_stream_raw_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:undeployModel
/// Undeploys a Model from an Endpoint, removing a DeployedModel from it, and freeing all resources it's using.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_undeploy_model_execute()` to send, or `aiplatform_projects_locations_endpoints_undeploy_model` for simplest API.

pub fn aiplatform_projects_locations_endpoints_undeploy_model_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1UndeployModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:undeployModel",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:undeployModel
/// Undeploys a Model from an Endpoint, removing a DeployedModel from it, and freeing all resources it's using.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_undeploy_model_execute()` or `aiplatform_projects_locations_endpoints_undeploy_model`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_undeploy_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_undeploy_model_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:undeployModel
/// Undeploys a Model from an Endpoint, removing a DeployedModel from it, and freeing all resources it's using.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_undeploy_model_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_undeploy_model_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_undeploy_model()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_undeploy_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_undeploy_model_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_undeploy_model_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_undeploy_model`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsUndeployModelArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UndeployModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:undeployModel
/// Undeploys a Model from an Endpoint, removing a DeployedModel from it, and freeing all resources it's using.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_undeploy_model_builder()` + `aiplatform_projects_locations_endpoints_undeploy_model_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_undeploy_model_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_undeploy_model(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsUndeployModelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_undeploy_model_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_undeploy_model_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:update
/// Updates an Endpoint with a long running operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_update_execute()` to send, or `aiplatform_projects_locations_endpoints_update` for simplest API.

pub fn aiplatform_projects_locations_endpoints_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1UpdateEndpointLongRunningRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}:update",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:update
/// Updates an Endpoint with a long running operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_update_execute()` or `aiplatform_projects_locations_endpoints_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:update
/// Updates an Endpoint with a long running operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_update_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UpdateEndpointLongRunningRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}:update
/// Updates an Endpoint with a long running operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_update_builder()` + `aiplatform_projects_locations_endpoints_update_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_update(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_endpoints_update_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_endpoints_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_chat_completions_execute()` to send, or `aiplatform_projects_locations_endpoints_chat_completions` for simplest API.

pub fn aiplatform_projects_locations_endpoints_chat_completions_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/chat/completions",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_chat_completions_execute()` or `aiplatform_projects_locations_endpoints_chat_completions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_chat_completions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_chat_completions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_chat_completions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_chat_completions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_chat_completions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_chat_completions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_chat_completions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_chat_completions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_chat_completions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsChatCompletionsArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/chat/completions
/// Exposes an OpenAI-compatible endpoint for chat completions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_chat_completions_builder()` + `aiplatform_projects_locations_endpoints_chat_completions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_chat_completions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_chat_completions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsChatCompletionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_chat_completions_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_chat_completions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/deployedModels/{deployedModelId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_execute()` to send, or `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke` for simplest API.

pub fn aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    deployedModelId: &str,
    invokeId: &str,
    body: &GoogleCloudAiplatformV1InvokeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/deployedModels/{}/invoke/{}",
        endpoint,
        deployedModelId,
        invokeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/deployedModels/{deployedModelId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_execute()` or `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/deployedModels/{deployedModelId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsDeployedModelsInvokeInvokeArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Path parameter: deployedModelId
    pub deployedModelId: String,
    /// Path parameter: invokeId
    pub invokeId: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1InvokeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/deployedModels/{deployedModelId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder()` + `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsDeployedModelsInvokeInvokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder(
        client,
        &args.endpoint,
        &args.deployedModelId,
        &args.invokeId,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/google/science/inference
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_google_science_inference_execute()` to send, or `aiplatform_projects_locations_endpoints_google_science_inference` for simplest API.

pub fn aiplatform_projects_locations_endpoints_google_science_inference_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    deployedModelId: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/google/science/inference",
        endpoint,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deployedModelId {
        query_parts.push(format!("deployedModelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/google/science/inference
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_google_science_inference_execute()` or `aiplatform_projects_locations_endpoints_google_science_inference`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_google_science_inference_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_google_science_inference_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/google/science/inference
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_google_science_inference_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_google_science_inference_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_google_science_inference()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_google_science_inference_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_google_science_inference_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_google_science_inference_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_google_science_inference`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsGoogleScienceInferenceArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Query parameter: deployedModelId
    pub deployedModelId: Option<String>,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/google/science/inference
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_google_science_inference_builder()` + `aiplatform_projects_locations_endpoints_google_science_inference_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_google_science_inference_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_google_science_inference(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsGoogleScienceInferenceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_google_science_inference_builder(
        client,
        &args.endpoint,
        args.deployedModelId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_google_science_inference_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_invoke_invoke_execute()` to send, or `aiplatform_projects_locations_endpoints_invoke_invoke` for simplest API.

pub fn aiplatform_projects_locations_endpoints_invoke_invoke_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    invokeId: &str,
    body: &GoogleCloudAiplatformV1InvokeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/invoke/{}",
        endpoint, invokeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_invoke_invoke_execute()` or `aiplatform_projects_locations_endpoints_invoke_invoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_invoke_invoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_invoke_invoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_invoke_invoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_invoke_invoke_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_invoke_invoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_invoke_invoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_invoke_invoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_invoke_invoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_invoke_invoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsInvokeInvokeArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Path parameter: invokeId
    pub invokeId: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1InvokeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_invoke_invoke_builder()` + `aiplatform_projects_locations_endpoints_invoke_invoke_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_invoke_invoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_invoke_invoke(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsInvokeInvokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_invoke_invoke_builder(
        client,
        &args.endpoint,
        &args.invokeId,
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_invoke_invoke_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/completions
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_openapi_completions_execute()` to send, or `aiplatform_projects_locations_endpoints_openapi_completions` for simplest API.

pub fn aiplatform_projects_locations_endpoints_openapi_completions_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    deployedModelId: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/openapi/completions",
        endpoint,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deployedModelId {
        query_parts.push(format!("deployedModelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/completions
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_openapi_completions_execute()` or `aiplatform_projects_locations_endpoints_openapi_completions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_openapi_completions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_openapi_completions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/completions
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_openapi_completions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_openapi_completions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_openapi_completions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_openapi_completions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_openapi_completions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_openapi_completions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_openapi_completions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOpenapiCompletionsArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Query parameter: deployedModelId
    pub deployedModelId: Option<String>,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/completions
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_openapi_completions_builder()` + `aiplatform_projects_locations_endpoints_openapi_completions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_openapi_completions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_openapi_completions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOpenapiCompletionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_openapi_completions_builder(
        client,
        &args.endpoint,
        args.deployedModelId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_openapi_completions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/embeddings
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_openapi_embeddings_execute()` to send, or `aiplatform_projects_locations_endpoints_openapi_embeddings` for simplest API.

pub fn aiplatform_projects_locations_endpoints_openapi_embeddings_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    deployedModelId: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/openapi/embeddings",
        endpoint,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deployedModelId {
        query_parts.push(format!("deployedModelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/embeddings
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_openapi_embeddings_execute()` or `aiplatform_projects_locations_endpoints_openapi_embeddings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_openapi_embeddings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_openapi_embeddings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/embeddings
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_openapi_embeddings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_openapi_embeddings_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_openapi_embeddings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_openapi_embeddings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_openapi_embeddings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_openapi_embeddings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_openapi_embeddings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOpenapiEmbeddingsArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Query parameter: deployedModelId
    pub deployedModelId: Option<String>,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/embeddings
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_openapi_embeddings_builder()` + `aiplatform_projects_locations_endpoints_openapi_embeddings_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_openapi_embeddings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_openapi_embeddings(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOpenapiEmbeddingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_openapi_embeddings_builder(
        client,
        &args.endpoint,
        args.deployedModelId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_openapi_embeddings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/responses
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_openapi_responses_execute()` to send, or `aiplatform_projects_locations_endpoints_openapi_responses` for simplest API.

pub fn aiplatform_projects_locations_endpoints_openapi_responses_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    deployedModelId: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/openapi/responses",
        endpoint,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deployedModelId {
        query_parts.push(format!("deployedModelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/responses
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_openapi_responses_execute()` or `aiplatform_projects_locations_endpoints_openapi_responses`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_openapi_responses_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_openapi_responses_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/responses
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_openapi_responses_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_openapi_responses_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_openapi_responses()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_openapi_responses_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_openapi_responses_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_openapi_responses_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_openapi_responses`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOpenapiResponsesArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Query parameter: deployedModelId
    pub deployedModelId: Option<String>,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/openapi/responses
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_openapi_responses_builder()` + `aiplatform_projects_locations_endpoints_openapi_responses_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_openapi_responses_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_openapi_responses(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOpenapiResponsesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_openapi_responses_builder(
        client,
        &args.endpoint,
        args.deployedModelId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_endpoints_openapi_responses_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_operations_cancel_execute()` to send, or `aiplatform_projects_locations_endpoints_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_endpoints_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_operations_cancel_execute()` or `aiplatform_projects_locations_endpoints_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_operations_cancel_builder()` + `aiplatform_projects_locations_endpoints_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_endpoints_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_endpoints_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_operations_delete_execute()` to send, or `aiplatform_projects_locations_endpoints_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_endpoints_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_operations_delete_execute()` or `aiplatform_projects_locations_endpoints_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_operations_delete_builder()` + `aiplatform_projects_locations_endpoints_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_endpoints_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_endpoints_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_operations_get_execute()` to send, or `aiplatform_projects_locations_endpoints_operations_get` for simplest API.

pub fn aiplatform_projects_locations_endpoints_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_operations_get_execute()` or `aiplatform_projects_locations_endpoints_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_operations_get_builder()` + `aiplatform_projects_locations_endpoints_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_endpoints_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_endpoints_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_operations_list_execute()` to send, or `aiplatform_projects_locations_endpoints_operations_list` for simplest API.

pub fn aiplatform_projects_locations_endpoints_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_operations_list_execute()` or `aiplatform_projects_locations_endpoints_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_operations_list_builder()` + `aiplatform_projects_locations_endpoints_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_endpoints_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_endpoints_operations_wait_execute()` to send, or `aiplatform_projects_locations_endpoints_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_endpoints_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/endpoints/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_endpoints_operations_wait_execute()` or `aiplatform_projects_locations_endpoints_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_endpoints_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_endpoints_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_endpoints_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_endpoints_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_endpoints_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEndpointsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_endpoints_operations_wait_builder()` + `aiplatform_projects_locations_endpoints_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_endpoints_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_endpoints_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEndpointsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_endpoints_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_endpoints_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Creates an Evaluation Item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_items_create_execute()` to send, or `aiplatform_projects_locations_evaluation_items_create` for simplest API.

pub fn aiplatform_projects_locations_evaluation_items_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1EvaluationItem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationItems",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Creates an Evaluation Item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_items_create_execute()` or `aiplatform_projects_locations_evaluation_items_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Creates an Evaluation Item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_items_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_items_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_items_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_items_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_items_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationItemsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluationItem,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Creates an Evaluation Item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_items_create_builder()` + `aiplatform_projects_locations_evaluation_items_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationItemsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_items_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_evaluation_items_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Deletes an Evaluation Item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_items_delete_execute()` to send, or `aiplatform_projects_locations_evaluation_items_delete` for simplest API.

pub fn aiplatform_projects_locations_evaluation_items_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationItems/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Deletes an Evaluation Item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_items_delete_execute()` or `aiplatform_projects_locations_evaluation_items_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Deletes an Evaluation Item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_items_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_items_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_items_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_items_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationItemsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Deletes an Evaluation Item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_items_delete_builder()` + `aiplatform_projects_locations_evaluation_items_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationItemsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_evaluation_items_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_evaluation_items_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Gets an Evaluation Item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_items_get_execute()` to send, or `aiplatform_projects_locations_evaluation_items_get` for simplest API.

pub fn aiplatform_projects_locations_evaluation_items_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationItems/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Gets an Evaluation Item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_items_get_execute()` or `aiplatform_projects_locations_evaluation_items_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Gets an Evaluation Item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_items_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_items_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_items_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_items_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_items_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationItemsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems/{evaluationItemsId}
/// Gets an Evaluation Item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_items_get_builder()` + `aiplatform_projects_locations_evaluation_items_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationItemsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_items_get_builder(client, &args.name)?;
    aiplatform_projects_locations_evaluation_items_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Lists Evaluation Items.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_items_list_execute()` to send, or `aiplatform_projects_locations_evaluation_items_list` for simplest API.

pub fn aiplatform_projects_locations_evaluation_items_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationItems",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Lists Evaluation Items.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_items_list_execute()` or `aiplatform_projects_locations_evaluation_items_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListEvaluationItemsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Lists Evaluation Items.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_items_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_items_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_items_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_items_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationItemsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationItems
/// Lists Evaluation Items.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_items_list_builder()` + `aiplatform_projects_locations_evaluation_items_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_items_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_items_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationItemsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_items_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_evaluation_items_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}:cancel
/// Cancels an Evaluation Run. Attempts to cancel a running Evaluation Run asynchronously. Status of run can be checked via GetEvaluationRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_runs_cancel_execute()` to send, or `aiplatform_projects_locations_evaluation_runs_cancel` for simplest API.

pub fn aiplatform_projects_locations_evaluation_runs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelEvaluationRunRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationRuns/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}:cancel
/// Cancels an Evaluation Run. Attempts to cancel a running Evaluation Run asynchronously. Status of run can be checked via GetEvaluationRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_runs_cancel_execute()` or `aiplatform_projects_locations_evaluation_runs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}:cancel
/// Cancels an Evaluation Run. Attempts to cancel a running Evaluation Run asynchronously. Status of run can be checked via GetEvaluationRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_runs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_runs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_runs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_runs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_runs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationRunsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelEvaluationRunRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}:cancel
/// Cancels an Evaluation Run. Attempts to cancel a running Evaluation Run asynchronously. Status of run can be checked via GetEvaluationRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_runs_cancel_builder()` + `aiplatform_projects_locations_evaluation_runs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationRunsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_runs_cancel_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_evaluation_runs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Creates an Evaluation Run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_runs_create_execute()` to send, or `aiplatform_projects_locations_evaluation_runs_create` for simplest API.

pub fn aiplatform_projects_locations_evaluation_runs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1EvaluationRun,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationRuns",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Creates an Evaluation Run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_runs_create_execute()` or `aiplatform_projects_locations_evaluation_runs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Creates an Evaluation Run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_runs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_runs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_runs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_runs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_runs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationRunsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluationRun,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Creates an Evaluation Run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_runs_create_builder()` + `aiplatform_projects_locations_evaluation_runs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationRunsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_runs_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_evaluation_runs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Deletes an Evaluation Run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_runs_delete_execute()` to send, or `aiplatform_projects_locations_evaluation_runs_delete` for simplest API.

pub fn aiplatform_projects_locations_evaluation_runs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationRuns/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Deletes an Evaluation Run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_runs_delete_execute()` or `aiplatform_projects_locations_evaluation_runs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Deletes an Evaluation Run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_runs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_runs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_runs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_runs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationRunsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Deletes an Evaluation Run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_runs_delete_builder()` + `aiplatform_projects_locations_evaluation_runs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationRunsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_runs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_evaluation_runs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Gets an Evaluation Run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_runs_get_execute()` to send, or `aiplatform_projects_locations_evaluation_runs_get` for simplest API.

pub fn aiplatform_projects_locations_evaluation_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationRuns/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Gets an Evaluation Run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_runs_get_execute()` or `aiplatform_projects_locations_evaluation_runs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Gets an Evaluation Run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_runs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_runs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_runs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationRunsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns/{evaluationRunsId}
/// Gets an Evaluation Run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_runs_get_builder()` + `aiplatform_projects_locations_evaluation_runs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationRunsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_runs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_evaluation_runs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Lists Evaluation Runs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_runs_list_execute()` to send, or `aiplatform_projects_locations_evaluation_runs_list` for simplest API.

pub fn aiplatform_projects_locations_evaluation_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationRuns",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Lists Evaluation Runs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_runs_list_execute()` or `aiplatform_projects_locations_evaluation_runs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListEvaluationRunsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Lists Evaluation Runs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_runs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_runs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_runs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationRunsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationRuns
/// Lists Evaluation Runs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_runs_list_builder()` + `aiplatform_projects_locations_evaluation_runs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_runs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_runs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationRunsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_runs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_evaluation_runs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Creates an Evaluation Set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_sets_create_execute()` to send, or `aiplatform_projects_locations_evaluation_sets_create` for simplest API.

pub fn aiplatform_projects_locations_evaluation_sets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1EvaluationSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationSets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Creates an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_sets_create_execute()` or `aiplatform_projects_locations_evaluation_sets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Creates an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_sets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_sets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_sets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationSetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluationSet,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Creates an Evaluation Set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_sets_create_builder()` + `aiplatform_projects_locations_evaluation_sets_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationSetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_sets_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_evaluation_sets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Deletes an Evaluation Set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_sets_delete_execute()` to send, or `aiplatform_projects_locations_evaluation_sets_delete` for simplest API.

pub fn aiplatform_projects_locations_evaluation_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Deletes an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_sets_delete_execute()` or `aiplatform_projects_locations_evaluation_sets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Deletes an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_sets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_sets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_sets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationSetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Deletes an Evaluation Set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_sets_delete_builder()` + `aiplatform_projects_locations_evaluation_sets_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationSetsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_sets_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_evaluation_sets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Gets an Evaluation Set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_sets_get_execute()` to send, or `aiplatform_projects_locations_evaluation_sets_get` for simplest API.

pub fn aiplatform_projects_locations_evaluation_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Gets an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_sets_get_execute()` or `aiplatform_projects_locations_evaluation_sets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Gets an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_sets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_sets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_sets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationSetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Gets an Evaluation Set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_sets_get_builder()` + `aiplatform_projects_locations_evaluation_sets_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationSetsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_sets_get_builder(client, &args.name)?;
    aiplatform_projects_locations_evaluation_sets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Lists Evaluation Sets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_sets_list_execute()` to send, or `aiplatform_projects_locations_evaluation_sets_list` for simplest API.

pub fn aiplatform_projects_locations_evaluation_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Lists Evaluation Sets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_sets_list_execute()` or `aiplatform_projects_locations_evaluation_sets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListEvaluationSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Lists Evaluation Sets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_sets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_sets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_sets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationSetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets
/// Lists Evaluation Sets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_sets_list_builder()` + `aiplatform_projects_locations_evaluation_sets_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationSetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEvaluationSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_sets_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_evaluation_sets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Updates an Evaluation Set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_evaluation_sets_patch_execute()` to send, or `aiplatform_projects_locations_evaluation_sets_patch` for simplest API.

pub fn aiplatform_projects_locations_evaluation_sets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1EvaluationSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/evaluationSets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Updates an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_evaluation_sets_patch_execute()` or `aiplatform_projects_locations_evaluation_sets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluationSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Updates an Evaluation Set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_evaluation_sets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_evaluation_sets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_evaluation_sets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_evaluation_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_evaluation_sets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_evaluation_sets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsEvaluationSetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluationSet,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluationSets/{evaluationSetsId}
/// Updates an Evaluation Set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_evaluation_sets_patch_builder()` + `aiplatform_projects_locations_evaluation_sets_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_evaluation_sets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_evaluation_sets_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsEvaluationSetsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluationSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_evaluation_sets_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_evaluation_sets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Creates a new FeatureGroup in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_create_execute()` to send, or `aiplatform_projects_locations_feature_groups_create` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    featureGroupId: Option<&str>,
    body: &GoogleCloudAiplatformV1FeatureGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = featureGroupId {
        query_parts.push(format!("featureGroupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Creates a new FeatureGroup in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_create_execute()` or `aiplatform_projects_locations_feature_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Creates a new FeatureGroup in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: featureGroupId
    pub featureGroupId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureGroup,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Creates a new FeatureGroup in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_create_builder()` + `aiplatform_projects_locations_feature_groups_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_create_builder(
        client,
        &args.parent,
        args.featureGroupId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_groups_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Deletes a single FeatureGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_delete_execute()` to send, or `aiplatform_projects_locations_feature_groups_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Deletes a single FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_delete_execute()` or `aiplatform_projects_locations_feature_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Deletes a single FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Deletes a single FeatureGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_delete_builder()` + `aiplatform_projects_locations_feature_groups_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_delete_builder(
        client, &args.name, args.force,
    )?;
    aiplatform_projects_locations_feature_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Gets details of a single FeatureGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_get_execute()` to send, or `aiplatform_projects_locations_feature_groups_get` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Gets details of a single FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_get_execute()` or `aiplatform_projects_locations_feature_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FeatureGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Gets details of a single FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Gets details of a single FeatureGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_get_builder()` + `aiplatform_projects_locations_feature_groups_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_get_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_feature_groups_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_get_iam_policy_execute()` or `aiplatform_projects_locations_feature_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_get_iam_policy_builder()` + `aiplatform_projects_locations_feature_groups_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_feature_groups_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Lists FeatureGroups in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_list_execute()` to send, or `aiplatform_projects_locations_feature_groups_list` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Lists FeatureGroups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_list_execute()` or `aiplatform_projects_locations_feature_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeatureGroupsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Lists FeatureGroups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups
/// Lists FeatureGroups in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_list_builder()` + `aiplatform_projects_locations_feature_groups_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_feature_groups_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Updates the parameters of a single FeatureGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_patch_execute()` to send, or `aiplatform_projects_locations_feature_groups_patch` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1FeatureGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Updates the parameters of a single FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_patch_execute()` or `aiplatform_projects_locations_feature_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Updates the parameters of a single FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureGroup,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}
/// Updates the parameters of a single FeatureGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_patch_builder()` + `aiplatform_projects_locations_feature_groups_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_groups_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_feature_groups_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_set_iam_policy_execute()` or `aiplatform_projects_locations_feature_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_set_iam_policy_builder()` + `aiplatform_projects_locations_feature_groups_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_feature_groups_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_feature_groups_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_test_iam_permissions_execute()` or `aiplatform_projects_locations_feature_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_test_iam_permissions_builder()` + `aiplatform_projects_locations_feature_groups_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_test_iam_permissions_builder(
        client,
        &args.resource,
        args.permissions.as_deref(),
    )?;
    aiplatform_projects_locations_feature_groups_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features:batchCreate
/// Creates a batch of Features in a given FeatureGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_batch_create_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_batch_create` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchCreateFeaturesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features:batchCreate
/// Creates a batch of Features in a given FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_batch_create_execute()` or `aiplatform_projects_locations_feature_groups_features_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features:batchCreate
/// Creates a batch of Features in a given FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_batch_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchCreateFeaturesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features:batchCreate
/// Creates a batch of Features in a given FeatureGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_batch_create_builder()` + `aiplatform_projects_locations_feature_groups_features_batch_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_batch_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_batch_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_feature_groups_features_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Creates a new Feature in a given FeatureGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_create_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_create` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    featureId: Option<&str>,
    body: &GoogleCloudAiplatformV1Feature,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = featureId {
        query_parts.push(format!("featureId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Creates a new Feature in a given FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_create_execute()` or `aiplatform_projects_locations_feature_groups_features_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Creates a new Feature in a given FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: featureId
    pub featureId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Feature,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Creates a new Feature in a given FeatureGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_create_builder()` + `aiplatform_projects_locations_feature_groups_features_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_create_builder(
        client,
        &args.parent,
        args.featureId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_groups_features_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_delete_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_delete_execute()` or `aiplatform_projects_locations_feature_groups_features_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_delete_builder()` + `aiplatform_projects_locations_feature_groups_features_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_groups_features_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_groups_features_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_get_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_get` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_get_execute()` or `aiplatform_projects_locations_feature_groups_features_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Feature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_get_builder()` + `aiplatform_projects_locations_feature_groups_features_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_groups_features_get_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_groups_features_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Lists Features in a given FeatureGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_list_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_list` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    latestStatsCount: Option<i32>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = latestStatsCount {
        query_parts.push(format!("latestStatsCount={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Lists Features in a given FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_list_execute()` or `aiplatform_projects_locations_feature_groups_features_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeaturesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Lists Features in a given FeatureGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: latestStatsCount
    pub latestStatsCount: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features
/// Lists Features in a given FeatureGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_list_builder()` + `aiplatform_projects_locations_feature_groups_features_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.latestStatsCount,
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_feature_groups_features_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_patch_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_patch` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Feature,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_patch_execute()` or `aiplatform_projects_locations_feature_groups_features_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Feature,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_patch_builder()` + `aiplatform_projects_locations_feature_groups_features_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_groups_features_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_operations_delete_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_operations_delete_execute()` or `aiplatform_projects_locations_feature_groups_features_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_groups_features_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_operations_delete_builder()` + `aiplatform_projects_locations_feature_groups_features_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_feature_groups_features_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_operations_get_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_operations_get` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_operations_get_execute()` or `aiplatform_projects_locations_feature_groups_features_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_operations_get_builder()` + `aiplatform_projects_locations_feature_groups_features_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_feature_groups_features_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_operations_list_wait_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_operations_list_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_operations_list_wait_execute()` or `aiplatform_projects_locations_feature_groups_features_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_groups_features_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_operations_list_wait_builder()` + `aiplatform_projects_locations_feature_groups_features_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_groups_features_operations_list_wait_builder(
            client,
            &args.name,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.returnPartialSuccess,
        )?;
    aiplatform_projects_locations_feature_groups_features_operations_list_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_features_operations_wait_execute()` to send, or `aiplatform_projects_locations_feature_groups_features_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_features_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/features/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_features_operations_wait_execute()` or `aiplatform_projects_locations_feature_groups_features_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_features_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_features_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_features_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_features_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_features_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_features_operations_wait_builder()` + `aiplatform_projects_locations_feature_groups_features_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_features_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_features_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_features_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_feature_groups_features_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_operations_delete_execute()` to send, or `aiplatform_projects_locations_feature_groups_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_operations_delete_execute()` or `aiplatform_projects_locations_feature_groups_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_operations_delete_builder()` + `aiplatform_projects_locations_feature_groups_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_groups_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_groups_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_operations_get_execute()` to send, or `aiplatform_projects_locations_feature_groups_operations_get` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_operations_get_execute()` or `aiplatform_projects_locations_feature_groups_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_operations_get_builder()` + `aiplatform_projects_locations_feature_groups_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_groups_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_groups_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_operations_list_wait_execute()` to send, or `aiplatform_projects_locations_feature_groups_operations_list_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_operations_list_wait_execute()` or `aiplatform_projects_locations_feature_groups_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_operations_list_wait_builder()` + `aiplatform_projects_locations_feature_groups_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_operations_list_wait_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_feature_groups_operations_list_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_groups_operations_wait_execute()` to send, or `aiplatform_projects_locations_feature_groups_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_groups_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureGroups/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_groups_operations_wait_execute()` or `aiplatform_projects_locations_feature_groups_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_groups_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_groups_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_groups_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_groups_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_groups_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_groups_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureGroupsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureGroups/{featureGroupsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_groups_operations_wait_builder()` + `aiplatform_projects_locations_feature_groups_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_groups_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_groups_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureGroupsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_groups_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_feature_groups_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Creates a new FeatureOnlineStore in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_create_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_create` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    featureOnlineStoreId: Option<&str>,
    body: &GoogleCloudAiplatformV1FeatureOnlineStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = featureOnlineStoreId {
        query_parts.push(format!("featureOnlineStoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Creates a new FeatureOnlineStore in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_create_execute()` or `aiplatform_projects_locations_feature_online_stores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Creates a new FeatureOnlineStore in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: featureOnlineStoreId
    pub featureOnlineStoreId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureOnlineStore,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Creates a new FeatureOnlineStore in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_create_builder()` + `aiplatform_projects_locations_feature_online_stores_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_create_builder(
        client,
        &args.parent,
        args.featureOnlineStoreId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_online_stores_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Deletes a single FeatureOnlineStore. The FeatureOnlineStore must not contain any FeatureViews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_delete_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Deletes a single FeatureOnlineStore. The FeatureOnlineStore must not contain any FeatureViews.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_delete_execute()` or `aiplatform_projects_locations_feature_online_stores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Deletes a single FeatureOnlineStore. The FeatureOnlineStore must not contain any FeatureViews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Deletes a single FeatureOnlineStore. The FeatureOnlineStore must not contain any FeatureViews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_delete_builder()` + `aiplatform_projects_locations_feature_online_stores_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_delete_builder(
        client, &args.name, args.force,
    )?;
    aiplatform_projects_locations_feature_online_stores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Gets details of a single FeatureOnlineStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_get_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_get` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Gets details of a single FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_get_execute()` or `aiplatform_projects_locations_feature_online_stores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureOnlineStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FeatureOnlineStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Gets details of a single FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureOnlineStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Gets details of a single FeatureOnlineStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_get_builder()` + `aiplatform_projects_locations_feature_online_stores_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureOnlineStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_online_stores_get_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_online_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_get_iam_policy_execute()` or `aiplatform_projects_locations_feature_online_stores_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder()` + `aiplatform_projects_locations_feature_online_stores_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_feature_online_stores_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Lists FeatureOnlineStores in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_list_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_list` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Lists FeatureOnlineStores in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_list_execute()` or `aiplatform_projects_locations_feature_online_stores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListFeatureOnlineStoresResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeatureOnlineStoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Lists FeatureOnlineStores in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListFeatureOnlineStoresResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores
/// Lists FeatureOnlineStores in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_list_builder()` + `aiplatform_projects_locations_feature_online_stores_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListFeatureOnlineStoresResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_feature_online_stores_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Updates the parameters of a single FeatureOnlineStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_patch_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_patch` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1FeatureOnlineStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Updates the parameters of a single FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_patch_execute()` or `aiplatform_projects_locations_feature_online_stores_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Updates the parameters of a single FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureOnlineStore,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}
/// Updates the parameters of a single FeatureOnlineStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_patch_builder()` + `aiplatform_projects_locations_feature_online_stores_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_online_stores_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_set_iam_policy_execute()` or `aiplatform_projects_locations_feature_online_stores_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder()` + `aiplatform_projects_locations_feature_online_stores_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_feature_online_stores_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_execute()` or `aiplatform_projects_locations_feature_online_stores_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder()` + `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder(
        client,
        &args.resource,
        args.permissions.as_deref(),
    )?;
    aiplatform_projects_locations_feature_online_stores_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Creates a new FeatureView in a given FeatureOnlineStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_create_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_create` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    featureViewId: Option<&str>,
    runSyncImmediately: Option<bool>,
    body: &GoogleCloudAiplatformV1FeatureView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = featureViewId {
        query_parts.push(format!("featureViewId={}", val));
    }
    if let Some(val) = runSyncImmediately {
        query_parts.push(format!("runSyncImmediately={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Creates a new FeatureView in a given FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_create_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Creates a new FeatureView in a given FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: featureViewId
    pub featureViewId: Option<String>,
    /// Query parameter: runSyncImmediately
    pub runSyncImmediately: Option<bool>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureView,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Creates a new FeatureView in a given FeatureOnlineStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_create_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_create_builder(
        client,
        &args.parent,
        args.featureViewId.as_deref(),
        args.runSyncImmediately,
        &args.body,
    )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Deletes a single FeatureView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_delete_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Deletes a single FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_delete_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Deletes a single FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Deletes a single FeatureView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:directWrite
/// Bidirectional streaming RPC to directly write to feature values in a feature view. Requests may not have a one-to-one mapping to responses and responses may be returned out-of-order to reduce latency.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder(
    client: &SimpleHttpClient,
    featureView: &str,
    body: &GoogleCloudAiplatformV1FeatureViewDirectWriteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:directWrite",
        featureView,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:directWrite
/// Bidirectional streaming RPC to directly write to feature values in a feature view. Requests may not have a one-to-one mapping to responses and responses may be returned out-of-order to reduce latency.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1FeatureViewDirectWriteResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FeatureViewDirectWriteResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:directWrite
/// Bidirectional streaming RPC to directly write to feature values in a feature view. Requests may not have a one-to-one mapping to responses and responses may be returned out-of-order to reduce latency.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1FeatureViewDirectWriteResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_direct_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDirectWriteArgs {
    /// Path parameter: featureView
    pub featureView: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureViewDirectWriteRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:directWrite
/// Bidirectional streaming RPC to directly write to feature values in a feature view. Requests may not have a one-to-one mapping to responses and responses may be returned out-of-order to reduce latency.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_direct_write(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDirectWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1FeatureViewDirectWriteResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder(
            client,
            &args.featureView,
            &args.body,
        )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:fetchFeatureValues
/// Fetch feature values under a FeatureView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder(
    client: &SimpleHttpClient,
    featureView: &str,
    body: &GoogleCloudAiplatformV1FetchFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:fetchFeatureValues",
        featureView,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:fetchFeatureValues
/// Fetch feature values under a FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FetchFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FetchFeatureValuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:fetchFeatureValues
/// Fetch feature values under a FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FetchFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFetchFeatureValuesArgs {
    /// Path parameter: featureView
    pub featureView: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FetchFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:fetchFeatureValues
/// Fetch feature values under a FeatureView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFetchFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FetchFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder(client, &args.featureView, &args.body)?;
    aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:generateFetchAccessToken
/// RPC to generate an access token for the given feature view. FeatureViews under the same FeatureOnlineStore share the same access token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder(
    client: &SimpleHttpClient,
    featureView: &str,
    body: &GoogleCloudAiplatformV1GenerateFetchAccessTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:generateFetchAccessToken",
        featureView,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:generateFetchAccessToken
/// RPC to generate an access token for the given feature view. FeatureViews under the same FeatureOnlineStore share the same access token.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:generateFetchAccessToken
/// RPC to generate an access token for the given feature view. FeatureViews under the same FeatureOnlineStore share the same access token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGenerateFetchAccessTokenArgs {
    /// Path parameter: featureView
    pub featureView: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateFetchAccessTokenRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:generateFetchAccessToken
/// RPC to generate an access token for the given feature view. FeatureViews under the same FeatureOnlineStore share the same access token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGenerateFetchAccessTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder(client, &args.featureView, &args.body)?;
    aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Gets details of a single FeatureView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_get_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_get` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Gets details of a single FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FeatureView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Gets details of a single FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Gets details of a single FeatureView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder(
            client,
            &args.resource,
            args.options_requestedPolicyVersion,
        )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Lists FeatureViews in a given FeatureOnlineStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_list_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_list` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Lists FeatureViews in a given FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_list_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureViewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeatureViewsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Lists FeatureViews in a given FeatureOnlineStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureViewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews
/// Lists FeatureViews in a given FeatureOnlineStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_list_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureViewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Updates the parameters of a single FeatureView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_patch_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_patch` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1FeatureView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Updates the parameters of a single FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_patch_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Updates the parameters of a single FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FeatureView,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}
/// Updates the parameters of a single FeatureView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:searchNearestEntities
/// Search the nearest entities under a FeatureView. Search only works for indexable feature view; if a feature view isn't indexable, returns Invalid argument response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder(
    client: &SimpleHttpClient,
    featureView: &str,
    body: &GoogleCloudAiplatformV1SearchNearestEntitiesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:searchNearestEntities",
        featureView,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:searchNearestEntities
/// Search the nearest entities under a FeatureView. Search only works for indexable feature view; if a feature view isn't indexable, returns Invalid argument response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchNearestEntitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SearchNearestEntitiesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:searchNearestEntities
/// Search the nearest entities under a FeatureView. Search only works for indexable feature view; if a feature view isn't indexable, returns Invalid argument response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchNearestEntitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSearchNearestEntitiesArgs {
    /// Path parameter: featureView
    pub featureView: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SearchNearestEntitiesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:searchNearestEntities
/// Search the nearest entities under a FeatureView. Search only works for indexable feature view; if a feature view isn't indexable, returns Invalid argument response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSearchNearestEntitiesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchNearestEntitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder(client, &args.featureView, &args.body)?;
    aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder(
            client,
            &args.resource,
            &args.body,
        )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:sync
/// Triggers on-demand sync for the FeatureView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_sync_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_sync` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder(
    client: &SimpleHttpClient,
    featureView: &str,
    body: &GoogleCloudAiplatformV1SyncFeatureViewRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:sync",
        featureView,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:sync
/// Triggers on-demand sync for the FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_sync_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_sync`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_sync_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SyncFeatureViewResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SyncFeatureViewResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:sync
/// Triggers on-demand sync for the FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_sync_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_sync()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SyncFeatureViewResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_sync_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_sync`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSyncArgs {
    /// Path parameter: featureView
    pub featureView: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SyncFeatureViewRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:sync
/// Triggers on-demand sync for the FeatureView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_sync_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_sync_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_sync(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSyncArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SyncFeatureViewResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder(
        client,
        &args.featureView,
        &args.body,
    )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_sync_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder(client, &args.resource, args.permissions.as_deref())?;
    aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs/{featureViewSyncsId}
/// Gets details of a single FeatureViewSync.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}/featureViewSyncs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs/{featureViewSyncsId}
/// Gets details of a single FeatureViewSync.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureViewSync>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FeatureViewSync = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs/{featureViewSyncsId}
/// Gets details of a single FeatureViewSync.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureViewSync>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFeatureViewSyncsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs/{featureViewSyncsId}
/// Gets details of a single FeatureViewSync.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFeatureViewSyncsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FeatureViewSync>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs
/// Lists FeatureViewSyncs in a given FeatureView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}/featureViewSyncs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs
/// Lists FeatureViewSyncs in a given FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureViewSyncsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeatureViewSyncsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs
/// Lists FeatureViewSyncs in a given FeatureView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureViewSyncsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFeatureViewSyncsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/featureViewSyncs
/// Lists FeatureViewSyncs in a given FeatureView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFeatureViewSyncsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeatureViewSyncsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_builder(client, &args.parent, args.filter.as_deref(), args.orderBy.as_deref(), args.pageSize, args.pageToken.as_deref())?;
    aiplatform_projects_locations_feature_online_stores_feature_views_feature_view_syncs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_builder(client, &args.name, args.filter.as_deref(), args.pageSize, args.pageToken.as_deref(), args.returnPartialSuccess)?;
    aiplatform_projects_locations_feature_online_stores_feature_views_operations_list_wait_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/featureViews/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_execute()` or `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/featureViews/{featureViewsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder()` + `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder(
            client,
            &args.name,
            args.timeout.as_deref(),
        )?;
    aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_operations_delete_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_operations_delete_execute()` or `aiplatform_projects_locations_feature_online_stores_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_operations_delete_builder()` + `aiplatform_projects_locations_feature_online_stores_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_feature_online_stores_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_operations_get_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_operations_get` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_operations_get_execute()` or `aiplatform_projects_locations_feature_online_stores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_operations_get_builder()` + `aiplatform_projects_locations_feature_online_stores_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_feature_online_stores_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_operations_list_wait_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_operations_list_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_operations_list_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_operations_list_wait_execute()` or `aiplatform_projects_locations_feature_online_stores_operations_list_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_list_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_operations_list_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_list_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_operations_list_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_list_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_operations_list_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_feature_online_stores_operations_list_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_operations_list_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresOperationsListWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_operations_list_wait_builder()` + `aiplatform_projects_locations_feature_online_stores_operations_list_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_list_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_list_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresOperationsListWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_operations_list_wait_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_feature_online_stores_operations_list_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_feature_online_stores_operations_wait_execute()` to send, or `aiplatform_projects_locations_feature_online_stores_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_feature_online_stores_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featureOnlineStores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_feature_online_stores_operations_wait_execute()` or `aiplatform_projects_locations_feature_online_stores_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_feature_online_stores_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_feature_online_stores_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_feature_online_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_feature_online_stores_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_feature_online_stores_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_feature_online_stores_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeatureOnlineStoresOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featureOnlineStores/{featureOnlineStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_feature_online_stores_operations_wait_builder()` + `aiplatform_projects_locations_feature_online_stores_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_feature_online_stores_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_feature_online_stores_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeatureOnlineStoresOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_feature_online_stores_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_feature_online_stores_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:batchReadFeatureValues
/// Batch reads Feature values from a Featurestore. This API enables batch reading Feature values, where each read instance in the batch may read Feature values of entities from one or more EntityTypes. Point-in-time correctness is guaranteed for Feature values of each read instance as of each instance's read timestamp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_batch_read_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_batch_read_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_batch_read_feature_values_builder(
    client: &SimpleHttpClient,
    featurestore: &str,
    body: &GoogleCloudAiplatformV1BatchReadFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}:batchReadFeatureValues",
        featurestore,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:batchReadFeatureValues
/// Batch reads Feature values from a Featurestore. This API enables batch reading Feature values, where each read instance in the batch may read Feature values of entities from one or more EntityTypes. Point-in-time correctness is guaranteed for Feature values of each read instance as of each instance's read timestamp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_batch_read_feature_values_execute()` or `aiplatform_projects_locations_featurestores_batch_read_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_batch_read_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_batch_read_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:batchReadFeatureValues
/// Batch reads Feature values from a Featurestore. This API enables batch reading Feature values, where each read instance in the batch may read Feature values of entities from one or more EntityTypes. Point-in-time correctness is guaranteed for Feature values of each read instance as of each instance's read timestamp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_batch_read_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_batch_read_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_batch_read_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_batch_read_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_batch_read_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_batch_read_feature_values_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_batch_read_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresBatchReadFeatureValuesArgs {
    /// Path parameter: featurestore
    pub featurestore: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchReadFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:batchReadFeatureValues
/// Batch reads Feature values from a Featurestore. This API enables batch reading Feature values, where each read instance in the batch may read Feature values of entities from one or more EntityTypes. Point-in-time correctness is guaranteed for Feature values of each read instance as of each instance's read timestamp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_batch_read_feature_values_builder()` + `aiplatform_projects_locations_featurestores_batch_read_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_batch_read_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_batch_read_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresBatchReadFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_batch_read_feature_values_builder(
        client,
        &args.featurestore,
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_batch_read_feature_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Creates a new Featurestore in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_create_execute()` to send, or `aiplatform_projects_locations_featurestores_create` for simplest API.

pub fn aiplatform_projects_locations_featurestores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    featurestoreId: Option<&str>,
    body: &GoogleCloudAiplatformV1Featurestore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = featurestoreId {
        query_parts.push(format!("featurestoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Creates a new Featurestore in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_create_execute()` or `aiplatform_projects_locations_featurestores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Creates a new Featurestore in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: featurestoreId
    pub featurestoreId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Featurestore,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Creates a new Featurestore in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_create_builder()` + `aiplatform_projects_locations_featurestores_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_create_builder(
        client,
        &args.parent,
        args.featurestoreId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Deletes a single Featurestore. The Featurestore must not contain any EntityTypes or force must be set to `true` for the request to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_delete_execute()` to send, or `aiplatform_projects_locations_featurestores_delete` for simplest API.

pub fn aiplatform_projects_locations_featurestores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Deletes a single Featurestore. The Featurestore must not contain any EntityTypes or force must be set to `true` for the request to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_delete_execute()` or `aiplatform_projects_locations_featurestores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Deletes a single Featurestore. The Featurestore must not contain any EntityTypes or force must be set to `true` for the request to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Deletes a single Featurestore. The Featurestore must not contain any EntityTypes or force must be set to `true` for the request to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_delete_builder()` + `aiplatform_projects_locations_featurestores_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_delete_builder(client, &args.name, args.force)?;
    aiplatform_projects_locations_featurestores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Gets details of a single Featurestore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_get_execute()` to send, or `aiplatform_projects_locations_featurestores_get` for simplest API.

pub fn aiplatform_projects_locations_featurestores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Gets details of a single Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_get_execute()` or `aiplatform_projects_locations_featurestores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Featurestore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Featurestore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Gets details of a single Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Featurestore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Gets details of a single Featurestore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_get_builder()` + `aiplatform_projects_locations_featurestores_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Featurestore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_get_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_featurestores_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_featurestores_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_get_iam_policy_execute()` or `aiplatform_projects_locations_featurestores_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_get_iam_policy_builder()` + `aiplatform_projects_locations_featurestores_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_featurestores_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Lists Featurestores in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_list_execute()` to send, or `aiplatform_projects_locations_featurestores_list` for simplest API.

pub fn aiplatform_projects_locations_featurestores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Lists Featurestores in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_list_execute()` or `aiplatform_projects_locations_featurestores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturestoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeaturestoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Lists Featurestores in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturestoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores
/// Lists Featurestores in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_list_builder()` + `aiplatform_projects_locations_featurestores_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturestoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Updates the parameters of a single Featurestore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_patch_execute()` to send, or `aiplatform_projects_locations_featurestores_patch` for simplest API.

pub fn aiplatform_projects_locations_featurestores_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Featurestore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Updates the parameters of a single Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_patch_execute()` or `aiplatform_projects_locations_featurestores_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Updates the parameters of a single Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Featurestore,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}
/// Updates the parameters of a single Featurestore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_patch_builder()` + `aiplatform_projects_locations_featurestores_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores:searchFeatures
/// Searches Features matching a query in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_search_features_execute()` to send, or `aiplatform_projects_locations_featurestores_search_features` for simplest API.

pub fn aiplatform_projects_locations_featurestores_search_features_builder(
    client: &SimpleHttpClient,
    location: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores:searchFeatures",
        location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores:searchFeatures
/// Searches Features matching a query in a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_search_features_execute()` or `aiplatform_projects_locations_featurestores_search_features`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_search_features_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_search_features_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SearchFeaturesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores:searchFeatures
/// Searches Features matching a query in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_search_features_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_search_features_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_search_features()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_search_features_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_search_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_search_features_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_search_features`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresSearchFeaturesArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores:searchFeatures
/// Searches Features matching a query in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_search_features_builder()` + `aiplatform_projects_locations_featurestores_search_features_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_search_features_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_search_features(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresSearchFeaturesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SearchFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_search_features_builder(
        client,
        &args.location,
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_search_features_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_featurestores_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_featurestores_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_set_iam_policy_execute()` or `aiplatform_projects_locations_featurestores_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_set_iam_policy_builder()` + `aiplatform_projects_locations_featurestores_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_featurestores_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_featurestores_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_test_iam_permissions_execute()` or `aiplatform_projects_locations_featurestores_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_test_iam_permissions_builder()` + `aiplatform_projects_locations_featurestores_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_test_iam_permissions_builder(
        client,
        &args.resource,
        args.permissions.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Creates a new EntityType in a given Featurestore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_create_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_create` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    entityTypeId: Option<&str>,
    body: &GoogleCloudAiplatformV1EntityType,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = entityTypeId {
        query_parts.push(format!("entityTypeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Creates a new EntityType in a given Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_create_execute()` or `aiplatform_projects_locations_featurestores_entity_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Creates a new EntityType in a given Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: entityTypeId
    pub entityTypeId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EntityType,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Creates a new EntityType in a given Featurestore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_create_builder()` + `aiplatform_projects_locations_featurestores_entity_types_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_create_builder(
        client,
        &args.parent,
        args.entityTypeId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Deletes a single EntityType. The EntityType must not have any Features or force must be set to `true` for the request to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_delete_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_delete` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Deletes a single EntityType. The EntityType must not have any Features or force must be set to `true` for the request to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_delete_execute()` or `aiplatform_projects_locations_featurestores_entity_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Deletes a single EntityType. The EntityType must not have any Features or force must be set to `true` for the request to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Deletes a single EntityType. The EntityType must not have any Features or force must be set to `true` for the request to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_delete_builder()` + `aiplatform_projects_locations_featurestores_entity_types_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_delete_builder(
        client, &args.name, args.force,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:deleteFeatureValues
/// Delete Feature values from Featurestore. The progress of the deletion is tracked by the returned operation. The deleted feature values are guaranteed to be invisible to subsequent read operations after the operation is marked as successfully done. If a delete feature values operation fails, the feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same delete request again and wait till the new operation returned is marked as successfully done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    body: &GoogleCloudAiplatformV1DeleteFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:deleteFeatureValues",
        entityType,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:deleteFeatureValues
/// Delete Feature values from Featurestore. The progress of the deletion is tracked by the returned operation. The deleted feature values are guaranteed to be invisible to subsequent read operations after the operation is marked as successfully done. If a delete feature values operation fails, the feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same delete request again and wait till the new operation returned is marked as successfully done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_execute()` or `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:deleteFeatureValues
/// Delete Feature values from Featurestore. The progress of the deletion is tracked by the returned operation. The deleted feature values are guaranteed to be invisible to subsequent read operations after the operation is marked as successfully done. If a delete feature values operation fails, the feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same delete request again and wait till the new operation returned is marked as successfully done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_delete_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteFeatureValuesArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DeleteFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:deleteFeatureValues
/// Delete Feature values from Featurestore. The progress of the deletion is tracked by the returned operation. The deleted feature values are guaranteed to be invisible to subsequent read operations after the operation is marked as successfully done. If a delete feature values operation fails, the feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same delete request again and wait till the new operation returned is marked as successfully done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder()` + `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_delete_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder(
            client,
            &args.entityType,
            &args.body,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:exportFeatureValues
/// Exports Feature values from all the entities of a target EntityType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_export_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    body: &GoogleCloudAiplatformV1ExportFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:exportFeatureValues",
        entityType,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:exportFeatureValues
/// Exports Feature values from all the entities of a target EntityType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_execute()` or `aiplatform_projects_locations_featurestores_entity_types_export_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_export_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:exportFeatureValues
/// Exports Feature values from all the entities of a target EntityType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_export_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_export_feature_values_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_export_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesExportFeatureValuesArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExportFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:exportFeatureValues
/// Exports Feature values from all the entities of a target EntityType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder()` + `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_export_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_export_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesExportFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder(
            client,
            &args.entityType,
            &args.body,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_export_feature_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Gets details of a single EntityType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_get_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_get` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Gets details of a single EntityType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_get_execute()` or `aiplatform_projects_locations_featurestores_entity_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Gets details of a single EntityType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Gets details of a single EntityType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_get_builder()` + `aiplatform_projects_locations_featurestores_entity_types_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_get_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_entity_types_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_execute()` or `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder()` + `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:importFeatureValues
/// Imports Feature values into the Featurestore from a source storage. The progress of the import is tracked by the returned operation. The imported features are guaranteed to be visible to subsequent read operations after the operation is marked as successfully done. If an import operation fails, the Feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same import request again and wait till the new operation returned is marked as successfully done. There are also scenarios where the caller can cause inconsistency. - Source data for import contains multiple distinct Feature values for the same entity ID and timestamp. - Source is modified during an import. This includes adding, updating, or removing source data `and/or` metadata. Examples of updating metadata include but are not limited to changing storage location, storage class, or retention policy. - Online serving cluster is under-provisioned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_import_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    body: &GoogleCloudAiplatformV1ImportFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:importFeatureValues",
        entityType,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:importFeatureValues
/// Imports Feature values into the Featurestore from a source storage. The progress of the import is tracked by the returned operation. The imported features are guaranteed to be visible to subsequent read operations after the operation is marked as successfully done. If an import operation fails, the Feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same import request again and wait till the new operation returned is marked as successfully done. There are also scenarios where the caller can cause inconsistency. - Source data for import contains multiple distinct Feature values for the same entity ID and timestamp. - Source is modified during an import. This includes adding, updating, or removing source data `and/or` metadata. Examples of updating metadata include but are not limited to changing storage location, storage class, or retention policy. - Online serving cluster is under-provisioned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_execute()` or `aiplatform_projects_locations_featurestores_entity_types_import_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_import_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:importFeatureValues
/// Imports Feature values into the Featurestore from a source storage. The progress of the import is tracked by the returned operation. The imported features are guaranteed to be visible to subsequent read operations after the operation is marked as successfully done. If an import operation fails, the Feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same import request again and wait till the new operation returned is marked as successfully done. There are also scenarios where the caller can cause inconsistency. - Source data for import contains multiple distinct Feature values for the same entity ID and timestamp. - Source is modified during an import. This includes adding, updating, or removing source data `and/or` metadata. Examples of updating metadata include but are not limited to changing storage location, storage class, or retention policy. - Online serving cluster is under-provisioned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_import_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_import_feature_values_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_import_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesImportFeatureValuesArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ImportFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:importFeatureValues
/// Imports Feature values into the Featurestore from a source storage. The progress of the import is tracked by the returned operation. The imported features are guaranteed to be visible to subsequent read operations after the operation is marked as successfully done. If an import operation fails, the Feature values returned from reads and exports may be inconsistent. If consistency is required, the caller must retry the same import request again and wait till the new operation returned is marked as successfully done. There are also scenarios where the caller can cause inconsistency. - Source data for import contains multiple distinct Feature values for the same entity ID and timestamp. - Source is modified during an import. This includes adding, updating, or removing source data `and/or` metadata. Examples of updating metadata include but are not limited to changing storage location, storage class, or retention policy. - Online serving cluster is under-provisioned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder()` + `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_import_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_import_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesImportFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder(
            client,
            &args.entityType,
            &args.body,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_import_feature_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Lists EntityTypes in a given Featurestore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_list_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_list` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Lists EntityTypes in a given Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_list_execute()` or `aiplatform_projects_locations_featurestores_entity_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEntityTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListEntityTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Lists EntityTypes in a given Featurestore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEntityTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes
/// Lists EntityTypes in a given Featurestore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_list_builder()` + `aiplatform_projects_locations_featurestores_entity_types_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEntityTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_entity_types_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Updates the parameters of a single EntityType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_patch_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_patch` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1EntityType,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Updates the parameters of a single EntityType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_patch_execute()` or `aiplatform_projects_locations_featurestores_entity_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Updates the parameters of a single EntityType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EntityType,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}
/// Updates the parameters of a single EntityType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_patch_builder()` + `aiplatform_projects_locations_featurestores_entity_types_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:readFeatureValues
/// Reads Feature values of a specific entity of an EntityType. For reading feature values of multiple entities of an EntityType, please use StreamingReadFeatureValues.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_read_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    body: &GoogleCloudAiplatformV1ReadFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:readFeatureValues",
        entityType,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:readFeatureValues
/// Reads Feature values of a specific entity of an EntityType. For reading feature values of multiple entities of an EntityType, please use StreamingReadFeatureValues.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_execute()` or `aiplatform_projects_locations_featurestores_entity_types_read_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_read_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadFeatureValuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:readFeatureValues
/// Reads Feature values of a specific entity of an EntityType. For reading feature values of multiple entities of an EntityType, please use StreamingReadFeatureValues.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_read_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_read_feature_values_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_read_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesReadFeatureValuesArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ReadFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:readFeatureValues
/// Reads Feature values of a specific entity of an EntityType. For reading feature values of multiple entities of an EntityType, please use StreamingReadFeatureValues.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder()` + `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_read_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_read_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesReadFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder(
            client,
            &args.entityType,
            &args.body,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_read_feature_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_execute()` or `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder()` + `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:streamingReadFeatureValues
/// Reads Feature values for multiple entities. Depending on their size, data for different entities may be broken up across multiple responses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    body: &GoogleCloudAiplatformV1StreamingReadFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:streamingReadFeatureValues",
        entityType,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:streamingReadFeatureValues
/// Reads Feature values for multiple entities. Depending on their size, data for different entities may be broken up across multiple responses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_execute()` or `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadFeatureValuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:streamingReadFeatureValues
/// Reads Feature values for multiple entities. Depending on their size, data for different entities may be broken up across multiple responses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesStreamingReadFeatureValuesArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamingReadFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:streamingReadFeatureValues
/// Reads Feature values for multiple entities. Depending on their size, data for different entities may be broken up across multiple responses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder()` + `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesStreamingReadFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder(client, &args.entityType, &args.body)?;
    aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_execute()` or `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder()` + `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder(
            client,
            &args.resource,
            args.permissions.as_deref(),
        )?;
    aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:writeFeatureValues
/// Writes Feature values of one or more entities of an EntityType. The Feature values are merged into existing entities if any. The Feature values to be written must have timestamp within the online storage retention.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_write_feature_values` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    body: &GoogleCloudAiplatformV1WriteFeatureValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}:writeFeatureValues",
        entityType,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:writeFeatureValues
/// Writes Feature values of one or more entities of an EntityType. The Feature values are merged into existing entities if any. The Feature values to be written must have timestamp within the online storage retention.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_execute()` or `aiplatform_projects_locations_featurestores_entity_types_write_feature_values`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_write_feature_values_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1WriteFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1WriteFeatureValuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:writeFeatureValues
/// Writes Feature values of one or more entities of an EntityType. The Feature values are merged into existing entities if any. The Feature values to be written must have timestamp within the online storage retention.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_write_feature_values_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1WriteFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_write_feature_values_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_write_feature_values`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesWriteFeatureValuesArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1WriteFeatureValuesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}:writeFeatureValues
/// Writes Feature values of one or more entities of an EntityType. The Feature values are merged into existing entities if any. The Feature values to be written must have timestamp within the online storage retention.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder()` + `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_write_feature_values_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_write_feature_values(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesWriteFeatureValuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1WriteFeatureValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder(
            client,
            &args.entityType,
            &args.body,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_write_feature_values_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features:batchCreate
/// Creates a batch of Features in a given EntityType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_batch_create` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchCreateFeaturesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features:batchCreate
/// Creates a batch of Features in a given EntityType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features:batchCreate
/// Creates a batch of Features in a given EntityType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_features_batch_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchCreateFeaturesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features:batchCreate
/// Creates a batch of Features in a given EntityType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_batch_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_features_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Creates a new Feature in a given EntityType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_create_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_create` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    featureId: Option<&str>,
    body: &GoogleCloudAiplatformV1Feature,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = featureId {
        query_parts.push(format!("featureId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Creates a new Feature in a given EntityType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_create_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Creates a new Feature in a given EntityType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: featureId
    pub featureId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Feature,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Creates a new Feature in a given EntityType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_create_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_create_builder(
        client,
        &args.parent,
        args.featureId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_features_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_delete_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_delete` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_delete_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Deletes a single Feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_delete_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_features_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_get_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_get` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_get_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Feature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_entity_types_features_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Gets details of a single Feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_get_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_features_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Lists Features in a given EntityType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_list_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_list` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    latestStatsCount: Option<i32>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = latestStatsCount {
        query_parts.push(format!("latestStatsCount={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Lists Features in a given EntityType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_list_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListFeaturesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Lists Features in a given EntityType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: latestStatsCount
    pub latestStatsCount: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features
/// Lists Features in a given EntityType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_list_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.latestStatsCount,
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_entity_types_features_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_patch_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_patch` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Feature,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_patch_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Feature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Feature,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}
/// Updates the parameters of a single Feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_patch_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Feature>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_features_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_operations_get` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_get_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_features_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_operations_list` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_list_builder(
            client,
            &args.name,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.returnPartialSuccess,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_features_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/features/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_execute()` or `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_features_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/features/{featuresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder()` + `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder(
            client,
            &args.name,
            args.timeout.as_deref(),
        )?;
    aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_execute()` or `aiplatform_projects_locations_featurestores_entity_types_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder()` + `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_operations_delete_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_operations_delete_execute()` or `aiplatform_projects_locations_featurestores_entity_types_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder()` + `aiplatform_projects_locations_featurestores_entity_types_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_featurestores_entity_types_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_operations_get_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_operations_get` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_operations_get_execute()` or `aiplatform_projects_locations_featurestores_entity_types_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_get_builder()` + `aiplatform_projects_locations_featurestores_entity_types_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_operations_list_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_operations_list` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_operations_list_execute()` or `aiplatform_projects_locations_featurestores_entity_types_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_list_builder()` + `aiplatform_projects_locations_featurestores_entity_types_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_featurestores_entity_types_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_entity_types_operations_wait_execute()` to send, or `aiplatform_projects_locations_featurestores_entity_types_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/entityTypes/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_entity_types_operations_wait_execute()` or `aiplatform_projects_locations_featurestores_entity_types_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_entity_types_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_featurestores_entity_types_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_entity_types_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/entityTypes/{entityTypesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder()` + `aiplatform_projects_locations_featurestores_entity_types_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_entity_types_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_entity_types_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_entity_types_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_operations_cancel_execute()` to send, or `aiplatform_projects_locations_featurestores_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_featurestores_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_operations_cancel_execute()` or `aiplatform_projects_locations_featurestores_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_operations_cancel_builder()` + `aiplatform_projects_locations_featurestores_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_operations_delete_execute()` to send, or `aiplatform_projects_locations_featurestores_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_featurestores_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_operations_delete_execute()` or `aiplatform_projects_locations_featurestores_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_operations_delete_builder()` + `aiplatform_projects_locations_featurestores_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_operations_get_execute()` to send, or `aiplatform_projects_locations_featurestores_operations_get` for simplest API.

pub fn aiplatform_projects_locations_featurestores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_operations_get_execute()` or `aiplatform_projects_locations_featurestores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_operations_get_builder()` + `aiplatform_projects_locations_featurestores_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_featurestores_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_featurestores_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_operations_list_execute()` to send, or `aiplatform_projects_locations_featurestores_operations_list` for simplest API.

pub fn aiplatform_projects_locations_featurestores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_operations_list_execute()` or `aiplatform_projects_locations_featurestores_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_operations_list_builder()` + `aiplatform_projects_locations_featurestores_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_featurestores_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_featurestores_operations_wait_execute()` to send, or `aiplatform_projects_locations_featurestores_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_featurestores_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/featurestores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_featurestores_operations_wait_execute()` or `aiplatform_projects_locations_featurestores_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_featurestores_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_featurestores_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_featurestores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_featurestores_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_featurestores_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_featurestores_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsFeaturestoresOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/featurestores/{featurestoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_featurestores_operations_wait_builder()` + `aiplatform_projects_locations_featurestores_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_featurestores_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_featurestores_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsFeaturestoresOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_featurestores_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_featurestores_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}:cancel
/// Cancels a HyperparameterTuningJob. Starts asynchronous cancellation on the HyperparameterTuningJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetHyperparameterTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the HyperparameterTuningJob is not deleted; instead it becomes a job with a HyperparameterTuningJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and HyperparameterTuningJob.state is set to CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelHyperparameterTuningJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}:cancel
/// Cancels a HyperparameterTuningJob. Starts asynchronous cancellation on the HyperparameterTuningJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetHyperparameterTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the HyperparameterTuningJob is not deleted; instead it becomes a job with a HyperparameterTuningJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and HyperparameterTuningJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}:cancel
/// Cancels a HyperparameterTuningJob. Starts asynchronous cancellation on the HyperparameterTuningJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetHyperparameterTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the HyperparameterTuningJob is not deleted; instead it becomes a job with a HyperparameterTuningJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and HyperparameterTuningJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelHyperparameterTuningJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}:cancel
/// Cancels a HyperparameterTuningJob. Starts asynchronous cancellation on the HyperparameterTuningJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetHyperparameterTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the HyperparameterTuningJob is not deleted; instead it becomes a job with a HyperparameterTuningJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and HyperparameterTuningJob.state is set to CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Creates a HyperparameterTuningJob
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1HyperparameterTuningJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Creates a HyperparameterTuningJob
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1HyperparameterTuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1HyperparameterTuningJob =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Creates a HyperparameterTuningJob
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1HyperparameterTuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1HyperparameterTuningJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Creates a HyperparameterTuningJob
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1HyperparameterTuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Deletes a HyperparameterTuningJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Deletes a HyperparameterTuningJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Deletes a HyperparameterTuningJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Deletes a HyperparameterTuningJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Gets a HyperparameterTuningJob
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Gets a HyperparameterTuningJob
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1HyperparameterTuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1HyperparameterTuningJob =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Gets a HyperparameterTuningJob
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1HyperparameterTuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}
/// Gets a HyperparameterTuningJob
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1HyperparameterTuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Lists HyperparameterTuningJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Lists HyperparameterTuningJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListHyperparameterTuningJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListHyperparameterTuningJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Lists HyperparameterTuningJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListHyperparameterTuningJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs
/// Lists HyperparameterTuningJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListHyperparameterTuningJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_execute()` to send, or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/hyperparameterTuningJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_execute()` or `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsHyperparameterTuningJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/hyperparameterTuningJobs/{hyperparameterTuningJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder()` + `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Creates an IndexEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_create_execute()` to send, or `aiplatform_projects_locations_index_endpoints_create` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1IndexEndpoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Creates an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_create_execute()` or `aiplatform_projects_locations_index_endpoints_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Creates an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1IndexEndpoint,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Creates an IndexEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_create_builder()` + `aiplatform_projects_locations_index_endpoints_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Deletes an IndexEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_delete_execute()` to send, or `aiplatform_projects_locations_index_endpoints_delete` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Deletes an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_delete_execute()` or `aiplatform_projects_locations_index_endpoints_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Deletes an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Deletes an IndexEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_delete_builder()` + `aiplatform_projects_locations_index_endpoints_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_index_endpoints_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:deployIndex
/// Deploys an Index into this IndexEndpoint, creating a DeployedIndex within it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_deploy_index_execute()` to send, or `aiplatform_projects_locations_index_endpoints_deploy_index` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_deploy_index_builder(
    client: &SimpleHttpClient,
    indexEndpoint: &str,
    body: &GoogleCloudAiplatformV1DeployIndexRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}:deployIndex",
        indexEndpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:deployIndex
/// Deploys an Index into this IndexEndpoint, creating a DeployedIndex within it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_deploy_index_execute()` or `aiplatform_projects_locations_index_endpoints_deploy_index`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_deploy_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_deploy_index_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:deployIndex
/// Deploys an Index into this IndexEndpoint, creating a DeployedIndex within it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_deploy_index_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_deploy_index_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_deploy_index()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_deploy_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_deploy_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_deploy_index_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_deploy_index`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsDeployIndexArgs {
    /// Path parameter: indexEndpoint
    pub indexEndpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DeployIndexRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:deployIndex
/// Deploys an Index into this IndexEndpoint, creating a DeployedIndex within it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_deploy_index_builder()` + `aiplatform_projects_locations_index_endpoints_deploy_index_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_deploy_index_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_deploy_index(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsDeployIndexArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_deploy_index_builder(
        client,
        &args.indexEndpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_deploy_index_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:findNeighbors
/// Finds the nearest neighbors of each vector within the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_find_neighbors_execute()` to send, or `aiplatform_projects_locations_index_endpoints_find_neighbors` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_find_neighbors_builder(
    client: &SimpleHttpClient,
    indexEndpoint: &str,
    body: &GoogleCloudAiplatformV1FindNeighborsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}:findNeighbors",
        indexEndpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:findNeighbors
/// Finds the nearest neighbors of each vector within the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_find_neighbors_execute()` or `aiplatform_projects_locations_index_endpoints_find_neighbors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_find_neighbors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_find_neighbors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FindNeighborsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1FindNeighborsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:findNeighbors
/// Finds the nearest neighbors of each vector within the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_find_neighbors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_find_neighbors_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_find_neighbors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_find_neighbors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_find_neighbors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FindNeighborsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_find_neighbors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_find_neighbors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsFindNeighborsArgs {
    /// Path parameter: indexEndpoint
    pub indexEndpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FindNeighborsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:findNeighbors
/// Finds the nearest neighbors of each vector within the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_find_neighbors_builder()` + `aiplatform_projects_locations_index_endpoints_find_neighbors_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_find_neighbors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_find_neighbors(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsFindNeighborsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1FindNeighborsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_find_neighbors_builder(
        client,
        &args.indexEndpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_find_neighbors_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Gets an IndexEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_get_execute()` to send, or `aiplatform_projects_locations_index_endpoints_get` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Gets an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_get_execute()` or `aiplatform_projects_locations_index_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1IndexEndpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1IndexEndpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Gets an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1IndexEndpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Gets an IndexEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_get_builder()` + `aiplatform_projects_locations_index_endpoints_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1IndexEndpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_get_builder(client, &args.name)?;
    aiplatform_projects_locations_index_endpoints_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Lists IndexEndpoints in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_list_execute()` to send, or `aiplatform_projects_locations_index_endpoints_list` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Lists IndexEndpoints in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_list_execute()` or `aiplatform_projects_locations_index_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListIndexEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListIndexEndpointsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Lists IndexEndpoints in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListIndexEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints
/// Lists IndexEndpoints in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_list_builder()` + `aiplatform_projects_locations_index_endpoints_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListIndexEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_index_endpoints_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:mutateDeployedIndex
/// Update an existing DeployedIndex under an IndexEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_execute()` to send, or `aiplatform_projects_locations_index_endpoints_mutate_deployed_index` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder(
    client: &SimpleHttpClient,
    indexEndpoint: &str,
    body: &GoogleCloudAiplatformV1DeployedIndex,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}:mutateDeployedIndex",
        indexEndpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:mutateDeployedIndex
/// Update an existing DeployedIndex under an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_execute()` or `aiplatform_projects_locations_index_endpoints_mutate_deployed_index`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_mutate_deployed_index_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:mutateDeployedIndex
/// Update an existing DeployedIndex under an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_mutate_deployed_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_mutate_deployed_index_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_mutate_deployed_index`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsMutateDeployedIndexArgs {
    /// Path parameter: indexEndpoint
    pub indexEndpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1DeployedIndex,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:mutateDeployedIndex
/// Update an existing DeployedIndex under an IndexEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder()` + `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_mutate_deployed_index_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_mutate_deployed_index(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsMutateDeployedIndexArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder(
        client,
        &args.indexEndpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_mutate_deployed_index_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Updates an IndexEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_patch_execute()` to send, or `aiplatform_projects_locations_index_endpoints_patch` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1IndexEndpoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Updates an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_patch_execute()` or `aiplatform_projects_locations_index_endpoints_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1IndexEndpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1IndexEndpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Updates an IndexEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1IndexEndpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1IndexEndpoint,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}
/// Updates an IndexEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_patch_builder()` + `aiplatform_projects_locations_index_endpoints_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1IndexEndpoint>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:readIndexDatapoints
/// Reads the `datapoints/vectors` of the given IDs. A maximum of 1000 datapoints can be retrieved in a batch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_read_index_datapoints_execute()` to send, or `aiplatform_projects_locations_index_endpoints_read_index_datapoints` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder(
    client: &SimpleHttpClient,
    indexEndpoint: &str,
    body: &GoogleCloudAiplatformV1ReadIndexDatapointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}:readIndexDatapoints",
        indexEndpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:readIndexDatapoints
/// Reads the `datapoints/vectors` of the given IDs. A maximum of 1000 datapoints can be retrieved in a batch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_read_index_datapoints_execute()` or `aiplatform_projects_locations_index_endpoints_read_index_datapoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_read_index_datapoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadIndexDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadIndexDatapointsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:readIndexDatapoints
/// Reads the `datapoints/vectors` of the given IDs. A maximum of 1000 datapoints can be retrieved in a batch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_read_index_datapoints_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_read_index_datapoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_read_index_datapoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadIndexDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_read_index_datapoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_read_index_datapoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsReadIndexDatapointsArgs {
    /// Path parameter: indexEndpoint
    pub indexEndpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ReadIndexDatapointsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:readIndexDatapoints
/// Reads the `datapoints/vectors` of the given IDs. A maximum of 1000 datapoints can be retrieved in a batch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder()` + `aiplatform_projects_locations_index_endpoints_read_index_datapoints_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_read_index_datapoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_read_index_datapoints(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsReadIndexDatapointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadIndexDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder(
        client,
        &args.indexEndpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_read_index_datapoints_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:undeployIndex
/// Undeploys an Index from an IndexEndpoint, removing a DeployedIndex from it, and freeing all resources it's using.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_undeploy_index_execute()` to send, or `aiplatform_projects_locations_index_endpoints_undeploy_index` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_undeploy_index_builder(
    client: &SimpleHttpClient,
    indexEndpoint: &str,
    body: &GoogleCloudAiplatformV1UndeployIndexRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}:undeployIndex",
        indexEndpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:undeployIndex
/// Undeploys an Index from an IndexEndpoint, removing a DeployedIndex from it, and freeing all resources it's using.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_undeploy_index_execute()` or `aiplatform_projects_locations_index_endpoints_undeploy_index`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_undeploy_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_undeploy_index_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:undeployIndex
/// Undeploys an Index from an IndexEndpoint, removing a DeployedIndex from it, and freeing all resources it's using.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_undeploy_index_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_undeploy_index_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_undeploy_index()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_undeploy_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_undeploy_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_undeploy_index_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_undeploy_index`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsUndeployIndexArgs {
    /// Path parameter: indexEndpoint
    pub indexEndpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UndeployIndexRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}:undeployIndex
/// Undeploys an Index from an IndexEndpoint, removing a DeployedIndex from it, and freeing all resources it's using.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_undeploy_index_builder()` + `aiplatform_projects_locations_index_endpoints_undeploy_index_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_undeploy_index_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_undeploy_index(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsUndeployIndexArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_undeploy_index_builder(
        client,
        &args.indexEndpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_index_endpoints_undeploy_index_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_operations_cancel_execute()` to send, or `aiplatform_projects_locations_index_endpoints_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_operations_cancel_execute()` or `aiplatform_projects_locations_index_endpoints_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_operations_cancel_builder()` + `aiplatform_projects_locations_index_endpoints_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_index_endpoints_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_operations_delete_execute()` to send, or `aiplatform_projects_locations_index_endpoints_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_operations_delete_execute()` or `aiplatform_projects_locations_index_endpoints_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_operations_delete_builder()` + `aiplatform_projects_locations_index_endpoints_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_index_endpoints_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_operations_get_execute()` to send, or `aiplatform_projects_locations_index_endpoints_operations_get` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_operations_get_execute()` or `aiplatform_projects_locations_index_endpoints_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_operations_get_builder()` + `aiplatform_projects_locations_index_endpoints_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_index_endpoints_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_index_endpoints_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_operations_list_execute()` to send, or `aiplatform_projects_locations_index_endpoints_operations_list` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_operations_list_execute()` or `aiplatform_projects_locations_index_endpoints_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_operations_list_builder()` + `aiplatform_projects_locations_index_endpoints_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_index_endpoints_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_index_endpoints_operations_wait_execute()` to send, or `aiplatform_projects_locations_index_endpoints_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_index_endpoints_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexEndpoints/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_index_endpoints_operations_wait_execute()` or `aiplatform_projects_locations_index_endpoints_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_index_endpoints_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_index_endpoints_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_index_endpoints_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_index_endpoints_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_index_endpoints_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_index_endpoints_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexEndpointsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexEndpoints/{indexEndpointsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_index_endpoints_operations_wait_builder()` + `aiplatform_projects_locations_index_endpoints_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_index_endpoints_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_index_endpoints_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexEndpointsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_index_endpoints_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_index_endpoints_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Creates an Index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_create_execute()` to send, or `aiplatform_projects_locations_indexes_create` for simplest API.

pub fn aiplatform_projects_locations_indexes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1Index,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Creates an Index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_create_execute()` or `aiplatform_projects_locations_indexes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Creates an Index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Index,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Creates an Index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_create_builder()` + `aiplatform_projects_locations_indexes_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_indexes_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_indexes_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Deletes an Index. An Index can only be deleted when all its DeployedIndexes had been undeployed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_delete_execute()` to send, or `aiplatform_projects_locations_indexes_delete` for simplest API.

pub fn aiplatform_projects_locations_indexes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Deletes an Index. An Index can only be deleted when all its DeployedIndexes had been undeployed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_delete_execute()` or `aiplatform_projects_locations_indexes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Deletes an Index. An Index can only be deleted when all its DeployedIndexes had been undeployed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Deletes an Index. An Index can only be deleted when all its DeployedIndexes had been undeployed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_delete_builder()` + `aiplatform_projects_locations_indexes_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_indexes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Gets an Index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_get_execute()` to send, or `aiplatform_projects_locations_indexes_get` for simplest API.

pub fn aiplatform_projects_locations_indexes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Gets an Index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_get_execute()` or `aiplatform_projects_locations_indexes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Index>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Index = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Gets an Index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Index>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Gets an Index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_get_builder()` + `aiplatform_projects_locations_indexes_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Index>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_get_builder(client, &args.name)?;
    aiplatform_projects_locations_indexes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Lists Indexes in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_list_execute()` to send, or `aiplatform_projects_locations_indexes_list` for simplest API.

pub fn aiplatform_projects_locations_indexes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Lists Indexes in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_list_execute()` or `aiplatform_projects_locations_indexes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListIndexesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListIndexesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Lists Indexes in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListIndexesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes
/// Lists Indexes in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_list_builder()` + `aiplatform_projects_locations_indexes_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListIndexesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_indexes_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Updates an Index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_patch_execute()` to send, or `aiplatform_projects_locations_indexes_patch` for simplest API.

pub fn aiplatform_projects_locations_indexes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Index,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Updates an Index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_patch_execute()` or `aiplatform_projects_locations_indexes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Updates an Index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Index,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}
/// Updates an Index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_patch_builder()` + `aiplatform_projects_locations_indexes_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_indexes_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:removeDatapoints
/// Remove Datapoints from an Index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_remove_datapoints_execute()` to send, or `aiplatform_projects_locations_indexes_remove_datapoints` for simplest API.

pub fn aiplatform_projects_locations_indexes_remove_datapoints_builder(
    client: &SimpleHttpClient,
    index: &str,
    body: &GoogleCloudAiplatformV1RemoveDatapointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}:removeDatapoints",
        index,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:removeDatapoints
/// Remove Datapoints from an Index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_remove_datapoints_execute()` or `aiplatform_projects_locations_indexes_remove_datapoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_remove_datapoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_remove_datapoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RemoveDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RemoveDatapointsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:removeDatapoints
/// Remove Datapoints from an Index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_remove_datapoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_remove_datapoints_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_remove_datapoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_remove_datapoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_remove_datapoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RemoveDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_remove_datapoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_remove_datapoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesRemoveDatapointsArgs {
    /// Path parameter: index
    pub index: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RemoveDatapointsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:removeDatapoints
/// Remove Datapoints from an Index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_remove_datapoints_builder()` + `aiplatform_projects_locations_indexes_remove_datapoints_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_remove_datapoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_remove_datapoints(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesRemoveDatapointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RemoveDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_remove_datapoints_builder(
        client,
        &args.index,
        &args.body,
    )?;
    aiplatform_projects_locations_indexes_remove_datapoints_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:upsertDatapoints
/// A`dd/update` Datapoints into an Index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_upsert_datapoints_execute()` to send, or `aiplatform_projects_locations_indexes_upsert_datapoints` for simplest API.

pub fn aiplatform_projects_locations_indexes_upsert_datapoints_builder(
    client: &SimpleHttpClient,
    index: &str,
    body: &GoogleCloudAiplatformV1UpsertDatapointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}:upsertDatapoints",
        index,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:upsertDatapoints
/// A`dd/update` Datapoints into an Index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_upsert_datapoints_execute()` or `aiplatform_projects_locations_indexes_upsert_datapoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_upsert_datapoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_upsert_datapoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1UpsertDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1UpsertDatapointsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:upsertDatapoints
/// A`dd/update` Datapoints into an Index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_upsert_datapoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_upsert_datapoints_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_upsert_datapoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_upsert_datapoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_upsert_datapoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1UpsertDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_upsert_datapoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_upsert_datapoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesUpsertDatapointsArgs {
    /// Path parameter: index
    pub index: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UpsertDatapointsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}:upsertDatapoints
/// A`dd/update` Datapoints into an Index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_upsert_datapoints_builder()` + `aiplatform_projects_locations_indexes_upsert_datapoints_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_upsert_datapoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_upsert_datapoints(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesUpsertDatapointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1UpsertDatapointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_upsert_datapoints_builder(
        client,
        &args.index,
        &args.body,
    )?;
    aiplatform_projects_locations_indexes_upsert_datapoints_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_operations_cancel_execute()` to send, or `aiplatform_projects_locations_indexes_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_indexes_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_operations_cancel_execute()` or `aiplatform_projects_locations_indexes_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_operations_cancel_builder()` + `aiplatform_projects_locations_indexes_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_indexes_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_indexes_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_operations_delete_execute()` to send, or `aiplatform_projects_locations_indexes_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_indexes_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_operations_delete_execute()` or `aiplatform_projects_locations_indexes_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_operations_delete_builder()` + `aiplatform_projects_locations_indexes_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_indexes_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_indexes_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_operations_get_execute()` to send, or `aiplatform_projects_locations_indexes_operations_get` for simplest API.

pub fn aiplatform_projects_locations_indexes_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_operations_get_execute()` or `aiplatform_projects_locations_indexes_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_operations_get_builder()` + `aiplatform_projects_locations_indexes_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_indexes_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_operations_list_execute()` to send, or `aiplatform_projects_locations_indexes_operations_list` for simplest API.

pub fn aiplatform_projects_locations_indexes_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_operations_list_execute()` or `aiplatform_projects_locations_indexes_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_operations_list_builder()` + `aiplatform_projects_locations_indexes_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_indexes_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_indexes_operations_wait_execute()` to send, or `aiplatform_projects_locations_indexes_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_indexes_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/indexes/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_indexes_operations_wait_execute()` or `aiplatform_projects_locations_indexes_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_indexes_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_indexes_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_indexes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_indexes_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_indexes_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_indexes_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsIndexesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/indexes/{indexesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_indexes_operations_wait_builder()` + `aiplatform_projects_locations_indexes_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_indexes_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_indexes_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsIndexesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_indexes_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_indexes_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Initializes a MetadataStore, including allocation of resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_create_execute()` to send, or `aiplatform_projects_locations_metadata_stores_create` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    metadataStoreId: Option<&str>,
    body: &GoogleCloudAiplatformV1MetadataStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = metadataStoreId {
        query_parts.push(format!("metadataStoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Initializes a MetadataStore, including allocation of resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_create_execute()` or `aiplatform_projects_locations_metadata_stores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Initializes a MetadataStore, including allocation of resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: metadataStoreId
    pub metadataStoreId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1MetadataStore,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Initializes a MetadataStore, including allocation of resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_create_builder()` + `aiplatform_projects_locations_metadata_stores_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_create_builder(
        client,
        &args.parent,
        args.metadataStoreId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Deletes a single MetadataStore and all its child resources (Artifacts, Executions, and Contexts).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Deletes a single MetadataStore and all its child resources (Artifacts, Executions, and Contexts).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_delete_execute()` or `aiplatform_projects_locations_metadata_stores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Deletes a single MetadataStore and all its child resources (Artifacts, Executions, and Contexts).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Deletes a single MetadataStore and all its child resources (Artifacts, Executions, and Contexts).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_delete_builder()` + `aiplatform_projects_locations_metadata_stores_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_delete_builder(
        client, &args.name, args.force,
    )?;
    aiplatform_projects_locations_metadata_stores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Retrieves a specific MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Retrieves a specific MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_get_execute()` or `aiplatform_projects_locations_metadata_stores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1MetadataStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Retrieves a specific MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}
/// Retrieves a specific MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_get_builder()` + `aiplatform_projects_locations_metadata_stores_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_get_builder(client, &args.name)?;
    aiplatform_projects_locations_metadata_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Lists MetadataStores for a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Lists MetadataStores for a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_list_execute()` or `aiplatform_projects_locations_metadata_stores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMetadataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListMetadataStoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Lists MetadataStores for a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMetadataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores
/// Lists MetadataStores for a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_list_builder()` + `aiplatform_projects_locations_metadata_stores_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMetadataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Creates an Artifact associated with a MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_create_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_create` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    artifactId: Option<&str>,
    body: &GoogleCloudAiplatformV1Artifact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = artifactId {
        query_parts.push(format!("artifactId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Creates an Artifact associated with a MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_create_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Creates an Artifact associated with a MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: artifactId
    pub artifactId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Artifact,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Creates an Artifact associated with a MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_create_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_create_builder(
        client,
        &args.parent,
        args.artifactId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Deletes an Artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Deletes an Artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_delete_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Deletes an Artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Deletes an Artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_delete_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_delete_builder(
        client,
        &args.name,
        args.etag.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Retrieves a specific Artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Retrieves a specific Artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_get_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Retrieves a specific Artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Retrieves a specific Artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_get_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_artifacts_get_builder(client, &args.name)?;
    aiplatform_projects_locations_metadata_stores_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Lists Artifacts in the MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Lists Artifacts in the MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_list_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListArtifactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListArtifactsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Lists Artifacts in the MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListArtifactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts
/// Lists Artifacts in the MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_list_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListArtifactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Updates a stored Artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_patch_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_patch` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Artifact,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Updates a stored Artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_patch_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Updates a stored Artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Artifact,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}
/// Updates a stored Artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_patch_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Artifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts:purge
/// Purges Artifacts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_purge_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_purge` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1PurgeArtifactsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts:purge
/// Purges Artifacts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_purge_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts:purge
/// Purges Artifacts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_purge_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PurgeArtifactsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts:purge
/// Purges Artifacts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_purge_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_purge_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_purge(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_purge_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}:queryArtifactLineageSubgraph
/// Retrieves lineage of an Artifact represented through Artifacts and Executions connected by Event edges and returned as a LineageSubgraph.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_builder(
    client: &SimpleHttpClient,
    artifact: &str,
    filter: Option<&str>,
    maxHops: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}:queryArtifactLineageSubgraph",
        artifact,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxHops {
        query_parts.push(format!("maxHops={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}:queryArtifactLineageSubgraph
/// Retrieves lineage of an Artifact represented through Artifacts and Executions connected by Event edges and returned as a LineageSubgraph.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1LineageSubgraph = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}:queryArtifactLineageSubgraph
/// Retrieves lineage of an Artifact represented through Artifacts and Executions connected by Event edges and returned as a LineageSubgraph.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsQueryArtifactLineageSubgraphArgs {
    /// Path parameter: artifact
    pub artifact: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxHops
    pub maxHops: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}:queryArtifactLineageSubgraph
/// Retrieves lineage of an Artifact represented through Artifacts and Executions connected by Event edges and returned as a LineageSubgraph.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsQueryArtifactLineageSubgraphArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_builder(client, &args.artifact, args.filter.as_deref(), args.maxHops)?;
    aiplatform_projects_locations_metadata_stores_artifacts_query_artifact_lineage_subgraph_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_operations_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_operations_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_execute()` to send, or `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/artifacts/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_execute()` or `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_artifacts_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresArtifactsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/artifacts/{artifactsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder()` + `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextArtifactsAndExecutions
/// Adds a set of Artifacts and Executions to a Context. If any of the Artifacts or Executions have already been added to a Context, they are simply skipped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder(
    client: &SimpleHttpClient,
    context: &str,
    body: &GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}:addContextArtifactsAndExecutions",
        context,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextArtifactsAndExecutions
/// Adds a set of Artifacts and Executions to a Context. If any of the Artifacts or Executions have already been added to a Context, they are simply skipped.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextArtifactsAndExecutions
/// Adds a set of Artifacts and Executions to a Context. If any of the Artifacts or Executions have already been added to a Context, they are simply skipped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsAddContextArtifactsAndExecutionsArgs {
    /// Path parameter: context
    pub context: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextArtifactsAndExecutions
/// Adds a set of Artifacts and Executions to a Context. If any of the Artifacts or Executions have already been added to a Context, they are simply skipped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsAddContextArtifactsAndExecutionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder(client, &args.context, &args.body)?;
    aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextChildren
/// Adds a set of Contexts as children to a parent Context. If any of the child Contexts have already been added to the parent Context, they are simply skipped. If this call would create a cycle or cause any Context to have more than 10 parents, the request will fail with an INVALID_ARGUMENT error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_add_context_children` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder(
    client: &SimpleHttpClient,
    context: &str,
    body: &GoogleCloudAiplatformV1AddContextChildrenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}:addContextChildren",
        context,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextChildren
/// Adds a set of Contexts as children to a parent Context. If any of the child Contexts have already been added to the parent Context, they are simply skipped. If this call would create a cycle or cause any Context to have more than 10 parents, the request will fail with an INVALID_ARGUMENT error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_add_context_children`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_children_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AddContextChildrenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AddContextChildrenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextChildren
/// Adds a set of Contexts as children to a parent Context. If any of the child Contexts have already been added to the parent Context, they are simply skipped. If this call would create a cycle or cause any Context to have more than 10 parents, the request will fail with an INVALID_ARGUMENT error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_children_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AddContextChildrenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_contexts_add_context_children_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_add_context_children`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsAddContextChildrenArgs {
    /// Path parameter: context
    pub context: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AddContextChildrenRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:addContextChildren
/// Adds a set of Contexts as children to a parent Context. If any of the child Contexts have already been added to the parent Context, they are simply skipped. If this call would create a cycle or cause any Context to have more than 10 parents, the request will fail with an INVALID_ARGUMENT error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_add_context_children_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_children(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsAddContextChildrenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AddContextChildrenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder(
            client,
            &args.context,
            &args.body,
        )?;
    aiplatform_projects_locations_metadata_stores_contexts_add_context_children_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Creates a Context associated with a MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_create_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_create` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    contextId: Option<&str>,
    body: &GoogleCloudAiplatformV1Context,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contextId {
        query_parts.push(format!("contextId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Creates a Context associated with a MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_create_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Context = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Creates a Context associated with a MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: contextId
    pub contextId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Context,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Creates a Context associated with a MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_create_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_create_builder(
        client,
        &args.parent,
        args.contextId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Deletes a stored Context.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Deletes a stored Context.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_delete_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Deletes a stored Context.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Deletes a stored Context.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_delete_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_delete_builder(
        client,
        &args.name,
        args.etag.as_deref(),
        args.force,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Retrieves a specific Context.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Retrieves a specific Context.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_get_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Context = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Retrieves a specific Context.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Retrieves a specific Context.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_get_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_contexts_get_builder(client, &args.name)?;
    aiplatform_projects_locations_metadata_stores_contexts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Lists Contexts on the MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Lists Contexts on the MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_list_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListContextsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Lists Contexts on the MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts
/// Lists Contexts on the MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_list_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListContextsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Updates a stored Context.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_patch_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_patch` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Context,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Updates a stored Context.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_patch_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Context = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Updates a stored Context.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Context,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}
/// Updates a stored Context.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_patch_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Context>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts:purge
/// Purges Contexts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_purge_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_purge` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1PurgeContextsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts:purge
/// Purges Contexts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_purge_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts:purge
/// Purges Contexts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_purge_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PurgeContextsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts:purge
/// Purges Contexts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_purge_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_purge_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_purge(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_purge_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:queryContextLineageSubgraph
/// Retrieves Artifacts and Executions within the specified Context, connected by Event edges and returned as a LineageSubgraph.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_builder(
    client: &SimpleHttpClient,
    context: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}:queryContextLineageSubgraph",
        context,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:queryContextLineageSubgraph
/// Retrieves Artifacts and Executions within the specified Context, connected by Event edges and returned as a LineageSubgraph.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1LineageSubgraph = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:queryContextLineageSubgraph
/// Retrieves Artifacts and Executions within the specified Context, connected by Event edges and returned as a LineageSubgraph.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsQueryContextLineageSubgraphArgs {
    /// Path parameter: context
    pub context: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:queryContextLineageSubgraph
/// Retrieves Artifacts and Executions within the specified Context, connected by Event edges and returned as a LineageSubgraph.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsQueryContextLineageSubgraphArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_builder(client, &args.context)?;
    aiplatform_projects_locations_metadata_stores_contexts_query_context_lineage_subgraph_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:removeContextChildren
/// Remove a set of children contexts from a parent Context. If any of the child Contexts were NOT added to the parent Context, they are simply skipped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder(
    client: &SimpleHttpClient,
    context: &str,
    body: &GoogleCloudAiplatformV1RemoveContextChildrenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}:removeContextChildren",
        context,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:removeContextChildren
/// Remove a set of children contexts from a parent Context. If any of the child Contexts were NOT added to the parent Context, they are simply skipped.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RemoveContextChildrenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RemoveContextChildrenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:removeContextChildren
/// Remove a set of children contexts from a parent Context. If any of the child Contexts were NOT added to the parent Context, they are simply skipped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RemoveContextChildrenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_remove_context_children`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsRemoveContextChildrenArgs {
    /// Path parameter: context
    pub context: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RemoveContextChildrenRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}:removeContextChildren
/// Remove a set of children contexts from a parent Context. If any of the child Contexts were NOT added to the parent Context, they are simply skipped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_remove_context_children(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsRemoveContextChildrenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RemoveContextChildrenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder(
            client,
            &args.context,
            &args.body,
        )?;
    aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_contexts_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_operations_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_operations_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_operations_get_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_contexts_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_get_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_operations_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_operations_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_operations_list_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_contexts_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_list_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_execute()` to send, or `aiplatform_projects_locations_metadata_stores_contexts_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/contexts/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_execute()` or `aiplatform_projects_locations_metadata_stores_contexts_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_contexts_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_contexts_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresContextsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/contexts/{contextsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder()` + `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_contexts_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_contexts_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:addExecutionEvents
/// Adds Events to the specified Execution. An Event indicates whether an Artifact was used as an input or output for an Execution. If an Event already exists between the Execution and the Artifact, the Event is skipped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_add_execution_events` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder(
    client: &SimpleHttpClient,
    execution: &str,
    body: &GoogleCloudAiplatformV1AddExecutionEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}:addExecutionEvents",
        execution,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:addExecutionEvents
/// Adds Events to the specified Execution. An Event indicates whether an Artifact was used as an input or output for an Execution. If an Event already exists between the Execution and the Artifact, the Event is skipped.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_execute()` or `aiplatform_projects_locations_metadata_stores_executions_add_execution_events`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_add_execution_events_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AddExecutionEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AddExecutionEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:addExecutionEvents
/// Adds Events to the specified Execution. An Event indicates whether an Artifact was used as an input or output for an Execution. If an Event already exists between the Execution and the Artifact, the Event is skipped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_add_execution_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AddExecutionEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_add_execution_events_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_add_execution_events`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsAddExecutionEventsArgs {
    /// Path parameter: execution
    pub execution: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AddExecutionEventsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:addExecutionEvents
/// Adds Events to the specified Execution. An Event indicates whether an Artifact was used as an input or output for an Execution. If an Event already exists between the Execution and the Artifact, the Event is skipped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder()` + `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_add_execution_events_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_add_execution_events(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsAddExecutionEventsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AddExecutionEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder(
            client,
            &args.execution,
            &args.body,
        )?;
    aiplatform_projects_locations_metadata_stores_executions_add_execution_events_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Creates an Execution associated with a MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_create_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_create` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    executionId: Option<&str>,
    body: &GoogleCloudAiplatformV1Execution,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionId {
        query_parts.push(format!("executionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Creates an Execution associated with a MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_create_execute()` or `aiplatform_projects_locations_metadata_stores_executions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Execution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Creates an Execution associated with a MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: executionId
    pub executionId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Execution,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Creates an Execution associated with a MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_create_builder()` + `aiplatform_projects_locations_metadata_stores_executions_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_create_builder(
        client,
        &args.parent,
        args.executionId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_executions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Deletes an Execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Deletes an Execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_delete_execute()` or `aiplatform_projects_locations_metadata_stores_executions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Deletes an Execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Deletes an Execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_delete_builder()` + `aiplatform_projects_locations_metadata_stores_executions_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_delete_builder(
        client,
        &args.name,
        args.etag.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_executions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Retrieves a specific Execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Retrieves a specific Execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_get_execute()` or `aiplatform_projects_locations_metadata_stores_executions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Execution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Retrieves a specific Execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Retrieves a specific Execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_get_builder()` + `aiplatform_projects_locations_metadata_stores_executions_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_executions_get_builder(client, &args.name)?;
    aiplatform_projects_locations_metadata_stores_executions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Lists Executions in the MetadataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Lists Executions in the MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_list_execute()` or `aiplatform_projects_locations_metadata_stores_executions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListExecutionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Lists Executions in the MetadataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions
/// Lists Executions in the MetadataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_list_builder()` + `aiplatform_projects_locations_metadata_stores_executions_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_executions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Updates a stored Execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_patch_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_patch` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Execution,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Updates a stored Execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_patch_execute()` or `aiplatform_projects_locations_metadata_stores_executions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Execution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Updates a stored Execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Execution,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}
/// Updates a stored Execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_patch_builder()` + `aiplatform_projects_locations_metadata_stores_executions_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Execution>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_executions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions:purge
/// Purges Executions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_purge_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_purge` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1PurgeExecutionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions:purge
/// Purges Executions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_purge_execute()` or `aiplatform_projects_locations_metadata_stores_executions_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions:purge
/// Purges Executions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_purge_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PurgeExecutionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions:purge
/// Purges Executions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_purge_builder()` + `aiplatform_projects_locations_metadata_stores_executions_purge_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_purge(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_purge_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_executions_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:queryExecutionInputsAndOutputs
/// Obtains the set of input and output Artifacts for this Execution, in the form of LineageSubgraph that also contains the Execution and connecting Events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_builder(
    client: &SimpleHttpClient,
    execution: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}:queryExecutionInputsAndOutputs",
        execution,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:queryExecutionInputsAndOutputs
/// Obtains the set of input and output Artifacts for this Execution, in the form of LineageSubgraph that also contains the Execution and connecting Events.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_execute()` or `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1LineageSubgraph = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:queryExecutionInputsAndOutputs
/// Obtains the set of input and output Artifacts for this Execution, in the form of LineageSubgraph that also contains the Execution and connecting Events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsQueryExecutionInputsAndOutputsArgs {
    /// Path parameter: execution
    pub execution: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}:queryExecutionInputsAndOutputs
/// Obtains the set of input and output Artifacts for this Execution, in the form of LineageSubgraph that also contains the Execution and connecting Events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_builder()` + `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsQueryExecutionInputsAndOutputsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1LineageSubgraph>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_builder(client, &args.execution)?;
    aiplatform_projects_locations_metadata_stores_executions_query_execution_inputs_and_outputs_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_execute()` or `aiplatform_projects_locations_metadata_stores_executions_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_executions_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder()` + `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_metadata_stores_executions_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_operations_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_operations_delete_execute()` or `aiplatform_projects_locations_metadata_stores_executions_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_executions_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder()` + `aiplatform_projects_locations_metadata_stores_executions_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_metadata_stores_executions_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_operations_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_operations_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_operations_get_execute()` or `aiplatform_projects_locations_metadata_stores_executions_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_executions_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_get_builder()` + `aiplatform_projects_locations_metadata_stores_executions_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_executions_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_operations_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_operations_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_operations_list_execute()` or `aiplatform_projects_locations_metadata_stores_executions_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_executions_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_list_builder()` + `aiplatform_projects_locations_metadata_stores_executions_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_metadata_stores_executions_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_executions_operations_wait_execute()` to send, or `aiplatform_projects_locations_metadata_stores_executions_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/executions/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_executions_operations_wait_execute()` or `aiplatform_projects_locations_metadata_stores_executions_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_executions_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_metadata_stores_executions_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_executions_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresExecutionsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/executions/{executionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder()` + `aiplatform_projects_locations_metadata_stores_executions_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_executions_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_executions_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_executions_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Creates a MetadataSchema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_execute()` to send, or `aiplatform_projects_locations_metadata_stores_metadata_schemas_create` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    metadataSchemaId: Option<&str>,
    body: &GoogleCloudAiplatformV1MetadataSchema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/metadataSchemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = metadataSchemaId {
        query_parts.push(format!("metadataSchemaId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Creates a MetadataSchema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_execute()` or `aiplatform_projects_locations_metadata_stores_metadata_schemas_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1MetadataSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Creates a MetadataSchema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_metadata_schemas_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_metadata_schemas_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresMetadataSchemasCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: metadataSchemaId
    pub metadataSchemaId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1MetadataSchema,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Creates a MetadataSchema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder()` + `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresMetadataSchemasCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder(
        client,
        &args.parent,
        args.metadataSchemaId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_metadata_stores_metadata_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas/{metadataSchemasId}
/// Retrieves a specific MetadataSchema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_metadata_schemas_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/metadataSchemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas/{metadataSchemasId}
/// Retrieves a specific MetadataSchema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_execute()` or `aiplatform_projects_locations_metadata_stores_metadata_schemas_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1MetadataSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas/{metadataSchemasId}
/// Retrieves a specific MetadataSchema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_metadata_schemas_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_metadata_schemas_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresMetadataSchemasGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas/{metadataSchemasId}
/// Retrieves a specific MetadataSchema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_builder()` + `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresMetadataSchemasGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MetadataSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_metadata_schemas_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_metadata_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Lists MetadataSchemas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_metadata_schemas_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/metadataSchemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Lists MetadataSchemas.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_execute()` or `aiplatform_projects_locations_metadata_stores_metadata_schemas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMetadataSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListMetadataSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Lists MetadataSchemas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMetadataSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_metadata_schemas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_metadata_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresMetadataSchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/metadataSchemas
/// Lists MetadataSchemas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_builder()` + `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_metadata_schemas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresMetadataSchemasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMetadataSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_metadata_schemas_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_metadata_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_operations_cancel_execute()` to send, or `aiplatform_projects_locations_metadata_stores_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_operations_cancel_execute()` or `aiplatform_projects_locations_metadata_stores_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_operations_cancel_builder()` + `aiplatform_projects_locations_metadata_stores_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_operations_delete_execute()` to send, or `aiplatform_projects_locations_metadata_stores_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_operations_delete_execute()` or `aiplatform_projects_locations_metadata_stores_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_operations_delete_builder()` + `aiplatform_projects_locations_metadata_stores_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_metadata_stores_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_operations_get_execute()` to send, or `aiplatform_projects_locations_metadata_stores_operations_get` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_operations_get_execute()` or `aiplatform_projects_locations_metadata_stores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_operations_get_builder()` + `aiplatform_projects_locations_metadata_stores_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_metadata_stores_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_metadata_stores_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_operations_list_execute()` to send, or `aiplatform_projects_locations_metadata_stores_operations_list` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_operations_list_execute()` or `aiplatform_projects_locations_metadata_stores_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_operations_list_builder()` + `aiplatform_projects_locations_metadata_stores_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_metadata_stores_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_metadata_stores_operations_wait_execute()` to send, or `aiplatform_projects_locations_metadata_stores_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_metadata_stores_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/metadataStores/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_metadata_stores_operations_wait_execute()` or `aiplatform_projects_locations_metadata_stores_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_metadata_stores_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_metadata_stores_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_metadata_stores_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_metadata_stores_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_metadata_stores_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_metadata_stores_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMetadataStoresOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/metadataStores/{metadataStoresId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_metadata_stores_operations_wait_builder()` + `aiplatform_projects_locations_metadata_stores_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_metadata_stores_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_metadata_stores_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMetadataStoresOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_metadata_stores_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_metadata_stores_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:batchMigrate
/// Batch migrates resources from ml.googleapis.com, automl.googleapis.com, and datalabeling.googleapis.com to Vertex AI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_batch_migrate_execute()` to send, or `aiplatform_projects_locations_migratable_resources_batch_migrate` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_batch_migrate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchMigrateResourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources:batchMigrate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:batchMigrate
/// Batch migrates resources from ml.googleapis.com, automl.googleapis.com, and datalabeling.googleapis.com to Vertex AI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_batch_migrate_execute()` or `aiplatform_projects_locations_migratable_resources_batch_migrate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_batch_migrate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_batch_migrate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:batchMigrate
/// Batch migrates resources from ml.googleapis.com, automl.googleapis.com, and datalabeling.googleapis.com to Vertex AI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_batch_migrate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_batch_migrate_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_batch_migrate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_batch_migrate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_batch_migrate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_batch_migrate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_batch_migrate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesBatchMigrateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchMigrateResourcesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:batchMigrate
/// Batch migrates resources from ml.googleapis.com, automl.googleapis.com, and datalabeling.googleapis.com to Vertex AI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_batch_migrate_builder()` + `aiplatform_projects_locations_migratable_resources_batch_migrate_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_batch_migrate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_batch_migrate(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesBatchMigrateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_batch_migrate_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_migratable_resources_batch_migrate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:search
/// Searches all of the resources in automl.googleapis.com, datalabeling.googleapis.com and ml.googleapis.com that can be migrated to Vertex AI's given location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_search_execute()` to send, or `aiplatform_projects_locations_migratable_resources_search` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1SearchMigratableResourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources:search",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:search
/// Searches all of the resources in automl.googleapis.com, datalabeling.googleapis.com and ml.googleapis.com that can be migrated to Vertex AI's given location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_search_execute()` or `aiplatform_projects_locations_migratable_resources_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1SearchMigratableResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SearchMigratableResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:search
/// Searches all of the resources in automl.googleapis.com, datalabeling.googleapis.com and ml.googleapis.com that can be migrated to Vertex AI's given location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_search_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1SearchMigratableResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SearchMigratableResourcesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources:search
/// Searches all of the resources in automl.googleapis.com, datalabeling.googleapis.com and ml.googleapis.com that can be migrated to Vertex AI's given location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_search_builder()` + `aiplatform_projects_locations_migratable_resources_search_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_search(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1SearchMigratableResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_search_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_migratable_resources_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_operations_cancel_execute()` to send, or `aiplatform_projects_locations_migratable_resources_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_operations_cancel_execute()` or `aiplatform_projects_locations_migratable_resources_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_operations_cancel_builder()` + `aiplatform_projects_locations_migratable_resources_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_migratable_resources_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_operations_delete_execute()` to send, or `aiplatform_projects_locations_migratable_resources_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_operations_delete_execute()` or `aiplatform_projects_locations_migratable_resources_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_operations_delete_builder()` + `aiplatform_projects_locations_migratable_resources_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_migratable_resources_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_operations_get_execute()` to send, or `aiplatform_projects_locations_migratable_resources_operations_get` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_operations_get_execute()` or `aiplatform_projects_locations_migratable_resources_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_operations_get_builder()` + `aiplatform_projects_locations_migratable_resources_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_migratable_resources_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_operations_list_execute()` to send, or `aiplatform_projects_locations_migratable_resources_operations_list` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_operations_list_execute()` or `aiplatform_projects_locations_migratable_resources_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_operations_list_builder()` + `aiplatform_projects_locations_migratable_resources_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_migratable_resources_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_migratable_resources_operations_wait_execute()` to send, or `aiplatform_projects_locations_migratable_resources_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_migratable_resources_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/migratableResources/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_migratable_resources_operations_wait_execute()` or `aiplatform_projects_locations_migratable_resources_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_migratable_resources_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_migratable_resources_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_migratable_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_migratable_resources_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_migratable_resources_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_migratable_resources_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsMigratableResourcesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/migratableResources/{migratableResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_migratable_resources_operations_wait_builder()` + `aiplatform_projects_locations_migratable_resources_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_migratable_resources_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_migratable_resources_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsMigratableResourcesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_migratable_resources_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_migratable_resources_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Creates a ModelDeploymentMonitoringJob. It will run periodically on a configured interval.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1ModelDeploymentMonitoringJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Creates a ModelDeploymentMonitoringJob. It will run periodically on a configured interval.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ModelDeploymentMonitoringJob =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Creates a ModelDeploymentMonitoringJob. It will run periodically on a configured interval.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ModelDeploymentMonitoringJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Creates a ModelDeploymentMonitoringJob. It will run periodically on a configured interval.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Deletes a ModelDeploymentMonitoringJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Deletes a ModelDeploymentMonitoringJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Deletes a ModelDeploymentMonitoringJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Deletes a ModelDeploymentMonitoringJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Gets a ModelDeploymentMonitoringJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Gets a ModelDeploymentMonitoringJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ModelDeploymentMonitoringJob =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Gets a ModelDeploymentMonitoringJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Gets a ModelDeploymentMonitoringJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Lists ModelDeploymentMonitoringJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Lists ModelDeploymentMonitoringJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelDeploymentMonitoringJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListModelDeploymentMonitoringJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Lists ModelDeploymentMonitoringJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelDeploymentMonitoringJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs
/// Lists ModelDeploymentMonitoringJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelDeploymentMonitoringJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Updates a ModelDeploymentMonitoringJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1ModelDeploymentMonitoringJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Updates a ModelDeploymentMonitoringJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Updates a ModelDeploymentMonitoringJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ModelDeploymentMonitoringJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}
/// Updates a ModelDeploymentMonitoringJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:pause
/// Pauses a ModelDeploymentMonitoringJob. If the job is running, the server makes a best effort to cancel the job. Will mark ModelDeploymentMonitoringJob.state to 'PAUSED'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1PauseModelDeploymentMonitoringJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}:pause",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:pause
/// Pauses a ModelDeploymentMonitoringJob. If the job is running, the server makes a best effort to cancel the job. Will mark ModelDeploymentMonitoringJob.state to 'PAUSED'.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:pause
/// Pauses a ModelDeploymentMonitoringJob. If the job is running, the server makes a best effort to cancel the job. Will mark ModelDeploymentMonitoringJob.state to 'PAUSED'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsPauseArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PauseModelDeploymentMonitoringJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:pause
/// Pauses a ModelDeploymentMonitoringJob. If the job is running, the server makes a best effort to cancel the job. Will mark ModelDeploymentMonitoringJob.state to 'PAUSED'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_pause(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:resume
/// Resumes a paused ModelDeploymentMonitoringJob. It will start to run from next scheduled time. A deleted ModelDeploymentMonitoringJob can't be resumed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ResumeModelDeploymentMonitoringJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}:resume",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:resume
/// Resumes a paused ModelDeploymentMonitoringJob. It will start to run from next scheduled time. A deleted ModelDeploymentMonitoringJob can't be resumed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:resume
/// Resumes a paused ModelDeploymentMonitoringJob. It will start to run from next scheduled time. A deleted ModelDeploymentMonitoringJob can't be resumed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsResumeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ResumeModelDeploymentMonitoringJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:resume
/// Resumes a paused ModelDeploymentMonitoringJob. It will start to run from next scheduled time. A deleted ModelDeploymentMonitoringJob can't be resumed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_resume(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:searchModelDeploymentMonitoringStatsAnomalies
/// Searches Model Monitoring Statistics generated within a given time window.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder(
    client: &SimpleHttpClient,
    modelDeploymentMonitoringJob: &str,
    body: &GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}:searchModelDeploymentMonitoringStatsAnomalies",
        modelDeploymentMonitoringJob,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:searchModelDeploymentMonitoringStatsAnomalies
/// Searches Model Monitoring Statistics generated within a given time window.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:searchModelDeploymentMonitoringStatsAnomalies
/// Searches Model Monitoring Statistics generated within a given time window.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsSearchModelDeploymentMonitoringStatsAnomaliesArgs
{
    /// Path parameter: modelDeploymentMonitoringJob
    pub modelDeploymentMonitoringJob: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}:searchModelDeploymentMonitoringStatsAnomalies
/// Searches Model Monitoring Statistics generated within a given time window.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsSearchModelDeploymentMonitoringStatsAnomaliesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder(client, &args.modelDeploymentMonitoringJob, &args.body)?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_builder(
            client,
            &args.name,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.returnPartialSuccess,
        )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_execute()` to send, or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/modelDeploymentMonitoringJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_execute()` or `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/modelDeploymentMonitoringJobs/{modelDeploymentMonitoringJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder()` + `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder(
            client,
            &args.name,
            args.timeout.as_deref(),
        )?;
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:copy
/// Copies an already existing Vertex AI Model into the specified Location. The source Model must exist in the same Project. When copying custom Models, the users themselves are responsible for Model.metadata content to be region-agnostic, as well as making sure that any resources (e.g. files) it depends on remain accessible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_copy_execute()` to send, or `aiplatform_projects_locations_models_copy` for simplest API.

pub fn aiplatform_projects_locations_models_copy_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1CopyModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models:copy",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:copy
/// Copies an already existing Vertex AI Model into the specified Location. The source Model must exist in the same Project. When copying custom Models, the users themselves are responsible for Model.metadata content to be region-agnostic, as well as making sure that any resources (e.g. files) it depends on remain accessible.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_copy_execute()` or `aiplatform_projects_locations_models_copy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_copy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_copy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:copy
/// Copies an already existing Vertex AI Model into the specified Location. The source Model must exist in the same Project. When copying custom Models, the users themselves are responsible for Model.metadata content to be region-agnostic, as well as making sure that any resources (e.g. files) it depends on remain accessible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_copy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_copy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_copy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_copy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_copy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_copy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_copy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsCopyArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CopyModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:copy
/// Copies an already existing Vertex AI Model into the specified Location. The source Model must exist in the same Project. When copying custom Models, the users themselves are responsible for Model.metadata content to be region-agnostic, as well as making sure that any resources (e.g. files) it depends on remain accessible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_copy_builder()` + `aiplatform_projects_locations_models_copy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_copy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_copy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsCopyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_models_copy_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_models_copy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a Model. A model cannot be deleted if any Endpoint resource has a DeployedModel based on the model in its deployed_models field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_delete_execute()` to send, or `aiplatform_projects_locations_models_delete` for simplest API.

pub fn aiplatform_projects_locations_models_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a Model. A model cannot be deleted if any Endpoint resource has a DeployedModel based on the model in its deployed_models field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_delete_execute()` or `aiplatform_projects_locations_models_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a Model. A model cannot be deleted if any Endpoint resource has a DeployedModel based on the model in its deployed_models field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a Model. A model cannot be deleted if any Endpoint resource has a DeployedModel based on the model in its deployed_models field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_delete_builder()` + `aiplatform_projects_locations_models_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_models_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:deleteVersion
/// Deletes a Model version. Model version can only be deleted if there are no DeployedModels created from it. Deleting the only version in the Model is not allowed. Use DeleteModel for deleting the Model instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_delete_version_execute()` to send, or `aiplatform_projects_locations_models_delete_version` for simplest API.

pub fn aiplatform_projects_locations_models_delete_version_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:deleteVersion",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:deleteVersion
/// Deletes a Model version. Model version can only be deleted if there are no DeployedModels created from it. Deleting the only version in the Model is not allowed. Use DeleteModel for deleting the Model instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_delete_version_execute()` or `aiplatform_projects_locations_models_delete_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_delete_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_delete_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:deleteVersion
/// Deletes a Model version. Model version can only be deleted if there are no DeployedModels created from it. Deleting the only version in the Model is not allowed. Use DeleteModel for deleting the Model instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_delete_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_delete_version_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_delete_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_delete_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_delete_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_delete_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_delete_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsDeleteVersionArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:deleteVersion
/// Deletes a Model version. Model version can only be deleted if there are no DeployedModels created from it. Deleting the only version in the Model is not allowed. Use DeleteModel for deleting the Model instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_delete_version_builder()` + `aiplatform_projects_locations_models_delete_version_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_delete_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_delete_version(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsDeleteVersionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_delete_version_builder(client, &args.name)?;
    aiplatform_projects_locations_models_delete_version_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:export
/// Exports a trained, exportable Model to a location specified by the user. A Model is considered to be exportable if it has at least one supported export format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_export_execute()` to send, or `aiplatform_projects_locations_models_export` for simplest API.

pub fn aiplatform_projects_locations_models_export_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ExportModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:export",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:export
/// Exports a trained, exportable Model to a location specified by the user. A Model is considered to be exportable if it has at least one supported export format.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_export_execute()` or `aiplatform_projects_locations_models_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:export
/// Exports a trained, exportable Model to a location specified by the user. A Model is considered to be exportable if it has at least one supported export format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_export_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsExportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExportModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:export
/// Exports a trained, exportable Model to a location specified by the user. A Model is considered to be exportable if it has at least one supported export format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_export_builder()` + `aiplatform_projects_locations_models_export_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_export(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_models_export_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_models_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a Model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_get_execute()` to send, or `aiplatform_projects_locations_models_get` for simplest API.

pub fn aiplatform_projects_locations_models_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a Model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_get_execute()` or `aiplatform_projects_locations_models_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a Model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a Model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_get_builder()` + `aiplatform_projects_locations_models_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_get_builder(client, &args.name)?;
    aiplatform_projects_locations_models_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_models_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_models_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_get_iam_policy_execute()` or `aiplatform_projects_locations_models_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_get_iam_policy_builder()` + `aiplatform_projects_locations_models_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_models_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models
/// Lists Models in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_list_execute()` to send, or `aiplatform_projects_locations_models_list` for simplest API.

pub fn aiplatform_projects_locations_models_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models
/// Lists Models in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_list_execute()` or `aiplatform_projects_locations_models_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListModelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models
/// Lists Models in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models
/// Lists Models in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_list_builder()` + `aiplatform_projects_locations_models_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_models_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listCheckpoints
/// Lists checkpoints of the specified model version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_list_checkpoints_execute()` to send, or `aiplatform_projects_locations_models_list_checkpoints` for simplest API.

pub fn aiplatform_projects_locations_models_list_checkpoints_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:listCheckpoints",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listCheckpoints
/// Lists checkpoints of the specified model version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_list_checkpoints_execute()` or `aiplatform_projects_locations_models_list_checkpoints`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_list_checkpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_list_checkpoints_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelVersionCheckpointsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListModelVersionCheckpointsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listCheckpoints
/// Lists checkpoints of the specified model version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_list_checkpoints_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_list_checkpoints_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_list_checkpoints()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_list_checkpoints_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_list_checkpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelVersionCheckpointsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_list_checkpoints_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_list_checkpoints`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsListCheckpointsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listCheckpoints
/// Lists checkpoints of the specified model version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_list_checkpoints_builder()` + `aiplatform_projects_locations_models_list_checkpoints_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_list_checkpoints_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_list_checkpoints(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsListCheckpointsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelVersionCheckpointsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_list_checkpoints_builder(
        client,
        &args.name,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_models_list_checkpoints_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listVersions
/// Lists versions of the specified model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_list_versions_execute()` to send, or `aiplatform_projects_locations_models_list_versions` for simplest API.

pub fn aiplatform_projects_locations_models_list_versions_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:listVersions",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listVersions
/// Lists versions of the specified model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_list_versions_execute()` or `aiplatform_projects_locations_models_list_versions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_list_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_list_versions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListModelVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listVersions
/// Lists versions of the specified model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_list_versions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_list_versions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_list_versions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_list_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_list_versions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_list_versions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_list_versions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsListVersionsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:listVersions
/// Lists versions of the specified model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_list_versions_builder()` + `aiplatform_projects_locations_models_list_versions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_list_versions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_list_versions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsListVersionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_list_versions_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_models_list_versions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:mergeVersionAliases
/// Merges a set of aliases for a Model version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_merge_version_aliases_execute()` to send, or `aiplatform_projects_locations_models_merge_version_aliases` for simplest API.

pub fn aiplatform_projects_locations_models_merge_version_aliases_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1MergeVersionAliasesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:mergeVersionAliases",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:mergeVersionAliases
/// Merges a set of aliases for a Model version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_merge_version_aliases_execute()` or `aiplatform_projects_locations_models_merge_version_aliases`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_merge_version_aliases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_merge_version_aliases_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:mergeVersionAliases
/// Merges a set of aliases for a Model version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_merge_version_aliases_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_merge_version_aliases_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_merge_version_aliases()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_merge_version_aliases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_merge_version_aliases_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_merge_version_aliases_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_merge_version_aliases`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsMergeVersionAliasesArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1MergeVersionAliasesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:mergeVersionAliases
/// Merges a set of aliases for a Model version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_merge_version_aliases_builder()` + `aiplatform_projects_locations_models_merge_version_aliases_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_merge_version_aliases_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_merge_version_aliases(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsMergeVersionAliasesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_merge_version_aliases_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_models_merge_version_aliases_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Updates a Model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_patch_execute()` to send, or `aiplatform_projects_locations_models_patch` for simplest API.

pub fn aiplatform_projects_locations_models_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Model,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Updates a Model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_patch_execute()` or `aiplatform_projects_locations_models_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Updates a Model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Model,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Updates a Model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_patch_builder()` + `aiplatform_projects_locations_models_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Model>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_models_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_models_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_models_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_set_iam_policy_execute()` or `aiplatform_projects_locations_models_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_set_iam_policy_builder()` + `aiplatform_projects_locations_models_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_models_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_models_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_models_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_test_iam_permissions_execute()` or `aiplatform_projects_locations_models_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_test_iam_permissions_builder()` + `aiplatform_projects_locations_models_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_test_iam_permissions_builder(
        client,
        &args.resource,
        args.permissions.as_deref(),
    )?;
    aiplatform_projects_locations_models_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:updateExplanationDataset
/// Incrementally update the dataset used for an examples model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_update_explanation_dataset_execute()` to send, or `aiplatform_projects_locations_models_update_explanation_dataset` for simplest API.

pub fn aiplatform_projects_locations_models_update_explanation_dataset_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1UpdateExplanationDatasetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}:updateExplanationDataset",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:updateExplanationDataset
/// Incrementally update the dataset used for an examples model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_update_explanation_dataset_execute()` or `aiplatform_projects_locations_models_update_explanation_dataset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_update_explanation_dataset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_update_explanation_dataset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:updateExplanationDataset
/// Incrementally update the dataset used for an examples model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_update_explanation_dataset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_update_explanation_dataset_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_update_explanation_dataset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_update_explanation_dataset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_update_explanation_dataset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_update_explanation_dataset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_update_explanation_dataset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsUpdateExplanationDatasetArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UpdateExplanationDatasetRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}:updateExplanationDataset
/// Incrementally update the dataset used for an examples model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_update_explanation_dataset_builder()` + `aiplatform_projects_locations_models_update_explanation_dataset_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_update_explanation_dataset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_update_explanation_dataset(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsUpdateExplanationDatasetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_update_explanation_dataset_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_projects_locations_models_update_explanation_dataset_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:upload
/// Uploads a Model artifact into Vertex AI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_upload_execute()` to send, or `aiplatform_projects_locations_models_upload` for simplest API.

pub fn aiplatform_projects_locations_models_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1UploadModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models:upload",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:upload
/// Uploads a Model artifact into Vertex AI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_upload_execute()` or `aiplatform_projects_locations_models_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:upload
/// Uploads a Model artifact into Vertex AI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_upload_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsUploadArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UploadModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models:upload
/// Uploads a Model artifact into Vertex AI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_upload_builder()` + `aiplatform_projects_locations_models_upload_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_upload(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_models_upload_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_models_upload_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}
/// Gets a ModelEvaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_get_execute()` to send, or `aiplatform_projects_locations_models_evaluations_get` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}
/// Gets a ModelEvaluation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_get_execute()` or `aiplatform_projects_locations_models_evaluations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ModelEvaluation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}
/// Gets a ModelEvaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}
/// Gets a ModelEvaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_get_builder()` + `aiplatform_projects_locations_models_evaluations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_models_evaluations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations:import
/// Imports an externally generated ModelEvaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_import_execute()` to send, or `aiplatform_projects_locations_models_evaluations_import` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1ImportModelEvaluationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations:import
/// Imports an externally generated ModelEvaluation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_import_execute()` or `aiplatform_projects_locations_models_evaluations_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ModelEvaluation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations:import
/// Imports an externally generated ModelEvaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_import_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsImportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ImportModelEvaluationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations:import
/// Imports an externally generated ModelEvaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_import_builder()` + `aiplatform_projects_locations_models_evaluations_import_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_import(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_import_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_models_evaluations_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations
/// Lists ModelEvaluations in a Model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_list_execute()` to send, or `aiplatform_projects_locations_models_evaluations_list` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations
/// Lists ModelEvaluations in a Model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_list_execute()` or `aiplatform_projects_locations_models_evaluations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListModelEvaluationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations
/// Lists ModelEvaluations in a Model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations
/// Lists ModelEvaluations in a Model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_list_builder()` + `aiplatform_projects_locations_models_evaluations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListModelEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_models_evaluations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_operations_cancel_execute()` to send, or `aiplatform_projects_locations_models_evaluations_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_operations_cancel_execute()` or `aiplatform_projects_locations_models_evaluations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_operations_cancel_builder()` + `aiplatform_projects_locations_models_evaluations_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_models_evaluations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_operations_delete_execute()` to send, or `aiplatform_projects_locations_models_evaluations_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_operations_delete_execute()` or `aiplatform_projects_locations_models_evaluations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_operations_delete_builder()` + `aiplatform_projects_locations_models_evaluations_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_models_evaluations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_operations_get_execute()` to send, or `aiplatform_projects_locations_models_evaluations_operations_get` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_operations_get_execute()` or `aiplatform_projects_locations_models_evaluations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_operations_get_builder()` + `aiplatform_projects_locations_models_evaluations_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_models_evaluations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_operations_list_execute()` to send, or `aiplatform_projects_locations_models_evaluations_operations_list` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_operations_list_execute()` or `aiplatform_projects_locations_models_evaluations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_operations_list_builder()` + `aiplatform_projects_locations_models_evaluations_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_models_evaluations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_operations_wait_execute()` to send, or `aiplatform_projects_locations_models_evaluations_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_operations_wait_execute()` or `aiplatform_projects_locations_models_evaluations_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_operations_wait_builder()` + `aiplatform_projects_locations_models_evaluations_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_models_evaluations_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}:batchImport
/// Imports a list of externally generated EvaluatedAnnotations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_slices_batch_import_execute()` to send, or `aiplatform_projects_locations_models_evaluations_slices_batch_import` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_slices_batch_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/slices/{}:batchImport",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}:batchImport
/// Imports a list of externally generated EvaluatedAnnotations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_slices_batch_import_execute()` or `aiplatform_projects_locations_models_evaluations_slices_batch_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_slices_batch_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_slices_batch_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}:batchImport
/// Imports a list of externally generated EvaluatedAnnotations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_slices_batch_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_slices_batch_import_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_slices_batch_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_slices_batch_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_slices_batch_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_slices_batch_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_slices_batch_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsSlicesBatchImportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}:batchImport
/// Imports a list of externally generated EvaluatedAnnotations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_slices_batch_import_builder()` + `aiplatform_projects_locations_models_evaluations_slices_batch_import_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_slices_batch_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_slices_batch_import(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsSlicesBatchImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_slices_batch_import_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_models_evaluations_slices_batch_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}
/// Gets a ModelEvaluationSlice.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_slices_get_execute()` to send, or `aiplatform_projects_locations_models_evaluations_slices_get` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_slices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/slices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}
/// Gets a ModelEvaluationSlice.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_slices_get_execute()` or `aiplatform_projects_locations_models_evaluations_slices_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_slices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_slices_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluationSlice>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ModelEvaluationSlice =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}
/// Gets a ModelEvaluationSlice.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_slices_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_slices_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_slices_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_slices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_slices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluationSlice>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_slices_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_slices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsSlicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices/{slicesId}
/// Gets a ModelEvaluationSlice.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_slices_get_builder()` + `aiplatform_projects_locations_models_evaluations_slices_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_slices_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_slices_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsSlicesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ModelEvaluationSlice>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_models_evaluations_slices_get_builder(client, &args.name)?;
    aiplatform_projects_locations_models_evaluations_slices_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices
/// Lists ModelEvaluationSlices in a ModelEvaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_evaluations_slices_list_execute()` to send, or `aiplatform_projects_locations_models_evaluations_slices_list` for simplest API.

pub fn aiplatform_projects_locations_models_evaluations_slices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/evaluations/{}/slices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices
/// Lists ModelEvaluationSlices in a ModelEvaluation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_evaluations_slices_list_execute()` or `aiplatform_projects_locations_models_evaluations_slices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_slices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_slices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelEvaluationSlicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListModelEvaluationSlicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices
/// Lists ModelEvaluationSlices in a ModelEvaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_evaluations_slices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_slices_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_evaluations_slices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_evaluations_slices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_evaluations_slices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelEvaluationSlicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_evaluations_slices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_evaluations_slices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsEvaluationsSlicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/evaluations/{evaluationsId}/slices
/// Lists ModelEvaluationSlices in a ModelEvaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_evaluations_slices_list_builder()` + `aiplatform_projects_locations_models_evaluations_slices_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_evaluations_slices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_evaluations_slices_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsEvaluationsSlicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListModelEvaluationSlicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_evaluations_slices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_models_evaluations_slices_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_operations_cancel_execute()` to send, or `aiplatform_projects_locations_models_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_models_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_operations_cancel_execute()` or `aiplatform_projects_locations_models_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_operations_cancel_builder()` + `aiplatform_projects_locations_models_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_models_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_models_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_operations_delete_execute()` to send, or `aiplatform_projects_locations_models_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_models_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_operations_delete_execute()` or `aiplatform_projects_locations_models_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_operations_delete_builder()` + `aiplatform_projects_locations_models_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_models_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_models_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_operations_get_execute()` to send, or `aiplatform_projects_locations_models_operations_get` for simplest API.

pub fn aiplatform_projects_locations_models_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_operations_get_execute()` or `aiplatform_projects_locations_models_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_operations_get_builder()` + `aiplatform_projects_locations_models_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_models_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_operations_list_execute()` to send, or `aiplatform_projects_locations_models_operations_list` for simplest API.

pub fn aiplatform_projects_locations_models_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_operations_list_execute()` or `aiplatform_projects_locations_models_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_operations_list_builder()` + `aiplatform_projects_locations_models_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_models_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_models_operations_wait_execute()` to send, or `aiplatform_projects_locations_models_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_models_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/models/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_models_operations_wait_execute()` or `aiplatform_projects_locations_models_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_models_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_models_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_models_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_models_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_models_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_models_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_models_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsModelsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/models/{modelsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_models_operations_wait_builder()` + `aiplatform_projects_locations_models_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_models_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_models_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsModelsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_models_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_models_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}:cancel
/// Cancels a NasJob. Starts asynchronous cancellation on the NasJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetNasJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the NasJob is not deleted; instead it becomes a job with a NasJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and NasJob.state is set to CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_nas_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelNasJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}:cancel
/// Cancels a NasJob. Starts asynchronous cancellation on the NasJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetNasJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the NasJob is not deleted; instead it becomes a job with a NasJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and NasJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_cancel_execute()` or `aiplatform_projects_locations_nas_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}:cancel
/// Cancels a NasJob. Starts asynchronous cancellation on the NasJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetNasJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the NasJob is not deleted; instead it becomes a job with a NasJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and NasJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelNasJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}:cancel
/// Cancels a NasJob. Starts asynchronous cancellation on the NasJob. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use JobService.GetNasJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the NasJob is not deleted; instead it becomes a job with a NasJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and NasJob.state is set to CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_cancel_builder()` + `aiplatform_projects_locations_nas_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_nas_jobs_cancel_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_nas_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Creates a NasJob
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_create_execute()` to send, or `aiplatform_projects_locations_nas_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1NasJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Creates a NasJob
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_create_execute()` or `aiplatform_projects_locations_nas_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NasJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Creates a NasJob
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1NasJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Creates a NasJob
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_create_builder()` + `aiplatform_projects_locations_nas_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_nas_jobs_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_nas_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Deletes a NasJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_delete_execute()` to send, or `aiplatform_projects_locations_nas_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Deletes a NasJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_delete_execute()` or `aiplatform_projects_locations_nas_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Deletes a NasJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Deletes a NasJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_delete_builder()` + `aiplatform_projects_locations_nas_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_nas_jobs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_nas_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Gets a NasJob
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_get_execute()` to send, or `aiplatform_projects_locations_nas_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Gets a NasJob
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_get_execute()` or `aiplatform_projects_locations_nas_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NasJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Gets a NasJob
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}
/// Gets a NasJob
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_get_builder()` + `aiplatform_projects_locations_nas_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_nas_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_nas_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Lists NasJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_list_execute()` to send, or `aiplatform_projects_locations_nas_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Lists NasJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_list_execute()` or `aiplatform_projects_locations_nas_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNasJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListNasJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Lists NasJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNasJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs
/// Lists NasJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_list_builder()` + `aiplatform_projects_locations_nas_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNasJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_nas_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_nas_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails/{nasTrialDetailsId}
/// Gets a NasTrialDetail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_execute()` to send, or `aiplatform_projects_locations_nas_jobs_nas_trial_details_get` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs/{}/nasTrialDetails/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails/{nasTrialDetailsId}
/// Gets a NasTrialDetail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_execute()` or `aiplatform_projects_locations_nas_jobs_nas_trial_details_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasTrialDetail>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NasTrialDetail = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails/{nasTrialDetailsId}
/// Gets a NasTrialDetail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasTrialDetail>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_nas_trial_details_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_nas_trial_details_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsNasTrialDetailsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails/{nasTrialDetailsId}
/// Gets a NasTrialDetail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_builder()` + `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsNasTrialDetailsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NasTrialDetail>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_nas_jobs_nas_trial_details_get_builder(client, &args.name)?;
    aiplatform_projects_locations_nas_jobs_nas_trial_details_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails
/// List top NasTrialDetails of a NasJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_execute()` to send, or `aiplatform_projects_locations_nas_jobs_nas_trial_details_list` for simplest API.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/nasJobs/{}/nasTrialDetails",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails
/// List top NasTrialDetails of a NasJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_execute()` or `aiplatform_projects_locations_nas_jobs_nas_trial_details_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNasTrialDetailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListNasTrialDetailsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails
/// List top NasTrialDetails of a NasJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNasTrialDetailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_nas_jobs_nas_trial_details_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_nas_jobs_nas_trial_details_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNasJobsNasTrialDetailsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/nasJobs/{nasJobsId}/nasTrialDetails
/// List top NasTrialDetails of a NasJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_builder()` + `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_nas_jobs_nas_trial_details_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_nas_jobs_nas_trial_details_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNasJobsNasTrialDetailsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNasTrialDetailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_nas_jobs_nas_trial_details_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_nas_jobs_nas_trial_details_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Creates a NotebookExecutionJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_create_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    notebookExecutionJobId: Option<&str>,
    body: &GoogleCloudAiplatformV1NotebookExecutionJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notebookExecutionJobId {
        query_parts.push(format!("notebookExecutionJobId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Creates a NotebookExecutionJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_create_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Creates a NotebookExecutionJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notebookExecutionJobId
    pub notebookExecutionJobId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1NotebookExecutionJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Creates a NotebookExecutionJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_create_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_create_builder(
        client,
        &args.parent,
        args.notebookExecutionJobId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Deletes a NotebookExecutionJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_delete_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Deletes a NotebookExecutionJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_delete_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Deletes a NotebookExecutionJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Deletes a NotebookExecutionJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_delete_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_execution_jobs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_notebook_execution_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Gets a NotebookExecutionJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_get_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Gets a NotebookExecutionJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_get_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookExecutionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NotebookExecutionJob =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Gets a NotebookExecutionJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookExecutionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}
/// Gets a NotebookExecutionJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_get_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookExecutionJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Lists NotebookExecutionJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_list_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Lists NotebookExecutionJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_list_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListNotebookExecutionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListNotebookExecutionJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Lists NotebookExecutionJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListNotebookExecutionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs
/// Lists NotebookExecutionJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_list_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListNotebookExecutionJobsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_execution_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_operations_get_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_get_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_operations_list_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_list_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_execute()` to send, or `aiplatform_projects_locations_notebook_execution_jobs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookExecutionJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_execute()` or `aiplatform_projects_locations_notebook_execution_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_execution_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_execution_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookExecutionJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookExecutionJobs/{notebookExecutionJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder()` + `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_execution_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_execution_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Creates a NotebookRuntimeTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_create_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_create` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    notebookRuntimeTemplateId: Option<&str>,
    body: &GoogleCloudAiplatformV1NotebookRuntimeTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = notebookRuntimeTemplateId {
        query_parts.push(format!("notebookRuntimeTemplateId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Creates a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_create_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Creates a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtime_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: notebookRuntimeTemplateId
    pub notebookRuntimeTemplateId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1NotebookRuntimeTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Creates a NotebookRuntimeTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_create_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_create_builder(
        client,
        &args.parent,
        args.notebookRuntimeTemplateId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Deletes a NotebookRuntimeTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_delete_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_delete` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Deletes a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_delete_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Deletes a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtime_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Deletes a NotebookRuntimeTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_delete_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Gets a NotebookRuntimeTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_get_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_get` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Gets a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_get_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntimeTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NotebookRuntimeTemplate =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Gets a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntimeTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtime_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Gets a NotebookRuntimeTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_get_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntimeTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_runtime_templates_get_builder(client, &args.name)?;
    aiplatform_projects_locations_notebook_runtime_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Lists NotebookRuntimeTemplates in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_list_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_list` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Lists NotebookRuntimeTemplates in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_list_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListNotebookRuntimeTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListNotebookRuntimeTemplatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Lists NotebookRuntimeTemplates in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListNotebookRuntimeTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtime_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates
/// Lists NotebookRuntimeTemplates in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_list_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListNotebookRuntimeTemplatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Updates a NotebookRuntimeTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_patch_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_patch` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1NotebookRuntimeTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Updates a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_patch_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntimeTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NotebookRuntimeTemplate =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Updates a NotebookRuntimeTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntimeTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtime_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1NotebookRuntimeTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}
/// Updates a NotebookRuntimeTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_patch_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntimeTemplate>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder(
            client,
            &args.resource,
            args.permissions.as_deref(),
        )?;
    aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_notebook_runtime_templates_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_operations_get_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_operations_get` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_operations_get_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_get_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_operations_list_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_operations_list` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_operations_list_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_list_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_execute()` to send, or `aiplatform_projects_locations_notebook_runtime_templates_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimeTemplates/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_execute()` or `aiplatform_projects_locations_notebook_runtime_templates_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_notebook_runtime_templates_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtime_templates_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimeTemplates/{notebookRuntimeTemplatesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder()` + `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtime_templates_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_runtime_templates_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes:assign
/// Assigns a NotebookRuntime to a user for a particular Notebook file. This method will either returns an existing assignment or generates a new one.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_assign_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_assign` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_assign_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1AssignNotebookRuntimeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes:assign",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes:assign
/// Assigns a NotebookRuntime to a user for a particular Notebook file. This method will either returns an existing assignment or generates a new one.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_assign_execute()` or `aiplatform_projects_locations_notebook_runtimes_assign`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_assign_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_assign_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes:assign
/// Assigns a NotebookRuntime to a user for a particular Notebook file. This method will either returns an existing assignment or generates a new one.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_assign_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_assign_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_assign()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_assign_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_assign_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_assign_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_assign`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesAssignArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AssignNotebookRuntimeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes:assign
/// Assigns a NotebookRuntime to a user for a particular Notebook file. This method will either returns an existing assignment or generates a new one.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_assign_builder()` + `aiplatform_projects_locations_notebook_runtimes_assign_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_assign_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_assign(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesAssignArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_assign_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtimes_assign_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Deletes a NotebookRuntime.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_delete_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_delete` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Deletes a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_delete_execute()` or `aiplatform_projects_locations_notebook_runtimes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Deletes a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Deletes a NotebookRuntime.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_delete_builder()` + `aiplatform_projects_locations_notebook_runtimes_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_runtimes_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_notebook_runtimes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Gets a NotebookRuntime.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_get_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_get` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Gets a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_get_execute()` or `aiplatform_projects_locations_notebook_runtimes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntime>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1NotebookRuntime = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Gets a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntime>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}
/// Gets a NotebookRuntime.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_get_builder()` + `aiplatform_projects_locations_notebook_runtimes_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1NotebookRuntime>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_get_builder(client, &args.name)?;
    aiplatform_projects_locations_notebook_runtimes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes
/// Lists NotebookRuntimes in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_list_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_list` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes
/// Lists NotebookRuntimes in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_list_execute()` or `aiplatform_projects_locations_notebook_runtimes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNotebookRuntimesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListNotebookRuntimesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes
/// Lists NotebookRuntimes in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNotebookRuntimesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes
/// Lists NotebookRuntimes in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_list_builder()` + `aiplatform_projects_locations_notebook_runtimes_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListNotebookRuntimesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_runtimes_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:start
/// Starts a NotebookRuntime.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_start_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_start` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_start_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1StartNotebookRuntimeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}:start",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:start
/// Starts a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_start_execute()` or `aiplatform_projects_locations_notebook_runtimes_start`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_start_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:start
/// Starts a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_start_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_start_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_start()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_start_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_start`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesStartArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StartNotebookRuntimeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:start
/// Starts a NotebookRuntime.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_start_builder()` + `aiplatform_projects_locations_notebook_runtimes_start_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_start_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_start(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesStartArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_start_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtimes_start_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:stop
/// Stops a NotebookRuntime.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_stop_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_stop` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_stop_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1StopNotebookRuntimeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}:stop",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:stop
/// Stops a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_stop_execute()` or `aiplatform_projects_locations_notebook_runtimes_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:stop
/// Stops a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_stop_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesStopArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StopNotebookRuntimeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:stop
/// Stops a NotebookRuntime.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_stop_builder()` + `aiplatform_projects_locations_notebook_runtimes_stop_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_stop(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesStopArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_stop_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtimes_stop_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:upgrade
/// Upgrades a NotebookRuntime.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_upgrade_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_upgrade` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_upgrade_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1UpgradeNotebookRuntimeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}:upgrade",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:upgrade
/// Upgrades a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_upgrade_execute()` or `aiplatform_projects_locations_notebook_runtimes_upgrade`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_upgrade_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:upgrade
/// Upgrades a NotebookRuntime.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_upgrade_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_upgrade_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_upgrade()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_upgrade_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_upgrade`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesUpgradeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1UpgradeNotebookRuntimeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}:upgrade
/// Upgrades a NotebookRuntime.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_upgrade_builder()` + `aiplatform_projects_locations_notebook_runtimes_upgrade_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_upgrade_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_upgrade(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesUpgradeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_upgrade_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_notebook_runtimes_upgrade_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_operations_cancel_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_operations_cancel_execute()` or `aiplatform_projects_locations_notebook_runtimes_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder()` + `aiplatform_projects_locations_notebook_runtimes_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_runtimes_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_operations_delete_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_operations_delete_execute()` or `aiplatform_projects_locations_notebook_runtimes_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_operations_delete_builder()` + `aiplatform_projects_locations_notebook_runtimes_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_notebook_runtimes_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_operations_get_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_operations_get` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_operations_get_execute()` or `aiplatform_projects_locations_notebook_runtimes_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_operations_get_builder()` + `aiplatform_projects_locations_notebook_runtimes_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_notebook_runtimes_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_notebook_runtimes_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_operations_list_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_operations_list` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_operations_list_execute()` or `aiplatform_projects_locations_notebook_runtimes_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_operations_list_builder()` + `aiplatform_projects_locations_notebook_runtimes_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_notebook_runtimes_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_notebook_runtimes_operations_wait_execute()` to send, or `aiplatform_projects_locations_notebook_runtimes_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/notebookRuntimes/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_notebook_runtimes_operations_wait_execute()` or `aiplatform_projects_locations_notebook_runtimes_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_notebook_runtimes_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_notebook_runtimes_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_notebook_runtimes_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_notebook_runtimes_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_notebook_runtimes_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsNotebookRuntimesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notebookRuntimes/{notebookRuntimesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_notebook_runtimes_operations_wait_builder()` + `aiplatform_projects_locations_notebook_runtimes_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_notebook_runtimes_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_notebook_runtimes_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsNotebookRuntimesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_notebook_runtimes_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_notebook_runtimes_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_operations_cancel_execute()` to send, or `aiplatform_projects_locations_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_operations_cancel_execute()` or `aiplatform_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_operations_cancel_builder()` + `aiplatform_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_operations_delete_execute()` to send, or `aiplatform_projects_locations_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_operations_delete_execute()` or `aiplatform_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_operations_delete_builder()` + `aiplatform_projects_locations_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_operations_get_execute()` to send, or `aiplatform_projects_locations_operations_get` for simplest API.

pub fn aiplatform_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_operations_get_execute()` or `aiplatform_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_operations_get_builder()` + `aiplatform_projects_locations_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_operations_list_execute()` to send, or `aiplatform_projects_locations_operations_list` for simplest API.

pub fn aiplatform_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_operations_list_execute()` or `aiplatform_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_operations_list_builder()` + `aiplatform_projects_locations_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_operations_wait_execute()` to send, or `aiplatform_projects_locations_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_operations_wait_execute()` or `aiplatform_projects_locations_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_operations_wait_builder()` + `aiplatform_projects_locations_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Creates a PersistentResource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_create_execute()` to send, or `aiplatform_projects_locations_persistent_resources_create` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    persistentResourceId: Option<&str>,
    body: &GoogleCloudAiplatformV1PersistentResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = persistentResourceId {
        query_parts.push(format!("persistentResourceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Creates a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_create_execute()` or `aiplatform_projects_locations_persistent_resources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Creates a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: persistentResourceId
    pub persistentResourceId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PersistentResource,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Creates a PersistentResource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_create_builder()` + `aiplatform_projects_locations_persistent_resources_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_create_builder(
        client,
        &args.parent,
        args.persistentResourceId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_persistent_resources_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Deletes a PersistentResource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_delete_execute()` to send, or `aiplatform_projects_locations_persistent_resources_delete` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Deletes a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_delete_execute()` or `aiplatform_projects_locations_persistent_resources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Deletes a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Deletes a PersistentResource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_delete_builder()` + `aiplatform_projects_locations_persistent_resources_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_persistent_resources_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_persistent_resources_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Gets a PersistentResource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_get_execute()` to send, or `aiplatform_projects_locations_persistent_resources_get` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Gets a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_get_execute()` or `aiplatform_projects_locations_persistent_resources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PersistentResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PersistentResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Gets a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PersistentResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Gets a PersistentResource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_get_builder()` + `aiplatform_projects_locations_persistent_resources_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PersistentResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_persistent_resources_get_builder(client, &args.name)?;
    aiplatform_projects_locations_persistent_resources_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Lists PersistentResources in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_list_execute()` to send, or `aiplatform_projects_locations_persistent_resources_list` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Lists PersistentResources in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_list_execute()` or `aiplatform_projects_locations_persistent_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListPersistentResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListPersistentResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Lists PersistentResources in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListPersistentResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources
/// Lists PersistentResources in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_list_builder()` + `aiplatform_projects_locations_persistent_resources_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListPersistentResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_persistent_resources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Updates a PersistentResource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_patch_execute()` to send, or `aiplatform_projects_locations_persistent_resources_patch` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1PersistentResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Updates a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_patch_execute()` or `aiplatform_projects_locations_persistent_resources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Updates a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PersistentResource,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}
/// Updates a PersistentResource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_patch_builder()` + `aiplatform_projects_locations_persistent_resources_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_persistent_resources_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}:reboot
/// Reboots a PersistentResource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_reboot_execute()` to send, or `aiplatform_projects_locations_persistent_resources_reboot` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_reboot_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1RebootPersistentResourceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}:reboot",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}:reboot
/// Reboots a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_reboot_execute()` or `aiplatform_projects_locations_persistent_resources_reboot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_reboot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_reboot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}:reboot
/// Reboots a PersistentResource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_reboot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_reboot_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_reboot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_reboot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_reboot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_reboot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_reboot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesRebootArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RebootPersistentResourceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}:reboot
/// Reboots a PersistentResource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_reboot_builder()` + `aiplatform_projects_locations_persistent_resources_reboot_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_reboot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_reboot(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesRebootArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_reboot_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_persistent_resources_reboot_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_operations_cancel_execute()` to send, or `aiplatform_projects_locations_persistent_resources_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_operations_cancel_execute()` or `aiplatform_projects_locations_persistent_resources_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_operations_cancel_builder()` + `aiplatform_projects_locations_persistent_resources_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_persistent_resources_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_operations_delete_execute()` to send, or `aiplatform_projects_locations_persistent_resources_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_operations_delete_execute()` or `aiplatform_projects_locations_persistent_resources_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_operations_delete_builder()` + `aiplatform_projects_locations_persistent_resources_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_persistent_resources_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_operations_get_execute()` to send, or `aiplatform_projects_locations_persistent_resources_operations_get` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_operations_get_execute()` or `aiplatform_projects_locations_persistent_resources_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_operations_get_builder()` + `aiplatform_projects_locations_persistent_resources_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_persistent_resources_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_operations_list_execute()` to send, or `aiplatform_projects_locations_persistent_resources_operations_list` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_operations_list_execute()` or `aiplatform_projects_locations_persistent_resources_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_operations_list_builder()` + `aiplatform_projects_locations_persistent_resources_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_persistent_resources_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_persistent_resources_operations_wait_execute()` to send, or `aiplatform_projects_locations_persistent_resources_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_persistent_resources_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/persistentResources/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_persistent_resources_operations_wait_execute()` or `aiplatform_projects_locations_persistent_resources_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_persistent_resources_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_persistent_resources_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_persistent_resources_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_persistent_resources_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_persistent_resources_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_persistent_resources_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPersistentResourcesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/persistentResources/{persistentResourcesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_persistent_resources_operations_wait_builder()` + `aiplatform_projects_locations_persistent_resources_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_persistent_resources_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_persistent_resources_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPersistentResourcesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_persistent_resources_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_persistent_resources_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchCancel
/// Batch cancel PipelineJobs. Firstly the server will check if all the jobs are in non-terminal states, and skip the jobs that are already terminated. If the operation failed, none of the pipeline jobs are cancelled. The server will poll the states of all the pipeline jobs periodically to check the cancellation status. This operation will return an LRO.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_batch_cancel_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_batch_cancel` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchCancelPipelineJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs:batchCancel",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchCancel
/// Batch cancel PipelineJobs. Firstly the server will check if all the jobs are in non-terminal states, and skip the jobs that are already terminated. If the operation failed, none of the pipeline jobs are cancelled. The server will poll the states of all the pipeline jobs periodically to check the cancellation status. This operation will return an LRO.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_batch_cancel_execute()` or `aiplatform_projects_locations_pipeline_jobs_batch_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchCancel
/// Batch cancel PipelineJobs. Firstly the server will check if all the jobs are in non-terminal states, and skip the jobs that are already terminated. If the operation failed, none of the pipeline jobs are cancelled. The server will poll the states of all the pipeline jobs periodically to check the cancellation status. This operation will return an LRO.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_batch_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_batch_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_batch_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_batch_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsBatchCancelArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchCancelPipelineJobsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchCancel
/// Batch cancel PipelineJobs. Firstly the server will check if all the jobs are in non-terminal states, and skip the jobs that are already terminated. If the operation failed, none of the pipeline jobs are cancelled. The server will poll the states of all the pipeline jobs periodically to check the cancellation status. This operation will return an LRO.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder()` + `aiplatform_projects_locations_pipeline_jobs_batch_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_batch_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsBatchCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_pipeline_jobs_batch_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchDelete
/// Batch deletes PipelineJobs The Operation is atomic. If it fails, none of the PipelineJobs are deleted. If it succeeds, all of the PipelineJobs are deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_batch_delete_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_batch_delete` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchDeletePipelineJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs:batchDelete",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchDelete
/// Batch deletes PipelineJobs The Operation is atomic. If it fails, none of the PipelineJobs are deleted. If it succeeds, all of the PipelineJobs are deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_batch_delete_execute()` or `aiplatform_projects_locations_pipeline_jobs_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchDelete
/// Batch deletes PipelineJobs The Operation is atomic. If it fails, none of the PipelineJobs are deleted. If it succeeds, all of the PipelineJobs are deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_batch_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsBatchDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchDeletePipelineJobsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs:batchDelete
/// Batch deletes PipelineJobs The Operation is atomic. If it fails, none of the PipelineJobs are deleted. If it succeeds, all of the PipelineJobs are deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_batch_delete_builder()` + `aiplatform_projects_locations_pipeline_jobs_batch_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_batch_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_batch_delete_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_pipeline_jobs_batch_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}:cancel
/// Cancels a PipelineJob. Starts asynchronous cancellation on the PipelineJob. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetPipelineJob or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the PipelineJob is not deleted; instead it becomes a pipeline with a PipelineJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and PipelineJob.state is set to CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelPipelineJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}:cancel
/// Cancels a PipelineJob. Starts asynchronous cancellation on the PipelineJob. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetPipelineJob or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the PipelineJob is not deleted; instead it becomes a pipeline with a PipelineJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and PipelineJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_cancel_execute()` or `aiplatform_projects_locations_pipeline_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}:cancel
/// Cancels a PipelineJob. Starts asynchronous cancellation on the PipelineJob. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetPipelineJob or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the PipelineJob is not deleted; instead it becomes a pipeline with a PipelineJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and PipelineJob.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelPipelineJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}:cancel
/// Cancels a PipelineJob. Starts asynchronous cancellation on the PipelineJob. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetPipelineJob or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the PipelineJob is not deleted; instead it becomes a pipeline with a PipelineJob.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and PipelineJob.state is set to CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_cancel_builder()` + `aiplatform_projects_locations_pipeline_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_pipeline_jobs_cancel_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_pipeline_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Creates a PipelineJob. A PipelineJob will run immediately when created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_create_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pipelineJobId: Option<&str>,
    body: &GoogleCloudAiplatformV1PipelineJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pipelineJobId {
        query_parts.push(format!("pipelineJobId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Creates a PipelineJob. A PipelineJob will run immediately when created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_create_execute()` or `aiplatform_projects_locations_pipeline_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PipelineJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PipelineJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Creates a PipelineJob. A PipelineJob will run immediately when created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PipelineJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pipelineJobId
    pub pipelineJobId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PipelineJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Creates a PipelineJob. A PipelineJob will run immediately when created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_create_builder()` + `aiplatform_projects_locations_pipeline_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PipelineJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_create_builder(
        client,
        &args.parent,
        args.pipelineJobId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_pipeline_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Deletes a PipelineJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_delete_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_delete` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Deletes a PipelineJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_delete_execute()` or `aiplatform_projects_locations_pipeline_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Deletes a PipelineJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Deletes a PipelineJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_delete_builder()` + `aiplatform_projects_locations_pipeline_jobs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_pipeline_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Gets a PipelineJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_get_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Gets a PipelineJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_get_execute()` or `aiplatform_projects_locations_pipeline_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PipelineJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PipelineJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Gets a PipelineJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PipelineJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}
/// Gets a PipelineJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_get_builder()` + `aiplatform_projects_locations_pipeline_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PipelineJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_pipeline_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Lists PipelineJobs in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_list_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Lists PipelineJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_list_execute()` or `aiplatform_projects_locations_pipeline_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListPipelineJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListPipelineJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Lists PipelineJobs in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListPipelineJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs
/// Lists PipelineJobs in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_list_builder()` + `aiplatform_projects_locations_pipeline_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListPipelineJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_pipeline_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_pipeline_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_pipeline_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_pipeline_jobs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_operations_delete_execute()` or `aiplatform_projects_locations_pipeline_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_operations_delete_builder()` + `aiplatform_projects_locations_pipeline_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_pipeline_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_pipeline_jobs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_operations_get_execute()` or `aiplatform_projects_locations_pipeline_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_operations_get_builder()` + `aiplatform_projects_locations_pipeline_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_pipeline_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_pipeline_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_operations_list_execute()` or `aiplatform_projects_locations_pipeline_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_operations_list_builder()` + `aiplatform_projects_locations_pipeline_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_pipeline_jobs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_pipeline_jobs_operations_wait_execute()` to send, or `aiplatform_projects_locations_pipeline_jobs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/pipelineJobs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_pipeline_jobs_operations_wait_execute()` or `aiplatform_projects_locations_pipeline_jobs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_pipeline_jobs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_pipeline_jobs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_pipeline_jobs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_pipeline_jobs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_pipeline_jobs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPipelineJobsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelineJobs/{pipelineJobsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_pipeline_jobs_operations_wait_builder()` + `aiplatform_projects_locations_pipeline_jobs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_pipeline_jobs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_pipeline_jobs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPipelineJobsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_pipeline_jobs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_pipeline_jobs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_compute_tokens_execute()` to send, or `aiplatform_projects_locations_publishers_models_compute_tokens` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_compute_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1ComputeTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:computeTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_compute_tokens_execute()` or `aiplatform_projects_locations_publishers_models_compute_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_compute_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ComputeTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_compute_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_compute_tokens_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_compute_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_compute_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_compute_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_compute_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsComputeTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ComputeTokensRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_compute_tokens_builder()` + `aiplatform_projects_locations_publishers_models_compute_tokens_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_compute_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_compute_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsComputeTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_compute_tokens_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_compute_tokens_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_count_tokens_execute()` to send, or `aiplatform_projects_locations_publishers_models_count_tokens` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_count_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1CountTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:countTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_count_tokens_execute()` or `aiplatform_projects_locations_publishers_models_count_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_count_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CountTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_count_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_count_tokens_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_count_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_count_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_count_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_count_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsCountTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CountTokensRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_count_tokens_builder()` + `aiplatform_projects_locations_publishers_models_count_tokens_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_count_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_count_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsCountTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_count_tokens_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_count_tokens_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:embedContent
/// Embed content with multimodal inputs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_embed_content_execute()` to send, or `aiplatform_projects_locations_publishers_models_embed_content` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_embed_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1EmbedContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:embedContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:embedContent
/// Embed content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_embed_content_execute()` or `aiplatform_projects_locations_publishers_models_embed_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_embed_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_embed_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EmbedContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EmbedContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:embedContent
/// Embed content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_embed_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_embed_content_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_embed_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_embed_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_embed_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EmbedContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_embed_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_embed_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsEmbedContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1EmbedContentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:embedContent
/// Embed content with multimodal inputs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_embed_content_builder()` + `aiplatform_projects_locations_publishers_models_embed_content_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_embed_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_embed_content(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsEmbedContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EmbedContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_embed_content_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_embed_content_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_fetch_predict_operation_execute()` to send, or `aiplatform_projects_locations_publishers_models_fetch_predict_operation` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1FetchPredictOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:fetchPredictOperation",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_fetch_predict_operation_execute()` or `aiplatform_projects_locations_publishers_models_fetch_predict_operation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_fetch_predict_operation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_fetch_predict_operation_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_fetch_predict_operation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_fetch_predict_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_publishers_models_fetch_predict_operation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_fetch_predict_operation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsFetchPredictOperationArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FetchPredictOperationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder()` + `aiplatform_projects_locations_publishers_models_fetch_predict_operation_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_fetch_predict_operation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_fetch_predict_operation(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsFetchPredictOperationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_fetch_predict_operation_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_generate_content_execute()` to send, or `aiplatform_projects_locations_publishers_models_generate_content` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:generateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_generate_content_execute()` or `aiplatform_projects_locations_publishers_models_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_generate_content_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_generate_content_builder()` + `aiplatform_projects_locations_publishers_models_generate_content_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_generate_content_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_generate_content_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_predict_execute()` to send, or `aiplatform_projects_locations_publishers_models_predict` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:predict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_predict_execute()` or `aiplatform_projects_locations_publishers_models_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PredictResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_predict_builder()` + `aiplatform_projects_locations_publishers_models_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_predict_long_running_execute()` to send, or `aiplatform_projects_locations_publishers_models_predict_long_running` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_predict_long_running_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictLongRunningRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:predictLongRunning",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_predict_long_running_execute()` or `aiplatform_projects_locations_publishers_models_predict_long_running`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_predict_long_running_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_predict_long_running_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_predict_long_running_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_predict_long_running()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_predict_long_running_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_predict_long_running_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_predict_long_running`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsPredictLongRunningArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictLongRunningRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_predict_long_running_builder()` + `aiplatform_projects_locations_publishers_models_predict_long_running_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_predict_long_running_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_predict_long_running(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsPredictLongRunningArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_predict_long_running_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_predict_long_running_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_raw_predict_execute()` to send, or `aiplatform_projects_locations_publishers_models_raw_predict` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_raw_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1RawPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:rawPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_raw_predict_execute()` or `aiplatform_projects_locations_publishers_models_raw_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_raw_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_raw_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_raw_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_raw_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_raw_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_raw_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_raw_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsRawPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RawPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:rawPredict
/// Perform an online prediction with an arbitrary HTTP payload. The response includes the following HTTP headers: * X-Vertex-AI-Endpoint-Id: ID of the Endpoint that served this prediction. * X-Vertex-AI-Deployed-Model-Id: ID of the Endpoint's DeployedModel that served this prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_raw_predict_builder()` + `aiplatform_projects_locations_publishers_models_raw_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_raw_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_raw_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsRawPredictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_raw_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_raw_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_server_streaming_predict_execute()` to send, or `aiplatform_projects_locations_publishers_models_server_streaming_predict` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_server_streaming_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1StreamingPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:serverStreamingPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_server_streaming_predict_execute()` or `aiplatform_projects_locations_publishers_models_server_streaming_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_server_streaming_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_server_streaming_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1StreamingPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1StreamingPredictResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_server_streaming_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_server_streaming_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_server_streaming_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_server_streaming_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_server_streaming_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1StreamingPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_publishers_models_server_streaming_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_server_streaming_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsServerStreamingPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamingPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:serverStreamingPredict
/// Perform a server-side streaming online prediction request for Vertex LLM streaming.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_server_streaming_predict_builder()` + `aiplatform_projects_locations_publishers_models_server_streaming_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_server_streaming_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_server_streaming_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsServerStreamingPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1StreamingPredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_server_streaming_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_server_streaming_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_stream_generate_content_execute()` to send, or `aiplatform_projects_locations_publishers_models_stream_generate_content` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_stream_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:streamGenerateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_stream_generate_content_execute()` or `aiplatform_projects_locations_publishers_models_stream_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_stream_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_stream_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_stream_generate_content_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_stream_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_stream_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_publishers_models_stream_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_stream_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsStreamGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_stream_generate_content_builder()` + `aiplatform_projects_locations_publishers_models_stream_generate_content_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_stream_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_stream_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsStreamGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_stream_generate_content_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_stream_generate_content_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_stream_raw_predict_execute()` to send, or `aiplatform_projects_locations_publishers_models_stream_raw_predict` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_stream_raw_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1StreamRawPredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}:streamRawPredict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_stream_raw_predict_execute()` or `aiplatform_projects_locations_publishers_models_stream_raw_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_stream_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_stream_raw_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_stream_raw_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_stream_raw_predict_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_stream_raw_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_stream_raw_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_stream_raw_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_stream_raw_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_stream_raw_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsStreamRawPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamRawPredictRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}:streamRawPredict
/// Perform a streaming online prediction with an arbitrary HTTP payload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_stream_raw_predict_builder()` + `aiplatform_projects_locations_publishers_models_stream_raw_predict_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_stream_raw_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_stream_raw_predict(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsStreamRawPredictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_stream_raw_predict_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_stream_raw_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_publishers_models_invoke_invoke_execute()` to send, or `aiplatform_projects_locations_publishers_models_invoke_invoke` for simplest API.

pub fn aiplatform_projects_locations_publishers_models_invoke_invoke_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    invokeId: &str,
    body: &GoogleCloudAiplatformV1InvokeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/publishers/{}/models/{}/invoke/{}",
        endpoint,
        invokeId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_publishers_models_invoke_invoke_execute()` or `aiplatform_projects_locations_publishers_models_invoke_invoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_invoke_invoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_invoke_invoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_publishers_models_invoke_invoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_invoke_invoke_task()`.
/// For the simplest API, use `aiplatform_projects_locations_publishers_models_invoke_invoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_publishers_models_invoke_invoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_publishers_models_invoke_invoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_publishers_models_invoke_invoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_publishers_models_invoke_invoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsPublishersModelsInvokeInvokeArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Path parameter: invokeId
    pub invokeId: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1InvokeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/publishers/{publishersId}/models/{modelsId}/invoke/{invokeId}
/// Forwards arbitrary HTTP requests for both streaming and non-streaming cases. To use this method, invoke_route_prefix must be set to allow the paths that will be specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_publishers_models_invoke_invoke_builder()` + `aiplatform_projects_locations_publishers_models_invoke_invoke_execute()`.
/// For task-level control, use `aiplatform_projects_locations_publishers_models_invoke_invoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_publishers_models_invoke_invoke(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsPublishersModelsInvokeInvokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_publishers_models_invoke_invoke_builder(
        client,
        &args.endpoint,
        &args.invokeId,
        &args.body,
    )?;
    aiplatform_projects_locations_publishers_models_invoke_invoke_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Creates a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_create_execute()` to send, or `aiplatform_projects_locations_rag_corpora_create` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1RagCorpus,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Creates a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_create_execute()` or `aiplatform_projects_locations_rag_corpora_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Creates a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RagCorpus,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Creates a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_create_builder()` + `aiplatform_projects_locations_rag_corpora_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_rag_corpora_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Deletes a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_delete_execute()` to send, or `aiplatform_projects_locations_rag_corpora_delete` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Deletes a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_delete_execute()` or `aiplatform_projects_locations_rag_corpora_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Deletes a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Deletes a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_delete_builder()` + `aiplatform_projects_locations_rag_corpora_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_delete_builder(client, &args.name, args.force)?;
    aiplatform_projects_locations_rag_corpora_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Gets a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_get_execute()` to send, or `aiplatform_projects_locations_rag_corpora_get` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Gets a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_get_execute()` or `aiplatform_projects_locations_rag_corpora_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagCorpus>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RagCorpus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Gets a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagCorpus>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Gets a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_get_builder()` + `aiplatform_projects_locations_rag_corpora_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagCorpus>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_get_builder(client, &args.name)?;
    aiplatform_projects_locations_rag_corpora_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Lists RagCorpora in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_list_execute()` to send, or `aiplatform_projects_locations_rag_corpora_list` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Lists RagCorpora in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_list_execute()` or `aiplatform_projects_locations_rag_corpora_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListRagCorporaResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListRagCorporaResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Lists RagCorpora in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListRagCorporaResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora
/// Lists RagCorpora in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_list_builder()` + `aiplatform_projects_locations_rag_corpora_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListRagCorporaResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_rag_corpora_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Updates a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_patch_execute()` to send, or `aiplatform_projects_locations_rag_corpora_patch` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1RagCorpus,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Updates a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_patch_execute()` or `aiplatform_projects_locations_rag_corpora_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Updates a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RagCorpus,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}
/// Updates a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_patch_builder()` + `aiplatform_projects_locations_rag_corpora_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_patch_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_rag_corpora_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_operations_cancel_execute()` to send, or `aiplatform_projects_locations_rag_corpora_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_operations_cancel_execute()` or `aiplatform_projects_locations_rag_corpora_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_operations_cancel_builder()` + `aiplatform_projects_locations_rag_corpora_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_rag_corpora_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_operations_delete_execute()` to send, or `aiplatform_projects_locations_rag_corpora_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_operations_delete_execute()` or `aiplatform_projects_locations_rag_corpora_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_operations_delete_builder()` + `aiplatform_projects_locations_rag_corpora_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_rag_corpora_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_operations_get_execute()` to send, or `aiplatform_projects_locations_rag_corpora_operations_get` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_operations_get_execute()` or `aiplatform_projects_locations_rag_corpora_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_operations_get_builder()` + `aiplatform_projects_locations_rag_corpora_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_rag_corpora_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_operations_list_execute()` to send, or `aiplatform_projects_locations_rag_corpora_operations_list` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_operations_list_execute()` or `aiplatform_projects_locations_rag_corpora_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_operations_list_builder()` + `aiplatform_projects_locations_rag_corpora_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_rag_corpora_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_operations_wait_execute()` to send, or `aiplatform_projects_locations_rag_corpora_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_operations_wait_execute()` or `aiplatform_projects_locations_rag_corpora_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_operations_wait_builder()` + `aiplatform_projects_locations_rag_corpora_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_rag_corpora_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Deletes a RagFile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_delete_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_delete` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    forceDelete: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = forceDelete {
        query_parts.push(format!("forceDelete={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Deletes a RagFile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_delete_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Deletes a RagFile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: forceDelete
    pub forceDelete: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Deletes a RagFile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_delete_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_delete_builder(
        client,
        &args.name,
        args.forceDelete,
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Gets a RagFile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_get_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_get` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Gets a RagFile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_get_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RagFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Gets a RagFile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}
/// Gets a RagFile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_get_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RagFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_corpora_rag_files_get_builder(client, &args.name)?;
    aiplatform_projects_locations_rag_corpora_rag_files_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:import
/// Import files from Google Cloud Storage or Google Drive into a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_import_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_import` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1ImportRagFilesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:import
/// Import files from Google Cloud Storage or Google Drive into a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_import_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:import
/// Import files from Google Cloud Storage or Google Drive into a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_import_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesImportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ImportRagFilesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles:import
/// Import files from Google Cloud Storage or Google Drive into a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_import_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_import_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_import(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_import_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles
/// Lists RagFiles in a RagCorpus.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_list_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_list` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles
/// Lists RagFiles in a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_list_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListRagFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListRagFilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles
/// Lists RagFiles in a RagCorpus.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListRagFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles
/// Lists RagFiles in a RagCorpus.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_list_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListRagFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_operations_get` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_operations_list` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_execute()` to send, or `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragCorpora/{}/ragFiles/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_execute()` or `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_corpora_rag_files_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagCorporaRagFilesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder()` + `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_engine_config_operations_cancel_execute()` to send, or `aiplatform_projects_locations_rag_engine_config_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_rag_engine_config_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_engine_config_operations_cancel_execute()` or `aiplatform_projects_locations_rag_engine_config_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_engine_config_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_engine_config_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_engine_config_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_engine_config_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_engine_config_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagEngineConfigOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_engine_config_operations_cancel_builder()` + `aiplatform_projects_locations_rag_engine_config_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagEngineConfigOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_engine_config_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_rag_engine_config_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_engine_config_operations_delete_execute()` to send, or `aiplatform_projects_locations_rag_engine_config_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_rag_engine_config_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_engine_config_operations_delete_execute()` or `aiplatform_projects_locations_rag_engine_config_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_engine_config_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_engine_config_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_engine_config_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_engine_config_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_engine_config_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagEngineConfigOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_engine_config_operations_delete_builder()` + `aiplatform_projects_locations_rag_engine_config_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagEngineConfigOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_engine_config_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_rag_engine_config_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_engine_config_operations_get_execute()` to send, or `aiplatform_projects_locations_rag_engine_config_operations_get` for simplest API.

pub fn aiplatform_projects_locations_rag_engine_config_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_engine_config_operations_get_execute()` or `aiplatform_projects_locations_rag_engine_config_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_engine_config_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_engine_config_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_engine_config_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_engine_config_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_engine_config_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagEngineConfigOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_engine_config_operations_get_builder()` + `aiplatform_projects_locations_rag_engine_config_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagEngineConfigOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_rag_engine_config_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_rag_engine_config_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_engine_config_operations_list_execute()` to send, or `aiplatform_projects_locations_rag_engine_config_operations_list` for simplest API.

pub fn aiplatform_projects_locations_rag_engine_config_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_engine_config_operations_list_execute()` or `aiplatform_projects_locations_rag_engine_config_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_engine_config_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_engine_config_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_engine_config_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_engine_config_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_engine_config_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagEngineConfigOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_engine_config_operations_list_builder()` + `aiplatform_projects_locations_rag_engine_config_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagEngineConfigOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_engine_config_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_rag_engine_config_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_rag_engine_config_operations_wait_execute()` to send, or `aiplatform_projects_locations_rag_engine_config_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_rag_engine_config_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/ragEngineConfig/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_rag_engine_config_operations_wait_execute()` or `aiplatform_projects_locations_rag_engine_config_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_rag_engine_config_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_rag_engine_config_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_rag_engine_config_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_rag_engine_config_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_rag_engine_config_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_rag_engine_config_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsRagEngineConfigOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_rag_engine_config_operations_wait_builder()` + `aiplatform_projects_locations_rag_engine_config_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_rag_engine_config_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_rag_engine_config_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsRagEngineConfigOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_rag_engine_config_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_rag_engine_config_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Creates a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_create_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_create` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1ReasoningEngine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Creates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_create_execute()` or `aiplatform_projects_locations_reasoning_engines_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Creates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ReasoningEngine,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Creates a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_create_builder()` + `aiplatform_projects_locations_reasoning_engines_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_delete_builder(
        client, &args.name, args.force,
    )?;
    aiplatform_projects_locations_reasoning_engines_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_execute_code_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_execute_code` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_execute_code_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ExecuteCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}:executeCode",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_execute_code_execute()` or `aiplatform_projects_locations_reasoning_engines_execute_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_execute_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_execute_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExecuteCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ExecuteCodeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_execute_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_execute_code_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_execute_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_execute_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_execute_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExecuteCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_execute_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_execute_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesExecuteCodeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExecuteCodeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_execute_code_builder()` + `aiplatform_projects_locations_reasoning_engines_execute_code_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_execute_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_execute_code(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesExecuteCodeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExecuteCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_execute_code_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_execute_code_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_get_execute()` or `aiplatform_projects_locations_reasoning_engines_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReasoningEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReasoningEngine = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReasoningEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_get_builder()` + `aiplatform_projects_locations_reasoning_engines_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReasoningEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_get_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_get_iam_policy_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_get_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_get_iam_policy_execute()` or `aiplatform_projects_locations_reasoning_engines_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_get_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder()` + `aiplatform_projects_locations_reasoning_engines_get_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_get_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    aiplatform_projects_locations_reasoning_engines_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_list_execute()` or `aiplatform_projects_locations_reasoning_engines_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListReasoningEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListReasoningEnginesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListReasoningEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_list_builder()` + `aiplatform_projects_locations_reasoning_engines_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListReasoningEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_patch_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_patch` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1ReasoningEngine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_patch_execute()` or `aiplatform_projects_locations_reasoning_engines_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ReasoningEngine,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_patch_builder()` + `aiplatform_projects_locations_reasoning_engines_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_query_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_query` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1QueryReasoningEngineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_query_execute()` or `aiplatform_projects_locations_reasoning_engines_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryReasoningEngineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1QueryReasoningEngineResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_query_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryReasoningEngineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1QueryReasoningEngineRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_query_builder()` + `aiplatform_projects_locations_reasoning_engines_query_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_query(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryReasoningEngineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_query_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_set_iam_policy_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_set_iam_policy` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_set_iam_policy_execute()` or `aiplatform_projects_locations_reasoning_engines_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_set_iam_policy_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder()` + `aiplatform_projects_locations_reasoning_engines_set_iam_policy_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_set_iam_policy(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_stream_query_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_stream_query` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_stream_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1StreamQueryReasoningEngineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}:streamQuery",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_stream_query_execute()` or `aiplatform_projects_locations_reasoning_engines_stream_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_stream_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_stream_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_stream_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_stream_query_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_stream_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_stream_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_stream_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_stream_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_stream_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesStreamQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamQueryReasoningEngineRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_stream_query_builder()` + `aiplatform_projects_locations_reasoning_engines_stream_query_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_stream_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_stream_query(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesStreamQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_stream_query_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_stream_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_test_iam_permissions` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    permissions: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}:testIamPermissions",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = permissions {
        query_parts.push(format!("permissions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_execute()` or `aiplatform_projects_locations_reasoning_engines_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: permissions
    pub permissions: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder()` + `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder(
        client,
        &args.resource,
        args.permissions.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// Create a Memory.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_create_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_create` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    memoryId: Option<&str>,
    body: &GoogleCloudAiplatformV1Memory,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = memoryId {
        query_parts.push(format!("memoryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// Create a Memory.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_create_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// Create a Memory.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: memoryId
    pub memoryId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Memory,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// Create a Memory.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_create_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_create_builder(
        client,
        &args.parent,
        args.memoryId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Delete a Memory.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Delete a Memory.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Delete a Memory.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Delete a Memory.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:generate
/// Generate memories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_generate_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_generate` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_generate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1GenerateMemoriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories:generate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:generate
/// Generate memories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_generate_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:generate
/// Generate memories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_generate_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesGenerateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateMemoriesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:generate
/// Generate memories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_generate_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_generate_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_generate(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesGenerateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_generate_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_generate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Get a Memory.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Get a Memory.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_get_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Memory>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Memory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Get a Memory.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Memory>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Get a Memory.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_get_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Memory>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_memories_get_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_memories_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// List Memories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// List Memories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_list_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMemoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListMemoriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// List Memories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMemoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories
/// List Memories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_list_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMemoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Update a Memory.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_patch_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_patch` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Memory,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Update a Memory.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_patch_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Update a Memory.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Memory,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}
/// Update a Memory.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_patch_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:purge
/// Purge memories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_purge_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_purge` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1PurgeMemoriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:purge
/// Purge memories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_purge_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:purge
/// Purge memories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_purge_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PurgeMemoriesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:purge
/// Purge memories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_purge_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_purge_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_purge(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_purge_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:retrieve
/// Retrieve memories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_retrieve_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_retrieve` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1RetrieveMemoriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories:retrieve",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:retrieve
/// Retrieve memories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_retrieve_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_retrieve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_retrieve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RetrieveMemoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1RetrieveMemoriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:retrieve
/// Retrieve memories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_retrieve_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_retrieve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_retrieve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RetrieveMemoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_retrieve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_retrieve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesRetrieveArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RetrieveMemoriesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories:retrieve
/// Retrieve memories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_retrieve_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_retrieve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_retrieve(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesRetrieveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1RetrieveMemoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_retrieve_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}:rollback
/// Rollback Memory to a specific revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_rollback_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_rollback` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_rollback_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1RollbackMemoryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}:rollback",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}:rollback
/// Rollback Memory to a specific revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_rollback_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}:rollback
/// Rollback Memory to a specific revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_rollback_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_memories_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesRollbackArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RollbackMemoryRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}:rollback
/// Rollback Memory to a specific revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_rollback_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_rollback_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_rollback(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesRollbackArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_rollback_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_rollback_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_reasoning_engines_memories_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_operations_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_operations_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_operations_get_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_get_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_operations_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_operations_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_operations_list_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_list_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions/{revisionsId}
/// Get a Memory Revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_revisions_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/revisions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions/{revisionsId}
/// Get a Memory Revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_revisions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MemoryRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1MemoryRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions/{revisionsId}
/// Get a Memory Revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MemoryRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_revisions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_revisions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesRevisionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions/{revisionsId}
/// Get a Memory Revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesRevisionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1MemoryRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_revisions_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_revisions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions
/// List Memory Revisions for a Memory.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_memories_revisions_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/memories/{}/revisions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions
/// List Memory Revisions for a Memory.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_execute()` or `aiplatform_projects_locations_reasoning_engines_memories_revisions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMemoryRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListMemoryRevisionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions
/// List Memory Revisions for a Memory.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMemoryRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_memories_revisions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_memories_revisions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesMemoriesRevisionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/revisions
/// List Memory Revisions for a Memory.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_builder()` + `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_memories_revisions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_memories_revisions_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesMemoriesRevisionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListMemoryRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_memories_revisions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_memories_revisions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_operations_cancel_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_operations_cancel_execute()` or `aiplatform_projects_locations_reasoning_engines_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_operations_cancel_builder()` + `aiplatform_projects_locations_reasoning_engines_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_operations_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_operations_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_operations_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_operations_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_operations_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_operations_get_execute()` or `aiplatform_projects_locations_reasoning_engines_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_operations_get_builder()` + `aiplatform_projects_locations_reasoning_engines_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_operations_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_operations_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_operations_list_execute()` or `aiplatform_projects_locations_reasoning_engines_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_operations_list_builder()` + `aiplatform_projects_locations_reasoning_engines_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_reasoning_engines_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_operations_wait_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_operations_wait_execute()` or `aiplatform_projects_locations_reasoning_engines_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_operations_wait_builder()` + `aiplatform_projects_locations_reasoning_engines_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1SandboxEnvironment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SandboxEnvironment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ExecuteSandboxEnvironmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}:execute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsExecuteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExecuteSandboxEnvironmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsExecuteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder(
            client, &args.name, &args.body,
        )?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SandboxEnvironment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SandboxEnvironment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SandboxEnvironment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SandboxEnvironment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_execute()` or `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder()` + `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}:appendEvent
/// Appends an event to a given session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_append_event_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_append_event` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1SessionEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}:appendEvent",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}:appendEvent
/// Appends an event to a given session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_append_event_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_append_event`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_append_event_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AppendEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1AppendEventResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}:appendEvent
/// Appends an event to a given session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_append_event_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_append_event()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_append_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AppendEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_append_event_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_append_event`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsAppendEventArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SessionEvent,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}:appendEvent
/// Appends an event to a given session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_append_event_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_append_event_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_append_event(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsAppendEventArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1AppendEventResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_append_event_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Creates a new Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_create_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_create` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    sessionId: Option<&str>,
    body: &GoogleCloudAiplatformV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sessionId {
        query_parts.push(format!("sessionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Creates a new Session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_create_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Creates a new Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: sessionId
    pub sessionId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Session,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Creates a new Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_create_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_create_builder(
        client,
        &args.parent,
        args.sessionId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Deletes details of the specific Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Deletes details of the specific Session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Deletes details of the specific Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Deletes details of the specific Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Gets details of the specific Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Gets details of the specific Session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_get_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Gets details of the specific Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Gets details of the specific Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_get_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sessions_get_builder(client, &args.name)?;
    aiplatform_projects_locations_reasoning_engines_sessions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Lists Sessions in a given reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Lists Sessions in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_list_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListSessionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Lists Sessions in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions
/// Lists Sessions in a given reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_list_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Updates the specific Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_patch_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_patch` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Updates the specific Session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_patch_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Updates the specific Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Session,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}
/// Updates the specific Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_patch_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/events
/// Lists Events in a given session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_events_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_events_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_events_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}/events",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/events
/// Lists Events in a given session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_events_list_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_events_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_events_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/events
/// Lists Events in a given session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_events_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_events_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_events_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_reasoning_engines_sessions_events_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_events_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsEventsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/events
/// Lists Events in a given session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_events_list_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_events_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_events_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_events_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsEventsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_events_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_events_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_operations_get` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_operations_list` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_execute()` to send, or `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/reasoningEngines/{}/sessions/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_execute()` or `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_reasoning_engines_sessions_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsReasoningEnginesSessionsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder()` + `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Creates a Schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_create_execute()` to send, or `aiplatform_projects_locations_schedules_create` for simplest API.

pub fn aiplatform_projects_locations_schedules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1Schedule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Creates a Schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_create_execute()` or `aiplatform_projects_locations_schedules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Creates a Schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Schedule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Creates a Schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_create_builder()` + `aiplatform_projects_locations_schedules_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_schedules_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_schedules_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Deletes a Schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_delete_execute()` to send, or `aiplatform_projects_locations_schedules_delete` for simplest API.

pub fn aiplatform_projects_locations_schedules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Deletes a Schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_delete_execute()` or `aiplatform_projects_locations_schedules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Deletes a Schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Deletes a Schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_delete_builder()` + `aiplatform_projects_locations_schedules_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_schedules_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_schedules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Gets a Schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_get_execute()` to send, or `aiplatform_projects_locations_schedules_get` for simplest API.

pub fn aiplatform_projects_locations_schedules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Gets a Schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_get_execute()` or `aiplatform_projects_locations_schedules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Gets a Schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Gets a Schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_get_builder()` + `aiplatform_projects_locations_schedules_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_schedules_get_builder(client, &args.name)?;
    aiplatform_projects_locations_schedules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Lists Schedules in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_list_execute()` to send, or `aiplatform_projects_locations_schedules_list` for simplest API.

pub fn aiplatform_projects_locations_schedules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Lists Schedules in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_list_execute()` or `aiplatform_projects_locations_schedules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSchedulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListSchedulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Lists Schedules in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSchedulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules
/// Lists Schedules in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_list_builder()` + `aiplatform_projects_locations_schedules_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSchedulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_schedules_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_schedules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Updates an active or paused Schedule. When the Schedule is updated, new runs will be scheduled starting from the updated next execution time after the update time based on the time_specification in the updated Schedule. All unstarted runs before the update time will be skipped while already created runs will NOT be paused or canceled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_patch_execute()` to send, or `aiplatform_projects_locations_schedules_patch` for simplest API.

pub fn aiplatform_projects_locations_schedules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Schedule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Updates an active or paused Schedule. When the Schedule is updated, new runs will be scheduled starting from the updated next execution time after the update time based on the time_specification in the updated Schedule. All unstarted runs before the update time will be skipped while already created runs will NOT be paused or canceled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_patch_execute()` or `aiplatform_projects_locations_schedules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Schedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Updates an active or paused Schedule. When the Schedule is updated, new runs will be scheduled starting from the updated next execution time after the update time based on the time_specification in the updated Schedule. All unstarted runs before the update time will be skipped while already created runs will NOT be paused or canceled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Schedule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}
/// Updates an active or paused Schedule. When the Schedule is updated, new runs will be scheduled starting from the updated next execution time after the update time based on the time_specification in the updated Schedule. All unstarted runs before the update time will be skipped while already created runs will NOT be paused or canceled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_patch_builder()` + `aiplatform_projects_locations_schedules_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Schedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_schedules_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_schedules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:pause
/// Pauses a Schedule. Will mark Schedule.state to 'PAUSED'. If the schedule is paused, no new runs will be created. Already created runs will NOT be paused or canceled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_pause_execute()` to send, or `aiplatform_projects_locations_schedules_pause` for simplest API.

pub fn aiplatform_projects_locations_schedules_pause_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1PauseScheduleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}:pause",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:pause
/// Pauses a Schedule. Will mark Schedule.state to 'PAUSED'. If the schedule is paused, no new runs will be created. Already created runs will NOT be paused or canceled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_pause_execute()` or `aiplatform_projects_locations_schedules_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:pause
/// Pauses a Schedule. Will mark Schedule.state to 'PAUSED'. If the schedule is paused, no new runs will be created. Already created runs will NOT be paused or canceled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_pause_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesPauseArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PauseScheduleRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:pause
/// Pauses a Schedule. Will mark Schedule.state to 'PAUSED'. If the schedule is paused, no new runs will be created. Already created runs will NOT be paused or canceled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_pause_builder()` + `aiplatform_projects_locations_schedules_pause_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_pause(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_schedules_pause_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_schedules_pause_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:resume
/// Resumes a paused Schedule to start scheduling new runs. Will mark Schedule.state to '`ACTIVE`'. Only paused Schedule can be resumed. When the Schedule is resumed, new runs will be scheduled starting from the next execution time after the current time based on the time_specification in the Schedule. If Schedule.catch_up is set up `true`, all missed runs will be scheduled for backfill first.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_resume_execute()` to send, or `aiplatform_projects_locations_schedules_resume` for simplest API.

pub fn aiplatform_projects_locations_schedules_resume_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ResumeScheduleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}:resume",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:resume
/// Resumes a paused Schedule to start scheduling new runs. Will mark Schedule.state to '`ACTIVE`'. Only paused Schedule can be resumed. When the Schedule is resumed, new runs will be scheduled starting from the next execution time after the current time based on the time_specification in the Schedule. If Schedule.catch_up is set up `true`, all missed runs will be scheduled for backfill first.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_resume_execute()` or `aiplatform_projects_locations_schedules_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:resume
/// Resumes a paused Schedule to start scheduling new runs. Will mark Schedule.state to '`ACTIVE`'. Only paused Schedule can be resumed. When the Schedule is resumed, new runs will be scheduled starting from the next execution time after the current time based on the time_specification in the Schedule. If Schedule.catch_up is set up `true`, all missed runs will be scheduled for backfill first.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_resume_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesResumeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ResumeScheduleRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}:resume
/// Resumes a paused Schedule to start scheduling new runs. Will mark Schedule.state to '`ACTIVE`'. Only paused Schedule can be resumed. When the Schedule is resumed, new runs will be scheduled starting from the next execution time after the current time based on the time_specification in the Schedule. If Schedule.catch_up is set up `true`, all missed runs will be scheduled for backfill first.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_resume_builder()` + `aiplatform_projects_locations_schedules_resume_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_resume(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_schedules_resume_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_schedules_resume_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_operations_cancel_execute()` to send, or `aiplatform_projects_locations_schedules_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_schedules_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_operations_cancel_execute()` or `aiplatform_projects_locations_schedules_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_operations_cancel_builder()` + `aiplatform_projects_locations_schedules_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_schedules_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_schedules_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_operations_delete_execute()` to send, or `aiplatform_projects_locations_schedules_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_schedules_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_operations_delete_execute()` or `aiplatform_projects_locations_schedules_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_operations_delete_builder()` + `aiplatform_projects_locations_schedules_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_schedules_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_schedules_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_operations_get_execute()` to send, or `aiplatform_projects_locations_schedules_operations_get` for simplest API.

pub fn aiplatform_projects_locations_schedules_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_operations_get_execute()` or `aiplatform_projects_locations_schedules_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_operations_get_builder()` + `aiplatform_projects_locations_schedules_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_schedules_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_schedules_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_operations_list_execute()` to send, or `aiplatform_projects_locations_schedules_operations_list` for simplest API.

pub fn aiplatform_projects_locations_schedules_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_operations_list_execute()` or `aiplatform_projects_locations_schedules_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_operations_list_builder()` + `aiplatform_projects_locations_schedules_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_schedules_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_schedules_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_schedules_operations_wait_execute()` to send, or `aiplatform_projects_locations_schedules_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_schedules_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/schedules/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_schedules_operations_wait_execute()` or `aiplatform_projects_locations_schedules_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_schedules_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_schedules_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_schedules_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_schedules_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_schedules_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_schedules_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSchedulesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_schedules_operations_wait_builder()` + `aiplatform_projects_locations_schedules_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_schedules_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_schedules_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSchedulesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_schedules_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_schedules_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Creates a SpecialistPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_create_execute()` to send, or `aiplatform_projects_locations_specialist_pools_create` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1SpecialistPool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Creates a SpecialistPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_create_execute()` or `aiplatform_projects_locations_specialist_pools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Creates a SpecialistPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SpecialistPool,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Creates a SpecialistPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_create_builder()` + `aiplatform_projects_locations_specialist_pools_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_specialist_pools_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Deletes a SpecialistPool as well as all Specialists in the pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_delete_execute()` to send, or `aiplatform_projects_locations_specialist_pools_delete` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Deletes a SpecialistPool as well as all Specialists in the pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_delete_execute()` or `aiplatform_projects_locations_specialist_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Deletes a SpecialistPool as well as all Specialists in the pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Deletes a SpecialistPool as well as all Specialists in the pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_delete_builder()` + `aiplatform_projects_locations_specialist_pools_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_delete_builder(
        client, &args.name, args.force,
    )?;
    aiplatform_projects_locations_specialist_pools_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Gets a SpecialistPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_get_execute()` to send, or `aiplatform_projects_locations_specialist_pools_get` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Gets a SpecialistPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_get_execute()` or `aiplatform_projects_locations_specialist_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SpecialistPool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SpecialistPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Gets a SpecialistPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SpecialistPool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Gets a SpecialistPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_get_builder()` + `aiplatform_projects_locations_specialist_pools_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SpecialistPool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_get_builder(client, &args.name)?;
    aiplatform_projects_locations_specialist_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Lists SpecialistPools in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_list_execute()` to send, or `aiplatform_projects_locations_specialist_pools_list` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Lists SpecialistPools in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_list_execute()` or `aiplatform_projects_locations_specialist_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSpecialistPoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListSpecialistPoolsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Lists SpecialistPools in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSpecialistPoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools
/// Lists SpecialistPools in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_list_builder()` + `aiplatform_projects_locations_specialist_pools_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListSpecialistPoolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_specialist_pools_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Updates a SpecialistPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_patch_execute()` to send, or `aiplatform_projects_locations_specialist_pools_patch` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1SpecialistPool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Updates a SpecialistPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_patch_execute()` or `aiplatform_projects_locations_specialist_pools_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Updates a SpecialistPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SpecialistPool,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}
/// Updates a SpecialistPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_patch_builder()` + `aiplatform_projects_locations_specialist_pools_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_specialist_pools_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_operations_cancel_execute()` to send, or `aiplatform_projects_locations_specialist_pools_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_operations_cancel_execute()` or `aiplatform_projects_locations_specialist_pools_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_operations_cancel_builder()` + `aiplatform_projects_locations_specialist_pools_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_specialist_pools_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_operations_delete_execute()` to send, or `aiplatform_projects_locations_specialist_pools_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_operations_delete_execute()` or `aiplatform_projects_locations_specialist_pools_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_operations_delete_builder()` + `aiplatform_projects_locations_specialist_pools_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_specialist_pools_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_operations_get_execute()` to send, or `aiplatform_projects_locations_specialist_pools_operations_get` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_operations_get_execute()` or `aiplatform_projects_locations_specialist_pools_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_operations_get_builder()` + `aiplatform_projects_locations_specialist_pools_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_specialist_pools_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_specialist_pools_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_operations_list_execute()` to send, or `aiplatform_projects_locations_specialist_pools_operations_list` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_operations_list_execute()` or `aiplatform_projects_locations_specialist_pools_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_operations_list_builder()` + `aiplatform_projects_locations_specialist_pools_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_specialist_pools_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_specialist_pools_operations_wait_execute()` to send, or `aiplatform_projects_locations_specialist_pools_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_specialist_pools_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/specialistPools/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_specialist_pools_operations_wait_execute()` or `aiplatform_projects_locations_specialist_pools_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_specialist_pools_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_specialist_pools_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_specialist_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_specialist_pools_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_specialist_pools_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_specialist_pools_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsSpecialistPoolsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_specialist_pools_operations_wait_builder()` + `aiplatform_projects_locations_specialist_pools_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_specialist_pools_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_specialist_pools_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsSpecialistPoolsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_specialist_pools_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_specialist_pools_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Creates a Study. A resource name will be generated after creation of the Study.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_create_execute()` to send, or `aiplatform_projects_locations_studies_create` for simplest API.

pub fn aiplatform_projects_locations_studies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1Study,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Creates a Study. A resource name will be generated after creation of the Study.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_create_execute()` or `aiplatform_projects_locations_studies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Study = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Creates a Study. A resource name will be generated after creation of the Study.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Study,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Creates a Study. A resource name will be generated after creation of the Study.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_create_builder()` + `aiplatform_projects_locations_studies_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_studies_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Deletes a Study.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_delete_execute()` to send, or `aiplatform_projects_locations_studies_delete` for simplest API.

pub fn aiplatform_projects_locations_studies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Deletes a Study.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_delete_execute()` or `aiplatform_projects_locations_studies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Deletes a Study.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Deletes a Study.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_delete_builder()` + `aiplatform_projects_locations_studies_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Gets a Study by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_get_execute()` to send, or `aiplatform_projects_locations_studies_get` for simplest API.

pub fn aiplatform_projects_locations_studies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Gets a Study by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_get_execute()` or `aiplatform_projects_locations_studies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Study = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Gets a Study by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}
/// Gets a Study by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_get_builder()` + `aiplatform_projects_locations_studies_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_get_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Lists all the studies in a region for an associated project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_list_execute()` to send, or `aiplatform_projects_locations_studies_list` for simplest API.

pub fn aiplatform_projects_locations_studies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Lists all the studies in a region for an associated project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_list_execute()` or `aiplatform_projects_locations_studies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListStudiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListStudiesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Lists all the studies in a region for an associated project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListStudiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies
/// Lists all the studies in a region for an associated project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_list_builder()` + `aiplatform_projects_locations_studies_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListStudiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_studies_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies:lookup
/// Looks a study up using the user-defined display_name field instead of the fully qualified resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_lookup_execute()` to send, or `aiplatform_projects_locations_studies_lookup` for simplest API.

pub fn aiplatform_projects_locations_studies_lookup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1LookupStudyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies:lookup",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies:lookup
/// Looks a study up using the user-defined display_name field instead of the fully qualified resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_lookup_execute()` or `aiplatform_projects_locations_studies_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Study = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies:lookup
/// Looks a study up using the user-defined display_name field instead of the fully qualified resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_lookup_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesLookupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1LookupStudyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies:lookup
/// Looks a study up using the user-defined display_name field instead of the fully qualified resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_lookup_builder()` + `aiplatform_projects_locations_studies_lookup_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_lookup(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesLookupArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Study>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_lookup_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_studies_lookup_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_operations_cancel_execute()` to send, or `aiplatform_projects_locations_studies_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_studies_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_operations_cancel_execute()` or `aiplatform_projects_locations_studies_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_operations_cancel_builder()` + `aiplatform_projects_locations_studies_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_operations_delete_execute()` to send, or `aiplatform_projects_locations_studies_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_studies_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_operations_delete_execute()` or `aiplatform_projects_locations_studies_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_operations_delete_builder()` + `aiplatform_projects_locations_studies_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_operations_get_execute()` to send, or `aiplatform_projects_locations_studies_operations_get` for simplest API.

pub fn aiplatform_projects_locations_studies_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_operations_get_execute()` or `aiplatform_projects_locations_studies_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_operations_get_builder()` + `aiplatform_projects_locations_studies_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_operations_list_execute()` to send, or `aiplatform_projects_locations_studies_operations_list` for simplest API.

pub fn aiplatform_projects_locations_studies_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_operations_list_execute()` or `aiplatform_projects_locations_studies_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_operations_list_builder()` + `aiplatform_projects_locations_studies_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_studies_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_operations_wait_execute()` to send, or `aiplatform_projects_locations_studies_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_studies_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_operations_wait_execute()` or `aiplatform_projects_locations_studies_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_operations_wait_builder()` + `aiplatform_projects_locations_studies_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_studies_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:addTrialMeasurement
/// Adds a measurement of the objective metrics to a Trial. This measurement is assumed to have been taken before the Trial is complete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_add_trial_measurement_execute()` to send, or `aiplatform_projects_locations_studies_trials_add_trial_measurement` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_add_trial_measurement_builder(
    client: &SimpleHttpClient,
    trialName: &str,
    body: &GoogleCloudAiplatformV1AddTrialMeasurementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}:addTrialMeasurement",
        trialName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:addTrialMeasurement
/// Adds a measurement of the objective metrics to a Trial. This measurement is assumed to have been taken before the Trial is complete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_add_trial_measurement_execute()` or `aiplatform_projects_locations_studies_trials_add_trial_measurement`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_add_trial_measurement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_add_trial_measurement_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Trial = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:addTrialMeasurement
/// Adds a measurement of the objective metrics to a Trial. This measurement is assumed to have been taken before the Trial is complete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_add_trial_measurement_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_add_trial_measurement_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_add_trial_measurement()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_add_trial_measurement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_add_trial_measurement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_add_trial_measurement_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_add_trial_measurement`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsAddTrialMeasurementArgs {
    /// Path parameter: trialName
    pub trialName: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1AddTrialMeasurementRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:addTrialMeasurement
/// Adds a measurement of the objective metrics to a Trial. This measurement is assumed to have been taken before the Trial is complete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_add_trial_measurement_builder()` + `aiplatform_projects_locations_studies_trials_add_trial_measurement_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_add_trial_measurement_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_add_trial_measurement(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsAddTrialMeasurementArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_add_trial_measurement_builder(
        client,
        &args.trialName,
        &args.body,
    )?;
    aiplatform_projects_locations_studies_trials_add_trial_measurement_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:checkTrialEarlyStoppingState
/// Checks whether a Trial should stop or not. Returns a long-running operation. When the operation is successful, it will contain a CheckTrialEarlyStoppingStateResponse.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_execute()` to send, or `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder(
    client: &SimpleHttpClient,
    trialName: &str,
    body: &GoogleCloudAiplatformV1CheckTrialEarlyStoppingStateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}:checkTrialEarlyStoppingState",
        trialName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:checkTrialEarlyStoppingState
/// Checks whether a Trial should stop or not. Returns a long-running operation. When the operation is successful, it will contain a CheckTrialEarlyStoppingStateResponse.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_execute()` or `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:checkTrialEarlyStoppingState
/// Checks whether a Trial should stop or not. Returns a long-running operation. When the operation is successful, it will contain a CheckTrialEarlyStoppingStateResponse.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsCheckTrialEarlyStoppingStateArgs {
    /// Path parameter: trialName
    pub trialName: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CheckTrialEarlyStoppingStateRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:checkTrialEarlyStoppingState
/// Checks whether a Trial should stop or not. Returns a long-running operation. When the operation is successful, it will contain a CheckTrialEarlyStoppingStateResponse.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder()` + `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsCheckTrialEarlyStoppingStateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder(
            client,
            &args.trialName,
            &args.body,
        )?;
    aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:complete
/// Marks a Trial as complete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_complete_execute()` to send, or `aiplatform_projects_locations_studies_trials_complete` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_complete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CompleteTrialRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}:complete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:complete
/// Marks a Trial as complete.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_complete_execute()` or `aiplatform_projects_locations_studies_trials_complete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_complete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_complete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Trial = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:complete
/// Marks a Trial as complete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_complete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_complete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_complete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_complete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_complete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_complete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_complete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsCompleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CompleteTrialRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:complete
/// Marks a Trial as complete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_complete_builder()` + `aiplatform_projects_locations_studies_trials_complete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_complete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_complete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsCompleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_complete_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_studies_trials_complete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Adds a user provided Trial to a Study.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_create_execute()` to send, or `aiplatform_projects_locations_studies_trials_create` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1Trial,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Adds a user provided Trial to a Study.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_create_execute()` or `aiplatform_projects_locations_studies_trials_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Trial = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Adds a user provided Trial to a Study.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Trial,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Adds a user provided Trial to a Study.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_create_builder()` + `aiplatform_projects_locations_studies_trials_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_studies_trials_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Deletes a Trial.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_delete_execute()` to send, or `aiplatform_projects_locations_studies_trials_delete` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Deletes a Trial.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_delete_execute()` or `aiplatform_projects_locations_studies_trials_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Deletes a Trial.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Deletes a Trial.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_delete_builder()` + `aiplatform_projects_locations_studies_trials_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_trials_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Gets a Trial.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_get_execute()` to send, or `aiplatform_projects_locations_studies_trials_get` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Gets a Trial.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_get_execute()` or `aiplatform_projects_locations_studies_trials_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Trial = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Gets a Trial.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}
/// Gets a Trial.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_get_builder()` + `aiplatform_projects_locations_studies_trials_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_get_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_trials_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Lists the Trials associated with a Study.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_list_execute()` to send, or `aiplatform_projects_locations_studies_trials_list` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Lists the Trials associated with a Study.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_list_execute()` or `aiplatform_projects_locations_studies_trials_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTrialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTrialsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Lists the Trials associated with a Study.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTrialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials
/// Lists the Trials associated with a Study.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_list_builder()` + `aiplatform_projects_locations_studies_trials_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTrialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_studies_trials_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:listOptimalTrials
/// Lists the pareto-optimal Trials for multi-objective Study or the optimal Trials for single-objective Study. The definition of pareto-optimal can be checked in wiki page. <https://en.wikipedia.`org/wiki/Pareto_efficiency`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_list_optimal_trials_execute()` to send, or `aiplatform_projects_locations_studies_trials_list_optimal_trials` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_list_optimal_trials_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1ListOptimalTrialsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials:listOptimalTrials",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:listOptimalTrials
/// Lists the pareto-optimal Trials for multi-objective Study or the optimal Trials for single-objective Study. The definition of pareto-optimal can be checked in wiki page. <https://en.wikipedia.`org/wiki/Pareto_efficiency`>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_list_optimal_trials_execute()` or `aiplatform_projects_locations_studies_trials_list_optimal_trials`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_list_optimal_trials_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_list_optimal_trials_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListOptimalTrialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListOptimalTrialsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:listOptimalTrials
/// Lists the pareto-optimal Trials for multi-objective Study or the optimal Trials for single-objective Study. The definition of pareto-optimal can be checked in wiki page. <https://en.wikipedia.`org/wiki/Pareto_efficiency`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_list_optimal_trials_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_list_optimal_trials_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_list_optimal_trials()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_list_optimal_trials_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_list_optimal_trials_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListOptimalTrialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_list_optimal_trials_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_list_optimal_trials`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsListOptimalTrialsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ListOptimalTrialsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:listOptimalTrials
/// Lists the pareto-optimal Trials for multi-objective Study or the optimal Trials for single-objective Study. The definition of pareto-optimal can be checked in wiki page. <https://en.wikipedia.`org/wiki/Pareto_efficiency`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_list_optimal_trials_builder()` + `aiplatform_projects_locations_studies_trials_list_optimal_trials_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_list_optimal_trials_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_list_optimal_trials(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsListOptimalTrialsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListOptimalTrialsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_list_optimal_trials_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_studies_trials_list_optimal_trials_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:stop
/// Stops a Trial.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_stop_execute()` to send, or `aiplatform_projects_locations_studies_trials_stop` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_stop_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1StopTrialRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}:stop",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:stop
/// Stops a Trial.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_stop_execute()` or `aiplatform_projects_locations_studies_trials_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Trial = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:stop
/// Stops a Trial.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_stop_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsStopArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StopTrialRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}:stop
/// Stops a Trial.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_stop_builder()` + `aiplatform_projects_locations_studies_trials_stop_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_stop(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsStopArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Trial>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_trials_stop_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_studies_trials_stop_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:suggest
/// Adds one or more Trials to a Study, with parameter values suggested by Vertex AI Vizier. Returns a long-running operation associated with the generation of Trial suggestions. When this long-running operation succeeds, it will contain a SuggestTrialsResponse.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_suggest_execute()` to send, or `aiplatform_projects_locations_studies_trials_suggest` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_suggest_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1SuggestTrialsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials:suggest",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:suggest
/// Adds one or more Trials to a Study, with parameter values suggested by Vertex AI Vizier. Returns a long-running operation associated with the generation of Trial suggestions. When this long-running operation succeeds, it will contain a SuggestTrialsResponse.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_suggest_execute()` or `aiplatform_projects_locations_studies_trials_suggest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_suggest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_suggest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:suggest
/// Adds one or more Trials to a Study, with parameter values suggested by Vertex AI Vizier. Returns a long-running operation associated with the generation of Trial suggestions. When this long-running operation succeeds, it will contain a SuggestTrialsResponse.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_suggest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_suggest_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_suggest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_suggest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_suggest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_suggest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_suggest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsSuggestArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SuggestTrialsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials:suggest
/// Adds one or more Trials to a Study, with parameter values suggested by Vertex AI Vizier. Returns a long-running operation associated with the generation of Trial suggestions. When this long-running operation succeeds, it will contain a SuggestTrialsResponse.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_suggest_builder()` + `aiplatform_projects_locations_studies_trials_suggest_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_suggest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_suggest(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsSuggestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_suggest_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_studies_trials_suggest_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_operations_cancel_execute()` to send, or `aiplatform_projects_locations_studies_trials_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_operations_cancel_execute()` or `aiplatform_projects_locations_studies_trials_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_operations_cancel_builder()` + `aiplatform_projects_locations_studies_trials_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_trials_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_trials_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_operations_delete_execute()` to send, or `aiplatform_projects_locations_studies_trials_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_operations_delete_execute()` or `aiplatform_projects_locations_studies_trials_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_operations_delete_builder()` + `aiplatform_projects_locations_studies_trials_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_trials_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_trials_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_operations_get_execute()` to send, or `aiplatform_projects_locations_studies_trials_operations_get` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_operations_get_execute()` or `aiplatform_projects_locations_studies_trials_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_operations_get_builder()` + `aiplatform_projects_locations_studies_trials_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_studies_trials_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_studies_trials_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_operations_list_execute()` to send, or `aiplatform_projects_locations_studies_trials_operations_list` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_operations_list_execute()` or `aiplatform_projects_locations_studies_trials_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_operations_list_builder()` + `aiplatform_projects_locations_studies_trials_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_studies_trials_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_studies_trials_operations_wait_execute()` to send, or `aiplatform_projects_locations_studies_trials_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_studies_trials_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/studies/{}/trials/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_studies_trials_operations_wait_execute()` or `aiplatform_projects_locations_studies_trials_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_studies_trials_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_studies_trials_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_studies_trials_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_studies_trials_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_studies_trials_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_studies_trials_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsStudiesTrialsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_studies_trials_operations_wait_builder()` + `aiplatform_projects_locations_studies_trials_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_studies_trials_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_studies_trials_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsStudiesTrialsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_studies_trials_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_studies_trials_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:batchRead
/// Reads multiple TensorboardTimeSeries' data. The data point number limit is 1000 for scalars, 100 for tensors and blob references. If the number of data points stored is less than the limit, all data is returned. Otherwise, the number limit of data points is randomly selected from this time series and returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_batch_read_execute()` to send, or `aiplatform_projects_locations_tensorboards_batch_read` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_batch_read_builder(
    client: &SimpleHttpClient,
    tensorboard: &str,
    timeSeries: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}:batchRead",
        tensorboard,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeSeries {
        query_parts.push(format!("timeSeries={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:batchRead
/// Reads multiple TensorboardTimeSeries' data. The data point number limit is 1000 for scalars, 100 for tensors and blob references. If the number of data points stored is less than the limit, all data is returned. Otherwise, the number limit of data points is randomly selected from this time series and returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_batch_read_execute()` or `aiplatform_projects_locations_tensorboards_batch_read`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_batch_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_batch_read_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchReadTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchReadTensorboardTimeSeriesDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:batchRead
/// Reads multiple TensorboardTimeSeries' data. The data point number limit is 1000 for scalars, 100 for tensors and blob references. If the number of data points stored is less than the limit, all data is returned. Otherwise, the number limit of data points is randomly selected from this time series and returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_batch_read_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_batch_read_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_batch_read()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_batch_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_batch_read_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchReadTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_batch_read_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_batch_read`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsBatchReadArgs {
    /// Path parameter: tensorboard
    pub tensorboard: String,
    /// Query parameter: timeSeries
    pub timeSeries: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:batchRead
/// Reads multiple TensorboardTimeSeries' data. The data point number limit is 1000 for scalars, 100 for tensors and blob references. If the number of data points stored is less than the limit, all data is returned. Otherwise, the number limit of data points is randomly selected from this time series and returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_batch_read_builder()` + `aiplatform_projects_locations_tensorboards_batch_read_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_batch_read_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_batch_read(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsBatchReadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchReadTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_batch_read_builder(
        client,
        &args.tensorboard,
        args.timeSeries.as_deref(),
    )?;
    aiplatform_projects_locations_tensorboards_batch_read_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Creates a Tensorboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_create_execute()` to send, or `aiplatform_projects_locations_tensorboards_create` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1Tensorboard,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Creates a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_create_execute()` or `aiplatform_projects_locations_tensorboards_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Creates a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Tensorboard,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Creates a Tensorboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_create_builder()` + `aiplatform_projects_locations_tensorboards_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Deletes a Tensorboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Deletes a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_delete_execute()` or `aiplatform_projects_locations_tensorboards_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Deletes a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Deletes a Tensorboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_delete_builder()` + `aiplatform_projects_locations_tensorboards_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Gets a Tensorboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Gets a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_get_execute()` or `aiplatform_projects_locations_tensorboards_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Tensorboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1Tensorboard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Gets a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Tensorboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Gets a Tensorboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_get_builder()` + `aiplatform_projects_locations_tensorboards_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1Tensorboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_get_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Lists Tensorboards in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Lists Tensorboards in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_list_execute()` or `aiplatform_projects_locations_tensorboards_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTensorboardsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTensorboardsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Lists Tensorboards in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTensorboardsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards
/// Lists Tensorboards in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_list_builder()` + `aiplatform_projects_locations_tensorboards_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTensorboardsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_tensorboards_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Updates a Tensorboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_patch_execute()` to send, or `aiplatform_projects_locations_tensorboards_patch` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1Tensorboard,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Updates a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_patch_execute()` or `aiplatform_projects_locations_tensorboards_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Updates a Tensorboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1Tensorboard,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}
/// Updates a Tensorboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_patch_builder()` + `aiplatform_projects_locations_tensorboards_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readSize
/// Returns the storage size for a given TensorBoard instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_read_size_execute()` to send, or `aiplatform_projects_locations_tensorboards_read_size` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_read_size_builder(
    client: &SimpleHttpClient,
    tensorboard: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}:readSize",
        tensorboard,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readSize
/// Returns the storage size for a given TensorBoard instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_read_size_execute()` or `aiplatform_projects_locations_tensorboards_read_size`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_read_size_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_read_size_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadTensorboardSizeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadTensorboardSizeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readSize
/// Returns the storage size for a given TensorBoard instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_read_size_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_read_size_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_read_size()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_read_size_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_read_size_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadTensorboardSizeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_read_size_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_read_size`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsReadSizeArgs {
    /// Path parameter: tensorboard
    pub tensorboard: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readSize
/// Returns the storage size for a given TensorBoard instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_read_size_builder()` + `aiplatform_projects_locations_tensorboards_read_size_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_read_size_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_read_size(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsReadSizeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadTensorboardSizeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_read_size_builder(client, &args.tensorboard)?;
    aiplatform_projects_locations_tensorboards_read_size_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readUsage
/// Returns a list of monthly active users for a given TensorBoard instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_read_usage_execute()` to send, or `aiplatform_projects_locations_tensorboards_read_usage` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_read_usage_builder(
    client: &SimpleHttpClient,
    tensorboard: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}:readUsage",
        tensorboard,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readUsage
/// Returns a list of monthly active users for a given TensorBoard instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_read_usage_execute()` or `aiplatform_projects_locations_tensorboards_read_usage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_read_usage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_read_usage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadTensorboardUsageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadTensorboardUsageResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readUsage
/// Returns a list of monthly active users for a given TensorBoard instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_read_usage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_read_usage_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_read_usage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_read_usage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_read_usage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadTensorboardUsageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_read_usage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_read_usage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsReadUsageArgs {
    /// Path parameter: tensorboard
    pub tensorboard: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}:readUsage
/// Returns a list of monthly active users for a given TensorBoard instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_read_usage_builder()` + `aiplatform_projects_locations_tensorboards_read_usage_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_read_usage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_read_usage(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsReadUsageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReadTensorboardUsageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_read_usage_builder(client, &args.tensorboard)?;
    aiplatform_projects_locations_tensorboards_read_usage_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:batchCreate
/// Batch create TensorboardTimeSeries that belong to a TensorboardExperiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_batch_create_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_batch_create` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:batchCreate
/// Batch create TensorboardTimeSeries that belong to a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_batch_create_execute()` or `aiplatform_projects_locations_tensorboards_experiments_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:batchCreate
/// Batch create TensorboardTimeSeries that belong to a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_batch_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:batchCreate
/// Batch create TensorboardTimeSeries that belong to a TensorboardExperiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_batch_create_builder()` + `aiplatform_projects_locations_tensorboards_experiments_batch_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_batch_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_batch_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Creates a TensorboardExperiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_create_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_create` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    tensorboardExperimentId: Option<&str>,
    body: &GoogleCloudAiplatformV1TensorboardExperiment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tensorboardExperimentId {
        query_parts.push(format!("tensorboardExperimentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Creates a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_create_execute()` or `aiplatform_projects_locations_tensorboards_experiments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardExperiment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Creates a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: tensorboardExperimentId
    pub tensorboardExperimentId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TensorboardExperiment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Creates a TensorboardExperiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_create_builder()` + `aiplatform_projects_locations_tensorboards_experiments_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_create_builder(
        client,
        &args.parent,
        args.tensorboardExperimentId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Deletes a TensorboardExperiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Deletes a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_delete_execute()` or `aiplatform_projects_locations_tensorboards_experiments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Deletes a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Deletes a TensorboardExperiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_delete_builder()` + `aiplatform_projects_locations_tensorboards_experiments_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_experiments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Gets a TensorboardExperiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Gets a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_get_execute()` or `aiplatform_projects_locations_tensorboards_experiments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardExperiment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Gets a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Gets a TensorboardExperiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_get_builder()` + `aiplatform_projects_locations_tensorboards_experiments_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_get_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_experiments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Lists TensorboardExperiments in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Lists TensorboardExperiments in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_list_execute()` or `aiplatform_projects_locations_tensorboards_experiments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListTensorboardExperimentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTensorboardExperimentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Lists TensorboardExperiments in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListTensorboardExperimentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments
/// Lists TensorboardExperiments in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_list_builder()` + `aiplatform_projects_locations_tensorboards_experiments_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListTensorboardExperimentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_tensorboards_experiments_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Updates a TensorboardExperiment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_patch_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_patch` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1TensorboardExperiment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Updates a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_patch_execute()` or `aiplatform_projects_locations_tensorboards_experiments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardExperiment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Updates a TensorboardExperiment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TensorboardExperiment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}
/// Updates a TensorboardExperiment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_patch_builder()` + `aiplatform_projects_locations_tensorboards_experiments_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardExperiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:write
/// Write time series data points of multiple TensorboardTimeSeries in multiple TensorboardRun's. If any data fail to be ingested, an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_write_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_write` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_write_builder(
    client: &SimpleHttpClient,
    tensorboardExperiment: &str,
    body: &GoogleCloudAiplatformV1WriteTensorboardExperimentDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}:write",
        tensorboardExperiment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:write
/// Write time series data points of multiple TensorboardTimeSeries in multiple TensorboardRun's. If any data fail to be ingested, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_write_execute()` or `aiplatform_projects_locations_tensorboards_experiments_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:write
/// Write time series data points of multiple TensorboardTimeSeries in multiple TensorboardRun's. If any data fail to be ingested, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_write_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsWriteArgs {
    /// Path parameter: tensorboardExperiment
    pub tensorboardExperiment: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1WriteTensorboardExperimentDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}:write
/// Write time series data points of multiple TensorboardTimeSeries in multiple TensorboardRun's. If any data fail to be ingested, an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_write_builder()` + `aiplatform_projects_locations_tensorboards_experiments_write_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_write(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_write_builder(
        client,
        &args.tensorboardExperiment,
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_execute()` or `aiplatform_projects_locations_tensorboards_experiments_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder()` + `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_operations_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_operations_delete_execute()` or `aiplatform_projects_locations_tensorboards_experiments_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder()` + `aiplatform_projects_locations_tensorboards_experiments_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_operations_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_operations_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_operations_get_execute()` or `aiplatform_projects_locations_tensorboards_experiments_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_get_builder()` + `aiplatform_projects_locations_tensorboards_experiments_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_operations_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_operations_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_operations_list_execute()` or `aiplatform_projects_locations_tensorboards_experiments_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_list_builder()` + `aiplatform_projects_locations_tensorboards_experiments_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_operations_wait_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_operations_wait_execute()` or `aiplatform_projects_locations_tensorboards_experiments_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder()` + `aiplatform_projects_locations_tensorboards_experiments_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_tensorboards_experiments_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs:batchCreate
/// Batch create TensorboardRuns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1BatchCreateTensorboardRunsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs:batchCreate
/// Batch create TensorboardRuns.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs:batchCreate
/// Batch create TensorboardRuns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1BatchCreateTensorboardRunsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs:batchCreate
/// Batch create TensorboardRuns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_batch_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Creates a TensorboardRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_create_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_create` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    tensorboardRunId: Option<&str>,
    body: &GoogleCloudAiplatformV1TensorboardRun,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tensorboardRunId {
        query_parts.push(format!("tensorboardRunId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Creates a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_create_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Creates a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: tensorboardRunId
    pub tensorboardRunId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TensorboardRun,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Creates a TensorboardRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_create_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_create_builder(
        client,
        &args.parent,
        args.tensorboardRunId.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Deletes a TensorboardRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Deletes a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_delete_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Deletes a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Deletes a TensorboardRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Gets a TensorboardRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Gets a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_get_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Gets a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Gets a TensorboardRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_get_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Lists TensorboardRuns in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Lists TensorboardRuns in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_list_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTensorboardRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTensorboardRunsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Lists TensorboardRuns in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTensorboardRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs
/// Lists TensorboardRuns in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_list_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTensorboardRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Updates a TensorboardRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_patch_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_patch` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1TensorboardRun,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Updates a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_patch_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Updates a TensorboardRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TensorboardRun,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}
/// Updates a TensorboardRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardRun>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}:write
/// Write time series data points into multiple TensorboardTimeSeries under a TensorboardRun. If any data fail to be ingested, an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_write_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_write` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_write_builder(
    client: &SimpleHttpClient,
    tensorboardRun: &str,
    body: &GoogleCloudAiplatformV1WriteTensorboardRunDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}:write",
        tensorboardRun,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}:write
/// Write time series data points into multiple TensorboardTimeSeries under a TensorboardRun. If any data fail to be ingested, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_write_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1WriteTensorboardRunDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1WriteTensorboardRunDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}:write
/// Write time series data points into multiple TensorboardTimeSeries under a TensorboardRun. If any data fail to be ingested, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_write_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1WriteTensorboardRunDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsWriteArgs {
    /// Path parameter: tensorboardRun
    pub tensorboardRun: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1WriteTensorboardRunDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}:write
/// Write time series data points into multiple TensorboardTimeSeries under a TensorboardRun. If any data fail to be ingested, an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_write_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_write_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_write(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1WriteTensorboardRunDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_write_builder(
        client,
        &args.tensorboardRun,
        &args.body,
    )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_builder(
            client,
            &args.name,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.returnPartialSuccess,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder(
            client,
            &args.name,
            args.timeout.as_deref(),
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Creates a TensorboardTimeSeries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    tensorboardTimeSeriesId: Option<&str>,
    body: &GoogleCloudAiplatformV1TensorboardTimeSeries,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tensorboardTimeSeriesId {
        query_parts.push(format!("tensorboardTimeSeriesId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Creates a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardTimeSeries =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Creates a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: tensorboardTimeSeriesId
    pub tensorboardTimeSeriesId: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TensorboardTimeSeries,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Creates a TensorboardTimeSeries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder(
            client,
            &args.parent,
            args.tensorboardTimeSeriesId.as_deref(),
            &args.body,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Deletes a TensorboardTimeSeries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Deletes a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Deletes a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Deletes a TensorboardTimeSeries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:exportTensorboardTimeSeries
/// Exports a TensorboardTimeSeries' data. Data is returned in paginated responses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder(
    client: &SimpleHttpClient,
    tensorboardTimeSeries: &str,
    body: &GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}:exportTensorboardTimeSeries",
        tensorboardTimeSeries,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:exportTensorboardTimeSeries
/// Exports a TensorboardTimeSeries' data. Data is returned in paginated responses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:exportTensorboardTimeSeries
/// Exports a TensorboardTimeSeries' data. Data is returned in paginated responses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesExportTensorboardTimeSeriesArgs
{
    /// Path parameter: tensorboardTimeSeries
    pub tensorboardTimeSeries: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:exportTensorboardTimeSeries
/// Exports a TensorboardTimeSeries' data. Data is returned in paginated responses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesExportTensorboardTimeSeriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder(client, &args.tensorboardTimeSeries, &args.body)?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Gets a TensorboardTimeSeries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Gets a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardTimeSeries =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Gets a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Gets a TensorboardTimeSeries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_builder(
            client, &args.name,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Lists TensorboardTimeSeries in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Lists TensorboardTimeSeries in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListTensorboardTimeSeriesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTensorboardTimeSeriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Lists TensorboardTimeSeries in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListTensorboardTimeSeriesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries
/// Lists TensorboardTimeSeries in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListTensorboardTimeSeriesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.orderBy.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.readMask.as_deref(),
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Updates a TensorboardTimeSeries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1TensorboardTimeSeries,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Updates a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TensorboardTimeSeries =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Updates a TensorboardTimeSeries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TensorboardTimeSeries,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}
/// Updates a TensorboardTimeSeries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TensorboardTimeSeries>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder(
            client,
            &args.name,
            args.updateMask.as_deref(),
            &args.body,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:read
/// Reads a TensorboardTimeSeries' data. By default, if the number of data points stored is less than 1000, all data is returned. Otherwise, 1000 data points is randomly selected from this time series and returned. This value can be changed by changing max_data_points, which can't be greater than 10k.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_builder(
    client: &SimpleHttpClient,
    tensorboardTimeSeries: &str,
    filter: Option<&str>,
    maxDataPoints: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}:read",
        tensorboardTimeSeries,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxDataPoints {
        query_parts.push(format!("maxDataPoints={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:read
/// Reads a TensorboardTimeSeries' data. By default, if the number of data points stored is less than 1000, all data is returned. Otherwise, 1000 data points is randomly selected from this time series and returned. This value can be changed by changing max_data_points, which can't be greater than 10k.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ReadTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadTensorboardTimeSeriesDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:read
/// Reads a TensorboardTimeSeries' data. By default, if the number of data points stored is less than 1000, all data is returned. Otherwise, 1000 data points is randomly selected from this time series and returned. This value can be changed by changing max_data_points, which can't be greater than 10k.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ReadTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesReadArgs {
    /// Path parameter: tensorboardTimeSeries
    pub tensorboardTimeSeries: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxDataPoints
    pub maxDataPoints: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:read
/// Reads a TensorboardTimeSeries' data. By default, if the number of data points stored is less than 1000, all data is returned. Otherwise, 1000 data points is randomly selected from this time series and returned. This value can be changed by changing max_data_points, which can't be greater than 10k.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesReadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ReadTensorboardTimeSeriesDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_builder(
            client,
            &args.tensorboardTimeSeries,
            args.filter.as_deref(),
            args.maxDataPoints,
        )?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:readBlobData
/// Gets bytes of TensorboardBlobs. This is to allow reading blob data stored in consumer project's Cloud Storage bucket without users having to obtain Cloud Storage access permission.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_builder(
    client: &SimpleHttpClient,
    timeSeries: &str,
    blobIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}:readBlobData",
        timeSeries,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = blobIds {
        query_parts.push(format!("blobIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:readBlobData
/// Gets bytes of TensorboardBlobs. This is to allow reading blob data stored in consumer project's Cloud Storage bucket without users having to obtain Cloud Storage access permission.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ReadTensorboardBlobDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReadTensorboardBlobDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:readBlobData
/// Gets bytes of TensorboardBlobs. This is to allow reading blob data stored in consumer project's Cloud Storage bucket without users having to obtain Cloud Storage access permission.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ReadTensorboardBlobDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesReadBlobDataArgs {
    /// Path parameter: timeSeries
    pub timeSeries: String,
    /// Query parameter: blobIds
    pub blobIds: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}:readBlobData
/// Gets bytes of TensorboardBlobs. This is to allow reading blob data stored in consumer project's Cloud Storage bucket without users having to obtain Cloud Storage access permission.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesReadBlobDataArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ReadTensorboardBlobDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_builder(client, &args.timeSeries, args.blobIds.as_deref())?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_read_blob_data_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_builder(client, &args.name, args.filter.as_deref(), args.pageSize, args.pageToken.as_deref(), args.returnPartialSuccess)?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_execute()` to send, or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_execute()` or `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder()` + `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_operations_cancel_execute()` to send, or `aiplatform_projects_locations_tensorboards_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_operations_cancel_execute()` or `aiplatform_projects_locations_tensorboards_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_operations_cancel_builder()` + `aiplatform_projects_locations_tensorboards_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_operations_delete_execute()` to send, or `aiplatform_projects_locations_tensorboards_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_operations_delete_execute()` or `aiplatform_projects_locations_tensorboards_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_operations_delete_builder()` + `aiplatform_projects_locations_tensorboards_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_operations_get_execute()` to send, or `aiplatform_projects_locations_tensorboards_operations_get` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_operations_get_execute()` or `aiplatform_projects_locations_tensorboards_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_operations_get_builder()` + `aiplatform_projects_locations_tensorboards_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tensorboards_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_tensorboards_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_operations_list_execute()` to send, or `aiplatform_projects_locations_tensorboards_operations_list` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_operations_list_execute()` or `aiplatform_projects_locations_tensorboards_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_operations_list_builder()` + `aiplatform_projects_locations_tensorboards_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_tensorboards_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tensorboards_operations_wait_execute()` to send, or `aiplatform_projects_locations_tensorboards_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_tensorboards_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tensorboards/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tensorboards_operations_wait_execute()` or `aiplatform_projects_locations_tensorboards_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tensorboards_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tensorboards_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tensorboards_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tensorboards_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tensorboards_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tensorboards_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTensorboardsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tensorboards_operations_wait_builder()` + `aiplatform_projects_locations_tensorboards_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tensorboards_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tensorboards_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTensorboardsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tensorboards_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_tensorboards_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}:cancel
/// Cancels a TrainingPipeline. Starts asynchronous cancellation on the TrainingPipeline. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetTrainingPipeline or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the TrainingPipeline is not deleted; instead it becomes a pipeline with a TrainingPipeline.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and TrainingPipeline.state is set to CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_cancel_execute()` to send, or `aiplatform_projects_locations_training_pipelines_cancel` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelTrainingPipelineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}:cancel
/// Cancels a TrainingPipeline. Starts asynchronous cancellation on the TrainingPipeline. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetTrainingPipeline or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the TrainingPipeline is not deleted; instead it becomes a pipeline with a TrainingPipeline.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and TrainingPipeline.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_cancel_execute()` or `aiplatform_projects_locations_training_pipelines_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}:cancel
/// Cancels a TrainingPipeline. Starts asynchronous cancellation on the TrainingPipeline. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetTrainingPipeline or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the TrainingPipeline is not deleted; instead it becomes a pipeline with a TrainingPipeline.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and TrainingPipeline.state is set to CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelTrainingPipelineRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}:cancel
/// Cancels a TrainingPipeline. Starts asynchronous cancellation on the TrainingPipeline. The server makes a best effort to cancel the pipeline, but success is not guaranteed. Clients can use PipelineService.GetTrainingPipeline or other methods to check whether the cancellation succeeded or whether the pipeline completed despite cancellation. On successful cancellation, the TrainingPipeline is not deleted; instead it becomes a pipeline with a TrainingPipeline.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED, and TrainingPipeline.state is set to CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_cancel_builder()` + `aiplatform_projects_locations_training_pipelines_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_cancel_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_projects_locations_training_pipelines_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Creates a TrainingPipeline. A created TrainingPipeline right away will be attempted to be run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_create_execute()` to send, or `aiplatform_projects_locations_training_pipelines_create` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1TrainingPipeline,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Creates a TrainingPipeline. A created TrainingPipeline right away will be attempted to be run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_create_execute()` or `aiplatform_projects_locations_training_pipelines_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TrainingPipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TrainingPipeline =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Creates a TrainingPipeline. A created TrainingPipeline right away will be attempted to be run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TrainingPipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TrainingPipeline,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Creates a TrainingPipeline. A created TrainingPipeline right away will be attempted to be run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_create_builder()` + `aiplatform_projects_locations_training_pipelines_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TrainingPipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_training_pipelines_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Deletes a TrainingPipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_delete_execute()` to send, or `aiplatform_projects_locations_training_pipelines_delete` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Deletes a TrainingPipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_delete_execute()` or `aiplatform_projects_locations_training_pipelines_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Deletes a TrainingPipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Deletes a TrainingPipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_delete_builder()` + `aiplatform_projects_locations_training_pipelines_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_training_pipelines_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_training_pipelines_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Gets a TrainingPipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_get_execute()` to send, or `aiplatform_projects_locations_training_pipelines_get` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Gets a TrainingPipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_get_execute()` or `aiplatform_projects_locations_training_pipelines_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TrainingPipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TrainingPipeline =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Gets a TrainingPipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TrainingPipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}
/// Gets a TrainingPipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_get_builder()` + `aiplatform_projects_locations_training_pipelines_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TrainingPipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_get_builder(client, &args.name)?;
    aiplatform_projects_locations_training_pipelines_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Lists TrainingPipelines in a Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_list_execute()` to send, or `aiplatform_projects_locations_training_pipelines_list` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Lists TrainingPipelines in a Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_list_execute()` or `aiplatform_projects_locations_training_pipelines_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTrainingPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTrainingPipelinesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Lists TrainingPipelines in a Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTrainingPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines
/// Lists TrainingPipelines in a Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_list_builder()` + `aiplatform_projects_locations_training_pipelines_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTrainingPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readMask.as_deref(),
    )?;
    aiplatform_projects_locations_training_pipelines_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_operations_cancel_execute()` to send, or `aiplatform_projects_locations_training_pipelines_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_operations_cancel_execute()` or `aiplatform_projects_locations_training_pipelines_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_operations_cancel_builder()` + `aiplatform_projects_locations_training_pipelines_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_training_pipelines_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_operations_delete_execute()` to send, or `aiplatform_projects_locations_training_pipelines_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_operations_delete_execute()` or `aiplatform_projects_locations_training_pipelines_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_operations_delete_builder()` + `aiplatform_projects_locations_training_pipelines_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_training_pipelines_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_operations_get_execute()` to send, or `aiplatform_projects_locations_training_pipelines_operations_get` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_operations_get_execute()` or `aiplatform_projects_locations_training_pipelines_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_operations_get_builder()` + `aiplatform_projects_locations_training_pipelines_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_projects_locations_training_pipelines_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_operations_list_execute()` to send, or `aiplatform_projects_locations_training_pipelines_operations_list` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_operations_list_execute()` or `aiplatform_projects_locations_training_pipelines_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_operations_list_builder()` + `aiplatform_projects_locations_training_pipelines_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_training_pipelines_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_training_pipelines_operations_wait_execute()` to send, or `aiplatform_projects_locations_training_pipelines_operations_wait` for simplest API.

pub fn aiplatform_projects_locations_training_pipelines_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/trainingPipelines/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_training_pipelines_operations_wait_execute()` or `aiplatform_projects_locations_training_pipelines_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_training_pipelines_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_wait_task()`.
/// For the simplest API, use `aiplatform_projects_locations_training_pipelines_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_training_pipelines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_training_pipelines_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_training_pipelines_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_training_pipelines_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTrainingPipelinesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_training_pipelines_operations_wait_builder()` + `aiplatform_projects_locations_training_pipelines_operations_wait_execute()`.
/// For task-level control, use `aiplatform_projects_locations_training_pipelines_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_training_pipelines_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTrainingPipelinesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_training_pipelines_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_projects_locations_training_pipelines_operations_wait_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}:cancel
/// Cancels a tuning job. Starts an asynchronous cancellation request. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use GenAiTuningService.GetTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the tuning job is not deleted. Instead, its state is set to CANCELLED, and error is set to a status with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_cancel_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_cancel` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1CancelTuningJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}:cancel
/// Cancels a tuning job. Starts an asynchronous cancellation request. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use GenAiTuningService.GetTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the tuning job is not deleted. Instead, its state is set to CANCELLED, and error is set to a status with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_cancel_execute()` or `aiplatform_projects_locations_tuning_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}:cancel
/// Cancels a tuning job. Starts an asynchronous cancellation request. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use GenAiTuningService.GetTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the tuning job is not deleted. Instead, its state is set to CANCELLED, and error is set to a status with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CancelTuningJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}:cancel
/// Cancels a tuning job. Starts an asynchronous cancellation request. The server makes a best effort to cancel the job, but success is not guaranteed. Clients can use GenAiTuningService.GetTuningJob or other methods to check whether the cancellation succeeded or whether the job completed despite cancellation. On successful cancellation, the tuning job is not deleted. Instead, its state is set to CANCELLED, and error is set to a status with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_cancel_builder()` + `aiplatform_projects_locations_tuning_jobs_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tuning_jobs_cancel_builder(client, &args.name, &args.body)?;
    aiplatform_projects_locations_tuning_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Creates a tuning job. A created tuning job will be subsequently executed to start the model tuning process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_create_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_create` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1TuningJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Creates a tuning job. A created tuning job will be subsequently executed to start the model tuning process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_create_execute()` or `aiplatform_projects_locations_tuning_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TuningJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Creates a tuning job. A created tuning job will be subsequently executed to start the model tuning process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_create_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1TuningJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Creates a tuning job. A created tuning job will be subsequently executed to start the model tuning process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_create_builder()` + `aiplatform_projects_locations_tuning_jobs_create_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_create(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tuning_jobs_create_builder(client, &args.parent, &args.body)?;
    aiplatform_projects_locations_tuning_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}
/// Gets a tuning job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_get_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_get` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}
/// Gets a tuning job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_get_execute()` or `aiplatform_projects_locations_tuning_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1TuningJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}
/// Gets a tuning job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}
/// Gets a tuning job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_get_builder()` + `aiplatform_projects_locations_tuning_jobs_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1TuningJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tuning_jobs_get_builder(client, &args.name)?;
    aiplatform_projects_locations_tuning_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Lists tuning jobs in a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_list_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_list` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Lists tuning jobs in a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_list_execute()` or `aiplatform_projects_locations_tuning_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTuningJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListTuningJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Lists tuning jobs in a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTuningJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs
/// Lists tuning jobs in a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_list_builder()` + `aiplatform_projects_locations_tuning_jobs_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListTuningJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tuning_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_projects_locations_tuning_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs:rebaseTunedModel
/// Rebase a tuned model. A rebase operation takes a model that was previously tuned on a base model version, and retunes it on a new base model version. The rebase operation creates a new tuning job and a new tuned model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1RebaseTunedModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs:rebaseTunedModel",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs:rebaseTunedModel
/// Rebase a tuned model. A rebase operation takes a model that was previously tuned on a base model version, and retunes it on a new base model version. The rebase operation creates a new tuning job and a new tuned model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_execute()` or `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs:rebaseTunedModel
/// Rebase a tuned model. A rebase operation takes a model that was previously tuned on a base model version, and retunes it on a new base model version. The rebase operation creates a new tuning job and a new tuned model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_rebase_tuned_model`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsRebaseTunedModelArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1RebaseTunedModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs:rebaseTunedModel
/// Rebase a tuned model. A rebase operation takes a model that was previously tuned on a base model version, and retunes it on a new base model version. The rebase operation creates a new tuning job and a new tuned model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder()` + `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_rebase_tuned_model(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsRebaseTunedModelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_operations_cancel_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_operations_cancel` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_operations_cancel_execute()` or `aiplatform_projects_locations_tuning_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_operations_cancel_builder()` + `aiplatform_projects_locations_tuning_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tuning_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_projects_locations_tuning_jobs_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_operations_delete_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_operations_delete` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_operations_delete_execute()` or `aiplatform_projects_locations_tuning_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_operations_delete_builder()` + `aiplatform_projects_locations_tuning_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tuning_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_projects_locations_tuning_jobs_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_operations_get_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_operations_get` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_operations_get_execute()` or `aiplatform_projects_locations_tuning_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_operations_get_builder()` + `aiplatform_projects_locations_tuning_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_projects_locations_tuning_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_projects_locations_tuning_jobs_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_projects_locations_tuning_jobs_operations_list_execute()` to send, or `aiplatform_projects_locations_tuning_jobs_operations_list` for simplest API.

pub fn aiplatform_projects_locations_tuning_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/projects/{}/locations/{}/tuningJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_projects_locations_tuning_jobs_operations_list_execute()` or `aiplatform_projects_locations_tuning_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_projects_locations_tuning_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_projects_locations_tuning_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_projects_locations_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_projects_locations_tuning_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_projects_locations_tuning_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_projects_locations_tuning_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformProjectsLocationsTuningJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_projects_locations_tuning_jobs_operations_list_builder()` + `aiplatform_projects_locations_tuning_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_projects_locations_tuning_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_projects_locations_tuning_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformProjectsLocationsTuningJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_projects_locations_tuning_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_projects_locations_tuning_jobs_operations_list_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_compute_tokens_execute()` to send, or `aiplatform_publishers_models_compute_tokens` for simplest API.

pub fn aiplatform_publishers_models_compute_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1ComputeTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:computeTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_compute_tokens_execute()` or `aiplatform_publishers_models_compute_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_compute_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ComputeTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_compute_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_compute_tokens_task()`.
/// For the simplest API, use `aiplatform_publishers_models_compute_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_compute_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_compute_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_compute_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_compute_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsComputeTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ComputeTokensRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:computeTokens
/// Return a list of tokens based on the input text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_compute_tokens_builder()` + `aiplatform_publishers_models_compute_tokens_execute()`.
/// For task-level control, use `aiplatform_publishers_models_compute_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_compute_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsComputeTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ComputeTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_publishers_models_compute_tokens_builder(client, &args.endpoint, &args.body)?;
    aiplatform_publishers_models_compute_tokens_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_count_tokens_execute()` to send, or `aiplatform_publishers_models_count_tokens` for simplest API.

pub fn aiplatform_publishers_models_count_tokens_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1CountTokensRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:countTokens",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_count_tokens_execute()` or `aiplatform_publishers_models_count_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_count_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1CountTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_count_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_count_tokens_task()`.
/// For the simplest API, use `aiplatform_publishers_models_count_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_count_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_count_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_count_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_count_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsCountTokensArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1CountTokensRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:countTokens
/// Perform a token counting.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_count_tokens_builder()` + `aiplatform_publishers_models_count_tokens_execute()`.
/// For task-level control, use `aiplatform_publishers_models_count_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_count_tokens(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsCountTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1CountTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_publishers_models_count_tokens_builder(client, &args.endpoint, &args.body)?;
    aiplatform_publishers_models_count_tokens_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_fetch_predict_operation_execute()` to send, or `aiplatform_publishers_models_fetch_predict_operation` for simplest API.

pub fn aiplatform_publishers_models_fetch_predict_operation_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1FetchPredictOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:fetchPredictOperation",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_fetch_predict_operation_execute()` or `aiplatform_publishers_models_fetch_predict_operation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_fetch_predict_operation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_fetch_predict_operation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_fetch_predict_operation_task()`.
/// For the simplest API, use `aiplatform_publishers_models_fetch_predict_operation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_fetch_predict_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_fetch_predict_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_fetch_predict_operation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_fetch_predict_operation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsFetchPredictOperationArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1FetchPredictOperationRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:fetchPredictOperation
/// Fetch an asynchronous online prediction operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_fetch_predict_operation_builder()` + `aiplatform_publishers_models_fetch_predict_operation_execute()`.
/// For task-level control, use `aiplatform_publishers_models_fetch_predict_operation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_fetch_predict_operation(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsFetchPredictOperationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_publishers_models_fetch_predict_operation_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_publishers_models_fetch_predict_operation_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_generate_content_execute()` to send, or `aiplatform_publishers_models_generate_content` for simplest API.

pub fn aiplatform_publishers_models_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:generateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_generate_content_execute()` or `aiplatform_publishers_models_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_generate_content_task()`.
/// For the simplest API, use `aiplatform_publishers_models_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:generateContent
/// Generate content with multimodal inputs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_generate_content_builder()` + `aiplatform_publishers_models_generate_content_execute()`.
/// For task-level control, use `aiplatform_publishers_models_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_publishers_models_generate_content_builder(client, &args.model, &args.body)?;
    aiplatform_publishers_models_generate_content_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}
/// Gets a Model Garden publisher model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_get_execute()` to send, or `aiplatform_publishers_models_get` for simplest API.

pub fn aiplatform_publishers_models_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    huggingFaceToken: Option<&str>,
    isHuggingFaceModel: Option<bool>,
    languageCode: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = huggingFaceToken {
        query_parts.push(format!("huggingFaceToken={}", val));
    }
    if let Some(val) = isHuggingFaceModel {
        query_parts.push(format!("isHuggingFaceModel={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}
/// Gets a Model Garden publisher model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_get_execute()` or `aiplatform_publishers_models_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PublisherModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PublisherModel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}
/// Gets a Model Garden publisher model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_get_task()`.
/// For the simplest API, use `aiplatform_publishers_models_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PublisherModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: huggingFaceToken
    pub huggingFaceToken: Option<String>,
    /// Query parameter: isHuggingFaceModel
    pub isHuggingFaceModel: Option<bool>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}
/// Gets a Model Garden publisher model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_get_builder()` + `aiplatform_publishers_models_get_execute()`.
/// For task-level control, use `aiplatform_publishers_models_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_get(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PublisherModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_publishers_models_get_builder(
        client,
        &args.name,
        args.huggingFaceToken.as_deref(),
        args.isHuggingFaceModel,
        args.languageCode.as_deref(),
        args.view.as_deref(),
    )?;
    aiplatform_publishers_models_get_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_predict_execute()` to send, or `aiplatform_publishers_models_predict` for simplest API.

pub fn aiplatform_publishers_models_predict_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:predict",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_predict_execute()` or `aiplatform_publishers_models_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1PredictResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_predict_task()`.
/// For the simplest API, use `aiplatform_publishers_models_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsPredictArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predict
/// Perform an online prediction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_predict_builder()` + `aiplatform_publishers_models_predict_execute()`.
/// For task-level control, use `aiplatform_publishers_models_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_predict(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1PredictResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_publishers_models_predict_builder(client, &args.endpoint, &args.body)?;
    aiplatform_publishers_models_predict_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_predict_long_running_execute()` to send, or `aiplatform_publishers_models_predict_long_running` for simplest API.

pub fn aiplatform_publishers_models_predict_long_running_builder(
    client: &SimpleHttpClient,
    endpoint: &str,
    body: &GoogleCloudAiplatformV1PredictLongRunningRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:predictLongRunning",
        endpoint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_predict_long_running_execute()` or `aiplatform_publishers_models_predict_long_running`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_predict_long_running_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_predict_long_running_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_predict_long_running_task()`.
/// For the simplest API, use `aiplatform_publishers_models_predict_long_running()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_predict_long_running_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_predict_long_running_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_predict_long_running_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_predict_long_running`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsPredictLongRunningArgs {
    /// Path parameter: endpoint
    pub endpoint: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1PredictLongRunningRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:predictLongRunning
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_predict_long_running_builder()` + `aiplatform_publishers_models_predict_long_running_execute()`.
/// For task-level control, use `aiplatform_publishers_models_predict_long_running_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_predict_long_running(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsPredictLongRunningArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_publishers_models_predict_long_running_builder(
        client,
        &args.endpoint,
        &args.body,
    )?;
    aiplatform_publishers_models_predict_long_running_execute(builder)
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_publishers_models_stream_generate_content_execute()` to send, or `aiplatform_publishers_models_stream_generate_content` for simplest API.

pub fn aiplatform_publishers_models_stream_generate_content_builder(
    client: &SimpleHttpClient,
    model: &str,
    body: &GoogleCloudAiplatformV1GenerateContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/publishers/{}/models/{}:streamGenerateContent",
        model,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_publishers_models_stream_generate_content_execute()` or `aiplatform_publishers_models_stream_generate_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_stream_generate_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_publishers_models_stream_generate_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_publishers_models_stream_generate_content_task()`.
/// For the simplest API, use `aiplatform_publishers_models_stream_generate_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_publishers_models_stream_generate_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_publishers_models_stream_generate_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_publishers_models_stream_generate_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_publishers_models_stream_generate_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformPublishersModelsStreamGenerateContentArgs {
    /// Path parameter: model
    pub model: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateContentRequest,
}

/// GET v1/publishers/{publishersId}/models/{modelsId}:streamGenerateContent
/// Generate content with multimodal inputs with streaming support.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_publishers_models_stream_generate_content_builder()` + `aiplatform_publishers_models_stream_generate_content_execute()`.
/// For task-level control, use `aiplatform_publishers_models_stream_generate_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_publishers_models_stream_generate_content(
    client: &SimpleHttpClient,
    args: &AiplatformPublishersModelsStreamGenerateContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1GenerateContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_publishers_models_stream_generate_content_builder(
        client,
        &args.model,
        &args.body,
    )?;
    aiplatform_publishers_models_stream_generate_content_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_operations_cancel_execute()` to send, or `aiplatform_rag_corpora_operations_cancel` for simplest API.

pub fn aiplatform_rag_corpora_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_operations_cancel_execute()` or `aiplatform_rag_corpora_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_operations_cancel_builder()` + `aiplatform_rag_corpora_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_operations_cancel_builder(client, &args.name)?;
    aiplatform_rag_corpora_operations_cancel_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_operations_delete_execute()` to send, or `aiplatform_rag_corpora_operations_delete` for simplest API.

pub fn aiplatform_rag_corpora_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_operations_delete_execute()` or `aiplatform_rag_corpora_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_operations_delete_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_operations_delete_builder()` + `aiplatform_rag_corpora_operations_delete_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_operations_delete_builder(client, &args.name)?;
    aiplatform_rag_corpora_operations_delete_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_operations_get_execute()` to send, or `aiplatform_rag_corpora_operations_get` for simplest API.

pub fn aiplatform_rag_corpora_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_operations_get_execute()` or `aiplatform_rag_corpora_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_operations_get_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_operations_get_builder()` + `aiplatform_rag_corpora_operations_get_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_operations_get_builder(client, &args.name)?;
    aiplatform_rag_corpora_operations_get_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_operations_list_execute()` to send, or `aiplatform_rag_corpora_operations_list` for simplest API.

pub fn aiplatform_rag_corpora_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_operations_list_execute()` or `aiplatform_rag_corpora_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_operations_list_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/ragCorpora/{ragCorporaId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_operations_list_builder()` + `aiplatform_rag_corpora_operations_list_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_rag_corpora_operations_list_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_operations_wait_execute()` to send, or `aiplatform_rag_corpora_operations_wait` for simplest API.

pub fn aiplatform_rag_corpora_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_operations_wait_execute()` or `aiplatform_rag_corpora_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_operations_wait_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/ragCorpora/{ragCorporaId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_operations_wait_builder()` + `aiplatform_rag_corpora_operations_wait_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_rag_corpora_operations_wait_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_rag_files_operations_cancel_execute()` to send, or `aiplatform_rag_corpora_rag_files_operations_cancel` for simplest API.

pub fn aiplatform_rag_corpora_rag_files_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/ragFiles/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_rag_files_operations_cancel_execute()` or `aiplatform_rag_corpora_rag_files_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_rag_files_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_rag_files_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_rag_files_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_rag_files_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_rag_files_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaRagFilesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_rag_files_operations_cancel_builder()` + `aiplatform_rag_corpora_rag_files_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaRagFilesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_rag_files_operations_cancel_builder(client, &args.name)?;
    aiplatform_rag_corpora_rag_files_operations_cancel_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_rag_files_operations_delete_execute()` to send, or `aiplatform_rag_corpora_rag_files_operations_delete` for simplest API.

pub fn aiplatform_rag_corpora_rag_files_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/ragFiles/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_rag_files_operations_delete_execute()` or `aiplatform_rag_corpora_rag_files_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_rag_files_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_delete_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_rag_files_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_rag_files_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_rag_files_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_rag_files_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaRagFilesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_rag_files_operations_delete_builder()` + `aiplatform_rag_corpora_rag_files_operations_delete_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaRagFilesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_rag_files_operations_delete_builder(client, &args.name)?;
    aiplatform_rag_corpora_rag_files_operations_delete_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_rag_files_operations_get_execute()` to send, or `aiplatform_rag_corpora_rag_files_operations_get` for simplest API.

pub fn aiplatform_rag_corpora_rag_files_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/ragFiles/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_rag_files_operations_get_execute()` or `aiplatform_rag_corpora_rag_files_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_rag_files_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_get_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_rag_files_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_rag_files_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_rag_files_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_rag_files_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaRagFilesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_rag_files_operations_get_builder()` + `aiplatform_rag_corpora_rag_files_operations_get_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaRagFilesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_rag_files_operations_get_builder(client, &args.name)?;
    aiplatform_rag_corpora_rag_files_operations_get_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_rag_files_operations_list_execute()` to send, or `aiplatform_rag_corpora_rag_files_operations_list` for simplest API.

pub fn aiplatform_rag_corpora_rag_files_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/ragFiles/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_rag_files_operations_list_execute()` or `aiplatform_rag_corpora_rag_files_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_rag_files_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_list_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_rag_files_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_rag_files_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_rag_files_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_rag_files_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaRagFilesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_rag_files_operations_list_builder()` + `aiplatform_rag_corpora_rag_files_operations_list_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaRagFilesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_rag_files_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_rag_corpora_rag_files_operations_list_execute(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_corpora_rag_files_operations_wait_execute()` to send, or `aiplatform_rag_corpora_rag_files_operations_wait` for simplest API.

pub fn aiplatform_rag_corpora_rag_files_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragCorpora/{}/ragFiles/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_corpora_rag_files_operations_wait_execute()` or `aiplatform_rag_corpora_rag_files_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_corpora_rag_files_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_wait_task()`.
/// For the simplest API, use `aiplatform_rag_corpora_rag_files_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_corpora_rag_files_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_corpora_rag_files_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_corpora_rag_files_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_corpora_rag_files_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagCorporaRagFilesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/ragCorpora/{ragCorporaId}/ragFiles/{ragFilesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_corpora_rag_files_operations_wait_builder()` + `aiplatform_rag_corpora_rag_files_operations_wait_execute()`.
/// For task-level control, use `aiplatform_rag_corpora_rag_files_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_corpora_rag_files_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformRagCorporaRagFilesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_corpora_rag_files_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_rag_corpora_rag_files_operations_wait_execute(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_engine_config_operations_cancel_execute()` to send, or `aiplatform_rag_engine_config_operations_cancel` for simplest API.

pub fn aiplatform_rag_engine_config_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragEngineConfig/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_engine_config_operations_cancel_execute()` or `aiplatform_rag_engine_config_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_engine_config_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_engine_config_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_rag_engine_config_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_engine_config_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_engine_config_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_engine_config_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagEngineConfigOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragEngineConfig/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_engine_config_operations_cancel_builder()` + `aiplatform_rag_engine_config_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_rag_engine_config_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformRagEngineConfigOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_engine_config_operations_cancel_builder(client, &args.name)?;
    aiplatform_rag_engine_config_operations_cancel_execute(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_engine_config_operations_delete_execute()` to send, or `aiplatform_rag_engine_config_operations_delete` for simplest API.

pub fn aiplatform_rag_engine_config_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragEngineConfig/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_engine_config_operations_delete_execute()` or `aiplatform_rag_engine_config_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_engine_config_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_engine_config_operations_delete_task()`.
/// For the simplest API, use `aiplatform_rag_engine_config_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_engine_config_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_engine_config_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_engine_config_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagEngineConfigOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_engine_config_operations_delete_builder()` + `aiplatform_rag_engine_config_operations_delete_execute()`.
/// For task-level control, use `aiplatform_rag_engine_config_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformRagEngineConfigOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_engine_config_operations_delete_builder(client, &args.name)?;
    aiplatform_rag_engine_config_operations_delete_execute(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_engine_config_operations_get_execute()` to send, or `aiplatform_rag_engine_config_operations_get` for simplest API.

pub fn aiplatform_rag_engine_config_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragEngineConfig/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_engine_config_operations_get_execute()` or `aiplatform_rag_engine_config_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_engine_config_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_engine_config_operations_get_task()`.
/// For the simplest API, use `aiplatform_rag_engine_config_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_engine_config_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_engine_config_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_engine_config_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagEngineConfigOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/ragEngineConfig/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_engine_config_operations_get_builder()` + `aiplatform_rag_engine_config_operations_get_execute()`.
/// For task-level control, use `aiplatform_rag_engine_config_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformRagEngineConfigOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_engine_config_operations_get_builder(client, &args.name)?;
    aiplatform_rag_engine_config_operations_get_execute(builder)
}

/// GET v1/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_engine_config_operations_list_execute()` to send, or `aiplatform_rag_engine_config_operations_list` for simplest API.

pub fn aiplatform_rag_engine_config_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragEngineConfig/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_engine_config_operations_list_execute()` or `aiplatform_rag_engine_config_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_engine_config_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_engine_config_operations_list_task()`.
/// For the simplest API, use `aiplatform_rag_engine_config_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_engine_config_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_engine_config_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_engine_config_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagEngineConfigOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/ragEngineConfig/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_engine_config_operations_list_builder()` + `aiplatform_rag_engine_config_operations_list_execute()`.
/// For task-level control, use `aiplatform_rag_engine_config_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformRagEngineConfigOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_engine_config_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_rag_engine_config_operations_list_execute(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_rag_engine_config_operations_wait_execute()` to send, or `aiplatform_rag_engine_config_operations_wait` for simplest API.

pub fn aiplatform_rag_engine_config_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/ragEngineConfig/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_rag_engine_config_operations_wait_execute()` or `aiplatform_rag_engine_config_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_rag_engine_config_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_rag_engine_config_operations_wait_task()`.
/// For the simplest API, use `aiplatform_rag_engine_config_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_rag_engine_config_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_rag_engine_config_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_rag_engine_config_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_rag_engine_config_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformRagEngineConfigOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/ragEngineConfig/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_rag_engine_config_operations_wait_builder()` + `aiplatform_rag_engine_config_operations_wait_execute()`.
/// For task-level control, use `aiplatform_rag_engine_config_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_rag_engine_config_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformRagEngineConfigOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_rag_engine_config_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_rag_engine_config_operations_wait_execute(builder)
}

/// GET v1/reasoningEngines
/// Creates a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_create_execute()` to send, or `aiplatform_reasoning_engines_create` for simplest API.

pub fn aiplatform_reasoning_engines_create_builder(
    client: &SimpleHttpClient,
    parent: Option<&str>,
    body: &GoogleCloudAiplatformV1ReasoningEngine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/reasoningEngines",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines
/// Creates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_create_execute()` or `aiplatform_reasoning_engines_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines
/// Creates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_create_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesCreateArgs {
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ReasoningEngine,
}

/// GET v1/reasoningEngines
/// Creates a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_create_builder()` + `aiplatform_reasoning_engines_create_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_create(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_create_builder(client, args.parent.as_deref(), &args.body)?;
    aiplatform_reasoning_engines_create_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_delete_execute()` to send, or `aiplatform_reasoning_engines_delete` for simplest API.

pub fn aiplatform_reasoning_engines_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_delete_execute()` or `aiplatform_reasoning_engines_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_delete_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Deletes a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_delete_builder()` + `aiplatform_reasoning_engines_delete_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_delete(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_delete_builder(client, &args.name, args.force)?;
    aiplatform_reasoning_engines_delete_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_execute_code_execute()` to send, or `aiplatform_reasoning_engines_execute_code` for simplest API.

pub fn aiplatform_reasoning_engines_execute_code_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ExecuteCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}:executeCode",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_execute_code_execute()` or `aiplatform_reasoning_engines_execute_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_execute_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_execute_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExecuteCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ExecuteCodeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_execute_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_execute_code_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_execute_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_execute_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_execute_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExecuteCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_execute_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_execute_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesExecuteCodeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExecuteCodeRequest,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:executeCode
/// Executes code statelessly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_execute_code_builder()` + `aiplatform_reasoning_engines_execute_code_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_execute_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_execute_code(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesExecuteCodeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ExecuteCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_execute_code_builder(client, &args.name, &args.body)?;
    aiplatform_reasoning_engines_execute_code_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_get_execute()` to send, or `aiplatform_reasoning_engines_get` for simplest API.

pub fn aiplatform_reasoning_engines_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_get_execute()` or `aiplatform_reasoning_engines_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReasoningEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ReasoningEngine = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_get_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReasoningEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Gets a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_get_builder()` + `aiplatform_reasoning_engines_get_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_get(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ReasoningEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_get_builder(client, &args.name)?;
    aiplatform_reasoning_engines_get_execute(builder)
}

/// GET v1/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_list_execute()` to send, or `aiplatform_reasoning_engines_list` for simplest API.

pub fn aiplatform_reasoning_engines_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1/reasoningEngines",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_list_execute()` or `aiplatform_reasoning_engines_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListReasoningEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListReasoningEnginesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_list_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListReasoningEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/reasoningEngines
/// Lists reasoning engines in a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_list_builder()` + `aiplatform_reasoning_engines_list_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_list(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1ListReasoningEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_list_builder(
        client,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    aiplatform_reasoning_engines_list_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_patch_execute()` to send, or `aiplatform_reasoning_engines_patch` for simplest API.

pub fn aiplatform_reasoning_engines_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudAiplatformV1ReasoningEngine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_patch_execute()` or `aiplatform_reasoning_engines_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_patch_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ReasoningEngine,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}
/// Updates a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_patch_builder()` + `aiplatform_reasoning_engines_patch_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_patch(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    aiplatform_reasoning_engines_patch_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_query_execute()` to send, or `aiplatform_reasoning_engines_query` for simplest API.

pub fn aiplatform_reasoning_engines_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1QueryReasoningEngineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_query_execute()` or `aiplatform_reasoning_engines_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryReasoningEngineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1QueryReasoningEngineResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_query_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryReasoningEngineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1QueryReasoningEngineRequest,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:query
/// Queries using a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_query_builder()` + `aiplatform_reasoning_engines_query_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_query(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1QueryReasoningEngineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_query_builder(client, &args.name, &args.body)?;
    aiplatform_reasoning_engines_query_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_stream_query_execute()` to send, or `aiplatform_reasoning_engines_stream_query` for simplest API.

pub fn aiplatform_reasoning_engines_stream_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1StreamQueryReasoningEngineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}:streamQuery",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_stream_query_execute()` or `aiplatform_reasoning_engines_stream_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_stream_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_stream_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_stream_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_stream_query_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_stream_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_stream_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_stream_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_stream_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_stream_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesStreamQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1StreamQueryReasoningEngineRequest,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}:streamQuery
/// Streams queries using a reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_stream_query_builder()` + `aiplatform_reasoning_engines_stream_query_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_stream_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_stream_query(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesStreamQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_stream_query_builder(client, &args.name, &args.body)?;
    aiplatform_reasoning_engines_stream_query_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_memories_operations_cancel_execute()` to send, or `aiplatform_reasoning_engines_memories_operations_cancel` for simplest API.

pub fn aiplatform_reasoning_engines_memories_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/memories/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_memories_operations_cancel_execute()` or `aiplatform_reasoning_engines_memories_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_memories_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_memories_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_memories_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_memories_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_memories_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesMemoriesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_memories_operations_cancel_builder()` + `aiplatform_reasoning_engines_memories_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesMemoriesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_memories_operations_cancel_builder(client, &args.name)?;
    aiplatform_reasoning_engines_memories_operations_cancel_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_memories_operations_delete_execute()` to send, or `aiplatform_reasoning_engines_memories_operations_delete` for simplest API.

pub fn aiplatform_reasoning_engines_memories_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/memories/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_memories_operations_delete_execute()` or `aiplatform_reasoning_engines_memories_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_memories_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_delete_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_memories_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_memories_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_memories_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_memories_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesMemoriesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_memories_operations_delete_builder()` + `aiplatform_reasoning_engines_memories_operations_delete_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesMemoriesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_memories_operations_delete_builder(client, &args.name)?;
    aiplatform_reasoning_engines_memories_operations_delete_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_memories_operations_get_execute()` to send, or `aiplatform_reasoning_engines_memories_operations_get` for simplest API.

pub fn aiplatform_reasoning_engines_memories_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/memories/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_memories_operations_get_execute()` or `aiplatform_reasoning_engines_memories_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_memories_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_get_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_memories_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_memories_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_memories_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_memories_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesMemoriesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_memories_operations_get_builder()` + `aiplatform_reasoning_engines_memories_operations_get_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesMemoriesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_memories_operations_get_builder(client, &args.name)?;
    aiplatform_reasoning_engines_memories_operations_get_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_memories_operations_list_execute()` to send, or `aiplatform_reasoning_engines_memories_operations_list` for simplest API.

pub fn aiplatform_reasoning_engines_memories_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/memories/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_memories_operations_list_execute()` or `aiplatform_reasoning_engines_memories_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_memories_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_list_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_memories_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_memories_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_memories_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_memories_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesMemoriesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_memories_operations_list_builder()` + `aiplatform_reasoning_engines_memories_operations_list_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesMemoriesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_memories_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_reasoning_engines_memories_operations_list_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_memories_operations_wait_execute()` to send, or `aiplatform_reasoning_engines_memories_operations_wait` for simplest API.

pub fn aiplatform_reasoning_engines_memories_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/memories/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_memories_operations_wait_execute()` or `aiplatform_reasoning_engines_memories_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_memories_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_wait_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_memories_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_memories_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_memories_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_memories_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_memories_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesMemoriesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/memories/{memoriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_memories_operations_wait_builder()` + `aiplatform_reasoning_engines_memories_operations_wait_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_memories_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_memories_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesMemoriesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_memories_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_reasoning_engines_memories_operations_wait_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_operations_cancel_execute()` to send, or `aiplatform_reasoning_engines_operations_cancel` for simplest API.

pub fn aiplatform_reasoning_engines_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_operations_cancel_execute()` or `aiplatform_reasoning_engines_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_operations_cancel_builder()` + `aiplatform_reasoning_engines_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_operations_cancel_builder(client, &args.name)?;
    aiplatform_reasoning_engines_operations_cancel_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_operations_delete_execute()` to send, or `aiplatform_reasoning_engines_operations_delete` for simplest API.

pub fn aiplatform_reasoning_engines_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_operations_delete_execute()` or `aiplatform_reasoning_engines_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_operations_delete_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_operations_delete_builder()` + `aiplatform_reasoning_engines_operations_delete_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_operations_delete_builder(client, &args.name)?;
    aiplatform_reasoning_engines_operations_delete_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_operations_get_execute()` to send, or `aiplatform_reasoning_engines_operations_get` for simplest API.

pub fn aiplatform_reasoning_engines_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_operations_get_execute()` or `aiplatform_reasoning_engines_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_operations_get_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_operations_get_builder()` + `aiplatform_reasoning_engines_operations_get_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_operations_get_builder(client, &args.name)?;
    aiplatform_reasoning_engines_operations_get_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_operations_list_execute()` to send, or `aiplatform_reasoning_engines_operations_list` for simplest API.

pub fn aiplatform_reasoning_engines_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_operations_list_execute()` or `aiplatform_reasoning_engines_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_operations_list_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_operations_list_builder()` + `aiplatform_reasoning_engines_operations_list_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_reasoning_engines_operations_list_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_operations_wait_execute()` to send, or `aiplatform_reasoning_engines_operations_wait` for simplest API.

pub fn aiplatform_reasoning_engines_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_operations_wait_execute()` or `aiplatform_reasoning_engines_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_operations_wait_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_operations_wait_builder()` + `aiplatform_reasoning_engines_operations_wait_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_reasoning_engines_operations_wait_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_create_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_create` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudAiplatformV1SandboxEnvironment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_create_execute()` or `aiplatform_reasoning_engines_sandbox_environments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_create_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1SandboxEnvironment,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Creates a SandboxEnvironment in a given reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_create_builder()` + `aiplatform_reasoning_engines_sandbox_environments_create_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_create(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    aiplatform_reasoning_engines_sandbox_environments_create_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_delete_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_delete` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_delete_execute()` or `aiplatform_reasoning_engines_sandbox_environments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_delete_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Deletes the specific SandboxEnvironment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_delete_builder()` + `aiplatform_reasoning_engines_sandbox_environments_delete_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_delete(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_sandbox_environments_delete_builder(client, &args.name)?;
    aiplatform_reasoning_engines_sandbox_environments_delete_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_execute_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_execute` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAiplatformV1ExecuteSandboxEnvironmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}:execute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_execute_execute()` or `aiplatform_reasoning_engines_sandbox_environments_execute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_execute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_execute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_execute_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_execute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_execute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_execute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsExecuteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudAiplatformV1ExecuteSandboxEnvironmentRequest,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}:execute
/// Executes using a sandbox environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_execute_builder()` + `aiplatform_reasoning_engines_sandbox_environments_execute_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_execute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_execute(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsExecuteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_execute_builder(
        client, &args.name, &args.body,
    )?;
    aiplatform_reasoning_engines_sandbox_environments_execute_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_get_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_get` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_get_execute()` or `aiplatform_reasoning_engines_sandbox_environments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SandboxEnvironment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1SandboxEnvironment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_get_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SandboxEnvironment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}
/// Gets details of the specific SandboxEnvironment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_get_builder()` + `aiplatform_reasoning_engines_sandbox_environments_get_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_get(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1SandboxEnvironment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_sandbox_environments_get_builder(client, &args.name)?;
    aiplatform_reasoning_engines_sandbox_environments_get_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_list_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_list` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_list_execute()` or `aiplatform_reasoning_engines_sandbox_environments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_list_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments
/// Lists SandboxEnvironments in a given reasoning engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_list_builder()` + `aiplatform_reasoning_engines_sandbox_environments_list_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_list(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1ListSandboxEnvironmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    aiplatform_reasoning_engines_sandbox_environments_list_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_operations_cancel` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_execute()` or `aiplatform_reasoning_engines_sandbox_environments_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder()` + `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_reasoning_engines_sandbox_environments_operations_cancel_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_operations_delete_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_operations_delete` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_operations_delete_execute()` or `aiplatform_reasoning_engines_sandbox_environments_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_delete_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder()` + `aiplatform_reasoning_engines_sandbox_environments_operations_delete_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_reasoning_engines_sandbox_environments_operations_delete_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_operations_get_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_operations_get` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_operations_get_execute()` or `aiplatform_reasoning_engines_sandbox_environments_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_get_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_get_builder()` + `aiplatform_reasoning_engines_sandbox_environments_operations_get_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_reasoning_engines_sandbox_environments_operations_get_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sandbox_environments_operations_wait_execute()` to send, or `aiplatform_reasoning_engines_sandbox_environments_operations_wait` for simplest API.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sandboxEnvironments/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sandbox_environments_operations_wait_execute()` or `aiplatform_reasoning_engines_sandbox_environments_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_wait_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sandbox_environments_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sandbox_environments_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sandbox_environments_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSandboxEnvironmentsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sandboxEnvironments/{sandboxEnvironmentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder()` + `aiplatform_reasoning_engines_sandbox_environments_operations_wait_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sandbox_environments_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sandbox_environments_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_reasoning_engines_sandbox_environments_operations_wait_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sessions_operations_cancel_execute()` to send, or `aiplatform_reasoning_engines_sessions_operations_cancel` for simplest API.

pub fn aiplatform_reasoning_engines_sessions_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sessions/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sessions_operations_cancel_execute()` or `aiplatform_reasoning_engines_sessions_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sessions_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sessions_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sessions_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sessions_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sessions_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSessionsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sessions_operations_cancel_builder()` + `aiplatform_reasoning_engines_sessions_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSessionsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_sessions_operations_cancel_builder(client, &args.name)?;
    aiplatform_reasoning_engines_sessions_operations_cancel_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sessions_operations_delete_execute()` to send, or `aiplatform_reasoning_engines_sessions_operations_delete` for simplest API.

pub fn aiplatform_reasoning_engines_sessions_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sessions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sessions_operations_delete_execute()` or `aiplatform_reasoning_engines_sessions_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sessions_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_delete_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sessions_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sessions_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sessions_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sessions_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSessionsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sessions_operations_delete_builder()` + `aiplatform_reasoning_engines_sessions_operations_delete_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSessionsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_reasoning_engines_sessions_operations_delete_builder(client, &args.name)?;
    aiplatform_reasoning_engines_sessions_operations_delete_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sessions_operations_get_execute()` to send, or `aiplatform_reasoning_engines_sessions_operations_get` for simplest API.

pub fn aiplatform_reasoning_engines_sessions_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sessions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sessions_operations_get_execute()` or `aiplatform_reasoning_engines_sessions_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sessions_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_get_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sessions_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sessions_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sessions_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sessions_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSessionsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sessions_operations_get_builder()` + `aiplatform_reasoning_engines_sessions_operations_get_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSessionsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sessions_operations_get_builder(client, &args.name)?;
    aiplatform_reasoning_engines_sessions_operations_get_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sessions_operations_list_execute()` to send, or `aiplatform_reasoning_engines_sessions_operations_list` for simplest API.

pub fn aiplatform_reasoning_engines_sessions_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sessions/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sessions_operations_list_execute()` or `aiplatform_reasoning_engines_sessions_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sessions_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_list_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sessions_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sessions_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sessions_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sessions_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSessionsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sessions_operations_list_builder()` + `aiplatform_reasoning_engines_sessions_operations_list_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSessionsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sessions_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_reasoning_engines_sessions_operations_list_execute(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_reasoning_engines_sessions_operations_wait_execute()` to send, or `aiplatform_reasoning_engines_sessions_operations_wait` for simplest API.

pub fn aiplatform_reasoning_engines_sessions_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/reasoningEngines/{}/sessions/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_reasoning_engines_sessions_operations_wait_execute()` or `aiplatform_reasoning_engines_sessions_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_reasoning_engines_sessions_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_wait_task()`.
/// For the simplest API, use `aiplatform_reasoning_engines_sessions_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_reasoning_engines_sessions_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_reasoning_engines_sessions_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_reasoning_engines_sessions_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_reasoning_engines_sessions_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformReasoningEnginesSessionsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/reasoningEngines/{reasoningEnginesId}/sessions/{sessionsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_reasoning_engines_sessions_operations_wait_builder()` + `aiplatform_reasoning_engines_sessions_operations_wait_execute()`.
/// For task-level control, use `aiplatform_reasoning_engines_sessions_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_reasoning_engines_sessions_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformReasoningEnginesSessionsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_reasoning_engines_sessions_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_reasoning_engines_sessions_operations_wait_execute(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_schedules_operations_cancel_execute()` to send, or `aiplatform_schedules_operations_cancel` for simplest API.

pub fn aiplatform_schedules_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/schedules/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_schedules_operations_cancel_execute()` or `aiplatform_schedules_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_schedules_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_schedules_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_schedules_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_schedules_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_schedules_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_schedules_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSchedulesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_schedules_operations_cancel_builder()` + `aiplatform_schedules_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_schedules_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformSchedulesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_schedules_operations_cancel_builder(client, &args.name)?;
    aiplatform_schedules_operations_cancel_execute(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_schedules_operations_delete_execute()` to send, or `aiplatform_schedules_operations_delete` for simplest API.

pub fn aiplatform_schedules_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/schedules/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_schedules_operations_delete_execute()` or `aiplatform_schedules_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_schedules_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_schedules_operations_delete_task()`.
/// For the simplest API, use `aiplatform_schedules_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_schedules_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_schedules_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_schedules_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSchedulesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_schedules_operations_delete_builder()` + `aiplatform_schedules_operations_delete_execute()`.
/// For task-level control, use `aiplatform_schedules_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformSchedulesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_schedules_operations_delete_builder(client, &args.name)?;
    aiplatform_schedules_operations_delete_execute(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_schedules_operations_get_execute()` to send, or `aiplatform_schedules_operations_get` for simplest API.

pub fn aiplatform_schedules_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/schedules/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_schedules_operations_get_execute()` or `aiplatform_schedules_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_schedules_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_schedules_operations_get_task()`.
/// For the simplest API, use `aiplatform_schedules_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_schedules_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_schedules_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_schedules_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSchedulesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_schedules_operations_get_builder()` + `aiplatform_schedules_operations_get_execute()`.
/// For task-level control, use `aiplatform_schedules_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformSchedulesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_schedules_operations_get_builder(client, &args.name)?;
    aiplatform_schedules_operations_get_execute(builder)
}

/// GET v1/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_schedules_operations_list_execute()` to send, or `aiplatform_schedules_operations_list` for simplest API.

pub fn aiplatform_schedules_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/schedules/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_schedules_operations_list_execute()` or `aiplatform_schedules_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_schedules_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_schedules_operations_list_task()`.
/// For the simplest API, use `aiplatform_schedules_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_schedules_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_schedules_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_schedules_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSchedulesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/schedules/{schedulesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_schedules_operations_list_builder()` + `aiplatform_schedules_operations_list_execute()`.
/// For task-level control, use `aiplatform_schedules_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformSchedulesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_schedules_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_schedules_operations_list_execute(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_schedules_operations_wait_execute()` to send, or `aiplatform_schedules_operations_wait` for simplest API.

pub fn aiplatform_schedules_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/schedules/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_schedules_operations_wait_execute()` or `aiplatform_schedules_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_schedules_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_schedules_operations_wait_task()`.
/// For the simplest API, use `aiplatform_schedules_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_schedules_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_schedules_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_schedules_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_schedules_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSchedulesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/schedules/{schedulesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_schedules_operations_wait_builder()` + `aiplatform_schedules_operations_wait_execute()`.
/// For task-level control, use `aiplatform_schedules_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_schedules_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformSchedulesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_schedules_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_schedules_operations_wait_execute(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_specialist_pools_operations_cancel_execute()` to send, or `aiplatform_specialist_pools_operations_cancel` for simplest API.

pub fn aiplatform_specialist_pools_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/specialistPools/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_specialist_pools_operations_cancel_execute()` or `aiplatform_specialist_pools_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_specialist_pools_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_specialist_pools_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_specialist_pools_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_specialist_pools_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_specialist_pools_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_specialist_pools_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSpecialistPoolsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_specialist_pools_operations_cancel_builder()` + `aiplatform_specialist_pools_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_specialist_pools_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformSpecialistPoolsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_specialist_pools_operations_cancel_builder(client, &args.name)?;
    aiplatform_specialist_pools_operations_cancel_execute(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_specialist_pools_operations_delete_execute()` to send, or `aiplatform_specialist_pools_operations_delete` for simplest API.

pub fn aiplatform_specialist_pools_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/specialistPools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_specialist_pools_operations_delete_execute()` or `aiplatform_specialist_pools_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_specialist_pools_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_specialist_pools_operations_delete_task()`.
/// For the simplest API, use `aiplatform_specialist_pools_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_specialist_pools_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_specialist_pools_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_specialist_pools_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSpecialistPoolsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_specialist_pools_operations_delete_builder()` + `aiplatform_specialist_pools_operations_delete_execute()`.
/// For task-level control, use `aiplatform_specialist_pools_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformSpecialistPoolsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_specialist_pools_operations_delete_builder(client, &args.name)?;
    aiplatform_specialist_pools_operations_delete_execute(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_specialist_pools_operations_get_execute()` to send, or `aiplatform_specialist_pools_operations_get` for simplest API.

pub fn aiplatform_specialist_pools_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/specialistPools/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_specialist_pools_operations_get_execute()` or `aiplatform_specialist_pools_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_specialist_pools_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_specialist_pools_operations_get_task()`.
/// For the simplest API, use `aiplatform_specialist_pools_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_specialist_pools_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_specialist_pools_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_specialist_pools_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSpecialistPoolsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_specialist_pools_operations_get_builder()` + `aiplatform_specialist_pools_operations_get_execute()`.
/// For task-level control, use `aiplatform_specialist_pools_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformSpecialistPoolsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_specialist_pools_operations_get_builder(client, &args.name)?;
    aiplatform_specialist_pools_operations_get_execute(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_specialist_pools_operations_list_execute()` to send, or `aiplatform_specialist_pools_operations_list` for simplest API.

pub fn aiplatform_specialist_pools_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/specialistPools/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_specialist_pools_operations_list_execute()` or `aiplatform_specialist_pools_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_specialist_pools_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_specialist_pools_operations_list_task()`.
/// For the simplest API, use `aiplatform_specialist_pools_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_specialist_pools_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_specialist_pools_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_specialist_pools_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSpecialistPoolsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/specialistPools/{specialistPoolsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_specialist_pools_operations_list_builder()` + `aiplatform_specialist_pools_operations_list_execute()`.
/// For task-level control, use `aiplatform_specialist_pools_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformSpecialistPoolsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_specialist_pools_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_specialist_pools_operations_list_execute(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_specialist_pools_operations_wait_execute()` to send, or `aiplatform_specialist_pools_operations_wait` for simplest API.

pub fn aiplatform_specialist_pools_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/specialistPools/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_specialist_pools_operations_wait_execute()` or `aiplatform_specialist_pools_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_specialist_pools_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_specialist_pools_operations_wait_task()`.
/// For the simplest API, use `aiplatform_specialist_pools_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_specialist_pools_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_specialist_pools_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_specialist_pools_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_specialist_pools_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformSpecialistPoolsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/specialistPools/{specialistPoolsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_specialist_pools_operations_wait_builder()` + `aiplatform_specialist_pools_operations_wait_execute()`.
/// For task-level control, use `aiplatform_specialist_pools_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_specialist_pools_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformSpecialistPoolsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_specialist_pools_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_specialist_pools_operations_wait_execute(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_operations_cancel_execute()` to send, or `aiplatform_studies_operations_cancel` for simplest API.

pub fn aiplatform_studies_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_operations_cancel_execute()` or `aiplatform_studies_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_studies_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_operations_cancel_builder()` + `aiplatform_studies_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_studies_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_operations_cancel_builder(client, &args.name)?;
    aiplatform_studies_operations_cancel_execute(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_operations_delete_execute()` to send, or `aiplatform_studies_operations_delete` for simplest API.

pub fn aiplatform_studies_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_operations_delete_execute()` or `aiplatform_studies_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_operations_delete_task()`.
/// For the simplest API, use `aiplatform_studies_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_operations_delete_builder()` + `aiplatform_studies_operations_delete_execute()`.
/// For task-level control, use `aiplatform_studies_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_operations_delete_builder(client, &args.name)?;
    aiplatform_studies_operations_delete_execute(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_operations_get_execute()` to send, or `aiplatform_studies_operations_get` for simplest API.

pub fn aiplatform_studies_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_operations_get_execute()` or `aiplatform_studies_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_operations_get_task()`.
/// For the simplest API, use `aiplatform_studies_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/studies/{studiesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_operations_get_builder()` + `aiplatform_studies_operations_get_execute()`.
/// For task-level control, use `aiplatform_studies_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_operations_get_builder(client, &args.name)?;
    aiplatform_studies_operations_get_execute(builder)
}

/// GET v1/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_operations_list_execute()` to send, or `aiplatform_studies_operations_list` for simplest API.

pub fn aiplatform_studies_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_operations_list_execute()` or `aiplatform_studies_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_operations_list_task()`.
/// For the simplest API, use `aiplatform_studies_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/studies/{studiesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_operations_list_builder()` + `aiplatform_studies_operations_list_execute()`.
/// For task-level control, use `aiplatform_studies_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_studies_operations_list_execute(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_operations_wait_execute()` to send, or `aiplatform_studies_operations_wait` for simplest API.

pub fn aiplatform_studies_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_operations_wait_execute()` or `aiplatform_studies_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_operations_wait_task()`.
/// For the simplest API, use `aiplatform_studies_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/studies/{studiesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_operations_wait_builder()` + `aiplatform_studies_operations_wait_execute()`.
/// For task-level control, use `aiplatform_studies_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_studies_operations_wait_builder(client, &args.name, args.timeout.as_deref())?;
    aiplatform_studies_operations_wait_execute(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_trials_operations_cancel_execute()` to send, or `aiplatform_studies_trials_operations_cancel` for simplest API.

pub fn aiplatform_studies_trials_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/trials/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_trials_operations_cancel_execute()` or `aiplatform_studies_trials_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_trials_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_trials_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_studies_trials_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_trials_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_trials_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_trials_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesTrialsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_trials_operations_cancel_builder()` + `aiplatform_studies_trials_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_studies_trials_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesTrialsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_trials_operations_cancel_builder(client, &args.name)?;
    aiplatform_studies_trials_operations_cancel_execute(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_trials_operations_delete_execute()` to send, or `aiplatform_studies_trials_operations_delete` for simplest API.

pub fn aiplatform_studies_trials_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/trials/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_trials_operations_delete_execute()` or `aiplatform_studies_trials_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_trials_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_trials_operations_delete_task()`.
/// For the simplest API, use `aiplatform_studies_trials_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_trials_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_trials_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_trials_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesTrialsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_trials_operations_delete_builder()` + `aiplatform_studies_trials_operations_delete_execute()`.
/// For task-level control, use `aiplatform_studies_trials_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesTrialsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_trials_operations_delete_builder(client, &args.name)?;
    aiplatform_studies_trials_operations_delete_execute(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_trials_operations_get_execute()` to send, or `aiplatform_studies_trials_operations_get` for simplest API.

pub fn aiplatform_studies_trials_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/trials/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_trials_operations_get_execute()` or `aiplatform_studies_trials_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_trials_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_trials_operations_get_task()`.
/// For the simplest API, use `aiplatform_studies_trials_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_trials_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_trials_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_trials_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesTrialsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_trials_operations_get_builder()` + `aiplatform_studies_trials_operations_get_execute()`.
/// For task-level control, use `aiplatform_studies_trials_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesTrialsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_trials_operations_get_builder(client, &args.name)?;
    aiplatform_studies_trials_operations_get_execute(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_trials_operations_list_execute()` to send, or `aiplatform_studies_trials_operations_list` for simplest API.

pub fn aiplatform_studies_trials_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/trials/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_trials_operations_list_execute()` or `aiplatform_studies_trials_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_trials_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_trials_operations_list_task()`.
/// For the simplest API, use `aiplatform_studies_trials_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_trials_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_trials_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_trials_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesTrialsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_trials_operations_list_builder()` + `aiplatform_studies_trials_operations_list_execute()`.
/// For task-level control, use `aiplatform_studies_trials_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesTrialsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_trials_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_studies_trials_operations_list_execute(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_studies_trials_operations_wait_execute()` to send, or `aiplatform_studies_trials_operations_wait` for simplest API.

pub fn aiplatform_studies_trials_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/studies/{}/trials/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_studies_trials_operations_wait_execute()` or `aiplatform_studies_trials_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_studies_trials_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_studies_trials_operations_wait_task()`.
/// For the simplest API, use `aiplatform_studies_trials_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_studies_trials_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_studies_trials_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_studies_trials_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_studies_trials_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformStudiesTrialsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/studies/{studiesId}/trials/{trialsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_studies_trials_operations_wait_builder()` + `aiplatform_studies_trials_operations_wait_execute()`.
/// For task-level control, use `aiplatform_studies_trials_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_studies_trials_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformStudiesTrialsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_studies_trials_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_studies_trials_operations_wait_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_operations_cancel_execute()` to send, or `aiplatform_tensorboards_experiments_operations_cancel` for simplest API.

pub fn aiplatform_tensorboards_experiments_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_operations_cancel_execute()` or `aiplatform_tensorboards_experiments_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_operations_cancel_builder()` + `aiplatform_tensorboards_experiments_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_tensorboards_experiments_operations_cancel_builder(client, &args.name)?;
    aiplatform_tensorboards_experiments_operations_cancel_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_operations_delete_execute()` to send, or `aiplatform_tensorboards_experiments_operations_delete` for simplest API.

pub fn aiplatform_tensorboards_experiments_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_operations_delete_execute()` or `aiplatform_tensorboards_experiments_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_delete_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_operations_delete_builder()` + `aiplatform_tensorboards_experiments_operations_delete_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_tensorboards_experiments_operations_delete_builder(client, &args.name)?;
    aiplatform_tensorboards_experiments_operations_delete_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_operations_get_execute()` to send, or `aiplatform_tensorboards_experiments_operations_get` for simplest API.

pub fn aiplatform_tensorboards_experiments_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_operations_get_execute()` or `aiplatform_tensorboards_experiments_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_get_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_operations_get_builder()` + `aiplatform_tensorboards_experiments_operations_get_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_operations_get_builder(client, &args.name)?;
    aiplatform_tensorboards_experiments_operations_get_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_operations_list_execute()` to send, or `aiplatform_tensorboards_experiments_operations_list` for simplest API.

pub fn aiplatform_tensorboards_experiments_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_operations_list_execute()` or `aiplatform_tensorboards_experiments_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_list_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_operations_list_builder()` + `aiplatform_tensorboards_experiments_operations_list_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_tensorboards_experiments_operations_list_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_operations_wait_execute()` to send, or `aiplatform_tensorboards_experiments_operations_wait` for simplest API.

pub fn aiplatform_tensorboards_experiments_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_operations_wait_execute()` or `aiplatform_tensorboards_experiments_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_wait_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_operations_wait_builder()` + `aiplatform_tensorboards_experiments_operations_wait_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_tensorboards_experiments_operations_wait_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_operations_cancel_execute()` to send, or `aiplatform_tensorboards_experiments_runs_operations_cancel` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_operations_cancel_execute()` or `aiplatform_tensorboards_experiments_runs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_operations_cancel_builder()` + `aiplatform_tensorboards_experiments_runs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_tensorboards_experiments_runs_operations_cancel_builder(client, &args.name)?;
    aiplatform_tensorboards_experiments_runs_operations_cancel_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_operations_delete_execute()` to send, or `aiplatform_tensorboards_experiments_runs_operations_delete` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_operations_delete_execute()` or `aiplatform_tensorboards_experiments_runs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_operations_delete_builder()` + `aiplatform_tensorboards_experiments_runs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_tensorboards_experiments_runs_operations_delete_builder(client, &args.name)?;
    aiplatform_tensorboards_experiments_runs_operations_delete_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_operations_get_execute()` to send, or `aiplatform_tensorboards_experiments_runs_operations_get` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_operations_get_execute()` or `aiplatform_tensorboards_experiments_runs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_get_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_operations_get_builder()` + `aiplatform_tensorboards_experiments_runs_operations_get_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        aiplatform_tensorboards_experiments_runs_operations_get_builder(client, &args.name)?;
    aiplatform_tensorboards_experiments_runs_operations_get_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_operations_list_execute()` to send, or `aiplatform_tensorboards_experiments_runs_operations_list` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_operations_list_execute()` or `aiplatform_tensorboards_experiments_runs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_list_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_operations_list_builder()` + `aiplatform_tensorboards_experiments_runs_operations_list_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_tensorboards_experiments_runs_operations_list_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_operations_wait_execute()` to send, or `aiplatform_tensorboards_experiments_runs_operations_wait` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_operations_wait_execute()` or `aiplatform_tensorboards_experiments_runs_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_wait_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_operations_wait_builder()` + `aiplatform_tensorboards_experiments_runs_operations_wait_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_tensorboards_experiments_runs_operations_wait_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_execute()` to send, or `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_execute()` or `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_time_series_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder()` + `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder(
        client, &args.name,
    )?;
    aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_execute()` to send, or `aiplatform_tensorboards_experiments_runs_time_series_operations_delete` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_execute()` or `aiplatform_tensorboards_experiments_runs_time_series_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        aiplatform_tensorboards_experiments_runs_time_series_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_time_series_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder()` + `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder(
        client, &args.name,
    )?;
    aiplatform_tensorboards_experiments_runs_time_series_operations_delete_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_time_series_operations_get_execute()` to send, or `aiplatform_tensorboards_experiments_runs_time_series_operations_get` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_time_series_operations_get_execute()` or `aiplatform_tensorboards_experiments_runs_time_series_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_get_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_time_series_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_time_series_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_time_series_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_get_builder()` + `aiplatform_tensorboards_experiments_runs_time_series_operations_get_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_get_builder(
        client, &args.name,
    )?;
    aiplatform_tensorboards_experiments_runs_time_series_operations_get_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_time_series_operations_list_execute()` to send, or `aiplatform_tensorboards_experiments_runs_time_series_operations_list` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_time_series_operations_list_execute()` or `aiplatform_tensorboards_experiments_runs_time_series_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_list_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_time_series_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_time_series_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_time_series_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_list_builder()` + `aiplatform_tensorboards_experiments_runs_time_series_operations_list_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_tensorboards_experiments_runs_time_series_operations_list_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_execute()` to send, or `aiplatform_tensorboards_experiments_runs_time_series_operations_wait` for simplest API.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/experiments/{}/runs/{}/timeSeries/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_execute()` or `aiplatform_tensorboards_experiments_runs_time_series_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_task()`.
/// For the simplest API, use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_experiments_runs_time_series_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_experiments_runs_time_series_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/tensorboards/{tensorboardsId}/experiments/{experimentsId}/runs/{runsId}/timeSeries/{timeSeriesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder()` + `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_execute()`.
/// For task-level control, use `aiplatform_tensorboards_experiments_runs_time_series_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_tensorboards_experiments_runs_time_series_operations_wait_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_operations_cancel_execute()` to send, or `aiplatform_tensorboards_operations_cancel` for simplest API.

pub fn aiplatform_tensorboards_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_operations_cancel_execute()` or `aiplatform_tensorboards_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_tensorboards_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_operations_cancel_builder()` + `aiplatform_tensorboards_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_tensorboards_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_operations_cancel_builder(client, &args.name)?;
    aiplatform_tensorboards_operations_cancel_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_operations_delete_execute()` to send, or `aiplatform_tensorboards_operations_delete` for simplest API.

pub fn aiplatform_tensorboards_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_operations_delete_execute()` or `aiplatform_tensorboards_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_operations_delete_task()`.
/// For the simplest API, use `aiplatform_tensorboards_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_operations_delete_builder()` + `aiplatform_tensorboards_operations_delete_execute()`.
/// For task-level control, use `aiplatform_tensorboards_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_operations_delete_builder(client, &args.name)?;
    aiplatform_tensorboards_operations_delete_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_operations_get_execute()` to send, or `aiplatform_tensorboards_operations_get` for simplest API.

pub fn aiplatform_tensorboards_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_operations_get_execute()` or `aiplatform_tensorboards_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_operations_get_task()`.
/// For the simplest API, use `aiplatform_tensorboards_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_operations_get_builder()` + `aiplatform_tensorboards_operations_get_execute()`.
/// For task-level control, use `aiplatform_tensorboards_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_operations_get_builder(client, &args.name)?;
    aiplatform_tensorboards_operations_get_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_operations_list_execute()` to send, or `aiplatform_tensorboards_operations_list` for simplest API.

pub fn aiplatform_tensorboards_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_operations_list_execute()` or `aiplatform_tensorboards_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_operations_list_task()`.
/// For the simplest API, use `aiplatform_tensorboards_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/tensorboards/{tensorboardsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_operations_list_builder()` + `aiplatform_tensorboards_operations_list_execute()`.
/// For task-level control, use `aiplatform_tensorboards_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_tensorboards_operations_list_execute(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tensorboards_operations_wait_execute()` to send, or `aiplatform_tensorboards_operations_wait` for simplest API.

pub fn aiplatform_tensorboards_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tensorboards/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tensorboards_operations_wait_execute()` or `aiplatform_tensorboards_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tensorboards_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tensorboards_operations_wait_task()`.
/// For the simplest API, use `aiplatform_tensorboards_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tensorboards_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tensorboards_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tensorboards_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tensorboards_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTensorboardsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/tensorboards/{tensorboardsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tensorboards_operations_wait_builder()` + `aiplatform_tensorboards_operations_wait_execute()`.
/// For task-level control, use `aiplatform_tensorboards_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tensorboards_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformTensorboardsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tensorboards_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_tensorboards_operations_wait_execute(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_training_pipelines_operations_cancel_execute()` to send, or `aiplatform_training_pipelines_operations_cancel` for simplest API.

pub fn aiplatform_training_pipelines_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/trainingPipelines/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_training_pipelines_operations_cancel_execute()` or `aiplatform_training_pipelines_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_training_pipelines_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_training_pipelines_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_training_pipelines_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_training_pipelines_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_training_pipelines_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_training_pipelines_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTrainingPipelinesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_training_pipelines_operations_cancel_builder()` + `aiplatform_training_pipelines_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_training_pipelines_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformTrainingPipelinesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_training_pipelines_operations_cancel_builder(client, &args.name)?;
    aiplatform_training_pipelines_operations_cancel_execute(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_training_pipelines_operations_delete_execute()` to send, or `aiplatform_training_pipelines_operations_delete` for simplest API.

pub fn aiplatform_training_pipelines_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/trainingPipelines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_training_pipelines_operations_delete_execute()` or `aiplatform_training_pipelines_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_training_pipelines_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_training_pipelines_operations_delete_task()`.
/// For the simplest API, use `aiplatform_training_pipelines_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_training_pipelines_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_training_pipelines_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_training_pipelines_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTrainingPipelinesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_training_pipelines_operations_delete_builder()` + `aiplatform_training_pipelines_operations_delete_execute()`.
/// For task-level control, use `aiplatform_training_pipelines_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformTrainingPipelinesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_training_pipelines_operations_delete_builder(client, &args.name)?;
    aiplatform_training_pipelines_operations_delete_execute(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_training_pipelines_operations_get_execute()` to send, or `aiplatform_training_pipelines_operations_get` for simplest API.

pub fn aiplatform_training_pipelines_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/trainingPipelines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_training_pipelines_operations_get_execute()` or `aiplatform_training_pipelines_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_training_pipelines_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_training_pipelines_operations_get_task()`.
/// For the simplest API, use `aiplatform_training_pipelines_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_training_pipelines_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_training_pipelines_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_training_pipelines_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTrainingPipelinesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_training_pipelines_operations_get_builder()` + `aiplatform_training_pipelines_operations_get_execute()`.
/// For task-level control, use `aiplatform_training_pipelines_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformTrainingPipelinesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_training_pipelines_operations_get_builder(client, &args.name)?;
    aiplatform_training_pipelines_operations_get_execute(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_training_pipelines_operations_list_execute()` to send, or `aiplatform_training_pipelines_operations_list` for simplest API.

pub fn aiplatform_training_pipelines_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/trainingPipelines/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_training_pipelines_operations_list_execute()` or `aiplatform_training_pipelines_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_training_pipelines_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_training_pipelines_operations_list_task()`.
/// For the simplest API, use `aiplatform_training_pipelines_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_training_pipelines_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_training_pipelines_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_training_pipelines_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTrainingPipelinesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_training_pipelines_operations_list_builder()` + `aiplatform_training_pipelines_operations_list_execute()`.
/// For task-level control, use `aiplatform_training_pipelines_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformTrainingPipelinesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_training_pipelines_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_training_pipelines_operations_list_execute(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_training_pipelines_operations_wait_execute()` to send, or `aiplatform_training_pipelines_operations_wait` for simplest API.

pub fn aiplatform_training_pipelines_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    timeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/trainingPipelines/{}/operations/{}:wait",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_training_pipelines_operations_wait_execute()` or `aiplatform_training_pipelines_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_training_pipelines_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_training_pipelines_operations_wait_task()`.
/// For the simplest API, use `aiplatform_training_pipelines_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_training_pipelines_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_training_pipelines_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_training_pipelines_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_training_pipelines_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTrainingPipelinesOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: timeout
    pub timeout: Option<String>,
}

/// GET v1/trainingPipelines/{trainingPipelinesId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_training_pipelines_operations_wait_builder()` + `aiplatform_training_pipelines_operations_wait_execute()`.
/// For task-level control, use `aiplatform_training_pipelines_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_training_pipelines_operations_wait(
    client: &SimpleHttpClient,
    args: &AiplatformTrainingPipelinesOperationsWaitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_training_pipelines_operations_wait_builder(
        client,
        &args.name,
        args.timeout.as_deref(),
    )?;
    aiplatform_training_pipelines_operations_wait_execute(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tuning_jobs_operations_cancel_execute()` to send, or `aiplatform_tuning_jobs_operations_cancel` for simplest API.

pub fn aiplatform_tuning_jobs_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tuningJobs/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tuning_jobs_operations_cancel_execute()` or `aiplatform_tuning_jobs_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tuning_jobs_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tuning_jobs_operations_cancel_task()`.
/// For the simplest API, use `aiplatform_tuning_jobs_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tuning_jobs_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tuning_jobs_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tuning_jobs_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTuningJobsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tuning_jobs_operations_cancel_builder()` + `aiplatform_tuning_jobs_operations_cancel_execute()`.
/// For task-level control, use `aiplatform_tuning_jobs_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_cancel(
    client: &SimpleHttpClient,
    args: &AiplatformTuningJobsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tuning_jobs_operations_cancel_builder(client, &args.name)?;
    aiplatform_tuning_jobs_operations_cancel_execute(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tuning_jobs_operations_delete_execute()` to send, or `aiplatform_tuning_jobs_operations_delete` for simplest API.

pub fn aiplatform_tuning_jobs_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tuning_jobs_operations_delete_execute()` or `aiplatform_tuning_jobs_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tuning_jobs_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tuning_jobs_operations_delete_task()`.
/// For the simplest API, use `aiplatform_tuning_jobs_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tuning_jobs_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tuning_jobs_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tuning_jobs_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTuningJobsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tuning_jobs_operations_delete_builder()` + `aiplatform_tuning_jobs_operations_delete_execute()`.
/// For task-level control, use `aiplatform_tuning_jobs_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_delete(
    client: &SimpleHttpClient,
    args: &AiplatformTuningJobsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tuning_jobs_operations_delete_builder(client, &args.name)?;
    aiplatform_tuning_jobs_operations_delete_execute(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tuning_jobs_operations_get_execute()` to send, or `aiplatform_tuning_jobs_operations_get` for simplest API.

pub fn aiplatform_tuning_jobs_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tuningJobs/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tuning_jobs_operations_get_execute()` or `aiplatform_tuning_jobs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tuning_jobs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tuning_jobs_operations_get_task()`.
/// For the simplest API, use `aiplatform_tuning_jobs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tuning_jobs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tuning_jobs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tuning_jobs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTuningJobsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/tuningJobs/{tuningJobsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tuning_jobs_operations_get_builder()` + `aiplatform_tuning_jobs_operations_get_execute()`.
/// For task-level control, use `aiplatform_tuning_jobs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_get(
    client: &SimpleHttpClient,
    args: &AiplatformTuningJobsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tuning_jobs_operations_get_builder(client, &args.name)?;
    aiplatform_tuning_jobs_operations_get_execute(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_tuning_jobs_operations_list_execute()` to send, or `aiplatform_tuning_jobs_operations_list` for simplest API.

pub fn aiplatform_tuning_jobs_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://aiplatform.googleapis.com/v1/tuningJobs/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_tuning_jobs_operations_list_execute()` or `aiplatform_tuning_jobs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_tuning_jobs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_tuning_jobs_operations_list_task()`.
/// For the simplest API, use `aiplatform_tuning_jobs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_tuning_jobs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_tuning_jobs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_tuning_jobs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_tuning_jobs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformTuningJobsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/tuningJobs/{tuningJobsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_tuning_jobs_operations_list_builder()` + `aiplatform_tuning_jobs_operations_list_execute()`.
/// For task-level control, use `aiplatform_tuning_jobs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_tuning_jobs_operations_list(
    client: &SimpleHttpClient,
    args: &AiplatformTuningJobsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_tuning_jobs_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    aiplatform_tuning_jobs_operations_list_execute(builder)
}

/// GET v1:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_evaluate_dataset_execute()` to send, or `aiplatform_evaluate_dataset` for simplest API.

pub fn aiplatform_evaluate_dataset_builder(
    client: &SimpleHttpClient,
    body: &GoogleCloudAiplatformV1EvaluateDatasetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1:evaluateDataset",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_evaluate_dataset_execute()` or `aiplatform_evaluate_dataset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_evaluate_dataset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_evaluate_dataset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_evaluate_dataset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_evaluate_dataset_task()`.
/// For the simplest API, use `aiplatform_evaluate_dataset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_evaluate_dataset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_evaluate_dataset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_evaluate_dataset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_evaluate_dataset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEvaluateDatasetArgs {
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluateDatasetRequest,
}

/// GET v1:evaluateDataset
/// Evaluates a dataset based on a set of given metrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_evaluate_dataset_builder()` + `aiplatform_evaluate_dataset_execute()`.
/// For task-level control, use `aiplatform_evaluate_dataset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_evaluate_dataset(
    client: &SimpleHttpClient,
    args: &AiplatformEvaluateDatasetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_evaluate_dataset_builder(client, &args.body)?;
    aiplatform_evaluate_dataset_execute(builder)
}

/// GET v1:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_evaluate_instances_execute()` to send, or `aiplatform_evaluate_instances` for simplest API.

pub fn aiplatform_evaluate_instances_builder(
    client: &SimpleHttpClient,
    body: &GoogleCloudAiplatformV1EvaluateInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1:evaluateInstances",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_evaluate_instances_execute()` or `aiplatform_evaluate_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_evaluate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_evaluate_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluateInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1EvaluateInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_evaluate_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_evaluate_instances_task()`.
/// For the simplest API, use `aiplatform_evaluate_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_evaluate_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_evaluate_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluateInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_evaluate_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_evaluate_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformEvaluateInstancesArgs {
    /// Request body.
    pub body: GoogleCloudAiplatformV1EvaluateInstancesRequest,
}

/// GET v1:evaluateInstances
/// Evaluates instances based on a given metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_evaluate_instances_builder()` + `aiplatform_evaluate_instances_execute()`.
/// For task-level control, use `aiplatform_evaluate_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_evaluate_instances(
    client: &SimpleHttpClient,
    args: &AiplatformEvaluateInstancesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAiplatformV1EvaluateInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_evaluate_instances_builder(client, &args.body)?;
    aiplatform_evaluate_instances_execute(builder)
}

/// GET v1:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `aiplatform_generate_instance_rubrics_execute()` to send, or `aiplatform_generate_instance_rubrics` for simplest API.

pub fn aiplatform_generate_instance_rubrics_builder(
    client: &SimpleHttpClient,
    body: &GoogleCloudAiplatformV1GenerateInstanceRubricsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://aiplatform.googleapis.com/v1:generateInstanceRubrics",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `aiplatform_generate_instance_rubrics_execute()` or `aiplatform_generate_instance_rubrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_generate_instance_rubrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_generate_instance_rubrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAiplatformV1GenerateInstanceRubricsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `aiplatform_generate_instance_rubrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `aiplatform_generate_instance_rubrics_task()`.
/// For the simplest API, use `aiplatform_generate_instance_rubrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `aiplatform_generate_instance_rubrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn aiplatform_generate_instance_rubrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = aiplatform_generate_instance_rubrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`aiplatform_generate_instance_rubrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AiplatformGenerateInstanceRubricsArgs {
    /// Request body.
    pub body: GoogleCloudAiplatformV1GenerateInstanceRubricsRequest,
}

/// GET v1:generateInstanceRubrics
/// Generates rubrics for a given prompt. A rubric represents a single testable criterion for evaluation. One input prompt could have multiple rubrics This RPC allows users to get suggested rubrics based on provided prompt, which can then be reviewed and used for subsequent evaluations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `aiplatform_generate_instance_rubrics_builder()` + `aiplatform_generate_instance_rubrics_execute()`.
/// For task-level control, use `aiplatform_generate_instance_rubrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn aiplatform_generate_instance_rubrics(
    client: &SimpleHttpClient,
    args: &AiplatformGenerateInstanceRubricsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = aiplatform_generate_instance_rubrics_builder(client, &args.body)?;
    aiplatform_generate_instance_rubrics_execute(builder)
}
