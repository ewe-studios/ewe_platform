//! Auto-generated API clients for gcp/pagespeedonline.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/pagespeedonline`

#![cfg(feature = "gcp/pagespeedonline")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pagespeedonline_pagespeedapi_runpagespeed_execute()` to send, or `pagespeedonline_pagespeedapi_runpagespeed` for simplest API.

pub fn pagespeedonline_pagespeedapi_runpagespeed_builder(
    client: &SimpleHttpClient,
    url: &str,
    captchaToken: Option<&str>,
    category: Option<&str>,
    locale: Option<&str>,
    strategy: Option<&str>,
    utm_campaign: Option<&str>,
    utm_source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://pagespeedonline.googleapis.com/pagespeedonline/v5/runPagespeed",
        url,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = captchaToken {
        query_parts.push(format!("captchaToken={}", val));
    }
    if let Some(val) = category {
        query_parts.push(format!("category={}", val));
    }
    if let Some(val) = locale {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = strategy {
        query_parts.push(format!("strategy={}", val));
    }
    if let Some(val) = utm_campaign {
        query_parts.push(format!("utm_campaign={}", val));
    }
    if let Some(val) = utm_source {
        query_parts.push(format!("utm_source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `pagespeedonline_pagespeedapi_runpagespeed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from pagespeedonline_pagespeedapi_runpagespeed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pagespeedonline_pagespeedapi_runpagespeed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PagespeedApiPagespeedResponseV5>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PagespeedApiPagespeedResponseV5 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pagespeedonline_pagespeedapi_runpagespeed_builder()` + `pagespeedonline_pagespeedapi_runpagespeed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pagespeedonline_pagespeedapi_runpagespeed(
    client: &SimpleHttpClient,
    url: &str,
    captchaToken: Option<&str>,
    category: Option<&str>,
    locale: Option<&str>,
    strategy: Option<&str>,
    utm_campaign: Option<&str>,
    utm_source: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PagespeedApiPagespeedResponseV5>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = pagespeedonline_pagespeedapi_runpagespeed_builder(
        client,
        url,
        captchaToken,
        category,
        locale,
        strategy,
        utm_campaign,
        utm_source,
    )?;
    pagespeedonline_pagespeedapi_runpagespeed_execute(builder)
}
