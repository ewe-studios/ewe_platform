//! Auto-generated API clients for gcp/pagespeedonline.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pagespeedonline_pagespeedapi_runpagespeed_execute()` to send, or `pagespeedonline_pagespeedapi_runpagespeed` for simplest API.

pub fn pagespeedonline_pagespeedapi_runpagespeed_builder(
    client: &SimpleHttpClient,
    captchaToken: &Option<Option<String>>,
    category: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    strategy: &Option<Option<String>>,
    url: &Option<Option<String>>,
    utm_campaign: &Option<Option<String>>,
    utm_source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://pagespeedonline.googleapis.com/pagespeedonline/v5/runPagespeed",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = captchaToken.as_ref() {
        query_parts.push(format!("captchaToken={}", val));
    }
    if let Some(val) = category.as_ref() {
        query_parts.push(format!("category={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = strategy.as_ref() {
        query_parts.push(format!("strategy={}", val));
    }
    if let Some(val) = url.as_ref() {
        query_parts.push(format!("url={}", val));
    }
    if let Some(val) = utm_campaign.as_ref() {
        query_parts.push(format!("utm_campaign={}", val));
    }
    if let Some(val) = utm_source.as_ref() {
        query_parts.push(format!("utm_source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pagespeedonline_pagespeedapi_runpagespeed_execute()` or `pagespeedonline_pagespeedapi_runpagespeed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pagespeedonline_pagespeedapi_runpagespeed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pagespeedonline_pagespeedapi_runpagespeed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PagespeedApiPagespeedResponseV5>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PagespeedApiPagespeedResponseV5 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pagespeedonline_pagespeedapi_runpagespeed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pagespeedonline_pagespeedapi_runpagespeed_task()`.
/// For the simplest API, use `pagespeedonline_pagespeedapi_runpagespeed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pagespeedonline_pagespeedapi_runpagespeed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pagespeedonline_pagespeedapi_runpagespeed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PagespeedApiPagespeedResponseV5>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = pagespeedonline_pagespeedapi_runpagespeed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pagespeedonline_pagespeedapi_runpagespeed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PagespeedonlinePagespeedapiRunpagespeedArgs {
    /// Query parameter: captchaToken
    pub captchaToken: Option<Option<String>>,
    /// Query parameter: category
    pub category: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: strategy
    pub strategy: Option<Option<String>>,
    /// Query parameter: url
    pub url: Option<Option<String>>,
    /// Query parameter: utm_campaign
    pub utm_campaign: Option<Option<String>>,
    /// Query parameter: utm_source
    pub utm_source: Option<Option<String>>,
}

/// GET pagespeedonline/v5/runPagespeed
/// Runs PageSpeed analysis on the page at the specified URL, and returns PageSpeed scores, a list of suggestions to make that page faster, and other information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pagespeedonline_pagespeedapi_runpagespeed_builder()` + `pagespeedonline_pagespeedapi_runpagespeed_execute()`.
/// For task-level control, use `pagespeedonline_pagespeedapi_runpagespeed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pagespeedonline_pagespeedapi_runpagespeed(
    client: &SimpleHttpClient,
    args: &PagespeedonlinePagespeedapiRunpagespeedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PagespeedApiPagespeedResponseV5>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = pagespeedonline_pagespeedapi_runpagespeed_builder(
        client,
        &args.captchaToken,
        &args.category,
        &args.locale,
        &args.strategy,
        &args.url,
        &args.utm_campaign,
        &args.utm_source,
    )?;
    pagespeedonline_pagespeedapi_runpagespeed_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for PagespeedApiPagespeedResponseV5
// =============================================================================

/// ResourceIdentifier implementation for PagespeedApiPagespeedResponseV5 with PagespeedonlinePagespeedapiRunpagespeedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PagespeedonlinePagespeedapiRunpagespeedArgs>
    for PagespeedApiPagespeedResponseV5
{
    fn generate_resource_id(&self, input: &PagespeedonlinePagespeedapiRunpagespeedArgs) -> String {
        "gcp::pagespeedonline::PagespeedApiPagespeedResponseV5".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pagespeedonline::PagespeedApiPagespeedResponseV5"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
