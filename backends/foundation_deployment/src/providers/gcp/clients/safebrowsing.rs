//! Auto-generated API clients for gcp/safebrowsing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hash_list_get_execute()` to send, or `safebrowsing_hash_list_get` for simplest API.

pub fn safebrowsing_hash_list_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    sizeConstraints_maxDatabaseEntries: Option<i32>,
    sizeConstraints_maxUpdateEntries: Option<i32>,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv5/hashList/{}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sizeConstraints_maxDatabaseEntries",
            sizeConstraints_maxDatabaseEntries
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sizeConstraints_maxUpdateEntries",
            sizeConstraints_maxUpdateEntries
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `safebrowsing_hash_list_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from safebrowsing_hash_list_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_list_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5HashList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5HashList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hash_list_get_builder()` + `safebrowsing_hash_list_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_list_get(
    client: &SimpleHttpClient,
    name: &str,
    sizeConstraints_maxDatabaseEntries: Option<i32>,
    sizeConstraints_maxUpdateEntries: Option<i32>,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5HashList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hash_list_get_builder(
        client,
        name,
        sizeConstraints_maxDatabaseEntries,
        sizeConstraints_maxUpdateEntries,
        version,
    )?;
    safebrowsing_hash_list_get_execute(builder)
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hash_lists_batch_get_execute()` to send, or `safebrowsing_hash_lists_batch_get` for simplest API.

pub fn safebrowsing_hash_lists_batch_get_builder(
    client: &SimpleHttpClient,
    names: Option<&str>,
    sizeConstraints_maxDatabaseEntries: Option<i32>,
    sizeConstraints_maxUpdateEntries: Option<i32>,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv5/hashLists:batchGet",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sizeConstraints_maxDatabaseEntries",
            sizeConstraints_maxDatabaseEntries
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sizeConstraints_maxUpdateEntries",
            sizeConstraints_maxUpdateEntries
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `safebrowsing_hash_lists_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from safebrowsing_hash_lists_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5BatchGetHashListsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5BatchGetHashListsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hash_lists_batch_get_builder()` + `safebrowsing_hash_lists_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_batch_get(
    client: &SimpleHttpClient,
    names: Option<&str>,
    sizeConstraints_maxDatabaseEntries: Option<i32>,
    sizeConstraints_maxUpdateEntries: Option<i32>,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5BatchGetHashListsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hash_lists_batch_get_builder(
        client,
        names,
        sizeConstraints_maxDatabaseEntries,
        sizeConstraints_maxUpdateEntries,
        version,
    )?;
    safebrowsing_hash_lists_batch_get_execute(builder)
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hash_lists_list_execute()` to send, or `safebrowsing_hash_lists_list` for simplest API.

pub fn safebrowsing_hash_lists_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv5/hashLists",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `safebrowsing_hash_lists_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from safebrowsing_hash_lists_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5ListHashListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5ListHashListsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hash_lists_list_builder()` + `safebrowsing_hash_lists_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5ListHashListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hash_lists_list_builder(client, pageSize, pageToken)?;
    safebrowsing_hash_lists_list_execute(builder)
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hashes_search_execute()` to send, or `safebrowsing_hashes_search` for simplest API.

pub fn safebrowsing_hashes_search_builder(
    client: &SimpleHttpClient,
    hashPrefixes: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv5/hashes:search",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hashPrefixes",
            hashPrefixes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `safebrowsing_hashes_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from safebrowsing_hashes_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hashes_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5SearchHashesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hashes_search_builder()` + `safebrowsing_hashes_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hashes_search(
    client: &SimpleHttpClient,
    hashPrefixes: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hashes_search_builder(client, hashPrefixes)?;
    safebrowsing_hashes_search_execute(builder)
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_urls_search_execute()` to send, or `safebrowsing_urls_search` for simplest API.

pub fn safebrowsing_urls_search_builder(
    client: &SimpleHttpClient,
    urls: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv5/urls:search",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "urls",
            urls.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `safebrowsing_urls_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from safebrowsing_urls_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_urls_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchUrlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5SearchUrlsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_urls_search_builder()` + `safebrowsing_urls_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_urls_search(
    client: &SimpleHttpClient,
    urls: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchUrlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_urls_search_builder(client, urls)?;
    safebrowsing_urls_search_execute(builder)
}
