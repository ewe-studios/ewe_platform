//! Auto-generated API clients for gcp/safebrowsing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hash_list_get_execute()` to send, or `safebrowsing_hash_list_get` for simplest API.

pub fn safebrowsing_hash_list_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    sizeConstraints_maxDatabaseEntries: &Option<Option<String>>,
    sizeConstraints_maxUpdateEntries: &Option<Option<String>>,
    version: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashList/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sizeConstraints_maxDatabaseEntries.as_ref() {
        query_parts.push(format!("sizeConstraints.maxDatabaseEntries={}", val));
    }
    if let Some(val) = sizeConstraints_maxUpdateEntries.as_ref() {
        query_parts.push(format!("sizeConstraints.maxUpdateEntries={}", val));
    }
    if let Some(val) = version.as_ref() {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `safebrowsing_hash_list_get_execute()` or `safebrowsing_hash_list_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hash_list_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_list_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleSecuritySafebrowsingV5HashList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5HashList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `safebrowsing_hash_list_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `safebrowsing_hash_list_get_task()`.
/// For the simplest API, use `safebrowsing_hash_list_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hash_list_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn safebrowsing_hash_list_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5HashList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = safebrowsing_hash_list_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`safebrowsing_hash_list_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SafebrowsingHashListGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: sizeConstraints_maxDatabaseEntries
    pub sizeConstraints_maxDatabaseEntries: Option<Option<String>>,
    /// Query parameter: sizeConstraints_maxUpdateEntries
    pub sizeConstraints_maxUpdateEntries: Option<Option<String>>,
    /// Query parameter: version
    pub version: Option<Option<String>>,
}

/// GET v5/hashList/{name}
/// Gets the latest contents of a hash list. A hash list may either by a threat list or a non-threat list such as the Global Cache. This is a standard Get method as defined by <https://google.aip.`dev/131`> and the HTTP method is also GET.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hash_list_get_builder()` + `safebrowsing_hash_list_get_execute()`.
/// For task-level control, use `safebrowsing_hash_list_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_list_get(
    client: &SimpleHttpClient,
    args: &SafebrowsingHashListGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5HashList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hash_list_get_builder(
        client,
        &args.name,
        &args.sizeConstraints_maxDatabaseEntries,
        &args.sizeConstraints_maxUpdateEntries,
        &args.version,
    )?;
    safebrowsing_hash_list_get_execute(builder)
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hash_lists_batch_get_execute()` to send, or `safebrowsing_hash_lists_batch_get` for simplest API.

pub fn safebrowsing_hash_lists_batch_get_builder<R>(
    client: &SimpleHttpClient<R>,
    names: &Option<Option<String>>,
    sizeConstraints_maxDatabaseEntries: &Option<Option<String>>,
    sizeConstraints_maxUpdateEntries: &Option<Option<String>>,
    version: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashLists:batchGet",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names.as_ref() {
        query_parts.push(format!("names={}", val));
    }
    if let Some(val) = sizeConstraints_maxDatabaseEntries.as_ref() {
        query_parts.push(format!("sizeConstraints.maxDatabaseEntries={}", val));
    }
    if let Some(val) = sizeConstraints_maxUpdateEntries.as_ref() {
        query_parts.push(format!("sizeConstraints.maxUpdateEntries={}", val));
    }
    if let Some(val) = version.as_ref() {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `safebrowsing_hash_lists_batch_get_execute()` or `safebrowsing_hash_lists_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hash_lists_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5BatchGetHashListsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5BatchGetHashListsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `safebrowsing_hash_lists_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `safebrowsing_hash_lists_batch_get_task()`.
/// For the simplest API, use `safebrowsing_hash_lists_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hash_lists_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn safebrowsing_hash_lists_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5BatchGetHashListsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = safebrowsing_hash_lists_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`safebrowsing_hash_lists_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SafebrowsingHashListsBatchGetArgs {
    /// Query parameter: names
    pub names: Option<Option<String>>,
    /// Query parameter: sizeConstraints_maxDatabaseEntries
    pub sizeConstraints_maxDatabaseEntries: Option<Option<String>>,
    /// Query parameter: sizeConstraints_maxUpdateEntries
    pub sizeConstraints_maxUpdateEntries: Option<Option<String>>,
    /// Query parameter: version
    pub version: Option<Option<String>>,
}

/// GET v5/hashLists:batchGet
/// Gets multiple hash lists at once. It is very common for a client to need to get multiple hash lists. Using this method is preferred over using the regular Get method multiple times. This is a standard batch Get method as defined by <https://google.aip.`dev/231`> and the HTTP method is also GET.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hash_lists_batch_get_builder()` + `safebrowsing_hash_lists_batch_get_execute()`.
/// For task-level control, use `safebrowsing_hash_lists_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_batch_get(
    client: &SimpleHttpClient,
    args: &SafebrowsingHashListsBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5BatchGetHashListsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hash_lists_batch_get_builder(
        client,
        &args.names,
        &args.sizeConstraints_maxDatabaseEntries,
        &args.sizeConstraints_maxUpdateEntries,
        &args.version,
    )?;
    safebrowsing_hash_lists_batch_get_execute(builder)
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hash_lists_list_execute()` to send, or `safebrowsing_hash_lists_list` for simplest API.

pub fn safebrowsing_hash_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashLists",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `safebrowsing_hash_lists_list_execute()` or `safebrowsing_hash_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hash_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5ListHashListsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5ListHashListsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `safebrowsing_hash_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `safebrowsing_hash_lists_list_task()`.
/// For the simplest API, use `safebrowsing_hash_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hash_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn safebrowsing_hash_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5ListHashListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = safebrowsing_hash_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`safebrowsing_hash_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SafebrowsingHashListsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v5/hashLists
/// Lists hash lists. In the V5 API, Google will never remove a hash list that has ever been returned by this method. This enables clients to skip using this method and simply hard-code all hash lists they need. This is a standard List method as defined by <https://google.aip.`dev/132`> and the HTTP method is GET.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hash_lists_list_builder()` + `safebrowsing_hash_lists_list_execute()`.
/// For task-level control, use `safebrowsing_hash_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hash_lists_list(
    client: &SimpleHttpClient,
    args: &SafebrowsingHashListsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5ListHashListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hash_lists_list_builder(client, &args.pageSize, &args.pageToken)?;
    safebrowsing_hash_lists_list_execute(builder)
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_hashes_search_execute()` to send, or `safebrowsing_hashes_search` for simplest API.

pub fn safebrowsing_hashes_search_builder<R>(
    client: &SimpleHttpClient<R>,
    hashPrefixes: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashes:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = hashPrefixes.as_ref() {
        query_parts.push(format!("hashPrefixes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `safebrowsing_hashes_search_execute()` or `safebrowsing_hashes_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hashes_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hashes_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchHashesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5SearchHashesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `safebrowsing_hashes_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `safebrowsing_hashes_search_task()`.
/// For the simplest API, use `safebrowsing_hashes_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_hashes_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn safebrowsing_hashes_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = safebrowsing_hashes_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`safebrowsing_hashes_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SafebrowsingHashesSearchArgs {
    /// Query parameter: hashPrefixes
    pub hashPrefixes: Option<Option<String>>,
}

/// GET v5/hashes:search
/// Searches for full hashes matching the specified prefixes. This is a custom method as defined by <https://google.aip.`dev/136`> (the custom method refers to this method having a custom name within Google's general API development nomenclature; it does not refer to using a custom HTTP method).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_hashes_search_builder()` + `safebrowsing_hashes_search_execute()`.
/// For task-level control, use `safebrowsing_hashes_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_hashes_search(
    client: &SimpleHttpClient,
    args: &SafebrowsingHashesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_hashes_search_builder(client, &args.hashPrefixes)?;
    safebrowsing_hashes_search_execute(builder)
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `safebrowsing_urls_search_execute()` to send, or `safebrowsing_urls_search` for simplest API.

pub fn safebrowsing_urls_search_builder<R>(
    client: &SimpleHttpClient<R>,
    urls: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/urls:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = urls.as_ref() {
        query_parts.push(format!("urls={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `safebrowsing_urls_search_execute()` or `safebrowsing_urls_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_urls_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_urls_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchUrlsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5SearchUrlsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `safebrowsing_urls_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `safebrowsing_urls_search_task()`.
/// For the simplest API, use `safebrowsing_urls_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `safebrowsing_urls_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn safebrowsing_urls_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchUrlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = safebrowsing_urls_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`safebrowsing_urls_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SafebrowsingUrlsSearchArgs {
    /// Query parameter: urls
    pub urls: Option<Option<String>>,
}

/// GET v5/urls:search
/// Searches for URLs matching known threats. Each URL and it's host-suffix and path-prefix expressions (up to a limited depth) are checked. This means that the response may contain URLs that were not included in the request, but are expressions of the requested URLs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `safebrowsing_urls_search_builder()` + `safebrowsing_urls_search_execute()`.
/// For task-level control, use `safebrowsing_urls_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn safebrowsing_urls_search(
    client: &SimpleHttpClient,
    args: &SafebrowsingUrlsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleSecuritySafebrowsingV5SearchUrlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = safebrowsing_urls_search_builder(client, &args.urls)?;
    safebrowsing_urls_search_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5HashList
// =============================================================================

/// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5HashList with SafebrowsingHashListGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SafebrowsingHashListGetArgs> for GoogleSecuritySafebrowsingV5HashList {
    fn generate_resource_id(&self, input: &SafebrowsingHashListGetArgs) -> String {
        format!(
            "gcp::safebrowsing::GoogleSecuritySafebrowsingV5HashList/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5HashList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5BatchGetHashListsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5BatchGetHashListsResponse with SafebrowsingHashListsBatchGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SafebrowsingHashListsBatchGetArgs>
    for GoogleSecuritySafebrowsingV5BatchGetHashListsResponse
{
    fn generate_resource_id(&self, input: &SafebrowsingHashListsBatchGetArgs) -> String {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5BatchGetHashListsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5BatchGetHashListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5ListHashListsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5ListHashListsResponse with SafebrowsingHashListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SafebrowsingHashListsListArgs>
    for GoogleSecuritySafebrowsingV5ListHashListsResponse
{
    fn generate_resource_id(&self, input: &SafebrowsingHashListsListArgs) -> String {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5ListHashListsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5ListHashListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5SearchHashesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5SearchHashesResponse with SafebrowsingHashesSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SafebrowsingHashesSearchArgs>
    for GoogleSecuritySafebrowsingV5SearchHashesResponse
{
    fn generate_resource_id(&self, input: &SafebrowsingHashesSearchArgs) -> String {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5SearchHashesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5SearchHashesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5SearchUrlsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleSecuritySafebrowsingV5SearchUrlsResponse with SafebrowsingUrlsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SafebrowsingUrlsSearchArgs>
    for GoogleSecuritySafebrowsingV5SearchUrlsResponse
{
    fn generate_resource_id(&self, input: &SafebrowsingUrlsSearchArgs) -> String {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5SearchUrlsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::safebrowsing::GoogleSecuritySafebrowsingV5SearchUrlsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
