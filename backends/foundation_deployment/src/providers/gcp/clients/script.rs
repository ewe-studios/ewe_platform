//! Auto-generated API clients for gcp/script.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/script`

#![cfg(feature = "gcp/script")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/processes
/// List information about processes made by or on behalf of a user, such as process type and current status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_processes_list_execute()` to send, or `script_processes_list` for simplest API.

pub fn script_processes_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    userProcessFilter_deploymentId: Option<&str>,
    userProcessFilter_endTime: Option<&str>,
    userProcessFilter_functionName: Option<&str>,
    userProcessFilter_projectName: Option<&str>,
    userProcessFilter_scriptId: Option<&str>,
    userProcessFilter_startTime: Option<&str>,
    userProcessFilter_statuses: Option<&str>,
    userProcessFilter_types: Option<&str>,
    userProcessFilter_userAccessLevels: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://script.googleapis.com/v1/processes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = userProcessFilter_deploymentId {
        query_parts.push(format!("userProcessFilter_deploymentId={}", val));
    }
    if let Some(val) = userProcessFilter_endTime {
        query_parts.push(format!("userProcessFilter_endTime={}", val));
    }
    if let Some(val) = userProcessFilter_functionName {
        query_parts.push(format!("userProcessFilter_functionName={}", val));
    }
    if let Some(val) = userProcessFilter_projectName {
        query_parts.push(format!("userProcessFilter_projectName={}", val));
    }
    if let Some(val) = userProcessFilter_scriptId {
        query_parts.push(format!("userProcessFilter_scriptId={}", val));
    }
    if let Some(val) = userProcessFilter_startTime {
        query_parts.push(format!("userProcessFilter_startTime={}", val));
    }
    if let Some(val) = userProcessFilter_statuses {
        query_parts.push(format!("userProcessFilter_statuses={}", val));
    }
    if let Some(val) = userProcessFilter_types {
        query_parts.push(format!("userProcessFilter_types={}", val));
    }
    if let Some(val) = userProcessFilter_userAccessLevels {
        query_parts.push(format!("userProcessFilter_userAccessLevels={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/processes
/// List information about processes made by or on behalf of a user, such as process type and current status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_processes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_processes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_processes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUserProcessesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserProcessesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_processes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProcessesListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: userProcessFilter_deploymentId
    pub userProcessFilter_deploymentId: Option<String>,
    /// Query parameter: userProcessFilter_endTime
    pub userProcessFilter_endTime: Option<String>,
    /// Query parameter: userProcessFilter_functionName
    pub userProcessFilter_functionName: Option<String>,
    /// Query parameter: userProcessFilter_projectName
    pub userProcessFilter_projectName: Option<String>,
    /// Query parameter: userProcessFilter_scriptId
    pub userProcessFilter_scriptId: Option<String>,
    /// Query parameter: userProcessFilter_startTime
    pub userProcessFilter_startTime: Option<String>,
    /// Query parameter: userProcessFilter_statuses
    pub userProcessFilter_statuses: Option<String>,
    /// Query parameter: userProcessFilter_types
    pub userProcessFilter_types: Option<String>,
    /// Query parameter: userProcessFilter_userAccessLevels
    pub userProcessFilter_userAccessLevels: Option<String>,
}

/// GET v1/processes
/// List information about processes made by or on behalf of a user, such as process type and current status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_processes_list_builder()` + `script_processes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_processes_list(
    client: &SimpleHttpClient,
    args: &ScriptProcessesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUserProcessesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = script_processes_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.userProcessFilter_deploymentId.as_deref(),
        args.userProcessFilter_endTime.as_deref(),
        args.userProcessFilter_functionName.as_deref(),
        args.userProcessFilter_projectName.as_deref(),
        args.userProcessFilter_scriptId.as_deref(),
        args.userProcessFilter_startTime.as_deref(),
        args.userProcessFilter_statuses.as_deref(),
        args.userProcessFilter_types.as_deref(),
        args.userProcessFilter_userAccessLevels.as_deref(),
    )?;
    script_processes_list_execute(builder)
}

/// GET v1/processes:listScriptProcesses
/// List information about a script's executed processes, such as process type and current status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_processes_list_script_processes_execute()` to send, or `script_processes_list_script_processes` for simplest API.

pub fn script_processes_list_script_processes_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    scriptId: Option<&str>,
    scriptProcessFilter_deploymentId: Option<&str>,
    scriptProcessFilter_endTime: Option<&str>,
    scriptProcessFilter_functionName: Option<&str>,
    scriptProcessFilter_startTime: Option<&str>,
    scriptProcessFilter_statuses: Option<&str>,
    scriptProcessFilter_types: Option<&str>,
    scriptProcessFilter_userAccessLevels: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://script.googleapis.com/v1/processes:listScriptProcesses",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = scriptId {
        query_parts.push(format!("scriptId={}", val));
    }
    if let Some(val) = scriptProcessFilter_deploymentId {
        query_parts.push(format!("scriptProcessFilter_deploymentId={}", val));
    }
    if let Some(val) = scriptProcessFilter_endTime {
        query_parts.push(format!("scriptProcessFilter_endTime={}", val));
    }
    if let Some(val) = scriptProcessFilter_functionName {
        query_parts.push(format!("scriptProcessFilter_functionName={}", val));
    }
    if let Some(val) = scriptProcessFilter_startTime {
        query_parts.push(format!("scriptProcessFilter_startTime={}", val));
    }
    if let Some(val) = scriptProcessFilter_statuses {
        query_parts.push(format!("scriptProcessFilter_statuses={}", val));
    }
    if let Some(val) = scriptProcessFilter_types {
        query_parts.push(format!("scriptProcessFilter_types={}", val));
    }
    if let Some(val) = scriptProcessFilter_userAccessLevels {
        query_parts.push(format!("scriptProcessFilter_userAccessLevels={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/processes:listScriptProcesses
/// List information about a script's executed processes, such as process type and current status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_processes_list_script_processes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_processes_list_script_processes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_processes_list_script_processes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListScriptProcessesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListScriptProcessesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_processes_list_script_processes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProcessesListScriptProcessesArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: scriptId
    pub scriptId: Option<String>,
    /// Query parameter: scriptProcessFilter_deploymentId
    pub scriptProcessFilter_deploymentId: Option<String>,
    /// Query parameter: scriptProcessFilter_endTime
    pub scriptProcessFilter_endTime: Option<String>,
    /// Query parameter: scriptProcessFilter_functionName
    pub scriptProcessFilter_functionName: Option<String>,
    /// Query parameter: scriptProcessFilter_startTime
    pub scriptProcessFilter_startTime: Option<String>,
    /// Query parameter: scriptProcessFilter_statuses
    pub scriptProcessFilter_statuses: Option<String>,
    /// Query parameter: scriptProcessFilter_types
    pub scriptProcessFilter_types: Option<String>,
    /// Query parameter: scriptProcessFilter_userAccessLevels
    pub scriptProcessFilter_userAccessLevels: Option<String>,
}

/// GET v1/processes:listScriptProcesses
/// List information about a script's executed processes, such as process type and current status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_processes_list_script_processes_builder()` + `script_processes_list_script_processes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_processes_list_script_processes(
    client: &SimpleHttpClient,
    args: &ScriptProcessesListScriptProcessesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListScriptProcessesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = script_processes_list_script_processes_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.scriptId.as_deref(),
        args.scriptProcessFilter_deploymentId.as_deref(),
        args.scriptProcessFilter_endTime.as_deref(),
        args.scriptProcessFilter_functionName.as_deref(),
        args.scriptProcessFilter_startTime.as_deref(),
        args.scriptProcessFilter_statuses.as_deref(),
        args.scriptProcessFilter_types.as_deref(),
        args.scriptProcessFilter_userAccessLevels.as_deref(),
    )?;
    script_processes_list_script_processes_execute(builder)
}

/// GET v1/projects
/// Creates a new, empty script project with no script files and a base manifest file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_create_execute()` to send, or `script_projects_create` for simplest API.

pub fn script_projects_create_builder(
    client: &SimpleHttpClient,
    body: &CreateProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://script.googleapis.com/v1/projects",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects
/// Creates a new, empty script project with no script files and a base manifest file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsCreateArgs {
    /// Request body.
    pub body: CreateProjectRequest,
}

/// GET v1/projects
/// Creates a new, empty script project with no script files and a base manifest file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_create_builder()` + `script_projects_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_create(
    client: &SimpleHttpClient,
    args: &ScriptProjectsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_create_builder(client, &args.body)?;
    script_projects_create_execute(builder)
}

/// GET v1/projects/{scriptId}
/// Gets a script project's metadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_get_execute()` to send, or `script_projects_get` for simplest API.

pub fn script_projects_get_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://script.googleapis.com/v1/projects/{}", scriptId,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}
/// Gets a script project's metadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsGetArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
}

/// GET v1/projects/{scriptId}
/// Gets a script project's metadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_get_builder()` + `script_projects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_get(
    client: &SimpleHttpClient,
    args: &ScriptProjectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_get_builder(client, &args.scriptId)?;
    script_projects_get_execute(builder)
}

/// GET v1/projects/{scriptId}/content
/// Gets the content of the script project, including the code source and metadata for each script file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_get_content_execute()` to send, or `script_projects_get_content` for simplest API.

pub fn script_projects_get_content_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    versionNumber: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/content",
        scriptId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = versionNumber {
        query_parts.push(format!("versionNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/content
/// Gets the content of the script project, including the code source and metadata for each script file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_get_content_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_get_content_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_get_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Content>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Content = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_get_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsGetContentArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Query parameter: versionNumber
    pub versionNumber: Option<i32>,
}

/// GET v1/projects/{scriptId}/content
/// Gets the content of the script project, including the code source and metadata for each script file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_get_content_builder()` + `script_projects_get_content_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_get_content(
    client: &SimpleHttpClient,
    args: &ScriptProjectsGetContentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Content>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_get_content_builder(client, &args.scriptId, args.versionNumber)?;
    script_projects_get_content_execute(builder)
}

/// GET v1/projects/{scriptId}/metrics
/// Get metrics data for scripts, such as number of executions and active users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_get_metrics_execute()` to send, or `script_projects_get_metrics` for simplest API.

pub fn script_projects_get_metrics_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    metricsFilter_deploymentId: Option<&str>,
    metricsGranularity: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/metrics",
        scriptId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = metricsFilter_deploymentId {
        query_parts.push(format!("metricsFilter_deploymentId={}", val));
    }
    if let Some(val) = metricsGranularity {
        query_parts.push(format!("metricsGranularity={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/metrics
/// Get metrics data for scripts, such as number of executions and active users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_get_metrics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_get_metrics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_get_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Metrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_get_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsGetMetricsArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Query parameter: metricsFilter_deploymentId
    pub metricsFilter_deploymentId: Option<String>,
    /// Query parameter: metricsGranularity
    pub metricsGranularity: Option<String>,
}

/// GET v1/projects/{scriptId}/metrics
/// Get metrics data for scripts, such as number of executions and active users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_get_metrics_builder()` + `script_projects_get_metrics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_get_metrics(
    client: &SimpleHttpClient,
    args: &ScriptProjectsGetMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_get_metrics_builder(
        client,
        &args.scriptId,
        args.metricsFilter_deploymentId.as_deref(),
        args.metricsGranularity.as_deref(),
    )?;
    script_projects_get_metrics_execute(builder)
}

/// GET v1/projects/{scriptId}/content
/// Updates the content of the specified script project. This content is stored as the HEAD version, and is used when the script is executed as a trigger, in the script editor, in add-on preview mode, or as a web app or Apps Script API in development mode. This clears all the existing files in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_update_content_execute()` to send, or `script_projects_update_content` for simplest API.

pub fn script_projects_update_content_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    body: &Content,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/content",
        scriptId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{scriptId}/content
/// Updates the content of the specified script project. This content is stored as the HEAD version, and is used when the script is executed as a trigger, in the script editor, in add-on preview mode, or as a web app or Apps Script API in development mode. This clears all the existing files in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_update_content_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_update_content_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_update_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Content>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Content = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_update_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsUpdateContentArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Request body.
    pub body: Content,
}

/// GET v1/projects/{scriptId}/content
/// Updates the content of the specified script project. This content is stored as the HEAD version, and is used when the script is executed as a trigger, in the script editor, in add-on preview mode, or as a web app or Apps Script API in development mode. This clears all the existing files in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_update_content_builder()` + `script_projects_update_content_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_update_content(
    client: &SimpleHttpClient,
    args: &ScriptProjectsUpdateContentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Content>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_update_content_builder(client, &args.scriptId, &args.body)?;
    script_projects_update_content_execute(builder)
}

/// GET v1/projects/{scriptId}/deployments
/// Creates a deployment of an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_deployments_create_execute()` to send, or `script_projects_deployments_create` for simplest API.

pub fn script_projects_deployments_create_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    body: &DeploymentConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments",
        scriptId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{scriptId}/deployments
/// Creates a deployment of an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deployment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsCreateArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Request body.
    pub body: DeploymentConfig,
}

/// GET v1/projects/{scriptId}/deployments
/// Creates a deployment of an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_deployments_create_builder()` + `script_projects_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_create(
    client: &SimpleHttpClient,
    args: &ScriptProjectsDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deployment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_deployments_create_builder(client, &args.scriptId, &args.body)?;
    script_projects_deployments_create_execute(builder)
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Deletes a deployment of an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_deployments_delete_execute()` to send, or `script_projects_deployments_delete` for simplest API.

pub fn script_projects_deployments_delete_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    deploymentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments/{}",
        scriptId, deploymentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Deletes a deployment of an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_deployments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_deployments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsDeleteArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Path parameter: deploymentId
    pub deploymentId: String,
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Deletes a deployment of an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_deployments_delete_builder()` + `script_projects_deployments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_delete(
    client: &SimpleHttpClient,
    args: &ScriptProjectsDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        script_projects_deployments_delete_builder(client, &args.scriptId, &args.deploymentId)?;
    script_projects_deployments_delete_execute(builder)
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Gets a deployment of an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_deployments_get_execute()` to send, or `script_projects_deployments_get` for simplest API.

pub fn script_projects_deployments_get_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    deploymentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments/{}",
        scriptId, deploymentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Gets a deployment of an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deployment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsGetArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Path parameter: deploymentId
    pub deploymentId: String,
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Gets a deployment of an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_deployments_get_builder()` + `script_projects_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_get(
    client: &SimpleHttpClient,
    args: &ScriptProjectsDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deployment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        script_projects_deployments_get_builder(client, &args.scriptId, &args.deploymentId)?;
    script_projects_deployments_get_execute(builder)
}

/// GET v1/projects/{scriptId}/deployments
/// Lists the deployments of an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_deployments_list_execute()` to send, or `script_projects_deployments_list` for simplest API.

pub fn script_projects_deployments_list_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments",
        scriptId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/deployments
/// Lists the deployments of an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDeploymentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsListArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{scriptId}/deployments
/// Lists the deployments of an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_deployments_list_builder()` + `script_projects_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_list(
    client: &SimpleHttpClient,
    args: &ScriptProjectsDeploymentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDeploymentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = script_projects_deployments_list_builder(
        client,
        &args.scriptId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    script_projects_deployments_list_execute(builder)
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Updates a deployment of an Apps Script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_deployments_update_execute()` to send, or `script_projects_deployments_update` for simplest API.

pub fn script_projects_deployments_update_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    deploymentId: &str,
    body: &UpdateDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments/{}",
        scriptId, deploymentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Updates a deployment of an Apps Script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_deployments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_deployments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deployment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_deployments_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsUpdateArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Path parameter: deploymentId
    pub deploymentId: String,
    /// Request body.
    pub body: UpdateDeploymentRequest,
}

/// GET v1/projects/{scriptId}/deployments/{deploymentId}
/// Updates a deployment of an Apps Script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_deployments_update_builder()` + `script_projects_deployments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_deployments_update(
    client: &SimpleHttpClient,
    args: &ScriptProjectsDeploymentsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deployment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_deployments_update_builder(
        client,
        &args.scriptId,
        &args.deploymentId,
        &args.body,
    )?;
    script_projects_deployments_update_execute(builder)
}

/// GET v1/projects/{scriptId}/versions
/// Creates a new immutable version using the current code, with a unique version number.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_versions_create_execute()` to send, or `script_projects_versions_create` for simplest API.

pub fn script_projects_versions_create_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    body: &Version,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/versions",
        scriptId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{scriptId}/versions
/// Creates a new immutable version using the current code, with a unique version number.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_versions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_versions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Version>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Version = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsVersionsCreateArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Request body.
    pub body: Version,
}

/// GET v1/projects/{scriptId}/versions
/// Creates a new immutable version using the current code, with a unique version number.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_versions_create_builder()` + `script_projects_versions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_versions_create(
    client: &SimpleHttpClient,
    args: &ScriptProjectsVersionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Version>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_projects_versions_create_builder(client, &args.scriptId, &args.body)?;
    script_projects_versions_create_execute(builder)
}

/// GET v1/projects/{scriptId}/versions/{versionNumber}
/// Gets a version of a script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_versions_get_execute()` to send, or `script_projects_versions_get` for simplest API.

pub fn script_projects_versions_get_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    versionNumber: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/versions/{}",
        scriptId, versionNumber,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/versions/{versionNumber}
/// Gets a version of a script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Version>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Version = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsVersionsGetArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Path parameter: versionNumber
    pub versionNumber: String,
}

/// GET v1/projects/{scriptId}/versions/{versionNumber}
/// Gets a version of a script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_versions_get_builder()` + `script_projects_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_versions_get(
    client: &SimpleHttpClient,
    args: &ScriptProjectsVersionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Version>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        script_projects_versions_get_builder(client, &args.scriptId, &args.versionNumber)?;
    script_projects_versions_get_execute(builder)
}

/// GET v1/projects/{scriptId}/versions
/// List the versions of a script project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_projects_versions_list_execute()` to send, or `script_projects_versions_list` for simplest API.

pub fn script_projects_versions_list_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://script.googleapis.com/v1/projects/{}/versions",
        scriptId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{scriptId}/versions
/// List the versions of a script project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_projects_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_projects_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_projects_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsVersionsListArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{scriptId}/versions
/// List the versions of a script project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_projects_versions_list_builder()` + `script_projects_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_projects_versions_list(
    client: &SimpleHttpClient,
    args: &ScriptProjectsVersionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = script_projects_versions_list_builder(
        client,
        &args.scriptId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    script_projects_versions_list_execute(builder)
}

/// GET v1/scripts/{scriptId}:run
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `script_scripts_run_execute()` to send, or `script_scripts_run` for simplest API.

pub fn script_scripts_run_builder(
    client: &SimpleHttpClient,
    scriptId: &str,
    body: &ExecutionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://script.googleapis.com/v1/scripts/{}:run", scriptId,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/scripts/{scriptId}:run
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `script_scripts_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from script_scripts_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_scripts_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`script_scripts_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptScriptsRunArgs {
    /// Path parameter: scriptId
    pub scriptId: String,
    /// Request body.
    pub body: ExecutionRequest,
}

/// GET v1/scripts/{scriptId}:run
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `script_scripts_run_builder()` + `script_scripts_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn script_scripts_run(
    client: &SimpleHttpClient,
    args: &ScriptScriptsRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = script_scripts_run_builder(client, &args.scriptId, &args.body)?;
    script_scripts_run_execute(builder)
}
