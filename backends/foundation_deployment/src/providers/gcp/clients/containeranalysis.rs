//! Auto-generated API clients for gcp/containeranalysis.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/notes:batchCreate
/// Creates new notes in batch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `containeranalysis_projects_notes_batch_create_execute()` to send, or `containeranalysis_projects_notes_batch_create` for simplest API.

pub fn containeranalysis_projects_notes_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &BatchCreateNotesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://containeranalysis.googleapis.com/v1/projects/{}/notes:batchCreate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notes:batchCreate
/// Creates new notes in batch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `containeranalysis_projects_notes_batch_create_execute()` or `containeranalysis_projects_notes_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_notes_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_notes_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateNotesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateNotesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/notes:batchCreate
/// Creates new notes in batch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `containeranalysis_projects_notes_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `containeranalysis_projects_notes_batch_create_task()`.
/// For the simplest API, use `containeranalysis_projects_notes_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_notes_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn containeranalysis_projects_notes_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchCreateNotesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = containeranalysis_projects_notes_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`containeranalysis_projects_notes_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchCreateNotesRequest,
}

/// GET v1/projects/{projectsId}/notes:batchCreate
/// Creates new notes in batch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `containeranalysis_projects_notes_batch_create_builder()` + `containeranalysis_projects_notes_batch_create_execute()`.
/// For task-level control, use `containeranalysis_projects_notes_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_notes_batch_create(
    client: &SimpleHttpClient,
    args: &ContaineranalysisProjectsNotesBatchCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchCreateNotesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        containeranalysis_projects_notes_batch_create_builder(client, &args.parent, &args.body)?;
    containeranalysis_projects_notes_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/notes
/// Creates a new note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `containeranalysis_projects_notes_create_execute()` to send, or `containeranalysis_projects_notes_create` for simplest API.

pub fn containeranalysis_projects_notes_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    noteId: &Option<String>,
    body: &Note,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/projects/{}/notes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = noteId.as_ref() {
        query_parts.push(format!("noteId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notes
/// Creates a new note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `containeranalysis_projects_notes_create_execute()` or `containeranalysis_projects_notes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_notes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_notes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/notes
/// Creates a new note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `containeranalysis_projects_notes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `containeranalysis_projects_notes_create_task()`.
/// For the simplest API, use `containeranalysis_projects_notes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_notes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn containeranalysis_projects_notes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = containeranalysis_projects_notes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`containeranalysis_projects_notes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: noteId
    pub noteId: Option<String>,
    /// Request body.
    pub body: Note,
}

/// GET v1/projects/{projectsId}/notes
/// Creates a new note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `containeranalysis_projects_notes_create_builder()` + `containeranalysis_projects_notes_create_execute()`.
/// For task-level control, use `containeranalysis_projects_notes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_notes_create(
    client: &SimpleHttpClient,
    args: &ContaineranalysisProjectsNotesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Note>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = containeranalysis_projects_notes_create_builder(
        client,
        &args.parent,
        &args.noteId,
        &args.body,
    )?;
    containeranalysis_projects_notes_create_execute(builder)
}

/// GET v1/projects/{projectsId}/occurrences:batchCreate
/// Creates new occurrences in batch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `containeranalysis_projects_occurrences_batch_create_execute()` to send, or `containeranalysis_projects_occurrences_batch_create` for simplest API.

pub fn containeranalysis_projects_occurrences_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &BatchCreateOccurrencesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://containeranalysis.googleapis.com/v1/projects/{}/occurrences:batchCreate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/occurrences:batchCreate
/// Creates new occurrences in batch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `containeranalysis_projects_occurrences_batch_create_execute()` or `containeranalysis_projects_occurrences_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_occurrences_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_occurrences_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateOccurrencesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateOccurrencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/occurrences:batchCreate
/// Creates new occurrences in batch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `containeranalysis_projects_occurrences_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `containeranalysis_projects_occurrences_batch_create_task()`.
/// For the simplest API, use `containeranalysis_projects_occurrences_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_occurrences_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn containeranalysis_projects_occurrences_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateOccurrencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = containeranalysis_projects_occurrences_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`containeranalysis_projects_occurrences_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchCreateOccurrencesRequest,
}

/// GET v1/projects/{projectsId}/occurrences:batchCreate
/// Creates new occurrences in batch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `containeranalysis_projects_occurrences_batch_create_builder()` + `containeranalysis_projects_occurrences_batch_create_execute()`.
/// For task-level control, use `containeranalysis_projects_occurrences_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_occurrences_batch_create(
    client: &SimpleHttpClient,
    args: &ContaineranalysisProjectsOccurrencesBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateOccurrencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = containeranalysis_projects_occurrences_batch_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    containeranalysis_projects_occurrences_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/occurrences
/// Creates a new occurrence.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `containeranalysis_projects_occurrences_create_execute()` to send, or `containeranalysis_projects_occurrences_create` for simplest API.

pub fn containeranalysis_projects_occurrences_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &Occurrence,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://containeranalysis.googleapis.com/v1/projects/{}/occurrences",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/occurrences
/// Creates a new occurrence.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `containeranalysis_projects_occurrences_create_execute()` or `containeranalysis_projects_occurrences_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_occurrences_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_occurrences_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Occurrence = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/occurrences
/// Creates a new occurrence.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `containeranalysis_projects_occurrences_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `containeranalysis_projects_occurrences_create_task()`.
/// For the simplest API, use `containeranalysis_projects_occurrences_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_occurrences_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn containeranalysis_projects_occurrences_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Occurrence>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = containeranalysis_projects_occurrences_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`containeranalysis_projects_occurrences_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Occurrence,
}

/// GET v1/projects/{projectsId}/occurrences
/// Creates a new occurrence.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `containeranalysis_projects_occurrences_create_builder()` + `containeranalysis_projects_occurrences_create_execute()`.
/// For task-level control, use `containeranalysis_projects_occurrences_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_occurrences_create(
    client: &SimpleHttpClient,
    args: &ContaineranalysisProjectsOccurrencesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Occurrence>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        containeranalysis_projects_occurrences_create_builder(client, &args.parent, &args.body)?;
    containeranalysis_projects_occurrences_create_execute(builder)
}

/// GET v1/projects/{projectsId}/occurrences:vulnerabilitySummary
/// Gets a summary of the number and severity of occurrences.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `containeranalysis_projects_occurrences_get_vulnerability_summary_execute()` to send, or `containeranalysis_projects_occurrences_get_vulnerability_summary` for simplest API.

pub fn containeranalysis_projects_occurrences_get_vulnerability_summary_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    returnPartialSuccess: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/projects/{}/occurrences:vulnerabilitySummary",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/occurrences:vulnerabilitySummary
/// Gets a summary of the number and severity of occurrences.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `containeranalysis_projects_occurrences_get_vulnerability_summary_execute()` or `containeranalysis_projects_occurrences_get_vulnerability_summary`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_occurrences_get_vulnerability_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_occurrences_get_vulnerability_summary_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VulnerabilityOccurrencesSummary>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VulnerabilityOccurrencesSummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/occurrences:vulnerabilitySummary
/// Gets a summary of the number and severity of occurrences.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `containeranalysis_projects_occurrences_get_vulnerability_summary_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `containeranalysis_projects_occurrences_get_vulnerability_summary_task()`.
/// For the simplest API, use `containeranalysis_projects_occurrences_get_vulnerability_summary()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `containeranalysis_projects_occurrences_get_vulnerability_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn containeranalysis_projects_occurrences_get_vulnerability_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VulnerabilityOccurrencesSummary>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = containeranalysis_projects_occurrences_get_vulnerability_summary_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`containeranalysis_projects_occurrences_get_vulnerability_summary`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesGetVulnerabilitySummaryArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/occurrences:vulnerabilitySummary
/// Gets a summary of the number and severity of occurrences.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `containeranalysis_projects_occurrences_get_vulnerability_summary_builder()` + `containeranalysis_projects_occurrences_get_vulnerability_summary_execute()`.
/// For task-level control, use `containeranalysis_projects_occurrences_get_vulnerability_summary_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn containeranalysis_projects_occurrences_get_vulnerability_summary(
    client: &SimpleHttpClient,
    args: &ContaineranalysisProjectsOccurrencesGetVulnerabilitySummaryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VulnerabilityOccurrencesSummary>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = containeranalysis_projects_occurrences_get_vulnerability_summary_builder(
        client,
        &args.parent,
        &args.filter,
        &args.returnPartialSuccess,
    )?;
    containeranalysis_projects_occurrences_get_vulnerability_summary_execute(builder)
}
