//! Auto-generated API clients for gcp/content.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET accounts/authinfo
/// Returns information about the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_authinfo_execute()` to send, or `content_accounts_authinfo` for simplest API.

pub fn content_accounts_authinfo_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accounts/authinfo",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET accounts/authinfo
/// Returns information about the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_authinfo_execute()` or `content_accounts_authinfo`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_authinfo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_authinfo_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsAuthInfoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsAuthInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/authinfo
/// Returns information about the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_authinfo_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_authinfo_task()`.
/// For the simplest API, use `content_accounts_authinfo()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_authinfo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_authinfo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsAuthInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_authinfo_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accounts/authinfo
/// Returns information about the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_authinfo_builder()` + `content_accounts_authinfo_execute()`.
/// For task-level control, use `content_accounts_authinfo_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_authinfo(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsAuthInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_authinfo_builder(client)?;
    content_accounts_authinfo_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}/claimwebsite
/// Claims the website of a Merchant Center sub-account. Merchant accounts with approved third-party CSSs aren't required to claim a website.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_claimwebsite_execute()` to send, or `content_accounts_claimwebsite` for simplest API.

pub fn content_accounts_claimwebsite_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    overwrite: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/claimwebsite",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = overwrite {
        query_parts.push(format!("overwrite={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accounts/{accountId}/claimwebsite
/// Claims the website of a Merchant Center sub-account. Merchant accounts with approved third-party CSSs aren't required to claim a website.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_claimwebsite_execute()` or `content_accounts_claimwebsite`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_claimwebsite_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_claimwebsite_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsClaimWebsiteResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsClaimWebsiteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}/claimwebsite
/// Claims the website of a Merchant Center sub-account. Merchant accounts with approved third-party CSSs aren't required to claim a website.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_claimwebsite_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_claimwebsite_task()`.
/// For the simplest API, use `content_accounts_claimwebsite()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_claimwebsite_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_claimwebsite_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountsClaimWebsiteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_claimwebsite_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_claimwebsite`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsClaimwebsiteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: overwrite
    pub overwrite: Option<bool>,
}

/// GET {merchantId}/accounts/{accountId}/claimwebsite
/// Claims the website of a Merchant Center sub-account. Merchant accounts with approved third-party CSSs aren't required to claim a website.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_claimwebsite_builder()` + `content_accounts_claimwebsite_execute()`.
/// For task-level control, use `content_accounts_claimwebsite_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_claimwebsite(
    client: &SimpleHttpClient,
    args: &ContentAccountsClaimwebsiteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountsClaimWebsiteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_claimwebsite_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        args.overwrite.clone(),
    )?;
    content_accounts_claimwebsite_execute(builder)
}

/// GET accounts/batch
/// Retrieves, inserts, updates, and deletes multiple Merchant Center (sub-)accounts in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_custombatch_execute()` to send, or `content_accounts_custombatch` for simplest API.

pub fn content_accounts_custombatch_builder(
    client: &SimpleHttpClient,
    body: &AccountsCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accounts/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accounts/batch
/// Retrieves, inserts, updates, and deletes multiple Merchant Center (sub-)accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_custombatch_execute()` or `content_accounts_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/batch
/// Retrieves, inserts, updates, and deletes multiple Merchant Center (sub-)accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_custombatch_task()`.
/// For the simplest API, use `content_accounts_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsCustombatchArgs {
    /// Request body.
    pub body: AccountsCustomBatchRequest,
}

/// GET accounts/batch
/// Retrieves, inserts, updates, and deletes multiple Merchant Center (sub-)accounts in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_custombatch_builder()` + `content_accounts_custombatch_execute()`.
/// For task-level control, use `content_accounts_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_custombatch(
    client: &SimpleHttpClient,
    args: &ContentAccountsCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_custombatch_builder(client, &args.body)?;
    content_accounts_custombatch_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}
/// Deletes a Merchant Center sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_delete_execute()` to send, or `content_accounts_delete` for simplest API.

pub fn content_accounts_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accounts/{accountId}
/// Deletes a Merchant Center sub-account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_delete_execute()` or `content_accounts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}
/// Deletes a Merchant Center sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_delete_task()`.
/// For the simplest API, use `content_accounts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_accounts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET {merchantId}/accounts/{accountId}
/// Deletes a Merchant Center sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_delete_builder()` + `content_accounts_delete_execute()`.
/// For task-level control, use `content_accounts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_delete(
    client: &SimpleHttpClient,
    args: &ContentAccountsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_accounts_delete_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        args.force.clone(),
    )?;
    content_accounts_delete_execute(builder)
}

/// GET {merchantId}/accounts
/// Creates a Merchant Center sub-account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_insert_execute()` to send, or `content_accounts_insert` for simplest API.

pub fn content_accounts_insert_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &Account,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/accounts
/// Creates a Merchant Center sub-account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_insert_execute()` or `content_accounts_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts
/// Creates a Merchant Center sub-account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_insert_task()`.
/// For the simplest API, use `content_accounts_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_accounts_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsInsertArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: Account,
}

/// GET {merchantId}/accounts
/// Creates a Merchant Center sub-account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_insert_builder()` + `content_accounts_insert_execute()`.
/// For task-level control, use `content_accounts_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_insert(
    client: &SimpleHttpClient,
    args: &ContentAccountsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_accounts_insert_builder(client, args.merchantId.clone(), &args.body)?;
    content_accounts_insert_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}/link
/// Performs an action on a link between two Merchant Center accounts, namely `accountId` and `linkedAccountId`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_link_execute()` to send, or `content_accounts_link` for simplest API.

pub fn content_accounts_link_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    body: &AccountsLinkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/link",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/accounts/{accountId}/link
/// Performs an action on a link between two Merchant Center accounts, namely `accountId` and `linkedAccountId`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_link_execute()` or `content_accounts_link`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_link_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_link_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsLinkResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsLinkResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}/link
/// Performs an action on a link between two Merchant Center accounts, namely `accountId` and `linkedAccountId`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_link_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_link_task()`.
/// For the simplest API, use `content_accounts_link()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_link_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_link_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsLinkResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_link_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_link`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsLinkArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: AccountsLinkRequest,
}

/// GET {merchantId}/accounts/{accountId}/link
/// Performs an action on a link between two Merchant Center accounts, namely `accountId` and `linkedAccountId`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_link_builder()` + `content_accounts_link_execute()`.
/// For task-level control, use `content_accounts_link_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_link(
    client: &SimpleHttpClient,
    args: &ContentAccountsLinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsLinkResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_link_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        &args.body,
    )?;
    content_accounts_link_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}/listlinks
/// Returns the list of accounts linked to your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_listlinks_execute()` to send, or `content_accounts_listlinks` for simplest API.

pub fn content_accounts_listlinks_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/listlinks",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accounts/{accountId}/listlinks
/// Returns the list of accounts linked to your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_listlinks_execute()` or `content_accounts_listlinks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_listlinks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_listlinks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsListLinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}/listlinks
/// Returns the list of accounts linked to your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_listlinks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_listlinks_task()`.
/// For the simplest API, use `content_accounts_listlinks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_listlinks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_listlinks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_listlinks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_listlinks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsListlinksArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/accounts/{accountId}/listlinks
/// Returns the list of accounts linked to your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_listlinks_builder()` + `content_accounts_listlinks_execute()`.
/// For task-level control, use `content_accounts_listlinks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_listlinks(
    client: &SimpleHttpClient,
    args: &ContentAccountsListlinksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_listlinks_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    content_accounts_listlinks_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}/requestphoneverification
/// Request verification code to start phone verification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_requestphoneverification_execute()` to send, or `content_accounts_requestphoneverification` for simplest API.

pub fn content_accounts_requestphoneverification_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    body: &RequestPhoneVerificationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/requestphoneverification",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/accounts/{accountId}/requestphoneverification
/// Request verification code to start phone verification.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_requestphoneverification_execute()` or `content_accounts_requestphoneverification`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_requestphoneverification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_requestphoneverification_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RequestPhoneVerificationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestPhoneVerificationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}/requestphoneverification
/// Request verification code to start phone verification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_requestphoneverification_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_requestphoneverification_task()`.
/// For the simplest API, use `content_accounts_requestphoneverification()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_requestphoneverification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_requestphoneverification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RequestPhoneVerificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_requestphoneverification_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_requestphoneverification`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsRequestphoneverificationArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: RequestPhoneVerificationRequest,
}

/// GET {merchantId}/accounts/{accountId}/requestphoneverification
/// Request verification code to start phone verification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_requestphoneverification_builder()` + `content_accounts_requestphoneverification_execute()`.
/// For task-level control, use `content_accounts_requestphoneverification_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_requestphoneverification(
    client: &SimpleHttpClient,
    args: &ContentAccountsRequestphoneverificationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RequestPhoneVerificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_requestphoneverification_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        &args.body,
    )?;
    content_accounts_requestphoneverification_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}/updatelabels
/// Updates labels that are assigned to the Merchant Center account by CSS user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_updatelabels_execute()` to send, or `content_accounts_updatelabels` for simplest API.

pub fn content_accounts_updatelabels_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    body: &AccountsUpdateLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/updatelabels",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/accounts/{accountId}/updatelabels
/// Updates labels that are assigned to the Merchant Center account by CSS user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_updatelabels_execute()` or `content_accounts_updatelabels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_updatelabels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_updatelabels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsUpdateLabelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsUpdateLabelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}/updatelabels
/// Updates labels that are assigned to the Merchant Center account by CSS user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_updatelabels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_updatelabels_task()`.
/// For the simplest API, use `content_accounts_updatelabels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_updatelabels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_updatelabels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountsUpdateLabelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_updatelabels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_updatelabels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsUpdatelabelsArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: AccountsUpdateLabelsRequest,
}

/// GET {merchantId}/accounts/{accountId}/updatelabels
/// Updates labels that are assigned to the Merchant Center account by CSS user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_updatelabels_builder()` + `content_accounts_updatelabels_execute()`.
/// For task-level control, use `content_accounts_updatelabels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_updatelabels(
    client: &SimpleHttpClient,
    args: &ContentAccountsUpdatelabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountsUpdateLabelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_updatelabels_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        &args.body,
    )?;
    content_accounts_updatelabels_execute(builder)
}

/// GET {merchantId}/accounts/{accountId}/verifyphonenumber
/// Validates verification code to verify phone number for the account. If successful this will overwrite the value of accounts.businessinformation.`phoneNumber`. Only verified phone number will replace an existing verified phone number.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_verifyphonenumber_execute()` to send, or `content_accounts_verifyphonenumber` for simplest API.

pub fn content_accounts_verifyphonenumber_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    body: &VerifyPhoneNumberRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/verifyphonenumber",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/accounts/{accountId}/verifyphonenumber
/// Validates verification code to verify phone number for the account. If successful this will overwrite the value of accounts.businessinformation.`phoneNumber`. Only verified phone number will replace an existing verified phone number.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_verifyphonenumber_execute()` or `content_accounts_verifyphonenumber`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_verifyphonenumber_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_verifyphonenumber_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyPhoneNumberResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyPhoneNumberResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounts/{accountId}/verifyphonenumber
/// Validates verification code to verify phone number for the account. If successful this will overwrite the value of accounts.businessinformation.`phoneNumber`. Only verified phone number will replace an existing verified phone number.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_verifyphonenumber_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_verifyphonenumber_task()`.
/// For the simplest API, use `content_accounts_verifyphonenumber()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_verifyphonenumber_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_verifyphonenumber_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyPhoneNumberResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_verifyphonenumber_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_verifyphonenumber`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsVerifyphonenumberArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: VerifyPhoneNumberRequest,
}

/// GET {merchantId}/accounts/{accountId}/verifyphonenumber
/// Validates verification code to verify phone number for the account. If successful this will overwrite the value of accounts.businessinformation.`phoneNumber`. Only verified phone number will replace an existing verified phone number.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_verifyphonenumber_builder()` + `content_accounts_verifyphonenumber_execute()`.
/// For task-level control, use `content_accounts_verifyphonenumber_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_verifyphonenumber(
    client: &SimpleHttpClient,
    args: &ContentAccountsVerifyphonenumberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyPhoneNumberResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounts_verifyphonenumber_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        &args.body,
    )?;
    content_accounts_verifyphonenumber_execute(builder)
}

/// GET accounts/{accountId}/credentials
/// Uploads credentials for the Merchant Center account. If credentials already exist for this Merchant Center account and purpose, this method updates them.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_credentials_create_execute()` to send, or `content_accounts_credentials_create` for simplest API.

pub fn content_accounts_credentials_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    body: &AccountCredentials,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/credentials",
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accounts/{accountId}/credentials
/// Uploads credentials for the Merchant Center account. If credentials already exist for this Merchant Center account and purpose, this method updates them.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_credentials_create_execute()` or `content_accounts_credentials_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_credentials_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_credentials_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountCredentials>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountCredentials = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/{accountId}/credentials
/// Uploads credentials for the Merchant Center account. If credentials already exist for this Merchant Center account and purpose, this method updates them.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_credentials_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_credentials_create_task()`.
/// For the simplest API, use `content_accounts_credentials_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_credentials_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_credentials_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountCredentials>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_credentials_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_credentials_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsCredentialsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: AccountCredentials,
}

/// GET accounts/{accountId}/credentials
/// Uploads credentials for the Merchant Center account. If credentials already exist for this Merchant Center account and purpose, this method updates them.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_credentials_create_builder()` + `content_accounts_credentials_create_execute()`.
/// For task-level control, use `content_accounts_credentials_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_credentials_create(
    client: &SimpleHttpClient,
    args: &ContentAccountsCredentialsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountCredentials>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_accounts_credentials_create_builder(client, args.accountId.clone(), &args.body)?;
    content_accounts_credentials_create_execute(builder)
}

/// GET accounts/{accountId}/labels
/// Creates a new label, not assigned to any account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_labels_create_execute()` to send, or `content_accounts_labels_create` for simplest API.

pub fn content_accounts_labels_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    body: &AccountLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/labels",
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accounts/{accountId}/labels
/// Creates a new label, not assigned to any account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_labels_create_execute()` or `content_accounts_labels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_labels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountLabel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountLabel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/{accountId}/labels
/// Creates a new label, not assigned to any account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_labels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_labels_create_task()`.
/// For the simplest API, use `content_accounts_labels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_labels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountLabel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_labels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_labels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsLabelsCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: AccountLabel,
}

/// GET accounts/{accountId}/labels
/// Creates a new label, not assigned to any account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_labels_create_builder()` + `content_accounts_labels_create_execute()`.
/// For task-level control, use `content_accounts_labels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_labels_create(
    client: &SimpleHttpClient,
    args: &ContentAccountsLabelsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountLabel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_accounts_labels_create_builder(client, args.accountId.clone(), &args.body)?;
    content_accounts_labels_create_execute(builder)
}

/// GET accounts/{accountId}/labels/{labelId}
/// Deletes a label and removes it from all accounts to which it was assigned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_labels_delete_execute()` to send, or `content_accounts_labels_delete` for simplest API.

pub fn content_accounts_labels_delete_builder(
    client: &SimpleHttpClient,
    accountId: String,
    labelId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/labels/{}",
        accountId.as_str(),
        labelId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET accounts/{accountId}/labels/{labelId}
/// Deletes a label and removes it from all accounts to which it was assigned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_labels_delete_execute()` or `content_accounts_labels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_labels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/{accountId}/labels/{labelId}
/// Deletes a label and removes it from all accounts to which it was assigned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_labels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_labels_delete_task()`.
/// For the simplest API, use `content_accounts_labels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_labels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_accounts_labels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_labels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsLabelsDeleteArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: labelId
    pub labelId: String,
}

/// GET accounts/{accountId}/labels/{labelId}
/// Deletes a label and removes it from all accounts to which it was assigned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_labels_delete_builder()` + `content_accounts_labels_delete_execute()`.
/// For task-level control, use `content_accounts_labels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_labels_delete(
    client: &SimpleHttpClient,
    args: &ContentAccountsLabelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_accounts_labels_delete_builder(
        client,
        args.accountId.clone(),
        args.labelId.clone(),
    )?;
    content_accounts_labels_delete_execute(builder)
}

/// GET accounts/{accountId}/returncarrier
/// Links return carrier to a merchant account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_returncarrier_create_execute()` to send, or `content_accounts_returncarrier_create` for simplest API.

pub fn content_accounts_returncarrier_create_builder(
    client: &SimpleHttpClient,
    accountId: String,
    body: &AccountReturnCarrier,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/returncarrier",
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accounts/{accountId}/returncarrier
/// Links return carrier to a merchant account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_returncarrier_create_execute()` or `content_accounts_returncarrier_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_returncarrier_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_returncarrier_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountReturnCarrier>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountReturnCarrier = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/{accountId}/returncarrier
/// Links return carrier to a merchant account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_returncarrier_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_returncarrier_create_task()`.
/// For the simplest API, use `content_accounts_returncarrier_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_returncarrier_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_returncarrier_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountReturnCarrier>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounts_returncarrier_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_returncarrier_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsReturncarrierCreateArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Request body.
    pub body: AccountReturnCarrier,
}

/// GET accounts/{accountId}/returncarrier
/// Links return carrier to a merchant account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_returncarrier_create_builder()` + `content_accounts_returncarrier_create_execute()`.
/// For task-level control, use `content_accounts_returncarrier_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_returncarrier_create(
    client: &SimpleHttpClient,
    args: &ContentAccountsReturncarrierCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountReturnCarrier>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_accounts_returncarrier_create_builder(client, args.accountId.clone(), &args.body)?;
    content_accounts_returncarrier_create_execute(builder)
}

/// GET accounts/{accountId}/returncarrier/{carrierAccountId}
/// Delete a return carrier in the merchant account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounts_returncarrier_delete_execute()` to send, or `content_accounts_returncarrier_delete` for simplest API.

pub fn content_accounts_returncarrier_delete_builder(
    client: &SimpleHttpClient,
    accountId: String,
    carrierAccountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/returncarrier/{}",
        accountId.as_str(),
        carrierAccountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET accounts/{accountId}/returncarrier/{carrierAccountId}
/// Delete a return carrier in the merchant account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounts_returncarrier_delete_execute()` or `content_accounts_returncarrier_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_returncarrier_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_returncarrier_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounts/{accountId}/returncarrier/{carrierAccountId}
/// Delete a return carrier in the merchant account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounts_returncarrier_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounts_returncarrier_delete_task()`.
/// For the simplest API, use `content_accounts_returncarrier_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounts_returncarrier_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounts_returncarrier_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_accounts_returncarrier_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounts_returncarrier_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountsReturncarrierDeleteArgs {
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: carrierAccountId
    pub carrierAccountId: String,
}

/// GET accounts/{accountId}/returncarrier/{carrierAccountId}
/// Delete a return carrier in the merchant account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounts_returncarrier_delete_builder()` + `content_accounts_returncarrier_delete_execute()`.
/// For task-level control, use `content_accounts_returncarrier_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounts_returncarrier_delete(
    client: &SimpleHttpClient,
    args: &ContentAccountsReturncarrierDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_accounts_returncarrier_delete_builder(
        client,
        args.accountId.clone(),
        args.carrierAccountId.clone(),
    )?;
    content_accounts_returncarrier_delete_execute(builder)
}

/// GET accountstatuses/batch
/// Retrieves multiple Merchant Center account statuses in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accountstatuses_custombatch_execute()` to send, or `content_accountstatuses_custombatch` for simplest API.

pub fn content_accountstatuses_custombatch_builder(
    client: &SimpleHttpClient,
    body: &AccountstatusesCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accountstatuses/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accountstatuses/batch
/// Retrieves multiple Merchant Center account statuses in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accountstatuses_custombatch_execute()` or `content_accountstatuses_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accountstatuses_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accountstatuses_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountstatusesCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountstatusesCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accountstatuses/batch
/// Retrieves multiple Merchant Center account statuses in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accountstatuses_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accountstatuses_custombatch_task()`.
/// For the simplest API, use `content_accountstatuses_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accountstatuses_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accountstatuses_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountstatusesCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accountstatuses_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accountstatuses_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountstatusesCustombatchArgs {
    /// Request body.
    pub body: AccountstatusesCustomBatchRequest,
}

/// GET accountstatuses/batch
/// Retrieves multiple Merchant Center account statuses in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accountstatuses_custombatch_builder()` + `content_accountstatuses_custombatch_execute()`.
/// For task-level control, use `content_accountstatuses_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accountstatuses_custombatch(
    client: &SimpleHttpClient,
    args: &ContentAccountstatusesCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountstatusesCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accountstatuses_custombatch_builder(client, &args.body)?;
    content_accountstatuses_custombatch_execute(builder)
}

/// GET {merchantId}/accountstatuses/{accountId}
/// Retrieves the status of a Merchant Center account. No `itemLevelIssues` are returned for multi-client accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accountstatuses_get_execute()` to send, or `content_accountstatuses_get` for simplest API.

pub fn content_accountstatuses_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    destinations: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accountstatuses/{}",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinations {
        query_parts.push(format!("destinations={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accountstatuses/{accountId}
/// Retrieves the status of a Merchant Center account. No `itemLevelIssues` are returned for multi-client accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accountstatuses_get_execute()` or `content_accountstatuses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accountstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accountstatuses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accountstatuses/{accountId}
/// Retrieves the status of a Merchant Center account. No `itemLevelIssues` are returned for multi-client accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accountstatuses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accountstatuses_get_task()`.
/// For the simplest API, use `content_accountstatuses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accountstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accountstatuses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accountstatuses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accountstatuses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountstatusesGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: destinations
    pub destinations: Option<String>,
}

/// GET {merchantId}/accountstatuses/{accountId}
/// Retrieves the status of a Merchant Center account. No `itemLevelIssues` are returned for multi-client accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accountstatuses_get_builder()` + `content_accountstatuses_get_execute()`.
/// For task-level control, use `content_accountstatuses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accountstatuses_get(
    client: &SimpleHttpClient,
    args: &ContentAccountstatusesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_accountstatuses_get_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        args.destinations.clone(),
    )?;
    content_accountstatuses_get_execute(builder)
}

/// GET {merchantId}/accountstatuses
/// Lists the statuses of the sub-accounts in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accountstatuses_list_execute()` to send, or `content_accountstatuses_list` for simplest API.

pub fn content_accountstatuses_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    destinations: Option<String>,
    maxResults: Option<i32>,
    name: Option<String>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accountstatuses",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinations {
        query_parts.push(format!("destinations={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accountstatuses
/// Lists the statuses of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accountstatuses_list_execute()` or `content_accountstatuses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accountstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accountstatuses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountstatusesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountstatusesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accountstatuses
/// Lists the statuses of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accountstatuses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accountstatuses_list_task()`.
/// For the simplest API, use `content_accountstatuses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accountstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accountstatuses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountstatusesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accountstatuses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accountstatuses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccountstatusesListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: destinations
    pub destinations: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/accountstatuses
/// Lists the statuses of the sub-accounts in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accountstatuses_list_builder()` + `content_accountstatuses_list_execute()`.
/// For task-level control, use `content_accountstatuses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accountstatuses_list(
    client: &SimpleHttpClient,
    args: &ContentAccountstatusesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountstatusesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accountstatuses_list_builder(
        client,
        args.merchantId.clone(),
        args.destinations.clone(),
        args.maxResults.clone(),
        args.name.clone(),
        args.pageToken.clone(),
    )?;
    content_accountstatuses_list_execute(builder)
}

/// GET accounttax/batch
/// Retrieves and updates tax settings of multiple accounts in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounttax_custombatch_execute()` to send, or `content_accounttax_custombatch` for simplest API.

pub fn content_accounttax_custombatch_builder(
    client: &SimpleHttpClient,
    body: &AccounttaxCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accounttax/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET accounttax/batch
/// Retrieves and updates tax settings of multiple accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounttax_custombatch_execute()` or `content_accounttax_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounttax_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounttax_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccounttaxCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccounttaxCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET accounttax/batch
/// Retrieves and updates tax settings of multiple accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounttax_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounttax_custombatch_task()`.
/// For the simplest API, use `content_accounttax_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounttax_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounttax_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccounttaxCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_accounttax_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounttax_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccounttaxCustombatchArgs {
    /// Request body.
    pub body: AccounttaxCustomBatchRequest,
}

/// GET accounttax/batch
/// Retrieves and updates tax settings of multiple accounts in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounttax_custombatch_builder()` + `content_accounttax_custombatch_execute()`.
/// For task-level control, use `content_accounttax_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounttax_custombatch(
    client: &SimpleHttpClient,
    args: &ContentAccounttaxCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccounttaxCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounttax_custombatch_builder(client, &args.body)?;
    content_accounttax_custombatch_execute(builder)
}

/// GET {merchantId}/accounttax/{accountId}
/// Retrieves the tax settings of the account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounttax_get_execute()` to send, or `content_accounttax_get` for simplest API.

pub fn content_accounttax_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounttax/{}",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accounttax/{accountId}
/// Retrieves the tax settings of the account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounttax_get_execute()` or `content_accounttax_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounttax_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounttax_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountTax>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountTax = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounttax/{accountId}
/// Retrieves the tax settings of the account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounttax_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounttax_get_task()`.
/// For the simplest API, use `content_accounttax_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounttax_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounttax_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountTax>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_accounttax_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounttax_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccounttaxGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
}

/// GET {merchantId}/accounttax/{accountId}
/// Retrieves the tax settings of the account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounttax_get_builder()` + `content_accounttax_get_execute()`.
/// For task-level control, use `content_accounttax_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounttax_get(
    client: &SimpleHttpClient,
    args: &ContentAccounttaxGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountTax>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        content_accounttax_get_builder(client, args.merchantId.clone(), args.accountId.clone())?;
    content_accounttax_get_execute(builder)
}

/// GET {merchantId}/accounttax
/// Lists the tax settings of the sub-accounts in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_accounttax_list_execute()` to send, or `content_accounttax_list` for simplest API.

pub fn content_accounttax_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounttax",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/accounttax
/// Lists the tax settings of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_accounttax_list_execute()` or `content_accounttax_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounttax_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounttax_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccounttaxListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccounttaxListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/accounttax
/// Lists the tax settings of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_accounttax_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_accounttax_list_task()`.
/// For the simplest API, use `content_accounttax_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_accounttax_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_accounttax_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccounttaxListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_accounttax_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_accounttax_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentAccounttaxListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/accounttax
/// Lists the tax settings of the sub-accounts in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_accounttax_list_builder()` + `content_accounttax_list_execute()`.
/// For task-level control, use `content_accounttax_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_accounttax_list(
    client: &SimpleHttpClient,
    args: &ContentAccounttaxListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccounttaxListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_accounttax_list_builder(
        client,
        args.merchantId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    content_accounttax_list_execute(builder)
}

/// GET {merchantId}/collections
/// Uploads a collection to your Merchant Center account. If a collection with the same `collectionId` already exists, this method updates that entry. In each update, the collection is completely replaced by the fields in the body of the update request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_collections_create_execute()` to send, or `content_collections_create` for simplest API.

pub fn content_collections_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &Collection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collections",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/collections
/// Uploads a collection to your Merchant Center account. If a collection with the same `collectionId` already exists, this method updates that entry. In each update, the collection is completely replaced by the fields in the body of the update request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_collections_create_execute()` or `content_collections_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collections_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Collection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Collection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/collections
/// Uploads a collection to your Merchant Center account. If a collection with the same `collectionId` already exists, this method updates that entry. In each update, the collection is completely replaced by the fields in the body of the update request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_collections_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_collections_create_task()`.
/// For the simplest API, use `content_collections_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_collections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Collection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_collections_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_collections_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCollectionsCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: Collection,
}

/// GET {merchantId}/collections
/// Uploads a collection to your Merchant Center account. If a collection with the same `collectionId` already exists, this method updates that entry. In each update, the collection is completely replaced by the fields in the body of the update request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_collections_create_builder()` + `content_collections_create_execute()`.
/// For task-level control, use `content_collections_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collections_create(
    client: &SimpleHttpClient,
    args: &ContentCollectionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Collection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_collections_create_builder(client, args.merchantId.clone(), &args.body)?;
    content_collections_create_execute(builder)
}

/// GET {merchantId}/collections/{collectionId}
/// Deletes a collection from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_collections_delete_execute()` to send, or `content_collections_delete` for simplest API.

pub fn content_collections_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    collectionId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collections/{}",
        merchantId.as_str(),
        collectionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/collections/{collectionId}
/// Deletes a collection from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_collections_delete_execute()` or `content_collections_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collections_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/collections/{collectionId}
/// Deletes a collection from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_collections_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_collections_delete_task()`.
/// For the simplest API, use `content_collections_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_collections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_collections_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_collections_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCollectionsDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: collectionId
    pub collectionId: String,
}

/// GET {merchantId}/collections/{collectionId}
/// Deletes a collection from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_collections_delete_builder()` + `content_collections_delete_execute()`.
/// For task-level control, use `content_collections_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collections_delete(
    client: &SimpleHttpClient,
    args: &ContentCollectionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_collections_delete_builder(
        client,
        args.merchantId.clone(),
        args.collectionId.clone(),
    )?;
    content_collections_delete_execute(builder)
}

/// GET {merchantId}/collectionstatuses/{collectionId}
/// Gets the status of a collection from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_collectionstatuses_get_execute()` to send, or `content_collectionstatuses_get` for simplest API.

pub fn content_collectionstatuses_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    collectionId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collectionstatuses/{}",
        merchantId.as_str(),
        collectionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/collectionstatuses/{collectionId}
/// Gets the status of a collection from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_collectionstatuses_get_execute()` or `content_collectionstatuses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collectionstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collectionstatuses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CollectionStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CollectionStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/collectionstatuses/{collectionId}
/// Gets the status of a collection from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_collectionstatuses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_collectionstatuses_get_task()`.
/// For the simplest API, use `content_collectionstatuses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collectionstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_collectionstatuses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CollectionStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_collectionstatuses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_collectionstatuses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCollectionstatusesGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: collectionId
    pub collectionId: String,
}

/// GET {merchantId}/collectionstatuses/{collectionId}
/// Gets the status of a collection from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_collectionstatuses_get_builder()` + `content_collectionstatuses_get_execute()`.
/// For task-level control, use `content_collectionstatuses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collectionstatuses_get(
    client: &SimpleHttpClient,
    args: &ContentCollectionstatusesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CollectionStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_collectionstatuses_get_builder(
        client,
        args.merchantId.clone(),
        args.collectionId.clone(),
    )?;
    content_collectionstatuses_get_execute(builder)
}

/// GET {merchantId}/collectionstatuses
/// Lists the statuses of the collections in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_collectionstatuses_list_execute()` to send, or `content_collectionstatuses_list` for simplest API.

pub fn content_collectionstatuses_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collectionstatuses",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/collectionstatuses
/// Lists the statuses of the collections in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_collectionstatuses_list_execute()` or `content_collectionstatuses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collectionstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collectionstatuses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCollectionStatusesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCollectionStatusesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/collectionstatuses
/// Lists the statuses of the collections in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_collectionstatuses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_collectionstatuses_list_task()`.
/// For the simplest API, use `content_collectionstatuses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_collectionstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_collectionstatuses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCollectionStatusesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_collectionstatuses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_collectionstatuses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCollectionstatusesListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/collectionstatuses
/// Lists the statuses of the collections in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_collectionstatuses_list_builder()` + `content_collectionstatuses_list_execute()`.
/// For task-level control, use `content_collectionstatuses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_collectionstatuses_list(
    client: &SimpleHttpClient,
    args: &ContentCollectionstatusesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCollectionStatusesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_collectionstatuses_list_builder(
        client,
        args.merchantId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    content_collectionstatuses_list_execute(builder)
}

/// GET {merchantId}/conversionsources
/// Creates a new conversion source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_conversionsources_create_execute()` to send, or `content_conversionsources_create` for simplest API.

pub fn content_conversionsources_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &ConversionSource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/conversionsources
/// Creates a new conversion source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_conversionsources_create_execute()` or `content_conversionsources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_conversionsources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_conversionsources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/conversionsources
/// Creates a new conversion source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_conversionsources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_conversionsources_create_task()`.
/// For the simplest API, use `content_conversionsources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_conversionsources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_conversionsources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_conversionsources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_conversionsources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentConversionsourcesCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: ConversionSource,
}

/// GET {merchantId}/conversionsources
/// Creates a new conversion source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_conversionsources_create_builder()` + `content_conversionsources_create_execute()`.
/// For task-level control, use `content_conversionsources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_conversionsources_create(
    client: &SimpleHttpClient,
    args: &ContentConversionsourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_conversionsources_create_builder(client, args.merchantId.clone(), &args.body)?;
    content_conversionsources_create_execute(builder)
}

/// GET {merchantId}/conversionsources/{conversionSourceId}
/// Archives an existing conversion source. It will be recoverable for 30 days. This archiving behavior is not typical in the Content API and unique to this service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_conversionsources_delete_execute()` to send, or `content_conversionsources_delete` for simplest API.

pub fn content_conversionsources_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    conversionSourceId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources/{}",
        merchantId.as_str(),
        conversionSourceId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/conversionsources/{conversionSourceId}
/// Archives an existing conversion source. It will be recoverable for 30 days. This archiving behavior is not typical in the Content API and unique to this service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_conversionsources_delete_execute()` or `content_conversionsources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_conversionsources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_conversionsources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/conversionsources/{conversionSourceId}
/// Archives an existing conversion source. It will be recoverable for 30 days. This archiving behavior is not typical in the Content API and unique to this service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_conversionsources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_conversionsources_delete_task()`.
/// For the simplest API, use `content_conversionsources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_conversionsources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_conversionsources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_conversionsources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_conversionsources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentConversionsourcesDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: conversionSourceId
    pub conversionSourceId: String,
}

/// GET {merchantId}/conversionsources/{conversionSourceId}
/// Archives an existing conversion source. It will be recoverable for 30 days. This archiving behavior is not typical in the Content API and unique to this service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_conversionsources_delete_builder()` + `content_conversionsources_delete_execute()`.
/// For task-level control, use `content_conversionsources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_conversionsources_delete(
    client: &SimpleHttpClient,
    args: &ContentConversionsourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_conversionsources_delete_builder(
        client,
        args.merchantId.clone(),
        args.conversionSourceId.clone(),
    )?;
    content_conversionsources_delete_execute(builder)
}

/// GET {merchantId}/conversionsources/{conversionSourceId}:undelete
/// Re-enables an archived conversion source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_conversionsources_undelete_execute()` to send, or `content_conversionsources_undelete` for simplest API.

pub fn content_conversionsources_undelete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    conversionSourceId: String,
    body: &UndeleteConversionSourceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources/{}:undelete",
        merchantId.as_str(),
        conversionSourceId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/conversionsources/{conversionSourceId}:undelete
/// Re-enables an archived conversion source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_conversionsources_undelete_execute()` or `content_conversionsources_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_conversionsources_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_conversionsources_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/conversionsources/{conversionSourceId}:undelete
/// Re-enables an archived conversion source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_conversionsources_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_conversionsources_undelete_task()`.
/// For the simplest API, use `content_conversionsources_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_conversionsources_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_conversionsources_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_conversionsources_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_conversionsources_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentConversionsourcesUndeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: conversionSourceId
    pub conversionSourceId: String,
    /// Request body.
    pub body: UndeleteConversionSourceRequest,
}

/// GET {merchantId}/conversionsources/{conversionSourceId}:undelete
/// Re-enables an archived conversion source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_conversionsources_undelete_builder()` + `content_conversionsources_undelete_execute()`.
/// For task-level control, use `content_conversionsources_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_conversionsources_undelete(
    client: &SimpleHttpClient,
    args: &ContentConversionsourcesUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_conversionsources_undelete_builder(
        client,
        args.merchantId.clone(),
        args.conversionSourceId.clone(),
        &args.body,
    )?;
    content_conversionsources_undelete_execute(builder)
}

/// GET {cssGroupId}/csses/{cssDomainId}
/// Retrieves a single CSS domain by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_csses_get_execute()` to send, or `content_csses_get` for simplest API.

pub fn content_csses_get_builder(
    client: &SimpleHttpClient,
    cssGroupId: String,
    cssDomainId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/csses/{}",
        cssGroupId.as_str(),
        cssDomainId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {cssGroupId}/csses/{cssDomainId}
/// Retrieves a single CSS domain by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_csses_get_execute()` or `content_csses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_csses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_csses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Css>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Css = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {cssGroupId}/csses/{cssDomainId}
/// Retrieves a single CSS domain by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_csses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_csses_get_task()`.
/// For the simplest API, use `content_csses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_csses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_csses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Css>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_csses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_csses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCssesGetArgs {
    /// Path parameter: cssGroupId
    pub cssGroupId: String,
    /// Path parameter: cssDomainId
    pub cssDomainId: String,
}

/// GET {cssGroupId}/csses/{cssDomainId}
/// Retrieves a single CSS domain by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_csses_get_builder()` + `content_csses_get_execute()`.
/// For task-level control, use `content_csses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_csses_get(
    client: &SimpleHttpClient,
    args: &ContentCssesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Css>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        content_csses_get_builder(client, args.cssGroupId.clone(), args.cssDomainId.clone())?;
    content_csses_get_execute(builder)
}

/// GET {cssGroupId}/csses
/// Lists CSS domains affiliated with a CSS group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_csses_list_execute()` to send, or `content_csses_list` for simplest API.

pub fn content_csses_list_builder(
    client: &SimpleHttpClient,
    cssGroupId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/csses",
        cssGroupId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {cssGroupId}/csses
/// Lists CSS domains affiliated with a CSS group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_csses_list_execute()` or `content_csses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_csses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_csses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCssesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCssesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {cssGroupId}/csses
/// Lists CSS domains affiliated with a CSS group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_csses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_csses_list_task()`.
/// For the simplest API, use `content_csses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_csses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_csses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCssesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_csses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_csses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCssesListArgs {
    /// Path parameter: cssGroupId
    pub cssGroupId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {cssGroupId}/csses
/// Lists CSS domains affiliated with a CSS group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_csses_list_builder()` + `content_csses_list_execute()`.
/// For task-level control, use `content_csses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_csses_list(
    client: &SimpleHttpClient,
    args: &ContentCssesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCssesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_csses_list_builder(
        client,
        args.cssGroupId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    content_csses_list_execute(builder)
}

/// GET {cssGroupId}/csses/{cssDomainId}/updatelabels
/// Updates labels that are assigned to a CSS domain by its CSS group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_csses_updatelabels_execute()` to send, or `content_csses_updatelabels` for simplest API.

pub fn content_csses_updatelabels_builder(
    client: &SimpleHttpClient,
    cssGroupId: String,
    cssDomainId: String,
    body: &LabelIds,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/csses/{}/updatelabels",
        cssGroupId.as_str(),
        cssDomainId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {cssGroupId}/csses/{cssDomainId}/updatelabels
/// Updates labels that are assigned to a CSS domain by its CSS group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_csses_updatelabels_execute()` or `content_csses_updatelabels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_csses_updatelabels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_csses_updatelabels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Css>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Css = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {cssGroupId}/csses/{cssDomainId}/updatelabels
/// Updates labels that are assigned to a CSS domain by its CSS group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_csses_updatelabels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_csses_updatelabels_task()`.
/// For the simplest API, use `content_csses_updatelabels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_csses_updatelabels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_csses_updatelabels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Css>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_csses_updatelabels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_csses_updatelabels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentCssesUpdatelabelsArgs {
    /// Path parameter: cssGroupId
    pub cssGroupId: String,
    /// Path parameter: cssDomainId
    pub cssDomainId: String,
    /// Request body.
    pub body: LabelIds,
}

/// GET {cssGroupId}/csses/{cssDomainId}/updatelabels
/// Updates labels that are assigned to a CSS domain by its CSS group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_csses_updatelabels_builder()` + `content_csses_updatelabels_execute()`.
/// For task-level control, use `content_csses_updatelabels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_csses_updatelabels(
    client: &SimpleHttpClient,
    args: &ContentCssesUpdatelabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Css>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_csses_updatelabels_builder(
        client,
        args.cssGroupId.clone(),
        args.cssDomainId.clone(),
        &args.body,
    )?;
    content_csses_updatelabels_execute(builder)
}

/// GET datafeeds/batch
/// Deletes, fetches, gets, inserts and updates multiple datafeeds in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeeds_custombatch_execute()` to send, or `content_datafeeds_custombatch` for simplest API.

pub fn content_datafeeds_custombatch_builder(
    client: &SimpleHttpClient,
    body: &DatafeedsCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/datafeeds/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET datafeeds/batch
/// Deletes, fetches, gets, inserts and updates multiple datafeeds in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeeds_custombatch_execute()` or `content_datafeeds_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedsCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatafeedsCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET datafeeds/batch
/// Deletes, fetches, gets, inserts and updates multiple datafeeds in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeeds_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeeds_custombatch_task()`.
/// For the simplest API, use `content_datafeeds_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeeds_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatafeedsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_datafeeds_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeeds_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedsCustombatchArgs {
    /// Request body.
    pub body: DatafeedsCustomBatchRequest,
}

/// GET datafeeds/batch
/// Deletes, fetches, gets, inserts and updates multiple datafeeds in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeeds_custombatch_builder()` + `content_datafeeds_custombatch_execute()`.
/// For task-level control, use `content_datafeeds_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_custombatch(
    client: &SimpleHttpClient,
    args: &ContentDatafeedsCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatafeedsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_datafeeds_custombatch_builder(client, &args.body)?;
    content_datafeeds_custombatch_execute(builder)
}

/// GET {merchantId}/datafeeds/{datafeedId}
/// Deletes a datafeed configuration from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeeds_delete_execute()` to send, or `content_datafeeds_delete` for simplest API.

pub fn content_datafeeds_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    datafeedId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds/{}",
        merchantId.as_str(),
        datafeedId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/datafeeds/{datafeedId}
/// Deletes a datafeed configuration from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeeds_delete_execute()` or `content_datafeeds_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/datafeeds/{datafeedId}
/// Deletes a datafeed configuration from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeeds_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeeds_delete_task()`.
/// For the simplest API, use `content_datafeeds_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeeds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_datafeeds_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeeds_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedsDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: datafeedId
    pub datafeedId: String,
}

/// GET {merchantId}/datafeeds/{datafeedId}
/// Deletes a datafeed configuration from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeeds_delete_builder()` + `content_datafeeds_delete_execute()`.
/// For task-level control, use `content_datafeeds_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_delete(
    client: &SimpleHttpClient,
    args: &ContentDatafeedsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        content_datafeeds_delete_builder(client, args.merchantId.clone(), args.datafeedId.clone())?;
    content_datafeeds_delete_execute(builder)
}

/// GET {merchantId}/datafeeds/{datafeedId}/fetchNow
/// Invokes a fetch for the datafeed in your Merchant Center account. If you need to call this method more than once per day, we recommend you use the [Products service](<https://developers.google.`com/shopping-content/reference/rest/v2`.1/products>) to update your product data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeeds_fetchnow_execute()` to send, or `content_datafeeds_fetchnow` for simplest API.

pub fn content_datafeeds_fetchnow_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    datafeedId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds/{}/fetchNow",
        merchantId.as_str(),
        datafeedId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/datafeeds/{datafeedId}/fetchNow
/// Invokes a fetch for the datafeed in your Merchant Center account. If you need to call this method more than once per day, we recommend you use the [Products service](<https://developers.google.`com/shopping-content/reference/rest/v2`.1/products>) to update your product data.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeeds_fetchnow_execute()` or `content_datafeeds_fetchnow`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_fetchnow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_fetchnow_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedsFetchNowResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatafeedsFetchNowResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/datafeeds/{datafeedId}/fetchNow
/// Invokes a fetch for the datafeed in your Merchant Center account. If you need to call this method more than once per day, we recommend you use the [Products service](<https://developers.google.`com/shopping-content/reference/rest/v2`.1/products>) to update your product data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeeds_fetchnow_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeeds_fetchnow_task()`.
/// For the simplest API, use `content_datafeeds_fetchnow()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_fetchnow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeeds_fetchnow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatafeedsFetchNowResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_datafeeds_fetchnow_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeeds_fetchnow`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedsFetchnowArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: datafeedId
    pub datafeedId: String,
}

/// GET {merchantId}/datafeeds/{datafeedId}/fetchNow
/// Invokes a fetch for the datafeed in your Merchant Center account. If you need to call this method more than once per day, we recommend you use the [Products service](<https://developers.google.`com/shopping-content/reference/rest/v2`.1/products>) to update your product data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeeds_fetchnow_builder()` + `content_datafeeds_fetchnow_execute()`.
/// For task-level control, use `content_datafeeds_fetchnow_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_fetchnow(
    client: &SimpleHttpClient,
    args: &ContentDatafeedsFetchnowArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatafeedsFetchNowResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_datafeeds_fetchnow_builder(
        client,
        args.merchantId.clone(),
        args.datafeedId.clone(),
    )?;
    content_datafeeds_fetchnow_execute(builder)
}

/// GET {merchantId}/datafeeds
/// Registers a datafeed configuration with your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeeds_insert_execute()` to send, or `content_datafeeds_insert` for simplest API.

pub fn content_datafeeds_insert_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &Datafeed,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/datafeeds
/// Registers a datafeed configuration with your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeeds_insert_execute()` or `content_datafeeds_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Datafeed>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Datafeed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/datafeeds
/// Registers a datafeed configuration with your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeeds_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeeds_insert_task()`.
/// For the simplest API, use `content_datafeeds_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeeds_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeeds_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Datafeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_datafeeds_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeeds_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedsInsertArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: Datafeed,
}

/// GET {merchantId}/datafeeds
/// Registers a datafeed configuration with your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeeds_insert_builder()` + `content_datafeeds_insert_execute()`.
/// For task-level control, use `content_datafeeds_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeeds_insert(
    client: &SimpleHttpClient,
    args: &ContentDatafeedsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Datafeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_datafeeds_insert_builder(client, args.merchantId.clone(), &args.body)?;
    content_datafeeds_insert_execute(builder)
}

/// GET datafeedstatuses/batch
/// Gets multiple Merchant Center datafeed statuses in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeedstatuses_custombatch_execute()` to send, or `content_datafeedstatuses_custombatch` for simplest API.

pub fn content_datafeedstatuses_custombatch_builder(
    client: &SimpleHttpClient,
    body: &DatafeedstatusesCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/datafeedstatuses/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET datafeedstatuses/batch
/// Gets multiple Merchant Center datafeed statuses in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeedstatuses_custombatch_execute()` or `content_datafeedstatuses_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeedstatuses_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeedstatuses_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedstatusesCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatafeedstatusesCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET datafeedstatuses/batch
/// Gets multiple Merchant Center datafeed statuses in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeedstatuses_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeedstatuses_custombatch_task()`.
/// For the simplest API, use `content_datafeedstatuses_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeedstatuses_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeedstatuses_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatafeedstatusesCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_datafeedstatuses_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeedstatuses_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedstatusesCustombatchArgs {
    /// Request body.
    pub body: DatafeedstatusesCustomBatchRequest,
}

/// GET datafeedstatuses/batch
/// Gets multiple Merchant Center datafeed statuses in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeedstatuses_custombatch_builder()` + `content_datafeedstatuses_custombatch_execute()`.
/// For task-level control, use `content_datafeedstatuses_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeedstatuses_custombatch(
    client: &SimpleHttpClient,
    args: &ContentDatafeedstatusesCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatafeedstatusesCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_datafeedstatuses_custombatch_builder(client, &args.body)?;
    content_datafeedstatuses_custombatch_execute(builder)
}

/// GET {merchantId}/datafeedstatuses/{datafeedId}
/// Retrieves the status of a datafeed from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeedstatuses_get_execute()` to send, or `content_datafeedstatuses_get` for simplest API.

pub fn content_datafeedstatuses_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    datafeedId: String,
    country: Option<String>,
    feedLabel: Option<String>,
    language: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeedstatuses/{}",
        merchantId.as_str(),
        datafeedId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = country {
        query_parts.push(format!("country={}", val));
    }
    if let Some(val) = feedLabel {
        query_parts.push(format!("feedLabel={}", val));
    }
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/datafeedstatuses/{datafeedId}
/// Retrieves the status of a datafeed from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeedstatuses_get_execute()` or `content_datafeedstatuses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeedstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeedstatuses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatafeedStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/datafeedstatuses/{datafeedId}
/// Retrieves the status of a datafeed from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeedstatuses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeedstatuses_get_task()`.
/// For the simplest API, use `content_datafeedstatuses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeedstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeedstatuses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatafeedStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_datafeedstatuses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeedstatuses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedstatusesGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: datafeedId
    pub datafeedId: String,
    /// Query parameter: country
    pub country: Option<String>,
    /// Query parameter: feedLabel
    pub feedLabel: Option<String>,
    /// Query parameter: language
    pub language: Option<String>,
}

/// GET {merchantId}/datafeedstatuses/{datafeedId}
/// Retrieves the status of a datafeed from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeedstatuses_get_builder()` + `content_datafeedstatuses_get_execute()`.
/// For task-level control, use `content_datafeedstatuses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeedstatuses_get(
    client: &SimpleHttpClient,
    args: &ContentDatafeedstatusesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatafeedStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_datafeedstatuses_get_builder(
        client,
        args.merchantId.clone(),
        args.datafeedId.clone(),
        args.country.clone(),
        args.feedLabel.clone(),
        args.language.clone(),
    )?;
    content_datafeedstatuses_get_execute(builder)
}

/// GET {merchantId}/datafeedstatuses
/// Lists the statuses of the datafeeds in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_datafeedstatuses_list_execute()` to send, or `content_datafeedstatuses_list` for simplest API.

pub fn content_datafeedstatuses_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeedstatuses",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/datafeedstatuses
/// Lists the statuses of the datafeeds in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_datafeedstatuses_list_execute()` or `content_datafeedstatuses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeedstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeedstatuses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedstatusesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatafeedstatusesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/datafeedstatuses
/// Lists the statuses of the datafeeds in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_datafeedstatuses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_datafeedstatuses_list_task()`.
/// For the simplest API, use `content_datafeedstatuses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_datafeedstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_datafeedstatuses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatafeedstatusesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_datafeedstatuses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_datafeedstatuses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentDatafeedstatusesListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/datafeedstatuses
/// Lists the statuses of the datafeeds in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_datafeedstatuses_list_builder()` + `content_datafeedstatuses_list_execute()`.
/// For task-level control, use `content_datafeedstatuses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_datafeedstatuses_list(
    client: &SimpleHttpClient,
    args: &ContentDatafeedstatusesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DatafeedstatusesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_datafeedstatuses_list_builder(
        client,
        args.merchantId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    content_datafeedstatuses_list_execute(builder)
}

/// GET {merchantId}/freelistingsprogram
/// Retrieves the status and review eligibility for the free listing program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_freelistingsprogram_get_execute()` to send, or `content_freelistingsprogram_get` for simplest API.

pub fn content_freelistingsprogram_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/freelistingsprogram
/// Retrieves the status and review eligibility for the free listing program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_freelistingsprogram_get_execute()` or `content_freelistingsprogram_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_freelistingsprogram_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_freelistingsprogram_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FreeListingsProgramStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FreeListingsProgramStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/freelistingsprogram
/// Retrieves the status and review eligibility for the free listing program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_freelistingsprogram_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_freelistingsprogram_get_task()`.
/// For the simplest API, use `content_freelistingsprogram_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_freelistingsprogram_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_freelistingsprogram_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FreeListingsProgramStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_freelistingsprogram_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_freelistingsprogram_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/freelistingsprogram
/// Retrieves the status and review eligibility for the free listing program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_freelistingsprogram_get_builder()` + `content_freelistingsprogram_get_execute()`.
/// For task-level control, use `content_freelistingsprogram_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_freelistingsprogram_get(
    client: &SimpleHttpClient,
    args: &ContentFreelistingsprogramGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FreeListingsProgramStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_freelistingsprogram_get_builder(client, args.merchantId.clone())?;
    content_freelistingsprogram_get_execute(builder)
}

/// GET {merchantId}/freelistingsprogram/requestreview
/// Requests a review of free listings in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_freelistingsprogram_requestreview_execute()` to send, or `content_freelistingsprogram_requestreview` for simplest API.

pub fn content_freelistingsprogram_requestreview_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &RequestReviewFreeListingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram/requestreview",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/freelistingsprogram/requestreview
/// Requests a review of free listings in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_freelistingsprogram_requestreview_execute()` or `content_freelistingsprogram_requestreview`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_freelistingsprogram_requestreview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_freelistingsprogram_requestreview_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/freelistingsprogram/requestreview
/// Requests a review of free listings in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_freelistingsprogram_requestreview_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_freelistingsprogram_requestreview_task()`.
/// For the simplest API, use `content_freelistingsprogram_requestreview()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_freelistingsprogram_requestreview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_freelistingsprogram_requestreview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_freelistingsprogram_requestreview_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_freelistingsprogram_requestreview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramRequestreviewArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: RequestReviewFreeListingsRequest,
}

/// GET {merchantId}/freelistingsprogram/requestreview
/// Requests a review of free listings in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_freelistingsprogram_requestreview_builder()` + `content_freelistingsprogram_requestreview_execute()`.
/// For task-level control, use `content_freelistingsprogram_requestreview_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_freelistingsprogram_requestreview(
    client: &SimpleHttpClient,
    args: &ContentFreelistingsprogramRequestreviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_freelistingsprogram_requestreview_builder(
        client,
        args.merchantId.clone(),
        &args.body,
    )?;
    content_freelistingsprogram_requestreview_execute(builder)
}

/// GET {merchantId}/freelistingsprogram/checkoutsettings
/// Deletes Checkout settings and unenrolls merchant from Checkout program.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_freelistingsprogram_checkoutsettings_delete_execute()` to send, or `content_freelistingsprogram_checkoutsettings_delete` for simplest API.

pub fn content_freelistingsprogram_checkoutsettings_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram/checkoutsettings",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/freelistingsprogram/checkoutsettings
/// Deletes Checkout settings and unenrolls merchant from Checkout program.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_freelistingsprogram_checkoutsettings_delete_execute()` or `content_freelistingsprogram_checkoutsettings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_freelistingsprogram_checkoutsettings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_freelistingsprogram_checkoutsettings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/freelistingsprogram/checkoutsettings
/// Deletes Checkout settings and unenrolls merchant from Checkout program.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_freelistingsprogram_checkoutsettings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_freelistingsprogram_checkoutsettings_delete_task()`.
/// For the simplest API, use `content_freelistingsprogram_checkoutsettings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_freelistingsprogram_checkoutsettings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_freelistingsprogram_checkoutsettings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_freelistingsprogram_checkoutsettings_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_freelistingsprogram_checkoutsettings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramCheckoutsettingsDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/freelistingsprogram/checkoutsettings
/// Deletes Checkout settings and unenrolls merchant from Checkout program.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_freelistingsprogram_checkoutsettings_delete_builder()` + `content_freelistingsprogram_checkoutsettings_delete_execute()`.
/// For task-level control, use `content_freelistingsprogram_checkoutsettings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_freelistingsprogram_checkoutsettings_delete(
    client: &SimpleHttpClient,
    args: &ContentFreelistingsprogramCheckoutsettingsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_freelistingsprogram_checkoutsettings_delete_builder(
        client,
        args.merchantId.clone(),
    )?;
    content_freelistingsprogram_checkoutsettings_delete_execute(builder)
}

/// GET liasettings/batch
/// Retrieves `and/or` updates the LIA settings of multiple accounts in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_custombatch_execute()` to send, or `content_liasettings_custombatch` for simplest API.

pub fn content_liasettings_custombatch_builder(
    client: &SimpleHttpClient,
    body: &LiasettingsCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/liasettings/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET liasettings/batch
/// Retrieves `and/or` updates the LIA settings of multiple accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_custombatch_execute()` or `content_liasettings_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiasettingsCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET liasettings/batch
/// Retrieves `and/or` updates the LIA settings of multiple accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_custombatch_task()`.
/// For the simplest API, use `content_liasettings_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_liasettings_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_liasettings_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLiasettingsCustombatchArgs {
    /// Request body.
    pub body: LiasettingsCustomBatchRequest,
}

/// GET liasettings/batch
/// Retrieves `and/or` updates the LIA settings of multiple accounts in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_custombatch_builder()` + `content_liasettings_custombatch_execute()`.
/// For task-level control, use `content_liasettings_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_custombatch(
    client: &SimpleHttpClient,
    args: &ContentLiasettingsCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_liasettings_custombatch_builder(client, &args.body)?;
    content_liasettings_custombatch_execute(builder)
}

/// GET {merchantId}/liasettings/{accountId}
/// Retrieves the LIA settings of the account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_get_execute()` to send, or `content_liasettings_get` for simplest API.

pub fn content_liasettings_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/liasettings/{accountId}
/// Retrieves the LIA settings of the account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_get_execute()` or `content_liasettings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiaSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiaSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/liasettings/{accountId}
/// Retrieves the LIA settings of the account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_get_task()`.
/// For the simplest API, use `content_liasettings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiaSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_liasettings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_liasettings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLiasettingsGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
}

/// GET {merchantId}/liasettings/{accountId}
/// Retrieves the LIA settings of the account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_get_builder()` + `content_liasettings_get_execute()`.
/// For task-level control, use `content_liasettings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_get(
    client: &SimpleHttpClient,
    args: &ContentLiasettingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiaSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        content_liasettings_get_builder(client, args.merchantId.clone(), args.accountId.clone())?;
    content_liasettings_get_execute(builder)
}

/// GET {merchantId}/liasettings/{accountId}/accessiblegmbaccounts
/// Retrieves the list of accessible Business Profiles.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_getaccessiblegmbaccounts_execute()` to send, or `content_liasettings_getaccessiblegmbaccounts` for simplest API.

pub fn content_liasettings_getaccessiblegmbaccounts_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/accessiblegmbaccounts",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/liasettings/{accountId}/accessiblegmbaccounts
/// Retrieves the list of accessible Business Profiles.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_getaccessiblegmbaccounts_execute()` or `content_liasettings_getaccessiblegmbaccounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_getaccessiblegmbaccounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_getaccessiblegmbaccounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsGetAccessibleGmbAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiasettingsGetAccessibleGmbAccountsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/liasettings/{accountId}/accessiblegmbaccounts
/// Retrieves the list of accessible Business Profiles.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_getaccessiblegmbaccounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_getaccessiblegmbaccounts_task()`.
/// For the simplest API, use `content_liasettings_getaccessiblegmbaccounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_getaccessiblegmbaccounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_getaccessiblegmbaccounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsGetAccessibleGmbAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_liasettings_getaccessiblegmbaccounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_liasettings_getaccessiblegmbaccounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLiasettingsGetaccessiblegmbaccountsArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
}

/// GET {merchantId}/liasettings/{accountId}/accessiblegmbaccounts
/// Retrieves the list of accessible Business Profiles.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_getaccessiblegmbaccounts_builder()` + `content_liasettings_getaccessiblegmbaccounts_execute()`.
/// For task-level control, use `content_liasettings_getaccessiblegmbaccounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_getaccessiblegmbaccounts(
    client: &SimpleHttpClient,
    args: &ContentLiasettingsGetaccessiblegmbaccountsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsGetAccessibleGmbAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_liasettings_getaccessiblegmbaccounts_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
    )?;
    content_liasettings_getaccessiblegmbaccounts_execute(builder)
}

/// GET {merchantId}/liasettings
/// Lists the LIA settings of the sub-accounts in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_list_execute()` to send, or `content_liasettings_list` for simplest API.

pub fn content_liasettings_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/liasettings
/// Lists the LIA settings of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_list_execute()` or `content_liasettings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiasettingsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/liasettings
/// Lists the LIA settings of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_list_task()`.
/// For the simplest API, use `content_liasettings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiasettingsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_liasettings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_liasettings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLiasettingsListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/liasettings
/// Lists the LIA settings of the sub-accounts in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_list_builder()` + `content_liasettings_list_execute()`.
/// For task-level control, use `content_liasettings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_list(
    client: &SimpleHttpClient,
    args: &ContentLiasettingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiasettingsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_liasettings_list_builder(
        client,
        args.merchantId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    content_liasettings_list_execute(builder)
}

/// GET liasettings/posdataproviders
/// Retrieves the list of POS data providers that have active settings for the all eiligible countries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_listposdataproviders_execute()` to send, or `content_liasettings_listposdataproviders` for simplest API.

pub fn content_liasettings_listposdataproviders_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/liasettings/posdataproviders",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET liasettings/posdataproviders
/// Retrieves the list of POS data providers that have active settings for the all eiligible countries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_listposdataproviders_execute()` or `content_liasettings_listposdataproviders`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_listposdataproviders_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_listposdataproviders_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsListPosDataProvidersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiasettingsListPosDataProvidersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET liasettings/posdataproviders
/// Retrieves the list of POS data providers that have active settings for the all eiligible countries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_listposdataproviders_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_listposdataproviders_task()`.
/// For the simplest API, use `content_liasettings_listposdataproviders()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_listposdataproviders_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_listposdataproviders_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsListPosDataProvidersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_liasettings_listposdataproviders_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET liasettings/posdataproviders
/// Retrieves the list of POS data providers that have active settings for the all eiligible countries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_listposdataproviders_builder()` + `content_liasettings_listposdataproviders_execute()`.
/// For task-level control, use `content_liasettings_listposdataproviders_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_listposdataproviders(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsListPosDataProvidersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_liasettings_listposdataproviders_builder(client)?;
    content_liasettings_listposdataproviders_execute(builder)
}

/// GET {merchantId}/liasettings/{accountId}/requestinventoryverification/{country}
/// Requests inventory validation for the specified country.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_requestinventoryverification_execute()` to send, or `content_liasettings_requestinventoryverification` for simplest API.

pub fn content_liasettings_requestinventoryverification_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    country: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/requestinventoryverification/{}",
        merchantId.as_str(),
        accountId.as_str(),
        country.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/liasettings/{accountId}/requestinventoryverification/{country}
/// Requests inventory validation for the specified country.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_requestinventoryverification_execute()` or `content_liasettings_requestinventoryverification`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_requestinventoryverification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_requestinventoryverification_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsRequestInventoryVerificationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiasettingsRequestInventoryVerificationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/liasettings/{accountId}/requestinventoryverification/{country}
/// Requests inventory validation for the specified country.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_requestinventoryverification_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_requestinventoryverification_task()`.
/// For the simplest API, use `content_liasettings_requestinventoryverification()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_requestinventoryverification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_requestinventoryverification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsRequestInventoryVerificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_liasettings_requestinventoryverification_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_liasettings_requestinventoryverification`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLiasettingsRequestinventoryverificationArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Path parameter: country
    pub country: String,
}

/// GET {merchantId}/liasettings/{accountId}/requestinventoryverification/{country}
/// Requests inventory validation for the specified country.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_requestinventoryverification_builder()` + `content_liasettings_requestinventoryverification_execute()`.
/// For task-level control, use `content_liasettings_requestinventoryverification_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_requestinventoryverification(
    client: &SimpleHttpClient,
    args: &ContentLiasettingsRequestinventoryverificationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiasettingsRequestInventoryVerificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_liasettings_requestinventoryverification_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        args.country.clone(),
    )?;
    content_liasettings_requestinventoryverification_execute(builder)
}

/// GET {merchantId}/liasettings/{accountId}/setomnichannelexperience
/// Sets the omnichannel experience for the specified country. Only supported for merchants whose POS data provider is trusted to enable the corresponding experience. For more context, see these help articles [about LFP](<https://support.google.`com/merchants/answer/7676652`>) and [how to get started](<https://support.google.`com/merchants/answer/7676578`>) with it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_liasettings_setomnichannelexperience_execute()` to send, or `content_liasettings_setomnichannelexperience` for simplest API.

pub fn content_liasettings_setomnichannelexperience_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
    country: Option<String>,
    lsfType: Option<String>,
    pickupTypes: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/setomnichannelexperience",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = country {
        query_parts.push(format!("country={}", val));
    }
    if let Some(val) = lsfType {
        query_parts.push(format!("lsfType={}", val));
    }
    if let Some(val) = pickupTypes {
        query_parts.push(format!("pickupTypes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/liasettings/{accountId}/setomnichannelexperience
/// Sets the omnichannel experience for the specified country. Only supported for merchants whose POS data provider is trusted to enable the corresponding experience. For more context, see these help articles [about LFP](<https://support.google.`com/merchants/answer/7676652`>) and [how to get started](<https://support.google.`com/merchants/answer/7676578`>) with it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_liasettings_setomnichannelexperience_execute()` or `content_liasettings_setomnichannelexperience`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_setomnichannelexperience_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_setomnichannelexperience_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiaOmnichannelExperience>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiaOmnichannelExperience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/liasettings/{accountId}/setomnichannelexperience
/// Sets the omnichannel experience for the specified country. Only supported for merchants whose POS data provider is trusted to enable the corresponding experience. For more context, see these help articles [about LFP](<https://support.google.`com/merchants/answer/7676652`>) and [how to get started](<https://support.google.`com/merchants/answer/7676578`>) with it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_liasettings_setomnichannelexperience_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_liasettings_setomnichannelexperience_task()`.
/// For the simplest API, use `content_liasettings_setomnichannelexperience()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_liasettings_setomnichannelexperience_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_liasettings_setomnichannelexperience_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiaOmnichannelExperience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_liasettings_setomnichannelexperience_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_liasettings_setomnichannelexperience`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLiasettingsSetomnichannelexperienceArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
    /// Query parameter: country
    pub country: Option<String>,
    /// Query parameter: lsfType
    pub lsfType: Option<String>,
    /// Query parameter: pickupTypes
    pub pickupTypes: Option<String>,
}

/// GET {merchantId}/liasettings/{accountId}/setomnichannelexperience
/// Sets the omnichannel experience for the specified country. Only supported for merchants whose POS data provider is trusted to enable the corresponding experience. For more context, see these help articles [about LFP](<https://support.google.`com/merchants/answer/7676652`>) and [how to get started](<https://support.google.`com/merchants/answer/7676578`>) with it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_liasettings_setomnichannelexperience_builder()` + `content_liasettings_setomnichannelexperience_execute()`.
/// For task-level control, use `content_liasettings_setomnichannelexperience_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_liasettings_setomnichannelexperience(
    client: &SimpleHttpClient,
    args: &ContentLiasettingsSetomnichannelexperienceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiaOmnichannelExperience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_liasettings_setomnichannelexperience_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
        args.country.clone(),
        args.lsfType.clone(),
        args.pickupTypes.clone(),
    )?;
    content_liasettings_setomnichannelexperience_execute(builder)
}

/// GET localinventory/batch
/// Updates local inventory for multiple products or stores in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_localinventory_custombatch_execute()` to send, or `content_localinventory_custombatch` for simplest API.

pub fn content_localinventory_custombatch_builder(
    client: &SimpleHttpClient,
    body: &LocalinventoryCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/localinventory/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET localinventory/batch
/// Updates local inventory for multiple products or stores in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_localinventory_custombatch_execute()` or `content_localinventory_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_localinventory_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_localinventory_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocalinventoryCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LocalinventoryCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET localinventory/batch
/// Updates local inventory for multiple products or stores in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_localinventory_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_localinventory_custombatch_task()`.
/// For the simplest API, use `content_localinventory_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_localinventory_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_localinventory_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LocalinventoryCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_localinventory_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_localinventory_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLocalinventoryCustombatchArgs {
    /// Request body.
    pub body: LocalinventoryCustomBatchRequest,
}

/// GET localinventory/batch
/// Updates local inventory for multiple products or stores in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_localinventory_custombatch_builder()` + `content_localinventory_custombatch_execute()`.
/// For task-level control, use `content_localinventory_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_localinventory_custombatch(
    client: &SimpleHttpClient,
    args: &ContentLocalinventoryCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LocalinventoryCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_localinventory_custombatch_builder(client, &args.body)?;
    content_localinventory_custombatch_execute(builder)
}

/// GET {merchantId}/products/{productId}/localinventory
/// Updates the local inventory of a product in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_localinventory_insert_execute()` to send, or `content_localinventory_insert` for simplest API.

pub fn content_localinventory_insert_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    productId: String,
    body: &LocalInventory,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}/localinventory",
        merchantId.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/products/{productId}/localinventory
/// Updates the local inventory of a product in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_localinventory_insert_execute()` or `content_localinventory_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_localinventory_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_localinventory_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocalInventory>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LocalInventory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/products/{productId}/localinventory
/// Updates the local inventory of a product in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_localinventory_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_localinventory_insert_task()`.
/// For the simplest API, use `content_localinventory_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_localinventory_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_localinventory_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocalInventory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_localinventory_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_localinventory_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentLocalinventoryInsertArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: LocalInventory,
}

/// GET {merchantId}/products/{productId}/localinventory
/// Updates the local inventory of a product in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_localinventory_insert_builder()` + `content_localinventory_insert_execute()`.
/// For task-level control, use `content_localinventory_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_localinventory_insert(
    client: &SimpleHttpClient,
    args: &ContentLocalinventoryInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocalInventory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_localinventory_insert_builder(
        client,
        args.merchantId.clone(),
        args.productId.clone(),
        &args.body,
    )?;
    content_localinventory_insert_execute(builder)
}

/// GET {merchantId}/merchantsupport/renderaccountissues
/// Provide a list of merchant's issues with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_merchantsupport_renderaccountissues_execute()` to send, or `content_merchantsupport_renderaccountissues` for simplest API.

pub fn content_merchantsupport_renderaccountissues_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    languageCode: Option<String>,
    timeZone: Option<String>,
    body: &RenderAccountIssuesRequestPayload,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/merchantsupport/renderaccountissues",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = timeZone {
        query_parts.push(format!("timeZone={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/merchantsupport/renderaccountissues
/// Provide a list of merchant's issues with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_merchantsupport_renderaccountissues_execute()` or `content_merchantsupport_renderaccountissues`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_merchantsupport_renderaccountissues_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_merchantsupport_renderaccountissues_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RenderAccountIssuesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RenderAccountIssuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/merchantsupport/renderaccountissues
/// Provide a list of merchant's issues with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_merchantsupport_renderaccountissues_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_merchantsupport_renderaccountissues_task()`.
/// For the simplest API, use `content_merchantsupport_renderaccountissues()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_merchantsupport_renderaccountissues_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_merchantsupport_renderaccountissues_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RenderAccountIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_merchantsupport_renderaccountissues_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_merchantsupport_renderaccountissues`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentMerchantsupportRenderaccountissuesArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: timeZone
    pub timeZone: Option<String>,
    /// Request body.
    pub body: RenderAccountIssuesRequestPayload,
}

/// GET {merchantId}/merchantsupport/renderaccountissues
/// Provide a list of merchant's issues with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_merchantsupport_renderaccountissues_builder()` + `content_merchantsupport_renderaccountissues_execute()`.
/// For task-level control, use `content_merchantsupport_renderaccountissues_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_merchantsupport_renderaccountissues(
    client: &SimpleHttpClient,
    args: &ContentMerchantsupportRenderaccountissuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RenderAccountIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_merchantsupport_renderaccountissues_builder(
        client,
        args.merchantId.clone(),
        args.languageCode.clone(),
        args.timeZone.clone(),
        &args.body,
    )?;
    content_merchantsupport_renderaccountissues_execute(builder)
}

/// GET {merchantId}/merchantsupport/renderproductissues/{productId}
/// Provide a list of issues for merchant's product with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_merchantsupport_renderproductissues_execute()` to send, or `content_merchantsupport_renderproductissues` for simplest API.

pub fn content_merchantsupport_renderproductissues_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    productId: String,
    languageCode: Option<String>,
    timeZone: Option<String>,
    body: &RenderProductIssuesRequestPayload,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/merchantsupport/renderproductissues/{}",
        merchantId.as_str(),
        productId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = timeZone {
        query_parts.push(format!("timeZone={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/merchantsupport/renderproductissues/{productId}
/// Provide a list of issues for merchant's product with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_merchantsupport_renderproductissues_execute()` or `content_merchantsupport_renderproductissues`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_merchantsupport_renderproductissues_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_merchantsupport_renderproductissues_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RenderProductIssuesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RenderProductIssuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/merchantsupport/renderproductissues/{productId}
/// Provide a list of issues for merchant's product with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_merchantsupport_renderproductissues_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_merchantsupport_renderproductissues_task()`.
/// For the simplest API, use `content_merchantsupport_renderproductissues()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_merchantsupport_renderproductissues_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_merchantsupport_renderproductissues_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RenderProductIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_merchantsupport_renderproductissues_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_merchantsupport_renderproductissues`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentMerchantsupportRenderproductissuesArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: timeZone
    pub timeZone: Option<String>,
    /// Request body.
    pub body: RenderProductIssuesRequestPayload,
}

/// GET {merchantId}/merchantsupport/renderproductissues/{productId}
/// Provide a list of issues for merchant's product with a support content and available actions. This content and actions are meant to be rendered and shown in third-party applications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_merchantsupport_renderproductissues_builder()` + `content_merchantsupport_renderproductissues_execute()`.
/// For task-level control, use `content_merchantsupport_renderproductissues_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_merchantsupport_renderproductissues(
    client: &SimpleHttpClient,
    args: &ContentMerchantsupportRenderproductissuesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RenderProductIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_merchantsupport_renderproductissues_builder(
        client,
        args.merchantId.clone(),
        args.productId.clone(),
        args.languageCode.clone(),
        args.timeZone.clone(),
        &args.body,
    )?;
    content_merchantsupport_renderproductissues_execute(builder)
}

/// GET {merchantId}/merchantsupport/triggeraction
/// Start an action. The action can be requested by merchants in third-party application. Before merchants can request the action, the third-party application needs to show them action specific content and display a user input form. You can request access using [Trigger action allowlist form](<https://docs.google.`com/forms/d/e/1FAIpQLSfeV_sBW9MBQv9BMTV6JZ1g11PGHLdHsrefca-9h0LmpU7CUg/viewform`?usp=sharing>). The action can be successfully started only once all required inputs are provided. If any required input is missing, or invalid value was provided, the service will return 400 error. Validation errors will contain Ids for all problematic field together with translated, human readable error messages that can be shown to the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_merchantsupport_triggeraction_execute()` to send, or `content_merchantsupport_triggeraction` for simplest API.

pub fn content_merchantsupport_triggeraction_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    languageCode: Option<String>,
    body: &TriggerActionPayload,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/merchantsupport/triggeraction",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/merchantsupport/triggeraction
/// Start an action. The action can be requested by merchants in third-party application. Before merchants can request the action, the third-party application needs to show them action specific content and display a user input form. You can request access using [Trigger action allowlist form](<https://docs.google.`com/forms/d/e/1FAIpQLSfeV_sBW9MBQv9BMTV6JZ1g11PGHLdHsrefca-9h0LmpU7CUg/viewform`?usp=sharing>). The action can be successfully started only once all required inputs are provided. If any required input is missing, or invalid value was provided, the service will return 400 error. Validation errors will contain Ids for all problematic field together with translated, human readable error messages that can be shown to the user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_merchantsupport_triggeraction_execute()` or `content_merchantsupport_triggeraction`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_merchantsupport_triggeraction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_merchantsupport_triggeraction_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TriggerActionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TriggerActionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/merchantsupport/triggeraction
/// Start an action. The action can be requested by merchants in third-party application. Before merchants can request the action, the third-party application needs to show them action specific content and display a user input form. You can request access using [Trigger action allowlist form](<https://docs.google.`com/forms/d/e/1FAIpQLSfeV_sBW9MBQv9BMTV6JZ1g11PGHLdHsrefca-9h0LmpU7CUg/viewform`?usp=sharing>). The action can be successfully started only once all required inputs are provided. If any required input is missing, or invalid value was provided, the service will return 400 error. Validation errors will contain Ids for all problematic field together with translated, human readable error messages that can be shown to the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_merchantsupport_triggeraction_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_merchantsupport_triggeraction_task()`.
/// For the simplest API, use `content_merchantsupport_triggeraction()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_merchantsupport_triggeraction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_merchantsupport_triggeraction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TriggerActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_merchantsupport_triggeraction_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_merchantsupport_triggeraction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentMerchantsupportTriggeractionArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Request body.
    pub body: TriggerActionPayload,
}

/// GET {merchantId}/merchantsupport/triggeraction
/// Start an action. The action can be requested by merchants in third-party application. Before merchants can request the action, the third-party application needs to show them action specific content and display a user input form. You can request access using [Trigger action allowlist form](<https://docs.google.`com/forms/d/e/1FAIpQLSfeV_sBW9MBQv9BMTV6JZ1g11PGHLdHsrefca-9h0LmpU7CUg/viewform`?usp=sharing>). The action can be successfully started only once all required inputs are provided. If any required input is missing, or invalid value was provided, the service will return 400 error. Validation errors will contain Ids for all problematic field together with translated, human readable error messages that can be shown to the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_merchantsupport_triggeraction_builder()` + `content_merchantsupport_triggeraction_execute()`.
/// For task-level control, use `content_merchantsupport_triggeraction_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_merchantsupport_triggeraction(
    client: &SimpleHttpClient,
    args: &ContentMerchantsupportTriggeractionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TriggerActionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_merchantsupport_triggeraction_builder(
        client,
        args.merchantId.clone(),
        args.languageCode.clone(),
        &args.body,
    )?;
    content_merchantsupport_triggeraction_execute(builder)
}

/// GET {merchantId}/ordertrackingsignals
/// Creates new order tracking signal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_ordertrackingsignals_create_execute()` to send, or `content_ordertrackingsignals_create` for simplest API.

pub fn content_ordertrackingsignals_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &OrderTrackingSignal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/ordertrackingsignals",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/ordertrackingsignals
/// Creates new order tracking signal.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_ordertrackingsignals_create_execute()` or `content_ordertrackingsignals_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_ordertrackingsignals_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_ordertrackingsignals_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrderTrackingSignal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrderTrackingSignal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/ordertrackingsignals
/// Creates new order tracking signal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_ordertrackingsignals_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_ordertrackingsignals_create_task()`.
/// For the simplest API, use `content_ordertrackingsignals_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_ordertrackingsignals_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_ordertrackingsignals_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrderTrackingSignal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_ordertrackingsignals_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_ordertrackingsignals_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentOrdertrackingsignalsCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: OrderTrackingSignal,
}

/// GET {merchantId}/ordertrackingsignals
/// Creates new order tracking signal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_ordertrackingsignals_create_builder()` + `content_ordertrackingsignals_create_execute()`.
/// For task-level control, use `content_ordertrackingsignals_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_ordertrackingsignals_create(
    client: &SimpleHttpClient,
    args: &ContentOrdertrackingsignalsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrderTrackingSignal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_ordertrackingsignals_create_builder(client, args.merchantId.clone(), &args.body)?;
    content_ordertrackingsignals_create_execute(builder)
}

/// GET pos/batch
/// Batches multiple POS-related calls in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_pos_custombatch_execute()` to send, or `content_pos_custombatch` for simplest API.

pub fn content_pos_custombatch_builder(
    client: &SimpleHttpClient,
    body: &PosCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://shoppingcontent.googleapis.com/content/v2.1/pos/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET pos/batch
/// Batches multiple POS-related calls in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_pos_custombatch_execute()` or `content_pos_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PosCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET pos/batch
/// Batches multiple POS-related calls in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_pos_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_pos_custombatch_task()`.
/// For the simplest API, use `content_pos_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_pos_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosCustomBatchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_pos_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_pos_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPosCustombatchArgs {
    /// Request body.
    pub body: PosCustomBatchRequest,
}

/// GET pos/batch
/// Batches multiple POS-related calls in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_pos_custombatch_builder()` + `content_pos_custombatch_execute()`.
/// For task-level control, use `content_pos_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_custombatch(
    client: &SimpleHttpClient,
    args: &ContentPosCustombatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosCustomBatchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_pos_custombatch_builder(client, &args.body)?;
    content_pos_custombatch_execute(builder)
}

/// GET {merchantId}/pos/{targetMerchantId}/store/{storeCode}
/// Deletes a store for the given merchant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_pos_delete_execute()` to send, or `content_pos_delete` for simplest API.

pub fn content_pos_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    targetMerchantId: String,
    storeCode: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/store/{}",
        merchantId.as_str(),
        targetMerchantId.as_str(),
        storeCode.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/pos/{targetMerchantId}/store/{storeCode}
/// Deletes a store for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_pos_delete_execute()` or `content_pos_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/pos/{targetMerchantId}/store/{storeCode}
/// Deletes a store for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_pos_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_pos_delete_task()`.
/// For the simplest API, use `content_pos_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_pos_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_pos_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_pos_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPosDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: targetMerchantId
    pub targetMerchantId: String,
    /// Path parameter: storeCode
    pub storeCode: String,
}

/// GET {merchantId}/pos/{targetMerchantId}/store/{storeCode}
/// Deletes a store for the given merchant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_pos_delete_builder()` + `content_pos_delete_execute()`.
/// For task-level control, use `content_pos_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_delete(
    client: &SimpleHttpClient,
    args: &ContentPosDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_pos_delete_builder(
        client,
        args.merchantId.clone(),
        args.targetMerchantId.clone(),
        args.storeCode.clone(),
    )?;
    content_pos_delete_execute(builder)
}

/// GET {merchantId}/pos/{targetMerchantId}/store
/// Creates a store for the given merchant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_pos_insert_execute()` to send, or `content_pos_insert` for simplest API.

pub fn content_pos_insert_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    targetMerchantId: String,
    body: &PosStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/store",
        merchantId.as_str(),
        targetMerchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/pos/{targetMerchantId}/store
/// Creates a store for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_pos_insert_execute()` or `content_pos_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PosStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/pos/{targetMerchantId}/store
/// Creates a store for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_pos_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_pos_insert_task()`.
/// For the simplest API, use `content_pos_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_pos_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_pos_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_pos_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPosInsertArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: targetMerchantId
    pub targetMerchantId: String,
    /// Request body.
    pub body: PosStore,
}

/// GET {merchantId}/pos/{targetMerchantId}/store
/// Creates a store for the given merchant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_pos_insert_builder()` + `content_pos_insert_execute()`.
/// For task-level control, use `content_pos_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_insert(
    client: &SimpleHttpClient,
    args: &ContentPosInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_pos_insert_builder(
        client,
        args.merchantId.clone(),
        args.targetMerchantId.clone(),
        &args.body,
    )?;
    content_pos_insert_execute(builder)
}

/// GET {merchantId}/pos/{targetMerchantId}/inventory
/// Submit inventory for the given merchant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_pos_inventory_execute()` to send, or `content_pos_inventory` for simplest API.

pub fn content_pos_inventory_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    targetMerchantId: String,
    body: &PosInventoryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/inventory",
        merchantId.as_str(),
        targetMerchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/pos/{targetMerchantId}/inventory
/// Submit inventory for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_pos_inventory_execute()` or `content_pos_inventory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_inventory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosInventoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PosInventoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/pos/{targetMerchantId}/inventory
/// Submit inventory for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_pos_inventory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_pos_inventory_task()`.
/// For the simplest API, use `content_pos_inventory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_pos_inventory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosInventoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_pos_inventory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_pos_inventory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPosInventoryArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: targetMerchantId
    pub targetMerchantId: String,
    /// Request body.
    pub body: PosInventoryRequest,
}

/// GET {merchantId}/pos/{targetMerchantId}/inventory
/// Submit inventory for the given merchant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_pos_inventory_builder()` + `content_pos_inventory_execute()`.
/// For task-level control, use `content_pos_inventory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_inventory(
    client: &SimpleHttpClient,
    args: &ContentPosInventoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosInventoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_pos_inventory_builder(
        client,
        args.merchantId.clone(),
        args.targetMerchantId.clone(),
        &args.body,
    )?;
    content_pos_inventory_execute(builder)
}

/// GET {merchantId}/pos/{targetMerchantId}/sale
/// Submit a sale event for the given merchant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_pos_sale_execute()` to send, or `content_pos_sale` for simplest API.

pub fn content_pos_sale_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    targetMerchantId: String,
    body: &PosSaleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/sale",
        merchantId.as_str(),
        targetMerchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/pos/{targetMerchantId}/sale
/// Submit a sale event for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_pos_sale_execute()` or `content_pos_sale`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_sale_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_sale_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosSaleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PosSaleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/pos/{targetMerchantId}/sale
/// Submit a sale event for the given merchant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_pos_sale_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_pos_sale_task()`.
/// For the simplest API, use `content_pos_sale()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pos_sale_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_pos_sale_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosSaleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_pos_sale_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_pos_sale`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPosSaleArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: targetMerchantId
    pub targetMerchantId: String,
    /// Request body.
    pub body: PosSaleRequest,
}

/// GET {merchantId}/pos/{targetMerchantId}/sale
/// Submit a sale event for the given merchant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_pos_sale_builder()` + `content_pos_sale_execute()`.
/// For task-level control, use `content_pos_sale_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pos_sale(
    client: &SimpleHttpClient,
    args: &ContentPosSaleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PosSaleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_pos_sale_builder(
        client,
        args.merchantId.clone(),
        args.targetMerchantId.clone(),
        &args.body,
    )?;
    content_pos_sale_execute(builder)
}

/// GET {merchantId}/productdeliverytime
/// Creates or updates the delivery time of a product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_productdeliverytime_create_execute()` to send, or `content_productdeliverytime_create` for simplest API.

pub fn content_productdeliverytime_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &ProductDeliveryTime,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productdeliverytime",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/productdeliverytime
/// Creates or updates the delivery time of a product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_productdeliverytime_create_execute()` or `content_productdeliverytime_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productdeliverytime_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productdeliverytime_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductDeliveryTime>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductDeliveryTime = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/productdeliverytime
/// Creates or updates the delivery time of a product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_productdeliverytime_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_productdeliverytime_create_task()`.
/// For the simplest API, use `content_productdeliverytime_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productdeliverytime_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_productdeliverytime_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductDeliveryTime>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_productdeliverytime_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_productdeliverytime_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductdeliverytimeCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: ProductDeliveryTime,
}

/// GET {merchantId}/productdeliverytime
/// Creates or updates the delivery time of a product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_productdeliverytime_create_builder()` + `content_productdeliverytime_create_execute()`.
/// For task-level control, use `content_productdeliverytime_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productdeliverytime_create(
    client: &SimpleHttpClient,
    args: &ContentProductdeliverytimeCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductDeliveryTime>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_productdeliverytime_create_builder(client, args.merchantId.clone(), &args.body)?;
    content_productdeliverytime_create_execute(builder)
}

/// GET {merchantId}/productdeliverytime/{productId}
/// Deletes the delivery time of a product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_productdeliverytime_delete_execute()` to send, or `content_productdeliverytime_delete` for simplest API.

pub fn content_productdeliverytime_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    productId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productdeliverytime/{}",
        merchantId.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/productdeliverytime/{productId}
/// Deletes the delivery time of a product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_productdeliverytime_delete_execute()` or `content_productdeliverytime_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productdeliverytime_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productdeliverytime_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/productdeliverytime/{productId}
/// Deletes the delivery time of a product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_productdeliverytime_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_productdeliverytime_delete_task()`.
/// For the simplest API, use `content_productdeliverytime_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productdeliverytime_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_productdeliverytime_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_productdeliverytime_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_productdeliverytime_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductdeliverytimeDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: productId
    pub productId: String,
}

/// GET {merchantId}/productdeliverytime/{productId}
/// Deletes the delivery time of a product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_productdeliverytime_delete_builder()` + `content_productdeliverytime_delete_execute()`.
/// For task-level control, use `content_productdeliverytime_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productdeliverytime_delete(
    client: &SimpleHttpClient,
    args: &ContentProductdeliverytimeDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_productdeliverytime_delete_builder(
        client,
        args.merchantId.clone(),
        args.productId.clone(),
    )?;
    content_productdeliverytime_delete_execute(builder)
}

/// GET products/batch
/// Retrieves, inserts, and deletes multiple products in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_products_custombatch_execute()` to send, or `content_products_custombatch` for simplest API.

pub fn content_products_custombatch_builder(
    client: &SimpleHttpClient,
    body: &ProductsCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/products/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET products/batch
/// Retrieves, inserts, and deletes multiple products in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_products_custombatch_execute()` or `content_products_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_products_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_products_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductsCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductsCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET products/batch
/// Retrieves, inserts, and deletes multiple products in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_products_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_products_custombatch_task()`.
/// For the simplest API, use `content_products_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_products_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_products_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_products_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_products_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductsCustombatchArgs {
    /// Request body.
    pub body: ProductsCustomBatchRequest,
}

/// GET products/batch
/// Retrieves, inserts, and deletes multiple products in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_products_custombatch_builder()` + `content_products_custombatch_execute()`.
/// For task-level control, use `content_products_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_products_custombatch(
    client: &SimpleHttpClient,
    args: &ContentProductsCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_products_custombatch_builder(client, &args.body)?;
    content_products_custombatch_execute(builder)
}

/// GET {merchantId}/products/{productId}
/// Deletes a product from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_products_delete_execute()` to send, or `content_products_delete` for simplest API.

pub fn content_products_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    productId: String,
    feedId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}",
        merchantId.as_str(),
        productId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = feedId {
        query_parts.push(format!("feedId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/products/{productId}
/// Deletes a product from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_products_delete_execute()` or `content_products_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_products_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_products_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/products/{productId}
/// Deletes a product from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_products_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_products_delete_task()`.
/// For the simplest API, use `content_products_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_products_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_products_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_products_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_products_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductsDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: feedId
    pub feedId: Option<String>,
}

/// GET {merchantId}/products/{productId}
/// Deletes a product from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_products_delete_builder()` + `content_products_delete_execute()`.
/// For task-level control, use `content_products_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_products_delete(
    client: &SimpleHttpClient,
    args: &ContentProductsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_products_delete_builder(
        client,
        args.merchantId.clone(),
        args.productId.clone(),
        args.feedId.clone(),
    )?;
    content_products_delete_execute(builder)
}

/// GET {merchantId}/products
/// Uploads a product to your Merchant Center account. If an item with the same channel, `contentLanguage`, `offerId`, and `targetCountry` already exists, this method updates that entry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_products_insert_execute()` to send, or `content_products_insert` for simplest API.

pub fn content_products_insert_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    feedId: Option<String>,
    body: &Product,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = feedId {
        query_parts.push(format!("feedId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/products
/// Uploads a product to your Merchant Center account. If an item with the same channel, `contentLanguage`, `offerId`, and `targetCountry` already exists, this method updates that entry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_products_insert_execute()` or `content_products_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_products_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_products_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Product = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/products
/// Uploads a product to your Merchant Center account. If an item with the same channel, `contentLanguage`, `offerId`, and `targetCountry` already exists, this method updates that entry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_products_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_products_insert_task()`.
/// For the simplest API, use `content_products_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_products_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_products_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_products_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_products_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductsInsertArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: feedId
    pub feedId: Option<String>,
    /// Request body.
    pub body: Product,
}

/// GET {merchantId}/products
/// Uploads a product to your Merchant Center account. If an item with the same channel, `contentLanguage`, `offerId`, and `targetCountry` already exists, this method updates that entry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_products_insert_builder()` + `content_products_insert_execute()`.
/// For task-level control, use `content_products_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_products_insert(
    client: &SimpleHttpClient,
    args: &ContentProductsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Product>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_products_insert_builder(
        client,
        args.merchantId.clone(),
        args.feedId.clone(),
        &args.body,
    )?;
    content_products_insert_execute(builder)
}

/// GET productstatuses/batch
/// Gets the statuses of multiple products in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_productstatuses_custombatch_execute()` to send, or `content_productstatuses_custombatch` for simplest API.

pub fn content_productstatuses_custombatch_builder(
    client: &SimpleHttpClient,
    body: &ProductstatusesCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/productstatuses/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET productstatuses/batch
/// Gets the statuses of multiple products in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_productstatuses_custombatch_execute()` or `content_productstatuses_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productstatuses_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productstatuses_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductstatusesCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductstatusesCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET productstatuses/batch
/// Gets the statuses of multiple products in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_productstatuses_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_productstatuses_custombatch_task()`.
/// For the simplest API, use `content_productstatuses_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productstatuses_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_productstatuses_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductstatusesCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_productstatuses_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_productstatuses_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductstatusesCustombatchArgs {
    /// Request body.
    pub body: ProductstatusesCustomBatchRequest,
}

/// GET productstatuses/batch
/// Gets the statuses of multiple products in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_productstatuses_custombatch_builder()` + `content_productstatuses_custombatch_execute()`.
/// For task-level control, use `content_productstatuses_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productstatuses_custombatch(
    client: &SimpleHttpClient,
    args: &ContentProductstatusesCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductstatusesCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_productstatuses_custombatch_builder(client, &args.body)?;
    content_productstatuses_custombatch_execute(builder)
}

/// GET {merchantId}/productstatuses/{productId}
/// Gets the status of a product from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_productstatuses_get_execute()` to send, or `content_productstatuses_get` for simplest API.

pub fn content_productstatuses_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    productId: String,
    destinations: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productstatuses/{}",
        merchantId.as_str(),
        productId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinations {
        query_parts.push(format!("destinations={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/productstatuses/{productId}
/// Gets the status of a product from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_productstatuses_get_execute()` or `content_productstatuses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productstatuses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/productstatuses/{productId}
/// Gets the status of a product from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_productstatuses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_productstatuses_get_task()`.
/// For the simplest API, use `content_productstatuses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productstatuses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_productstatuses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_productstatuses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_productstatuses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductstatusesGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: destinations
    pub destinations: Option<String>,
}

/// GET {merchantId}/productstatuses/{productId}
/// Gets the status of a product from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_productstatuses_get_builder()` + `content_productstatuses_get_execute()`.
/// For task-level control, use `content_productstatuses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productstatuses_get(
    client: &SimpleHttpClient,
    args: &ContentProductstatusesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_productstatuses_get_builder(
        client,
        args.merchantId.clone(),
        args.productId.clone(),
        args.destinations.clone(),
    )?;
    content_productstatuses_get_execute(builder)
}

/// GET {merchantId}/productstatuses
/// Lists the statuses of the products in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_productstatuses_list_execute()` to send, or `content_productstatuses_list` for simplest API.

pub fn content_productstatuses_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    destinations: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productstatuses",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinations {
        query_parts.push(format!("destinations={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/productstatuses
/// Lists the statuses of the products in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_productstatuses_list_execute()` or `content_productstatuses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productstatuses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductstatusesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductstatusesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/productstatuses
/// Lists the statuses of the products in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_productstatuses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_productstatuses_list_task()`.
/// For the simplest API, use `content_productstatuses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_productstatuses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_productstatuses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductstatusesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_productstatuses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_productstatuses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentProductstatusesListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: destinations
    pub destinations: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/productstatuses
/// Lists the statuses of the products in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_productstatuses_list_builder()` + `content_productstatuses_list_execute()`.
/// For task-level control, use `content_productstatuses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_productstatuses_list(
    client: &SimpleHttpClient,
    args: &ContentProductstatusesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ProductstatusesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_productstatuses_list_builder(
        client,
        args.merchantId.clone(),
        args.destinations.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    content_productstatuses_list_execute(builder)
}

/// GET {merchantId}/promotions
/// Inserts a promotion for your Merchant Center account. If the promotion already exists, then it updates the promotion instead. To [end or delete] (<https://developers.google.`com/shopping-content/guides/promotions`#end_a_promotion>) a promotion update the time period of the promotion to a time that has already passed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_promotions_create_execute()` to send, or `content_promotions_create` for simplest API.

pub fn content_promotions_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &Promotion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/promotions",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/promotions
/// Inserts a promotion for your Merchant Center account. If the promotion already exists, then it updates the promotion instead. To [end or delete] (<https://developers.google.`com/shopping-content/guides/promotions`#end_a_promotion>) a promotion update the time period of the promotion to a time that has already passed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_promotions_create_execute()` or `content_promotions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_promotions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_promotions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Promotion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Promotion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/promotions
/// Inserts a promotion for your Merchant Center account. If the promotion already exists, then it updates the promotion instead. To [end or delete] (<https://developers.google.`com/shopping-content/guides/promotions`#end_a_promotion>) a promotion update the time period of the promotion to a time that has already passed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_promotions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_promotions_create_task()`.
/// For the simplest API, use `content_promotions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_promotions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_promotions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_promotions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_promotions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPromotionsCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: Promotion,
}

/// GET {merchantId}/promotions
/// Inserts a promotion for your Merchant Center account. If the promotion already exists, then it updates the promotion instead. To [end or delete] (<https://developers.google.`com/shopping-content/guides/promotions`#end_a_promotion>) a promotion update the time period of the promotion to a time that has already passed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_promotions_create_builder()` + `content_promotions_create_execute()`.
/// For task-level control, use `content_promotions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_promotions_create(
    client: &SimpleHttpClient,
    args: &ContentPromotionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_promotions_create_builder(client, args.merchantId.clone(), &args.body)?;
    content_promotions_create_execute(builder)
}

/// GET {merchantId}/promotions/{id}
/// Retrieves a promotion from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_promotions_get_execute()` to send, or `content_promotions_get` for simplest API.

pub fn content_promotions_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/promotions/{}",
        merchantId.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/promotions/{id}
/// Retrieves a promotion from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_promotions_get_execute()` or `content_promotions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_promotions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_promotions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Promotion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Promotion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/promotions/{id}
/// Retrieves a promotion from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_promotions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_promotions_get_task()`.
/// For the simplest API, use `content_promotions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_promotions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_promotions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_promotions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_promotions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPromotionsGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET {merchantId}/promotions/{id}
/// Retrieves a promotion from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_promotions_get_builder()` + `content_promotions_get_execute()`.
/// For task-level control, use `content_promotions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_promotions_get(
    client: &SimpleHttpClient,
    args: &ContentPromotionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Promotion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_promotions_get_builder(client, args.merchantId.clone(), args.id.clone())?;
    content_promotions_get_execute(builder)
}

/// GET {merchantId}/pubsubnotificationsettings
/// Retrieves a Merchant Center account's pubsub notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_pubsubnotificationsettings_get_execute()` to send, or `content_pubsubnotificationsettings_get` for simplest API.

pub fn content_pubsubnotificationsettings_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pubsubnotificationsettings",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/pubsubnotificationsettings
/// Retrieves a Merchant Center account's pubsub notification settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_pubsubnotificationsettings_get_execute()` or `content_pubsubnotificationsettings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pubsubnotificationsettings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pubsubnotificationsettings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PubsubNotificationSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PubsubNotificationSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/pubsubnotificationsettings
/// Retrieves a Merchant Center account's pubsub notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_pubsubnotificationsettings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_pubsubnotificationsettings_get_task()`.
/// For the simplest API, use `content_pubsubnotificationsettings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_pubsubnotificationsettings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_pubsubnotificationsettings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PubsubNotificationSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_pubsubnotificationsettings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_pubsubnotificationsettings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentPubsubnotificationsettingsGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/pubsubnotificationsettings
/// Retrieves a Merchant Center account's pubsub notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_pubsubnotificationsettings_get_builder()` + `content_pubsubnotificationsettings_get_execute()`.
/// For task-level control, use `content_pubsubnotificationsettings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_pubsubnotificationsettings_get(
    client: &SimpleHttpClient,
    args: &ContentPubsubnotificationsettingsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PubsubNotificationSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_pubsubnotificationsettings_get_builder(client, args.merchantId.clone())?;
    content_pubsubnotificationsettings_get_execute(builder)
}

/// GET {merchantId}/quotas
/// Lists the daily call quota and usage per method for your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_quotas_list_execute()` to send, or `content_quotas_list` for simplest API.

pub fn content_quotas_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/quotas",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/quotas
/// Lists the daily call quota and usage per method for your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_quotas_list_execute()` or `content_quotas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_quotas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_quotas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMethodQuotasResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMethodQuotasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/quotas
/// Lists the daily call quota and usage per method for your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_quotas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_quotas_list_task()`.
/// For the simplest API, use `content_quotas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_quotas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_quotas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMethodQuotasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_quotas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_quotas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentQuotasListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/quotas
/// Lists the daily call quota and usage per method for your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_quotas_list_builder()` + `content_quotas_list_execute()`.
/// For task-level control, use `content_quotas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_quotas_list(
    client: &SimpleHttpClient,
    args: &ContentQuotasListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMethodQuotasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_quotas_list_builder(
        client,
        args.merchantId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    content_quotas_list_execute(builder)
}

/// GET {merchantId}/recommendations/generate
/// Generates recommendations for a merchant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_recommendations_generate_execute()` to send, or `content_recommendations_generate` for simplest API.

pub fn content_recommendations_generate_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    allowedTag: Option<String>,
    languageCode: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/recommendations/generate",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowedTag {
        query_parts.push(format!("allowedTag={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/recommendations/generate
/// Generates recommendations for a merchant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_recommendations_generate_execute()` or `content_recommendations_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_recommendations_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_recommendations_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateRecommendationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateRecommendationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/recommendations/generate
/// Generates recommendations for a merchant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_recommendations_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_recommendations_generate_task()`.
/// For the simplest API, use `content_recommendations_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_recommendations_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_recommendations_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_recommendations_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_recommendations_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentRecommendationsGenerateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: allowedTag
    pub allowedTag: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET {merchantId}/recommendations/generate
/// Generates recommendations for a merchant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_recommendations_generate_builder()` + `content_recommendations_generate_execute()`.
/// For task-level control, use `content_recommendations_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_recommendations_generate(
    client: &SimpleHttpClient,
    args: &ContentRecommendationsGenerateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_recommendations_generate_builder(
        client,
        args.merchantId.clone(),
        args.allowedTag.clone(),
        args.languageCode.clone(),
    )?;
    content_recommendations_generate_execute(builder)
}

/// GET {merchantId}/recommendations/reportInteraction
/// Reports an interaction on a recommendation for a merchant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_recommendations_report_interaction_execute()` to send, or `content_recommendations_report_interaction` for simplest API.

pub fn content_recommendations_report_interaction_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &ReportInteractionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/recommendations/reportInteraction",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/recommendations/reportInteraction
/// Reports an interaction on a recommendation for a merchant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_recommendations_report_interaction_execute()` or `content_recommendations_report_interaction`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_recommendations_report_interaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_recommendations_report_interaction_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/recommendations/reportInteraction
/// Reports an interaction on a recommendation for a merchant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_recommendations_report_interaction_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_recommendations_report_interaction_task()`.
/// For the simplest API, use `content_recommendations_report_interaction()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_recommendations_report_interaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_recommendations_report_interaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_recommendations_report_interaction_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_recommendations_report_interaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentRecommendationsReportInteractionArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: ReportInteractionRequest,
}

/// GET {merchantId}/recommendations/reportInteraction
/// Reports an interaction on a recommendation for a merchant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_recommendations_report_interaction_builder()` + `content_recommendations_report_interaction_execute()`.
/// For task-level control, use `content_recommendations_report_interaction_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_recommendations_report_interaction(
    client: &SimpleHttpClient,
    args: &ContentRecommendationsReportInteractionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_recommendations_report_interaction_builder(
        client,
        args.merchantId.clone(),
        &args.body,
    )?;
    content_recommendations_report_interaction_execute(builder)
}

/// GET regionalinventory/batch
/// Updates regional inventory for multiple products or regions in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_regionalinventory_custombatch_execute()` to send, or `content_regionalinventory_custombatch` for simplest API.

pub fn content_regionalinventory_custombatch_builder(
    client: &SimpleHttpClient,
    body: &RegionalinventoryCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/regionalinventory/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET regionalinventory/batch
/// Updates regional inventory for multiple products or regions in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_regionalinventory_custombatch_execute()` or `content_regionalinventory_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regionalinventory_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regionalinventory_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionalinventoryCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionalinventoryCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET regionalinventory/batch
/// Updates regional inventory for multiple products or regions in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_regionalinventory_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_regionalinventory_custombatch_task()`.
/// For the simplest API, use `content_regionalinventory_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regionalinventory_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_regionalinventory_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionalinventoryCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_regionalinventory_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_regionalinventory_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentRegionalinventoryCustombatchArgs {
    /// Request body.
    pub body: RegionalinventoryCustomBatchRequest,
}

/// GET regionalinventory/batch
/// Updates regional inventory for multiple products or regions in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_regionalinventory_custombatch_builder()` + `content_regionalinventory_custombatch_execute()`.
/// For task-level control, use `content_regionalinventory_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regionalinventory_custombatch(
    client: &SimpleHttpClient,
    args: &ContentRegionalinventoryCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionalinventoryCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_regionalinventory_custombatch_builder(client, &args.body)?;
    content_regionalinventory_custombatch_execute(builder)
}

/// GET {merchantId}/products/{productId}/regionalinventory
/// Updates the regional inventory of a product in your Merchant Center account. If a regional inventory with the same region ID already exists, this method updates that entry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_regionalinventory_insert_execute()` to send, or `content_regionalinventory_insert` for simplest API.

pub fn content_regionalinventory_insert_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    productId: String,
    body: &RegionalInventory,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}/regionalinventory",
        merchantId.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/products/{productId}/regionalinventory
/// Updates the regional inventory of a product in your Merchant Center account. If a regional inventory with the same region ID already exists, this method updates that entry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_regionalinventory_insert_execute()` or `content_regionalinventory_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regionalinventory_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regionalinventory_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionalInventory>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionalInventory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/products/{productId}/regionalinventory
/// Updates the regional inventory of a product in your Merchant Center account. If a regional inventory with the same region ID already exists, this method updates that entry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_regionalinventory_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_regionalinventory_insert_task()`.
/// For the simplest API, use `content_regionalinventory_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regionalinventory_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_regionalinventory_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionalInventory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_regionalinventory_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_regionalinventory_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentRegionalinventoryInsertArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: RegionalInventory,
}

/// GET {merchantId}/products/{productId}/regionalinventory
/// Updates the regional inventory of a product in your Merchant Center account. If a regional inventory with the same region ID already exists, this method updates that entry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_regionalinventory_insert_builder()` + `content_regionalinventory_insert_execute()`.
/// For task-level control, use `content_regionalinventory_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regionalinventory_insert(
    client: &SimpleHttpClient,
    args: &ContentRegionalinventoryInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionalInventory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_regionalinventory_insert_builder(
        client,
        args.merchantId.clone(),
        args.productId.clone(),
        &args.body,
    )?;
    content_regionalinventory_insert_execute(builder)
}

/// GET {merchantId}/regions
/// Creates a region definition in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_regions_create_execute()` to send, or `content_regions_create` for simplest API.

pub fn content_regions_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    regionId: Option<String>,
    body: &Region,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = regionId {
        query_parts.push(format!("regionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/regions
/// Creates a region definition in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_regions_create_execute()` or `content_regions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Region>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Region = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/regions
/// Creates a region definition in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_regions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_regions_create_task()`.
/// For the simplest API, use `content_regions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_regions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Region>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_regions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_regions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentRegionsCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: regionId
    pub regionId: Option<String>,
    /// Request body.
    pub body: Region,
}

/// GET {merchantId}/regions
/// Creates a region definition in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_regions_create_builder()` + `content_regions_create_execute()`.
/// For task-level control, use `content_regions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regions_create(
    client: &SimpleHttpClient,
    args: &ContentRegionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Region>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_regions_create_builder(
        client,
        args.merchantId.clone(),
        args.regionId.clone(),
        &args.body,
    )?;
    content_regions_create_execute(builder)
}

/// GET {merchantId}/regions/{regionId}
/// Deletes a region definition from your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_regions_delete_execute()` to send, or `content_regions_delete` for simplest API.

pub fn content_regions_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    regionId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions/{}",
        merchantId.as_str(),
        regionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/regions/{regionId}
/// Deletes a region definition from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_regions_delete_execute()` or `content_regions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/regions/{regionId}
/// Deletes a region definition from your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_regions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_regions_delete_task()`.
/// For the simplest API, use `content_regions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_regions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_regions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_regions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_regions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentRegionsDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: regionId
    pub regionId: String,
}

/// GET {merchantId}/regions/{regionId}
/// Deletes a region definition from your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_regions_delete_builder()` + `content_regions_delete_execute()`.
/// For task-level control, use `content_regions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_regions_delete(
    client: &SimpleHttpClient,
    args: &ContentRegionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        content_regions_delete_builder(client, args.merchantId.clone(), args.regionId.clone())?;
    content_regions_delete_execute(builder)
}

/// GET {merchantId}/reports/search
/// Retrieves merchant performance metrics matching the search query and optionally segmented by selected dimensions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_reports_search_execute()` to send, or `content_reports_search` for simplest API.

pub fn content_reports_search_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/reports/search",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/reports/search
/// Retrieves merchant performance metrics matching the search query and optionally segmented by selected dimensions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_reports_search_execute()` or `content_reports_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_reports_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/reports/search
/// Retrieves merchant performance metrics matching the search query and optionally segmented by selected dimensions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_reports_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_reports_search_task()`.
/// For the simplest API, use `content_reports_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_reports_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_reports_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_reports_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentReportsSearchArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: SearchRequest,
}

/// GET {merchantId}/reports/search
/// Retrieves merchant performance metrics matching the search query and optionally segmented by selected dimensions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_reports_search_builder()` + `content_reports_search_execute()`.
/// For task-level control, use `content_reports_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_reports_search(
    client: &SimpleHttpClient,
    args: &ContentReportsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_reports_search_builder(client, args.merchantId.clone(), &args.body)?;
    content_reports_search_execute(builder)
}

/// GET {merchantId}/returnpolicyonline
/// Creates a new return policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_returnpolicyonline_create_execute()` to send, or `content_returnpolicyonline_create` for simplest API.

pub fn content_returnpolicyonline_create_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &ReturnPolicyOnline,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/returnpolicyonline
/// Creates a new return policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_returnpolicyonline_create_execute()` or `content_returnpolicyonline_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_returnpolicyonline_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_returnpolicyonline_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReturnPolicyOnline>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReturnPolicyOnline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/returnpolicyonline
/// Creates a new return policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_returnpolicyonline_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_returnpolicyonline_create_task()`.
/// For the simplest API, use `content_returnpolicyonline_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_returnpolicyonline_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_returnpolicyonline_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReturnPolicyOnline>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_returnpolicyonline_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_returnpolicyonline_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlineCreateArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: ReturnPolicyOnline,
}

/// GET {merchantId}/returnpolicyonline
/// Creates a new return policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_returnpolicyonline_create_builder()` + `content_returnpolicyonline_create_execute()`.
/// For task-level control, use `content_returnpolicyonline_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_returnpolicyonline_create(
    client: &SimpleHttpClient,
    args: &ContentReturnpolicyonlineCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReturnPolicyOnline>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_returnpolicyonline_create_builder(client, args.merchantId.clone(), &args.body)?;
    content_returnpolicyonline_create_execute(builder)
}

/// GET {merchantId}/returnpolicyonline/{returnPolicyId}
/// Deletes an existing return policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_returnpolicyonline_delete_execute()` to send, or `content_returnpolicyonline_delete` for simplest API.

pub fn content_returnpolicyonline_delete_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    returnPolicyId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline/{}",
        merchantId.as_str(),
        returnPolicyId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/returnpolicyonline/{returnPolicyId}
/// Deletes an existing return policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_returnpolicyonline_delete_execute()` or `content_returnpolicyonline_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_returnpolicyonline_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_returnpolicyonline_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/returnpolicyonline/{returnPolicyId}
/// Deletes an existing return policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_returnpolicyonline_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_returnpolicyonline_delete_task()`.
/// For the simplest API, use `content_returnpolicyonline_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_returnpolicyonline_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_returnpolicyonline_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_returnpolicyonline_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_returnpolicyonline_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlineDeleteArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: returnPolicyId
    pub returnPolicyId: String,
}

/// GET {merchantId}/returnpolicyonline/{returnPolicyId}
/// Deletes an existing return policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_returnpolicyonline_delete_builder()` + `content_returnpolicyonline_delete_execute()`.
/// For task-level control, use `content_returnpolicyonline_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_returnpolicyonline_delete(
    client: &SimpleHttpClient,
    args: &ContentReturnpolicyonlineDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_returnpolicyonline_delete_builder(
        client,
        args.merchantId.clone(),
        args.returnPolicyId.clone(),
    )?;
    content_returnpolicyonline_delete_execute(builder)
}

/// GET shippingsettings/batch
/// Retrieves and updates the shipping settings of multiple accounts in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shippingsettings_custombatch_execute()` to send, or `content_shippingsettings_custombatch` for simplest API.

pub fn content_shippingsettings_custombatch_builder(
    client: &SimpleHttpClient,
    body: &ShippingsettingsCustomBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/shippingsettings/batch",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET shippingsettings/batch
/// Retrieves and updates the shipping settings of multiple accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shippingsettings_custombatch_execute()` or `content_shippingsettings_custombatch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_custombatch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingsettingsCustomBatchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShippingsettingsCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET shippingsettings/batch
/// Retrieves and updates the shipping settings of multiple accounts in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shippingsettings_custombatch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shippingsettings_custombatch_task()`.
/// For the simplest API, use `content_shippingsettings_custombatch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_custombatch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shippingsettings_custombatch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_shippingsettings_custombatch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shippingsettings_custombatch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShippingsettingsCustombatchArgs {
    /// Request body.
    pub body: ShippingsettingsCustomBatchRequest,
}

/// GET shippingsettings/batch
/// Retrieves and updates the shipping settings of multiple accounts in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shippingsettings_custombatch_builder()` + `content_shippingsettings_custombatch_execute()`.
/// For task-level control, use `content_shippingsettings_custombatch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_custombatch(
    client: &SimpleHttpClient,
    args: &ContentShippingsettingsCustombatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsCustomBatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_shippingsettings_custombatch_builder(client, &args.body)?;
    content_shippingsettings_custombatch_execute(builder)
}

/// GET {merchantId}/shippingsettings/{accountId}
/// Retrieves the shipping settings of the account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shippingsettings_get_execute()` to send, or `content_shippingsettings_get` for simplest API.

pub fn content_shippingsettings_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    accountId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shippingsettings/{}",
        merchantId.as_str(),
        accountId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/shippingsettings/{accountId}
/// Retrieves the shipping settings of the account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shippingsettings_get_execute()` or `content_shippingsettings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShippingSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/shippingsettings/{accountId}
/// Retrieves the shipping settings of the account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shippingsettings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shippingsettings_get_task()`.
/// For the simplest API, use `content_shippingsettings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shippingsettings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShippingSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_shippingsettings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shippingsettings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Path parameter: accountId
    pub accountId: String,
}

/// GET {merchantId}/shippingsettings/{accountId}
/// Retrieves the shipping settings of the account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shippingsettings_get_builder()` + `content_shippingsettings_get_execute()`.
/// For task-level control, use `content_shippingsettings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_get(
    client: &SimpleHttpClient,
    args: &ContentShippingsettingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShippingSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_shippingsettings_get_builder(
        client,
        args.merchantId.clone(),
        args.accountId.clone(),
    )?;
    content_shippingsettings_get_execute(builder)
}

/// GET {merchantId}/supportedCarriers
/// Retrieves supported carriers and carrier services for an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shippingsettings_getsupportedcarriers_execute()` to send, or `content_shippingsettings_getsupportedcarriers` for simplest API.

pub fn content_shippingsettings_getsupportedcarriers_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/supportedCarriers",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/supportedCarriers
/// Retrieves supported carriers and carrier services for an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shippingsettings_getsupportedcarriers_execute()` or `content_shippingsettings_getsupportedcarriers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_getsupportedcarriers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_getsupportedcarriers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingsettingsGetSupportedCarriersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShippingsettingsGetSupportedCarriersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/supportedCarriers
/// Retrieves supported carriers and carrier services for an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shippingsettings_getsupportedcarriers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shippingsettings_getsupportedcarriers_task()`.
/// For the simplest API, use `content_shippingsettings_getsupportedcarriers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_getsupportedcarriers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shippingsettings_getsupportedcarriers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsGetSupportedCarriersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_shippingsettings_getsupportedcarriers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shippingsettings_getsupportedcarriers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetsupportedcarriersArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/supportedCarriers
/// Retrieves supported carriers and carrier services for an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shippingsettings_getsupportedcarriers_builder()` + `content_shippingsettings_getsupportedcarriers_execute()`.
/// For task-level control, use `content_shippingsettings_getsupportedcarriers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_getsupportedcarriers(
    client: &SimpleHttpClient,
    args: &ContentShippingsettingsGetsupportedcarriersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsGetSupportedCarriersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_shippingsettings_getsupportedcarriers_builder(client, args.merchantId.clone())?;
    content_shippingsettings_getsupportedcarriers_execute(builder)
}

/// GET {merchantId}/supportedHolidays
/// Retrieves supported holidays for an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shippingsettings_getsupportedholidays_execute()` to send, or `content_shippingsettings_getsupportedholidays` for simplest API.

pub fn content_shippingsettings_getsupportedholidays_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/supportedHolidays",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/supportedHolidays
/// Retrieves supported holidays for an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shippingsettings_getsupportedholidays_execute()` or `content_shippingsettings_getsupportedholidays`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_getsupportedholidays_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_getsupportedholidays_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingsettingsGetSupportedHolidaysResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShippingsettingsGetSupportedHolidaysResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/supportedHolidays
/// Retrieves supported holidays for an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shippingsettings_getsupportedholidays_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shippingsettings_getsupportedholidays_task()`.
/// For the simplest API, use `content_shippingsettings_getsupportedholidays()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_getsupportedholidays_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shippingsettings_getsupportedholidays_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsGetSupportedHolidaysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_shippingsettings_getsupportedholidays_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shippingsettings_getsupportedholidays`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetsupportedholidaysArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/supportedHolidays
/// Retrieves supported holidays for an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shippingsettings_getsupportedholidays_builder()` + `content_shippingsettings_getsupportedholidays_execute()`.
/// For task-level control, use `content_shippingsettings_getsupportedholidays_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_getsupportedholidays(
    client: &SimpleHttpClient,
    args: &ContentShippingsettingsGetsupportedholidaysArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsGetSupportedHolidaysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        content_shippingsettings_getsupportedholidays_builder(client, args.merchantId.clone())?;
    content_shippingsettings_getsupportedholidays_execute(builder)
}

/// GET {merchantId}/supportedPickupServices
/// Retrieves supported pickup services for an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shippingsettings_getsupportedpickupservices_execute()` to send, or `content_shippingsettings_getsupportedpickupservices` for simplest API.

pub fn content_shippingsettings_getsupportedpickupservices_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/supportedPickupServices",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/supportedPickupServices
/// Retrieves supported pickup services for an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shippingsettings_getsupportedpickupservices_execute()` or `content_shippingsettings_getsupportedpickupservices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_getsupportedpickupservices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_getsupportedpickupservices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ShippingsettingsGetSupportedPickupServicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShippingsettingsGetSupportedPickupServicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/supportedPickupServices
/// Retrieves supported pickup services for an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shippingsettings_getsupportedpickupservices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shippingsettings_getsupportedpickupservices_task()`.
/// For the simplest API, use `content_shippingsettings_getsupportedpickupservices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_getsupportedpickupservices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shippingsettings_getsupportedpickupservices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsGetSupportedPickupServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_shippingsettings_getsupportedpickupservices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shippingsettings_getsupportedpickupservices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetsupportedpickupservicesArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/supportedPickupServices
/// Retrieves supported pickup services for an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shippingsettings_getsupportedpickupservices_builder()` + `content_shippingsettings_getsupportedpickupservices_execute()`.
/// For task-level control, use `content_shippingsettings_getsupportedpickupservices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_getsupportedpickupservices(
    client: &SimpleHttpClient,
    args: &ContentShippingsettingsGetsupportedpickupservicesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsGetSupportedPickupServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_shippingsettings_getsupportedpickupservices_builder(
        client,
        args.merchantId.clone(),
    )?;
    content_shippingsettings_getsupportedpickupservices_execute(builder)
}

/// GET {merchantId}/shippingsettings
/// Lists the shipping settings of the sub-accounts in your Merchant Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shippingsettings_list_execute()` to send, or `content_shippingsettings_list` for simplest API.

pub fn content_shippingsettings_list_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shippingsettings",
        merchantId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/shippingsettings
/// Lists the shipping settings of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shippingsettings_list_execute()` or `content_shippingsettings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingsettingsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShippingsettingsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/shippingsettings
/// Lists the shipping settings of the sub-accounts in your Merchant Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shippingsettings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shippingsettings_list_task()`.
/// For the simplest API, use `content_shippingsettings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shippingsettings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shippingsettings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = content_shippingsettings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shippingsettings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShippingsettingsListArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET {merchantId}/shippingsettings
/// Lists the shipping settings of the sub-accounts in your Merchant Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shippingsettings_list_builder()` + `content_shippingsettings_list_execute()`.
/// For task-level control, use `content_shippingsettings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shippingsettings_list(
    client: &SimpleHttpClient,
    args: &ContentShippingsettingsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShippingsettingsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = content_shippingsettings_list_builder(
        client,
        args.merchantId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    content_shippingsettings_list_execute(builder)
}

/// GET {merchantId}/shoppingadsprogram
/// Retrieves the status and review eligibility for the Shopping Ads program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shoppingadsprogram_get_execute()` to send, or `content_shoppingadsprogram_get` for simplest API.

pub fn content_shoppingadsprogram_get_builder(
    client: &SimpleHttpClient,
    merchantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shoppingadsprogram",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {merchantId}/shoppingadsprogram
/// Retrieves the status and review eligibility for the Shopping Ads program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shoppingadsprogram_get_execute()` or `content_shoppingadsprogram_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shoppingadsprogram_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shoppingadsprogram_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShoppingAdsProgramStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShoppingAdsProgramStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/shoppingadsprogram
/// Retrieves the status and review eligibility for the Shopping Ads program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shoppingadsprogram_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shoppingadsprogram_get_task()`.
/// For the simplest API, use `content_shoppingadsprogram_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shoppingadsprogram_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shoppingadsprogram_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShoppingAdsProgramStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = content_shoppingadsprogram_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shoppingadsprogram_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShoppingadsprogramGetArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
}

/// GET {merchantId}/shoppingadsprogram
/// Retrieves the status and review eligibility for the Shopping Ads program. Returns errors and warnings if they require action to resolve, will become disapprovals, or impact impressions. Use accountstatuses to view all issues for an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shoppingadsprogram_get_builder()` + `content_shoppingadsprogram_get_execute()`.
/// For task-level control, use `content_shoppingadsprogram_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shoppingadsprogram_get(
    client: &SimpleHttpClient,
    args: &ContentShoppingadsprogramGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShoppingAdsProgramStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = content_shoppingadsprogram_get_builder(client, args.merchantId.clone())?;
    content_shoppingadsprogram_get_execute(builder)
}

/// GET {merchantId}/shoppingadsprogram/requestreview
/// Requests a review of Shopping ads in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `content_shoppingadsprogram_requestreview_execute()` to send, or `content_shoppingadsprogram_requestreview` for simplest API.

pub fn content_shoppingadsprogram_requestreview_builder(
    client: &SimpleHttpClient,
    merchantId: String,
    body: &RequestReviewShoppingAdsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shoppingadsprogram/requestreview",
        merchantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {merchantId}/shoppingadsprogram/requestreview
/// Requests a review of Shopping ads in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `content_shoppingadsprogram_requestreview_execute()` or `content_shoppingadsprogram_requestreview`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shoppingadsprogram_requestreview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shoppingadsprogram_requestreview_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {merchantId}/shoppingadsprogram/requestreview
/// Requests a review of Shopping ads in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `content_shoppingadsprogram_requestreview_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `content_shoppingadsprogram_requestreview_task()`.
/// For the simplest API, use `content_shoppingadsprogram_requestreview()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `content_shoppingadsprogram_requestreview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn content_shoppingadsprogram_requestreview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = content_shoppingadsprogram_requestreview_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`content_shoppingadsprogram_requestreview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentShoppingadsprogramRequestreviewArgs {
    /// Path parameter: merchantId
    pub merchantId: String,
    /// Request body.
    pub body: RequestReviewShoppingAdsRequest,
}

/// GET {merchantId}/shoppingadsprogram/requestreview
/// Requests a review of Shopping ads in a specific region. This method deprecated. Use the MerchantSupportService to view product and account issues and request a review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `content_shoppingadsprogram_requestreview_builder()` + `content_shoppingadsprogram_requestreview_execute()`.
/// For task-level control, use `content_shoppingadsprogram_requestreview_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn content_shoppingadsprogram_requestreview(
    client: &SimpleHttpClient,
    args: &ContentShoppingadsprogramRequestreviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = content_shoppingadsprogram_requestreview_builder(
        client,
        args.merchantId.clone(),
        &args.body,
    )?;
    content_shoppingadsprogram_requestreview_execute(builder)
}
