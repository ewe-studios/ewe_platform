//! Auto-generated API clients for gcp/merchantapi.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_merchant_reviews_insert_execute()` to send, or `merchantapi_accounts_merchant_reviews_insert` for simplest API.

pub fn merchantapi_accounts_merchant_reviews_insert_builder(
    client: &SimpleHttpClient,
    parent: String,
    dataSource: Option<String>,
    body: &MerchantReview,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{}/merchantReviews:insert",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataSource {
        query_parts.push(format!("dataSource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `merchantapi_accounts_merchant_reviews_insert_execute()` or `merchantapi_accounts_merchant_reviews_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_merchant_reviews_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MerchantReview>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MerchantReview = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `merchantapi_accounts_merchant_reviews_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `merchantapi_accounts_merchant_reviews_insert_task()`.
/// For the simplest API, use `merchantapi_accounts_merchant_reviews_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_merchant_reviews_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn merchantapi_accounts_merchant_reviews_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MerchantReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = merchantapi_accounts_merchant_reviews_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`merchantapi_accounts_merchant_reviews_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsMerchantReviewsInsertArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dataSource
    pub dataSource: Option<String>,
    /// Request body.
    pub body: MerchantReview,
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_merchant_reviews_insert_builder()` + `merchantapi_accounts_merchant_reviews_insert_execute()`.
/// For task-level control, use `merchantapi_accounts_merchant_reviews_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_insert(
    client: &SimpleHttpClient,
    args: &MerchantapiAccountsMerchantReviewsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MerchantReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_merchant_reviews_insert_builder(
        client,
        args.parent.clone(),
        args.dataSource.clone(),
        &args.body,
    )?;
    merchantapi_accounts_merchant_reviews_insert_execute(builder)
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews
/// Lists merchant reviews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_merchant_reviews_list_execute()` to send, or `merchantapi_accounts_merchant_reviews_list` for simplest API.

pub fn merchantapi_accounts_merchant_reviews_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://merchantapi.googleapis.com/reviews/v1beta/accounts/{}/merchantReviews",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews
/// Lists merchant reviews.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `merchantapi_accounts_merchant_reviews_list_execute()` or `merchantapi_accounts_merchant_reviews_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_merchant_reviews_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMerchantReviewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMerchantReviewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews
/// Lists merchant reviews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `merchantapi_accounts_merchant_reviews_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `merchantapi_accounts_merchant_reviews_list_task()`.
/// For the simplest API, use `merchantapi_accounts_merchant_reviews_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_merchant_reviews_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn merchantapi_accounts_merchant_reviews_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMerchantReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = merchantapi_accounts_merchant_reviews_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`merchantapi_accounts_merchant_reviews_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsMerchantReviewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews
/// Lists merchant reviews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_merchant_reviews_list_builder()` + `merchantapi_accounts_merchant_reviews_list_execute()`.
/// For task-level control, use `merchantapi_accounts_merchant_reviews_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_list(
    client: &SimpleHttpClient,
    args: &MerchantapiAccountsMerchantReviewsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMerchantReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_merchant_reviews_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    merchantapi_accounts_merchant_reviews_list_execute(builder)
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews:insert
/// Inserts a product review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_product_reviews_insert_execute()` to send, or `merchantapi_accounts_product_reviews_insert` for simplest API.

pub fn merchantapi_accounts_product_reviews_insert_builder(
    client: &SimpleHttpClient,
    parent: String,
    dataSource: Option<String>,
    body: &ProductReview,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{}/productReviews:insert",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataSource {
        query_parts.push(format!("dataSource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews:insert
/// Inserts a product review.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `merchantapi_accounts_product_reviews_insert_execute()` or `merchantapi_accounts_product_reviews_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_product_reviews_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductReview>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductReview = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews:insert
/// Inserts a product review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `merchantapi_accounts_product_reviews_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `merchantapi_accounts_product_reviews_insert_task()`.
/// For the simplest API, use `merchantapi_accounts_product_reviews_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_product_reviews_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn merchantapi_accounts_product_reviews_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = merchantapi_accounts_product_reviews_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`merchantapi_accounts_product_reviews_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsProductReviewsInsertArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dataSource
    pub dataSource: Option<String>,
    /// Request body.
    pub body: ProductReview,
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews:insert
/// Inserts a product review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_product_reviews_insert_builder()` + `merchantapi_accounts_product_reviews_insert_execute()`.
/// For task-level control, use `merchantapi_accounts_product_reviews_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_insert(
    client: &SimpleHttpClient,
    args: &MerchantapiAccountsProductReviewsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_product_reviews_insert_builder(
        client,
        args.parent.clone(),
        args.dataSource.clone(),
        &args.body,
    )?;
    merchantapi_accounts_product_reviews_insert_execute(builder)
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews
/// Lists product reviews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_product_reviews_list_execute()` to send, or `merchantapi_accounts_product_reviews_list` for simplest API.

pub fn merchantapi_accounts_product_reviews_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://merchantapi.googleapis.com/reviews/v1beta/accounts/{}/productReviews",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews
/// Lists product reviews.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `merchantapi_accounts_product_reviews_list_execute()` or `merchantapi_accounts_product_reviews_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_product_reviews_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductReviewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProductReviewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews
/// Lists product reviews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `merchantapi_accounts_product_reviews_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `merchantapi_accounts_product_reviews_list_task()`.
/// For the simplest API, use `merchantapi_accounts_product_reviews_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `merchantapi_accounts_product_reviews_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn merchantapi_accounts_product_reviews_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProductReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = merchantapi_accounts_product_reviews_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`merchantapi_accounts_product_reviews_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsProductReviewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET reviews/v1beta/accounts/{accountsId}/productReviews
/// Lists product reviews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_product_reviews_list_builder()` + `merchantapi_accounts_product_reviews_list_execute()`.
/// For task-level control, use `merchantapi_accounts_product_reviews_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_list(
    client: &SimpleHttpClient,
    args: &MerchantapiAccountsProductReviewsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProductReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_product_reviews_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    merchantapi_accounts_product_reviews_list_execute(builder)
}
