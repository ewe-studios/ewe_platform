//! Auto-generated API clients for gcp/merchantapi.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET reviews/v1beta/{+name}
/// Deletes merchant review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_merchant_reviews_delete_execute()` to send, or `merchantapi_accounts_merchant_reviews_delete` for simplest API.

pub fn merchantapi_accounts_merchant_reviews_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comreviews/v1beta/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Deletes merchant review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_merchant_reviews_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_merchant_reviews_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Deletes merchant review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_merchant_reviews_delete_builder()` + `merchantapi_accounts_merchant_reviews_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_merchant_reviews_delete_builder(client, name)?;
    merchantapi_accounts_merchant_reviews_delete_execute(builder)
}

/// GET reviews/v1beta/{+name}
/// Gets a merchant review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_merchant_reviews_get_execute()` to send, or `merchantapi_accounts_merchant_reviews_get` for simplest API.

pub fn merchantapi_accounts_merchant_reviews_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comreviews/v1beta/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Gets a merchant review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_merchant_reviews_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_merchant_reviews_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MerchantReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MerchantReview = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Gets a merchant review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_merchant_reviews_get_builder()` + `merchantapi_accounts_merchant_reviews_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MerchantReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_merchant_reviews_get_builder(client, name)?;
    merchantapi_accounts_merchant_reviews_get_execute(builder)
}

/// GET reviews/v1beta/{+parent}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_merchant_reviews_insert_execute()` to send, or `merchantapi_accounts_merchant_reviews_insert` for simplest API.

pub fn merchantapi_accounts_merchant_reviews_insert_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataSource: Option<&str>,
    body: &MerchantReview,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comreviews/v1beta/{+parent}/merchantReviews:insert",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dataSource",
            dataSource
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET reviews/v1beta/{+parent}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_merchant_reviews_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_merchant_reviews_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MerchantReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MerchantReview = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+parent}/merchantReviews:insert
/// Inserts a review for your Merchant Center account. If the review already exists, then the review is replaced with the new instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_merchant_reviews_insert_builder()` + `merchantapi_accounts_merchant_reviews_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_insert(
    client: &SimpleHttpClient,
    parent: &str,
    dataSource: Option<&str>,
    body: &MerchantReview,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MerchantReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        merchantapi_accounts_merchant_reviews_insert_builder(client, parent, dataSource, body)?;
    merchantapi_accounts_merchant_reviews_insert_execute(builder)
}

/// GET reviews/v1beta/{+parent}/merchantReviews
/// Lists merchant reviews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_merchant_reviews_list_execute()` to send, or `merchantapi_accounts_merchant_reviews_list` for simplest API.

pub fn merchantapi_accounts_merchant_reviews_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comreviews/v1beta/{+parent}/merchantReviews",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET reviews/v1beta/{+parent}/merchantReviews
/// Lists merchant reviews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_merchant_reviews_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_merchant_reviews_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMerchantReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMerchantReviewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+parent}/merchantReviews
/// Lists merchant reviews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_merchant_reviews_list_builder()` + `merchantapi_accounts_merchant_reviews_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_merchant_reviews_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMerchantReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        merchantapi_accounts_merchant_reviews_list_builder(client, parent, pageSize, pageToken)?;
    merchantapi_accounts_merchant_reviews_list_execute(builder)
}

/// GET reviews/v1beta/{+name}
/// Deletes a product review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_product_reviews_delete_execute()` to send, or `merchantapi_accounts_product_reviews_delete` for simplest API.

pub fn merchantapi_accounts_product_reviews_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comreviews/v1beta/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Deletes a product review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_product_reviews_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_product_reviews_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Deletes a product review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_product_reviews_delete_builder()` + `merchantapi_accounts_product_reviews_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_product_reviews_delete_builder(client, name)?;
    merchantapi_accounts_product_reviews_delete_execute(builder)
}

/// GET reviews/v1beta/{+name}
/// Gets a product review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_product_reviews_get_execute()` to send, or `merchantapi_accounts_product_reviews_get` for simplest API.

pub fn merchantapi_accounts_product_reviews_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comreviews/v1beta/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Gets a product review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_product_reviews_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_product_reviews_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductReview = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+name}
/// Gets a product review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_product_reviews_get_builder()` + `merchantapi_accounts_product_reviews_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = merchantapi_accounts_product_reviews_get_builder(client, name)?;
    merchantapi_accounts_product_reviews_get_execute(builder)
}

/// GET reviews/v1beta/{+parent}/productReviews:insert
/// Inserts a product review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_product_reviews_insert_execute()` to send, or `merchantapi_accounts_product_reviews_insert` for simplest API.

pub fn merchantapi_accounts_product_reviews_insert_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataSource: Option<&str>,
    body: &ProductReview,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comreviews/v1beta/{+parent}/productReviews:insert",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dataSource",
            dataSource
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET reviews/v1beta/{+parent}/productReviews:insert
/// Inserts a product review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_product_reviews_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_product_reviews_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductReview = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+parent}/productReviews:insert
/// Inserts a product review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_product_reviews_insert_builder()` + `merchantapi_accounts_product_reviews_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_insert(
    client: &SimpleHttpClient,
    parent: &str,
    dataSource: Option<&str>,
    body: &ProductReview,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductReview>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        merchantapi_accounts_product_reviews_insert_builder(client, parent, dataSource, body)?;
    merchantapi_accounts_product_reviews_insert_execute(builder)
}

/// GET reviews/v1beta/{+parent}/productReviews
/// Lists product reviews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `merchantapi_accounts_product_reviews_list_execute()` to send, or `merchantapi_accounts_product_reviews_list` for simplest API.

pub fn merchantapi_accounts_product_reviews_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comreviews/v1beta/{+parent}/productReviews",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET reviews/v1beta/{+parent}/productReviews
/// Lists product reviews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `merchantapi_accounts_product_reviews_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from merchantapi_accounts_product_reviews_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProductReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProductReviewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET reviews/v1beta/{+parent}/productReviews
/// Lists product reviews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `merchantapi_accounts_product_reviews_list_builder()` + `merchantapi_accounts_product_reviews_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn merchantapi_accounts_product_reviews_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProductReviewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        merchantapi_accounts_product_reviews_list_builder(client, parent, pageSize, pageToken)?;
    merchantapi_accounts_product_reviews_list_execute(builder)
}
