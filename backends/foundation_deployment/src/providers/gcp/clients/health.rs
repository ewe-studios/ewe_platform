//! Auto-generated API clients for gcp/health.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v4/users/{usersId}/identity
/// Gets the user's identity. It includes the legacy Fitbit user ID and the Google user ID and it can be used by migrating clients to map identifiers between the two systems.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_get_identity_execute()` to send, or `health_users_get_identity` for simplest API.

pub fn health_users_get_identity_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://health.googleapis.com/v4/users/{}/identity", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}/identity
/// Gets the user's identity. It includes the legacy Fitbit user ID and the Google user ID and it can be used by migrating clients to map identifiers between the two systems.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_get_identity_execute()` or `health_users_get_identity`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_get_identity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_get_identity_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Identity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Identity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}/identity
/// Gets the user's identity. It includes the legacy Fitbit user ID and the Google user ID and it can be used by migrating clients to map identifiers between the two systems.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_get_identity_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_get_identity_task()`.
/// For the simplest API, use `health_users_get_identity()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_get_identity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_get_identity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Identity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_get_identity_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_get_identity`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersGetIdentityArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v4/users/{usersId}/identity
/// Gets the user's identity. It includes the legacy Fitbit user ID and the Google user ID and it can be used by migrating clients to map identifiers between the two systems.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_get_identity_builder()` + `health_users_get_identity_execute()`.
/// For task-level control, use `health_users_get_identity_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_get_identity(
    client: &SimpleHttpClient,
    args: &HealthUsersGetIdentityArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Identity>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_get_identity_builder(client, &args.name)?;
    health_users_get_identity_execute(builder)
}

/// GET v4/users/{usersId}/profile
/// Returns user Profile details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_get_profile_execute()` to send, or `health_users_get_profile` for simplest API.

pub fn health_users_get_profile_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://health.googleapis.com/v4/users/{}/profile", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}/profile
/// Returns user Profile details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_get_profile_execute()` or `health_users_get_profile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_get_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_get_profile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}/profile
/// Returns user Profile details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_get_profile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_get_profile_task()`.
/// For the simplest API, use `health_users_get_profile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_get_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_get_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_get_profile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_get_profile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersGetProfileArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v4/users/{usersId}/profile
/// Returns user Profile details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_get_profile_builder()` + `health_users_get_profile_execute()`.
/// For task-level control, use `health_users_get_profile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_get_profile(
    client: &SimpleHttpClient,
    args: &HealthUsersGetProfileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_get_profile_builder(client, &args.name)?;
    health_users_get_profile_execute(builder)
}

/// GET v4/users/{usersId}/settings
/// Returns user settings details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_get_settings_execute()` to send, or `health_users_get_settings` for simplest API.

pub fn health_users_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://health.googleapis.com/v4/users/{}/settings", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}/settings
/// Returns user settings details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_get_settings_execute()` or `health_users_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}/settings
/// Returns user settings details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_get_settings_task()`.
/// For the simplest API, use `health_users_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v4/users/{usersId}/settings
/// Returns user settings details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_get_settings_builder()` + `health_users_get_settings_execute()`.
/// For task-level control, use `health_users_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_get_settings(
    client: &SimpleHttpClient,
    args: &HealthUsersGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_get_settings_builder(client, &args.name)?;
    health_users_get_settings_execute(builder)
}

/// PATCH v4/users/{usersId}/profile
/// Updates the user's profile details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_update_profile_execute()` to send, or `health_users_update_profile` for simplest API.

pub fn health_users_update_profile_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://health.googleapis.com/v4/users/{}/profile", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/users/{usersId}/profile
/// Updates the user's profile details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_update_profile_execute()` or `health_users_update_profile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_update_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_update_profile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/users/{usersId}/profile
/// Updates the user's profile details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_update_profile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_update_profile_task()`.
/// For the simplest API, use `health_users_update_profile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_update_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_update_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_update_profile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_update_profile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersUpdateProfileArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v4/users/{usersId}/profile
/// Updates the user's profile details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_update_profile_builder()` + `health_users_update_profile_execute()`.
/// For task-level control, use `health_users_update_profile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_update_profile(
    client: &SimpleHttpClient,
    args: &HealthUsersUpdateProfileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_update_profile_builder(client, &args.name, &args.updateMask)?;
    health_users_update_profile_execute(builder)
}

/// PATCH v4/users/{usersId}/settings
/// Updates the user's settings details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_update_settings_execute()` to send, or `health_users_update_settings` for simplest API.

pub fn health_users_update_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://health.googleapis.com/v4/users/{}/settings", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/users/{usersId}/settings
/// Updates the user's settings details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_update_settings_execute()` or `health_users_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/users/{usersId}/settings
/// Updates the user's settings details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_update_settings_task()`.
/// For the simplest API, use `health_users_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v4/users/{usersId}/settings
/// Updates the user's settings details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_update_settings_builder()` + `health_users_update_settings_execute()`.
/// For task-level control, use `health_users_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_update_settings(
    client: &SimpleHttpClient,
    args: &HealthUsersUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_update_settings_builder(client, &args.name, &args.updateMask)?;
    health_users_update_settings_execute(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:batchDelete
/// Delete a batch of identifyable data points.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_batch_delete_execute()` to send, or `health_users_data_types_data_points_batch_delete` for simplest API.

pub fn health_users_data_types_data_points_batch_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints:batchDelete",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:batchDelete
/// Delete a batch of identifyable data points.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_batch_delete_execute()` or `health_users_data_types_data_points_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:batchDelete
/// Delete a batch of identifyable data points.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_batch_delete_task()`.
/// For the simplest API, use `health_users_data_types_data_points_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsBatchDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:batchDelete
/// Delete a batch of identifyable data points.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_batch_delete_builder()` + `health_users_data_types_data_points_batch_delete_execute()`.
/// For task-level control, use `health_users_data_types_data_points_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_batch_delete(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_batch_delete_builder(client, &args.parent)?;
    health_users_data_types_data_points_batch_delete_execute(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Creates a single identifiable data point.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_create_execute()` to send, or `health_users_data_types_data_points_create` for simplest API.

pub fn health_users_data_types_data_points_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Creates a single identifiable data point.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_create_execute()` or `health_users_data_types_data_points_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Creates a single identifiable data point.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_create_task()`.
/// For the simplest API, use `health_users_data_types_data_points_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Creates a single identifiable data point.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_create_builder()` + `health_users_data_types_data_points_create_execute()`.
/// For task-level control, use `health_users_data_types_data_points_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_create(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_create_builder(client, &args.parent)?;
    health_users_data_types_data_points_create_execute(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:dailyRollUp
/// Roll up data points over civil time intervals for supported data types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_daily_roll_up_execute()` to send, or `health_users_data_types_data_points_daily_roll_up` for simplest API.

pub fn health_users_data_types_data_points_daily_roll_up_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints:dailyRollUp",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:dailyRollUp
/// Roll up data points over civil time intervals for supported data types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_daily_roll_up_execute()` or `health_users_data_types_data_points_daily_roll_up`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_daily_roll_up_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_daily_roll_up_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DailyRollUpDataPointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DailyRollUpDataPointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:dailyRollUp
/// Roll up data points over civil time intervals for supported data types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_daily_roll_up_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_daily_roll_up_task()`.
/// For the simplest API, use `health_users_data_types_data_points_daily_roll_up()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_daily_roll_up_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_daily_roll_up_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DailyRollUpDataPointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_daily_roll_up_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_daily_roll_up`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsDailyRollUpArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:dailyRollUp
/// Roll up data points over civil time intervals for supported data types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_daily_roll_up_builder()` + `health_users_data_types_data_points_daily_roll_up_execute()`.
/// For task-level control, use `health_users_data_types_data_points_daily_roll_up_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_daily_roll_up(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsDailyRollUpArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DailyRollUpDataPointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_daily_roll_up_builder(client, &args.parent)?;
    health_users_data_types_data_points_daily_roll_up_execute(builder)
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}:exportExerciseTcx
/// Exports exercise data in TCX format. Note: While the Authorization section below states that any one of the listed scopes is accepted, this specific method requires the user to provide both one of the activity_and_fitness scopes (normal or readonly) AND one of the location scopes (normal or readonly) in their access token to succeed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_export_exercise_tcx_execute()` to send, or `health_users_data_types_data_points_export_exercise_tcx` for simplest API.

pub fn health_users_data_types_data_points_export_exercise_tcx_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    partialData: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}:exportExerciseTcx",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partialData.as_ref() {
        query_parts.push(format!("partialData={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}:exportExerciseTcx
/// Exports exercise data in TCX format. Note: While the Authorization section below states that any one of the listed scopes is accepted, this specific method requires the user to provide both one of the activity_and_fitness scopes (normal or readonly) AND one of the location scopes (normal or readonly) in their access token to succeed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_export_exercise_tcx_execute()` or `health_users_data_types_data_points_export_exercise_tcx`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_export_exercise_tcx_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_export_exercise_tcx_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExportExerciseTcxResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExportExerciseTcxResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}:exportExerciseTcx
/// Exports exercise data in TCX format. Note: While the Authorization section below states that any one of the listed scopes is accepted, this specific method requires the user to provide both one of the activity_and_fitness scopes (normal or readonly) AND one of the location scopes (normal or readonly) in their access token to succeed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_export_exercise_tcx_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_export_exercise_tcx_task()`.
/// For the simplest API, use `health_users_data_types_data_points_export_exercise_tcx()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_export_exercise_tcx_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_export_exercise_tcx_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExportExerciseTcxResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_export_exercise_tcx_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_export_exercise_tcx`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsExportExerciseTcxArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: partialData
    pub partialData: Option<Option<String>>,
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}:exportExerciseTcx
/// Exports exercise data in TCX format. Note: While the Authorization section below states that any one of the listed scopes is accepted, this specific method requires the user to provide both one of the activity_and_fitness scopes (normal or readonly) AND one of the location scopes (normal or readonly) in their access token to succeed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_export_exercise_tcx_builder()` + `health_users_data_types_data_points_export_exercise_tcx_execute()`.
/// For task-level control, use `health_users_data_types_data_points_export_exercise_tcx_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_export_exercise_tcx(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsExportExerciseTcxArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExportExerciseTcxResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_export_exercise_tcx_builder(
        client,
        &args.name,
        &args.partialData,
    )?;
    health_users_data_types_data_points_export_exercise_tcx_execute(builder)
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Query user health and fitness data points.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_list_execute()` to send, or `health_users_data_types_data_points_list` for simplest API.

pub fn health_users_data_types_data_points_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Query user health and fitness data points.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_list_execute()` or `health_users_data_types_data_points_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataPointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataPointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Query user health and fitness data points.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_list_task()`.
/// For the simplest API, use `health_users_data_types_data_points_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataPointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints
/// Query user health and fitness data points.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_list_builder()` + `health_users_data_types_data_points_list_execute()`.
/// For task-level control, use `health_users_data_types_data_points_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_list(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataPointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    health_users_data_types_data_points_list_execute(builder)
}

/// PATCH v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}
/// Updates a single identifiable data point. If a data point with the specified name is not found, the request will fail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_patch_execute()` to send, or `health_users_data_types_data_points_patch` for simplest API.

pub fn health_users_data_types_data_points_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}
/// Updates a single identifiable data point. If a data point with the specified name is not found, the request will fail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_patch_execute()` or `health_users_data_types_data_points_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}
/// Updates a single identifiable data point. If a data point with the specified name is not found, the request will fail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_patch_task()`.
/// For the simplest API, use `health_users_data_types_data_points_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints/{dataPointsId}
/// Updates a single identifiable data point. If a data point with the specified name is not found, the request will fail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_patch_builder()` + `health_users_data_types_data_points_patch_execute()`.
/// For task-level control, use `health_users_data_types_data_points_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_patch(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_patch_builder(client, &args.name)?;
    health_users_data_types_data_points_patch_execute(builder)
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:reconcile
/// Reconcile data points from multiple data sources into a single data stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_reconcile_execute()` to send, or `health_users_data_types_data_points_reconcile` for simplest API.

pub fn health_users_data_types_data_points_reconcile_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dataSourceFamily: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints:reconcile",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataSourceFamily.as_ref() {
        query_parts.push(format!("dataSourceFamily={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:reconcile
/// Reconcile data points from multiple data sources into a single data stream.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_reconcile_execute()` or `health_users_data_types_data_points_reconcile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_reconcile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_reconcile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReconcileDataPointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReconcileDataPointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:reconcile
/// Reconcile data points from multiple data sources into a single data stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_reconcile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_reconcile_task()`.
/// For the simplest API, use `health_users_data_types_data_points_reconcile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_reconcile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_reconcile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReconcileDataPointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_reconcile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_reconcile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsReconcileArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dataSourceFamily
    pub dataSourceFamily: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:reconcile
/// Reconcile data points from multiple data sources into a single data stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_reconcile_builder()` + `health_users_data_types_data_points_reconcile_execute()`.
/// For task-level control, use `health_users_data_types_data_points_reconcile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_reconcile(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsReconcileArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReconcileDataPointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_reconcile_builder(
        client,
        &args.parent,
        &args.dataSourceFamily,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    health_users_data_types_data_points_reconcile_execute(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:rollUp
/// Roll up data points over physical time intervals for supported data types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `health_users_data_types_data_points_roll_up_execute()` to send, or `health_users_data_types_data_points_roll_up` for simplest API.

pub fn health_users_data_types_data_points_roll_up_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/users/{}/dataTypes/{dataTypesId}/dataPoints:rollUp",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:rollUp
/// Roll up data points over physical time intervals for supported data types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `health_users_data_types_data_points_roll_up_execute()` or `health_users_data_types_data_points_roll_up`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_roll_up_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_roll_up_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RollUpDataPointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RollUpDataPointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:rollUp
/// Roll up data points over physical time intervals for supported data types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `health_users_data_types_data_points_roll_up_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `health_users_data_types_data_points_roll_up_task()`.
/// For the simplest API, use `health_users_data_types_data_points_roll_up()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `health_users_data_types_data_points_roll_up_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn health_users_data_types_data_points_roll_up_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RollUpDataPointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = health_users_data_types_data_points_roll_up_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`health_users_data_types_data_points_roll_up`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsRollUpArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v4/users/{usersId}/dataTypes/{dataTypesId}/dataPoints:rollUp
/// Roll up data points over physical time intervals for supported data types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `health_users_data_types_data_points_roll_up_builder()` + `health_users_data_types_data_points_roll_up_execute()`.
/// For task-level control, use `health_users_data_types_data_points_roll_up_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn health_users_data_types_data_points_roll_up(
    client: &SimpleHttpClient,
    args: &HealthUsersDataTypesDataPointsRollUpArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RollUpDataPointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = health_users_data_types_data_points_roll_up_builder(client, &args.parent)?;
    health_users_data_types_data_points_roll_up_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Identity
// =============================================================================

/// ResourceIdentifier implementation for Identity with HealthUsersGetIdentityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersGetIdentityArgs> for Identity {
    fn generate_resource_id(&self, input: &HealthUsersGetIdentityArgs) -> String {
        format!("gcp::health::Identity/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Identity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Profile
// =============================================================================

/// ResourceIdentifier implementation for Profile with HealthUsersGetProfileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersGetProfileArgs> for Profile {
    fn generate_resource_id(&self, input: &HealthUsersGetProfileArgs) -> String {
        format!("gcp::health::Profile/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Profile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with HealthUsersGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersGetSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &HealthUsersGetSettingsArgs) -> String {
        format!("gcp::health::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Profile
// =============================================================================

/// ResourceIdentifier implementation for Profile with HealthUsersUpdateProfileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersUpdateProfileArgs> for Profile {
    fn generate_resource_id(&self, input: &HealthUsersUpdateProfileArgs) -> String {
        format!("gcp::health::Profile/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Profile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with HealthUsersUpdateSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersUpdateSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &HealthUsersUpdateSettingsArgs) -> String {
        format!("gcp::health::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthUsersDataTypesDataPointsBatchDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsBatchDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthUsersDataTypesDataPointsBatchDeleteArgs,
    ) -> String {
        format!("gcp::health::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthUsersDataTypesDataPointsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &HealthUsersDataTypesDataPointsCreateArgs) -> String {
        format!("gcp::health::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DailyRollUpDataPointsResponse
// =============================================================================

/// ResourceIdentifier implementation for DailyRollUpDataPointsResponse with HealthUsersDataTypesDataPointsDailyRollUpArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsDailyRollUpArgs>
    for DailyRollUpDataPointsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthUsersDataTypesDataPointsDailyRollUpArgs,
    ) -> String {
        format!(
            "gcp::health::DailyRollUpDataPointsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::DailyRollUpDataPointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ExportExerciseTcxResponse
// =============================================================================

/// ResourceIdentifier implementation for ExportExerciseTcxResponse with HealthUsersDataTypesDataPointsExportExerciseTcxArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsExportExerciseTcxArgs>
    for ExportExerciseTcxResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthUsersDataTypesDataPointsExportExerciseTcxArgs,
    ) -> String {
        format!("gcp::health::ExportExerciseTcxResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::ExportExerciseTcxResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDataPointsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDataPointsResponse with HealthUsersDataTypesDataPointsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsListArgs> for ListDataPointsResponse {
    fn generate_resource_id(&self, input: &HealthUsersDataTypesDataPointsListArgs) -> String {
        format!("gcp::health::ListDataPointsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::ListDataPointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthUsersDataTypesDataPointsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &HealthUsersDataTypesDataPointsPatchArgs) -> String {
        format!("gcp::health::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReconcileDataPointsResponse
// =============================================================================

/// ResourceIdentifier implementation for ReconcileDataPointsResponse with HealthUsersDataTypesDataPointsReconcileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsReconcileArgs>
    for ReconcileDataPointsResponse
{
    fn generate_resource_id(&self, input: &HealthUsersDataTypesDataPointsReconcileArgs) -> String {
        format!("gcp::health::ReconcileDataPointsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::ReconcileDataPointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RollUpDataPointsResponse
// =============================================================================

/// ResourceIdentifier implementation for RollUpDataPointsResponse with HealthUsersDataTypesDataPointsRollUpArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthUsersDataTypesDataPointsRollUpArgs> for RollUpDataPointsResponse {
    fn generate_resource_id(&self, input: &HealthUsersDataTypesDataPointsRollUpArgs) -> String {
        format!("gcp::health::RollUpDataPointsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::health::RollUpDataPointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
