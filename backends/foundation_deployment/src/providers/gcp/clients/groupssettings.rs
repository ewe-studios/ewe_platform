//! Auto-generated API clients for gcp/groupssettings.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/groupssettings`

#![cfg(feature = "gcp/groupssettings")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET {groupUniqueId}
/// Gets one resource by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `groups_settings_groups_get_execute()` to send, or `groups_settings_groups_get` for simplest API.

pub fn groups_settings_groups_get_builder(
    client: &SimpleHttpClient,
    groupUniqueId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/groups/v1/groups/{}",
        groupUniqueId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {groupUniqueId}
/// Gets one resource by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `groups_settings_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from groups_settings_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn groups_settings_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Groups>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Groups = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`groups_settings_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GroupsSettingsGroupsGetArgs {
    /// Path parameter: groupUniqueId
    pub groupUniqueId: String,
}

/// GET {groupUniqueId}
/// Gets one resource by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `groups_settings_groups_get_builder()` + `groups_settings_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn groups_settings_groups_get(
    client: &SimpleHttpClient,
    args: &GroupsSettingsGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Groups>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = groups_settings_groups_get_builder(client, &args.groupUniqueId)?;
    groups_settings_groups_get_execute(builder)
}

/// GET {groupUniqueId}
/// Updates an existing resource. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `groups_settings_groups_patch_execute()` to send, or `groups_settings_groups_patch` for simplest API.

pub fn groups_settings_groups_patch_builder(
    client: &SimpleHttpClient,
    groupUniqueId: &str,
    body: &Groups,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/groups/v1/groups/{}",
        groupUniqueId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {groupUniqueId}
/// Updates an existing resource. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `groups_settings_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from groups_settings_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn groups_settings_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Groups>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Groups = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`groups_settings_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GroupsSettingsGroupsPatchArgs {
    /// Path parameter: groupUniqueId
    pub groupUniqueId: String,
    /// Request body.
    pub body: Groups,
}

/// GET {groupUniqueId}
/// Updates an existing resource. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `groups_settings_groups_patch_builder()` + `groups_settings_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn groups_settings_groups_patch(
    client: &SimpleHttpClient,
    args: &GroupsSettingsGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Groups>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = groups_settings_groups_patch_builder(client, &args.groupUniqueId, &args.body)?;
    groups_settings_groups_patch_execute(builder)
}

/// GET {groupUniqueId}
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `groups_settings_groups_update_execute()` to send, or `groups_settings_groups_update` for simplest API.

pub fn groups_settings_groups_update_builder(
    client: &SimpleHttpClient,
    groupUniqueId: &str,
    body: &Groups,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/groups/v1/groups/{}",
        groupUniqueId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET {groupUniqueId}
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `groups_settings_groups_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from groups_settings_groups_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn groups_settings_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Groups>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Groups = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`groups_settings_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GroupsSettingsGroupsUpdateArgs {
    /// Path parameter: groupUniqueId
    pub groupUniqueId: String,
    /// Request body.
    pub body: Groups,
}

/// GET {groupUniqueId}
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `groups_settings_groups_update_builder()` + `groups_settings_groups_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn groups_settings_groups_update(
    client: &SimpleHttpClient,
    args: &GroupsSettingsGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Groups>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = groups_settings_groups_update_builder(client, &args.groupUniqueId, &args.body)?;
    groups_settings_groups_update_execute(builder)
}
