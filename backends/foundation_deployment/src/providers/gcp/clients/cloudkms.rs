//! Auto-generated API clients for gcp/cloudkms.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/cloudkms`

#![cfg(feature = "gcp/cloudkms")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/autokeyConfig
/// Returns the AutokeyConfig for a folder or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_folders_get_autokey_config_execute()` to send, or `cloudkms_folders_get_autokey_config` for simplest API.

pub fn cloudkms_folders_get_autokey_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/folders/{}/autokeyConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/autokeyConfig
/// Returns the AutokeyConfig for a folder or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_folders_get_autokey_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_folders_get_autokey_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_get_autokey_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutokeyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_folders_get_autokey_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsFoldersGetAutokeyConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/autokeyConfig
/// Returns the AutokeyConfig for a folder or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_folders_get_autokey_config_builder()` + `cloudkms_folders_get_autokey_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_get_autokey_config(
    client: &SimpleHttpClient,
    args: &CloudkmsFoldersGetAutokeyConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_folders_get_autokey_config_builder(client, &args.name)?;
    cloudkms_folders_get_autokey_config_execute(builder)
}

/// GET v1/folders/{foldersId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_folders_get_kaj_policy_config_execute()` to send, or `cloudkms_folders_get_kaj_policy_config` for simplest API.

pub fn cloudkms_folders_get_kaj_policy_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/folders/{}/kajPolicyConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_folders_get_kaj_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_folders_get_kaj_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_get_kaj_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyAccessJustificationsPolicyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_folders_get_kaj_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsFoldersGetKajPolicyConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_folders_get_kaj_policy_config_builder()` + `cloudkms_folders_get_kaj_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_get_kaj_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsFoldersGetKajPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_folders_get_kaj_policy_config_builder(client, &args.name)?;
    cloudkms_folders_get_kaj_policy_config_execute(builder)
}

/// GET v1/folders/{foldersId}/autokeyConfig
/// Updates the AutokeyConfig for a folder or a project. The caller must have both cloudkms.`autokeyConfigs`.update permission on the parent folder and cloudkms.`cryptoKeys`.`setIamPolicy` permission on the provided key project. A KeyHandle creation in the folder's descendant projects will use this configuration to determine where to create the resulting CryptoKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_folders_update_autokey_config_execute()` to send, or `cloudkms_folders_update_autokey_config` for simplest API.

pub fn cloudkms_folders_update_autokey_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &AutokeyConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/folders/{}/autokeyConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/autokeyConfig
/// Updates the AutokeyConfig for a folder or a project. The caller must have both cloudkms.`autokeyConfigs`.update permission on the parent folder and cloudkms.`cryptoKeys`.`setIamPolicy` permission on the provided key project. A KeyHandle creation in the folder's descendant projects will use this configuration to determine where to create the resulting CryptoKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_folders_update_autokey_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_folders_update_autokey_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_update_autokey_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutokeyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_folders_update_autokey_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsFoldersUpdateAutokeyConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: AutokeyConfig,
}

/// GET v1/folders/{foldersId}/autokeyConfig
/// Updates the AutokeyConfig for a folder or a project. The caller must have both cloudkms.`autokeyConfigs`.update permission on the parent folder and cloudkms.`cryptoKeys`.`setIamPolicy` permission on the provided key project. A KeyHandle creation in the folder's descendant projects will use this configuration to determine where to create the resulting CryptoKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_folders_update_autokey_config_builder()` + `cloudkms_folders_update_autokey_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_update_autokey_config(
    client: &SimpleHttpClient,
    args: &CloudkmsFoldersUpdateAutokeyConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_folders_update_autokey_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_folders_update_autokey_config_execute(builder)
}

/// GET v1/folders/{foldersId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_folders_update_kaj_policy_config_execute()` to send, or `cloudkms_folders_update_kaj_policy_config` for simplest API.

pub fn cloudkms_folders_update_kaj_policy_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &KeyAccessJustificationsPolicyConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/folders/{}/kajPolicyConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_folders_update_kaj_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_folders_update_kaj_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_update_kaj_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyAccessJustificationsPolicyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_folders_update_kaj_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsFoldersUpdateKajPolicyConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: KeyAccessJustificationsPolicyConfig,
}

/// GET v1/folders/{foldersId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_folders_update_kaj_policy_config_builder()` + `cloudkms_folders_update_kaj_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_folders_update_kaj_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsFoldersUpdateKajPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_folders_update_kaj_policy_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_folders_update_kaj_policy_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_organizations_get_kaj_policy_config_execute()` to send, or `cloudkms_organizations_get_kaj_policy_config` for simplest API.

pub fn cloudkms_organizations_get_kaj_policy_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/organizations/{}/kajPolicyConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_organizations_get_kaj_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_organizations_get_kaj_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_organizations_get_kaj_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyAccessJustificationsPolicyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_organizations_get_kaj_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsOrganizationsGetKajPolicyConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_organizations_get_kaj_policy_config_builder()` + `cloudkms_organizations_get_kaj_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_organizations_get_kaj_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsOrganizationsGetKajPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_organizations_get_kaj_policy_config_builder(client, &args.name)?;
    cloudkms_organizations_get_kaj_policy_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_organizations_update_kaj_policy_config_execute()` to send, or `cloudkms_organizations_update_kaj_policy_config` for simplest API.

pub fn cloudkms_organizations_update_kaj_policy_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &KeyAccessJustificationsPolicyConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/organizations/{}/kajPolicyConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_organizations_update_kaj_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_organizations_update_kaj_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_organizations_update_kaj_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyAccessJustificationsPolicyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_organizations_update_kaj_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsOrganizationsUpdateKajPolicyConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: KeyAccessJustificationsPolicyConfig,
}

/// GET v1/organizations/{organizationsId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_organizations_update_kaj_policy_config_builder()` + `cloudkms_organizations_update_kaj_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_organizations_update_kaj_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsOrganizationsUpdateKajPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_organizations_update_kaj_policy_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_organizations_update_kaj_policy_config_execute(builder)
}

/// GET v1/projects/{projectsId}/autokeyConfig
/// Returns the AutokeyConfig for a folder or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_get_autokey_config_execute()` to send, or `cloudkms_projects_get_autokey_config` for simplest API.

pub fn cloudkms_projects_get_autokey_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/autokeyConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/autokeyConfig
/// Returns the AutokeyConfig for a folder or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_get_autokey_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_get_autokey_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_get_autokey_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutokeyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_get_autokey_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsGetAutokeyConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/autokeyConfig
/// Returns the AutokeyConfig for a folder or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_get_autokey_config_builder()` + `cloudkms_projects_get_autokey_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_get_autokey_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsGetAutokeyConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_get_autokey_config_builder(client, &args.name)?;
    cloudkms_projects_get_autokey_config_execute(builder)
}

/// GET v1/projects/{projectsId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_get_kaj_policy_config_execute()` to send, or `cloudkms_projects_get_kaj_policy_config` for simplest API.

pub fn cloudkms_projects_get_kaj_policy_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/kajPolicyConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_get_kaj_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_get_kaj_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_get_kaj_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyAccessJustificationsPolicyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_get_kaj_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsGetKajPolicyConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/kajPolicyConfig
/// Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_get_kaj_policy_config_builder()` + `cloudkms_projects_get_kaj_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_get_kaj_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsGetKajPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_get_kaj_policy_config_builder(client, &args.name)?;
    cloudkms_projects_get_kaj_policy_config_execute(builder)
}

/// GET v1/projects/{projectsId}:showEffectiveAutokeyConfig
/// Returns the effective Cloud KMS Autokey configuration for a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_show_effective_autokey_config_execute()` to send, or `cloudkms_projects_show_effective_autokey_config` for simplest API.

pub fn cloudkms_projects_show_effective_autokey_config_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}:showEffectiveAutokeyConfig",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}:showEffectiveAutokeyConfig
/// Returns the effective Cloud KMS Autokey configuration for a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_show_effective_autokey_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_show_effective_autokey_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_show_effective_autokey_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShowEffectiveAutokeyConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShowEffectiveAutokeyConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_show_effective_autokey_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsShowEffectiveAutokeyConfigArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}:showEffectiveAutokeyConfig
/// Returns the effective Cloud KMS Autokey configuration for a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_show_effective_autokey_config_builder()` + `cloudkms_projects_show_effective_autokey_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_show_effective_autokey_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsShowEffectiveAutokeyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ShowEffectiveAutokeyConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_show_effective_autokey_config_builder(client, &args.parent)?;
    cloudkms_projects_show_effective_autokey_config_execute(builder)
}

/// GET v1/projects/{projectsId}:showEffectiveKeyAccessJustificationsEnrollmentConfig
/// Returns the KeyAccessJustificationsEnrollmentConfig of the resource closest to the given project in hierarchy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_show_effective_key_access_justifications_enrollment_config_execute()` to send, or `cloudkms_projects_show_effective_key_access_justifications_enrollment_config` for simplest API.

pub fn cloudkms_projects_show_effective_key_access_justifications_enrollment_config_builder(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}:showEffectiveKeyAccessJustificationsEnrollmentConfig",
        project,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}:showEffectiveKeyAccessJustificationsEnrollmentConfig
/// Returns the KeyAccessJustificationsEnrollmentConfig of the resource closest to the given project in hierarchy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_show_effective_key_access_justifications_enrollment_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_show_effective_key_access_justifications_enrollment_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_show_effective_key_access_justifications_enrollment_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ShowEffectiveKeyAccessJustificationsEnrollmentConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShowEffectiveKeyAccessJustificationsEnrollmentConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_show_effective_key_access_justifications_enrollment_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsShowEffectiveKeyAccessJustificationsEnrollmentConfigArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET v1/projects/{projectsId}:showEffectiveKeyAccessJustificationsEnrollmentConfig
/// Returns the KeyAccessJustificationsEnrollmentConfig of the resource closest to the given project in hierarchy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_show_effective_key_access_justifications_enrollment_config_builder()` + `cloudkms_projects_show_effective_key_access_justifications_enrollment_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_show_effective_key_access_justifications_enrollment_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsShowEffectiveKeyAccessJustificationsEnrollmentConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ShowEffectiveKeyAccessJustificationsEnrollmentConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_show_effective_key_access_justifications_enrollment_config_builder(
            client,
            &args.project,
        )?;
    cloudkms_projects_show_effective_key_access_justifications_enrollment_config_execute(builder)
}

/// GET v1/projects/{projectsId}:showEffectiveKeyAccessJustificationsPolicyConfig
/// Returns the KeyAccessJustificationsPolicyConfig of the resource closest to the given project in hierarchy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_show_effective_key_access_justifications_policy_config_execute()` to send, or `cloudkms_projects_show_effective_key_access_justifications_policy_config` for simplest API.

pub fn cloudkms_projects_show_effective_key_access_justifications_policy_config_builder(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}:showEffectiveKeyAccessJustificationsPolicyConfig",
        project,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}:showEffectiveKeyAccessJustificationsPolicyConfig
/// Returns the KeyAccessJustificationsPolicyConfig of the resource closest to the given project in hierarchy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_show_effective_key_access_justifications_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_show_effective_key_access_justifications_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_show_effective_key_access_justifications_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ShowEffectiveKeyAccessJustificationsPolicyConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShowEffectiveKeyAccessJustificationsPolicyConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_show_effective_key_access_justifications_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsShowEffectiveKeyAccessJustificationsPolicyConfigArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET v1/projects/{projectsId}:showEffectiveKeyAccessJustificationsPolicyConfig
/// Returns the KeyAccessJustificationsPolicyConfig of the resource closest to the given project in hierarchy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_show_effective_key_access_justifications_policy_config_builder()` + `cloudkms_projects_show_effective_key_access_justifications_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_show_effective_key_access_justifications_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsShowEffectiveKeyAccessJustificationsPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ShowEffectiveKeyAccessJustificationsPolicyConfigResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_show_effective_key_access_justifications_policy_config_builder(
        client,
        &args.project,
    )?;
    cloudkms_projects_show_effective_key_access_justifications_policy_config_execute(builder)
}

/// GET v1/projects/{projectsId}/autokeyConfig
/// Updates the AutokeyConfig for a folder or a project. The caller must have both cloudkms.`autokeyConfigs`.update permission on the parent folder and cloudkms.`cryptoKeys`.`setIamPolicy` permission on the provided key project. A KeyHandle creation in the folder's descendant projects will use this configuration to determine where to create the resulting CryptoKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_update_autokey_config_execute()` to send, or `cloudkms_projects_update_autokey_config` for simplest API.

pub fn cloudkms_projects_update_autokey_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &AutokeyConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/autokeyConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/autokeyConfig
/// Updates the AutokeyConfig for a folder or a project. The caller must have both cloudkms.`autokeyConfigs`.update permission on the parent folder and cloudkms.`cryptoKeys`.`setIamPolicy` permission on the provided key project. A KeyHandle creation in the folder's descendant projects will use this configuration to determine where to create the resulting CryptoKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_update_autokey_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_update_autokey_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_update_autokey_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutokeyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_update_autokey_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsUpdateAutokeyConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: AutokeyConfig,
}

/// GET v1/projects/{projectsId}/autokeyConfig
/// Updates the AutokeyConfig for a folder or a project. The caller must have both cloudkms.`autokeyConfigs`.update permission on the parent folder and cloudkms.`cryptoKeys`.`setIamPolicy` permission on the provided key project. A KeyHandle creation in the folder's descendant projects will use this configuration to determine where to create the resulting CryptoKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_update_autokey_config_builder()` + `cloudkms_projects_update_autokey_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_update_autokey_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsUpdateAutokeyConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutokeyConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_update_autokey_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_update_autokey_config_execute(builder)
}

/// GET v1/projects/{projectsId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_update_kaj_policy_config_execute()` to send, or `cloudkms_projects_update_kaj_policy_config` for simplest API.

pub fn cloudkms_projects_update_kaj_policy_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &KeyAccessJustificationsPolicyConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/kajPolicyConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_update_kaj_policy_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_update_kaj_policy_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_update_kaj_policy_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyAccessJustificationsPolicyConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_update_kaj_policy_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsUpdateKajPolicyConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: KeyAccessJustificationsPolicyConfig,
}

/// GET v1/projects/{projectsId}/kajPolicyConfig
/// Updates the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_update_kaj_policy_config_builder()` + `cloudkms_projects_update_kaj_policy_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_update_kaj_policy_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsUpdateKajPolicyConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<KeyAccessJustificationsPolicyConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_update_kaj_policy_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_update_kaj_policy_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateRandomBytes
/// Generate random bytes using the Cloud KMS randomness source in the provided location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_generate_random_bytes_execute()` to send, or `cloudkms_projects_locations_generate_random_bytes` for simplest API.

pub fn cloudkms_projects_locations_generate_random_bytes_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GenerateRandomBytesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}:generateRandomBytes",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateRandomBytes
/// Generate random bytes using the Cloud KMS randomness source in the provided location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_generate_random_bytes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_generate_random_bytes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_generate_random_bytes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRandomBytesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateRandomBytesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_generate_random_bytes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsGenerateRandomBytesArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GenerateRandomBytesRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generateRandomBytes
/// Generate random bytes using the Cloud KMS randomness source in the provided location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_generate_random_bytes_builder()` + `cloudkms_projects_locations_generate_random_bytes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_generate_random_bytes(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsGenerateRandomBytesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRandomBytesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_generate_random_bytes_builder(
        client,
        &args.location,
        &args.body,
    )?;
    cloudkms_projects_locations_generate_random_bytes_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_get_execute()` to send, or `cloudkms_projects_locations_get` for simplest API.

pub fn cloudkms_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_get_builder()` + `cloudkms_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_get_builder(client, &args.name)?;
    cloudkms_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig
/// Returns the EkmConfig singleton resource for a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_get_ekm_config_execute()` to send, or `cloudkms_projects_locations_get_ekm_config` for simplest API.

pub fn cloudkms_projects_locations_get_ekm_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig
/// Returns the EkmConfig singleton resource for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_get_ekm_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_get_ekm_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_get_ekm_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EkmConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_get_ekm_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsGetEkmConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig
/// Returns the EkmConfig singleton resource for a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_get_ekm_config_builder()` + `cloudkms_projects_locations_get_ekm_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_get_ekm_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsGetEkmConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_get_ekm_config_builder(client, &args.name)?;
    cloudkms_projects_locations_get_ekm_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_list_execute()` to send, or `cloudkms_projects_locations_list` for simplest API.

pub fn cloudkms_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_list_builder()` + `cloudkms_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudkms_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig
/// Updates the EkmConfig singleton resource for a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_update_ekm_config_execute()` to send, or `cloudkms_projects_locations_update_ekm_config` for simplest API.

pub fn cloudkms_projects_locations_update_ekm_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EkmConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig
/// Updates the EkmConfig singleton resource for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_update_ekm_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_update_ekm_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_update_ekm_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EkmConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_update_ekm_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsUpdateEkmConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: EkmConfig,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig
/// Updates the EkmConfig singleton resource for a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_update_ekm_config_builder()` + `cloudkms_projects_locations_update_ekm_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_update_ekm_config(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsUpdateEkmConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_update_ekm_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_update_ekm_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_config_get_iam_policy_execute()` to send, or `cloudkms_projects_locations_ekm_config_get_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_ekm_config_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConfig:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_config_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_config_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_config_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_config_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConfigGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_config_get_iam_policy_builder()` + `cloudkms_projects_locations_ekm_config_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_config_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConfigGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_config_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    cloudkms_projects_locations_ekm_config_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_config_set_iam_policy_execute()` to send, or `cloudkms_projects_locations_ekm_config_set_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_ekm_config_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConfig:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_config_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_config_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_config_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_config_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConfigSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_config_set_iam_policy_builder()` + `cloudkms_projects_locations_ekm_config_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_config_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConfigSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_config_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_ekm_config_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_config_test_iam_permissions_execute()` to send, or `cloudkms_projects_locations_ekm_config_test_iam_permissions` for simplest API.

pub fn cloudkms_projects_locations_ekm_config_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConfig:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_config_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_config_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_config_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_config_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConfigTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConfig:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_config_test_iam_permissions_builder()` + `cloudkms_projects_locations_ekm_config_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_config_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConfigTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_config_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_ekm_config_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections
/// Creates a new EkmConnection in a given Project and Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_create_execute()` to send, or `cloudkms_projects_locations_ekm_connections_create` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    ekmConnectionId: Option<&str>,
    body: &EkmConnection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ekmConnectionId {
        query_parts.push(format!("ekmConnectionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections
/// Creates a new EkmConnection in a given Project and Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EkmConnection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: ekmConnectionId
    pub ekmConnectionId: Option<String>,
    /// Request body.
    pub body: EkmConnection,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections
/// Creates a new EkmConnection in a given Project and Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_create_builder()` + `cloudkms_projects_locations_ekm_connections_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_create_builder(
        client,
        &args.parent,
        args.ekmConnectionId.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_ekm_connections_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}
/// Returns metadata for a given EkmConnection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_get_execute()` to send, or `cloudkms_projects_locations_ekm_connections_get` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}
/// Returns metadata for a given EkmConnection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EkmConnection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}
/// Returns metadata for a given EkmConnection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_get_builder()` + `cloudkms_projects_locations_ekm_connections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_get_builder(client, &args.name)?;
    cloudkms_projects_locations_ekm_connections_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_get_iam_policy_execute()` to send, or `cloudkms_projects_locations_ekm_connections_get_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_get_iam_policy_builder()` + `cloudkms_projects_locations_ekm_connections_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    cloudkms_projects_locations_ekm_connections_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections
/// Lists EkmConnections.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_list_execute()` to send, or `cloudkms_projects_locations_ekm_connections_list` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections
/// Lists EkmConnections.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEkmConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEkmConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections
/// Lists EkmConnections.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_list_builder()` + `cloudkms_projects_locations_ekm_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEkmConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudkms_projects_locations_ekm_connections_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}
/// Updates an EkmConnection's metadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_patch_execute()` to send, or `cloudkms_projects_locations_ekm_connections_patch` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &EkmConnection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}
/// Updates an EkmConnection's metadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EkmConnection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: EkmConnection,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}
/// Updates an EkmConnection's metadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_patch_builder()` + `cloudkms_projects_locations_ekm_connections_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_patch(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_ekm_connections_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_set_iam_policy_execute()` to send, or `cloudkms_projects_locations_ekm_connections_set_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_set_iam_policy_builder()` + `cloudkms_projects_locations_ekm_connections_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_ekm_connections_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_test_iam_permissions_execute()` to send, or `cloudkms_projects_locations_ekm_connections_test_iam_permissions` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_test_iam_permissions_builder()` + `cloudkms_projects_locations_ekm_connections_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_ekm_connections_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:verifyConnectivity
/// Verifies that Cloud KMS can successfully connect to the external key manager specified by an EkmConnection. If there is an error connecting to the EKM, this method returns a FAILED_PRECONDITION status containing structured information as described at <https://cloud.google.`com/kms/docs/reference/ekm_errors`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_ekm_connections_verify_connectivity_execute()` to send, or `cloudkms_projects_locations_ekm_connections_verify_connectivity` for simplest API.

pub fn cloudkms_projects_locations_ekm_connections_verify_connectivity_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/ekmConnections/{}:verifyConnectivity",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:verifyConnectivity
/// Verifies that Cloud KMS can successfully connect to the external key manager specified by an EkmConnection. If there is an error connecting to the EKM, this method returns a FAILED_PRECONDITION status containing structured information as described at <https://cloud.google.`com/kms/docs/reference/ekm_errors`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_ekm_connections_verify_connectivity_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_ekm_connections_verify_connectivity_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_verify_connectivity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VerifyConnectivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyConnectivityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_ekm_connections_verify_connectivity`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsEkmConnectionsVerifyConnectivityArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ekmConnections/{ekmConnectionsId}:verifyConnectivity
/// Verifies that Cloud KMS can successfully connect to the external key manager specified by an EkmConnection. If there is an error connecting to the EKM, this method returns a FAILED_PRECONDITION status containing structured information as described at <https://cloud.google.`com/kms/docs/reference/ekm_errors`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_ekm_connections_verify_connectivity_builder()` + `cloudkms_projects_locations_ekm_connections_verify_connectivity_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_ekm_connections_verify_connectivity(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsEkmConnectionsVerifyConnectivityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VerifyConnectivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_ekm_connections_verify_connectivity_builder(
        client, &args.name,
    )?;
    cloudkms_projects_locations_ekm_connections_verify_connectivity_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles
/// Creates a new KeyHandle, triggering the provisioning of a new CryptoKey for CMEK use with the given resource type in the configured key project and the same location. GetOperation should be used to resolve the resulting long-running operation and get the resulting KeyHandle and CryptoKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_handles_create_execute()` to send, or `cloudkms_projects_locations_key_handles_create` for simplest API.

pub fn cloudkms_projects_locations_key_handles_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    keyHandleId: Option<&str>,
    body: &KeyHandle,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyHandles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyHandleId {
        query_parts.push(format!("keyHandleId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles
/// Creates a new KeyHandle, triggering the provisioning of a new CryptoKey for CMEK use with the given resource type in the configured key project and the same location. GetOperation should be used to resolve the resulting long-running operation and get the resulting KeyHandle and CryptoKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_handles_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_handles_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_handles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_handles_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyHandlesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: keyHandleId
    pub keyHandleId: Option<String>,
    /// Request body.
    pub body: KeyHandle,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles
/// Creates a new KeyHandle, triggering the provisioning of a new CryptoKey for CMEK use with the given resource type in the configured key project and the same location. GetOperation should be used to resolve the resulting long-running operation and get the resulting KeyHandle and CryptoKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_handles_create_builder()` + `cloudkms_projects_locations_key_handles_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_handles_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyHandlesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_handles_create_builder(
        client,
        &args.parent,
        args.keyHandleId.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_key_handles_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles/{keyHandlesId}
/// Returns the KeyHandle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_handles_get_execute()` to send, or `cloudkms_projects_locations_key_handles_get` for simplest API.

pub fn cloudkms_projects_locations_key_handles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyHandles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles/{keyHandlesId}
/// Returns the KeyHandle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_handles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_handles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_handles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<KeyHandle>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyHandle = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_handles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyHandlesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles/{keyHandlesId}
/// Returns the KeyHandle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_handles_get_builder()` + `cloudkms_projects_locations_key_handles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_handles_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyHandlesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<KeyHandle>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_handles_get_builder(client, &args.name)?;
    cloudkms_projects_locations_key_handles_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles
/// Lists KeyHandles.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_handles_list_execute()` to send, or `cloudkms_projects_locations_key_handles_list` for simplest API.

pub fn cloudkms_projects_locations_key_handles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyHandles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles
/// Lists KeyHandles.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_handles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_handles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_handles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListKeyHandlesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListKeyHandlesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_handles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyHandlesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyHandles
/// Lists KeyHandles.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_handles_list_builder()` + `cloudkms_projects_locations_key_handles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_handles_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyHandlesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListKeyHandlesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_handles_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudkms_projects_locations_key_handles_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings
/// Create a new KeyRing in a given Project and Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_create_execute()` to send, or `cloudkms_projects_locations_key_rings_create` for simplest API.

pub fn cloudkms_projects_locations_key_rings_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    keyRingId: Option<&str>,
    body: &KeyRing,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyRingId {
        query_parts.push(format!("keyRingId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings
/// Create a new KeyRing in a given Project and Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<KeyRing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyRing = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: keyRingId
    pub keyRingId: Option<String>,
    /// Request body.
    pub body: KeyRing,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings
/// Create a new KeyRing in a given Project and Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_create_builder()` + `cloudkms_projects_locations_key_rings_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<KeyRing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_create_builder(
        client,
        &args.parent,
        args.keyRingId.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}
/// Returns metadata for a given KeyRing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_get_execute()` to send, or `cloudkms_projects_locations_key_rings_get` for simplest API.

pub fn cloudkms_projects_locations_key_rings_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}
/// Returns metadata for a given KeyRing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<KeyRing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: KeyRing = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}
/// Returns metadata for a given KeyRing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_get_builder()` + `cloudkms_projects_locations_key_rings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<KeyRing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_get_builder(client, &args.name)?;
    cloudkms_projects_locations_key_rings_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_get_iam_policy_execute()` to send, or `cloudkms_projects_locations_key_rings_get_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_get_iam_policy_builder()` + `cloudkms_projects_locations_key_rings_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    cloudkms_projects_locations_key_rings_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings
/// Lists KeyRings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_list_execute()` to send, or `cloudkms_projects_locations_key_rings_list` for simplest API.

pub fn cloudkms_projects_locations_key_rings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings
/// Lists KeyRings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListKeyRingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListKeyRingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings
/// Lists KeyRings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_list_builder()` + `cloudkms_projects_locations_key_rings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListKeyRingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudkms_projects_locations_key_rings_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_set_iam_policy_execute()` to send, or `cloudkms_projects_locations_key_rings_set_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_set_iam_policy_builder()` + `cloudkms_projects_locations_key_rings_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_test_iam_permissions_execute()` to send, or `cloudkms_projects_locations_key_rings_test_iam_permissions` for simplest API.

pub fn cloudkms_projects_locations_key_rings_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_test_iam_permissions_builder()` + `cloudkms_projects_locations_key_rings_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys
/// Create a new CryptoKey within a KeyRing. CryptoKey.purpose and CryptoKey.version_template.algorithm are required.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_create_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_create` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cryptoKeyId: Option<&str>,
    skipInitialVersionCreation: Option<bool>,
    body: &CryptoKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cryptoKeyId {
        query_parts.push(format!("cryptoKeyId={}", val));
    }
    if let Some(val) = skipInitialVersionCreation {
        query_parts.push(format!("skipInitialVersionCreation={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys
/// Create a new CryptoKey within a KeyRing. CryptoKey.purpose and CryptoKey.version_template.algorithm are required.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: cryptoKeyId
    pub cryptoKeyId: Option<String>,
    /// Query parameter: skipInitialVersionCreation
    pub skipInitialVersionCreation: Option<bool>,
    /// Request body.
    pub body: CryptoKey,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys
/// Create a new CryptoKey within a KeyRing. CryptoKey.purpose and CryptoKey.version_template.algorithm are required.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_create_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_create_builder(
        client,
        &args.parent,
        args.cryptoKeyId.as_deref(),
        args.skipInitialVersionCreation,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:decrypt
/// Decrypts data that was protected by Encrypt. The CryptoKey.purpose must be ENCRYPT_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_decrypt_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_decrypt` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_decrypt_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DecryptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}:decrypt",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:decrypt
/// Decrypts data that was protected by Encrypt. The CryptoKey.purpose must be ENCRYPT_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_decrypt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_decrypt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_decrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DecryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DecryptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_decrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysDecryptArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: DecryptRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:decrypt
/// Decrypts data that was protected by Encrypt. The CryptoKey.purpose must be ENCRYPT_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_decrypt_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_decrypt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_decrypt(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysDecryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DecryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_decrypt_builder(
        client, &args.name, &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_decrypt_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Permanently deletes the given CryptoKey. All child CryptoKeyVersions must have been previously deleted using KeyManagementService.DeleteCryptoKeyVersion. The specified crypto key will be immediately and permanently deleted upon calling this method. This action cannot be undone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_delete_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_delete` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Permanently deletes the given CryptoKey. All child CryptoKeyVersions must have been previously deleted using KeyManagementService.DeleteCryptoKeyVersion. The specified crypto key will be immediately and permanently deleted upon calling this method. This action cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Permanently deletes the given CryptoKey. All child CryptoKeyVersions must have been previously deleted using KeyManagementService.DeleteCryptoKeyVersion. The specified crypto key will be immediately and permanently deleted upon calling this method. This action cannot be undone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_delete_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_delete(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_delete_builder(client, &args.name)?;
    cloudkms_projects_locations_key_rings_crypto_keys_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:encrypt
/// Encrypts data, so that it can only be recovered by a call to Decrypt. The CryptoKey.purpose must be ENCRYPT_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_encrypt_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_encrypt` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_encrypt_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EncryptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}:encrypt",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:encrypt
/// Encrypts data, so that it can only be recovered by a call to Decrypt. The CryptoKey.purpose must be ENCRYPT_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_encrypt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_encrypt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_encrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EncryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EncryptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_encrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysEncryptArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: EncryptRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:encrypt
/// Encrypts data, so that it can only be recovered by a call to Decrypt. The CryptoKey.purpose must be ENCRYPT_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_encrypt_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_encrypt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_encrypt(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysEncryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EncryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_encrypt_builder(
        client, &args.name, &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_encrypt_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Returns metadata for a given CryptoKey, as well as its primary CryptoKeyVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_get_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_get` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Returns metadata for a given CryptoKey, as well as its primary CryptoKeyVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Returns metadata for a given CryptoKey, as well as its primary CryptoKeyVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_get_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_get_builder(client, &args.name)?;
    cloudkms_projects_locations_key_rings_crypto_keys_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys
/// Lists CryptoKeys.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_list_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_list` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    versionView: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = versionView {
        query_parts.push(format!("versionView={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys
/// Lists CryptoKeys.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCryptoKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCryptoKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: versionView
    pub versionView: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys
/// Lists CryptoKeys.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_list_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCryptoKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.versionView.as_deref(),
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Update a CryptoKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_patch_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_patch` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CryptoKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Update a CryptoKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: CryptoKey,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}
/// Update a CryptoKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_patch_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_patch(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:updatePrimaryVersion
/// Update the version of a CryptoKey that will be used in Encrypt. Returns an error if called on a key whose purpose is not ENCRYPT_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateCryptoKeyPrimaryVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}:updatePrimaryVersion",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:updatePrimaryVersion
/// Update the version of a CryptoKey that will be used in Encrypt. Returns an error if called on a key whose purpose is not ENCRYPT_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysUpdatePrimaryVersionArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UpdateCryptoKeyPrimaryVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}:updatePrimaryVersion
/// Update the version of a CryptoKey that will be used in Encrypt. Returns an error if called on a key whose purpose is not ENCRYPT_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysUpdatePrimaryVersionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_builder(
        client, &args.name, &args.body,
    )?;
    cloudkms_projects_locations_key_rings_crypto_keys_update_primary_version_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:asymmetricDecrypt
/// Decrypts data that was encrypted with a public key retrieved from GetPublicKey corresponding to a CryptoKeyVersion with CryptoKey.purpose ASYMMETRIC_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AsymmetricDecryptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:asymmetricDecrypt",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:asymmetricDecrypt
/// Decrypts data that was encrypted with a public key retrieved from GetPublicKey corresponding to a CryptoKeyVersion with CryptoKey.purpose ASYMMETRIC_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AsymmetricDecryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AsymmetricDecryptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsAsymmetricDecryptArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AsymmetricDecryptRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:asymmetricDecrypt
/// Decrypts data that was encrypted with a public key retrieved from GetPublicKey corresponding to a CryptoKeyVersion with CryptoKey.purpose ASYMMETRIC_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsAsymmetricDecryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AsymmetricDecryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_builder(client, &args.name, &args.body)?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_decrypt_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:asymmetricSign
/// Signs data using a CryptoKeyVersion with CryptoKey.purpose ASYMMETRIC_SIGN, producing a signature that can be verified with the public key retrieved from GetPublicKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AsymmetricSignRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:asymmetricSign",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:asymmetricSign
/// Signs data using a CryptoKeyVersion with CryptoKey.purpose ASYMMETRIC_SIGN, producing a signature that can be verified with the public key retrieved from GetPublicKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AsymmetricSignResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AsymmetricSignResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsAsymmetricSignArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AsymmetricSignRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:asymmetricSign
/// Signs data using a CryptoKeyVersion with CryptoKey.purpose ASYMMETRIC_SIGN, producing a signature that can be verified with the public key retrieved from GetPublicKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsAsymmetricSignArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AsymmetricSignResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_builder(client, &args.name, &args.body)?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_asymmetric_sign_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions
/// Create a new CryptoKeyVersion in a CryptoKey. The server will assign the next sequential id. If unset, state will be set to ENABLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CryptoKeyVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions
/// Create a new CryptoKeyVersion in a CryptoKey. The server will assign the next sequential id. If unset, state will be set to ENABLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKeyVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: CryptoKeyVersion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions
/// Create a new CryptoKeyVersion in a CryptoKey. The server will assign the next sequential id. If unset, state will be set to ENABLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:decapsulate
/// Decapsulates data that was encapsulated with a public key retrieved from GetPublicKey corresponding to a CryptoKeyVersion with CryptoKey.purpose KEY_ENCAPSULATION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DecapsulateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:decapsulate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:decapsulate
/// Decapsulates data that was encapsulated with a public key retrieved from GetPublicKey corresponding to a CryptoKeyVersion with CryptoKey.purpose KEY_ENCAPSULATION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DecapsulateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DecapsulateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsDecapsulateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: DecapsulateRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:decapsulate
/// Decapsulates data that was encapsulated with a public key retrieved from GetPublicKey corresponding to a CryptoKeyVersion with CryptoKey.purpose KEY_ENCAPSULATION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsDecapsulateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DecapsulateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_decapsulate_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Permanently deletes the given CryptoKeyVersion. Only possible if the version has not been previously imported and if its state is one of DESTROYED, IMPORT_FAILED, or GENERATION_FAILED. Successfully imported CryptoKeyVersions cannot be deleted at this time. The specified version will be immediately and permanently deleted upon calling this method. This action cannot be undone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Permanently deletes the given CryptoKeyVersion. Only possible if the version has not been previously imported and if its state is one of DESTROYED, IMPORT_FAILED, or GENERATION_FAILED. Successfully imported CryptoKeyVersions cannot be deleted at this time. The specified version will be immediately and permanently deleted upon calling this method. This action cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Permanently deletes the given CryptoKeyVersion. Only possible if the version has not been previously imported and if its state is one of DESTROYED, IMPORT_FAILED, or GENERATION_FAILED. Successfully imported CryptoKeyVersions cannot be deleted at this time. The specified version will be immediately and permanently deleted upon calling this method. This action cannot be undone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_builder(
            client, &args.name,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:destroy
/// Schedule a CryptoKeyVersion for destruction. Upon calling this method, CryptoKeyVersion.state will be set to DESTROY_SCHEDULED, and destroy_time will be set to the time destroy_scheduled_duration in the future. At that time, the state will automatically change to DESTROYED, and the key material will be irrevocably destroyed. Before the destroy_time is reached, RestoreCryptoKeyVersion may be called to reverse the process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DestroyCryptoKeyVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:destroy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:destroy
/// Schedule a CryptoKeyVersion for destruction. Upon calling this method, CryptoKeyVersion.state will be set to DESTROY_SCHEDULED, and destroy_time will be set to the time destroy_scheduled_duration in the future. At that time, the state will automatically change to DESTROYED, and the key material will be irrevocably destroyed. Before the destroy_time is reached, RestoreCryptoKeyVersion may be called to reverse the process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKeyVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsDestroyArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: DestroyCryptoKeyVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:destroy
/// Schedule a CryptoKeyVersion for destruction. Upon calling this method, CryptoKeyVersion.state will be set to DESTROY_SCHEDULED, and destroy_time will be set to the time destroy_scheduled_duration in the future. At that time, the state will automatically change to DESTROYED, and the key material will be irrevocably destroyed. Before the destroy_time is reached, RestoreCryptoKeyVersion may be called to reverse the process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsDestroyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_destroy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Returns metadata for a given CryptoKeyVersion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Returns metadata for a given CryptoKeyVersion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKeyVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Returns metadata for a given CryptoKeyVersion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_builder(
            client, &args.name,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}/publicKey
/// Returns the public key for the given CryptoKeyVersion. The CryptoKey.purpose must be ASYMMETRIC_SIGN or ASYMMETRIC_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_builder(
    client: &SimpleHttpClient,
    name: &str,
    publicKeyFormat: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}/publicKey",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publicKeyFormat {
        query_parts.push(format!("publicKeyFormat={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}/publicKey
/// Returns the public key for the given CryptoKeyVersion. The CryptoKey.purpose must be ASYMMETRIC_SIGN or ASYMMETRIC_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsGetPublicKeyArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: publicKeyFormat
    pub publicKeyFormat: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}/publicKey
/// Returns the public key for the given CryptoKeyVersion. The CryptoKey.purpose must be ASYMMETRIC_SIGN or ASYMMETRIC_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsGetPublicKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_builder(client, &args.name, args.publicKeyFormat.as_deref())?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_get_public_key_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions:import
/// Import wrapped key material into a CryptoKeyVersion. All requests must specify a CryptoKey. If a CryptoKeyVersion is additionally specified in the request, key material will be reimported into that version. Otherwise, a new version will be created, and will be assigned the next sequential id within the CryptoKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ImportCryptoKeyVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions:import
/// Import wrapped key material into a CryptoKeyVersion. All requests must specify a CryptoKey. If a CryptoKeyVersion is additionally specified in the request, key material will be reimported into that version. Otherwise, a new version will be created, and will be assigned the next sequential id within the CryptoKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKeyVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsImportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ImportCryptoKeyVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions:import
/// Import wrapped key material into a CryptoKeyVersion. All requests must specify a CryptoKey. If a CryptoKeyVersion is additionally specified in the request, key material will be reimported into that version. Otherwise, a new version will be created, and will be assigned the next sequential id within the CryptoKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions
/// Lists CryptoKeyVersions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions
/// Lists CryptoKeyVersions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCryptoKeyVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCryptoKeyVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions
/// Lists CryptoKeyVersions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCryptoKeyVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.orderBy.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.view.as_deref(),
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:macSign
/// Signs data using a CryptoKeyVersion with CryptoKey.purpose MAC, producing a tag that can be verified by another source with the same key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MacSignRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:macSign",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:macSign
/// Signs data using a CryptoKeyVersion with CryptoKey.purpose MAC, producing a tag that can be verified by another source with the same key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MacSignResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MacSignResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsMacSignArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: MacSignRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:macSign
/// Signs data using a CryptoKeyVersion with CryptoKey.purpose MAC, producing a tag that can be verified by another source with the same key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsMacSignArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MacSignResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_sign_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:macVerify
/// Verifies MAC tag using a CryptoKeyVersion with CryptoKey.purpose MAC, and returns a response that indicates whether or not the verification was successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MacVerifyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:macVerify",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:macVerify
/// Verifies MAC tag using a CryptoKeyVersion with CryptoKey.purpose MAC, and returns a response that indicates whether or not the verification was successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MacVerifyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MacVerifyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsMacVerifyArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: MacVerifyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:macVerify
/// Verifies MAC tag using a CryptoKeyVersion with CryptoKey.purpose MAC, and returns a response that indicates whether or not the verification was successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsMacVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MacVerifyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_mac_verify_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Update a CryptoKeyVersion's metadata. state may be changed between ENABLED and DISABLED using this method. See DestroyCryptoKeyVersion and RestoreCryptoKeyVersion to move between other states.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CryptoKeyVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Update a CryptoKeyVersion's metadata. state may be changed between ENABLED and DISABLED using this method. See DestroyCryptoKeyVersion and RestoreCryptoKeyVersion to move between other states.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKeyVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: CryptoKeyVersion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}
/// Update a CryptoKeyVersion's metadata. state may be changed between ENABLED and DISABLED using this method. See DestroyCryptoKeyVersion and RestoreCryptoKeyVersion to move between other states.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_builder(
            client,
            &args.name,
            args.updateMask.as_deref(),
            &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:rawDecrypt
/// Decrypts data that was originally encrypted using a raw cryptographic mechanism. The CryptoKey.purpose must be RAW_ENCRYPT_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RawDecryptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:rawDecrypt",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:rawDecrypt
/// Decrypts data that was originally encrypted using a raw cryptographic mechanism. The CryptoKey.purpose must be RAW_ENCRYPT_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RawDecryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RawDecryptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsRawDecryptArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RawDecryptRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:rawDecrypt
/// Decrypts data that was originally encrypted using a raw cryptographic mechanism. The CryptoKey.purpose must be RAW_ENCRYPT_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsRawDecryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RawDecryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_decrypt_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:rawEncrypt
/// Encrypts data using portable cryptographic primitives. Most users should choose Encrypt and Decrypt rather than their raw counterparts. The CryptoKey.purpose must be RAW_ENCRYPT_DECRYPT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RawEncryptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:rawEncrypt",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:rawEncrypt
/// Encrypts data using portable cryptographic primitives. Most users should choose Encrypt and Decrypt rather than their raw counterparts. The CryptoKey.purpose must be RAW_ENCRYPT_DECRYPT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RawEncryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RawEncryptResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsRawEncryptArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RawEncryptRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:rawEncrypt
/// Encrypts data using portable cryptographic primitives. Most users should choose Encrypt and Decrypt rather than their raw counterparts. The CryptoKey.purpose must be RAW_ENCRYPT_DECRYPT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsRawEncryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RawEncryptResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_raw_encrypt_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:restore
/// Restore a CryptoKeyVersion in the DESTROY_SCHEDULED state. Upon restoration of the CryptoKeyVersion, state will be set to DISABLED, and destroy_time will be cleared.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_execute()` to send, or `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore` for simplest API.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RestoreCryptoKeyVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/cryptoKeys/{}/cryptoKeyVersions/{}:restore",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:restore
/// Restore a CryptoKeyVersion in the DESTROY_SCHEDULED state. Upon restoration of the CryptoKeyVersion, state will be set to DISABLED, and destroy_time will be cleared.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CryptoKeyVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsRestoreArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RestoreCryptoKeyVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/cryptoKeys/{cryptoKeysId}/cryptoKeyVersions/{cryptoKeyVersionsId}:restore
/// Restore a CryptoKeyVersion in the DESTROY_SCHEDULED state. Upon restoration of the CryptoKeyVersion, state will be set to DISABLED, and destroy_time will be cleared.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_builder()` + `cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsCryptoKeysCryptoKeyVersionsRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CryptoKeyVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_key_rings_crypto_keys_crypto_key_versions_restore_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs
/// Create a new ImportJob within a KeyRing. ImportJob.import_method is required.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_import_jobs_create_execute()` to send, or `cloudkms_projects_locations_key_rings_import_jobs_create` for simplest API.

pub fn cloudkms_projects_locations_key_rings_import_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    importJobId: Option<&str>,
    body: &ImportJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/importJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = importJobId {
        query_parts.push(format!("importJobId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs
/// Create a new ImportJob within a KeyRing. ImportJob.import_method is required.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_import_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImportJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_import_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsImportJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: importJobId
    pub importJobId: Option<String>,
    /// Request body.
    pub body: ImportJob,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs
/// Create a new ImportJob within a KeyRing. ImportJob.import_method is required.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_create_builder()` + `cloudkms_projects_locations_key_rings_import_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsImportJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_import_jobs_create_builder(
        client,
        &args.parent,
        args.importJobId.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_import_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}
/// Returns metadata for a given ImportJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_import_jobs_get_execute()` to send, or `cloudkms_projects_locations_key_rings_import_jobs_get` for simplest API.

pub fn cloudkms_projects_locations_key_rings_import_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/importJobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}
/// Returns metadata for a given ImportJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_import_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImportJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_import_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsImportJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}
/// Returns metadata for a given ImportJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_get_builder()` + `cloudkms_projects_locations_key_rings_import_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsImportJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImportJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_key_rings_import_jobs_get_builder(client, &args.name)?;
    cloudkms_projects_locations_key_rings_import_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_execute()` to send, or `cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/importJobs/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsImportJobsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_builder()` + `cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsImportJobsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    cloudkms_projects_locations_key_rings_import_jobs_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs
/// Lists ImportJobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_import_jobs_list_execute()` to send, or `cloudkms_projects_locations_key_rings_import_jobs_list` for simplest API.

pub fn cloudkms_projects_locations_key_rings_import_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/importJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs
/// Lists ImportJobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_import_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImportJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImportJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_import_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsImportJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs
/// Lists ImportJobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_list_builder()` + `cloudkms_projects_locations_key_rings_import_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsImportJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImportJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_import_jobs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudkms_projects_locations_key_rings_import_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_execute()` to send, or `cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy` for simplest API.

pub fn cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/importJobs/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsImportJobsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_builder()` + `cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsImportJobsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_import_jobs_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_execute()` to send, or `cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions` for simplest API.

pub fn cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/keyRings/{}/importJobs/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsKeyRingsImportJobsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/keyRings/{keyRingsId}/importJobs/{importJobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_builder()` + `cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsKeyRingsImportJobsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudkms_projects_locations_key_rings_import_jobs_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_operations_get_execute()` to send, or `cloudkms_projects_locations_operations_get` for simplest API.

pub fn cloudkms_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_operations_get_builder()` + `cloudkms_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_operations_get_builder(client, &args.name)?;
    cloudkms_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/retiredResources/{retiredResourcesId}
/// Retrieves a specific RetiredResource resource, which represents the record of a deleted CryptoKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_retired_resources_get_execute()` to send, or `cloudkms_projects_locations_retired_resources_get` for simplest API.

pub fn cloudkms_projects_locations_retired_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/retiredResources/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/retiredResources/{retiredResourcesId}
/// Retrieves a specific RetiredResource resource, which represents the record of a deleted CryptoKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_retired_resources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_retired_resources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_retired_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RetiredResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetiredResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_retired_resources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsRetiredResourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/retiredResources/{retiredResourcesId}
/// Retrieves a specific RetiredResource resource, which represents the record of a deleted CryptoKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_retired_resources_get_builder()` + `cloudkms_projects_locations_retired_resources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_retired_resources_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsRetiredResourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RetiredResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_retired_resources_get_builder(client, &args.name)?;
    cloudkms_projects_locations_retired_resources_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/retiredResources
/// Lists the RetiredResources which are the records of deleted CryptoKeys. RetiredResources prevent the reuse of these resource names after deletion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_retired_resources_list_execute()` to send, or `cloudkms_projects_locations_retired_resources_list` for simplest API.

pub fn cloudkms_projects_locations_retired_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/retiredResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/retiredResources
/// Lists the RetiredResources which are the records of deleted CryptoKeys. RetiredResources prevent the reuse of these resource names after deletion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_retired_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_retired_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_retired_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRetiredResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRetiredResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_retired_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsRetiredResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/retiredResources
/// Lists the RetiredResources which are the records of deleted CryptoKeys. RetiredResources prevent the reuse of these resource names after deletion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_retired_resources_list_builder()` + `cloudkms_projects_locations_retired_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_retired_resources_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsRetiredResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRetiredResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_retired_resources_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudkms_projects_locations_retired_resources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances
/// Creates a new SingleTenantHsmInstance in a given Project and Location. User must create a RegisterTwoFactorAuthKeys proposal with this single-tenant HSM instance to finish setup of the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_create_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_create` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    singleTenantHsmInstanceId: Option<&str>,
    body: &SingleTenantHsmInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = singleTenantHsmInstanceId {
        query_parts.push(format!("singleTenantHsmInstanceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances
/// Creates a new SingleTenantHsmInstance in a given Project and Location. User must create a RegisterTwoFactorAuthKeys proposal with this single-tenant HSM instance to finish setup of the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: singleTenantHsmInstanceId
    pub singleTenantHsmInstanceId: Option<String>,
    /// Request body.
    pub body: SingleTenantHsmInstance,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances
/// Creates a new SingleTenantHsmInstance in a given Project and Location. User must create a RegisterTwoFactorAuthKeys proposal with this single-tenant HSM instance to finish setup of the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_create_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_single_tenant_hsm_instances_create_builder(
        client,
        &args.parent,
        args.singleTenantHsmInstanceId.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}
/// Returns metadata for a given SingleTenantHsmInstance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_get_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_get` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}
/// Returns metadata for a given SingleTenantHsmInstance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SingleTenantHsmInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SingleTenantHsmInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}
/// Returns metadata for a given SingleTenantHsmInstance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_get_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SingleTenantHsmInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_single_tenant_hsm_instances_get_builder(client, &args.name)?;
    cloudkms_projects_locations_single_tenant_hsm_instances_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances
/// Lists SingleTenantHsmInstances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_list_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_list` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances
/// Lists SingleTenantHsmInstances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSingleTenantHsmInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSingleTenantHsmInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances
/// Lists SingleTenantHsmInstances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_list_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSingleTenantHsmInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_single_tenant_hsm_instances_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.showDeleted,
    )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:approve
/// Approves a SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance. The proposal must be in the `PENDING` state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveSingleTenantHsmInstanceProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}/proposals/{}:approve",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:approve
/// Approves a SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance. The proposal must be in the `PENDING` state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ApproveSingleTenantHsmInstanceProposalResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApproveSingleTenantHsmInstanceProposalResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsApproveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ApproveSingleTenantHsmInstanceProposalRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:approve
/// Approves a SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance. The proposal must be in the `PENDING` state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsApproveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ApproveSingleTenantHsmInstanceProposalResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
/// Creates a new SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    singleTenantHsmInstanceProposalId: Option<&str>,
    body: &SingleTenantHsmInstanceProposal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}/proposals",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = singleTenantHsmInstanceProposalId {
        query_parts.push(format!("singleTenantHsmInstanceProposalId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
/// Creates a new SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: singleTenantHsmInstanceProposalId
    pub singleTenantHsmInstanceProposalId: Option<String>,
    /// Request body.
    pub body: SingleTenantHsmInstanceProposal,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
/// Creates a new SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_builder(
        client,
        &args.parent,
        args.singleTenantHsmInstanceProposalId.as_deref(),
        &args.body,
    )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
/// Deletes a SingleTenantHsmInstanceProposal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}/proposals/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
/// Deletes a SingleTenantHsmInstanceProposal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
/// Deletes a SingleTenantHsmInstanceProposal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_builder(
        client, &args.name,
    )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:execute
/// Executes a SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance. The proposal must be in the APPROVED state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteSingleTenantHsmInstanceProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}/proposals/{}:execute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:execute
/// Executes a SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance. The proposal must be in the APPROVED state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsExecuteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExecuteSingleTenantHsmInstanceProposalRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:execute
/// Executes a SingleTenantHsmInstanceProposal for a given SingleTenantHsmInstance. The proposal must be in the APPROVED state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsExecuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_builder(
            client, &args.name, &args.body,
        )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
/// Returns metadata for a given SingleTenantHsmInstanceProposal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}/proposals/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
/// Returns metadata for a given SingleTenantHsmInstanceProposal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SingleTenantHsmInstanceProposal>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SingleTenantHsmInstanceProposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
/// Returns metadata for a given SingleTenantHsmInstanceProposal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SingleTenantHsmInstanceProposal>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_builder(
        client, &args.name,
    )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
/// Lists SingleTenantHsmInstanceProposals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_execute()` to send, or `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list` for simplest API.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{}/locations/{}/singleTenantHsmInstances/{}/proposals",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
/// Lists SingleTenantHsmInstanceProposals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSingleTenantHsmInstanceProposalsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSingleTenantHsmInstanceProposalsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
/// Lists SingleTenantHsmInstanceProposals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_builder()` + `cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list(
    client: &SimpleHttpClient,
    args: &CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSingleTenantHsmInstanceProposalsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.showDeleted,
    )?;
    cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_execute(builder)
}
