//! Auto-generated API clients for gcp/contentwarehouse.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_fetch_acl_execute()` to send, or `contentwarehouse_projects_fetch_acl` for simplest API.

pub fn contentwarehouse_projects_fetch_acl_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &GoogleCloudContentwarehouseV1FetchAclRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://contentwarehouse.googleapis.com/v1/projects/{}:fetchAcl",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contentwarehouse_projects_fetch_acl_execute()` or `contentwarehouse_projects_fetch_acl`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contentwarehouse_projects_fetch_acl_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_fetch_acl_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1FetchAclResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contentwarehouse_projects_fetch_acl_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contentwarehouse_projects_fetch_acl_task()`.
/// For the simplest API, use `contentwarehouse_projects_fetch_acl()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contentwarehouse_projects_fetch_acl_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contentwarehouse_projects_fetch_acl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contentwarehouse_projects_fetch_acl_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contentwarehouse_projects_fetch_acl`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentwarehouseProjectsFetchAclArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudContentwarehouseV1FetchAclRequest,
}

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_fetch_acl_builder()` + `contentwarehouse_projects_fetch_acl_execute()`.
/// For task-level control, use `contentwarehouse_projects_fetch_acl_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_fetch_acl(
    client: &SimpleHttpClient,
    args: &ContentwarehouseProjectsFetchAclArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_fetch_acl_builder(client, args.resource.clone(), &args.body)?;
    contentwarehouse_projects_fetch_acl_execute(builder)
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_set_acl_execute()` to send, or `contentwarehouse_projects_set_acl` for simplest API.

pub fn contentwarehouse_projects_set_acl_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &GoogleCloudContentwarehouseV1SetAclRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://contentwarehouse.googleapis.com/v1/projects/{}:setAcl",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contentwarehouse_projects_set_acl_execute()` or `contentwarehouse_projects_set_acl`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contentwarehouse_projects_set_acl_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_set_acl_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SetAclResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contentwarehouse_projects_set_acl_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contentwarehouse_projects_set_acl_task()`.
/// For the simplest API, use `contentwarehouse_projects_set_acl()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contentwarehouse_projects_set_acl_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contentwarehouse_projects_set_acl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contentwarehouse_projects_set_acl_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contentwarehouse_projects_set_acl`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentwarehouseProjectsSetAclArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleCloudContentwarehouseV1SetAclRequest,
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_set_acl_builder()` + `contentwarehouse_projects_set_acl_execute()`.
/// For task-level control, use `contentwarehouse_projects_set_acl_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_set_acl(
    client: &SimpleHttpClient,
    args: &ContentwarehouseProjectsSetAclArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_set_acl_builder(client, args.resource.clone(), &args.body)?;
    contentwarehouse_projects_set_acl_execute(builder)
}
