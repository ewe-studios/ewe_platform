//! Auto-generated API clients for gcp/contentwarehouse.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/contentwarehouse`

#![cfg(feature = "gcp/contentwarehouse")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_fetch_acl_execute()` to send, or `contentwarehouse_projects_fetch_acl` for simplest API.

pub fn contentwarehouse_projects_fetch_acl_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1FetchAclRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}:fetchAcl",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_fetch_acl_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_fetch_acl_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_fetch_acl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1FetchAclResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_fetch_acl_builder()` + `contentwarehouse_projects_fetch_acl_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_fetch_acl(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1FetchAclRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_fetch_acl_builder(client, resource, body)?;
    contentwarehouse_projects_fetch_acl_execute(builder)
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_set_acl_execute()` to send, or `contentwarehouse_projects_set_acl` for simplest API.

pub fn contentwarehouse_projects_set_acl_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1SetAclRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}:setAcl",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_set_acl_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_set_acl_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_set_acl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SetAclResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_set_acl_builder()` + `contentwarehouse_projects_set_acl_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_set_acl(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1SetAclRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_set_acl_builder(client, resource, body)?;
    contentwarehouse_projects_set_acl_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:getStatus
/// Get the project status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_get_status_execute()` to send, or `contentwarehouse_projects_locations_get_status` for simplest API.

pub fn contentwarehouse_projects_locations_get_status_builder(
    client: &SimpleHttpClient,
    location: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}:getStatus",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:getStatus
/// Get the project status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_get_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_get_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_get_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1ProjectStatus>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ProjectStatus =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:getStatus
/// Get the project status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_get_status_builder()` + `contentwarehouse_projects_locations_get_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_get_status(
    client: &SimpleHttpClient,
    location: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1ProjectStatus>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_get_status_builder(client, location)?;
    contentwarehouse_projects_locations_get_status_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:initialize
/// Provisions resources for given tenant project. Returns a long running operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_initialize_execute()` to send, or `contentwarehouse_projects_locations_initialize` for simplest API.

pub fn contentwarehouse_projects_locations_initialize_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContentwarehouseV1InitializeProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}:initialize",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:initialize
/// Provisions resources for given tenant project. Returns a long running operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_initialize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_initialize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_initialize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:initialize
/// Provisions resources for given tenant project. Returns a long running operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_initialize_builder()` + `contentwarehouse_projects_locations_initialize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_initialize(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContentwarehouseV1InitializeProjectRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_initialize_builder(client, location, body)?;
    contentwarehouse_projects_locations_initialize_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:runPipeline
/// Run a predefined pipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_run_pipeline_execute()` to send, or `contentwarehouse_projects_locations_run_pipeline` for simplest API.

pub fn contentwarehouse_projects_locations_run_pipeline_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1RunPipelineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}:runPipeline",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:runPipeline
/// Run a predefined pipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_run_pipeline_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_run_pipeline_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_run_pipeline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:runPipeline
/// Run a predefined pipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_run_pipeline_builder()` + `contentwarehouse_projects_locations_run_pipeline_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_run_pipeline(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1RunPipelineRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_run_pipeline_builder(client, name, body)?;
    contentwarehouse_projects_locations_run_pipeline_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas
/// Creates a document schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_document_schemas_create_execute()` to send, or `contentwarehouse_projects_locations_document_schemas_create` for simplest API.

pub fn contentwarehouse_projects_locations_document_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1DocumentSchema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documentSchemas",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas
/// Creates a document schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_document_schemas_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1DocumentSchema =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas
/// Creates a document schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_create_builder()` + `contentwarehouse_projects_locations_document_schemas_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1DocumentSchema,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_document_schemas_create_builder(client, parent, body)?;
    contentwarehouse_projects_locations_document_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Deletes a document schema. Returns NOT_FOUND if the document schema does not exist. Returns BAD_REQUEST if the document schema has documents depending on it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_document_schemas_delete_execute()` to send, or `contentwarehouse_projects_locations_document_schemas_delete` for simplest API.

pub fn contentwarehouse_projects_locations_document_schemas_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documentSchemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Deletes a document schema. Returns NOT_FOUND if the document schema does not exist. Returns BAD_REQUEST if the document schema has documents depending on it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_document_schemas_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Deletes a document schema. Returns NOT_FOUND if the document schema does not exist. Returns BAD_REQUEST if the document schema has documents depending on it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_delete_builder()` + `contentwarehouse_projects_locations_document_schemas_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_document_schemas_delete_builder(client, name)?;
    contentwarehouse_projects_locations_document_schemas_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Gets a document schema. Returns NOT_FOUND if the document schema does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_document_schemas_get_execute()` to send, or `contentwarehouse_projects_locations_document_schemas_get` for simplest API.

pub fn contentwarehouse_projects_locations_document_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documentSchemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Gets a document schema. Returns NOT_FOUND if the document schema does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_document_schemas_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1DocumentSchema =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Gets a document schema. Returns NOT_FOUND if the document schema does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_get_builder()` + `contentwarehouse_projects_locations_document_schemas_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_document_schemas_get_builder(client, name)?;
    contentwarehouse_projects_locations_document_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas
/// Lists document schemas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_document_schemas_list_execute()` to send, or `contentwarehouse_projects_locations_document_schemas_list` for simplest API.

pub fn contentwarehouse_projects_locations_document_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documentSchemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas
/// Lists document schemas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_document_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListDocumentSchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ListDocumentSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas
/// Lists document schemas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_list_builder()` + `contentwarehouse_projects_locations_document_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListDocumentSchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_document_schemas_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    contentwarehouse_projects_locations_document_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Updates a Document Schema. Returns INVALID_ARGUMENT if the name of the Document Schema is non-empty and does not equal the existing name. Supports only appending new properties, adding new ENUM possible values, and updating the EnumTypeOptions.validation_check_disabled flag for ENUM possible values. Updating existing properties will result into INVALID_ARGUMENT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_document_schemas_patch_execute()` to send, or `contentwarehouse_projects_locations_document_schemas_patch` for simplest API.

pub fn contentwarehouse_projects_locations_document_schemas_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateDocumentSchemaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documentSchemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Updates a Document Schema. Returns INVALID_ARGUMENT if the name of the Document Schema is non-empty and does not equal the existing name. Supports only appending new properties, adding new ENUM possible values, and updating the EnumTypeOptions.validation_check_disabled flag for ENUM possible values. Updating existing properties will result into INVALID_ARGUMENT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_document_schemas_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1DocumentSchema =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documentSchemas/{documentSchemasId}
/// Updates a Document Schema. Returns INVALID_ARGUMENT if the name of the Document Schema is non-empty and does not equal the existing name. Supports only appending new properties, adding new ENUM possible values, and updating the EnumTypeOptions.validation_check_disabled flag for ENUM possible values. Updating existing properties will result into INVALID_ARGUMENT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_document_schemas_patch_builder()` + `contentwarehouse_projects_locations_document_schemas_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_document_schemas_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateDocumentSchemaRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_document_schemas_patch_builder(client, name, body)?;
    contentwarehouse_projects_locations_document_schemas_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents
/// Creates a document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_create_execute()` to send, or `contentwarehouse_projects_locations_documents_create` for simplest API.

pub fn contentwarehouse_projects_locations_documents_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1CreateDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents
/// Creates a document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1CreateDocumentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1CreateDocumentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents
/// Creates a document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_create_builder()` + `contentwarehouse_projects_locations_documents_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1CreateDocumentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1CreateDocumentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_create_builder(client, parent, body)?;
    contentwarehouse_projects_locations_documents_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:delete
/// Deletes a document. Returns NOT_FOUND if the document does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_delete_execute()` to send, or `contentwarehouse_projects_locations_documents_delete` for simplest API.

pub fn contentwarehouse_projects_locations_documents_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1DeleteDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}:delete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:delete
/// Deletes a document. Returns NOT_FOUND if the document does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:delete
/// Deletes a document. Returns NOT_FOUND if the document does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_delete_builder()` + `contentwarehouse_projects_locations_documents_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_delete(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1DeleteDocumentRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_delete_builder(client, name, body)?;
    contentwarehouse_projects_locations_documents_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_fetch_acl_execute()` to send, or `contentwarehouse_projects_locations_documents_fetch_acl` for simplest API.

pub fn contentwarehouse_projects_locations_documents_fetch_acl_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1FetchAclRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}:fetchAcl",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_fetch_acl_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_fetch_acl_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_fetch_acl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1FetchAclResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:fetchAcl
/// Gets the access control policy for a resource. Returns NOT_FOUND error if the resource does not exist. Returns an empty policy if the resource exists but does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_fetch_acl_builder()` + `contentwarehouse_projects_locations_documents_fetch_acl_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_fetch_acl(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1FetchAclRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1FetchAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_fetch_acl_builder(client, resource, body)?;
    contentwarehouse_projects_locations_documents_fetch_acl_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:get
/// Gets a document. Returns NOT_FOUND if the document does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_get_execute()` to send, or `contentwarehouse_projects_locations_documents_get` for simplest API.

pub fn contentwarehouse_projects_locations_documents_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1GetDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}:get",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:get
/// Gets a document. Returns NOT_FOUND if the document does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:get
/// Gets a document. Returns NOT_FOUND if the document does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_get_builder()` + `contentwarehouse_projects_locations_documents_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_get(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1GetDocumentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_get_builder(client, name, body)?;
    contentwarehouse_projects_locations_documents_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedSources
/// Return all source document-links from the document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_linked_sources_execute()` to send, or `contentwarehouse_projects_locations_documents_linked_sources` for simplest API.

pub fn contentwarehouse_projects_locations_documents_linked_sources_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1ListLinkedSourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}/linkedSources",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedSources
/// Return all source document-links from the document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_linked_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_linked_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_linked_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListLinkedSourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ListLinkedSourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedSources
/// Return all source document-links from the document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_linked_sources_builder()` + `contentwarehouse_projects_locations_documents_linked_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_linked_sources(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1ListLinkedSourcesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListLinkedSourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_linked_sources_builder(client, parent, body)?;
    contentwarehouse_projects_locations_documents_linked_sources_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedTargets
/// Return all target document-links from the document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_linked_targets_execute()` to send, or `contentwarehouse_projects_locations_documents_linked_targets` for simplest API.

pub fn contentwarehouse_projects_locations_documents_linked_targets_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1ListLinkedTargetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}/linkedTargets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedTargets
/// Return all target document-links from the document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_linked_targets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_linked_targets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_linked_targets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListLinkedTargetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ListLinkedTargetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedTargets
/// Return all target document-links from the document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_linked_targets_builder()` + `contentwarehouse_projects_locations_documents_linked_targets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_linked_targets(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1ListLinkedTargetsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListLinkedTargetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_linked_targets_builder(client, parent, body)?;
    contentwarehouse_projects_locations_documents_linked_targets_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:lock
/// Lock the document so the document cannot be updated by other users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_lock_execute()` to send, or `contentwarehouse_projects_locations_documents_lock` for simplest API.

pub fn contentwarehouse_projects_locations_documents_lock_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1LockDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}:lock",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:lock
/// Lock the document so the document cannot be updated by other users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_lock_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_lock_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_lock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:lock
/// Lock the document so the document cannot be updated by other users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_lock_builder()` + `contentwarehouse_projects_locations_documents_lock_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_lock(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1LockDocumentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_lock_builder(client, name, body)?;
    contentwarehouse_projects_locations_documents_lock_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}
/// Updates a document. Returns INVALID_ARGUMENT if the name of the document is non-empty and does not equal the existing name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_patch_execute()` to send, or `contentwarehouse_projects_locations_documents_patch` for simplest API.

pub fn contentwarehouse_projects_locations_documents_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}
/// Updates a document. Returns INVALID_ARGUMENT if the name of the document is non-empty and does not equal the existing name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1UpdateDocumentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1UpdateDocumentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}
/// Updates a document. Returns INVALID_ARGUMENT if the name of the document is non-empty and does not equal the existing name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_patch_builder()` + `contentwarehouse_projects_locations_documents_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateDocumentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1UpdateDocumentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_patch_builder(client, name, body)?;
    contentwarehouse_projects_locations_documents_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:search
/// Searches for documents using provided SearchDocumentsRequest. This call only returns documents that the caller has permission to search against.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_search_execute()` to send, or `contentwarehouse_projects_locations_documents_search` for simplest API.

pub fn contentwarehouse_projects_locations_documents_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1SearchDocumentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents:search",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:search
/// Searches for documents using provided SearchDocumentsRequest. This call only returns documents that the caller has permission to search against.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SearchDocumentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SearchDocumentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:search
/// Searches for documents using provided SearchDocumentsRequest. This call only returns documents that the caller has permission to search against.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_search_builder()` + `contentwarehouse_projects_locations_documents_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_search(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1SearchDocumentsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SearchDocumentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_search_builder(client, parent, body)?;
    contentwarehouse_projects_locations_documents_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_set_acl_execute()` to send, or `contentwarehouse_projects_locations_documents_set_acl` for simplest API.

pub fn contentwarehouse_projects_locations_documents_set_acl_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1SetAclRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}:setAcl",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_set_acl_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_set_acl_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_set_acl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SetAclResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}:setAcl
/// Sets the access control policy for a resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_set_acl_builder()` + `contentwarehouse_projects_locations_documents_set_acl_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_set_acl(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleCloudContentwarehouseV1SetAclRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SetAclResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_set_acl_builder(client, resource, body)?;
    contentwarehouse_projects_locations_documents_set_acl_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/documentLinks
/// Create a link between a source document and a target document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_document_links_create_execute()` to send, or `contentwarehouse_projects_locations_documents_document_links_create` for simplest API.

pub fn contentwarehouse_projects_locations_documents_document_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1CreateDocumentLinkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}/documentLinks",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/documentLinks
/// Create a link between a source document and a target document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_document_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_document_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_document_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1DocumentLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/documentLinks
/// Create a link between a source document and a target document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_document_links_create_builder()` + `contentwarehouse_projects_locations_documents_document_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_document_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1CreateDocumentLinkRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1DocumentLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_document_links_create_builder(
        client, parent, body,
    )?;
    contentwarehouse_projects_locations_documents_document_links_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/documentLinks/{documentLinksId}:delete
/// Remove the link between the source and target documents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_document_links_delete_execute()` to send, or `contentwarehouse_projects_locations_documents_document_links_delete` for simplest API.

pub fn contentwarehouse_projects_locations_documents_document_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1DeleteDocumentLinkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/{}/documentLinks/{}:delete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/documentLinks/{documentLinksId}:delete
/// Remove the link between the source and target documents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_document_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_document_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_document_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/documentLinks/{documentLinksId}:delete
/// Remove the link between the source and target documents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_document_links_delete_builder()` + `contentwarehouse_projects_locations_documents_document_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_document_links_delete(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1DeleteDocumentLinkRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_document_links_delete_builder(
        client, name, body,
    )?;
    contentwarehouse_projects_locations_documents_document_links_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}:delete
/// Deletes a document. Returns NOT_FOUND if the document does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_reference_id_delete_execute()` to send, or `contentwarehouse_projects_locations_documents_reference_id_delete` for simplest API.

pub fn contentwarehouse_projects_locations_documents_reference_id_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1DeleteDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/referenceId/{}:delete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}:delete
/// Deletes a document. Returns NOT_FOUND if the document does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_reference_id_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_reference_id_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_reference_id_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}:delete
/// Deletes a document. Returns NOT_FOUND if the document does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_reference_id_delete_builder()` + `contentwarehouse_projects_locations_documents_reference_id_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_reference_id_delete(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1DeleteDocumentRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_reference_id_delete_builder(
        client, name, body,
    )?;
    contentwarehouse_projects_locations_documents_reference_id_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}:get
/// Gets a document. Returns NOT_FOUND if the document does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_reference_id_get_execute()` to send, or `contentwarehouse_projects_locations_documents_reference_id_get` for simplest API.

pub fn contentwarehouse_projects_locations_documents_reference_id_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1GetDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/referenceId/{}:get",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}:get
/// Gets a document. Returns NOT_FOUND if the document does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_reference_id_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_reference_id_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_reference_id_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}:get
/// Gets a document. Returns NOT_FOUND if the document does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_reference_id_get_builder()` + `contentwarehouse_projects_locations_documents_reference_id_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_reference_id_get(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1GetDocumentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_documents_reference_id_get_builder(client, name, body)?;
    contentwarehouse_projects_locations_documents_reference_id_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}
/// Updates a document. Returns INVALID_ARGUMENT if the name of the document is non-empty and does not equal the existing name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_documents_reference_id_patch_execute()` to send, or `contentwarehouse_projects_locations_documents_reference_id_patch` for simplest API.

pub fn contentwarehouse_projects_locations_documents_reference_id_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/documents/referenceId/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}
/// Updates a document. Returns INVALID_ARGUMENT if the name of the document is non-empty and does not equal the existing name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_documents_reference_id_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_documents_reference_id_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_reference_id_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1UpdateDocumentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1UpdateDocumentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents/referenceId/{referenceIdId}
/// Updates a document. Returns INVALID_ARGUMENT if the name of the document is non-empty and does not equal the existing name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_documents_reference_id_patch_builder()` + `contentwarehouse_projects_locations_documents_reference_id_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_documents_reference_id_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateDocumentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1UpdateDocumentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_documents_reference_id_patch_builder(
        client, name, body,
    )?;
    contentwarehouse_projects_locations_documents_reference_id_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_operations_get_execute()` to send, or `contentwarehouse_projects_locations_operations_get` for simplest API.

pub fn contentwarehouse_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_operations_get_builder()` + `contentwarehouse_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_operations_get_builder(client, name)?;
    contentwarehouse_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets
/// Creates a ruleset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_rule_sets_create_execute()` to send, or `contentwarehouse_projects_locations_rule_sets_create` for simplest API.

pub fn contentwarehouse_projects_locations_rule_sets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1RuleSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/ruleSets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets
/// Creates a ruleset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_rule_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1RuleSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1RuleSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets
/// Creates a ruleset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_create_builder()` + `contentwarehouse_projects_locations_rule_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1RuleSet,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1RuleSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_rule_sets_create_builder(client, parent, body)?;
    contentwarehouse_projects_locations_rule_sets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Deletes a ruleset. Returns NOT_FOUND if the document does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_rule_sets_delete_execute()` to send, or `contentwarehouse_projects_locations_rule_sets_delete` for simplest API.

pub fn contentwarehouse_projects_locations_rule_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/ruleSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Deletes a ruleset. Returns NOT_FOUND if the document does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_rule_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Deletes a ruleset. Returns NOT_FOUND if the document does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_delete_builder()` + `contentwarehouse_projects_locations_rule_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_rule_sets_delete_builder(client, name)?;
    contentwarehouse_projects_locations_rule_sets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Gets a ruleset. Returns NOT_FOUND if the ruleset does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_rule_sets_get_execute()` to send, or `contentwarehouse_projects_locations_rule_sets_get` for simplest API.

pub fn contentwarehouse_projects_locations_rule_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/ruleSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Gets a ruleset. Returns NOT_FOUND if the ruleset does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_rule_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1RuleSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1RuleSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Gets a ruleset. Returns NOT_FOUND if the ruleset does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_get_builder()` + `contentwarehouse_projects_locations_rule_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1RuleSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_rule_sets_get_builder(client, name)?;
    contentwarehouse_projects_locations_rule_sets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets
/// Lists rulesets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_rule_sets_list_execute()` to send, or `contentwarehouse_projects_locations_rule_sets_list` for simplest API.

pub fn contentwarehouse_projects_locations_rule_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/ruleSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets
/// Lists rulesets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_rule_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1ListRuleSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ListRuleSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets
/// Lists rulesets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_list_builder()` + `contentwarehouse_projects_locations_rule_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1ListRuleSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_rule_sets_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    contentwarehouse_projects_locations_rule_sets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Updates a ruleset. Returns INVALID_ARGUMENT if the name of the ruleset is non-empty and does not equal the existing name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_rule_sets_patch_execute()` to send, or `contentwarehouse_projects_locations_rule_sets_patch` for simplest API.

pub fn contentwarehouse_projects_locations_rule_sets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateRuleSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/ruleSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Updates a ruleset. Returns INVALID_ARGUMENT if the name of the ruleset is non-empty and does not equal the existing name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_rule_sets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1RuleSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1RuleSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/ruleSets/{ruleSetsId}
/// Updates a ruleset. Returns INVALID_ARGUMENT if the name of the ruleset is non-empty and does not equal the existing name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_rule_sets_patch_builder()` + `contentwarehouse_projects_locations_rule_sets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_rule_sets_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1UpdateRuleSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1RuleSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_rule_sets_patch_builder(client, name, body)?;
    contentwarehouse_projects_locations_rule_sets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets
/// Creates a SynonymSet for a single context. Throws an ALREADY_EXISTS exception if a synonymset already exists for the context.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_synonym_sets_create_execute()` to send, or `contentwarehouse_projects_locations_synonym_sets_create` for simplest API.

pub fn contentwarehouse_projects_locations_synonym_sets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1SynonymSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/synonymSets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets
/// Creates a SynonymSet for a single context. Throws an ALREADY_EXISTS exception if a synonymset already exists for the context.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_synonym_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SynonymSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SynonymSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets
/// Creates a SynonymSet for a single context. Throws an ALREADY_EXISTS exception if a synonymset already exists for the context.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_create_builder()` + `contentwarehouse_projects_locations_synonym_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContentwarehouseV1SynonymSet,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SynonymSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_synonym_sets_create_builder(client, parent, body)?;
    contentwarehouse_projects_locations_synonym_sets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Deletes a SynonymSet for a given context. Throws a NOT_FOUND exception if the SynonymSet is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_synonym_sets_delete_execute()` to send, or `contentwarehouse_projects_locations_synonym_sets_delete` for simplest API.

pub fn contentwarehouse_projects_locations_synonym_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/synonymSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Deletes a SynonymSet for a given context. Throws a NOT_FOUND exception if the SynonymSet is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_synonym_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Deletes a SynonymSet for a given context. Throws a NOT_FOUND exception if the SynonymSet is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_delete_builder()` + `contentwarehouse_projects_locations_synonym_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_synonym_sets_delete_builder(client, name)?;
    contentwarehouse_projects_locations_synonym_sets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Gets a SynonymSet for a particular context. Throws a NOT_FOUND exception if the Synonymset does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_synonym_sets_get_execute()` to send, or `contentwarehouse_projects_locations_synonym_sets_get` for simplest API.

pub fn contentwarehouse_projects_locations_synonym_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/synonymSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Gets a SynonymSet for a particular context. Throws a NOT_FOUND exception if the Synonymset does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_synonym_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SynonymSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SynonymSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Gets a SynonymSet for a particular context. Throws a NOT_FOUND exception if the Synonymset does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_get_builder()` + `contentwarehouse_projects_locations_synonym_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SynonymSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_synonym_sets_get_builder(client, name)?;
    contentwarehouse_projects_locations_synonym_sets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets
/// Returns all SynonymSets (for all contexts) for the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_synonym_sets_list_execute()` to send, or `contentwarehouse_projects_locations_synonym_sets_list` for simplest API.

pub fn contentwarehouse_projects_locations_synonym_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/synonymSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets
/// Returns all SynonymSets (for all contexts) for the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_synonym_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1ListSynonymSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ListSynonymSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets
/// Returns all SynonymSets (for all contexts) for the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_list_builder()` + `contentwarehouse_projects_locations_synonym_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1ListSynonymSetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contentwarehouse_projects_locations_synonym_sets_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    contentwarehouse_projects_locations_synonym_sets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Remove the existing SynonymSet for the context and replaces it with a new one. Throws a NOT_FOUND exception if the SynonymSet is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contentwarehouse_projects_locations_synonym_sets_patch_execute()` to send, or `contentwarehouse_projects_locations_synonym_sets_patch` for simplest API.

pub fn contentwarehouse_projects_locations_synonym_sets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1SynonymSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{}/locations/{}/synonymSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Remove the existing SynonymSet for the context and replaces it with a new one. Throws a NOT_FOUND exception if the SynonymSet is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from contentwarehouse_projects_locations_synonym_sets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SynonymSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1SynonymSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/synonymSets/{synonymSetsId}
/// Remove the existing SynonymSet for the context and replaces it with a new one. Throws a NOT_FOUND exception if the SynonymSet is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contentwarehouse_projects_locations_synonym_sets_patch_builder()` + `contentwarehouse_projects_locations_synonym_sets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contentwarehouse_projects_locations_synonym_sets_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContentwarehouseV1SynonymSet,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContentwarehouseV1SynonymSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contentwarehouse_projects_locations_synonym_sets_patch_builder(client, name, body)?;
    contentwarehouse_projects_locations_synonym_sets_patch_execute(builder)
}
