//! Auto-generated API clients for gcp/calendar.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_delete_execute()` to send, or `calendar_acl_delete` for simplest API.

pub fn calendar_acl_delete_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/acl/{}",
        calendarId, ruleId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_delete_builder()` + `calendar_acl_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_delete(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_delete_builder(client, calendarId, ruleId)?;
    calendar_acl_delete_execute(builder)
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Returns an access control rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_get_execute()` to send, or `calendar_acl_get` for simplest API.

pub fn calendar_acl_get_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/acl/{}",
        calendarId, ruleId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Returns an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Returns an access control rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_get_builder()` + `calendar_acl_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_get(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_get_builder(client, calendarId, ruleId)?;
    calendar_acl_get_execute(builder)
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_insert_execute()` to send, or `calendar_acl_insert` for simplest API.

pub fn calendar_acl_insert_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}/acl", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_insert_builder()` + `calendar_acl_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_insert(
    client: &SimpleHttpClient,
    calendarId: &str,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_insert_builder(client, calendarId, sendNotifications, body)?;
    calendar_acl_insert_execute(builder)
}

/// GET calendars/{calendarId}/acl
/// Returns the rules in the access control list for the calendar.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_list_execute()` to send, or `calendar_acl_list` for simplest API.

pub fn calendar_acl_list_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}/acl", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/acl
/// Returns the rules in the access control list for the calendar.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Acl>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Acl = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl
/// Returns the rules in the access control list for the calendar.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_list_builder()` + `calendar_acl_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_list(
    client: &SimpleHttpClient,
    calendarId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Acl>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_list_builder(
        client,
        calendarId,
        maxResults,
        pageToken,
        showDeleted,
        syncToken,
    )?;
    calendar_acl_list_execute(builder)
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Updates an access control rule. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_patch_execute()` to send, or `calendar_acl_patch` for simplest API.

pub fn calendar_acl_patch_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/acl/{}",
        calendarId, ruleId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Updates an access control rule. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Updates an access control rule. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_patch_builder()` + `calendar_acl_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_patch(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_patch_builder(client, calendarId, ruleId, sendNotifications, body)?;
    calendar_acl_patch_execute(builder)
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Updates an access control rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_update_execute()` to send, or `calendar_acl_update` for simplest API.

pub fn calendar_acl_update_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/acl/{}",
        calendarId, ruleId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Updates an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Updates an access control rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_update_builder()` + `calendar_acl_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_update(
    client: &SimpleHttpClient,
    calendarId: &str,
    ruleId: &str,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_update_builder(client, calendarId, ruleId, sendNotifications, body)?;
    calendar_acl_update_execute(builder)
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_watch_execute()` to send, or `calendar_acl_watch` for simplest API.

pub fn calendar_acl_watch_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    syncToken: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}/acl/watch", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_acl_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_acl_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_watch_builder()` + `calendar_acl_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_watch(
    client: &SimpleHttpClient,
    calendarId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    syncToken: Option<&str>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_watch_builder(
        client,
        calendarId,
        maxResults,
        pageToken,
        showDeleted,
        syncToken,
        body,
    )?;
    calendar_acl_watch_execute(builder)
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_delete_execute()` to send, or `calendar_calendar_list_delete` for simplest API.

pub fn calendar_calendar_list_delete_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comusers/me/calendarList/{}",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_delete_builder()` + `calendar_calendar_list_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_delete(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_delete_builder(client, calendarId)?;
    calendar_calendar_list_delete_execute(builder)
}

/// GET users/me/calendarList/{calendarId}
/// Returns a calendar from the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_get_execute()` to send, or `calendar_calendar_list_get` for simplest API.

pub fn calendar_calendar_list_get_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comusers/me/calendarList/{}",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/{calendarId}
/// Returns a calendar from the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarListEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/{calendarId}
/// Returns a calendar from the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_get_builder()` + `calendar_calendar_list_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_get(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_get_builder(client, calendarId)?;
    calendar_calendar_list_get_execute(builder)
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_insert_execute()` to send, or `calendar_calendar_list_insert` for simplest API.

pub fn calendar_calendar_list_insert_builder(
    client: &SimpleHttpClient,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comusers/me/calendarList",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "colorRgbFormat",
            colorRgbFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarListEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_insert_builder()` + `calendar_calendar_list_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_insert(
    client: &SimpleHttpClient,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_insert_builder(client, colorRgbFormat, body)?;
    calendar_calendar_list_insert_execute(builder)
}

/// GET users/me/calendarList
/// Returns the calendars on the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_list_execute()` to send, or `calendar_calendar_list_list` for simplest API.

pub fn calendar_calendar_list_list_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    minAccessRole: Option<&str>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comusers/me/calendarList",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minAccessRole",
            minAccessRole
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showHidden",
            showHidden
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET users/me/calendarList
/// Returns the calendars on the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList
/// Returns the calendars on the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_list_builder()` + `calendar_calendar_list_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_list(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    minAccessRole: Option<&str>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_list_builder(
        client,
        maxResults,
        minAccessRole,
        pageToken,
        showDeleted,
        showHidden,
        syncToken,
    )?;
    calendar_calendar_list_list_execute(builder)
}

/// GET users/me/calendarList/{calendarId}
/// Updates an existing calendar on the user's calendar list. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_patch_execute()` to send, or `calendar_calendar_list_patch` for simplest API.

pub fn calendar_calendar_list_patch_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comusers/me/calendarList/{}",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "colorRgbFormat",
            colorRgbFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET users/me/calendarList/{calendarId}
/// Updates an existing calendar on the user's calendar list. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarListEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/{calendarId}
/// Updates an existing calendar on the user's calendar list. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_patch_builder()` + `calendar_calendar_list_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_patch(
    client: &SimpleHttpClient,
    calendarId: &str,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_patch_builder(client, calendarId, colorRgbFormat, body)?;
    calendar_calendar_list_patch_execute(builder)
}

/// GET users/me/calendarList/{calendarId}
/// Updates an existing calendar on the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_update_execute()` to send, or `calendar_calendar_list_update` for simplest API.

pub fn calendar_calendar_list_update_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comusers/me/calendarList/{}",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "colorRgbFormat",
            colorRgbFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET users/me/calendarList/{calendarId}
/// Updates an existing calendar on the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarListEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/{calendarId}
/// Updates an existing calendar on the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_update_builder()` + `calendar_calendar_list_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_update(
    client: &SimpleHttpClient,
    calendarId: &str,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_update_builder(client, calendarId, colorRgbFormat, body)?;
    calendar_calendar_list_update_execute(builder)
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_watch_execute()` to send, or `calendar_calendar_list_watch` for simplest API.

pub fn calendar_calendar_list_watch_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    minAccessRole: Option<&str>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    syncToken: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comusers/me/calendarList/watch",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minAccessRole",
            minAccessRole
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showHidden",
            showHidden
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendar_list_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendar_list_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_watch_builder()` + `calendar_calendar_list_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_watch(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    minAccessRole: Option<&str>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    syncToken: Option<&str>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_watch_builder(
        client,
        maxResults,
        minAccessRole,
        pageToken,
        showDeleted,
        showHidden,
        syncToken,
        body,
    )?;
    calendar_calendar_list_watch_execute(builder)
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_clear_execute()` to send, or `calendar_calendars_clear` for simplest API.

pub fn calendar_calendars_clear_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}/clear", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendars_clear_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendars_clear_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_clear_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_clear_builder()` + `calendar_calendars_clear_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_clear(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_clear_builder(client, calendarId)?;
    calendar_calendars_clear_execute(builder)
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_delete_execute()` to send, or `calendar_calendars_delete` for simplest API.

pub fn calendar_calendars_delete_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendars_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendars_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_delete_builder()` + `calendar_calendars_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_delete(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_delete_builder(client, calendarId)?;
    calendar_calendars_delete_execute(builder)
}

/// GET calendars/{calendarId}
/// Returns metadata for a calendar.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_get_execute()` to send, or `calendar_calendars_get` for simplest API.

pub fn calendar_calendars_get_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}
/// Returns metadata for a calendar.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendars_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendars_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Calendar = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}
/// Returns metadata for a calendar.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_get_builder()` + `calendar_calendars_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_get(
    client: &SimpleHttpClient,
    calendarId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_get_builder(client, calendarId)?;
    calendar_calendars_get_execute(builder)
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_insert_execute()` to send, or `calendar_calendars_insert` for simplest API.

pub fn calendar_calendars_insert_builder(
    client: &SimpleHttpClient,
    body: &Calendar,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendars_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendars_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Calendar = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_insert_builder()` + `calendar_calendars_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_insert(
    client: &SimpleHttpClient,
    body: &Calendar,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_insert_builder(client, body)?;
    calendar_calendars_insert_execute(builder)
}

/// GET calendars/{calendarId}
/// Updates metadata for a calendar. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_patch_execute()` to send, or `calendar_calendars_patch` for simplest API.

pub fn calendar_calendars_patch_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    body: &Calendar,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}
/// Updates metadata for a calendar. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendars_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendars_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Calendar = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}
/// Updates metadata for a calendar. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_patch_builder()` + `calendar_calendars_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_patch(
    client: &SimpleHttpClient,
    calendarId: &str,
    body: &Calendar,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_patch_builder(client, calendarId, body)?;
    calendar_calendars_patch_execute(builder)
}

/// GET calendars/{calendarId}
/// Updates metadata for a calendar.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_update_execute()` to send, or `calendar_calendars_update` for simplest API.

pub fn calendar_calendars_update_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    body: &Calendar,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}
/// Updates metadata for a calendar.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_calendars_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_calendars_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Calendar = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}
/// Updates metadata for a calendar.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_update_builder()` + `calendar_calendars_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_update(
    client: &SimpleHttpClient,
    calendarId: &str,
    body: &Calendar,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_update_builder(client, calendarId, body)?;
    calendar_calendars_update_execute(builder)
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_channels_stop_execute()` to send, or `calendar_channels_stop` for simplest API.

pub fn calendar_channels_stop_builder(
    client: &SimpleHttpClient,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comchannels/stop",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_channels_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_channels_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_channels_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_channels_stop_builder()` + `calendar_channels_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_channels_stop(
    client: &SimpleHttpClient,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_channels_stop_builder(client, body)?;
    calendar_channels_stop_execute(builder)
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_colors_get_execute()` to send, or `calendar_colors_get` for simplest API.

pub fn calendar_colors_get_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcolors",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_colors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_colors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_colors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Colors>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Colors = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_colors_get_builder()` + `calendar_colors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_colors_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Colors>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_colors_get_builder(client)?;
    calendar_colors_get_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_delete_execute()` to send, or `calendar_events_delete` for simplest API.

pub fn calendar_events_delete_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/{}",
        calendarId, eventId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendUpdates",
            sendUpdates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_delete_builder()` + `calendar_events_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_delete(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_delete_builder(
        client,
        calendarId,
        eventId,
        sendNotifications,
        sendUpdates,
    )?;
    calendar_events_delete_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}
/// Returns an event based on its Google Calendar ID. To retrieve an event using its iCalendar ID, call the events.list method using the `iCalUID` parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_get_execute()` to send, or `calendar_events_get` for simplest API.

pub fn calendar_events_get_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    maxAttendees: Option<i32>,
    timeZone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/{}",
        calendarId, eventId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alwaysIncludeEmail",
            alwaysIncludeEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeZone",
            timeZone
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/events/{eventId}
/// Returns an event based on its Google Calendar ID. To retrieve an event using its iCalendar ID, call the events.list method using the `iCalUID` parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/{eventId}
/// Returns an event based on its Google Calendar ID. To retrieve an event using its iCalendar ID, call the events.list method using the `iCalUID` parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_get_builder()` + `calendar_events_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_get(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    maxAttendees: Option<i32>,
    timeZone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_get_builder(
        client,
        calendarId,
        eventId,
        alwaysIncludeEmail,
        maxAttendees,
        timeZone,
    )?;
    calendar_events_get_execute(builder)
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_import_execute()` to send, or `calendar_events_import` for simplest API.

pub fn calendar_events_import_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    conferenceDataVersion: Option<i32>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/import",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "conferenceDataVersion",
            conferenceDataVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAttachments",
            supportsAttachments
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_import_builder()` + `calendar_events_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_import(
    client: &SimpleHttpClient,
    calendarId: &str,
    conferenceDataVersion: Option<i32>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_import_builder(
        client,
        calendarId,
        conferenceDataVersion,
        supportsAttachments,
        body,
    )?;
    calendar_events_import_execute(builder)
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_insert_execute()` to send, or `calendar_events_insert` for simplest API.

pub fn calendar_events_insert_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}/events", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "conferenceDataVersion",
            conferenceDataVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendUpdates",
            sendUpdates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAttachments",
            supportsAttachments
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_insert_builder()` + `calendar_events_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_insert(
    client: &SimpleHttpClient,
    calendarId: &str,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_insert_builder(
        client,
        calendarId,
        conferenceDataVersion,
        maxAttendees,
        sendNotifications,
        sendUpdates,
        supportsAttachments,
        body,
    )?;
    calendar_events_insert_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_instances_execute()` to send, or `calendar_events_instances` for simplest API.

pub fn calendar_events_instances_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    originalStart: Option<&str>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    timeMax: Option<&str>,
    timeMin: Option<&str>,
    timeZone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/{}/instances",
        calendarId, eventId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alwaysIncludeEmail",
            alwaysIncludeEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "originalStart",
            originalStart
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeMax",
            timeMax
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeMin",
            timeMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeZone",
            timeZone
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Events>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Events = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_instances_builder()` + `calendar_events_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_instances(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    originalStart: Option<&str>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    timeMax: Option<&str>,
    timeMin: Option<&str>,
    timeZone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Events>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_instances_builder(
        client,
        calendarId,
        eventId,
        alwaysIncludeEmail,
        maxAttendees,
        maxResults,
        originalStart,
        pageToken,
        showDeleted,
        timeMax,
        timeMin,
        timeZone,
    )?;
    calendar_events_instances_execute(builder)
}

/// GET calendars/{calendarId}/events
/// Returns events on the specified calendar.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_list_execute()` to send, or `calendar_events_list` for simplest API.

pub fn calendar_events_list_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    alwaysIncludeEmail: Option<bool>,
    eventTypes: Option<&str>,
    iCalUID: Option<&str>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    privateExtendedProperty: Option<&str>,
    q: Option<&str>,
    sharedExtendedProperty: Option<&str>,
    showDeleted: Option<bool>,
    showHiddenInvitations: Option<bool>,
    singleEvents: Option<bool>,
    syncToken: Option<&str>,
    timeMax: Option<&str>,
    timeMin: Option<&str>,
    timeZone: Option<&str>,
    updatedMin: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcalendars/{}/events", calendarId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alwaysIncludeEmail",
            alwaysIncludeEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "eventTypes",
            eventTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "iCalUID",
            iCalUID
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "privateExtendedProperty",
            privateExtendedProperty
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sharedExtendedProperty",
            sharedExtendedProperty
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showHiddenInvitations",
            showHiddenInvitations
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "singleEvents",
            singleEvents
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeMax",
            timeMax
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeMin",
            timeMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeZone",
            timeZone
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMin",
            updatedMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/events
/// Returns events on the specified calendar.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Events>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Events = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events
/// Returns events on the specified calendar.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_list_builder()` + `calendar_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_list(
    client: &SimpleHttpClient,
    calendarId: &str,
    alwaysIncludeEmail: Option<bool>,
    eventTypes: Option<&str>,
    iCalUID: Option<&str>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    privateExtendedProperty: Option<&str>,
    q: Option<&str>,
    sharedExtendedProperty: Option<&str>,
    showDeleted: Option<bool>,
    showHiddenInvitations: Option<bool>,
    singleEvents: Option<bool>,
    syncToken: Option<&str>,
    timeMax: Option<&str>,
    timeMin: Option<&str>,
    timeZone: Option<&str>,
    updatedMin: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Events>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_list_builder(
        client,
        calendarId,
        alwaysIncludeEmail,
        eventTypes,
        iCalUID,
        maxAttendees,
        maxResults,
        orderBy,
        pageToken,
        privateExtendedProperty,
        q,
        sharedExtendedProperty,
        showDeleted,
        showHiddenInvitations,
        singleEvents,
        syncToken,
        timeMax,
        timeMin,
        timeZone,
        updatedMin,
    )?;
    calendar_events_list_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}/move
/// Moves an event to another calendar, i.e. changes an event's organizer. Note that only default events can be moved; birthday, `focusTime`, `fromGmail`, `outOfOffice` and `workingLocation` events cannot be moved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_move_execute()` to send, or `calendar_events_move` for simplest API.

pub fn calendar_events_move_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    destination: Option<&str>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/{}/move",
        calendarId, eventId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "destination",
            destination
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendUpdates",
            sendUpdates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/events/{eventId}/move
/// Moves an event to another calendar, i.e. changes an event's organizer. Note that only default events can be moved; birthday, `focusTime`, `fromGmail`, `outOfOffice` and `workingLocation` events cannot be moved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/{eventId}/move
/// Moves an event to another calendar, i.e. changes an event's organizer. Note that only default events can be moved; birthday, `focusTime`, `fromGmail`, `outOfOffice` and `workingLocation` events cannot be moved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_move_builder()` + `calendar_events_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_move(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    destination: Option<&str>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_move_builder(
        client,
        calendarId,
        eventId,
        destination,
        sendNotifications,
        sendUpdates,
    )?;
    calendar_events_move_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}
/// Updates an event. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_patch_execute()` to send, or `calendar_events_patch` for simplest API.

pub fn calendar_events_patch_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/{}",
        calendarId, eventId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alwaysIncludeEmail",
            alwaysIncludeEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "conferenceDataVersion",
            conferenceDataVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendUpdates",
            sendUpdates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAttachments",
            supportsAttachments
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/events/{eventId}
/// Updates an event. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/{eventId}
/// Updates an event. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_patch_builder()` + `calendar_events_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_patch(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_patch_builder(
        client,
        calendarId,
        eventId,
        alwaysIncludeEmail,
        conferenceDataVersion,
        maxAttendees,
        sendNotifications,
        sendUpdates,
        supportsAttachments,
        body,
    )?;
    calendar_events_patch_execute(builder)
}

/// GET calendars/{calendarId}/events/quickAdd
/// Creates an event based on a simple text string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_quick_add_execute()` to send, or `calendar_events_quick_add` for simplest API.

pub fn calendar_events_quick_add_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    text: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/quickAdd",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendUpdates",
            sendUpdates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "text",
            text.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET calendars/{calendarId}/events/quickAdd
/// Creates an event based on a simple text string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_quick_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_quick_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_quick_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/quickAdd
/// Creates an event based on a simple text string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_quick_add_builder()` + `calendar_events_quick_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_quick_add(
    client: &SimpleHttpClient,
    calendarId: &str,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    text: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_quick_add_builder(
        client,
        calendarId,
        sendNotifications,
        sendUpdates,
        text,
    )?;
    calendar_events_quick_add_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}
/// Updates an event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_update_execute()` to send, or `calendar_events_update` for simplest API.

pub fn calendar_events_update_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/{}",
        calendarId, eventId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alwaysIncludeEmail",
            alwaysIncludeEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "conferenceDataVersion",
            conferenceDataVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendNotifications",
            sendNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendUpdates",
            sendUpdates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAttachments",
            supportsAttachments
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/events/{eventId}
/// Updates an event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/{eventId}
/// Updates an event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_update_builder()` + `calendar_events_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_update(
    client: &SimpleHttpClient,
    calendarId: &str,
    eventId: &str,
    alwaysIncludeEmail: Option<bool>,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<&str>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_update_builder(
        client,
        calendarId,
        eventId,
        alwaysIncludeEmail,
        conferenceDataVersion,
        maxAttendees,
        sendNotifications,
        sendUpdates,
        supportsAttachments,
        body,
    )?;
    calendar_events_update_execute(builder)
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_watch_execute()` to send, or `calendar_events_watch` for simplest API.

pub fn calendar_events_watch_builder(
    client: &SimpleHttpClient,
    calendarId: &str,
    alwaysIncludeEmail: Option<bool>,
    eventTypes: Option<&str>,
    iCalUID: Option<&str>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    privateExtendedProperty: Option<&str>,
    q: Option<&str>,
    sharedExtendedProperty: Option<&str>,
    showDeleted: Option<bool>,
    showHiddenInvitations: Option<bool>,
    singleEvents: Option<bool>,
    syncToken: Option<&str>,
    timeMax: Option<&str>,
    timeMin: Option<&str>,
    timeZone: Option<&str>,
    updatedMin: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comcalendars/{}/events/watch",
        calendarId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alwaysIncludeEmail",
            alwaysIncludeEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "eventTypes",
            eventTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "iCalUID",
            iCalUID
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAttendees",
            maxAttendees
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "privateExtendedProperty",
            privateExtendedProperty
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sharedExtendedProperty",
            sharedExtendedProperty
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showHiddenInvitations",
            showHiddenInvitations
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "singleEvents",
            singleEvents
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeMax",
            timeMax
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeMin",
            timeMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeZone",
            timeZone
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMin",
            updatedMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_events_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_events_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_watch_builder()` + `calendar_events_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_watch(
    client: &SimpleHttpClient,
    calendarId: &str,
    alwaysIncludeEmail: Option<bool>,
    eventTypes: Option<&str>,
    iCalUID: Option<&str>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    privateExtendedProperty: Option<&str>,
    q: Option<&str>,
    sharedExtendedProperty: Option<&str>,
    showDeleted: Option<bool>,
    showHiddenInvitations: Option<bool>,
    singleEvents: Option<bool>,
    syncToken: Option<&str>,
    timeMax: Option<&str>,
    timeMin: Option<&str>,
    timeZone: Option<&str>,
    updatedMin: Option<&str>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_watch_builder(
        client,
        calendarId,
        alwaysIncludeEmail,
        eventTypes,
        iCalUID,
        maxAttendees,
        maxResults,
        orderBy,
        pageToken,
        privateExtendedProperty,
        q,
        sharedExtendedProperty,
        showDeleted,
        showHiddenInvitations,
        singleEvents,
        syncToken,
        timeMax,
        timeMin,
        timeZone,
        updatedMin,
        body,
    )?;
    calendar_events_watch_execute(builder)
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_freebusy_query_execute()` to send, or `calendar_freebusy_query` for simplest API.

pub fn calendar_freebusy_query_builder(
    client: &SimpleHttpClient,
    body: &FreeBusyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfreeBusy",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_freebusy_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_freebusy_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_freebusy_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FreeBusyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FreeBusyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_freebusy_query_builder()` + `calendar_freebusy_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_freebusy_query(
    client: &SimpleHttpClient,
    body: &FreeBusyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FreeBusyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_freebusy_query_builder(client, body)?;
    calendar_freebusy_query_execute(builder)
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_settings_get_execute()` to send, or `calendar_settings_get` for simplest API.

pub fn calendar_settings_get_builder(
    client: &SimpleHttpClient,
    setting: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comusers/me/settings/{}", setting,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_settings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_settings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setting>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setting = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_settings_get_builder()` + `calendar_settings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_get(
    client: &SimpleHttpClient,
    setting: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setting>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_settings_get_builder(client, setting)?;
    calendar_settings_get_execute(builder)
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_settings_list_execute()` to send, or `calendar_settings_list` for simplest API.

pub fn calendar_settings_list_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comusers/me/settings",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_settings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_settings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_settings_list_builder()` + `calendar_settings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_list(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_settings_list_builder(client, maxResults, pageToken, syncToken)?;
    calendar_settings_list_execute(builder)
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_settings_watch_execute()` to send, or `calendar_settings_watch` for simplest API.

pub fn calendar_settings_watch_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    syncToken: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comusers/me/settings/watch",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `calendar_settings_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from calendar_settings_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_settings_watch_builder()` + `calendar_settings_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_watch(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    syncToken: Option<&str>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_settings_watch_builder(client, maxResults, pageToken, syncToken, body)?;
    calendar_settings_watch_execute(builder)
}
