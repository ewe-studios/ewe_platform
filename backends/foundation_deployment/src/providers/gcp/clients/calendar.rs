//! Auto-generated API clients for gcp/calendar.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_delete_execute()` to send, or `calendar_acl_delete` for simplest API.

pub fn calendar_acl_delete_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    ruleId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/acl/{}",
        calendarId.as_str(),
        ruleId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_acl_delete_execute()` or `calendar_acl_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_acl_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_acl_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_acl_delete_task()`.
/// For the simplest API, use `calendar_acl_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_acl_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_acl_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_acl_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_acl_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarAclDeleteArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Path parameter: ruleId
    pub ruleId: String,
}

/// GET calendars/{calendarId}/acl/{ruleId}
/// Deletes an access control rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_delete_builder()` + `calendar_acl_delete_execute()`.
/// For task-level control, use `calendar_acl_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_delete(
    client: &SimpleHttpClient,
    args: &CalendarAclDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        calendar_acl_delete_builder(client, args.calendarId.clone(), args.ruleId.clone())?;
    calendar_acl_delete_execute(builder)
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_insert_execute()` to send, or `calendar_acl_insert` for simplest API.

pub fn calendar_acl_insert_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    sendNotifications: Option<bool>,
    body: &AclRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/acl",
        calendarId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sendNotifications {
        query_parts.push(format!("sendNotifications={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_acl_insert_execute()` or `calendar_acl_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_acl_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AclRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_acl_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_acl_insert_task()`.
/// For the simplest API, use `calendar_acl_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_acl_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_acl_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_acl_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_acl_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarAclInsertArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Query parameter: sendNotifications
    pub sendNotifications: Option<bool>,
    /// Request body.
    pub body: AclRule,
}

/// GET calendars/{calendarId}/acl
/// Creates an access control rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_insert_builder()` + `calendar_acl_insert_execute()`.
/// For task-level control, use `calendar_acl_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_insert(
    client: &SimpleHttpClient,
    args: &CalendarAclInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclRule>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_insert_builder(
        client,
        args.calendarId.clone(),
        args.sendNotifications.clone(),
        &args.body,
    )?;
    calendar_acl_insert_execute(builder)
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_acl_watch_execute()` to send, or `calendar_acl_watch` for simplest API.

pub fn calendar_acl_watch_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    maxResults: Option<i32>,
    pageToken: Option<String>,
    showDeleted: Option<bool>,
    syncToken: Option<String>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/acl/watch",
        calendarId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = syncToken {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_acl_watch_execute()` or `calendar_acl_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_acl_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_acl_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_acl_watch_task()`.
/// For the simplest API, use `calendar_acl_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_acl_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_acl_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_acl_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_acl_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarAclWatchArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
    /// Query parameter: syncToken
    pub syncToken: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// GET calendars/{calendarId}/acl/watch
/// Watch for changes to ACL resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_acl_watch_builder()` + `calendar_acl_watch_execute()`.
/// For task-level control, use `calendar_acl_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_acl_watch(
    client: &SimpleHttpClient,
    args: &CalendarAclWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_acl_watch_builder(
        client,
        args.calendarId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
        args.showDeleted.clone(),
        args.syncToken.clone(),
        &args.body,
    )?;
    calendar_acl_watch_execute(builder)
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_delete_execute()` to send, or `calendar_calendar_list_delete` for simplest API.

pub fn calendar_calendar_list_delete_builder(
    client: &SimpleHttpClient,
    calendarId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/users/me/calendarList/{}",
        calendarId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_calendar_list_delete_execute()` or `calendar_calendar_list_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendar_list_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_calendar_list_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_calendar_list_delete_task()`.
/// For the simplest API, use `calendar_calendar_list_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendar_list_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_calendar_list_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_calendar_list_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_calendar_list_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarCalendarListDeleteArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
}

/// GET users/me/calendarList/{calendarId}
/// Removes a calendar from the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_delete_builder()` + `calendar_calendar_list_delete_execute()`.
/// For task-level control, use `calendar_calendar_list_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_delete(
    client: &SimpleHttpClient,
    args: &CalendarCalendarListDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_delete_builder(client, args.calendarId.clone())?;
    calendar_calendar_list_delete_execute(builder)
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_insert_execute()` to send, or `calendar_calendar_list_insert` for simplest API.

pub fn calendar_calendar_list_insert_builder(
    client: &SimpleHttpClient,
    colorRgbFormat: Option<bool>,
    body: &CalendarListEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/users/me/calendarList",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = colorRgbFormat {
        query_parts.push(format!("colorRgbFormat={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_calendar_list_insert_execute()` or `calendar_calendar_list_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendar_list_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CalendarListEntry>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarListEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_calendar_list_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_calendar_list_insert_task()`.
/// For the simplest API, use `calendar_calendar_list_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendar_list_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_calendar_list_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = calendar_calendar_list_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_calendar_list_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarCalendarListInsertArgs {
    /// Query parameter: colorRgbFormat
    pub colorRgbFormat: Option<bool>,
    /// Request body.
    pub body: CalendarListEntry,
}

/// GET users/me/calendarList
/// Inserts an existing calendar into the user's calendar list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_insert_builder()` + `calendar_calendar_list_insert_execute()`.
/// For task-level control, use `calendar_calendar_list_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_insert(
    client: &SimpleHttpClient,
    args: &CalendarCalendarListInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CalendarListEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        calendar_calendar_list_insert_builder(client, args.colorRgbFormat.clone(), &args.body)?;
    calendar_calendar_list_insert_execute(builder)
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendar_list_watch_execute()` to send, or `calendar_calendar_list_watch` for simplest API.

pub fn calendar_calendar_list_watch_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    minAccessRole: Option<String>,
    pageToken: Option<String>,
    showDeleted: Option<bool>,
    showHidden: Option<bool>,
    syncToken: Option<String>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/calendar/v3/users/me/calendarList/watch",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = minAccessRole {
        query_parts.push(format!("minAccessRole={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = showHidden {
        query_parts.push(format!("showHidden={}", val));
    }
    if let Some(val) = syncToken {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_calendar_list_watch_execute()` or `calendar_calendar_list_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendar_list_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_calendar_list_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_calendar_list_watch_task()`.
/// For the simplest API, use `calendar_calendar_list_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendar_list_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_calendar_list_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_calendar_list_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_calendar_list_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarCalendarListWatchArgs {
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: minAccessRole
    pub minAccessRole: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
    /// Query parameter: showHidden
    pub showHidden: Option<bool>,
    /// Query parameter: syncToken
    pub syncToken: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// GET users/me/calendarList/watch
/// Watch for changes to CalendarList resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendar_list_watch_builder()` + `calendar_calendar_list_watch_execute()`.
/// For task-level control, use `calendar_calendar_list_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendar_list_watch(
    client: &SimpleHttpClient,
    args: &CalendarCalendarListWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendar_list_watch_builder(
        client,
        args.maxResults.clone(),
        args.minAccessRole.clone(),
        args.pageToken.clone(),
        args.showDeleted.clone(),
        args.showHidden.clone(),
        args.syncToken.clone(),
        &args.body,
    )?;
    calendar_calendar_list_watch_execute(builder)
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_clear_execute()` to send, or `calendar_calendars_clear` for simplest API.

pub fn calendar_calendars_clear_builder(
    client: &SimpleHttpClient,
    calendarId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/clear",
        calendarId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_calendars_clear_execute()` or `calendar_calendars_clear`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendars_clear_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_clear_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_calendars_clear_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_calendars_clear_task()`.
/// For the simplest API, use `calendar_calendars_clear()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendars_clear_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_calendars_clear_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_calendars_clear_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_calendars_clear`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarCalendarsClearArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
}

/// GET calendars/{calendarId}/clear
/// Clears a primary calendar. This operation deletes all events associated with the primary calendar of an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_clear_builder()` + `calendar_calendars_clear_execute()`.
/// For task-level control, use `calendar_calendars_clear_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_clear(
    client: &SimpleHttpClient,
    args: &CalendarCalendarsClearArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_clear_builder(client, args.calendarId.clone())?;
    calendar_calendars_clear_execute(builder)
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_delete_execute()` to send, or `calendar_calendars_delete` for simplest API.

pub fn calendar_calendars_delete_builder(
    client: &SimpleHttpClient,
    calendarId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}",
        calendarId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_calendars_delete_execute()` or `calendar_calendars_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendars_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_calendars_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_calendars_delete_task()`.
/// For the simplest API, use `calendar_calendars_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendars_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_calendars_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_calendars_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_calendars_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarCalendarsDeleteArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
}

/// GET calendars/{calendarId}
/// Deletes a secondary calendar. Use calendars.clear for clearing all events on primary calendars.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_delete_builder()` + `calendar_calendars_delete_execute()`.
/// For task-level control, use `calendar_calendars_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_delete(
    client: &SimpleHttpClient,
    args: &CalendarCalendarsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_delete_builder(client, args.calendarId.clone())?;
    calendar_calendars_delete_execute(builder)
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_calendars_insert_execute()` to send, or `calendar_calendars_insert` for simplest API.

pub fn calendar_calendars_insert_builder(
    client: &SimpleHttpClient,
    body: &Calendar,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/calendars",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_calendars_insert_execute()` or `calendar_calendars_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendars_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Calendar>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Calendar = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_calendars_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_calendars_insert_task()`.
/// For the simplest API, use `calendar_calendars_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_calendars_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_calendars_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_calendars_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_calendars_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarCalendarsInsertArgs {
    /// Request body.
    pub body: Calendar,
}

/// GET calendars
/// Creates a secondary calendar. The authenticated user for the request is made the data owner of the new calendar.  Note: We recommend to authenticate as the intended data owner of the calendar. You can use domain-wide delegation of authority to allow applications to act on behalf of a specific user. Don't use a service account for authentication. If you use a service account for authentication, the service account is the data owner, which can lead to unexpected behavior. For example, if a service account is the data owner, data ownership cannot be transferred.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_calendars_insert_builder()` + `calendar_calendars_insert_execute()`.
/// For task-level control, use `calendar_calendars_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_calendars_insert(
    client: &SimpleHttpClient,
    args: &CalendarCalendarsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Calendar>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_calendars_insert_builder(client, &args.body)?;
    calendar_calendars_insert_execute(builder)
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_channels_stop_execute()` to send, or `calendar_channels_stop` for simplest API.

pub fn calendar_channels_stop_builder(
    client: &SimpleHttpClient,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/channels/stop",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_channels_stop_execute()` or `calendar_channels_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_channels_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_channels_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_channels_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_channels_stop_task()`.
/// For the simplest API, use `calendar_channels_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_channels_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_channels_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_channels_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_channels_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarChannelsStopArgs {
    /// Request body.
    pub body: Channel,
}

/// GET channels/stop
/// Stop watching resources through this channel
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_channels_stop_builder()` + `calendar_channels_stop_execute()`.
/// For task-level control, use `calendar_channels_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_channels_stop(
    client: &SimpleHttpClient,
    args: &CalendarChannelsStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_channels_stop_builder(client, &args.body)?;
    calendar_channels_stop_execute(builder)
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_colors_get_execute()` to send, or `calendar_colors_get` for simplest API.

pub fn calendar_colors_get_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/colors",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_colors_get_execute()` or `calendar_colors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_colors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_colors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Colors>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Colors = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_colors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_colors_get_task()`.
/// For the simplest API, use `calendar_colors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_colors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_colors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Colors>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_colors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET colors
/// Returns the color definitions for calendars and events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_colors_get_builder()` + `calendar_colors_get_execute()`.
/// For task-level control, use `calendar_colors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_colors_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Colors>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_colors_get_builder(client)?;
    calendar_colors_get_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_delete_execute()` to send, or `calendar_events_delete` for simplest API.

pub fn calendar_events_delete_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    eventId: String,
    sendNotifications: Option<bool>,
    sendUpdates: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/events/{}",
        calendarId.as_str(),
        eventId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sendNotifications {
        query_parts.push(format!("sendNotifications={}", val));
    }
    if let Some(val) = sendUpdates {
        query_parts.push(format!("sendUpdates={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_events_delete_execute()` or `calendar_events_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_events_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_events_delete_task()`.
/// For the simplest API, use `calendar_events_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_events_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_events_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_events_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarEventsDeleteArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Path parameter: eventId
    pub eventId: String,
    /// Query parameter: sendNotifications
    pub sendNotifications: Option<bool>,
    /// Query parameter: sendUpdates
    pub sendUpdates: Option<String>,
}

/// GET calendars/{calendarId}/events/{eventId}
/// Deletes an event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_delete_builder()` + `calendar_events_delete_execute()`.
/// For task-level control, use `calendar_events_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_delete(
    client: &SimpleHttpClient,
    args: &CalendarEventsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_delete_builder(
        client,
        args.calendarId.clone(),
        args.eventId.clone(),
        args.sendNotifications.clone(),
        args.sendUpdates.clone(),
    )?;
    calendar_events_delete_execute(builder)
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_import_execute()` to send, or `calendar_events_import` for simplest API.

pub fn calendar_events_import_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    conferenceDataVersion: Option<i32>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/events/import",
        calendarId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = conferenceDataVersion {
        query_parts.push(format!("conferenceDataVersion={}", val));
    }
    if let Some(val) = supportsAttachments {
        query_parts.push(format!("supportsAttachments={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_events_import_execute()` or `calendar_events_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Event>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_events_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_events_import_task()`.
/// For the simplest API, use `calendar_events_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_events_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_events_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarEventsImportArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Query parameter: conferenceDataVersion
    pub conferenceDataVersion: Option<i32>,
    /// Query parameter: supportsAttachments
    pub supportsAttachments: Option<bool>,
    /// Request body.
    pub body: Event,
}

/// GET calendars/{calendarId}/events/import
/// Imports an event. This operation is used to add a private copy of an existing event to a calendar. Only events with an `eventType` of default may be imported. Deprecated behavior: If a non-default event is imported, its type will be changed to default and any event-type-specific properties it may have will be dropped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_import_builder()` + `calendar_events_import_execute()`.
/// For task-level control, use `calendar_events_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_import(
    client: &SimpleHttpClient,
    args: &CalendarEventsImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_import_builder(
        client,
        args.calendarId.clone(),
        args.conferenceDataVersion.clone(),
        args.supportsAttachments.clone(),
        &args.body,
    )?;
    calendar_events_import_execute(builder)
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_insert_execute()` to send, or `calendar_events_insert` for simplest API.

pub fn calendar_events_insert_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    conferenceDataVersion: Option<i32>,
    maxAttendees: Option<i32>,
    sendNotifications: Option<bool>,
    sendUpdates: Option<String>,
    supportsAttachments: Option<bool>,
    body: &Event,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/events",
        calendarId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = conferenceDataVersion {
        query_parts.push(format!("conferenceDataVersion={}", val));
    }
    if let Some(val) = maxAttendees {
        query_parts.push(format!("maxAttendees={}", val));
    }
    if let Some(val) = sendNotifications {
        query_parts.push(format!("sendNotifications={}", val));
    }
    if let Some(val) = sendUpdates {
        query_parts.push(format!("sendUpdates={}", val));
    }
    if let Some(val) = supportsAttachments {
        query_parts.push(format!("supportsAttachments={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_events_insert_execute()` or `calendar_events_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Event>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Event = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_events_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_events_insert_task()`.
/// For the simplest API, use `calendar_events_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_events_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_events_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_events_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarEventsInsertArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Query parameter: conferenceDataVersion
    pub conferenceDataVersion: Option<i32>,
    /// Query parameter: maxAttendees
    pub maxAttendees: Option<i32>,
    /// Query parameter: sendNotifications
    pub sendNotifications: Option<bool>,
    /// Query parameter: sendUpdates
    pub sendUpdates: Option<String>,
    /// Query parameter: supportsAttachments
    pub supportsAttachments: Option<bool>,
    /// Request body.
    pub body: Event,
}

/// GET calendars/{calendarId}/events
/// Creates an event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_insert_builder()` + `calendar_events_insert_execute()`.
/// For task-level control, use `calendar_events_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_insert(
    client: &SimpleHttpClient,
    args: &CalendarEventsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Event>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_insert_builder(
        client,
        args.calendarId.clone(),
        args.conferenceDataVersion.clone(),
        args.maxAttendees.clone(),
        args.sendNotifications.clone(),
        args.sendUpdates.clone(),
        args.supportsAttachments.clone(),
        &args.body,
    )?;
    calendar_events_insert_execute(builder)
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_instances_execute()` to send, or `calendar_events_instances` for simplest API.

pub fn calendar_events_instances_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    eventId: String,
    alwaysIncludeEmail: Option<bool>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    originalStart: Option<String>,
    pageToken: Option<String>,
    showDeleted: Option<bool>,
    timeMax: Option<String>,
    timeMin: Option<String>,
    timeZone: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/events/{}/instances",
        calendarId.as_str(),
        eventId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = alwaysIncludeEmail {
        query_parts.push(format!("alwaysIncludeEmail={}", val));
    }
    if let Some(val) = maxAttendees {
        query_parts.push(format!("maxAttendees={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = originalStart {
        query_parts.push(format!("originalStart={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = timeMax {
        query_parts.push(format!("timeMax={}", val));
    }
    if let Some(val) = timeMin {
        query_parts.push(format!("timeMin={}", val));
    }
    if let Some(val) = timeZone {
        query_parts.push(format!("timeZone={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_events_instances_execute()` or `calendar_events_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Events>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Events = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_events_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_events_instances_task()`.
/// For the simplest API, use `calendar_events_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_events_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Events>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_events_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_events_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarEventsInstancesArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Path parameter: eventId
    pub eventId: String,
    /// Query parameter: alwaysIncludeEmail
    pub alwaysIncludeEmail: Option<bool>,
    /// Query parameter: maxAttendees
    pub maxAttendees: Option<i32>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: originalStart
    pub originalStart: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
    /// Query parameter: timeMax
    pub timeMax: Option<String>,
    /// Query parameter: timeMin
    pub timeMin: Option<String>,
    /// Query parameter: timeZone
    pub timeZone: Option<String>,
}

/// GET calendars/{calendarId}/events/{eventId}/instances
/// Returns instances of the specified recurring event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_instances_builder()` + `calendar_events_instances_execute()`.
/// For task-level control, use `calendar_events_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_instances(
    client: &SimpleHttpClient,
    args: &CalendarEventsInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Events>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_instances_builder(
        client,
        args.calendarId.clone(),
        args.eventId.clone(),
        args.alwaysIncludeEmail.clone(),
        args.maxAttendees.clone(),
        args.maxResults.clone(),
        args.originalStart.clone(),
        args.pageToken.clone(),
        args.showDeleted.clone(),
        args.timeMax.clone(),
        args.timeMin.clone(),
        args.timeZone.clone(),
    )?;
    calendar_events_instances_execute(builder)
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_events_watch_execute()` to send, or `calendar_events_watch` for simplest API.

pub fn calendar_events_watch_builder(
    client: &SimpleHttpClient,
    calendarId: String,
    alwaysIncludeEmail: Option<bool>,
    eventTypes: Option<String>,
    iCalUID: Option<String>,
    maxAttendees: Option<i32>,
    maxResults: Option<i32>,
    orderBy: Option<String>,
    pageToken: Option<String>,
    privateExtendedProperty: Option<String>,
    q: Option<String>,
    sharedExtendedProperty: Option<String>,
    showDeleted: Option<bool>,
    showHiddenInvitations: Option<bool>,
    singleEvents: Option<bool>,
    syncToken: Option<String>,
    timeMax: Option<String>,
    timeMin: Option<String>,
    timeZone: Option<String>,
    updatedMin: Option<String>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/calendars/{}/events/watch",
        calendarId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = alwaysIncludeEmail {
        query_parts.push(format!("alwaysIncludeEmail={}", val));
    }
    if let Some(val) = eventTypes {
        query_parts.push(format!("eventTypes={}", val));
    }
    if let Some(val) = iCalUID {
        query_parts.push(format!("iCalUID={}", val));
    }
    if let Some(val) = maxAttendees {
        query_parts.push(format!("maxAttendees={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = privateExtendedProperty {
        query_parts.push(format!("privateExtendedProperty={}", val));
    }
    if let Some(val) = q {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = sharedExtendedProperty {
        query_parts.push(format!("sharedExtendedProperty={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = showHiddenInvitations {
        query_parts.push(format!("showHiddenInvitations={}", val));
    }
    if let Some(val) = singleEvents {
        query_parts.push(format!("singleEvents={}", val));
    }
    if let Some(val) = syncToken {
        query_parts.push(format!("syncToken={}", val));
    }
    if let Some(val) = timeMax {
        query_parts.push(format!("timeMax={}", val));
    }
    if let Some(val) = timeMin {
        query_parts.push(format!("timeMin={}", val));
    }
    if let Some(val) = timeZone {
        query_parts.push(format!("timeZone={}", val));
    }
    if let Some(val) = updatedMin {
        query_parts.push(format!("updatedMin={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_events_watch_execute()` or `calendar_events_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_events_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_events_watch_task()`.
/// For the simplest API, use `calendar_events_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_events_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_events_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_events_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_events_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarEventsWatchArgs {
    /// Path parameter: calendarId
    pub calendarId: String,
    /// Query parameter: alwaysIncludeEmail
    pub alwaysIncludeEmail: Option<bool>,
    /// Query parameter: eventTypes
    pub eventTypes: Option<String>,
    /// Query parameter: iCalUID
    pub iCalUID: Option<String>,
    /// Query parameter: maxAttendees
    pub maxAttendees: Option<i32>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: privateExtendedProperty
    pub privateExtendedProperty: Option<String>,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: sharedExtendedProperty
    pub sharedExtendedProperty: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
    /// Query parameter: showHiddenInvitations
    pub showHiddenInvitations: Option<bool>,
    /// Query parameter: singleEvents
    pub singleEvents: Option<bool>,
    /// Query parameter: syncToken
    pub syncToken: Option<String>,
    /// Query parameter: timeMax
    pub timeMax: Option<String>,
    /// Query parameter: timeMin
    pub timeMin: Option<String>,
    /// Query parameter: timeZone
    pub timeZone: Option<String>,
    /// Query parameter: updatedMin
    pub updatedMin: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// GET calendars/{calendarId}/events/watch
/// Watch for changes to Events resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_events_watch_builder()` + `calendar_events_watch_execute()`.
/// For task-level control, use `calendar_events_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_events_watch(
    client: &SimpleHttpClient,
    args: &CalendarEventsWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_events_watch_builder(
        client,
        args.calendarId.clone(),
        args.alwaysIncludeEmail.clone(),
        args.eventTypes.clone(),
        args.iCalUID.clone(),
        args.maxAttendees.clone(),
        args.maxResults.clone(),
        args.orderBy.clone(),
        args.pageToken.clone(),
        args.privateExtendedProperty.clone(),
        args.q.clone(),
        args.sharedExtendedProperty.clone(),
        args.showDeleted.clone(),
        args.showHiddenInvitations.clone(),
        args.singleEvents.clone(),
        args.syncToken.clone(),
        args.timeMax.clone(),
        args.timeMin.clone(),
        args.timeZone.clone(),
        args.updatedMin.clone(),
        &args.body,
    )?;
    calendar_events_watch_execute(builder)
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_freebusy_query_execute()` to send, or `calendar_freebusy_query` for simplest API.

pub fn calendar_freebusy_query_builder(
    client: &SimpleHttpClient,
    body: &FreeBusyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/freeBusy",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_freebusy_query_execute()` or `calendar_freebusy_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_freebusy_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_freebusy_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FreeBusyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FreeBusyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_freebusy_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_freebusy_query_task()`.
/// For the simplest API, use `calendar_freebusy_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_freebusy_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_freebusy_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FreeBusyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = calendar_freebusy_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_freebusy_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarFreebusyQueryArgs {
    /// Request body.
    pub body: FreeBusyRequest,
}

/// GET freeBusy
/// Returns `free/busy` information for a set of calendars.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_freebusy_query_builder()` + `calendar_freebusy_query_execute()`.
/// For task-level control, use `calendar_freebusy_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_freebusy_query(
    client: &SimpleHttpClient,
    args: &CalendarFreebusyQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FreeBusyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = calendar_freebusy_query_builder(client, &args.body)?;
    calendar_freebusy_query_execute(builder)
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_settings_get_execute()` to send, or `calendar_settings_get` for simplest API.

pub fn calendar_settings_get_builder(
    client: &SimpleHttpClient,
    setting: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/calendar/v3/users/me/settings/{}",
        setting.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_settings_get_execute()` or `calendar_settings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Setting>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Setting = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_settings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_settings_get_task()`.
/// For the simplest API, use `calendar_settings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setting>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_settings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_settings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarSettingsGetArgs {
    /// Path parameter: setting
    pub setting: String,
}

/// GET users/me/settings/{setting}
/// Returns a single user setting.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_settings_get_builder()` + `calendar_settings_get_execute()`.
/// For task-level control, use `calendar_settings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_get(
    client: &SimpleHttpClient,
    args: &CalendarSettingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Setting>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_settings_get_builder(client, args.setting.clone())?;
    calendar_settings_get_execute(builder)
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_settings_list_execute()` to send, or `calendar_settings_list` for simplest API.

pub fn calendar_settings_list_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<String>,
    syncToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/users/me/settings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = syncToken {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_settings_list_execute()` or `calendar_settings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_settings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_settings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_settings_list_task()`.
/// For the simplest API, use `calendar_settings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_settings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_settings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_settings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_settings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarSettingsListArgs {
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: syncToken
    pub syncToken: Option<String>,
}

/// GET users/me/settings
/// Returns all user settings for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_settings_list_builder()` + `calendar_settings_list_execute()`.
/// For task-level control, use `calendar_settings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_list(
    client: &SimpleHttpClient,
    args: &CalendarSettingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_settings_list_builder(
        client,
        args.maxResults.clone(),
        args.pageToken.clone(),
        args.syncToken.clone(),
    )?;
    calendar_settings_list_execute(builder)
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `calendar_settings_watch_execute()` to send, or `calendar_settings_watch` for simplest API.

pub fn calendar_settings_watch_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    pageToken: Option<String>,
    syncToken: Option<String>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/calendar/v3/users/me/settings/watch",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = syncToken {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `calendar_settings_watch_execute()` or `calendar_settings_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_settings_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `calendar_settings_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `calendar_settings_watch_task()`.
/// For the simplest API, use `calendar_settings_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `calendar_settings_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn calendar_settings_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = calendar_settings_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`calendar_settings_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CalendarSettingsWatchArgs {
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: syncToken
    pub syncToken: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// GET users/me/settings/watch
/// Watch for changes to Settings resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `calendar_settings_watch_builder()` + `calendar_settings_watch_execute()`.
/// For task-level control, use `calendar_settings_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn calendar_settings_watch(
    client: &SimpleHttpClient,
    args: &CalendarSettingsWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = calendar_settings_watch_builder(
        client,
        args.maxResults.clone(),
        args.pageToken.clone(),
        args.syncToken.clone(),
        &args.body,
    )?;
    calendar_settings_watch_execute(builder)
}
