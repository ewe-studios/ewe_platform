//! Auto-generated API clients for gcp/clouddeploy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_get_execute()` to send, or `clouddeploy_projects_locations_get` for simplest API.

pub fn clouddeploy_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_get_execute()` or `clouddeploy_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_get_builder()` + `clouddeploy_projects_locations_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Gets the configuration for a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_get_config_execute()` to send, or `clouddeploy_projects_locations_get_config` for simplest API.

pub fn clouddeploy_projects_locations_get_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Gets the configuration for a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_get_config_execute()` or `clouddeploy_projects_locations_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Config = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Gets the configuration for a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_get_config_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Gets the configuration for a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_get_config_builder()` + `clouddeploy_projects_locations_get_config_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_get_config(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_get_config_builder(client, &args.name)?;
    clouddeploy_projects_locations_get_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_list_execute()` to send, or `clouddeploy_projects_locations_list` for simplest API.

pub fn clouddeploy_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_list_execute()` or `clouddeploy_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_list_builder()` + `clouddeploy_projects_locations_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Creates a new CustomTargetType in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_create_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_create` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customTargetTypeId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &CustomTargetType,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customTargetTypeId {
        query_parts.push(format!("customTargetTypeId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Creates a new CustomTargetType in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_create_execute()` or `clouddeploy_projects_locations_custom_target_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Creates a new CustomTargetType in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customTargetTypeId
    pub customTargetTypeId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: CustomTargetType,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Creates a new CustomTargetType in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_create_builder()` + `clouddeploy_projects_locations_custom_target_types_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_custom_target_types_create_builder(
        client,
        &args.parent,
        args.customTargetTypeId.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_custom_target_types_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Deletes a single CustomTargetType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_delete_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_delete` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Deletes a single CustomTargetType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_delete_execute()` or `clouddeploy_projects_locations_custom_target_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Deletes a single CustomTargetType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_delete_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Deletes a single CustomTargetType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_delete_builder()` + `clouddeploy_projects_locations_custom_target_types_delete_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_delete(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_custom_target_types_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    clouddeploy_projects_locations_custom_target_types_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Gets details of a single CustomTargetType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_get_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_get` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Gets details of a single CustomTargetType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_get_execute()` or `clouddeploy_projects_locations_custom_target_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CustomTargetType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomTargetType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Gets details of a single CustomTargetType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomTargetType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Gets details of a single CustomTargetType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_get_builder()` + `clouddeploy_projects_locations_custom_target_types_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomTargetType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_custom_target_types_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_custom_target_types_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_get_iam_policy_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_get_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_get_iam_policy_execute()` or `clouddeploy_projects_locations_custom_target_types_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_get_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_get_iam_policy_builder()` + `clouddeploy_projects_locations_custom_target_types_get_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_custom_target_types_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    clouddeploy_projects_locations_custom_target_types_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Lists CustomTargetTypes in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_list_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_list` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Lists CustomTargetTypes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_list_execute()` or `clouddeploy_projects_locations_custom_target_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListCustomTargetTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomTargetTypesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Lists CustomTargetTypes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomTargetTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes
/// Lists CustomTargetTypes in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_list_builder()` + `clouddeploy_projects_locations_custom_target_types_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomTargetTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_custom_target_types_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_custom_target_types_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Updates a single CustomTargetType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_patch_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_patch` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &CustomTargetType,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Updates a single CustomTargetType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_patch_execute()` or `clouddeploy_projects_locations_custom_target_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Updates a single CustomTargetType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_patch_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: CustomTargetType,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}
/// Updates a single CustomTargetType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_patch_builder()` + `clouddeploy_projects_locations_custom_target_types_patch_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_patch(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_custom_target_types_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_custom_target_types_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_custom_target_types_set_iam_policy_execute()` to send, or `clouddeploy_projects_locations_custom_target_types_set_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_custom_target_types_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/customTargetTypes/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_custom_target_types_set_iam_policy_execute()` or `clouddeploy_projects_locations_custom_target_types_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_custom_target_types_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_set_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_custom_target_types_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_custom_target_types_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_custom_target_types_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_custom_target_types_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_custom_target_types_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/customTargetTypes/{customTargetTypesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_custom_target_types_set_iam_policy_builder()` + `clouddeploy_projects_locations_custom_target_types_set_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_custom_target_types_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_custom_target_types_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsCustomTargetTypesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_custom_target_types_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    clouddeploy_projects_locations_custom_target_types_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Creates a new DeliveryPipeline in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_create_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_create` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    deliveryPipelineId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &DeliveryPipeline,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deliveryPipelineId {
        query_parts.push(format!("deliveryPipelineId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Creates a new DeliveryPipeline in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_create_execute()` or `clouddeploy_projects_locations_delivery_pipelines_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Creates a new DeliveryPipeline in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: deliveryPipelineId
    pub deliveryPipelineId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: DeliveryPipeline,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Creates a new DeliveryPipeline in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_create_builder()` + `clouddeploy_projects_locations_delivery_pipelines_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_create_builder(
        client,
        &args.parent,
        args.deliveryPipelineId.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Deletes a single DeliveryPipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_delete_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_delete` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Deletes a single DeliveryPipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_delete_execute()` or `clouddeploy_projects_locations_delivery_pipelines_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Deletes a single DeliveryPipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_delete_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Deletes a single DeliveryPipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_delete_builder()` + `clouddeploy_projects_locations_delivery_pipelines_delete_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_delete(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.force,
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Gets details of a single DeliveryPipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_get_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_get` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Gets details of a single DeliveryPipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_get_execute()` or `clouddeploy_projects_locations_delivery_pipelines_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DeliveryPipeline>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeliveryPipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Gets details of a single DeliveryPipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeliveryPipeline>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Gets details of a single DeliveryPipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_get_builder()` + `clouddeploy_projects_locations_delivery_pipelines_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeliveryPipeline>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_delivery_pipelines_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_execute()` or `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_builder()` + `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Lists DeliveryPipelines in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_list_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_list` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Lists DeliveryPipelines in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_list_execute()` or `clouddeploy_projects_locations_delivery_pipelines_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListDeliveryPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDeliveryPipelinesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Lists DeliveryPipelines in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeliveryPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines
/// Lists DeliveryPipelines in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_list_builder()` + `clouddeploy_projects_locations_delivery_pipelines_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeliveryPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_delivery_pipelines_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Updates the parameters of a single DeliveryPipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_patch_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_patch` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &DeliveryPipeline,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Updates the parameters of a single DeliveryPipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_patch_execute()` or `clouddeploy_projects_locations_delivery_pipelines_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Updates the parameters of a single DeliveryPipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_patch_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: DeliveryPipeline,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}
/// Updates the parameters of a single DeliveryPipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_patch_builder()` + `clouddeploy_projects_locations_delivery_pipelines_patch_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_patch(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:rollbackTarget
/// Creates a Rollout to roll back the specified target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_rollback_target_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_rollback_target` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_rollback_target_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RollbackTargetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}:rollbackTarget",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:rollbackTarget
/// Creates a Rollout to roll back the specified target.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_rollback_target_execute()` or `clouddeploy_projects_locations_delivery_pipelines_rollback_target`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_rollback_target_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_rollback_target_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<RollbackTargetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RollbackTargetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:rollbackTarget
/// Creates a Rollout to roll back the specified target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_rollback_target_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_rollback_target_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_rollback_target()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_rollback_target_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_rollback_target_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RollbackTargetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_rollback_target_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_rollback_target`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesRollbackTargetArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RollbackTargetRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:rollbackTarget
/// Creates a Rollout to roll back the specified target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_rollback_target_builder()` + `clouddeploy_projects_locations_delivery_pipelines_rollback_target_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_rollback_target_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_rollback_target(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesRollbackTargetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RollbackTargetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_rollback_target_builder(
        client, &args.name, &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_rollback_target_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_execute()` or `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_builder()` + `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_execute()` or `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_builder()` + `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}:cancel
/// Cancels an AutomationRun. The state of the AutomationRun after cancelling is CANCELLED. CancelAutomationRun can be called on AutomationRun in the state IN_PROGRESS and `PENDING`; AutomationRun in a different state returns an FAILED_PRECONDITION error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelAutomationRunRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automationRuns/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}:cancel
/// Cancels an AutomationRun. The state of the AutomationRun after cancelling is CANCELLED. CancelAutomationRun can be called on AutomationRun in the state IN_PROGRESS and `PENDING`; AutomationRun in a different state returns an FAILED_PRECONDITION error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CancelAutomationRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelAutomationRunResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}:cancel
/// Cancels an AutomationRun. The state of the AutomationRun after cancelling is CANCELLED. CancelAutomationRun can be called on AutomationRun in the state IN_PROGRESS and `PENDING`; AutomationRun in a different state returns an FAILED_PRECONDITION error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelAutomationRunResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelAutomationRunRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}:cancel
/// Cancels an AutomationRun. The state of the AutomationRun after cancelling is CANCELLED. CancelAutomationRun can be called on AutomationRun in the state IN_PROGRESS and `PENDING`; AutomationRun in a different state returns an FAILED_PRECONDITION error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelAutomationRunResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_builder(
        client, &args.name, &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}
/// Gets details of a single AutomationRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automationRuns/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}
/// Gets details of a single AutomationRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutomationRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutomationRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}
/// Gets details of a single AutomationRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutomationRun>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automation_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns/{automationRunsId}
/// Gets details of a single AutomationRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutomationRun>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_builder(
        client, &args.name,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns
/// Lists AutomationRuns in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automationRuns",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns
/// Lists AutomationRuns in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAutomationRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutomationRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns
/// Lists AutomationRuns in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutomationRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automation_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automationRuns
/// Lists AutomationRuns in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutomationRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Creates a new Automation in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automations_create_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automations_create` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    automationId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Automation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = automationId {
        query_parts.push(format!("automationId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Creates a new Automation in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automations_create_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Creates a new Automation in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_automations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: automationId
    pub automationId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Automation,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Creates a new Automation in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_create_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automations_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automations_create_builder(
        client,
        &args.parent,
        args.automationId.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automations_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Deletes a single Automation resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automations_delete_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automations_delete` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Deletes a single Automation resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automations_delete_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Deletes a single Automation resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_delete_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_automations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Deletes a single Automation resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_delete_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automations_delete_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_delete(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automations_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Gets details of a single Automation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automations_get_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automations_get` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Gets details of a single Automation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automations_get_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Automation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Automation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Gets details of a single Automation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Automation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_automations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Gets details of a single Automation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_get_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automations_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Automation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automations_get_builder(
        client, &args.name,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Lists Automations in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automations_list_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automations_list` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Lists Automations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automations_list_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAutomationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutomationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Lists Automations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAutomationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_automations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations
/// Lists Automations in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_list_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automations_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAutomationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Updates the parameters of a single Automation resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_automations_patch_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_automations_patch` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Automation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/automations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Updates the parameters of a single Automation resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_automations_patch_execute()` or `clouddeploy_projects_locations_delivery_pipelines_automations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Updates the parameters of a single Automation resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_patch_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_automations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_automations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_automations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_automations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Automation,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/automations/{automationsId}
/// Updates the parameters of a single Automation resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_automations_patch_builder()` + `clouddeploy_projects_locations_delivery_pipelines_automations_patch_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_automations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_automations_patch(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_automations_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_automations_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}:abandon
/// Abandons a Release in the Delivery Pipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_abandon` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_abandon_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AbandonReleaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}:abandon",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}:abandon
/// Abandons a Release in the Delivery Pipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_abandon`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_abandon_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AbandonReleaseResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AbandonReleaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}:abandon
/// Abandons a Release in the Delivery Pipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_abandon_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AbandonReleaseResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_abandon_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_abandon`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesAbandonArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AbandonReleaseRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}:abandon
/// Abandons a Release in the Delivery Pipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_abandon_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_abandon(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesAbandonArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AbandonReleaseResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_releases_abandon_builder(
        client, &args.name, &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_abandon_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Creates a new Release in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_create_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_create` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    overrideDeployPolicy: Option<&str>,
    releaseId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Release,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = overrideDeployPolicy {
        query_parts.push(format!("overrideDeployPolicy={}", val));
    }
    if let Some(val) = releaseId {
        query_parts.push(format!("releaseId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Creates a new Release in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_create_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Creates a new Release in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: overrideDeployPolicy
    pub overrideDeployPolicy: Option<String>,
    /// Query parameter: releaseId
    pub releaseId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Release,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Creates a new Release in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_create_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_releases_create_builder(
        client,
        &args.parent,
        args.overrideDeployPolicy.as_deref(),
        args.releaseId.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}
/// Gets details of a single Release.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_get_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_get` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}
/// Gets details of a single Release.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_get_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}
/// Gets details of a single Release.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}
/// Gets details of a single Release.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_get_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_delivery_pipelines_releases_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Lists Releases in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_list_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_list` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Lists Releases in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_list_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReleasesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Lists Releases in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases
/// Lists Releases in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_list_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_releases_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:advance
/// Advances a Rollout in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AdvanceRolloutRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}:advance",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:advance
/// Advances a Rollout in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AdvanceRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvanceRolloutResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:advance
/// Advances a Rollout in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvanceRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsAdvanceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AdvanceRolloutRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:advance
/// Advances a Rollout in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsAdvanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvanceRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_builder(
            client, &args.name, &args.body,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:approve
/// Approves a Rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveRolloutRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}:approve",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:approve
/// Approves a Rollout.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ApproveRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApproveRolloutResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:approve
/// Approves a Rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApproveRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsApproveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ApproveRolloutRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:approve
/// Approves a Rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsApproveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApproveRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_builder(
            client, &args.name, &args.body,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:cancel
/// Cancels a Rollout in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelRolloutRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:cancel
/// Cancels a Rollout in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CancelRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelRolloutResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:cancel
/// Cancels a Rollout in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelRolloutRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:cancel
/// Cancels a Rollout in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelRolloutResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_builder(
            client, &args.name, &args.body,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Creates a new Rollout in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    overrideDeployPolicy: Option<&str>,
    requestId: Option<&str>,
    rolloutId: Option<&str>,
    startingPhaseId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Rollout,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = overrideDeployPolicy {
        query_parts.push(format!("overrideDeployPolicy={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = rolloutId {
        query_parts.push(format!("rolloutId={}", val));
    }
    if let Some(val) = startingPhaseId {
        query_parts.push(format!("startingPhaseId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Creates a new Rollout in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Creates a new Rollout in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: overrideDeployPolicy
    pub overrideDeployPolicy: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: rolloutId
    pub rolloutId: Option<String>,
    /// Query parameter: startingPhaseId
    pub startingPhaseId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Rollout,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Creates a new Rollout in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_builder(
            client,
            &args.parent,
            args.overrideDeployPolicy.as_deref(),
            args.requestId.as_deref(),
            args.rolloutId.as_deref(),
            args.startingPhaseId.as_deref(),
            args.validateOnly,
            &args.body,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}
/// Gets details of a single Rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}
/// Gets details of a single Rollout.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Rollout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}
/// Gets details of a single Rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}
/// Gets details of a single Rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_builder(
        client, &args.name,
    )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:ignoreJob
/// Ignores the specified Job in a Rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_builder(
    client: &SimpleHttpClient,
    rollout: &str,
    body: &IgnoreJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}:ignoreJob",
        rollout,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:ignoreJob
/// Ignores the specified Job in a Rollout.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<IgnoreJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IgnoreJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:ignoreJob
/// Ignores the specified Job in a Rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IgnoreJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsIgnoreJobArgs {
    /// Path parameter: rollout
    pub rollout: String,
    /// Request body.
    pub body: IgnoreJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:ignoreJob
/// Ignores the specified Job in a Rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsIgnoreJobArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IgnoreJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_builder(
            client,
            &args.rollout,
            &args.body,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Lists Rollouts in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Lists Rollouts in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListRolloutsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRolloutsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Lists Rollouts in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRolloutsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts
/// Lists Rollouts in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRolloutsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:retryJob
/// Retries the specified Job in a Rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_builder(
    client: &SimpleHttpClient,
    rollout: &str,
    body: &RetryJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}:retryJob",
        rollout,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:retryJob
/// Retries the specified Job in a Rollout.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<RetryJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetryJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:retryJob
/// Retries the specified Job in a Rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RetryJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsRetryJobArgs {
    /// Path parameter: rollout
    pub rollout: String,
    /// Request body.
    pub body: RetryJobRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}:retryJob
/// Retries the specified Job in a Rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsRetryJobArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RetryJobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_builder(
            client,
            &args.rollout,
            &args.body,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}
/// Gets details of a single JobRun.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}/jobRuns/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}
/// Gets details of a single JobRun.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JobRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}
/// Gets details of a single JobRun.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}
/// Gets details of a single JobRun.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_builder(
            client, &args.name,
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns
/// Lists JobRuns in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}/jobRuns",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns
/// Lists JobRuns in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListJobRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns
/// Lists JobRuns in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns
/// Lists JobRuns in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.orderBy.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}:terminate
/// Terminates a Job Run in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_execute()` to send, or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate` for simplest API.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &TerminateJobRunRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deliveryPipelines/{}/releases/{}/rollouts/{}/jobRuns/{}:terminate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}:terminate
/// Terminates a Job Run in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_execute()` or `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TerminateJobRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TerminateJobRunResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}:terminate
/// Terminates a Job Run in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminateJobRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsTerminateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: TerminateJobRunRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deliveryPipelines/{deliveryPipelinesId}/releases/{releasesId}/rollouts/{rolloutsId}/jobRuns/{jobRunsId}:terminate
/// Terminates a Job Run in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_builder()` + `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsTerminateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TerminateJobRunResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_builder(client, &args.name, &args.body)?;
    clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Creates a new DeployPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_create_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_create` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    deployPolicyId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &DeployPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deployPolicyId {
        query_parts.push(format!("deployPolicyId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Creates a new DeployPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_create_execute()` or `clouddeploy_projects_locations_deploy_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Creates a new DeployPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: deployPolicyId
    pub deployPolicyId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: DeployPolicy,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Creates a new DeployPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_create_builder()` + `clouddeploy_projects_locations_deploy_policies_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_create_builder(
        client,
        &args.parent,
        args.deployPolicyId.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_deploy_policies_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Deletes a single DeployPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_delete_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_delete` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Deletes a single DeployPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_delete_execute()` or `clouddeploy_projects_locations_deploy_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Deletes a single DeployPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_delete_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Deletes a single DeployPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_delete_builder()` + `clouddeploy_projects_locations_deploy_policies_delete_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_delete(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    clouddeploy_projects_locations_deploy_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Gets details of a single DeployPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_get_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_get` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Gets details of a single DeployPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_get_execute()` or `clouddeploy_projects_locations_deploy_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DeployPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeployPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Gets details of a single DeployPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Gets details of a single DeployPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_get_builder()` + `clouddeploy_projects_locations_deploy_policies_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_deploy_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_get_iam_policy_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_get_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_get_iam_policy_execute()` or `clouddeploy_projects_locations_deploy_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_get_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_get_iam_policy_builder()` + `clouddeploy_projects_locations_deploy_policies_get_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    clouddeploy_projects_locations_deploy_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Lists DeployPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_list_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_list` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Lists DeployPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_list_execute()` or `clouddeploy_projects_locations_deploy_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListDeployPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDeployPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Lists DeployPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeployPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies
/// Lists DeployPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_list_builder()` + `clouddeploy_projects_locations_deploy_policies_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeployPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_deploy_policies_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Updates the parameters of a single DeployPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_patch_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_patch` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &DeployPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Updates the parameters of a single DeployPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_patch_execute()` or `clouddeploy_projects_locations_deploy_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Updates the parameters of a single DeployPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_patch_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: DeployPolicy,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}
/// Updates the parameters of a single DeployPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_patch_builder()` + `clouddeploy_projects_locations_deploy_policies_patch_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_patch(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_deploy_policies_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_deploy_policies_set_iam_policy_execute()` to send, or `clouddeploy_projects_locations_deploy_policies_set_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_deploy_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/deployPolicies/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_deploy_policies_set_iam_policy_execute()` or `clouddeploy_projects_locations_deploy_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_deploy_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_set_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_deploy_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_deploy_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_deploy_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_deploy_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_deploy_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployPolicies/{deployPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_deploy_policies_set_iam_policy_builder()` + `clouddeploy_projects_locations_deploy_policies_set_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_deploy_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_deploy_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsDeployPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_deploy_policies_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    clouddeploy_projects_locations_deploy_policies_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_operations_cancel_execute()` to send, or `clouddeploy_projects_locations_operations_cancel` for simplest API.

pub fn clouddeploy_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_operations_cancel_execute()` or `clouddeploy_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_operations_cancel_builder()` + `clouddeploy_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        clouddeploy_projects_locations_operations_cancel_builder(client, &args.name, &args.body)?;
    clouddeploy_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_operations_delete_execute()` to send, or `clouddeploy_projects_locations_operations_delete` for simplest API.

pub fn clouddeploy_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_operations_delete_execute()` or `clouddeploy_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_operations_delete_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_operations_delete_builder()` + `clouddeploy_projects_locations_operations_delete_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_operations_delete_builder(client, &args.name)?;
    clouddeploy_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_operations_get_execute()` to send, or `clouddeploy_projects_locations_operations_get` for simplest API.

pub fn clouddeploy_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_operations_get_execute()` or `clouddeploy_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_operations_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_operations_get_builder()` + `clouddeploy_projects_locations_operations_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_operations_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_operations_list_execute()` to send, or `clouddeploy_projects_locations_operations_list` for simplest API.

pub fn clouddeploy_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_operations_list_execute()` or `clouddeploy_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_operations_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_operations_list_builder()` + `clouddeploy_projects_locations_operations_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    clouddeploy_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Creates a new Target in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_create_execute()` to send, or `clouddeploy_projects_locations_targets_create` for simplest API.

pub fn clouddeploy_projects_locations_targets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    targetId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Target,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = targetId {
        query_parts.push(format!("targetId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Creates a new Target in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_create_execute()` or `clouddeploy_projects_locations_targets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Creates a new Target in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_create_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: targetId
    pub targetId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Target,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Creates a new Target in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_create_builder()` + `clouddeploy_projects_locations_targets_create_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_create(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_create_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        args.targetId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_targets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Deletes a single Target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_delete_execute()` to send, or `clouddeploy_projects_locations_targets_delete` for simplest API.

pub fn clouddeploy_projects_locations_targets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Deletes a single Target.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_delete_execute()` or `clouddeploy_projects_locations_targets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Deletes a single Target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_delete_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Deletes a single Target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_delete_builder()` + `clouddeploy_projects_locations_targets_delete_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_delete(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    clouddeploy_projects_locations_targets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Gets details of a single Target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_get_execute()` to send, or `clouddeploy_projects_locations_targets_get` for simplest API.

pub fn clouddeploy_projects_locations_targets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Gets details of a single Target.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_get_execute()` or `clouddeploy_projects_locations_targets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Target>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Target = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Gets details of a single Target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_get_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Target>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Gets details of a single Target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_get_builder()` + `clouddeploy_projects_locations_targets_get_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_get(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Target>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_get_builder(client, &args.name)?;
    clouddeploy_projects_locations_targets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_get_iam_policy_execute()` to send, or `clouddeploy_projects_locations_targets_get_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_targets_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_get_iam_policy_execute()` or `clouddeploy_projects_locations_targets_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_get_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_get_iam_policy_builder()` + `clouddeploy_projects_locations_targets_get_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    clouddeploy_projects_locations_targets_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Lists Targets in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_list_execute()` to send, or `clouddeploy_projects_locations_targets_list` for simplest API.

pub fn clouddeploy_projects_locations_targets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Lists Targets in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_list_execute()` or `clouddeploy_projects_locations_targets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListTargetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTargetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Lists Targets in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_list_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTargetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets
/// Lists Targets in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_list_builder()` + `clouddeploy_projects_locations_targets_list_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_list(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTargetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    clouddeploy_projects_locations_targets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Updates the parameters of a single Target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_patch_execute()` to send, or `clouddeploy_projects_locations_targets_patch` for simplest API.

pub fn clouddeploy_projects_locations_targets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Target,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Updates the parameters of a single Target.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_patch_execute()` or `clouddeploy_projects_locations_targets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Updates the parameters of a single Target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_patch_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Target,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}
/// Updates the parameters of a single Target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_patch_builder()` + `clouddeploy_projects_locations_targets_patch_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_patch(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    clouddeploy_projects_locations_targets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_set_iam_policy_execute()` to send, or `clouddeploy_projects_locations_targets_set_iam_policy` for simplest API.

pub fn clouddeploy_projects_locations_targets_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_set_iam_policy_execute()` or `clouddeploy_projects_locations_targets_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_set_iam_policy_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_set_iam_policy_builder()` + `clouddeploy_projects_locations_targets_set_iam_policy_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    clouddeploy_projects_locations_targets_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouddeploy_projects_locations_targets_test_iam_permissions_execute()` to send, or `clouddeploy_projects_locations_targets_test_iam_permissions` for simplest API.

pub fn clouddeploy_projects_locations_targets_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://clouddeploy.googleapis.com/v1/projects/{}/locations/{}/targets/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouddeploy_projects_locations_targets_test_iam_permissions_execute()` or `clouddeploy_projects_locations_targets_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouddeploy_projects_locations_targets_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouddeploy_projects_locations_targets_test_iam_permissions_task()`.
/// For the simplest API, use `clouddeploy_projects_locations_targets_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouddeploy_projects_locations_targets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouddeploy_projects_locations_targets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = clouddeploy_projects_locations_targets_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouddeploy_projects_locations_targets_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targets/{targetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouddeploy_projects_locations_targets_test_iam_permissions_builder()` + `clouddeploy_projects_locations_targets_test_iam_permissions_execute()`.
/// For task-level control, use `clouddeploy_projects_locations_targets_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouddeploy_projects_locations_targets_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ClouddeployProjectsLocationsTargetsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = clouddeploy_projects_locations_targets_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    clouddeploy_projects_locations_targets_test_iam_permissions_execute(builder)
}
