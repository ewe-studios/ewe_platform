//! Auto-generated API clients for gcp/securitycenter.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_group_execute()` to send, or `securitycenter_folders_assets_group` for simplest API.

pub fn securitycenter_folders_assets_group_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GroupAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/folders/{}/assets:group",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_assets_group_execute()` or `securitycenter_folders_assets_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_assets_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_assets_group_task()`.
/// For the simplest API, use `securitycenter_folders_assets_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_assets_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_assets_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersAssetsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GroupAssetsRequest,
}

/// GET v1/folders/{foldersId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_group_builder()` + `securitycenter_folders_assets_group_execute()`.
/// For task-level control, use `securitycenter_folders_assets_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersAssetsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_assets_group_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_folders_assets_group_execute(builder)
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_assets_list_execute()` to send, or `securitycenter_folders_assets_list` for simplest API.

pub fn securitycenter_folders_assets_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    compareDuration: Option<String>,
    fieldMask: Option<String>,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    readTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/folders/{}/assets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_assets_list_execute()` or `securitycenter_folders_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_assets_list_task()`.
/// For the simplest API, use `securitycenter_folders_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/folders/{foldersId}/assets
/// Lists an organization's assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_assets_list_builder()` + `securitycenter_folders_assets_list_execute()`.
/// For task-level control, use `securitycenter_folders_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_assets_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_assets_list_builder(
        client,
        args.parent.clone(),
        args.compareDuration.clone(),
        args.fieldMask.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.readTime.clone(),
    )?;
    securitycenter_folders_assets_list_execute(builder)
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_big_query_exports_create_execute()` to send, or `securitycenter_folders_big_query_exports_create` for simplest API.

pub fn securitycenter_folders_big_query_exports_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    bigQueryExportId: Option<String>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/folders/{}/bigQueryExports",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_big_query_exports_create_execute()` or `securitycenter_folders_big_query_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_big_query_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_big_query_exports_create_task()`.
/// For the simplest API, use `securitycenter_folders_big_query_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_big_query_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_big_query_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersBigQueryExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bigQueryExportId
    pub bigQueryExportId: Option<String>,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1BigQueryExport,
}

/// GET v1/folders/{foldersId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_big_query_exports_create_builder()` + `securitycenter_folders_big_query_exports_create_execute()`.
/// For task-level control, use `securitycenter_folders_big_query_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_big_query_exports_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersBigQueryExportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_big_query_exports_create_builder(
        client,
        args.parent.clone(),
        args.bigQueryExportId.clone(),
        &args.body,
    )?;
    securitycenter_folders_big_query_exports_create_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings:validateCustomModule",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()` or `securitycenter_folders_event_threat_detection_settings_validate_custom_module`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_event_threat_detection_settings_validate_custom_module_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_validate_custom_module`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ValidateEventThreatDetectionCustomModuleRequest,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_validate_custom_module_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsValidateCustomModuleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_validate_custom_module_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_folders_event_threat_detection_settings_validate_custom_module_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: EventThreatDetectionCustomModule,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_event_threat_detection_settings_custom_modules_create_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_create_execute(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/customModules:listDescendant",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()` or `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_folders_event_threat_detection_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/eventThreatDetectionSettings/effectiveCustomModules",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()` or `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_folders_event_threat_detection_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_findings_bulk_mute_execute()` to send, or `securitycenter_folders_findings_bulk_mute` for simplest API.

pub fn securitycenter_folders_findings_bulk_mute_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &BulkMuteFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/folders/{}/findings:bulkMute",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_findings_bulk_mute_execute()` or `securitycenter_folders_findings_bulk_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_findings_bulk_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_findings_bulk_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_findings_bulk_mute_task()`.
/// For the simplest API, use `securitycenter_folders_findings_bulk_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_folders_findings_bulk_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_findings_bulk_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersFindingsBulkMuteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BulkMuteFindingsRequest,
}

/// GET v1/folders/{foldersId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_findings_bulk_mute_builder()` + `securitycenter_folders_findings_bulk_mute_execute()`.
/// For task-level control, use `securitycenter_folders_findings_bulk_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_findings_bulk_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersFindingsBulkMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_findings_bulk_mute_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_folders_findings_bulk_mute_execute(builder)
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_mute_configs_create_execute()` to send, or `securitycenter_folders_mute_configs_create` for simplest API.

pub fn securitycenter_folders_mute_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    muteConfigId: Option<String>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/folders/{}/muteConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_mute_configs_create_execute()` or `securitycenter_folders_mute_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_mute_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_mute_configs_create_task()`.
/// For the simplest API, use `securitycenter_folders_mute_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_mute_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_mute_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersMuteConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: muteConfigId
    pub muteConfigId: Option<String>,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1MuteConfig,
}

/// GET v1/folders/{foldersId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_mute_configs_create_builder()` + `securitycenter_folders_mute_configs_create_execute()`.
/// For task-level control, use `securitycenter_folders_mute_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_mute_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersMuteConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_mute_configs_create_builder(
        client,
        args.parent.clone(),
        args.muteConfigId.clone(),
        &args.body,
    )?;
    securitycenter_folders_mute_configs_create_execute(builder)
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_notification_configs_create_execute()` to send, or `securitycenter_folders_notification_configs_create` for simplest API.

pub fn securitycenter_folders_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    configId: Option<String>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/folders/{}/notificationConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_notification_configs_create_execute()` or `securitycenter_folders_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_notification_configs_create_task()`.
/// For the simplest API, use `securitycenter_folders_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: configId
    pub configId: Option<String>,
    /// Request body.
    pub body: NotificationConfig,
}

/// GET v1/folders/{foldersId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_notification_configs_create_builder()` + `securitycenter_folders_notification_configs_create_execute()`.
/// For task-level control, use `securitycenter_folders_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_notification_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_notification_configs_create_builder(
        client,
        args.parent.clone(),
        args.configId.clone(),
        &args.body,
    )?;
    securitycenter_folders_notification_configs_create_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_security_health_analytics_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_create_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_create_execute(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/customModules:simulate",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()` or `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_custom_modules_simulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SimulateSecurityHealthAnalyticsCustomModuleRequest,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_folders_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/folders/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()` or `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_folders_security_health_analytics_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_folders_sources_list_execute()` to send, or `securitycenter_folders_sources_list` for simplest API.

pub fn securitycenter_folders_sources_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/folders/{}/sources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_folders_sources_list_execute()` or `securitycenter_folders_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_folders_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_folders_sources_list_task()`.
/// For the simplest API, use `securitycenter_folders_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_folders_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_folders_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_folders_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_folders_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterFoldersSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/folders/{foldersId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_folders_sources_list_builder()` + `securitycenter_folders_sources_list_execute()`.
/// For task-level control, use `securitycenter_folders_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_folders_sources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterFoldersSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_folders_sources_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    securitycenter_folders_sources_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_get_organization_settings_execute()` to send, or `securitycenter_organizations_get_organization_settings` for simplest API.

pub fn securitycenter_organizations_get_organization_settings_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/organizationSettings",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_get_organization_settings_execute()` or `securitycenter_organizations_get_organization_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_get_organization_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_get_organization_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrganizationSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_get_organization_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_get_organization_settings_task()`.
/// For the simplest API, use `securitycenter_organizations_get_organization_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_get_organization_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_get_organization_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_get_organization_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_get_organization_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsGetOrganizationSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/organizationSettings
/// Gets the settings for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_get_organization_settings_builder()` + `securitycenter_organizations_get_organization_settings_execute()`.
/// For task-level control, use `securitycenter_organizations_get_organization_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_get_organization_settings(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsGetOrganizationSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OrganizationSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_get_organization_settings_builder(client, args.name.clone())?;
    securitycenter_organizations_get_organization_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_group_execute()` to send, or `securitycenter_organizations_assets_group` for simplest API.

pub fn securitycenter_organizations_assets_group_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GroupAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/assets:group",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_group_execute()` or `securitycenter_organizations_assets_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_group_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GroupAssetsRequest,
}

/// GET v1/organizations/{organizationsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_group_builder()` + `securitycenter_organizations_assets_group_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_assets_group_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_organizations_assets_group_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_list_execute()` to send, or `securitycenter_organizations_assets_list` for simplest API.

pub fn securitycenter_organizations_assets_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    compareDuration: Option<String>,
    fieldMask: Option<String>,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    readTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/organizations/{}/assets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_list_execute()` or `securitycenter_organizations_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_list_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/organizations/{organizationsId}/assets
/// Lists an organization's assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_list_builder()` + `securitycenter_organizations_assets_list_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_list_builder(
        client,
        args.parent.clone(),
        args.compareDuration.clone(),
        args.fieldMask.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.readTime.clone(),
    )?;
    securitycenter_organizations_assets_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_assets_run_discovery_execute()` to send, or `securitycenter_organizations_assets_run_discovery` for simplest API.

pub fn securitycenter_organizations_assets_run_discovery_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &RunAssetDiscoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/assets:runDiscovery",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_assets_run_discovery_execute()` or `securitycenter_organizations_assets_run_discovery`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_run_discovery_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_run_discovery_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_assets_run_discovery_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_assets_run_discovery_task()`.
/// For the simplest API, use `securitycenter_organizations_assets_run_discovery()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_assets_run_discovery_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_assets_run_discovery_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_assets_run_discovery_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_assets_run_discovery`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAssetsRunDiscoveryArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: RunAssetDiscoveryRequest,
}

/// GET v1/organizations/{organizationsId}/assets:runDiscovery
/// Runs asset discovery. The discovery is tracked with a long-running operation. This API can only be called with limited frequency for an organization. If it is called too frequently the caller will receive a TOO_MANY_REQUESTS error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_assets_run_discovery_builder()` + `securitycenter_organizations_assets_run_discovery_execute()`.
/// For task-level control, use `securitycenter_organizations_assets_run_discovery_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_assets_run_discovery(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAssetsRunDiscoveryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_assets_run_discovery_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    securitycenter_organizations_assets_run_discovery_execute(builder)
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_attack_paths_list_execute()` to send, or `securitycenter_organizations_attack_paths_list` for simplest API.

pub fn securitycenter_organizations_attack_paths_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/attackPaths",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_attack_paths_list_execute()` or `securitycenter_organizations_attack_paths_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_attack_paths_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttackPathsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttackPathsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_attack_paths_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_attack_paths_list_task()`.
/// For the simplest API, use `securitycenter_organizations_attack_paths_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_attack_paths_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_attack_paths_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_attack_paths_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_attack_paths_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsAttackPathsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/attackPaths
/// Lists the attack paths for a set of simulation results or valued resources and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_attack_paths_list_builder()` + `securitycenter_organizations_attack_paths_list_execute()`.
/// For task-level control, use `securitycenter_organizations_attack_paths_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_attack_paths_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsAttackPathsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttackPathsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_attack_paths_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    securitycenter_organizations_attack_paths_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_big_query_exports_create_execute()` to send, or `securitycenter_organizations_big_query_exports_create` for simplest API.

pub fn securitycenter_organizations_big_query_exports_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    bigQueryExportId: Option<String>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/bigQueryExports",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_big_query_exports_create_execute()` or `securitycenter_organizations_big_query_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_big_query_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_big_query_exports_create_task()`.
/// For the simplest API, use `securitycenter_organizations_big_query_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_big_query_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_big_query_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsBigQueryExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bigQueryExportId
    pub bigQueryExportId: Option<String>,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1BigQueryExport,
}

/// GET v1/organizations/{organizationsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_big_query_exports_create_builder()` + `securitycenter_organizations_big_query_exports_create_execute()`.
/// For task-level control, use `securitycenter_organizations_big_query_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_big_query_exports_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsBigQueryExportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_big_query_exports_create_builder(
        client,
        args.parent.clone(),
        args.bigQueryExportId.clone(),
        &args.body,
    )?;
    securitycenter_organizations_big_query_exports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings:validateCustomModule",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()` or `securitycenter_organizations_event_threat_detection_settings_validate_custom_module`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_validate_custom_module`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ValidateEventThreatDetectionCustomModuleRequest,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_validate_custom_module_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsValidateCustomModuleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_validate_custom_module_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_organizations_event_threat_detection_settings_validate_custom_module_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: EventThreatDetectionCustomModule,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_organizations_event_threat_detection_settings_custom_modules_create_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_create_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/customModules:listDescendant",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()` or `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_organizations_event_threat_detection_settings_custom_modules_list_descendant_execute(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/eventThreatDetectionSettings/effectiveCustomModules",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()` or `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_organizations_event_threat_detection_settings_effective_custom_modules_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_findings_bulk_mute_execute()` to send, or `securitycenter_organizations_findings_bulk_mute` for simplest API.

pub fn securitycenter_organizations_findings_bulk_mute_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &BulkMuteFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/findings:bulkMute",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_findings_bulk_mute_execute()` or `securitycenter_organizations_findings_bulk_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_findings_bulk_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_findings_bulk_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_findings_bulk_mute_task()`.
/// For the simplest API, use `securitycenter_organizations_findings_bulk_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_findings_bulk_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_findings_bulk_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsFindingsBulkMuteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BulkMuteFindingsRequest,
}

/// GET v1/organizations/{organizationsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_findings_bulk_mute_builder()` + `securitycenter_organizations_findings_bulk_mute_execute()`.
/// For task-level control, use `securitycenter_organizations_findings_bulk_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_findings_bulk_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsFindingsBulkMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_findings_bulk_mute_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    securitycenter_organizations_findings_bulk_mute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_mute_configs_create_execute()` to send, or `securitycenter_organizations_mute_configs_create` for simplest API.

pub fn securitycenter_organizations_mute_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    muteConfigId: Option<String>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/muteConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_mute_configs_create_execute()` or `securitycenter_organizations_mute_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_mute_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_mute_configs_create_task()`.
/// For the simplest API, use `securitycenter_organizations_mute_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_mute_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_mute_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsMuteConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: muteConfigId
    pub muteConfigId: Option<String>,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1MuteConfig,
}

/// GET v1/organizations/{organizationsId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_mute_configs_create_builder()` + `securitycenter_organizations_mute_configs_create_execute()`.
/// For task-level control, use `securitycenter_organizations_mute_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_mute_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsMuteConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_mute_configs_create_builder(
        client,
        args.parent.clone(),
        args.muteConfigId.clone(),
        &args.body,
    )?;
    securitycenter_organizations_mute_configs_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_notification_configs_create_execute()` to send, or `securitycenter_organizations_notification_configs_create` for simplest API.

pub fn securitycenter_organizations_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    configId: Option<String>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/notificationConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_notification_configs_create_execute()` or `securitycenter_organizations_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_notification_configs_create_task()`.
/// For the simplest API, use `securitycenter_organizations_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: configId
    pub configId: Option<String>,
    /// Request body.
    pub body: NotificationConfig,
}

/// GET v1/organizations/{organizationsId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_notification_configs_create_builder()` + `securitycenter_organizations_notification_configs_create_execute()`.
/// For task-level control, use `securitycenter_organizations_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_notification_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_notification_configs_create_builder(
        client,
        args.parent.clone(),
        args.configId.clone(),
        &args.body,
    )?;
    securitycenter_organizations_notification_configs_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_operations_list_execute()` to send, or `securitycenter_organizations_operations_list` for simplest API.

pub fn securitycenter_organizations_operations_list_builder(
    client: &SimpleHttpClient,
    name: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_operations_list_execute()` or `securitycenter_organizations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_operations_list_task()`.
/// For the simplest API, use `securitycenter_organizations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_operations_list_builder()` + `securitycenter_organizations_operations_list_execute()`.
/// For task-level control, use `securitycenter_organizations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_operations_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_operations_list_builder(
        client,
        args.name.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.returnPartialSuccess.clone(),
    )?;
    securitycenter_organizations_operations_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_batch_create_execute()` to send, or `securitycenter_organizations_resource_value_configs_batch_create` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_batch_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &BatchCreateResourceValueConfigsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs:batchCreate",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_batch_create_execute()` or `securitycenter_organizations_resource_value_configs_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateResourceValueConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_batch_create_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchCreateResourceValueConfigsRequest,
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs:batchCreate
/// Creates a ResourceValueConfig for an organization. Maps user's tags to difference resource values for use by the attack path simulation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_batch_create_builder()` + `securitycenter_organizations_resource_value_configs_batch_create_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_batch_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_batch_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    securitycenter_organizations_resource_value_configs_batch_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_resource_value_configs_list_execute()` to send, or `securitycenter_organizations_resource_value_configs_list` for simplest API.

pub fn securitycenter_organizations_resource_value_configs_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/resourceValueConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_resource_value_configs_list_execute()` or `securitycenter_organizations_resource_value_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResourceValueConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_resource_value_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_list_task()`.
/// For the simplest API, use `securitycenter_organizations_resource_value_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_resource_value_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_resource_value_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_resource_value_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_resource_value_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsResourceValueConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/resourceValueConfigs
/// Lists all ResourceValueConfigs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_resource_value_configs_list_builder()` + `securitycenter_organizations_resource_value_configs_list_execute()`.
/// For task-level control, use `securitycenter_organizations_resource_value_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_resource_value_configs_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsResourceValueConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListResourceValueConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_resource_value_configs_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    securitycenter_organizations_resource_value_configs_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_create_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_create_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_list_descendant_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/customModules:simulate",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()` or `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SimulateSecurityHealthAnalyticsCustomModuleRequest,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_organizations_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/organizations/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()` or `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_organizations_security_health_analytics_settings_effective_custom_modules_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_sources_create_execute()` to send, or `securitycenter_organizations_sources_create` for simplest API.

pub fn securitycenter_organizations_sources_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/sources",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_sources_create_execute()` or `securitycenter_organizations_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Source>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_sources_create_task()`.
/// For the simplest API, use `securitycenter_organizations_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsSourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Source,
}

/// GET v1/organizations/{organizationsId}/sources
/// Creates a source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_sources_create_builder()` + `securitycenter_organizations_sources_create_execute()`.
/// For task-level control, use `securitycenter_organizations_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_sources_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsSourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_sources_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    securitycenter_organizations_sources_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_organizations_valued_resources_list_execute()` to send, or `securitycenter_organizations_valued_resources_list` for simplest API.

pub fn securitycenter_organizations_valued_resources_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/organizations/{}/valuedResources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_organizations_valued_resources_list_execute()` or `securitycenter_organizations_valued_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_valued_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListValuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_organizations_valued_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_organizations_valued_resources_list_task()`.
/// For the simplest API, use `securitycenter_organizations_valued_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_organizations_valued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_organizations_valued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_organizations_valued_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_organizations_valued_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterOrganizationsValuedResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/valuedResources
/// Lists the valued resources for a set of simulation results and filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_organizations_valued_resources_list_builder()` + `securitycenter_organizations_valued_resources_list_execute()`.
/// For task-level control, use `securitycenter_organizations_valued_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_organizations_valued_resources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterOrganizationsValuedResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListValuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_organizations_valued_resources_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    securitycenter_organizations_valued_resources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_group_execute()` to send, or `securitycenter_projects_assets_group` for simplest API.

pub fn securitycenter_projects_assets_group_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GroupAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/projects/{}/assets:group",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_assets_group_execute()` or `securitycenter_projects_assets_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GroupAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GroupAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_assets_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_assets_group_task()`.
/// For the simplest API, use `securitycenter_projects_assets_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_assets_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_assets_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_assets_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsAssetsGroupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GroupAssetsRequest,
}

/// GET v1/projects/{projectsId}/assets:group
/// Filters an organization's assets and groups them by their specified properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_group_builder()` + `securitycenter_projects_assets_group_execute()`.
/// For task-level control, use `securitycenter_projects_assets_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_group(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsAssetsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GroupAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_assets_group_builder(client, args.parent.clone(), &args.body)?;
    securitycenter_projects_assets_group_execute(builder)
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_assets_list_execute()` to send, or `securitycenter_projects_assets_list` for simplest API.

pub fn securitycenter_projects_assets_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    compareDuration: Option<String>,
    fieldMask: Option<String>,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    readTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/projects/{}/assets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = compareDuration {
        query_parts.push(format!("compareDuration={}", val));
    }
    if let Some(val) = fieldMask {
        query_parts.push(format!("fieldMask={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_assets_list_execute()` or `securitycenter_projects_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_assets_list_task()`.
/// For the simplest API, use `securitycenter_projects_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: compareDuration
    pub compareDuration: Option<String>,
    /// Query parameter: fieldMask
    pub fieldMask: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/projects/{projectsId}/assets
/// Lists an organization's assets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_assets_list_builder()` + `securitycenter_projects_assets_list_execute()`.
/// For task-level control, use `securitycenter_projects_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_assets_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_assets_list_builder(
        client,
        args.parent.clone(),
        args.compareDuration.clone(),
        args.fieldMask.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.readTime.clone(),
    )?;
    securitycenter_projects_assets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_big_query_exports_create_execute()` to send, or `securitycenter_projects_big_query_exports_create` for simplest API.

pub fn securitycenter_projects_big_query_exports_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    bigQueryExportId: Option<String>,
    body: &GoogleCloudSecuritycenterV1BigQueryExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/projects/{}/bigQueryExports",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bigQueryExportId {
        query_parts.push(format!("bigQueryExportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_big_query_exports_create_execute()` or `securitycenter_projects_big_query_exports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1BigQueryExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_big_query_exports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_big_query_exports_create_task()`.
/// For the simplest API, use `securitycenter_projects_big_query_exports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_big_query_exports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_big_query_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_big_query_exports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_big_query_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsBigQueryExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bigQueryExportId
    pub bigQueryExportId: Option<String>,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1BigQueryExport,
}

/// GET v1/projects/{projectsId}/bigQueryExports
/// Creates a BigQuery export.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_big_query_exports_create_builder()` + `securitycenter_projects_big_query_exports_create_execute()`.
/// For task-level control, use `securitycenter_projects_big_query_exports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_big_query_exports_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsBigQueryExportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1BigQueryExport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_big_query_exports_create_builder(
        client,
        args.parent.clone(),
        args.bigQueryExportId.clone(),
        &args.body,
    )?;
    securitycenter_projects_big_query_exports_create_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_validate_custom_module` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &ValidateEventThreatDetectionCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings:validateCustomModule",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()` or `securitycenter_projects_event_threat_detection_settings_validate_custom_module`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateEventThreatDetectionCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_validate_custom_module_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_validate_custom_module`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ValidateEventThreatDetectionCustomModuleRequest,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings:validateCustomModule
/// Validates the given Event Threat Detection custom module.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder()` + `securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_validate_custom_module_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_validate_custom_module(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsValidateCustomModuleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateEventThreatDetectionCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_validate_custom_module_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_projects_event_threat_detection_settings_validate_custom_module_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_create` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &EventThreatDetectionCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventThreatDetectionCustomModule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_custom_modules_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: EventThreatDetectionCustomModule,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules
/// Creates a resident Event Threat Detection custom module at the scope of the given Resource Manager parent, and also creates inherited custom modules for all descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventThreatDetectionCustomModule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_event_threat_detection_settings_custom_modules_create_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_create_execute(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/customModules:listDescendant",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()` or `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/customModules:listDescendant
/// Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and its descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder()` + `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_projects_event_threat_detection_settings_custom_modules_list_descendant_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/eventThreatDetectionSettings/effectiveCustomModules",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()` or `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveEventThreatDetectionCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/eventThreatDetectionSettings/effectiveCustomModules
/// Lists all effective Event Threat Detection custom modules for the given parent. This includes resident modules defined at the scope of the parent along with modules inherited from its ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder()` + `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsEventThreatDetectionSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveEventThreatDetectionCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_projects_event_threat_detection_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_findings_bulk_mute_execute()` to send, or `securitycenter_projects_findings_bulk_mute` for simplest API.

pub fn securitycenter_projects_findings_bulk_mute_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &BulkMuteFindingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/projects/{}/findings:bulkMute",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_findings_bulk_mute_execute()` or `securitycenter_projects_findings_bulk_mute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_findings_bulk_mute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_findings_bulk_mute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_findings_bulk_mute_task()`.
/// For the simplest API, use `securitycenter_projects_findings_bulk_mute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_findings_bulk_mute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_findings_bulk_mute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = securitycenter_projects_findings_bulk_mute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_findings_bulk_mute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsFindingsBulkMuteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BulkMuteFindingsRequest,
}

/// GET v1/projects/{projectsId}/findings:bulkMute
/// Kicks off an LRO to bulk mute findings for a parent based on a filter. The parent can be either an organization, folder or project. The findings matched by the filter will be muted after the LRO is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_findings_bulk_mute_builder()` + `securitycenter_projects_findings_bulk_mute_execute()`.
/// For task-level control, use `securitycenter_projects_findings_bulk_mute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_findings_bulk_mute(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsFindingsBulkMuteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_findings_bulk_mute_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    securitycenter_projects_findings_bulk_mute_execute(builder)
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_mute_configs_create_execute()` to send, or `securitycenter_projects_mute_configs_create` for simplest API.

pub fn securitycenter_projects_mute_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    muteConfigId: Option<String>,
    body: &GoogleCloudSecuritycenterV1MuteConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/projects/{}/muteConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = muteConfigId {
        query_parts.push(format!("muteConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_mute_configs_create_execute()` or `securitycenter_projects_mute_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1MuteConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_mute_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_mute_configs_create_task()`.
/// For the simplest API, use `securitycenter_projects_mute_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_mute_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_mute_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_mute_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_mute_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsMuteConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: muteConfigId
    pub muteConfigId: Option<String>,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1MuteConfig,
}

/// GET v1/projects/{projectsId}/muteConfigs
/// Creates a mute config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_mute_configs_create_builder()` + `securitycenter_projects_mute_configs_create_execute()`.
/// For task-level control, use `securitycenter_projects_mute_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_mute_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsMuteConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudSecuritycenterV1MuteConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_mute_configs_create_builder(
        client,
        args.parent.clone(),
        args.muteConfigId.clone(),
        &args.body,
    )?;
    securitycenter_projects_mute_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_notification_configs_create_execute()` to send, or `securitycenter_projects_notification_configs_create` for simplest API.

pub fn securitycenter_projects_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    configId: Option<String>,
    body: &NotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://securitycenter.googleapis.com/v1/projects/{}/notificationConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_notification_configs_create_execute()` or `securitycenter_projects_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_notification_configs_create_task()`.
/// For the simplest API, use `securitycenter_projects_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: configId
    pub configId: Option<String>,
    /// Request body.
    pub body: NotificationConfig,
}

/// GET v1/projects/{projectsId}/notificationConfigs
/// Creates a notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_notification_configs_create_builder()` + `securitycenter_projects_notification_configs_create_execute()`.
/// For task-level control, use `securitycenter_projects_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_notification_configs_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_notification_configs_create_builder(
        client,
        args.parent.clone(),
        args.configId.clone(),
        &args.body,
    )?;
    securitycenter_projects_notification_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_create` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_security_health_analytics_settings_custom_modules_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules
/// Creates a resident SecurityHealthAnalyticsCustomModule at the scope of the given CRM parent, and also creates inherited SecurityHealthAnalyticsCustomModules for all CRM descendants of the given parent. These modules are enabled by default.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_create(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudSecuritycenterV1SecurityHealthAnalyticsCustomModule>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_create_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules:listDescendant",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDescendantSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:listDescendant
/// Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and all of the parent’s CRM descendants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesListDescendantArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListDescendantSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_list_descendant_execute(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &SimulateSecurityHealthAnalyticsCustomModuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/customModules:simulate",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()` or `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SimulateSecurityHealthAnalyticsCustomModuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_custom_modules_simulate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SimulateSecurityHealthAnalyticsCustomModuleRequest,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/customModules:simulate
/// Simulates a given SecurityHealthAnalyticsCustomModule and Resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder()` + `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_custom_modules_simulate(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsCustomModulesSimulateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SimulateSecurityHealthAnalyticsCustomModuleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_builder(
            client,
            args.parent.clone(),
            &args.body,
        )?;
    securitycenter_projects_security_health_analytics_settings_custom_modules_simulate_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()` to send, or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list` for simplest API.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://securitycenter.googleapis.com/v1/projects/{}/securityHealthAnalyticsSettings/effectiveCustomModules",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()` or `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveSecurityHealthAnalyticsCustomModulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task()`.
/// For the simplest API, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/securityHealthAnalyticsSettings/effectiveCustomModules
/// Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This includes resident modules defined at the scope of the parent, and inherited modules, inherited from CRM ancestors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder()` + `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute()`.
/// For task-level control, use `securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSecurityHealthAnalyticsSettingsEffectiveCustomModulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<ListEffectiveSecurityHealthAnalyticsCustomModulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_builder(client, args.parent.clone(), args.pageSize.clone(), args.pageToken.clone())?;
    securitycenter_projects_security_health_analytics_settings_effective_custom_modules_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `securitycenter_projects_sources_list_execute()` to send, or `securitycenter_projects_sources_list` for simplest API.

pub fn securitycenter_projects_sources_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://securitycenter.googleapis.com/v1/projects/{}/sources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `securitycenter_projects_sources_list_execute()` or `securitycenter_projects_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `securitycenter_projects_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `securitycenter_projects_sources_list_task()`.
/// For the simplest API, use `securitycenter_projects_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `securitycenter_projects_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn securitycenter_projects_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = securitycenter_projects_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`securitycenter_projects_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritycenterProjectsSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/sources
/// Lists all sources belonging to an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `securitycenter_projects_sources_list_builder()` + `securitycenter_projects_sources_list_execute()`.
/// For task-level control, use `securitycenter_projects_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn securitycenter_projects_sources_list(
    client: &SimpleHttpClient,
    args: &SecuritycenterProjectsSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = securitycenter_projects_sources_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    securitycenter_projects_sources_list_execute(builder)
}
